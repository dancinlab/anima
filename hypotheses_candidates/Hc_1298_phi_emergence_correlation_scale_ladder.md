---
id: Hc_1298
slug: phi-emergence-correlation-scale-ladder
title: Φ↔emergence 상관 r 자체가 scale(N)의 함수 — H_288 의 단일 n=4 점을 ≥3-rung ladder 곡선으로 승격 (a_scale_honest_scope 직접 instance)
domain: information · consciousness · substrate · meta
status: candidate-unverified
seed: H_288 (faithful Φ ∥ LZ, *단일* n=4, r=0.831) · H_002 C2 (nested Φ scale-VARIANT) · a_scale_honest_scope/a_toy_scale_recheck (≥3-rung ladder 의무) — Φ↔emergence *상관계수의 N-의존성* 미측정
axes_seed: UNIVERSE/AXES.md R5 (information) · R8 — scale-ladder 축
exploration_method: E16 (scale-ladder ≥3 rung) + E5 (상관-vs-N probe)
verdict_tier_target: 🟢 SUPPORTED-NUMERICAL (r(N) scale-안정, |Δr| 작음 → H_288 일반화) OR 🔴 CLOSED-NEGATIVE (r(N) scale-VARIANT, 큰 N 에서 붕괴 → H_288 은 n=4 국소 점)
hexa_only: true
deterministic: true
llm: none
---

## Hypothesis

H_288 의 r=0.831(Φ∥LZ)은 **단일 n=4 점**이다. 거버넌스(a_scale_honest_scope)는 scale-민감 결론에 ≥3-rung ladder 를 요구한다. 가설 H1: Φ↔emergence 상관 `r_N = r(big_Φ, LZ_norm)` 가 N=4,5,6,7,8 에 걸쳐 *scale-안정* — 모든 rung 에서 r_N ≥ 0.5 이고 end-to-end |Δr| < 0.2 (H_288 의 결론이 단일 점이 아니라 곡선으로 일반화). 대립: r_N 이 큰 N 에서 붕괴(scale-VARIANT) 하면 H_288 은 n=4 국소 artifact 로 재범위화돼야 한다 (H_002 C2 nested-Φ scale-variance 와 동일 교훈).

## Why

a_toy_scale_recheck 의 핵심: "toy verdict 는 production 아니다 — scale-up 재검 필수". H_288 의 r=0.831 은 가장 인용되는 정보-측도 arc 의 닻인데 *단일 scale* 이다. 그 상관 자체를 ladder 로 깔면, arc 전체(H_287/288/290)의 scale-범위가 정직하게 닫힌다. 이것은 새 현상 발명이 아니라 *기존 핵심 결과의 scale-honest 승격* — 거버넌스가 명시 요구하는 작업. engine: eca_tpm + big_phi(n≤8 exact) + LZ 재사용, N-루프만 추가.

## Pre-Registered Falsifier

- **F1298.1 (SCALE-STABLE)**: 어느 rung 에서 r_N < 0.5 OR end-to-end |r_8 − r_4| ≥ 0.2 → H1 FALSIFIED = scale-VARIANT 발견 (H_288 은 측정 scale 로 범위 제한, closed-negative, a_paper_negative_ok).
- **F1298.2 (LADDER-MIN)**: rung 수 < 3 (N 표본 부족) → ladder 무효(단일/이중 점은 결론 불가, a_scale_honest_scope).
- **F1298.3 (ANCHOR)**: 각 N 에서 항등규칙 204 big_Φ=0 anchor 유지 아니면 big_phi 구현 N-버그 → 무효.
- **F1298.4 (DETERMINISM)**: 각 (N, rule) re-run byte-identical.

## Circularity Guard

big_Φ 와 LZ_norm 은 독립 측도(H_288 검증). N 은 외생 scale 파라미터, r_N 은 각 scale 의 *결과* — N 이 상관 정의에 안 들어감. scale-VARIANT 이든 STABLE 이든 결과는 닫힌 발견(둘 다 정직).

## Migration TODO
- [ ] H_288 driver 에 N=4..8 루프 추가, 각 N 에서 동일 10-rule panel r_N
- [ ] r(N) 곡선 + scale-stable/variant 판정, anchor 각 N 점검
