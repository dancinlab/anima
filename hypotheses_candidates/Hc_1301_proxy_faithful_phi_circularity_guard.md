---
id: Hc_1301
slug: proxy-faithful-phi-circularity-guard
title: The capacity↔representation gap and the phi_proxy↔faithful-Φ gap are NOT the same computation — circularity guard for the G1 analogy
domain: consciousness, integration, methodology
status: candidate-unverified
source_doc: Lane A weak-lift closed-negative; G1 structural-signature analogy
seed: G1 — if the capacity↔representation analogy to phi_proxy↔faithful-Φ is to be a FINDING (not an identity), the two metrics must NOT collapse to one computation up to monotone reparametrization.
promoted_at: 2026-06-02
linked_h: H_278 (faithful Φ★ MIP vs phi_spatial proxy on identical n=8 lattice), Hc_1300 (capacity-without-integration law)
verdict_tier_target: 🟠 OPEN-BLOCKED candidate (circularity guard) → 🟢/🔴 on H_278 frozen rank-divergence
notes: "circularity guard per task brief — if two axes collapse to one computation the analogy is OPEN-BLOCKED, not GREEN."
---

## Hypothesis

The G1 claim (Lane A capacity↔representation gap MIRRORS the phi_proxy↔faithful-Φ gap)
is only a genuine finding if proxy-Φ and faithful-Φ are NOT reparametrizations of each
other. phi_spatial proxy = max(total_MI − min_partition_MI,0)/(n−1); faithful-Φ =
min-over-all-partitions cross-cut-MI / min(|A|,|B|). Both consume the SAME RFC-036
pairwise-MI primitive — so they could collapse.

CLAIM: they do NOT collapse — there exists at least one substrate where proxy-Φ and
faithful-Φ RANK-DISAGREE or where their ratio is non-monotone, proving the gap is a
real second axis and not a monotone relabeling.

## PRE-REGISTERED Falsifier

- **F-1301-MONOTONE**: on the H_278 frozen 6-scale ledger, if proxy-Φ and faithful-Φ
  are strictly rank-CONCORDANT across all 6 scales AND faithful/proxy ratio is constant
  (CV of ratio < 5%), the two are a monotone reparametrization → the G1 analogy is
  CIRCULAR → verdict OPEN-BLOCKED (NOT a finding).
  PASS (non-circular) = rank-disagreement on ≥1 scale OR ratio CV ≥ 5%.

## Honest Limits

- **L-1301-FROZEN**: tested on the H_278 frozen result.json (deterministic prior $0 run;
  the runner imports an absolute path to another tree so live re-run is blocked — the
  frozen ledger IS the canonical measurement, values used verbatim).
- **L-1301-N8**: 6 scales × n=8 only. A rank-disagreement on this small set is sufficient
  to defeat circularity but NOT to characterize the full gap shape.

## Cross-Links

- **sibling Hc**: Hc_1300, Hc_1302
- **method-bridge**: H_278 per_scale_ledger (phi_faithful vs phi_proxy columns)
