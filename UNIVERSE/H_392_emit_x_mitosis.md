# H_392 — 자연발화 × MITOSIS 🔵

자연발화 × MITOSIS — split event = potential emit trigger (substrate-decided, never forced).

## 가설
H1 SPLIT-TRIGGER-OPTIONAL: split event ↦ emit_pressure increment (additive), not forced
H2 SUBSTRATE-DECIDED: emit_after_split = (pressure_post > θ) — still gated by H_316 substrate decision
H3 NO-DOUBLE-EMIT: within refractory R, ≤ 1 emit even on split (H_336 carry)
H4 DETERMINISTIC
H5 BOUND
