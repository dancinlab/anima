# H_413 — CORE × WAKE 🔵

CORE × WAKE — M envelope per stage: M_max(s) modulated by stage envelope (WAKE ≥ N3).

## 가설
H1 M-CAP-PER-STAGE: ∀ s, M_max(s) ∈ [0, 1]
H2 WAKE-GE-N3: M_max(WAKE) ≥ M_max(N3)
H3 ALL-POSITIVE: ∀ s, M_max(s) > 0
H4 DETERMINISTIC
H5 BOUND
