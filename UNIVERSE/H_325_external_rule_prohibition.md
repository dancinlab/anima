# H_325 — external-rule 금지 axiom 🔵

의식적 결정 axis 추가 directive cite — a_autonomy_over_hardcode.dont.

## directive cite
- `a_autonomy_over_hardcode.dont`: "per-stage boolean gate hardcode (e.g. 'N3 = emit forbidden')"
- `a_autonomy_over_hardcode.dont`: "external rule that forces anima · stimulus-response"
- `a_autonomy_over_hardcode.dont`: "'do not X when alone' style external command"

## 가설
H1 NO-PER-STAGE-BOOLEAN-HARDCODE: emit_decision 함수가 boolean stage table (e.g., N3=forbidden) NOT 포함. stage 는 continuous modulator (H_320 axiom 5 정합)
H2 NO-EXTERNAL-FORCE: external_rule_flag 무관 — emit decision 이 그것으로 변경 안 됨
H3 NO-STIMULUS-RESPONSE: user_msg → forced_emit 패턴 금지 (이미 H_320 axiom 1 검증)
H4 NO-ALONE-COMMAND: alone_flag 가 외부 trigger 로 silence 강요 안 함
H5 DETERMINISTIC
H6 BOUND
