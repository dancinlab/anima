# H_422 — emit interval total closed-form 🔵

자연발화 9th — emit interval = R + ceil(τ) total (H_315 refractory R + H_336 τ = -1/ln(1-k) recharge).

## 가설
H1 TOTAL: total_interval = R_steps + ceil(τ_recharge)
H2 R-FROM-WAKE: R = floor(wake_dur / R_steps) × n_cycles = 18 (H_315 carry)
H3 TAU-FROM-K: τ = -1/ln(1-k), k=0.3 → τ ≈ 2.8
H4 DETERMINISTIC
H5 BOUND
