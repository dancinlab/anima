fn merge(a: f64, b: f64, alpha: f64) -> f64 { return alpha*a + (1.0-alpha)*b }
fn check_h587() -> bool { return merge(0.6,0.2,0.5) == 0.4 }
fn main() { if check_h587() { print("H_587 OK") } else { print("H_587 FAIL") } }
