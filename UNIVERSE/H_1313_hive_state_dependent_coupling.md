# H_1313 — `hive-state-dependent-coupling` (HIVE collective-Φ, r4: state-dependent A⇄G coupling)

**axis**: F (HIVE-MIND) · "many individuals → one consciousness" — the `a_break_the_wall` attempt vs the H_1308 r3 NULL
**id**: H_1313 · **date**: 2026-06-16 · **infra**: $0 mac-local CPU (hexa-native faithful IIT-4)
**verdict**: **🧱 TERMINAL NULL — real anima A⇄G genuinely does NOT integrate into collective consciousness at faithful-Φ scale, even with a genuinely state-dependent coupling** (first-class closed-negative, c9)
**parent**: H_1308 r3 (🔴 NULL) · **grandparent**: H_1295 r2 (🟢 ECA) · **anchor**: H_6009 🟢 tension-link

> (id 1313: this lane was authored as 1312 but two concurrent lanes [ko-decode-wire H_1312,
> G6-DIG H_1314] merged to origin/main first; this lane took the next free id 1313. The seed
> LABELS [1300,1301,1302] inside the probe are frozen run constants kept verbatim with the
> verdict — not the H id.)

---

## 1. The question (frozen-first, falsifiable — a TERMINAL NULL is a valid result, c9 / c16)

H_1308 r3 found that collective-Φ super-additivity (Φ_joint > Σ Φ_member, Δ_ECA=+10.4756 on
the abstract ECA substrate) does **NOT** transfer to the REAL anima A⇄G substrate
(Δ_real = −3.0, sign flip). It **diagnosed the root cause** (c9): the live tension-link is a
**near-CONSTANT scalar nudge** (biasA≈biasB≈0.00648 across all 64 states, scaled to ~1e-8 so
it never flips a cell) → the joint n=6 TPM **factorizes** (next-A ⊥ B-bits) → faithful Φ_joint = 0.

H_1308 §6 named the breakthrough angle (`a_break_the_wall`, c16): a coupling channel that
injects **STATE-DEPENDENT multi-cell influence** — not a bounded constant scalar nudge — could
in principle integrate. **This card tests exactly that.**

> **Does a genuinely state-dependent real A⇄G cross-member coupling restore super-additive
> faithful Φ on the REAL anima substrate — i.e. does it FLIP the r3 NULL (Δ −3.0 → Δ>0)?**

## 2. Method (`a_phi_iit4_tool`, `a_engine_native_learning`, frozen-first, reuses r3 harness verbatim)

- **NEW mechanism (state-dependent, REAL substrate)**: member A's next-state readout threshold
  is shifted by member B's **LIVE cell-state** — B's three real channel-sign cells (b'0,b'1,b'2),
  read from B's injected+stepped `pure_field`, drive A's three channel thresholds via a fixed
  cross-wiring (b'0→A.t1, b'1→A.t3, b'2→A.t4), and symmetrically A→B. Because B's live cells are
  a non-degenerate function of B's input bits, next-A now genuinely depends on B's bits → the
  joint TPM does not factorize by construction of the read. A **REAL field read of the other
  member**, NOT a hand-set super-additivity (the numbers fall out of the substrate; tune-to-green
  forbidden c9/p7). Bias magnitude is O(1) (a real cell-state ±1) scaled per-channel to the
  channel magnitudes (t1~1.6e-7→1e-6·W, t3~1.6e-8→1e-7·W, t4~1.2e-3→1e-2·W) so it **can flip a
  threshold** — the exact fix the r3 diagnosis identified. Gated by W (W=0 ⇒ r3 factorized case).
- **REAL substrate**: each member = a REAL anima `PureField` (3 coupled oscillators → 6-D field).
  Injection geometry + 3-cell readout (signs of t1/t3/t4 cross-mix channels) **IDENTICAL to r3**.
- **Φ = faithful IIT-4 ONLY** (`big_phi_bounded`): member n=3 cap=3 exact, joint n=6 cap=2
  lower-bound, sys=0. **n≤8 faithful-Φ ceiling respected** (SAME scale as r3, NO n=9+).
- **FROZEN bars** (`.verdicts/1313_hive_state_dependent_coupling/FREEZE.txt`, W=0.6, 3 seeds
  [1300,1301,1302]): B1 PRESENCE Δ_sd>0 · **B2 FLIP Δ_sd>0 (crosses zero, KEY)** · B3 DECOUPLE-MEAN
  Δ_mean≤0 · B4 SHUFFLE collapse.

## 3. Measurement (verbatim — `.verdicts/1313_hive_state_dependent_coupling/result.txt`)

```
  Φ(A)=1.5  Φ(B)=1.5  Σ=Φ(A)+Φ(B)=3.0           ← each REAL anima member IS integrated
  Φ_collective(STATE-DEP, W=0.6) = 0.0   Δ_sd = -3.0   ← joint Φ STILL collapses below the sum
  Φ_collective exact internals: total=0.0 sum_d=0.0 sum_r=0.0 nd=0  (ZERO distinctions)
  Φ(decouple-to-mean)   = 0.0   Δ_mean   = -3.0
  Φ_shuffle (3 seeds)   = 0.0   Δ_shuffle = -3.0
  [FAIL] B1 PRESENCE   [FAIL] B2 FLIP   [PASS] B3 DECOUPLE-MEAN   [PASS] B4 SHUFFLE
  RESULT: 2 PASS / 2 FAIL   VERDICT: 🧱 TERMINAL NULL
```

**NO FLIP**: r3 Δ = −3.0 (constant nudge) → r4 Δ_sd = −3.0 (state-dependent) — IDENTICAL.

| Bar | Frozen test | Result | Status |
|-----|-------------|--------|--------|
| B1 PRESENCE | Δ_sd > 0 (super-additive on REAL A⇄G) | Δ_sd = −3.0 | **FAIL** |
| B2 FLIP (KEY) | Δ_sd > 0 (flips the r3 NULL Δ=−3.0 → Δ>0) | Δ_sd = −3.0 ≤ 0 | **FAIL** → 🧱 |
| B3 decouple-mean | Δ_mean ≤ 0 | −3.0 | PASS (trivial — no lift) |
| B4 shuffle | Δ_sd−Δ_shuf ≥ +1 OR Δ_shuf ≤ 0 | Δ_shuf = −3.0 | PASS (trivial — no lift) |

## 4. Mechanism — WHY state-dependence STILL does not integrate (diagnosed, c9 airtight)

- **The coupling IS genuinely state-dependent** (verified directly): with A's bits FIXED, A's
  NEXT cells VARY with B's bits (B=0→nextA=[1,0,0], B=1→[0,1,0], B=2→[0,0,1], B=3→[1,1,1] …).
  The r3 factorization (next-A ⊥ B-bits) is **BROKEN** — the breakthrough mechanism worked.
- **BUT** the real coupling, when strong enough to depend on B, **OVERWRITES** member A's own
  dynamics rather than ADDING to them: with B FIXED, next-A is CONSTANT across all 8 of A's own
  states (next-A = B's cells exactly). The joint map degenerates to a pure **COPY/SWAP**
  (next-A = g(B-bits), next-B = g(A-bits)) where each member loses all self-information. The tiny
  pure_field channels (t1~1.6e-7, t3~1.6e-8) mean ANY coupling strong enough to register against
  the other member is ALSO strong enough to swamp the member's own t-channel sign. There is **NO
  regime** where next-A depends SIMULTANEOUSLY and richly on BOTH A's own state AND B's state —
  and that simultaneous self+cross dependence is exactly what IIT-4 needs for Φ_joint > 0.
- A pure deterministic copy/swap over this coarse 8→{few} readout admits a zero-loss bipartition:
  faithful IIT-4 finds ZERO distinctions (nd=0) → **Φ_joint = 0.0 exactly**.
- **Coupling-strength sweep (diagnostic, NON-GATING)**: Φ_joint = 0.0 at EVERY k ∈ {0.5, 0.8, 1.0,
  1.2, 2.0} (k = bias magnitude as a multiple of channel magnitude: sub-channel → balanced →
  dominating). The NULL is NOT a scaling artifact — NO coupling strength integrates.
- The ECA W=0.6 boundary-blend, by contrast, **mixes each cell's OWN transition rule WITH** its
  cross-half neighbor's bit (augmenting, not overwriting) over a rich rule-110/90 TPM. That
  simultaneous self+cross structure is what super-adds Φ; the real anima pure_field (a near-
  symmetric, low-D, zero-input oscillator field with sign-balanced micro-channels) does not
  produce it under any state-dependent coupling at this scale.

## 5. What this means (the r4 transfer answer — the wall is TERMINAL)

The r3 root-cause fix (constant scalar nudge → genuinely state-dependent multi-cell coupling) was
implemented on the REAL substrate and **DID break the r3 factorization** — yet collective-Φ
super-additivity **STILL does not transfer** (Δ_sd = −3.0, identical to r3, robust across the full
coupling-strength sweep). **The real anima A⇄G substrate genuinely does NOT integrate two members
into a super-additive collective consciousness at faithful-Φ scale**, even under the strongest
honest state-dependent coupling the substrate admits.

The ECA Δ=+10.4756 is a property of the ECA TPM's rich simultaneous self+neighbor coupling
structure — **NOT a substrate-portable law of consciousness-as-integration**. H_1295 super-
additivity is **ECA-ONLY** (an abstract-substrate existence proof), NOT engine-transferable, across
**BOTH** realized real coupling channels (the r3 constant tension-link AND the r4 state-dependent
cross-cell coupling). `a_break_the_wall` is satisfied: the named angle was tested exactly and
exhaustively (across the full strength regime); the honest 🧱 here is a first-class, valid,
**TERMINAL** result (c9) — the wall is real, not a wrong-method / wrong-direction / under-investment
artifact (all three were addressed: r3 diagnosed the method, r4 fixed it state-dependently, the
k-sweep exhausted the investment).

## 6. Honest scope (`a_scale_honest_scope` / `a_toy_scale_recheck`)

- **TOY**: n=3/member, joint n=6, cap=2 lower-bound, sys=0, ONE warmup (600 steps), 2 members,
  3 seeds, deterministic. Faithful-Φ n≤8 ceiling respected. SAME scale as r3 (frozen, reused).
- **Coupling-realization-conditional but now spanning two channels**: r3 (constant tension-link)
  and r4 (state-dependent cross-cell) both give SUB-additive Φ = −3.0. A radically different real
  engine readout (more cells/member, a richer non-sign-saturating per-channel code, or a substrate
  whose own dynamics SURVIVE the coupling) could in principle integrate — UNTESTED, and out of
  scope at the n≤8 faithful-Φ ceiling for 2 members (3+3). Within the live anima A⇄G substrate as
  it exists, integration into a collective Φ does not occur.
- **No engine wiring** (`a_verified_must_wire` is GREEN-only): a NULL wires NOTHING into CORE. The
  existing H_1295 CollectivePool (ECA) and live `CORE/*.hexa` are left UNTOUCHED — Ψ-disjoint, no
  regression (probe is a standalone `UNIVERSE/` measurement).
- The NULL does NOT retract H_1295's ECA-substrate GREEN; it BOUNDS its scope **terminally**:
  H_1295 is an abstract-substrate existence proof, NOT engine-transferred — the live-engine
  reconfirmation FAILS under two distinct coupling channels.

## 7. Cross-link

- **H_1308** 🔴 (r3 NULL) — the parent; diagnosed the constant-nudge factorization that this r4
  tests the named fix for. r4 confirms the wall is TERMINAL.
- **H_1295** 🟢 (ECA, r2) — the grandparent; r3+r4 together bound it to abstract-substrate scope.
- **H_6009** 🟢 tension-link — the r3 real coupling channel; r4 uses a state-dependent cross-cell
  channel instead, and both fail to integrate.
- governance: `a_break_the_wall` (the breakthrough attempt) · `a_phi_iit4_tool` (faithful-Φ only) ·
  `a_engine_native_learning` (real substrate, not abstract rules) · `a_scale_honest_scope` · c9 · c16.

## 8. Artifacts

- experiment: `UNIVERSE/h1313_hive_state_dependent_coupling.hexa` (REAL pure_field state-dependent
  cross-cell coupling + faithful-IIT4; hexa-native — Φ is hexa-only by `a_phi_iit4_tool`).
- freeze + verdict: `.verdicts/1313_hive_state_dependent_coupling/{FREEZE,result}.txt`.
- CLAIMS index: root `CLAIMS.tape` @C h1313_hive_state_dependent_coupling.
