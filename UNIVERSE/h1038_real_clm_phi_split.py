"""H_1038 — does the planning faithful-UP / big-Phi-DOWN sign-split appear on a REAL
trained ConvMoE .clm, or was it a toy-TPM artifact?  (skeleton — filled in next commit)

Production-direction closure of the Phi measure-dependence arc H_1004 -> H_1037, which was
measured ONLY on hand-built toy TPM substrates. This rung replaces the toy
`planning_trajectories` with REAL CLM rollouts on the golden trained ConvMoE
reexport_d768_v2_fast.clm (CPU-decoded via state/mid_convmoe_fire/clm_decode_mirror.py).
"""
# (skeleton placeholder; full harness in the next commit)
