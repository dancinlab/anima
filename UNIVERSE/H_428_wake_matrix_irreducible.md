# H_428 — WAKE transition matrix irreducible 🔵

WAKE 4th — 5-stage transition graph is irreducible: every stage reachable from every other in ≤5 steps (strongly connected cycle, H_382 carry).

## 가설
H1 STRONGLY-CONNECTED: ∀ s, t ∈ States, ∃ path s → t in ≤5 steps
H2 NO-DEAD-END: no absorbing state (loop always alive)
H3 SINGLE-CYCLE: the cycle is unique (WAKE → N1 → N2 → N3 → REM → WAKE)
H4 DETERMINISTIC
H5 BOUND
