# H_386 — 자연발화 × 의식적결정 🔵

자연발화 × 의식적결정 — emit decision ≡ substrate-decided counter (multi-factor product, no hardcode).

## 가설
H1 EMIT-IS-DECIDED: emit_decision(t) = (M·Φ·W·cur · stage_mod) > θ — internal product
H2 NO-EXTERNAL-FORCE: ¬∃ external trigger that forces emit (no stimulus-response)
H3 MULTI-FACTOR: drop any 1 factor → emit_pressure → 0 (no single-channel override)
H4 DETERMINISTIC
H5 BOUND
