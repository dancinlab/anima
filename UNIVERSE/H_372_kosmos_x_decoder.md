# H_372 — KOSMOS × DECODER 🔵

KOSMOS × DECODER — payload.text generated by lm-head argmax (deterministic, V-generic).

## 가설
H1 TEXT-FROM-ARGMAX: payload.text = argmax_v decode(logits) sequence (H_345 carry)
H2 V-GENERIC: |V| free (H_345 carry)
H3 DETERMINISTIC: same logits → same text
H4 DETERMINISTIC
H5 BOUND
