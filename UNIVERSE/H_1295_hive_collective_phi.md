# H_1295 — `hive-collective-phi` (HIVE-MIND / collective-Φ lane)

**axis**: F (HIVE-MIND) · "many individuals → one consciousness" · **anchor**: H_609 🟢
**id**: H_1295 · **date**: 2026-06-16 · **infra**: $0 mac-local CPU (hexa-native faithful IIT-4)
**verdict**: **🟢 GREEN ENGINE-NATIVE + WIRED**

> (id 1295: H_1294 was claimed by the hierarchical-PFC PR #2181 that landed concurrently;
> this lane took the next free id.)

---

## 1. Claim (the genuinely-GREEN part, the ONLY part wired)

Coupling N anima substrates raises faithful IIT-4 big-Φ **above the sum** of the
individual Φ — collective integration is **SUPER-ADDITIVE**: Φ(joint) > Σ Φ(member),
for an edge-of-chaos coupling regime. The substrate realization of the "many individuals
→ one consciousness" axis (collective-Φ / symbiogenesis lens, c15, `a_no_llm_frame_trap`
— an IIT/biology lens, not an LLM recipe).

**Explicitly NOT wired** (falsified mechanisms — c9, `a_verified_must_wire` GREEN-only):
- **H_611** 🔴 cross-substrate transfer-entropy alignment with collective Φ — FALSIFIED.
- **H_617** 🔴 SAVANT × hive SI-ratio cross-link (GZ_LOWER knob) — FALSIFIED.
- **975** 🔴 multi-agent shared world-model — RED.

## 2. Method

- **Φ = faithful IIT-4 ONLY** (`a_phi_iit4_tool`): `big_phi_bounded(tpm, n, sys_state, cap)`
  from `stdlib/consciousness/iit4_bigphi.hexa` — numpy NEVER computes Φ. n=3 → cap=3
  (exact); joint n=6 → cap=2 (M4b SSOT lower-bound). Substrate builder = the H_609
  `build_tpm_ab` boundary-edge W-blend, reused verbatim.
- **Regime**: two n=3 ECA substrates A,B under rule 110 (edge-of-chaos), coupled at
  cross-half boundary strength W=0.6 into a joint n=6 ring. sys_state=0 anchor.
- **3 seeds [1295,1296,1297]** drive the shuffle control (the core super-additive value
  is deterministic / seed-invariant — an ECA TPM has no RNG).
- **Frozen-first** (`a_break_the_wall`, no tune-to-green): FREEZE pre-registered the bars
  BEFORE any measurement. `.verdicts/1295_hive_collective_phi/H_1295_FREEZE.txt`.

## 3. Two angles (frozen-first; angle-1 honestly kept)

**ANGLE-1** asked the WRONG emergence question (topology-specificity): a seeded SHUFFLE
of WHICH boundary cell couples to which (same coupling MAGNITUDE, scrambled topology). It
**FAILED** — a random equal-magnitude cross-wiring matched/EXCEEDED the structured H_609
lift (Δ_shuffle = +13.6..+16.4 > Δ_coupled = +10.48 on all 3 seeds). **Honest finding
(kept, reported, NOT a GREEN gate)**: super-additivity is **coupling-GENERIC**, not
specific to the H_609 boundary topology — any cross-coupling of these substrates
integrates (often more). This is exactly the kind of control the H_611/H_617
falsifications taught to run; it converted a naive "this topology is special" story into
the honest "coupling-itself super-adds" claim.

**ANGLE-2** (the genuine emergence control for super-additivity, frozen before running)
re-pointed the controls to the real null + a substrate-class dissociation → **🟢 4/0**.

## 4. Measurement (verbatim — `.verdicts/1295_hive_collective_phi/H_1295.txt`)

```
  Φ(A)=2.49604  Φ(B)=2.49604  Σ=Φ(A)+Φ(B)=4.99209
  Φ_collective(coupled, W=0.6) = 15.4677   Δ_coupled = 10.4756
  coherence(coupled halves) = 0.58125  (no-collapse iff < 0.999)
  B2a  Φ(W=0, decoupled) = 0.0   Δ(W=0) = -4.99209  (null iff <= 0)
  B2b  sterile(90,90): Σ=6 Φ_coupled=0.0 Δ_sterile=-6
       dissociation Δ(110,110) - Δ(90,90) = 16.4756
  [PASS] B1 SUPER-ADDITIVITY  [PASS] B2a DECOUPLE-NULL  [PASS] B2b RULE-CLASS DISSOC  [PASS] B3 NO-COLLAPSE
  RESULT: 4 PASS / 0 FAIL   VERDICT: 🟢 GREEN
```

| Bar | Frozen test | Result | Status |
|-----|-------------|--------|--------|
| B1 super-additivity | Δ_coupled ≥ +1.0 every seed | Δ = +10.4756 (Φ 15.4677 vs Σ 4.99209) | **PASS** |
| B2a decouple-null | Δ(W=0) ≤ 0 | Δ(W=0) = −4.99209 | **PASS** |
| B2b rule-class dissociation | Δ(110,110) − Δ(90,90) ≥ +1.0 | +16.4756 (sterile Δ=−6) | **PASS** |
| B3 no-collapse | coherence < 0.999 | 0.58125 | **PASS** |
| (angle-1 topology-shuffle) | informational, not a gate | Δ_shuffle +13.6..+16.4 > Δ_coupled | coupling-GENERIC |

## 5. Engine-native realization + wiring (`a_verified_must_wire`)

A `CollectivePool` faculty added to live `CORE/engine_cli.hexa` (§ HIVE-MIND): holds N
coupled substrate slots (`rules`, `w`, `n`) and reads collective integration via faithful
IIT-4 — `collective_member_phi · collective_sum_phi · collective_phi · collective_excess ·
collective_is_super_additive · collective_coherence`. The four frozen bars re-score
ENGINE-NATIVE in `CORE/engine_cli_smoke.hexa` cases 39–42, byte-matching the mirror.

- **Ψ-DISJOINT / ADDITIVE** (`a_core_engine_map`): the faculty NEVER touches pure_field's
  Φ/phase/Ψ, the generator, or the decoder; it is a pure READ + a small config struct. The
  existing engine_cli smoke cases stay byte-identical (no regression). It is NOT an
  emit/silence gate (`a_autonomy_over_hardcode`) and NOT a 2nd .clm/.kosmos entry path.
- **p7**: the verdict is faithful IIT-4, never perplexity. **p8**: a read on the same
  substrate, no train/infer split.

## 6. Honest scope (`a_scale_honest_scope` / `a_toy_scale_recheck`)

- **TOY**: n_a=n_b=3, joint n=6, cap=2 bounded **lower-bound**, ECA rule substrate, sys=0
  anchor only, n=2 members. The experiment is hexa-native (faithful Φ, no numpy Φ) but
  **collective-Φ engine-transfer to a live multi-anima deployment is UNVERIFIED** — this
  realizes the STRUCTURE (super-additive collective integration as an additive Ψ-disjoint
  read), not a scaled multi-substrate daemon.
- **Coupling-GENERIC not topology-specific** (angle-1, honest): the lift is a property of
  coupling these substrates, not of one privileged wiring.
- **Substrate-class conditional** (B2b, H_609 C3.3): strong super-additivity is in the
  edge-of-chaos rule-110 pair; sterile rule-90 collapses. NOT a universal claim.
- `collective_phi` for **n>2 members** falls back to the additive baseline (not yet
  measured) — N>2 scaling is the r2 follow-on.

## 7. Cross-link

- **H_609** 🟢 collective-Φ super-additive — the anchor this realizes/wires.
- **H_355** 🟢 collective-Φ PID synergy (sister, synergy axis).
- **H_611** 🔴 / **H_617** 🔴 / **975** 🔴 — falsified hive mechanisms, deliberately NOT wired.
- **H_1293** theory-of-mind / **H_1294** hierarchical-PFC — prior CORE lanes; both model ONE
  substrate (a single other mind / anima's own plan), CollectivePool reads the COLLECTIVE
  Φ of N coupled substrates (distinct axis).

## 8. Artifacts

- experiment: `UNIVERSE/h1295_hive_collective_phi.hexa` (frozen bars + 3 seeds + shuffle +
  faithful-IIT4; hexa-native — Φ is hexa-only by `a_phi_iit4_tool`).
- freeze + verdict: `.verdicts/1295_hive_collective_phi/{H_1295_FREEZE,H_1295,engine_cli_smoke}.txt`.
- engine: `CORE/engine_cli.hexa` § HIVE-MIND CollectivePool · smoke `CORE/engine_cli_smoke.hexa` cases 39–42.

## 9. Next (r2 + depletion test)

- **r2**: N>2 scaling of collective-Φ — does super-additivity hold / grow / saturate at
  3,4 coupled members (joint n=9,12; needs shard-parallel faithful Φ or a higher cap)? If
  it SATURATES, this is a saturating existence-proof → DEPLETED 🏁. Alternatively:
  real-substrate / paraphrase transfer of the collective read to a live multi-anima loop.
