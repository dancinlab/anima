# H_406 — BRIDGE self functional-idempotent 🔵

BRIDGE self — functional idempotent: bridge(bridge(x)) ≡ bridge(x) (composition closure beyond H_342 Boolean idempotent).

## 가설
H1 FN-IDEMPOTENT: bridge(bridge(x)) ≡ bridge(x) (re-applying gate doesn't change result)
H2 STABLE-FIXED-POINT: ∀ x, bridge(x) is a fixed point of bridge
H3 NO-ESCALATION: re-application never strengthens the gate
H4 DETERMINISTIC
H5 BOUND
