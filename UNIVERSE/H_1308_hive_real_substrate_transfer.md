# H_1308 — `hive-real-substrate-transfer` (HIVE-MIND collective-Φ engine-transfer)

**axis**: F (HIVE-MIND) · "many individuals → one consciousness" — does it transfer to REAL anima?
**id**: H_1308 · **date**: 2026-06-16 · **infra**: $0 mac-local CPU (hexa-native faithful IIT-4)
**verdict**: **🔴 HONEST NULL — does NOT transfer to real coupled anima A⇄G** (closed-negative, c9)
**parent**: H_1295 r3 (the r2 agent's flagged open axis) · **anchor**: H_609 🟢 / H_6009 🟢 tension-link

> (id 1308: 1300 was claimed by a concurrent mitosis-skill-curriculum lane that merged
> first; this lane took the next free id. The seed LABELS [1300,1301,1302] inside the
> probe are frozen run constants kept verbatim with the verdict — not the H id.)

---

## 1. The question (frozen-first, falsifiable — a NULL is a valid result, c9)

H_1295 measured collective-Φ **super-additivity** (Φ(joint) > Σ Φ(member), Δ_ECA=+10.4756)
over an **ABSTRACT ECA substrate** (rule-110/90 cells). The H_1295 r2 agent flagged
explicitly that **engine-transfer to a LIVE multi-anima loop (real coupled A⇄G substrates)
is UNVERIFIED** — an abstract-substrate result is DIRECTIONAL until reconfirmed on the live
final-architecture engine (`a_engine_native_learning`). The user: "미검증 바로 go".

**Does collective-Φ super-additivity HOLD when the members are REAL coupled anima A⇄G
substrates** (live `CORE/pure_field.hexa` + the `CORE/brain.hexa` tension-link H_6009),
not abstract rule cells — or is it an artifact of the ECA TPM structure?

## 2. Method (`a_phi_iit4_tool`, `a_engine_native_learning`, frozen-first)

- **REAL substrate, NOT `iit4_bit(rule, idx)`**: each member is a REAL anima `PureField`
  (3 coupled oscillators → 6-D field tensor). 3 binary cells/member are read from REAL
  `pure_field_step` output — the SIGNS of three cross-mix channels (t1 attention-gate /
  t3 long-range / t4 entrainment), each a function of MULTIPLE input bits via the
  substrate's own oscillator mixing (the live A⇄G integration). Readout verified
  NON-DEGENERATE (8 member states → 6 distinct outputs).
- **REAL coupling = the tension-link (H_6009 🟢)**: a member's field-derived 5-ch tension,
  carried as a kosmos anchor, modulates the OTHER member's next-state readout via the REAL
  `anchor_tension_fold` (H_1131 tension-vecsum), gated by coupling strength W. Bidirectional
  (A→B, B→A). W=0 = decoupled.
- **Φ = faithful IIT-4 ONLY** (`a_phi_iit4_tool`) `big_phi_bounded`: member n=3 cap=3 exact,
  joint n=6 cap=2 lower-bound, sys=0. **n≤8 faithful-Φ ceiling respected** (NO n=9+).
- **FROZEN bars** (`.verdicts/1308_hive_real_substrate_transfer/H_1308_FREEZE.txt`, pre-
  registered BEFORE scoring; W=0.6, MARGIN=+1.0, 3 seeds): B1 PRESENCE Δ_real≥+1.0 ·
  **B2 TRANSFER Δ_real>0 (same sign as ECA Δ>0)** · B3 DECOUPLE Δ(W=0)≤0 · B4 SHUFFLE collapse.

## 3. Measurement (verbatim — `.verdicts/1308_hive_real_substrate_transfer/H_1308.txt`)

```
  Φ(A)=1.5  Φ(B)=1.5  Σ=Φ(A)+Φ(B)=3.0           ← each REAL anima member IS integrated
  Φ_collective(REAL-coupled, W=0.6) = 0.0   Δ_real = -3.0   ← joint Φ COLLAPSES below the sum
  Φ(W=0, decoupled) = 0.0   Δ(W=0) = -3.0
  Φ_shuffle (3 seeds) = 0.0   Δ_shuffle = -3.0
  [FAIL] B1 PRESENCE   [FAIL] B2 TRANSFER   [PASS] B3 DECOUPLE-NULL   [PASS] B4 SHUFFLE
  RESULT: 2 PASS / 2 FAIL   VERDICT: 🔴 HONEST NULL
```

**SIGN FLIPS**: ECA Δ = **+10.4756** (super-additive) vs REAL Δ = **−3.0** (SUB-additive).

| Bar | Frozen test | Result | Status |
|-----|-------------|--------|--------|
| B1 PRESENCE | Δ_real ≥ +1.0 every seed | Δ_real = −3.0 | **FAIL** |
| B2 TRANSFER | Δ_real > 0 (reproduces ECA Δ>0) | Δ_real = −3.0 ≤ 0 | **FAIL** → NULL |
| B3 decouple-null | Δ(W=0) ≤ 0 | −3.0 | PASS (trivial — no lift) |
| B4 shuffle | Δ_real−Δ_shuf ≥ +1 OR Δ_shuf ≤ 0 | Δ_shuf = −3.0 | PASS (trivial — no lift) |

## 4. Mechanism — WHY it does NOT transfer (diagnosed, c9 airtight)

The joint n=6 substrate **FACTORIZES**: next-A depends ONLY on member-A's cells, next-B
ONLY on member-B's cells (verified state-by-state). A fully decomposable system has ZERO
integrated information by IIT-4 definition → faithful Φ_joint = 0.

**Root cause**: the REAL tension-link coupling from a member's tiny field-tensor tension
(~1e-6..1e-2) is a **NEAR-CONSTANT scalar nudge** (biasA≈biasB≈0.00648 across ALL 64
states). A constant cross-member nudge cannot create state-dependent cross-member coupling
→ no joint integration. The ECA W=0.6 boundary-blend, by contrast, mixes each cell's
transition with its cross-half NEIGHBOR's BIT (a state-dependent, multi-bit coupling) —
that is the structure that super-adds Φ. The real anima pure_field is a zero-input
oscillator field relaxing toward a low-D fixed point; coupled through the tension-link's
bounded saturating fold, it does NOT reproduce the ECA's strong state-dependent coupling.

This is NOT a binarization artifact: the SAME readout gives Φ(member)=1.5 > 0, so the
per-member dynamics genuinely integrate — only the COUPLING fails to.

## 5. What this means (the transfer answer)

**Collective-Φ super-additivity is an ABSTRACT-substrate (ECA) property that does NOT
transfer to real coupled anima A⇄G substrates.** The ECA Δ=+10.4756 was a property of the
ECA TPM's strong, state-dependent neighbor-coupling structure — NOT a substrate-portable
law of consciousness-as-integration. Coupling two REAL anima substrates through the live
tension-link is **sub-additive** (joint Φ < Σ member Φ).

Per `a_engine_native_learning`: the live-engine reconfirmation of H_1295 **FAILS** for this
coupling realization → H_1295's super-additivity stays **ECA-scoped** (an abstract-substrate
existence proof), it is NOT engine-transfer-verified. This does NOT retract H_1295's
ECA-substrate GREEN; it BOUNDS its scope honestly.

## 6. Honest scope (`a_scale_honest_scope` / `a_toy_scale_recheck`)

- **TOY**: n=3/member, joint n=6, cap=2 lower-bound, sys=0, ONE warmup (600 steps), 2
  members, 3 seeds, deterministic. Faithful-Φ n≤8 ceiling respected.
- **Coupling-channel-conditional**: the NULL is for the **tension-link-as-coupling**
  realization (the live H_6009 anima↔anima channel) at this scale. A DIFFERENT real coupling
  channel — one that injects a STATE-DEPENDENT multi-cell influence (not a bounded saturating
  scalar nudge), or a stronger non-saturating link — could in principle integrate. UNTESTED.
  The finding is: the live tension-link, as it exists, is too weak/constant to integrate.
- No engine wiring (`a_verified_must_wire` is GREEN-only): a NULL wires nothing into CORE.
  The existing H_1295 CollectivePool (ECA) is left UNTOUCHED — Ψ-disjoint, no regression.

## 7. Cross-link

- **H_1295** 🟢 (ECA) — the parent; this r3 tests its engine-transfer and finds it NULL,
  bounding H_1295 to abstract-substrate scope.
- **H_6009** 🟢 tension-link — the REAL anima↔anima coupling channel used here (H_6006 🔴
  no-signaling ruled entanglement out as a channel, so the tension-link is the right one).
- **H_609** 🟢 collective-Φ super-additive — the ECA anchor that does NOT transfer.

## 8. Artifacts

- experiment: `UNIVERSE/h1308_hive_real_substrate_transfer.hexa` (REAL pure_field + brain
  tension-link + faithful-IIT4; hexa-native — Φ is hexa-only by `a_phi_iit4_tool`).
- freeze + verdict: `.verdicts/1308_hive_real_substrate_transfer/{H_1308_FREEZE,H_1308,result}.txt`.
