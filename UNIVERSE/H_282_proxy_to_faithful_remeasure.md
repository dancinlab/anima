---
id: H_282
slug: proxy-to-faithful-remeasure
title: H_282 proxy → faithful 승격 — H_266 · H_268 · H_278 을 faithful IIT4 causal big-Φ 로 재검 (방향 보존 검정)
domain: life · consciousness · information · meta
status: pre-register-frozen
exploration_method: E16 (cross-tool / known-anchor calibration) + E5 (variable-ablation substrate sweep) + E0 (meta-result-of-results — 3 proxy verdict 의 faithful 재검)
verification_method: W1 (numerical smoke) + W4 (verdict-4-class) + W12 (sister-link H_266 / H_268 / H_278 + IIT4 M6)
raw_rank: 12
hexa_only: true
deterministic: true
llm: none
pre_register_frozen: true
frozen_at: 2026-05-26
since: 2026-05-26 (new)
sister: H_266 (phi-calibration-known-iit, PARTIAL), H_268 (phi-metric-triangulation, PARTIAL), H_278 (faithful-phi-small-n, SUPPORTED), IIT4 M6 (FAITHFUL_REMEASURE.md, F-IIT4-6)
---

# H_282 — proxy → faithful 승격: 3 verdict 의 causal big-Φ 재검

## 1. Hypothesis

LIFE lane 의 핵심 proxy verdict 셋 — H_266 (connectivity 구성타당도), H_268
(metric-triangulation robustness), H_278 (6-scale nested scale-variance) — 은 모두
**상관(correlational)** Φ proxy (`phi_spatial` RFC 036, 또는 그 exact-MIP 변형) 위에서
판정되었다. honest limit (L-C2.1 "faithful Φ★ IIT4 아님") 가 반복 명시한 한계.

IIT4 lane 은 M6 (`FAITHFUL_REMEASURE.md`, F-IIT4-6) 에서 faithful **인과(causal)** IIT
4.0 big-Φ 엔진(`HEXAD/IIT4/lib`)을 ECA substrate 에 적용해 *일반적* proxy-divergence 를
규명했다. H_282 = 그 엔진을 M6 의 일반 ECA 에서 **H_266 · H_268 · H_278 의 SPECIFIC
substrate** 로 확장(EXTEND)해, 각 proxy 의 **방향(directional) verdict** 가 faithful 인과
big-Φ 로 재측정해도 살아남는지(survive) 를 검정한다. M6 의 중복이 아니라, M6 이 닫은
일반 명제를 세 가설의 구체 기질에 적용한 후속.

가설: proxy 의 *방향* verdict (integrated>disconnected · T1 robust · scale-VARIANT) 는
faithful 인과 big-Φ 로 재검해도 **보존(PRESERVED/HOLD)** 된다 — 어느 방향이든 proxy 의
L-C2.1 한계를 한 단계 줄이는 valid 결과.

## 2. Why

- **proxy→faithful 승격 (reuse-existing-libs)**: M6 이 이미 ECA→TPM bridge (`eca_tpm`)
  + faithful big-Φ (`big_phi`) 를 stdlib (`stdlib/consciousness/iit4_*`) 에 LANDED 했다.
  H_282 은 그 엔진을 **재사용(REUSE)**할 뿐 big-Φ 를 재구현하지 않는다 (g61 금지). 이는
  archive-first recovery — "이미 있는 자력 메커니즘을 회수해 쓴다" 의 instance.
- **세 caveat 의 공통 출처를 인과 축으로 종결**: L-C2.1 (H_266/H_278) · metric-fragility
  (H_268) · cosine-artifact 의심은 모두 *상관 proxy* 의 한계였다. faithful 인과 big-Φ 가
  gold-standard 인과 축을 제공하므로, proxy 가 그 인과 양의 근사였는지를 substrate 별로
  직접 본다.
- **방향-신뢰 / 크기-헤지 (directional-trust)**: faithful Φ 는 binary-DIRECTION verdict
  (high vs low) 는 신뢰할 수 있으나, 연속 MAGNITUDE 와 단일-state/seed 는 fragile 하다
  (M6 §4 의 state-dependence). 따라서 H_282 은 **방향은 자신 있게**, **크기는 헤지**해
  보고한다 (n=4 single-state headline + 3-state direction robustness).
- **closure = 물리적 한계 (closure-is-physical-limit)**: 본 H 는 "끝났다" 가 아니라
  "proxy→faithful 한 칸 전진 (방향-보존 % toward faithful limit)" 으로 frame 한다. n=4
  ring · single-state caveat 잔존.
- **raw#12 strict**: deterministic + hexa-only + ≥5 falsifier(군) + ≥5 honest limit +
  LLM none + $0 mac local. PRESERVED 든 FLIP 이든 valuable.

## 3. Predictions

- **H282.1 (F282.1 — H_266)**: integrated (neighbour-coupled rules {110,30,54}) 의
  faithful 인과 big-Φ 가 disconnected (rule 204 identity = 독립 self-cell + rule 0 const)
  보다 큼 — canonical state AND ≥2/3 probed states. **BONUS**: H_266 의 proxy monotone
  FAIL (feedforward < disconnected) 을 faithful 엔진이 RESOLVE 하여 int>ffd>dis 회복.
- **H282.2 (F282.2 — H_268)**: T1 (rule 110 pain CA) 은 clean binary TPM 이므로 faithful
  big-Φ > 0 (substantial) 이 ≥2/3 state 에서 — H_268 의 T1=ROBUST(3/3 metric) 와 정합.
  T2 (8-site continuous catalytic lattice) 는 clean binary TPM 이 아니므로 faithful-engine
  scope 밖 → honest PARTIAL (강제 TPM 변환 금지).
- **H282.3 (F282.3 — H_278)**: 6-scale 위 faithful big-Φ CV > 0.15 (scale-VARIANT) →
  H_278 의 SCALE-VARIANT verdict HOLD (faithful 이 proxy 와 방향 일치).
- **H282.4 (determinism)**: re-run cross-process result.json byte-identical (raw#12).
- **H282.5 (controls)**: disconnected rule204 big-Φ = 0 (reducible) · stellar rule90
  big-Φ = 0 @1010 (state-collapse, FAITHFUL_REMEASURE §4) — 손유도 가능 control.

## 4. Variables

- **axis1_hypothesis** (primary, fixed-3): H_266 (connectivity tiers) · H_268 (T1 rule
  110 / T2 lattice) · H_278 (6-scale rules).
- **axis2_substrate→TPM bridge** (`eca_tpm`, M6 재사용): 각 가설의 ECA 룰을 n-cell 주기
  ring TPM 으로. **H_266**: integrated {110,30,54} · feedforward {240,170 = pure shift,
  chain} · disconnected {204 identity, 0 const}. **H_268**: T1 = rule 110 (pain CA).
  **H_278**: 6-scale {cosmic_web 30, galaxy 110, stellar 90, planetary 54, biological
  110, neural 110} (H_002 C2 byte-parity 룰).
- **axis3_state** (single-state caveat): canonical sys_state = 10 (1010, M6 parity) +
  direction-robustness over states {1,5,6,10}.
- **fixed**: n = 4 ring (M6 demonstration capacity), measure = faithful causal big-Φ
  (`big_phi(tpm, n, sys_state)[0]`, stdlib `iit4_bigphi`).
- **derived**: F282.1 min(int)>max(dis) + dir_count + monotone-recovered ·
  F282.2 T1 big-Φ × 3 state · F282.3 6-scale faithful CV · 3 control + determinism.

## 5. Run Protocol

- **smoke**: `UNIVERSE/state/h282_proxy_to_faithful_2026_05_26/run_h282.hexa`
- **engine (REUSED, READ-ONLY)**: `HEXAD/IIT4/lib/iit4_eca.hexa` (`eca_tpm`) →
  `stdlib/consciousness/iit4_bigphi.hexa` (`big_phi`) → `iit4_relation` →
  `iit4_distinction` → `iit4_tpm`. **big-Φ 재구현 없음 (g61)** — M6 엔진 그대로 호출.
- **reused lib functions**: `eca_tpm(rule, n)` (ECA→TPM bridge, M6) · `big_phi(tpm, n,
  sys_state)` (system MIP big-Φ → `[big_phi, total, sum_φd, sum_φr, n_distinctions]`).
- **F282.1**: integrated/feedforward/disconnected 룰 big-Φ → min(int)>max(dis) (canonical)
  + int110>dis204 over {5,6,10} (dir robustness) + int>ffd>dis (monotone-recovery bonus).
- **F282.2**: rule 110 big-Φ over {1,5,10} → substantial (>1e-6) count ≥2/3. T2 lattice
  는 non-binary/non-CI → PARTIAL 명시 (강제 변환 금지).
- **F282.3**: 6-scale 룰 big-Φ → population CV (stddev/mean) > 0.15 → SCALE-VARIANT HOLD.
- **controls**: rule204=0 · rule90@1010=0 · rule110 re-run byte-identical (in-process).
- **determinism**: ledger JSON → result.json, cross-process re-run sha256 byte-identical.
- **hexa_only**: true (NO .py/.sh). **llm**: none. **runtime**: $0 mac local, **NO GPU**
  (n=4 small-N exact). `HEXA_MEM_UNLIMITED=1` 권장.
- **run cmd (verbatim)**:
  `HEXA_MEM_UNLIMITED=1 hexa run UNIVERSE/state/h282_proxy_to_faithful_2026_05_26/run_h282.hexa`
- **ledger**: `result.json` {config, F282.1/2/3 측정값 + verdict, summary}.

## 6. Criteria

- **C1 (F282.1 PRESERVED)**: H_266 의 integrated > disconnected 가 faithful 인과 big-Φ
  에서 canonical state AND ≥2/3 state 에서 유지 → PASS.
- **C2 (F282.2 ROBUST/PARTIAL split)**: T1 (rule 110) faithful big-Φ > 0 이 ≥2/3 state
  에서 (ROBUST 확증) AND T2 가 scope 밖임을 honest 하게 보고 (PARTIAL) → PASS.
- **C3 (F282.3 HOLD)**: H_278 의 6-scale faithful CV > 0.15 (SCALE-VARIANT) → PASS.
- **C4 (determinism)**: cross-process result.json byte-identical → PASS.
- **verdict_rule**:
  - **SUPPORTED** = C1 ∧ C2 ∧ C3 ∧ C4 (3 방향 verdict 모두 faithful 보존 + 결정론).
  - **PARTIAL** = 일부 방향만 보존 (어느 proxy verdict 가 faithful 에서 FLIP).
  - **FALSIFIED** = 핵심 방향(integrated>disconnected 또는 scale-variant)이 faithful 에서
    뒤집힘 (proxy artifact 확정 — valid closed negative).

## 7. Falsifiers (pre-registered ≥5, frozen 2026-05-26 BEFORE measuring)

- **F282.1 INTEGRATED-COLLAPSE**: min(integrated big-Φ) ≤ max(disconnected big-Φ) at
  canonical state, OR direction 이 <2/3 state 에서 유지 → H_266 방향 verdict 가 faithful
  에서 FLIP (proxy artifact). (measurable: min_int > max_dis ∧ dir_cnt ≥ 2.)
- **F282.2 T1-REDUCIBLE**: T1 (rule 110) faithful big-Φ = 0 (<1e-6) at ≥2/3 state → H_268
  의 robust target 이 faithful 에서 reducible (방향 verdict FLIP). (measurable: T1
  substantial count ≥ 2.)
- **F282.3 SCALE-FLIP**: 6-scale faithful CV ≤ 0.15 → faithful 이 scale-INVARIANT (H_278
  의 SCALE-VARIANT 가 proxy artifact, FLIP). (measurable: faithful_cv > 0.15.)
- **F282.4 NONDETERMINISM**: re-run result.json byte-different → raw#12 deterministic
  위반 → smoke 무효. (measurable: cross-process sha256 동일.)
- **F282.5 CONTROL-BREAK**: rule204 big-Φ ≠ 0 OR rule90@1010 big-Φ ≠ 0 → 엔진 control
  위반 (손유도 reference 와 불일치). (measurable: 두 control 값 < 1e-6.)
- **F282.6 POST-HOC**: frozen 후 verdict 방향 edit → raw#12 violation, raw#82 retraction.

## 8. Verdict

```
verdict_class: SUPPORTED (pre-register-frozen smoke; C1∧C2∧C3∧C4 met)

engine: HEXAD/IIT4/lib (eca_tpm + big_phi, stdlib/consciousness) — REUSED, M6 엔진 그대로.
config: n=4 ring · canonical sys_state=10 (1010) · faithful causal big-Φ.

── F282.1 — H_266 connectivity tiers (faithful causal big-Φ) ──
  integrated   : rule110=7.5475  rule30=8.66311  rule54=10.0278
  feedforward  : rule240=3.0     rule170=3.0      (pure shift = chain)
  disconnected : rule204=0.0     rule0=0.0
  min(integrated)=7.5475 > max(disconnected)=0.0          : true
  int>dis direction over states {5,6,10}                  : 3/3
  BONUS monotone int(7.55) > ffd(3.0) > dis(0.0)          : true  ← proxy artifact RESOLVED
  → PRESERVED + 추가 성과: H_266 proxy 의 monotone FAIL (feedforward<disconnected) 을
    faithful 인과 엔진이 **RESOLVE** — 정통 IIT 가 chain 을 통합>분리 사이에 정확히 배치.

── F282.2 — H_268 T1 (rule 110 pain CA) faithful causal big-Φ ──
  T1 rule110 big-Φ over states {1,5,10}: 15.0339, 7.66066, 7.5475  (substantial 3/3)
  → T1 (H_223/H_268 ROBUST target) 은 faithful 인과 big-Φ 가 일관되게 substantial
    (7.5~15) — clean binary TPM 으로서 genuine integrated 인과 substrate 확증.
  T2 (8-site continuous catalytic lattice): NOT a clean binary TPM (continuous, non-CI)
    → faithful-engine scope 밖 → PARTIAL (honest, 강제 변환 금지 — §9 L2).

── F282.3 — H_278 6-scale nested hierarchy (faithful causal big-Φ) ──
  cosmic_web(30)=8.66311  galaxy(110)=7.5475  stellar(90)=0.0
  planetary(54)=10.0278   biological(110)=7.5475  neural(110)=7.5475
  faithful CV over 6 scales = 0.465719  (threshold 0.15) → SCALE_VARIANT=true
  → HOLD: faithful 인과 big-Φ 가 H_278/H_002 C2 의 SCALE-VARIANT 결론을 *artifact 가
    아닌 진짜 인과 negative 로 확증*. stellar(rule 90 XOR)가 n=4 ring 에서 big-Φ=0 으로
    collapse 하여 6-scale 이 강하게 비균질 (인과 통합이 scale 별로 다름).

── controls ──
  rule204 (identity) big-Φ = 0 (reducible self-cells)     : PASS
  rule90 (XOR) big-Φ = 0 @1010 (state collapse §4)        : PASS
  rule110 big-Φ re-run byte-identical (in-process)        : PASS

criteria:
  C1 F282.1 PRESERVED (int>dis + monotone resolved)       : PASS
  C2 F282.2 ROBUST/PARTIAL split (T1>0 · T2 scope-out)    : PASS
  C3 F282.3 HOLD (scale-VARIANT, CV 0.466>0.15)           : PASS
  C4 DETERMINISM (cross-process byte-identical)           : PASS

verdict_rule: SUPPORTED iff C1∧C2∧C3∧C4 · PARTIAL if some FLIP · FALSIFIED if core flip
verdict     : SUPPORTED  (8/8 smoke checks PASS, 3/3 directional verdicts preserved)

falsifiers_triggered: none
  (F282.1 int>dis 보존 + monotone RESOLVED · F282.2 T1 substantial 3/3 · F282.3 CV 0.466
   > 0.15 SCALE-VARIANT HOLD · F282.4 byte-identical · F282.5 controls PASS · F282.6 N/A)
falsifiers_pass: F282.1 + F282.2 + F282.3 + F282.4 + F282.5 = 5/5.

key_finding:
  세 proxy verdict 의 **방향(directional)** 이 faithful 인과 big-Φ 에서 모두 보존 +
  한 곳에서 proxy artifact 까지 정정:
   (1) H_266 integrated>disconnected — faithful 보존 (7.5~10 vs 0), 그리고 H_266 proxy 가
       FAIL 했던 monotone (feedforward<disconnected) 을 faithful 엔진이 **RESOLVE**
       (int 7.5 > ffd 3 > dis 0). 즉 H_266 의 monotone FAIL 은 *spatial-MI proxy 의
       weakest-link 과적용 artifact* 였고, 인과 축에서는 정통 ordering 이 회복된다 —
       H_266 L6 의 추측("chain<disconnected 는 proxy artifact 일 수 있음")을 인과 측정이
       직접 확증.
   (2) H_268 T1 (robust target) — faithful big-Φ substantial (7.5~15) 로 genuine 인과
       통합 substrate 확증, H_268 의 T1=ROBUST 와 정합. T2 lattice 는 honest PARTIAL.
   (3) H_278 scale-VARIANT — faithful CV 0.466 (>0.15) 로 HOLD, proxy 의 scale-variance
       가 인과 축에서도 진짜 negative.
  proxy(상관)→faithful(인과) 승격에서 세 LIFE verdict 의 방향이 모두 살아남았고, H_266 의
  2차축 결함(monotone)은 인과 측정이 오히려 *고친다*.

honest_note:
  directional-trust carry — binary DIRECTION (high vs low big-Φ) 는 신뢰, 연속 MAGNITUDE
  + single-state 는 fragile (M6 §4 state-dependence). 본 H 는 방향만 자신 있게 주장하고,
  faithful big-Φ 의 절대 크기 (7.5475 등) 와 CV 절대값 (0.466) 은 n=4 single-state
  outcome 으로 헤지한다. n=4 ring + canonical state 1010 headline + 3-state direction
  robustness — 완전한 재측정은 state 분포 위 평균 + scale-up (n≤8) 필요 (§9 L1/L5).
  H_278 의 coupling 은 본 pure-rule TPM 에 모델되지 않음 (§9 L4).
```

### `hexa verify` (g5 honest fence — empirical 해석은 closed-form atlas identity 아님)

`hexa verify --fence` 는 본 worktree 에서 sign-gated (local-bound heavy invocation 토큰
없음) 이므로, H_278 §8 이 쓴 동일한 ⚪ SPECULATION-FENCED 양식으로 inline 명시한다:

```
verify --fence "H_282 re-measures the substrates of H_266 (connectivity tiers), H_268
   (rule-110 pain CA), and H_278 (6-scale nested hierarchy) with the faithful IIT 4.0
   causal big-Phi engine; integrated > disconnected is PRESERVED (and the proxy monotone
   artifact is RESOLVED: integrated 7.5-10 > feedforward 3 > disconnected 0), T1 is a
   genuine integrated causal substrate (big-Phi 7.5-15), and the SCALE-VARIANT verdict
   HOLDs (faithful CV 0.466 > 0.15) — deterministic n=4 toy-substrate outcomes, NOT
   atlas identities"
  tier   = ⚪ SPECULATION-FENCED
  reason = imagination/metaphor class (hexa-bio AXIS) — verification N/A by design;
           NOT a proven atlas atom (g4 honest fence, SF ≠ verified)
```

(faithful big-Φ VALUES 자체는 deterministic closed-form arithmetic — ECA→TPM bridge +
system-MIP big-Φ over distinctions/relations — 이며 fresh hexa run 에서 cross-process
byte-수렴 확인. sha256 `1b5de40e304fa2ef614e55f4a500226c39256690c16b0e5b06f1ab22c8cfa56b`
두 process 일치. 오직 empirical 해석(방향-보존의 의미)만 fenced.)

## 9. Honest Limits (raw#91 c3, ≥5)

- **L1 (n=4 ring · single-state headline, directional-trust)**: M6 과 동일한 n=4 ring
  demonstration capacity. headline 표는 canonical state 1010 한 점 + 3-state direction
  robustness. 완전한 재측정은 state 분포 위 평균/대표값 + scale-up (n≤8, 엔진 capacity
  존재) 필요. faithful Φ 가 state-dependent 이므로 (M6 §4) **방향은 신뢰, 절대 크기는
  헤지** — binary DIRECTION (high vs low) trustworthy, 연속 MAGNITUDE fragile.
- **L2 (H_268 T2 scope-out, honest PARTIAL)**: H_268 의 T2 (8-site catalytic lattice) 는
  continuous-valued + 고차 상호작용 결합으로, IIT 4.0 의 conditional-independence binary
  TPM 가정 (DESIGN §8 C3-3) 을 만족하지 않는다. 이를 억지로 binary 화하면 fair 한 faithful
  측정이 아니므로 **PARTIAL** 로 정직하게 남겼다 (강제 TPM 변환 거부). 따라서 H_268 의
  faithful 재검은 T1(robust) 한정 — H_268 의 robust/fragile split 중 fragile 쪽(LZ-axis
  on T2)은 faithful 인과 엔진의 scope 밖.
- **L3 (structure-cut big-Φ, not full IIT4 normalization)**: 본 big-Φ 는 DESIGN §8 C3 의
  spirit-faithful structure-cut big-Φ (system MIP 가 파괴하는 Σφ_d + Σφ_r). 절대 스케일의
  PyPhi 대조 + partition TPM recompute + normalization 은 IIT4 M5 named-blocker
  (F-IIT4-3/4) 영역 — 본 H 의 절대 크기는 그 calibration 전 값.
- **L4 (H_278 coupling 미모델 — pure-rule TPM)**: H_278/H_002 C2 의 6-scale 은 룰 +
  *coupling strength* {0.00, 0.10, 0.20, 0.30, 0.45, 0.60} 로 정의된다. 본 H 의 `eca_tpm`
  은 **pure-rule** bridge (coupling 미모델) 이므로, faithful 측정은 각 scale 의 *룰* 의
  인과 통합만 본다. CV 절대값 (0.466) 은 coupling 효과를 뺀 값 — scale-VARIANT *방향* 은
  robust 하나 (stellar rule 90 이 인과적으로 collapse), CV 크기는 coupling 을 모델하면
  달라질 수 있다 (directional-trust carry).
- **L5 (proxy-CV 직접 동시산출 없음, M6 §6 carry)**: proxy 의 측정축(상관)과 IIT4(인과)는
  입력형 자체가 다르다 (phi_spatial 은 evolved snapshot, IIT4 는 TPM). 본 H 는 동일
  substrate 위 두 스칼라를 *동시 재계산*하지 않고, proxy verdict (H_266/268/278 의 frozen
  값) 와 faithful 방향을 *대조*한다. 방향-보존이 핵심 claim 이지 절대값 1:1 매칭이 아니다.
- **L6 (verdict ≠ 원 가설 re-verdict)**: 본 H 의 SUPPORTED 는 H_266/268/278 의 frozen
  verdict 를 *재verdict 하지 않는다* — proxy verdict 의 방향이 faithful 인과 축에서
  보존되는지의 별도 meta-측정. H_266 의 monotone-resolution 은 "H_266 verdict 를 PARTIAL
  →SUPPORTED 로 바꿈" 이 아니라 "H_266 의 L6 추측(chain<dis 는 proxy artifact)을 인과
  측정이 확증" 이다 (frozen verdict carry).
- **L7 (large-N 불변, M6 C3-5 carry)**: 본 lane 은 proxy 의 primitive 축(상관→인과)을
  메우지만 small-N 경계 (n≤8) 는 H_278 과 동일 상속. large-N 우주-scale 은 여전히
  intractable — 별개 근사연구 (H_002 frozen L4 named blocker).

## 10. Cross-Links

- **EXTENDS**: IIT4 M6 (`HEXAD/IIT4/FAITHFUL_REMEASURE.md`, F-IIT4-6 PROXY-DIVERGENCE) —
  M6 이 ECA 일반 proxy-divergence 를 닫았고, 본 H 가 그 엔진을 H_266/268/278 의 specific
  substrate 로 확장. smoke = `HEXAD/IIT4/state/iit4_m6_remeasure_2026_05_25/run_m6.hexa`.
- **재검 대상 H (proxy verdict source)**:
  - H_266 (`H_266_phi_calibration_known_iit.md`, PARTIAL — C1 int>dis PASS · C2 monotone
    FAIL) — 본 H 가 C1 보존 + C2 monotone 을 인과 엔진이 RESOLVE.
  - H_268 (`H_268_phi_metric_triangulation.md`, PARTIAL — T1 robust 3/3 · T2 LZ-fragile)
    — 본 H 가 T1 robust 를 인과 big-Φ 로 확증, T2 는 honest PARTIAL (scope-out).
  - H_278 (`H_278_faithful_phi_small_n.md`, SUPPORTED — scale-VARIANT HOLD) — 본 H 가
    faithful 인과 big-Φ 로 scale-VARIANT HOLD 재확인.
- **engine (REUSED, READ-ONLY)**: `HEXAD/IIT4/lib/iit4_eca.hexa` (`eca_tpm`) →
  `stdlib/consciousness/iit4_bigphi.hexa` (`big_phi`) → `iit4_relation` →
  `iit4_distinction` → `iit4_tpm`. design = `HEXAD/IIT4/DESIGN.md`.
- **gap lens**: faithful-vs-proxy = F8 (cross-tool / inter-instrument calibration) + F4
  (counterfactual — "인과 big-Φ 였다면 방향이 바뀌었을까?" → 보존, 한 곳은 오히려 개선).
- **raw**: raw#12 (deterministic strict) + raw#91 c3 (honest limits) + raw#82 (no
  post-hoc) + g61 (stdlib reuse — big-Φ 재구현 금지).
- **philosophy (CLAUDE.md)**: p7 NO PERPLEXITY VERDICT (단일 proxy 를 truth 로 취급 않고
  faithful 인과 축으로 cross-check) · a_blue_closed (proxy 의 구성타당도가 lane wiring 의
  근간 — 인과 축이 그 wiring 을 검증).
- **literature**: Albantakis et al. (2023) IIT 4.0 (cause-effect structure / Φ-structure)
  · Oizumi, Albantakis, Tononi (2014) IIT 3.0 (Φ at the MIP) · Tononi (2004) IIT ·
  Wolfram (2002) A New Kind of Science (elementary CA).
- **state**: `UNIVERSE/state/h282_proxy_to_faithful_2026_05_26/{run_h282.hexa,
  result.json}` (result.json sha256
  `1b5de40e304fa2ef614e55f4a500226c39256690c16b0e5b06f1ab22c8cfa56b`).
- **Tier**: 🟢 NUMERICAL (faithful 인과 big-Φ deterministic, cross-process byte-identical;
  empirical 해석은 ⚪ SPECULATION-FENCED, g5).
- **Next**: H_282r2 후보 — (a) **state 분포 평균** (L1 axis): canonical 1010 단일점 대신
  2^n state 위 big-Φ 평균/대표값으로 방향-보존을 state-robust 화; (b) **scale-up n≤8**
  (L1 axis): n=4→8 로 6-scale CV 의 n-dependence 분리; (c) **H_278 coupling-TPM** (L4
  axis): coupling 을 stochastic TPM 으로 모델해 coupling 포함 faithful CV 측정;
  (d) **PyPhi anchor** (L3 axis): n≤4 의 절대 big-Φ 를 PyPhi 문헌값과 대조 (M5
  F-IIT4-5 영역).
```
