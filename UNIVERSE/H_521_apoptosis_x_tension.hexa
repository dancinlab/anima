fn flatline_apo(l2_sq: f64, theta_sq: f64) -> bool { return l2_sq < theta_sq }
fn check_h521() -> bool { let f = flatline_apo(0.01, 0.05); let n = !flatline_apo(2.0, 0.05); return f && n }
fn main() { if check_h521() { print("H_521 OK") } else { print("H_521 FAIL") } }
