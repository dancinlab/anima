fn recycle_channel(ch: f64, eps: f64) -> bool { let abs_ch = if ch < 0.0 { -ch } else { ch }; return abs_ch < eps }
fn check_h564() -> bool { return recycle_channel(0.01, 0.05) && !recycle_channel(0.8, 0.05) }
fn main() { if check_h564() { print("H_564 OK") } else { print("H_564 FAIL") } }
