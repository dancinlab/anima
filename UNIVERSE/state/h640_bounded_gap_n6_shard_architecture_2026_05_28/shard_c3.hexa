// H_640 — ONE cap=3 shard at n=6, single-state st=21. RULE edited per-run.
// rule90 ~39s, rule110 ~40s (probe-confirmed < 60s). rule30 c3 = 118s (slow shard).
import "/tmp/u-h640/HEXAD/IIT4/lib/iit4_eca.hexa"
import "stdlib/consciousness/iit4_bounded.hexa"
let N = 6
let ST = 21
let RULE = 110
let CAP = 3
fn main() {
    let tpm = eca_tpm(RULE, N)
    let r = big_phi_bounded(tpm, N, ST, CAP)
    println("SHARD n6 r" + str(RULE) + " st21 cap" + str(CAP) + " phi=" + str(r[0]))
    println("SHARD_DONE")
}
