# H_396 — 의식적결정 × CORE 🔵

의식적결정 × CORE — decision threshold θ scales with M activation (θ = θ_base · (1 - M_norm) — higher M lowers threshold).

## 가설
H1 THETA-FROM-M: θ(M) = θ_base · (1 - M_norm), M_norm ∈ [0, 1]
H2 BOUNDED: 0 < θ(M) ≤ θ_base
H3 MONOTONE-INVERSE: M ↑ → θ ↓ (high motivation → easier to decide)
H4 DETERMINISTIC
H5 BOUND
