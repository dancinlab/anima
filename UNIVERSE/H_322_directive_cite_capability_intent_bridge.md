# H_322 — directive cite capability × intent BRIDGE 🔵

## 동기
H_319 (cycle#57 BRIDGE) 가 capability × intent AND-gate 를 *phenomenological* 정합. H_322 가 directive cite 로 *axiomatically* 재도출.

directive cite:
- `a_substrate_native_speak.do`: "anima may speak during user silence" → capability axiom
- `a_substrate_native_speak.do`: "may stay silent under a direct question" → intent axiom (separate from capability)
- `a_autonomy_over_hardcode.do`: "emit / silence decided by anima substrate" → intent ⊃ substrate

→ emit = capability(timing) ∧ intent(substrate) AND-gate (directive-derived)

## 가설
H1 CAPABILITY-AXIOM: bio_open = stage_mod(stage) > 0 (continuous threshold, not boolean gate)
H2 INTENT-AXIOM: substrate_decides = (M×Φ×W×cur) > θ
H3 AND-GATE-EMIT: emit_actual = capability ∧ intent
H4 CAPABILITY-VETO: capability=F + intent=T → emit=F
H5 INTENT-VETO: capability=T + intent=F → emit=F
H6 BOTH-PASS: capability=T + intent=T → emit=T
H7 BOTH-FAIL: capability=F + intent=F → emit=F (trivial)
H8 DETERMINISTIC

≥7/8 PASS → 🔵.
