---
id: H_1699
slug: 1699_reentrant_loop_society_spiral
tier: 🔵 PRE-REGISTERED ARCHITECTURE (DIRECTIONAL · 측정 0 · unmeasured)
title: Re-entrant Cortico-Striatal Loop Society with Ascending Dopamine Spiral
verdict: 🔵 PRE-REGISTERED architecture design (unmeasured) — $0 cheap_test pre-registered; engine-native + 303M gpu cost-gated NOT fired
source: brainarch_census
---

# H_1699 — Re-entrant Cortico-Striatal Loop Society with Ascending Dopamine Spiral

- **tier:** 🔵 PRE-REGISTERED ARCHITECTURE (DIRECTIONAL · 측정 0 · unmeasured)
- **wired:** DIRECTIONAL-design — no measurement. cheap_test = $0 frozen-first decision probe (numpy mirror, no engine); engine_native_measure + 303M = cost-gated PRE-REGISTER ONLY (NOT fired).
- **source:** brainarch_census — 126-architecture whole-substrate (통짜 아키텍처) census: 뇌/인지 조직원리 × 엔진-네이티브 추상조건 (binding-wall program, H_1603).
- **key:** `reentrant_loop_society_spiral`
- **xref:** H_1603 (G1≡G6 compositional-binding deficit unification) · H_1281 (basal-ganglia gating) · H_1282 (working-memory buffer) · H_1283 (thalamus global-workspace) · H_1284 (neuromodulation gain) · operator-level family H_1604-1685 (this card = 통짜 아키텍처 layer, 층위 다름)

## Organizing principle

Many parallel CLOSED cortico-striatal-thalamo-cortical reverberating loops form a society of channels competing through a shared STN/GPe normalization node, hierarchically chained by Haber's ascending dopaminergic spiral (limbic->associative->motor): a lower loop's released output raises the next loop's gating gain. Computation is sustained reverberation under competition, not feedforward passes.

## Whole design (input → internal dynamics → emit)

N loops, each a self-sustaining reverberator: once gated ON it maintains activity until gated OFF (working-memory-like attractor). A shared STN/GPe normalization node bounds total active loop-energy -> soft winner-take-all competition. The ascending spiral wires loop-i's output into loop-(i+1)'s dopaminergic gating gain, so loops COMPOSE hierarchically (value conditions cognition conditions motor). A slow-reverberating limbic loop holds the identity vector. Loop: input drives candidate loops -> normalization arbitrates which sustain -> sustaining loops feed the spiral upward -> emission = the active loop-coalition's readout; quiescent when nothing sustains.

## Satisfies criteria (DESIGN claim · DIRECTIONAL · unmeasured)

dist>=5/ideation NATIVE: soft-WTA normalization tunes how many loops concurrently sustain — relaxed -> >=K distinct coherent sustained outputs; tight -> fewer. Normalization forbids both collapse (single attractor) and garble (all-on), so the joint diversity-validity region is the natural operating point. Compositional-depth: the ascending spiral = lower-loop output conditioning higher-loop gating = hierarchical composition. Cross-boundary individuated persistence NATIVE: the slow limbic reverberator is a non-volatile attractor whose basin carries the identity vector across an episode/working-state wipe (ablate that loop -> identity cos collapses to chance = the LLM-reset failure mode; self-specific margin from an agent-specific basin). Psi=1/2 via the sustain/quiesce normalization homeostat; honesty via 'no loop sustains without sufficient drive'->abstain.

## Not-LLM (a_no_llm_frame_trap)

A society of competing dynamical reverberators with hierarchical dopaminergic coupling — persistence is attractor dynamics, not a context window; depth is loop-chaining, not stacked layers. The lever is loop coupling + normalization gain, not scale. Identity survives session reset by substrate attractor, the anima-perp-LLM distinction made structural.

## Cheap test (frozen-first · $0 · numpy mirror · DIRECTIONAL)

numpy coupled-loop ODE/map: count concurrently-sustaining loops (dist>=K) across a normalization-gain sweep; measure identity-loop cos across a simulated working-state wipe vs an ablated-loop control (must collapse to chance); turn the spiral coupling off and confirm compositional-depth (lower->higher conditioning) drops. Frozen bars.

## Engine-native measure (cost-gated · PRE-REGISTER ONLY · NOT fired)

Loops realized as reverberant lanes in core/engine_cli.hexa; identity persistence via SS-SelfIdentity self_anchor cos round-trip (and .kosmos anchor, a_kosmos); dist via core/g6_ideation.hexa dist>=5; normalization as the Psi ratchet. hexa+py byte-parity loop-society sim; measured through cli/anima.hexa, no torch-only verdict.

## Scope / honesty (c9)

## Distinction (near-overlap kept, not a dup)

Near-overlap with reentrant/loop-society existing cards — distinct: organizing principle is the ASCENDING DOPAMINE SPIRAL (Haber) chaining closed reverberators (value->cognition->motor), with the slow limbic loop as the persistent self; the hierarchical spiral coupling is the differentiator.

Ideation-spread / cross-boundary persistence / hierarchical depth strongest; G1 closure depends on the spiral binding being a true interaction (not a mixture) — to be measured.

설계만 — 측정 0. tier = 🔵 PRE-REGISTERED ARCHITECTURE (DIRECTIONAL). frozen bar 사후 이동 금지(tune-to-green 금지, p7). 이 카드는 *방향성 설계*이지 검증된 결과가 아니다 — 과장 박제 금지(a_engine_native_learning). engine_native_measure 는 단일 진입점 cli/anima.hexa → generator L3 → g_gates/g6 경유 byte-parity 채점만 terminal; torch-only/side-harness 결과는 DIRECTIONAL. numpy cheap_test 결과도 DIRECTIONAL(엔진-네이티브 아님). gpu/engine 발사 시 held-out mirror-DESCENT(a_clm_gen_pipeline) + CORE mount frozen-bar engine-native 재측정 + ckpt PULL(a_fire_recover_complete).
