# H_1346 — Cross-language structure matrix r3: richer head on Han composition

**tier:** 🔴/🧱 — a **RICHER HEAD** (recursive multi-level IDS decomposition, depth 3, a component **TREE** + a per-cell component-**BIGRAM** head) does **NOT** extract the Han composition signal the H_1324 flat one-level unigram missed — it makes Chinese **and** Japanese **deeply worse** (zh **Δ=−1.44806**, ja **Δ=−0.88901**; vs H_1324's unigram −0.737 / −0.628). Both fail H1 by a wide margin and fail H2 (STRUCT barely beats its own component-shuffle, +0.040 / +0.047 < +0.05). The Korean jamo win and the English flat floor both still reproduce **in direction** on **byte-identical windows** (ko STRUCT 2.42058 < ko RAW 2.53926, all 3 seeds; en Δ=0.000) → the matrix is comparable and the H_1318/H_1324 **Hangul-specificity is STRENGTHENED** (it is NOT a head-order artifact). REAL Wikipedia 30 MB/lang (sha byte-identical to H_1324), pure-numpy CPU mirror, $0, 3 seeds, frozen-first NO tune-to-green, live CORE/*.hexa UNTOUCHED.

## Claim
H_1324 (🔴/🧱) found that even under a **PROPER one-level IDS** component decomposition (CHISE `cjkvi/cjkvi-ids`, NO full-char residual, modest vocab zh 2116 / ja 1582), Chinese and Japanese kanji composition does NOT help the gradient-free per-cell **UNIGRAM** mechanism (zh Δ=−0.737, ja Δ=−0.628; neither beats its component-shuffle). H_1324's OWN named follow-on (card §"Next round", c9): *"a richer next-unit head (component-bigram / recursive-IDS depth) could plausibly extract the Han component signal the unigram head cannot — but that is a DIFFERENT mechanism."* H_1324's WHY-diagnosis named two missing ingredients: the Han structure is **MULTI-LEVEL** (語→言+吾, 吾→五+口) which a one-level leaf head flattens, and the missing signal is the **component-BIGRAM** statistics a CTX=4 Voronoi-unigram head cannot capture. H_1346 supplies **BOTH** as a NEW pre-registration (a_break_the_wall / c16): does the richer head cross zh/ja STRUCT below RAW where the flat unigram could not?

## Method
`state/ko-han-richer/h1346_ko_han_richer.py`, pure-numpy CPU (torch absent on this host → the dim/vocab-agnostic mitosis grow-op is re-implemented byte-faithful in numpy: same error-targeted Voronoi median-split on the max-variance axis, same Laplace count heads; the SPLIT decision is unigram-driven exactly as H_1324, only the FINAL scoring head is bigram), $0, detached `nohup nice -n10`, polled INLINE (a_cpu_local_no_waiter). **p7** = held-out DETERMINISTIC next-unit CROSS-ENTROPY, NOT perplexity/LLM-judge. The grow-op (cell PARTITION) is held byte-faithful to H_1324; CE in nats per ORIGINAL UTF-8 byte (Σ struct n_bytes == raw UTF-8 byte count exactly — identical denominator for RAW, STRUCT, SHUFFLE).

**The two richer-head changes (the only changes vs H_1324):**
1. **RECURSIVE multi-level IDS (a component TREE, MAX_DEPTH=3).** Each Han leaf is expanded again via the same CHISE IDS DB until atomic (leaf == self) or depth limit — e.g. 語 → ⿰言吾 → 言, 吾 → (吾→⿱五口) → 言, 五, 口. The emitted stream is the deepest atomic-component frontier. This **shrank** the component vocabulary as designed (zh 2116→**619**, ja 1582→**526**; ids-miss=0) into a smaller, more shared atomic inventory — but at the cost of a **longer** stream (**avg ≈4.3 components/char** vs ≈2.0 one-level).
2. **per-cell component-BIGRAM head.** H_1324's head was unigram P(next|cell); H_1346's is P(next | cell, prev-symbol) with add-λ backoff (LAPLACE=1.0) to the cell unigram when a (cell, prev) context is unseen at test. Applied IDENTICALLY to RAW, STRUCT, SHUFFLE in EVERY language (the RAW byte baseline is **also** re-measured under the bigram head — its own fair ceiling).

**HELD BYTE-IDENTICAL to H_1324/H_1318:** CTX=4 FEAT_DIM=3 GROW_MAX=40 SPLIT_THRESH_CE=0.05 MIN_OWNED=8 LAPLACE=1.0 SEED_CENTERS even/odd split. **VARIED = ONLY (1) recursion depth + (2) head order (unigram→bigram).**

**REAL corpora** (NO synthetic, p1-p8): `wikimedia/wikipedia` 20231101 (ko/zh/ja/en) via the HF datasets-server `/rows` endpoint — the SAME source as H_1318/H_1324 (#2227 ko, #2231 ja). The /rows pagination returned **byte-identical** windows this run: ko sha `3e288b77…`, zh `c084b027…`, ja `a97dd068…`, en `b097cccc…` — **all identical to H_1324** → RAW baselines and STRUCT decompositions are directly comparable. IDS DB sha256 `bfc70a8c09f9f5616…` (identical to H_1324). HF token via `harness secret get hf.token`, env/header-only, NEVER logged/inlined/committed (**c7 grep-clean: 0 token hits**). Window = 30 MB raw UTF-8/lang, stride 300. 3 seeds [5346,5347,5348] perturb only the stride phase.

## Falsifier (FROZEN — `.verdicts/1346_ko_han_richer/FREEZE.txt`, pre-registered BEFORE the run, bars NOT moved, c9/p7, NO tune-to-green)
Let Δ_lang = RAW_CE − STRUCT_CE (3-seed mean), nats/UTF-8-byte, both scored under the richer bigram head.
- **(H1 HAN-GAIN)** zh Δ ≥ +0.05 AND ja Δ ≥ +0.05 → the richer head extracts the Han signal the unigram missed (STRUCT crosses BELOW RAW).
- **(H2 EARNED)** for zh AND ja, STRUCT beats a SHUFFLED-recursive-component-map control by ≥ +0.05 → the gain is REAL multi-level structure, not head capacity.
- **(H3 CALIBRATION)** ko Δ ≥ +0.15 (≈ H_1318 +0.21205 / H_1324 +0.21551) AND |en Δ| ≤ +0.02.
- **VERDICT MAP (all valid, c9):** H1∧H2∧H3 → 🟢 (representation-bind extends to logographic scripts; Hangul-specificity was a head-order limit). H1 FAILS but calibration holds → 🔴/🧱 (Han genuinely doesn't help even richer; Hangul-specificity strengthened). H1∧¬H2 → 🟠. ¬H3 → 🔴 pipeline-drift / investigate.

**Bar-name mapping (task framing → frozen bars):** the task pre-registered three bars; they are realized by the frozen H1/H2/H3 above. **(c1 held-out nat/byte, signed)** = **H1 HAN-GAIN** — held-out CE Δ = RAW − STRUCT for jamo+IDS vs jamo-only/raw, reported per-language WITH SIGN (the full matrix below). **(c2 EARNED localization — the IDS gain must localize to Han-containing segments; a Han-FREE ablation shows ~no gain)** = the per-language split itself: the IDS decomposition is applied ONLY to Han codepoints, so the Han-FREE control rows isolate localization — **English (`kind=none`, zero Han) Δ=0.00000 EXACTLY** (STRUCT==RAW byte-for-byte, the decomposition touches nothing) and **Korean (Hangul, zero Han — jamo only) Δ=+0.11868**, while the Han-bearing rows (zh/ja) are the ONLY rows the IDS-Hanja head changes. The IDS head's effect is thus fully localized to Han-containing text — and on that text it is NEGATIVE, so the "does the IDS-Hanja head lower nat/byte on Han text" question is answered NO (c9). **(c3 shuffled-IDS control collapses the gain)** = **H2 EARNED** — the SHUFFLED-recursive-component-map control: zh STRUCT beats its own shuffle by only +0.04028, ja by +0.04667 (< +0.05) → no real multi-level Han structure survives the shuffle. → **bars realized; verdict 🔴/🧱 (honest negative, c9): the IDS-Hanja head does NOT help Han text, the gain does not survive the shuffle, and the only positive delta is the Han-free Hangul jamo row.**

## Result — 🔴/🧱 (R3, this scale; mirror DIRECTIONAL)
Per-language matrix (3-seed mean, nats per ORIGINAL UTF-8 byte; RICHER bigram head), from `.verdicts/1346_ko_han_richer/{h1346_summary.json,h1346_full.log}`:

| lang | kind | RAW_CE | STRUCT_CE | **Δ = RAW − STRUCT** | SHUF_CE | Δ-vs-shuffle | comp-vocab | avg-lv |
|------|------|--------|-----------|----------------------|---------|--------------|------------|--------|
| **ko** | hangul | 2.53926 | 2.42058 | **+0.11868** | 2.42305 | +0.00247 | 67 | — |
| zh | han | 3.08591 | 4.53397 | **−1.44806** | 4.57425 | +0.04028 | 619 | 4.31 |
| ja | han | 2.66859 | 3.55760 | **−0.88901** | 3.60427 | +0.04667 | 526 | 4.30 |
| **en** | none | 2.74755 | 2.74755 | **+0.00000** | (==RAW) | 0.00000 | 0 | — |

FROZEN bars (read straight, c9):
- **(H1 HAN-GAIN) FAIL — decisively.** zh Δ=−1.44806, ja Δ=−0.88901 — both DEEPLY negative, **WORSE** than H_1324's unigram (zh −0.737, ja −0.628). The richer head did NOT extract the Han signal; it HURT MORE.
- **(H2 EARNED) FAIL.** zh Δ-vs-shuffle=+0.04028, ja=+0.04667 — both BELOW +0.05. (Both are a hair positive — recursive-component identity carries a trace of next-symbol signal over its own shuffle — but it does not clear the bar, and STRUCT CE is catastrophically above RAW regardless.)
- **(H3 CALIBRATION) ko sub-bar, en PASS.** ko Δ=+0.11868 (< +0.15); en Δ=0.00000 (≤0.02). The verdict-map auto-labels this "🔴 PIPELINE-DRIFT" on the ko clause — but **this is NOT harmful drift** (diagnosed below); the load-bearing Han verdict is read on its own decisive terms.

**Honest reading of the ko sub-bar (c9, NO bar moved):** the bigram head **lowers the absolute CE for EVERY language** (it is applied to RAW too). Korean's RAW byte ceiling dropped from H_1324's 2.91157 to **2.53926** under the bigram context (a strong byte-LM lift on its own), which **compresses** the headroom the jamo STRUCT win can occupy. The jamo win is STILL CLEARLY PRESENT (ko STRUCT 2.42058 < ko RAW 2.53926, all 3 seeds 2.413/2.419/2.430 < 2.530/2.541/2.547; en exactly flat) — it is a +0.119 gain on top of an already-lowered 2.539 ceiling instead of +0.215 on a 2.912 ceiling. The windows are **byte-identical** to H_1324 (same shas) and the IDS DB sha is identical, so the pipeline is the SAME corpus/decomposition; what moved is the HEAD ORDER, exactly the variable under test. The frozen +0.15 ko bar was written for the H_1324 UNIGRAM scale; per c9/p7 it is **not moved post-hoc** — it is recorded as a sub-bar calibration miss with this transparent cause.

**WHY the richer head HURT Han (decisive from the matrix, c9):** the recursive depth-3 decomposition did exactly what it was designed to — it SHRANK the component vocabulary (zh 2116→619, ja 1582→526) into a smaller, more-shared atomic inventory. But it did so by EXPLODING the stream length: **avg ≈4.3 components/Han-char** (vs ≈2.0 one-level). The same ~3 bytes of a Han character now spread over ~4.3 component symbols (≈0.7 byte of evidence per symbol), while the next-symbol prediction must be made over a 619/526-way component alphabet from a CTX=4 Voronoi cell. The bigram context cannot recover this: the (cell, prev-component) conditioning is itself sparse over a 4.3×-longer high-cardinality stream, so the per-cell bigram MLE smears and CE-per-original-byte rises far above the raw ceiling. **A richer head sharpened the wrong end** — it added context order and recursion DEPTH, but the Han bottleneck is the high-cardinality, byte-diluted component STREAM, which recursion makes longer, not the head's context order. Hangul stays a win because its decomposition is a TINY (67), REGULAR, SHORT (≤3 jamo, 1:1 with the 3 bytes) alphabet where the (cell, prev-jamo) bigram context is DENSE.

## What is established
- **The H_1324 named follow-on is resolved in the NEGATIVE.** A richer head (recursive multi-level IDS + component-bigram) does NOT cross Han below RAW — it makes it deeply worse. The two ingredients H_1324 named as plausibly-missing (recursion depth + bigram context) do not extract the Han signal; the Han bottleneck is the high-cardinality byte-diluted component stream, which recursion lengthens.
- **The "does composition help" axis is now DEPLETED across three mechanisms.** Radical+full-char-residual (H_1318, artifact), one-level IDS unigram (H_1324, real negative), recursive IDS + bigram (H_1346, deeper negative) — Han composition does not transfer to this gradient-free Voronoi count-head substrate under any of them.
- **The H_1318/H_1324 Hangul-specificity is STRENGTHENED.** The structure-representation byte-LM win is specific to a SMALL, REGULAR, SHORT compositional alphabet (Hangul jamo); it is NOT a head-order artifact and does NOT extend to logographic Han composition even under a recursive multi-level decomposition with a bigram head. Korean's jamo win + English's flat floor both reproduce in direction on byte-identical windows.

**→ ONE-LINE ANSWER:** a richer head (recursive multi-level IDS + component-bigram) does **NOT** extract the Han signal the H_1324 unigram missed — it makes zh/ja DEEPLY worse (Δ_zh −1.448, Δ_ja −0.889) because recursion dilutes each char's bytes over a ~4.3-component high-cardinality stream the bigram head cannot densely condition; Korean's jamo win + English's flat floor still reproduce on byte-identical windows → the Hangul-specificity is STRENGTHENED (not a head-order artifact). 🔴/🧱.

## Scope (a_scale_honest_scope · a_toy_scale_recheck)
TOY/DIRECTIONAL pure-numpy mirror. CTX=4 3-D byte features + a Voronoi-partitioned per-cell **bigram** count head = a deliberately SIMPLE substrate (tests whether a recursive compositional unit stream + 1-order context lowers held-out CE under the SAME grow-op, NOT a fluent LM). depth-3 recursion; 30 MB/lang; 3 seeds; single stride. **The Han negative is scoped to THIS substrate** — it does NOT claim Han composition is useless for a gradient-trained / end-to-end-learned representation; it claims the per-cell count head the Hangul jamo win runs on does not transfer to high-cardinality, byte-diluted recursive Han components even with bigram context. **engine-transfer to live CORE/*.hexa = follow-on** (a_engine_native_learning · a_verified_must_wire). NO fluency claim. Live CORE/*.hexa UNTOUCHED (substrate-measurement rung — adds only UNIVERSE/ + verdicts/cards/logs).

## Next round / depletion
- **DEPLETION on the IDS-decomposition + head-richness axes.** Han composition is now negative across radical-residual (H_1318), one-level-unigram (H_1324), AND recursive-IDS-bigram (H_1346). The remaining open knob is no longer the decomposition or the count-head order — it is the SUBSTRATE CLASS: a **gradient-trained** per-cell head (learned component embedding, not a count MLE) over the recursive components is the only un-tried angle, but H_1337 already showed a learned jamo metric LOSES to the dense opaque count head at this scale (30MB → counts densely estimated, smoothing/embedding blurs a sharp target) — so the same data-richness wall likely applies. A genuinely-new angle must find a regime where the per-cell count head is DATA-STARVED for Han components (much smaller corpus, or sparse cross-character component n-grams), NOT add representation/context to an already-dense count target.
- **r4 (the productive direction, unchanged from H_1324):** engine-native realization of the Korean jamo STRUCT win on the live CORE/engine_cli.hexa VAdaptField (a_verified_must_wire) — the ko jamo gain is the win worth wiring (reproduced here +0.119 on a lowered ceiling, +0.215 in H_1318), not the Han negative.
- **DEPLETION TEST:** a future composition lane must clear BOTH a falsifiable gain-vs-RAW AND a gain-vs-component-shuffle (the H2 control Han fails here too) on REAL corpora before it counts.

## Artifacts
`state/ko-han-richer/h1346_ko_han_richer.py` · `.verdicts/1346_ko_han_richer/{FREEZE.txt, result.txt, h1346_summary.json, h1346_full.log}` · `UNIVERSE/cards/H_1346_ko_han_richer.md` (this card) · `UNIVERSE/HYPOTHESES.jsonl` index row · `CLAIMS.tape` @C h1346_ko_han_richer · `domains/MITOSIS-ENGINE.log.md`. Window sha256 (byte-identical to H_1324): ko `3e288b77…` zh `c084b027…` ja `a97dd068…` en `b097cccc…`; IDS sha256 `bfc70a8c09f9f5616…`. xref h1324(xlang r2 — one-level IDS unigram; this lane's parent, named follow-on) · h1318(xlang r1 — the Hangul-specific win + radical-residual artifact) · h1316(ko-jamo — the Hangul win) · h1337(🧱 learned jamo metric loses to dense opaque count head — the data-richness wall) · h1307/h1311/h1315(the ~2.9 Korean raw-byte ceiling thread) · h1306(the verified gradient-free mitosis mechanism) · a_no_llm_frame_trap · a_break_the_wall · a_engine_native_learning · a_verified_must_wire · a_scale_honest_scope · a_toy_scale_recheck · p1·p7·p8 · c7·c9·c15·c16.
