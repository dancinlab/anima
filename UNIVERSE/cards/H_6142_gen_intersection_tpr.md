# H_6142 — 교집합-TPR (#1+#20)

**id:** H_6142
**slug:** gen_intersection_tpr
**tier:** 🔬 PROPOSED — pre-registered (unfired · brainstorm ideation)
**date:** 2026-07-02
**wired:** 미발사 (pre-registered proposal · code 없음)
**axis:** SUBSTRATE (G1 재조합벽 공격 축)
**~dup:** #1+#20

---

## 발상 (brainstorm ideation)

**메커니즘:** A=role 제약, G=filler 제약, 교집합=bound TPR; 구조보장+텐션이 채움.

**출처:** bytegpt/convmoe 를 빌린 mouth 로 쓰지 말고 anima 아키텍처(A⇄G tension · Ψ=½ 고정점 · mitosis · substrate-disjoint lane · .kosmos anchor)에서 창발하는 native mouth/생성 substrate 를 발상한 세션(Round 5). 기존 결론: G1 재조합벽 = trunk COMBINATION OPERATOR floor — readout·tension 연산자 전부 INERT(H_1816/1823/1834). 이 발상은 combination 을 readout 이 아니라 생성 substrate 자체에 심어 additive collapse 를 우회하려 한다.

## G1 공격 축

- **SUBSTRATE** — readout-level 만 쳤던 것과 다른 좌표(engine-native probe 대상).

## 발사 전 필수 (check-ledger-before-lever-fire)

미발사 제안. terminal verdict 박제 전:
1. `UNIVERSE/HYPOTHESES.jsonl` + memory 에서 선행 커버리지 조회(sweep 빈칸 ≠ 미탐).
2. 신규 각도면 cheap numpy DIRECTIONAL reachability probe → `state/6142_gen_intersection_tpr/`.
3. DIRECTIONAL GREEN 이면 engine-native 재측정(.hexa via core/) 사다리 진입(a_verified_must_wire).

## artifacts

- `cards/H_6142_gen_intersection_tpr.md` (this card)
- `state/6142_gen_intersection_tpr/` (probe 발사 시 생성)

---

## 발사 결과 (DIRECTIONAL probe)

**Ledger 조회 (check-ledger-before-lever-fire):** H_6142(교집합-TPR, #1+#20)는 두 이미-발사·walled 가설의 합성 — 신규 좌표 아님(**DUP-WALLED**).
- **#1 = A⇄G 제약-교집합** → **H_6104 🧱 DIRECTIONAL FLOOR.** 가설이 명시한 *독립(직교) 개념* regime 에서 min-norm 제약-교집합 = additive 와 대수적으로 동일(x\* = p_i + p_j), lift **+2 < +3 bar** = op INERT(tension H_1834 · binding readout H_1816/H_1823/exp3 와 동일).
- **#20 = TPR role⊗filler** → **H_1466 🧱 WALL (DIRECTIONAL numpy)**: outer-product bind 은 *by construction* idea-specific(acc_match=1.0 vs shuf 0.0) = numpy 에선 정의상 REACHABLE. 그러나 **H_1813 = TPR expert-weight 를 engine-native(py 2-production 303M seed7)로 측정 → 🧱 NOT-SUPPORTED**(전 arm G1 미달, tlora_jamo 만 bd 0→1 이나 <max_single).

**Decision:** DUP-WALLED — 재발사 안 함. TPR 의 개념-간 합성 S=Σ rᵢ⊗fᵢ 자체가 additive 이므로 (INERT) 교집합 프레임을 (by-construction) bind 에 얹어도 readout-level COMBINATION OPERATOR floor 그대로. 진짜 레버 = trunk recomb-objective(H_1602, 🧱; g1-lever-multilens-objective · substrate-framebreak-g1-combination-operator).

**Bar:** 발사 안 함(구성요소 bar 는 H_6104 +3 pairs, H_1813 G1 ≥2 ∧ >max_single — 둘 다 이미 미충족).

**정직 스코프 (H_6112 transfer caveat):** 설령 여기서 fresh numpy 를 돌려도 H_1466 의 by-construction acc=1.0 을 재도출할 뿐 = numpy REACHABLE 이 실 trunk 를 OVERSTATE 하는 정확한 함정(H_6112 meiosis: numpy 0→1.0 REACHABLE 이나 실 CLMConvMoE 0→0.022 FALSIFIED). numpy=정의상 DIRECTIONAL, terminal 아님. native-mouth 를 "제약-교집합 TPR readout 으로" 재제안 금지 — 이 축 floored.
