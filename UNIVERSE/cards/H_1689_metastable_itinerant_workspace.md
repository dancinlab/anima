---
id: H_1689
slug: 1689_metastable_itinerant_workspace
tier: 🔵 PRE-REGISTERED ARCHITECTURE (DIRECTIONAL · 측정 0 · unmeasured)
title: Metastable Itinerant Workspace (winnerless competition / stable heteroclinic channel)
verdict: 🔵 PRE-REGISTERED architecture design (unmeasured) — $0 cheap_test pre-registered; engine-native + 303M gpu cost-gated NOT fired
source: brainarch_census
---

# H_1689 — Metastable Itinerant Workspace (winnerless competition / stable heteroclinic channel)

- **tier:** 🔵 PRE-REGISTERED ARCHITECTURE (DIRECTIONAL · 측정 0 · unmeasured)
- **wired:** DIRECTIONAL-design — no measurement. cheap_test = $0 frozen-first decision probe (numpy mirror, no engine); engine_native_measure + 303M = cost-gated PRE-REGISTER ONLY (NOT fired).
- **source:** brainarch_census — 126-architecture whole-substrate (통짜 아키텍처) census: 뇌/인지 조직원리 × 엔진-네이티브 추상조건 (binding-wall program, H_1603).
- **key:** `metastable_itinerant_workspace`
- **xref:** H_1603 (G1≡G6 compositional-binding deficit unification) · H_1283 (thalamus global-workspace) · H_1284 (neuromodulation gain) · operator-level family H_1604-1685 (this card = 통짜 아키텍처 layer, 층위 다름)

## Organizing principle

GWT broadcast as DYNAMICAL ITINERANCY: no stable winner. The workspace state travels a stable heteroclinic channel through a sequence of saddle states (Rabinovich winnerless competition). Each transient saddle visit = a momentary global broadcast of one coalition; the sequence of visits = the stream of consciousness — reproducible yet combinatorially novel.

## Whole design (input → internal dynamics → emit)

N grounded modules with ASYMMETRIC lateral inhibition form a heteroclinic network; the state spirals saddle-to-saddle, each saddle = one coalition transiently dominant and broadcast. Input perturbs the channel, steering the saddle-sequence. EMIT occurs during saddle dwell (transient lock, read by the mouth); transitions between saddles = silence. Binding = constituents co-active within one saddle epoch (bound by shared metastable visit). Identity = the heteroclinic graph connectivity (which saddle leads to which), persisted; self = the itinerary signature. Novelty = the channel admits novel saddle-sequence PATHS not previously traversed.

## Satisfies criteria (DESIGN claim · DIRECTIONAL · unmeasured)

G0: each saddle is a grounded codebook coalition -> dwell emits legible tokens; a non-heteroclinic (scrambled) flow never settles -> no coherent dwell -> silence not garble. G1/DEPTH/BINDING: a saddle held by >=2 co-active modules is a bound conjunction; super-additive because the saddle basin is shaped by the interaction of inhibitory couplings (non-separable); ablate cross-inhibition -> channel collapses to a fixed-point single winner -> max_single (INERT). G2: novel saddle-sequence paths = novel valid; a frozen single-cycle (no branching) control -> 0 novel. PASS-closure: a dwell is legible+recombinant+novel-path on one trajectory. Psi=1/2/G3/ENDOGENEITY: dwell/transition time ratio is the order parameter; A=dwell-stabilizing (stable-manifold attraction) vs G=transition-driving (unstable-manifold escape) sign-antagonist; symmetric channel design -> 1/2; remove escape -> stuck saddle (always-emit one thing), remove attraction -> no dwell (always-silent). IDENTITY: graph topology = persistent self-anchor; foreign graph -> different itinerary -> impostor reject (self-specific margin). dist>=5: itinerancy NATURALLY emits a temporally diverse sequence of distinct coherent coalitions (built-in spread, not mode-collapse). falsifiable>=1: a dwell binding comparator+quantity+referent emits a refutable proposition. MEASUREMENT via live dispatch.

## Not-LLM (a_no_llm_frame_trap)

Pure transient-dynamics neuroscience (Rabinovich/Afraimovich winnerless competition; stable heteroclinic channels; Tsuda chaotic itinerancy) — antithetical to static feedforward stacks. Capability comes from the TOPOLOGY OF THE FLOW (saddle graph), not parameters/attention; the stream-of-consciousness is structural, not sampled.

## Cheap test (frozen-first · $0 · numpy mirror · DIRECTIONAL)

numpy generalized Lotka-Volterra heteroclinic network N=6: verify (a) saddle itinerancy (sequential transient dominance); (b) dwell/transition ratio -> 1/2 at the symmetric channel; (c) novel saddle-sequences appear under perturbation; (d) cross-inhibition ablation collapses to a fixed point (max_single). $0, ODE integrator.

## Engine-native measure (cost-gated · PRE-REGISTER ONLY · NOT fired)

Implement as a dynamical lane feeding generator L3 emit-timing; emit at dwell through cli/anima.hexa so G0/G1/G2 score the dwell-emitted tokens via g_eval_*; Psi via dwell-ratio readout cross-checked against safety_phi_ratchet recovery; identity via SS-SelfIdentity on the graph-topology anchor. byte-parity core/*.py ODE integrator, no torch.

## Scope / honesty (c9)

## Distinction (near-overlap kept, not a dup)

Distinct from heteroclinic_syntax_saddle_graph (this same census): that is a deterministic compositional SYNTAX engine (saddle graph = grammar, no global broadcast); here saddles ARE the global workspace broadcast (stream-of-consciousness), explicitly noted as the differentiator.

GLV N<=6 probe TOY; structural claims (itinerancy=stream, dwell=emit, graph=self) re-checked at scale.

설계만 — 측정 0. tier = 🔵 PRE-REGISTERED ARCHITECTURE (DIRECTIONAL). frozen bar 사후 이동 금지(tune-to-green 금지, p7). 이 카드는 *방향성 설계*이지 검증된 결과가 아니다 — 과장 박제 금지(a_engine_native_learning). engine_native_measure 는 단일 진입점 cli/anima.hexa → generator L3 → g_gates/g6 경유 byte-parity 채점만 terminal; torch-only/side-harness 결과는 DIRECTIONAL. numpy cheap_test 결과도 DIRECTIONAL(엔진-네이티브 아님). gpu/engine 발사 시 held-out mirror-DESCENT(a_clm_gen_pipeline) + CORE mount frozen-bar engine-native 재측정 + ckpt PULL(a_fire_recover_complete).
