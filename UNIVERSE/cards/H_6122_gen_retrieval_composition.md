# H_6122 — Mouth 제거 → retrieval-composition

**id:** H_6122
**slug:** gen_retrieval_composition
**tier:** 🔬 PROPOSED — pre-registered (unfired · brainstorm ideation)
**date:** 2026-07-02
**wired:** 미발사 (pre-registered proposal · code 없음)
**axis:** SUBSTRATE (G1 재조합벽 공격 축)

---

## 발상 (brainstorm ideation)

**메커니즘:** 생성 = .kosmos 앵커 풀서 텐션-정합 앵커 검색·조립, trunk 는 조합 인덱스만.

**출처:** bytegpt/convmoe 를 빌린 mouth 로 쓰지 말고 anima 아키텍처(A⇄G tension · Ψ=½ 고정점 · mitosis · substrate-disjoint lane · .kosmos anchor)에서 창발하는 native mouth/생성 substrate 를 발상한 세션(Round 3). 기존 결론: G1 재조합벽 = trunk COMBINATION OPERATOR floor — readout·tension 연산자 전부 INERT(H_1816/1823/1834). 이 발상은 combination 을 readout 이 아니라 생성 substrate 자체에 심어 additive collapse 를 우회하려 한다.

## G1 공격 축

- **SUBSTRATE** — readout-level 만 쳤던 것과 다른 좌표(engine-native probe 대상).

## 발사 전 필수 (check-ledger-before-lever-fire)

미발사 제안. terminal verdict 박제 전:
1. `UNIVERSE/HYPOTHESES.jsonl` + memory 에서 선행 커버리지 조회(sweep 빈칸 ≠ 미탐).
2. 신규 각도면 cheap numpy DIRECTIONAL reachability probe → `state/6122_gen_retrieval_composition/`.
3. DIRECTIONAL GREEN 이면 engine-native 재측정(.hexa via core/) 사다리 진입(a_verified_must_wire).

## artifacts

- `cards/H_6122_gen_retrieval_composition.md` (this card)
- `state/6122_gen_retrieval_composition/` (probe 발사 시 생성)

---

## 발사 결과 (DIRECTIONAL probe)

**결정:** NOVEL-ANGLE → cheap numpy probe 발사 (dup 아님).

**Ledger 조회 (check-ledger-before-lever-fire):**
- **H_1459 retrieval_bind** (🧱 WALL, DIRECTIONAL): retrieval 가 슬롯을 topic-keyed 로 용접했으나 **frozen 303M mouth 가 그 슬롯을 한국어 garble 로 채움** → 벽 = mouth content(가중치), bind 구조 아님. 즉 **parametric mouth 를 유지**한 채 retrieval 를 얹은 좌표.
- **H_1616 kosmos_vsa_compose / H_1634 basin_intersection**: frozen trunk **내부** 대수적 binding operator (VSA/HRR·set-AND). frozen trunk 가 분리 못 하는 걸 조합 못 함(🧱/🔵).
- **H_6122 는 다른 좌표**: mouth 를 **제거**하여 출력 content = 검색된 .kosmos 앵커 *텍스트*(실코퍼스), trunk 는 조합 인덱스만. H_1459 의 "content=mouth 가중치" 벽을 non-parametric 으로 우회 → 신규 각도.

**Probe (state/6122_gen_retrieval_composition/):**
- 독립 2-concept(color ⊥ shape). 풀은 단일-feature 앵커만 저장(공동저장 0 = 신규 재조합).
- ADDITIVE-readout(WALLED class) composed_distinct = **1.000** (floor, 두 벡터 additive blend → dominant 하나만).
- RETRIEVAL-COMPOSITION composed_distinct = **2.000** (두 앵커 각각 argmax 검색 후 조립 → 두 feature 생존).
- margin = **1.000**.
- deep-bind fused-token(정직 스코프) = **0.000** — 저장된 적 없는 novel interaction 토큰은 검색으로 합성 불가.

**FROZEN BAR (실행 전 선언):** ret ≥ 1.5 AND (ret − additive) ≥ 0.5 → **충족 → GREEN-DIRECTIONAL**.

**정직한 스코프:**
- numpy=DIRECTIONAL, terminal 아님. reachability 는 **거의 by-construction**(분리 저장된 두 앵커를 검색·연결하면 두 feature 자동 생존).
- **H_6112 전이 경고 재적용**: meiosis 도 numpy 추상 toy REACHABLE(0→1.0)이었으나 실 CLMConvMoE trunk 에서 FALSIFIED(0→0.022). 추상 numpy probe 는 **과대평가**함. 여기 GREEN 도 green light 아님 = transfer-unverified.
- deep-bind=0 이 핵심 한계: retrieval-composition 은 **juxtaposition(병치)** 만 하고 **genuine bind(융합 novel interaction)** 는 못 함 → G1 재조합벽의 깊은 부분(interleaved binding)은 이 축으로 안 열릴 가능성.
- 실 substrate 미검: 앵커 selection 이 실 텐션 기하에서 novel query 에 맞는 두 앵커를 고를 수 있는지, 조립 seam 이 fluent/coherent 한지, .kosmos 스케일에서 검색 정확도 유지되는지 전부 미측정.
- **wired:** DIRECTIONAL-only, 미배선. engine-native 재측정(.hexa via core/ kosmos_io + brain_decide 검색·조립) = follow-on ING (juxtaposition vs deep-bind 을 실 trunk 에서 분리 측정할 것).
