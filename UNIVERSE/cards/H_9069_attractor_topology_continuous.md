# H_9069 — Ring/line continuous attractors: topology of the state manifold as a capability metric

- **tier:** ⏳ PROPOSED (brainstorm-depletion 2026-07-02)
- **slug:** `attractor_topology_continuous`
- **frontier:** manifold-geometry (penult 비가산화 escape — frontier b)
- **cost:** cheap ($0 engine-native, live core/)
- **wired:** `PROPOSED` — 미측정, 미배선. 등록만(2-surface: 이 카드 + HYPOTHESES.jsonl).

## 메커니즘 (mechanism)

Beyond the Ψ=½ POINT attractor, ask whether the substrate can form CONTINUOUS attractors (ring/line/plane) to hold a continuous variable (heading/phase/analog quantity) in working memory. Capability = analog persistence, measurable by the topology (Betti numbers) of the trajectory manifold — not by any text readout.

## engine-native falsifiable metric (shuffle + ablation, p7)

Persistent-homology (b1 for a ring) of live-core state trajectories while holding a continuous cue; capability iff a stable 1-cycle persists and decoded angle tracks the cue with low drift. SHUFFLE: randomize cue-time bins → b1 collapses. ABLATION: remove the recurrent lateral coupling → attractor decays to a point (drift → uniform).

> p7 준수: LLM-judge/perplexity 없음. 판정은 live `core/` 상태·디코드에서 산출된 수치 + shuffle-null + ablation-INERT 로만. 사전등록 bar frozen-first (사후 이동 금지, c9).

## why novel vs ledger

All prior Ψ work treats the fixed point as a single point attractor; continuous-attractor topology (analog memory) is a new capability lens and a distinct manifold-geometry probe (topological, not additivity-of-composition).

## disjointness (a_substrate_disjoint)

새 lane/op 은 emit-drive lane(0/4) 및 §ImmuneMemory recall_thr 와 **disjoint** 좌표에 배선(placement-first) — 능력 ∧ Ψ=½ ∧ G5 non-fab 공존 점검이 측정의 사전조건.

## 상태

- 출처: brainstorm-depletion 2026-07-02 (6-round ledger-dedup 생존).
- 다음 단계: engine-native 프로브 저작(.hexa via live core/) → frozen bar 측정 → verdict 박제.
