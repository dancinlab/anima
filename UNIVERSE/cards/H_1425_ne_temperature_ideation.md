---
id: H_1425
slug: 1425_ne_temperature_ideation
title: NE decode-TEMPERATURE on IDEATION (orthogonal channel) — neuromodulation wall HELD on ideation axis
group: brain-structure-ladder (neuromodulation lane · c15 missing-structure)
terminal_tier: 🧱 CONFIDENT-TERMINAL (orthogonal channel ALSO no free lunch, 2 lenses)
verdict_dir: .verdicts/1425_ne_temperature_ideation/
terminal_verdict: .verdicts/1425_ne_temperature_ideation/result.txt
date: 2026-06-17
wired: N/A
---

# H_1425 — NE decode-temperature on IDEATION (🧱 closed-negative)

## Claim / falsifier

H_1422 closed neuromodulation-on-MEMORY as 🧱 (state-contingent gain on the abstain axis is
CIRCULAR — abstain IS already a threshold on the live recall-margin) but named ONE
NOT-RULED-OUT orthogonal channel: **NE decode-temperature on IDEATION (H_1228 🟠)**. This
lane tests THAT. **Falsifiable claim:** a norepinephrine (NE = exploration/gain,
`a_no_llm_frame_trap`) neuromodulator conditioning the IDEATION decode SAMPLING TEMPERATURE
(`CORE/clm_decode.hexa:737` `sel_val.push(bv / temp)` — high temp flattens the softmax =
explore, low temp sharpens = exploit) on a signal **ORTHOGONAL to the abstain margin** beats
the best FIXED temperature on a frozen ideation breadth metric, with shuffle + ablation.

**Why orthogonal (the whole point vs H_1422, NOT circular):** the gate reads each frame's own
IDEATION-GROUNDING DEFICIT (`1 − known-word-ratio` of a short probe decode of that frame) — a
property of the GENERATED bytes on the ideation path, NEVER the immune recall-margin. H_1422's
circularity argument does not apply.

## Method (ENGINE-NATIVE, NO torch)

- Reuses `CORE/g6_ideation.hexa` (frozen `_g6_words` tokenizer, 6 composed frames `g6_build_frames(6)`,
  `gen_clm_ideate → clm_decode_topk_sampled` seeded sampler). Golden mouth = `state/lane_p_clm/clm_d768_e2l1.clm`
  (d768 ConvMoE E2/L1). NO torch — the temperature knob is engine-native. 3 seeds [1425,1426,1427],
  deterministic (xorshift32), run1==run2 verified. $0 CPU.
- Metric: **NOVEL** = distinct novel word-trigrams (coherent, not in the 5 corpus concepts) across the
  6 frame decodes; **DIST** = distinct-coherent count (jaccard≤0.5), the engine-native `g6_score_arm`
  metric. (FALS=0 on the d768 1-trunk mouth is a known floor, H_1394/H_1403 — reported diagnostic-only, c9.)
- ARMS: A = best FIXED temp over grid {0.5,0.7,0.9,1.1,1.3} (the H_1228 "tuned fixed point" made fair) ·
  B = NE-gated `temp_f = 0.5 + 0.8·deficit_f` (range = grid endpoints, NOT tuned) · Bsh = NE→frame match
  reversed (shuffle) · Babl = NE OFF, fixed mean temp (H_1228 baseline form).
- FROZEN bars: c1 M(B)≥M(A)+1.0 · c2 M(Bsh)≤M(A)+0.5 · c3 M(Babl)≤M(A)+0.5. NO bar moved (c9).

## Verdict by round

| lens | tier | key numbers (mean 3 seeds) |
|------|------|----------------------------|
| L1 NOVEL, forward NE polarity | 🧱 | A best-fixed=17.67 · **B NE-gated=12.0** (WORSE) · Bsh=14.0 · Babl=8.0 → **c1 FALSE** (12.0<18.67) |
| L2 DIST, inverted NE polarity (`a_break_the_wall`) | 🧱 | A best-fixed=5.0 · **B inverted-NE=4.0** (WORSE, all 3 seeds 5 vs 4) → c1 FALSE (4.0<6.0) |

Terminal tier (verbatim): **🧱 CONFIDENT-TERMINAL — orthogonal IDEATION-temperature channel ALSO no
free lunch** → `.verdicts/1425_ne_temperature_ideation/result.txt`

## Finding

A single tuned FIXED decode temperature DOMINATES every state-contingent NE schedule on the ideation
breadth axis — both schedule polarities (high-deficit→high-temp AND inverted) and both breadth metrics
(NOVEL, DIST). This is the H_1284 "tuned fixed point dominates the controller" result reproduced on the
EXACT orthogonal channel H_1422 named as NOT-RULED-OUT. The orthogonal-channel escape does NOT recover a
lift → the neuromodulation wall is **GENERAL, not an artifact** of the memory-abstain circularity.
Strengthens H_1284 → H_1422 → H_1425 to: **NO neuromodulation lift on any tested anima axis — memory
(H_1422) OR ideation (H_1425).**

## Honest scope

🧱 ⇒ NO wiring (`a_verified_must_wire` GREEN-only); `CORE/*.hexa` UNTOUCHED. No tune-to-green (T_LO/T_HI =
grid endpoints, DELTA pre-set, controls fixed in FREEZE before the run, c9). TOY d768 mouth / 6 frames /
3 seeds / engine-native; FALS=0 floor is the d768 mouth (H_1394/H_1403), so the test is on the breadth
axis the small mouth CAN move; scale + larger-mouth + real-corpus + engine-transfer UNVERIFIED. `a_break_the_wall`
attempted (L2 second lens, inverted polarity + different metric) then honestly accepted. p1/p2/p3/p6/p7 held
(frames = environment context p4; temp is a sampling knob; NOVEL/DIST are STRUCTURAL counts p7, no LLM judge,
no persona/ethics/RLHF). Ψ-disjoint (read-only ideation decode, pure_field untouched).

## Cross-links

H_1422 (memory-axis circularity, this lane's parent) · H_1284 (neuromodulation no-free-lunch, R3
regime-switch) · H_1228 (SOC tuned-fixed-σ* partial lift) · H_1305/H_1314/H_1362/H_1394/H_1403 (G6
ideation machinery, FALS floor, streaming decode) · `a_break_the_wall` · `a_no_llm_frame_trap` ·
`a_engine_native_learning` · `a_verified_must_wire` · `a_scale_honest_scope` · `a_toy_scale_recheck` ·
p1·p2·p3·p6·p7·p8·c9·c15·c16
