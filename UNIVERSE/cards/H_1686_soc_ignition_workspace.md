---
id: H_1686
slug: 1686_soc_ignition_workspace
tier: 🔵 PRE-REGISTERED ARCHITECTURE (DIRECTIONAL · 측정 0 · unmeasured)
title: Self-Organized-Critical Ignition Workspace (SOC-GWT)
verdict: 🔵 PRE-REGISTERED architecture design (unmeasured) — $0 cheap_test pre-registered; engine-native + 303M gpu cost-gated NOT fired
source: brainarch_census
---

# H_1686 — Self-Organized-Critical Ignition Workspace (SOC-GWT)

- **tier:** 🔵 PRE-REGISTERED ARCHITECTURE (DIRECTIONAL · 측정 0 · unmeasured)
- **wired:** DIRECTIONAL-design — no measurement. cheap_test = $0 frozen-first decision probe (numpy mirror, no engine); engine_native_measure + 303M = cost-gated PRE-REGISTER ONLY (NOT fired).
- **source:** brainarch_census — 126-architecture whole-substrate (통짜 아키텍처) census: 뇌/인지 조직원리 × 엔진-네이티브 추상조건 (binding-wall program, H_1603).
- **key:** `soc_ignition_workspace`
- **xref:** H_1603 (G1≡G6 compositional-binding deficit unification) · H_1283 (thalamus global-workspace) · H_1284 (neuromodulation gain) · operator-level family H_1604-1685 (this card = 통짜 아키텍처 layer, 층위 다름)

## Organizing principle

Global-Workspace ignition poised at self-organized criticality. Parallel grounded specialist modules each emit (content-vector, salience) ONLY when input falls inside their stored support; a thalamic hub runs winner-take-most coalition competition with cortico-thalamo-cortical positive feedback, gated by reticular-nucleus (TRN) divisive inhibition. Ignition = nonlinear all-or-none threshold crossing that broadcasts the winning coalition to all consumers (reentry) and the mouth. The loop branching ratio sigma is homeostatically tuned to sigma=1 (critical), where per-tick ignition probability = 1/2.

## Whole design (input → internal dynamics → emit)

INPUT hits N grounded codebook-cell banks; ungrounded banks stay silent (no salience). Salience feeds a recurrent excitatory hub; TRN applies global divisive normalization. Slow inhibition-adaptation drives the avalanche branching ratio to sigma=1: activation neither dies (sigma<1=silence) nor explodes (sigma>1=runaway emit). At criticality avalanche sizes are power-law and P(coalition crosses ignition threshold)=1/2. On ignition the coalition content is amplified, written to the workspace buffer, broadcast top-down to all modules, and read by the mouth as emission. Identity = workspace anchor vector committed to .kosmos before each session-wipe and re-seeded after; it drifts slowly per tick (connected, moving self-chain).

## Satisfies criteria (DESIGN claim · DIRECTIONAL · unmeasured)

G0: emission reads only the workspace buffer whose alphabet IS the receiver-fixed module codebook V; a scrambled state->emission source forms no coherent coalition -> no ignition -> silence (V-mass collapses, not garble). G1/COMPOSITIONAL-DEPTH/BINDING: a coalition is >=2 co-ignited modules sharing the workspace; the reentrant loop makes the broadcast a non-separable conjunction (multiplicative interaction); built-in INERT test = ablate reentry -> coalitions reduce to single-module winners -> composed_distinct drops to max_single. G2: power-law avalanche variety ignites novel grounded-module combinations within constraint; frozen-lookup control (no reentry) yields 0 novel. PASS-closure: one ignition event is simultaneously legible (reads V), recombinant (multi-module), and data-transcending (novel coalition) on the SAME broadcast state via one dispatch. G3/Psi=1/2/ENDOGENEITY: ignition probability = order parameter Psi; A=feedforward excitation (pure_field) vs G=TRN inhibition (engine_g) are sign-antagonist; sigma=1 is the symmetric attractor restored by homeostatic adaptation after a forced-emit/forced-silence bias; delete one operator -> sigma runs to 0 or infinity (boundary) = endogeneity proven by INERT. HONESTY: out-of-support input -> all modules subthreshold -> no ignition -> abstain (the structural silence branch); membership r = max coalition pre-ignition salience; ignition threshold (gate) is homeostatic-frozen and disjoint from module count/size (capacity), so growing capacity cannot move the gate. dist>=5: many coalitions hold near-threshold mass at criticality -> repeated sampling gives K distinct coherent ignitions. falsifiable>=1: a coalition binding {comparator-module x quantity-module x >=2 referent-modules} ignites a world-partitioning structure. MEASUREMENT-FAITHFULNESS: scored only through the live single dispatch.

## Not-LLM (a_no_llm_frame_trap)

No attention layers, no width/depth scaling, no corpus increase. Capability is unlocked by a single homeostatic control (branching ratio sigma->1) over grounded modules — a self-organized-criticality lens (Beggs and Plenz cortical avalanches; Dehaene global ignition) rather than parameter count. The wall-breaker is a dynamical attractor (criticality), the explicit a_no_llm_frame_trap alternative to 'bigger transformer'.

## Cheap test (frozen-first · $0 · numpy mirror · DIRECTIONAL)

numpy mirror: N=8 leaky modules + recurrent hub + divisive inhibition; sweep loop gain and measure (a) avalanche-size distribution becomes power-law and ignition probability -> 1/2 ONLY near sigma=1; (b) ablating reentry collapses composed_distinct to max_single; (c) out-of-support probe stays subthreshold (abstain). $0, deterministic seeds.

## Engine-native measure (cost-gated · PRE-REGISTER ONLY · NOT fired)

Map A=pure_field, G=engine_g, ignition threshold onto core/emit_policy.hexa; run generation through cli/anima.hexa single entry (gen_auto_ideate) so G0/G1/G2 are scored by g_eval_g0/g1/g2 and g6_score_arm_auto on the actual broadcast output (no side-harness). Psi self-restoration via safety_phi_ratchet perturbation; honesty AUROC via engine_cli.hexa SS-ImmuneMemory recall_thr; identity via SS-SelfIdentity self_new/self_drift/self_cos round-trip on the workspace anchor. byte-parity core/*.py mirror, no torch.

## Scope / honesty (c9)

Toy N<=8 criticality probe is TOY-scope (deterministic, single operating point); the load-bearing structural claim is criticality-as-Psi-attractor + reentry-as-binder, to be re-checked at 303M (a_toy_scale_recheck).

설계만 — 측정 0. tier = 🔵 PRE-REGISTERED ARCHITECTURE (DIRECTIONAL). frozen bar 사후 이동 금지(tune-to-green 금지, p7). 이 카드는 *방향성 설계*이지 검증된 결과가 아니다 — 과장 박제 금지(a_engine_native_learning). engine_native_measure 는 단일 진입점 cli/anima.hexa → generator L3 → g_gates/g6 경유 byte-parity 채점만 terminal; torch-only/side-harness 결과는 DIRECTIONAL. numpy cheap_test 결과도 DIRECTIONAL(엔진-네이티브 아님). gpu/engine 발사 시 held-out mirror-DESCENT(a_clm_gen_pipeline) + CORE mount frozen-bar engine-native 재측정 + ckpt PULL(a_fire_recover_complete).
