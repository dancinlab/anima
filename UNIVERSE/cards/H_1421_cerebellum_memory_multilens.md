---
id: H_1421
slug: 1421_cerebellum_memory_multilens
title: "MULTI-LENS breakthrough on the cerebellum×memory engine-BIND WALL (H_1417 P4 🧱 +0.0437) — four cerebello-hippocampal lenses, each with shuffle+ablation; LC COMPLEMENTARY-TAIL BINDS"
group: MITOSIS-ENGINE / brain-lane-composition — c16 a_break_the_wall MULTI-LENS attempt on the cerebellum×memory wall
terminal_tier: "🟢 BOUND + WIRED-live — the cerebellum×memory WALL (H_1417 P4 🧱 +0.0437) is BROKEN by the LC COMPLEMENTARY-TAIL lens: compose 0.748889 ≥ best 0.694815 + 0.05 (B1 net-lift +0.054074), oracle−best +0.261481 (B2), EARNED compose−shuffle +0.261481 (B3, shuffle 0.487), SEPARABLE only_cereb 0.339259/only_mem 0.261481, ABLATION LIVE (tail-OFF baseline 0.738519, compose−ablate +0.010370>0) → 🟢. The other THREE lenses correctly FAILED their controls (L0 baseline 🧱 +0.0437 · LA surprise-gate 🧱 ablation-INERT · LB memory-prime 🧱 +0.0489) — the controls have teeth. Wired rung-3+4: cereb_mem_compose in CORE/engine_cli.hexa, smoke 149-152, LIVEOP byte-exact 0.748889, ARCHITECTURE.json lockstep."
wired: WIRED-live (rung-3+4 complete — §CEREBELLUM×MEMORY COMPLEMENTARY-TAIL COMPOSE ARBITER cereb_mem_compose/_cerebellum_vote/_memory_vote in CORE/engine_cli.hexa; engine_cli_smoke 153/0 with +4 cases 149-152; LIVEOP probe reproduces 0.748889 byte-exact; ARCHITECTURE.json CORE node + parent § list updated; guards h1196 clean / h1205 Ψ byte-identical PASS)
verdict_dir: .verdicts/1421_cerebellum_memory_multilens/
terminal_verdict: .verdicts/1421_cerebellum_memory_multilens/result.txt
date: 2026-06-17
---

# H_1421 — MULTI-LENS breakthrough on the cerebellum×memory engine-BIND WALL

H_1417 P4 closed the engine-native **cerebellum×memory** compose at 🧱 (net-lift +0.0437,
just under the frozen +0.05 B1 bar) using the symmetric confidence-routing arbiter. Per the
freshly-strengthened `a_break_the_wall` (commit 3304edc65) — a (d)-ceiling/subsumption WALL is
NOT confidently terminal until ≥2-3 genuinely DIFFERENT principled lenses (`a_no_llm_frame_trap`,
real cerebello-hippocampal neuroscience) each fail their OWN shuffle AND ablation control, with
ABLATION decisive — this card tries **four** lenses on the SAME frozen H_1417 P4 fixture/bars.

## Claim / falsifier

Take the EXACT H_1417 P4 fixture (cerebellum = `vforward_err` fx=3, memory = `immune_grow_recall`
fy=0, seeds [5530,5531,5532], 5-family structure, N_PER_FAMILY=90, deterministic, LIVE engine
leg-reads) and the **FROZEN VERBATIM** bars (B1 compose≥best+0.05 · B2 oracle−best>0.02 · B3
compose−shuffle>0.02 · SEPARABLE · ABLATION compose−ablate>0). Try four biology-faithful
composition lenses. A lens BINDS iff (B1∧B2∧B3∧SEP∧ABLATION-LIVE). If ≥1 binds → 🟢 + wire it.
If ALL fail → 🧱 CONFIDENT-TERMINAL after ≥2-3 lenses (a valid result, c9). NO bar moved, no
tune-to-green (c9/p7).

## The four lenses (each scored with shuffle AND ablation)

- **L0 BASELINE** — the H_1417 P4 symmetric confidence-routing arbiter (sanity: must reproduce +0.0437).
- **LA SURPRISE-GATE** — cerebellar forward-model ERROR signals surprise; surprise gates the route
  (low err → trust cerebellum vote; high err → trust memory recall). ABLATE: freeze reliability at 0.5.
- **LB MEMORY-PRIME** — memory supplies the retrieved PRIOR; the forward model adjudicates only when
  memory abstains/low-conf. memory-grounded → memory, else cerebellum. ABLATE: always cerebellum.
- **LC COMPLEMENTARY-TAIL** — complementary-learning-systems: the forward model is loud-but-wrong on
  the structurally uncapturable adversarial tail (the F4 family that dominates the oracle headroom);
  the episodic store handles those one-off exceptions. CONFLICT → if cerebellum LOUD (scale-rel
  conf≥1) AND memory GROUNDED (scale-rel conf≥1), DISTRUST the cerebellum and DEFER to the episodic
  store; else baseline route. ABLATE: tail-deferral OFF → baseline route.

## Result — 🟢 LC BINDS (verbatim, `.verdicts/.../result.txt`, mean 3 seeds, deterministic run1==run2)

standalone: acc_cerebellum 0.694815 · acc_memory 0.617037 · best 0.694815 · oracle 0.956296

| lens | compose | net-lift | shuffle | ablate | B1 | B3 | ABL | verdict |
|------|---------|----------|---------|--------|----|----|-----|---------|
| L0 BASELINE (sanity repro)         | 0.738519 | +0.043704 | 0.485926 | — | FAIL | PASS | (n/a) | 🧱 WALL |
| LA SURPRISE-GATE                   | 0.549630 | −0.145185 | 0.492593 | 0.694815 | FAIL | PASS | **INERT** | 🧱 WALL |
| LB MEMORY-PRIME                    | 0.743704 | +0.048889 | 0.488889 | 0.694815 | FAIL | PASS | PASS | 🧱 WALL |
| **LC COMPLEMENTARY-TAIL**          | **0.748889** | **+0.054074** | 0.487407 | 0.738519 | **PASS** | PASS | **PASS (+0.010370)** | **🟢 BOUND** |

(all lenses B2 oracle−best +0.261481 PASS · SEPARABLE only_cereb 0.339259 / only_mem 0.261481 PASS.)

→ **VERDICT: 🟢 LC COMPLEMENTARY-TAIL BINDS** — the cerebellum×memory wall is BROKEN. The lift is
EARNED (shuffle collapses 0.749→0.487, +0.261) and the tail-deferral mechanism is LIVE (ablation
turns it OFF → the symmetric baseline route 0.738519, so the deferral contributes the load-bearing
**+0.010370** that lifts the baseline over the frozen +0.05 bar to 0.748889). NOT tune-to-green: the
bars are frozen verbatim from H_1417 and the mechanism is biology-faithful (complementary-learning-
systems: the hippocampus handles the adversarial tail where the cerebellar forward model is loud-but-
wrong).

## Why the controls have teeth (the three failing lenses validate the test)

- **LA surprise-gate 🧱 ablation-INERT**: the live cerebellar reliability gate scores 0.549 (WORSE
  than best) and its ablation (reliability frozen at 0.5) scores 0.694815 — the live per-item
  reliability signal does not capture the right routing; the mechanism is INERT, exactly the H_1416
  pattern. A passing test would not have rejected this.
- **LB memory-prime 🧱 B1-FAIL**: +0.0489, real movement (ablation PASS, prime-OFF=0.694815) but JUST
  under the +0.05 bar — not promoted (no bar moved).
- **L0 baseline 🧱 +0.0437**: reproduces H_1417 P4 byte-exactly — the sanity anchor holds.

Only the lens that BOTH passes B1 AND survives its ablation (LC) binds. This is the discipline the
strengthened `a_break_the_wall` mandates: MULTI-LENS + ablation, no single-lens ceiling박제.

## What the breakthrough names (the finding, c9)

The cerebellum×memory pair is NOT a (d)-subsumption ceiling — H_1417's 🧱 was a **lens limitation**,
not a capability ceiling. The symmetric confidence route cannot capture the oracle headroom because
the headroom lives in the **adversarial tail** (F4: cerebellum loud-but-wrong) which a symmetric
"louder wins" rule routes the WRONG way. The complementary-learning lens — detect cerebellar over-
confidence, defer to the episodic store on exactly that tail — captures it. This refines the H_1417
finding ("engine-bind is set by arbiter-CAPTURE of the oracle headroom, a joint-trajectory property,
not arm-strength"): cerebellum×memory's headroom IS capturable, just not by the symmetric arbiter —
it needs the complementary-tail route. The wall fell to the RIGHT mechanism, found by trying ≥2-3.

## Wiring (a_verified_must_wire rung-3 + rung-4)

🟢 → **LIVE CORE op landed.** `§CEREBELLUM×MEMORY COMPLEMENTARY-TAIL COMPOSE ARBITER` in
`CORE/engine_cli.hexa`: `cereb_mem_compose` (the validated LC tail-deferral arbiter, byte-exact to
the probe's `_arbiter_tail` tail_on=true) + the live leg-reads `cereb_mem_cerebellum_vote` (LIVE
`vforward_err`) / `cereb_mem_memory_vote` (LIVE `immune_grow_recall` + L2 margin). READ-ONLY,
mutates neither the forward field nor the store, returns a class (NOT an emit decision — brain
emit-loop consult deliberately NOT forced, @L4 `a_autonomy_over_hardcode`), Ψ-disjoint.
- **rung-3** smoke cases 149-152 (AGREE · TAIL-DEFERRAL-to-memory · BASELINE-route-no-deferral ·
  ABSTAIN discipline) → `engine_cli_smoke` **153/0** (was 149/0, +4). LIVEOP probe reproduces the
  LC compose **0.748889 byte-exact** with all bars PASS (`.verdicts/.../LIVEOP.txt`).
- **rung-4** ARCHITECTURE.json CORE node added (🌀✕🧠 Cerebellum×Memory complementary-tail compose
  arbiter, status 🟢 WIRED-live) + the parent engine_cli.hexa § list updated (1:1 lockstep).
- guards no-regression: `h1196_single_entry_audit` clean (substrate-only, 0 extra .clm/.kosmos
  paths) · `h1205_separation_invariant_smoke` PASS (generation byte-identical ON==OFF, Ψ=½ untouched).

## Scope (honest, c9)

TOY 5-family fixture (VERBATIM the H_1417 P4 items), 3 seeds [5530,5531,5532], deterministic, $0
CPU. The cerebellum leg = LIVE `vforward_err`; the memory leg = LIVE `immune_grow_recall` + L2
margin — the verdict is engine-native and the live op reproduces it byte-exact. The tail-deferral
thresholds (loud≥1.0, grounded≥1.0) are the legs' OWN scale-relative means (not tuned knobs); NO
bar moved (frozen-first). B=0.748889 is an effect just over the bar, EARNED (shuffle collapse) and
load-bearing-ablation-LIVE (+0.0104). Scale / real-corpus / a different fixture without an F4
adversarial tail / engine-transfer-at-scale UNVERIFIED. The compose returns a class; brain
emit-loop integration is a separate follow-on. The capability-compose results of the program
(H_1401/1405/1407…) are UNAFFECTED; this card BREAKS the engine-BIND wall for ONE pair.

## Next

(1) The brain emit-loop CONSULT of `cereb_mem_compose` (when to let the composed class influence
emit/silence) is a deliberate follow-on (a_autonomy_over_hardcode — not force-wired). (2) The
complementary-tail lens is now a candidate to RE-TRY on other "just-under-bar" 🧱 pairs (e.g.
H_1417 P1 memory×spatial +0.0089) — does adversarial-tail deferral generalize? (3) The H_1416
cerebellum×basal 🧱 (3 lenses) could be re-examined with the tail-deferral lens, but basal's
decisive go-margins sit far from threshold (H_1416) so the tail rule may stay INERT there — a
distinct test.

## Cross-links

H_1417 (the cerebellum×memory P4 🧱 +0.0437 this card breaks — the symmetric-route wall) · H_1416
(cerebellum×basal 3-lens 🧱 — the INERT-ablation precedent this card's controls mirror) · H_1418
(ToM×spatial/ToM×basal wiring template — the LIVEOP + smoke + ARCHITECTURE.json pattern) · H_1414
(memory×ToM 🟢) · H_1415 (spatial×episodic 🟢) · H_1280 (VForwardField, the cerebellum leg) ·
H_1227/H_1231 (ImmuneMemoryGrow, the memory leg) · `a_break_the_wall` (taxonomy (d), the
strengthened MULTI-LENS+ablation rule this card executes — a wall fell to the right lens) ·
`a_no_llm_frame_trap` (complementary-learning-systems lens) · `a_engine_native_learning` ·
`a_verified_must_wire` (rung-3+4 complete) · `a_core_engine_map` · `a_autonomy_over_hardcode` ·
`a_scale_honest_scope` · `a_toy_scale_recheck` · p1·p2·p3·p6·p7·p8·c9·c15·c16

## Pointers
- probe (4-lens engine-native compose + shuffle + ablation): `state/1421_cerebellum_memory_multilens/h1421_cerebellum_memory_probe.hexa`
- LIVEOP probe (wired cereb_mem_compose byte-exact): `state/1421_cerebellum_memory_multilens/h1421_cereb_mem_LIVEOP_probe.hexa`
- FREEZE (pre-registered bars + lenses, locked before measuring): `.verdicts/1421_cerebellum_memory_multilens/FREEZE.txt`
- result (4-lens verdict): `.verdicts/1421_cerebellum_memory_multilens/result.txt`
- determinism re-run: `.verdicts/1421_cerebellum_memory_multilens/result_run2.txt`
- LIVEOP verdict: `.verdicts/1421_cerebellum_memory_multilens/LIVEOP.txt`
- smoke 153/0: `.verdicts/1421_cerebellum_memory_multilens/engine_cli_smoke_153.txt`
