---
id: H_1369
slug: 1369_cp_2d
title: 2-D categorical perception — does the move-the-cells relocation win (H_1360, 1-D) GENERALIZE to a 2-D feature space with a FIXED TRUE partition (a half-plane LINE u>p shifting p_A=1/3→p_A'=2/3), or does the extra dimension reintroduce the split-only incoherence (H_1364)?
group: cognition-representation (c15 developmental-plasticity + memory-protection-vs-overwrite lens, a_no_llm_frame_trap, a_break_the_wall)
terminal_tier: 🟢 GREEN (R2, MIRROR, DIRECTIONAL) — MOVE-THE-CELLS GENERALIZES TO 2-D. At a FIXED LOW budget (DIM=64/GROW2=48, η=0.15), drifting the residual phase-1 cells' u-coordinate toward the moved boundary LINE u=p_A' lands the discrimination ridge AT p_A' (|peak_u−p_A'| 0.254→0.042, all 3 seeds) AND restores a COHERENT thin concentrated ridge where the split-only ablation (H_1364 mechanism) stays SHORT (0.254) and less concentrated. R1's NCOMP coherence metric SATURATED on the shuffle smear (a grid-filling noise field is trivially 4-connected, NCOMP=1 — the SAME metric-space artifact H_1343 documented), honestly caught by the c3b SHUFFLE control; a_break_the_wall R2 re-specified a BOUNDED ridge-CONCENTRATION metric COH2D that cleanly separates RE-PACK 0.689 / SPLIT-ONLY 0.538 / SHUFFLE 0.000. c1✅ c2'✅ c3'✅ c4'✅. NO bar moved (R1 NCOMP reported as the honest catch, c9/p7)
verdict_dir: .verdicts/1369_cp_2d/
freeze: .verdicts/1369_cp_2d/FREEZE.txt
terminal_verdict: .verdicts/1369_cp_2d/result.txt
date: 2026-06-16
proj_seed: PROJ_SEED H_1369 (1360=parent move-the-cells GREEN in 1-D; 1364=split-only incoherence INTRINSIC at finer N; 1343/1358=prior 2-D Whorf work; 1369 = re-fire of a storm-interrupted lane)
---

# H_1369 — 2-D categorical perception: move-the-cells relocation in a 2-D feature space

## Claim / falsifier

H_1360 (🟢, 1-D) proved CARVING RELOCATION IS MOVE-THE-CELLS: at a FIXED LOW budget, physically
DRIFTING the residual phase-1 prototype POSITIONS toward the moved boundary p_A' lands a COHERENT
single CP peak AT p_A' where budget (H_1340) and decay (H_1352) both failed coherence. H_1364
showed split-only re-growth incoherence is INTRINSIC (worse at finer lattices). BOTH were on a 1-D
categorical AXIS (boundary = a POINT).

**Falsifiable claim:** the move-the-cells win GENERALIZES to a 2-D FEATURE SPACE where the TRUE
category partition is a FIXED, KNOWN geometry — the half-plane VERTICAL LINE u=p (cat = u>p),
shifting p_A=1/3 → p_A'=2/3. The free dimension v is the categorically-IRRELEVANT axis (where
2-D fragmentation can appear). Physically drifting the residual phase-1 cells' u-coordinate toward
the moved line p_A' (move the CELLS in feature space) lands a COHERENT thin discrimination ridge AT
p_A', DISTINCT from the split-only (H_1364) ablation. **Depletion alternative (c9):** if the extra
dimension fragments the ridge even when the cells are moved → move-the-cells is a 1-D-only artifact,
honest 🧱. Lens: c15 developmental plasticity + memory-protection-vs-overwrite (a_no_llm_frame_trap,
a_break_the_wall) — NOT an LLM recipe, NOT a human-cognition claim, a TOY synthetic 2-D continuum.

## Method

`state/cp-2d/h1369_cp_2d.py` — R1 numpy MIRROR (DIRECTIONAL, $0 CPU, gradient-free, 3 seeds
[4333,4334,4335], p7). Reuses family machinery: 2-D RBF population code (H_1343), error-targeted
SPLIT-only Voronoi/mitosis growth (p8, H_1343/H_1360), softmin-vote soft posterior, geometric
RE-PACK of residual phase-1 cells (H_1360). 13×13=169 stimulus grid, K_RBF=8 (DIM=64, LOW, fixed
across arms), phase-1 budget GROW1=48, phase-2 GROW2=48 (NO inflation; identical every arm). TRUE
partition cat(p)=int(u>P_CUT), P_A=1/3→P_A'=2/3.

ONE new mechanism = the 2-D geometric RE-PACK: each phase-1 cell tracks SOURCE 2-D position + BIRTH
PHASE; after EACH phase-2 split every residual phase-1 cell's u drifts u←u+η·(p_A'−u) (clamped at
p_A', v fixed = irrelevant axis untouched), is re-embedded, label re-read from the new line. η FROZEN
at 0.15 (NON-GATING ladder {0.10,0.15,0.25} robust). η=0.0 = split-only re-growth (= the H_1364
ablation). 4 arms: SPLIT-ONLY (η=0.0) · RE-PACK (η=0.15) · NO-RETRAIN (p_A only → must hold old line) ·
SHUFFLE+repack (permuted labels → must collapse). 2-D metrics: DISCRIMINATION FIELD D = max
|Δ posterior| to 4 grid neighbors; RIDGE = nodes ≥0.5·max; peak_u = u of ridge centroid (RELOCATES);
NCOMP = #4-connected ridge components (R1 coherence); R2 COH2D = U_CONC·(1−RIDGE_FRAC) bounded
concentration (R2 coherence).

p1/p2/p3/p6: discrimination reads ONLY representational distance; NO injected boundary at test; the
re-pack keys on BIRTH PHASE + own 2-D source position (structural, NO injected target/persona/RLHF);
labels enter ONLY at training. Live CORE/*.hexa UNTOUCHED (substrate-measurement rung).

## Frozen bars (pre-registered in FREEZE.txt; GREEN iff c1∧c2∧c3∧c4, NO bar moved c9/c16/p7)

- **c1 RELOCATES**: RE-PACK |peak_u − p_A'| ≤ LOC_TOL=0.12 all 3 seeds.
- **c2 COHERENT** (R1): RE-PACK mean NCOMP ≤ 1 (single connected ridge).
- **c3 EARNED** (R1): (3a) no-retrain holds p_A |peak_u−p_A|≤0.12 AND (3b) shuffle NCOMP ≥ 2.
- **c4 DISTINCT vs SPLIT-ONLY** (R1): split-only NCOMP ≥ 2 (4a) OR |peak_u−p_A'| > 0.12 (4b).
- **R2 (a_break_the_wall, frozen-first BEFORE R2 scoring — NOT a relaxation):** bounded coherence
  COH2D = U_CONC·(1−RIDGE_FRAC), U_STD_REF=0.20. c2' RE-PACK COH2D ≥ 0.50 · c3b' SHUFFLE COH2D ≤ 0.20 ·
  c4' RE-PACK COH2D ≥ SPLIT-ONLY+0.10 OR split-only short.

## Verdict (per round)

| round | tier | key numbers |
|---|---|---|
| R1 (NCOMP metric) | CONFOUNDED — c3b SHUFFLE control caught NCOMP saturation | c1✅ c2✅(NCOMP 1.0) c4✅; **c3b ❌** SHUFFLE NCOMP=1.0 (the smear fills 116/169 nodes, trivially 4-connected). The control did its job: NCOMP is the wrong 2-D coherence metric (same unbounded-metric failure as H_1343). NO bar moved (c9) |
| R2 (a_break_the_wall, bounded COH2D) | **🟢 GREEN (MIRROR, DIRECTIONAL)** | **COH2D: RE-PACK 0.689 / SPLIT-ONLY 0.538 / SHUFFLE 0.000.** c1✅ c2'✅ c3'✅ c4'✅ |

**Per-seed (deterministic, all 3 seeds identical):** SPLIT-ONLY peak_u 0.412 (|·−p_A'|=0.254) / RE-PACK
peak_u 0.708 (|·−p_A'|=0.042) / no-retrain peak_u 0.375 (|·−p_A|=0.042); COH2D RE-PACK 0.689,
SPLIT-ONLY 0.538, SHUFFLE 0.000. Re-pack-ladder (NON-GATING) η=0.10/0.15/0.25 all → |peak_u−p_A'| 0.042
/ NCOMP 1.0 (robust, not a knife-edge).

- **c1 RELOCATES ✅**: RE-PACK |peak_u−p_A'| = [0.042,0.042,0.042] ≤ 0.12 — the ridge lands essentially
  ON the moved line p_A'=0.667 (peak_u 0.708). SPLIT-ONLY stays at 0.412 (|·−p_A'|=0.254, short).
- **c2'/c3'/c4' (R2 bounded) ✅**: RE-PACK COH2D 0.689 ≥ 0.50 (thin concentrated ridge, u-spread std
  0.042); SHUFFLE COH2D 0.000 ≤ 0.20 (the smear's u-spread std 0.281 ≥ U_STD_REF → U_CONC=0 → COH2D=0
  — the move does NOT fabricate a concentrated ridge from permuted labels); RE-PACK 0.689 ≥ SPLIT-ONLY
  0.538 + 0.10 (move-the-cells is MORE concentrated AND split-only stays short). no-retrain held the old
  line (|peak_u−p_A|=0.042).

**FINDING.** The 1-D move-the-cells win (H_1360) GENERALIZES to a 2-D feature space with a fixed true
half-plane partition: drifting the residual phase-1 cells along the categorically-relevant u-axis toward
the moved line lands a coherent, thin, concentrated discrimination ridge AT p_A', where the split-only
re-growth (H_1364 mechanism) stays short and less concentrated. The extra dimension does NOT reintroduce
the incoherence — the relocation residual is a GEOMETRIC-PLACEMENT problem in 2-D just as in 1-D. The
load-bearing honesty (c9): R1's NCOMP control correctly EXPOSED that connectivity-count saturates on a
grid-filling shuffle smear (the H_1343 metric-space failure mode); a_break_the_wall replaced it with a
BOUNDED concentration metric (frozen-first, NOT a relaxation) that cleanly discriminates signal (0.689)
from smear (0.000). NO bar moved (R1 NCOMP reported verbatim as the catch, c9/c16/p7).

## Honest scope (a_scale_honest_scope / a_toy_scale_recheck)

DIRECTIONAL numpy mirror — engine-transfer UNVERIFIED (same family as H_1333/H_1340/H_1343/H_1352/
H_1360/H_1364 R1). TOY synthetic 2-D continuum (169 stimuli, 3 seeds, single shift, one frozen η,
deterministic readout — tests the 2-D move-the-cells STRUCTURE, not a learned drift). AXIS-ALIGNED
half-plane boundary ONLY — the relocation drifts along the single relevant u-axis. DIAGONAL/CURVED 2-D
boundaries (where the irrelevant-axis decomposition does NOT hold and H_1343 showed strong warp),
higher-D, real corpora, multi-shift, a LEARNED (gradient) drift, engine-native §CategoricalPerception
move-the-cells wiring (a_verified_must_wire) = follow-on. NO human-cognition claim. Live CORE/*.hexa
UNTOUCHED. Honest residual: SPLIT-ONLY also produced NCOMP=1 (a connected ridge) — c4 distinctness rests
on split-only staying SHORT of p_A' (4b, |·−p_A'|=0.254) AND less concentrated (c4', 0.538<0.689); the
move's distinctness is the RELOCATION + concentration, not the connectivity.

## Next / depletion

R3 candidates (each frozen ANEW, NOT a relaxation): (1) **DIAGONAL 2-D boundary** move-the-cells — the
decisive 2-D test (drift along the boundary NORMAL, not a single axis; H_1343 showed diagonal warps as
strongly as axis-aligned — does move-the-cells survive a non-axis-aligned partition?). (2) higher-D
(3-D+) relocation. (3) engine-native §CategoricalPerception move-the-cells on the live A⇄G immune store
(a_engine_native_learning · a_verified_must_wire). **CP-lane DEPLETION TEST:** the lane DEPLETES when
move-the-cells either (a) is shown engine-native (transfer no longer DIRECTIONAL) OR (b) hits an honest
🧱 on a DIAGONAL/curved boundary where the axis decomposition fails — i.e. when no FROZEN, control-
surviving relocation angle is left that is BOTH (i) a new boundary geometry vs the axis-aligned cases
AND (ii) not already covered by the 1-D (H_1360) / 2-D-axis-aligned (H_1369) wins.

## Pointers

- code: `state/cp-2d/h1369_cp_2d.py`
- freeze: `.verdicts/1369_cp_2d/FREEZE.txt` (R1 + R2 §) · result: `.verdicts/1369_cp_2d/result.txt`
- `CLAIMS.tape` @C h1369_cp_2d · `UNIVERSE/HYPOTHESES.jsonl` · `domains/COGNITION-REPRESENTATION.log.md`
- xref H_1360 (1-D move-the-cells parent GREEN) · H_1364 (split-only incoherence INTRINSIC, the
  ablation mechanism) · H_1343 (2-D warp, the BOUNDED-metric prescription + diagonal-warp finding) ·
  H_1340/H_1352 (budget/decay walls) · a_no_llm_frame_trap · a_break_the_wall · a_engine_native_learning ·
  a_verified_must_wire · a_scale_honest_scope · a_toy_scale_recheck · p1·p2·p3·p6·p7·p8·c9·c15·c16.
