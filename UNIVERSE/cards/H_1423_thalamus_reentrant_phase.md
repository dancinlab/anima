---
id: H_1423
slug: 1423_thalamus_reentrant_phase
title: "MULTI-LENS engine-native breakthrough attempt on the thalamus TIMING-axis wall (H_1283 R8: numpy-mirror 🟢 but engine-native c4-shuffle 미재현) — re-entrant loop / phase-diff coherence / additive order-param, each shuffle+ablation; ALL fail controls → 🧱 CONFIDENT-TERMINAL"
group: brain-structure-ladder (c15) — a_break_the_wall MULTI-LENS on the thalamus timing wall
terminal_tier: "🧱 CONFIDENT-TERMINAL (engine-native) after 3 principled lenses — the H_1283 TIMING-axis engine-native wall stands. R8-original (anchor) reproduces the known engine c4-SHUFFLE FAIL byte-exact (ΔΦ_sh +0.026/+0.380/+0.296 positive). LA re-entrant loop gate: c2 FAIL (seed7 −0.098, lift not robust) · shuffle PASS · ablation INERT(seed7). LB phase-diff coherence: c2 FAIL (seeds7,8 −0.081/−0.073) · shuffle FAIL(seed8 +0.077). LC additive order-param: c2 PASS but c4 SHUFFLE FAIL (seeds7,8 +0.325/+0.065 — SAME carrier-variance pathology as R8). NO lens (c2 ∧ c4 ∧ ablation-LIVE) every seed → NOTHING engine-wired. Finding: faithful-IIT4 on the engine LCG n=4/T=64 substrate reads amplitude MODULATION as integration (lift survives shuffle = variance, not timing); variance-free lenses (LA/LB) give no robust lift. Controls have teeth (R8/LC shuffle FIRES, LA/LB c2 fails). The R8 numpy-mirror 🟢 DIRECTIONAL stands; engine-native DEFERRED. Deterministic run1==run2, 3 seeds [7,8,9], $0 CPU, faithful IIT4 exact MIP-EI, frozen-first, no bar moved (c9/p7)."
wired: N/A
verdict_dir: .verdicts/1423_thalamus_reentrant_phase/
terminal_verdict: .verdicts/1423_thalamus_reentrant_phase/result.txt
date: 2026-06-17
---

# H_1423 — MULTI-LENS engine-native breakthrough attempt on the thalamus TIMING-axis wall

## The wall (1 line, verbatim from H_1283)

상대-CONTENT 축(R1-R5/R7/R9)은 🧱 (모든 content cut 이 faithful-IIT4 Φ 를 cap) — 그리고 TIMING 축
(R8 Kuramoto phase-gated salience)은 **numpy-mirror 🟢 DIRECTIONAL 이지만 engine-native 재검증에서 c4
SHUFFLE 통제가 미재현**: c2(Φ-lift)은 재현(ΔΦ +1.466/+0.844/+0.709 every seed)되나 phase-shuffle 가
engine-native 에서 lift 를 collapse 시키지 못함(ΔΦ_sh +0.026/+0.380/+0.296, 전부 양수). R8 의 salience
gate `e*(1+cosθ)/2` 는 **multiplicative carrier** 로, 그 *variance* 만으로도 shuffle 된 phase 에서조차 Φ 를
들어 올린다 → PhaseField lane = HONEST DEFERRED, 미배선.

## Claim / falsifier

R8 의 engine-native 진짜 벽은 "timing-binding 의 Φ-lift 가 자기 shuffle 통제를 engine-native 로 통과하지
못한다"는 것. 이 카드는 freshly-strengthened `a_break_the_wall` (≥2-3 원리적 렌즈 × 각 shuffle+ablation,
ablation 이 (d)천장-확정 도구)대로, **lift 를 만들되 phase-shuffle 에 collapse 하는** timing read-out 을
engine-native LCG 기질 위에서 찾으려 시도한다. 한 렌즈라도 (c2 ∧ c4 ∧ ablation-LIVE) every-seed 면 🟢
→ 배선; 전부 통제에 기각되면 🧱 CONFIDENT-TERMINAL (유효 결과, c9). bar 는 H_1283 R8 에서 VERBATIM,
이동 없음 (c9/p7). faithful IIT4 (exact MIP-EI n=4), proxy 아님.

## Method

engine-native 기질 == H_1283 R8 engine gate (`_lcg_*` byte-identical, N_MOD=4 DIM=8 T=64, content update
모든 arm 동일 — binding 은 TIMING). 4 arm/렌즈: ARM A(direct) · ARM B(gate ON) · SHUFFLE · ABLATE.
3 seeds [7,8,9], deterministic. FROZEN bars: c2 ΔΦ(B−A)≥+0.02 every seed · c4 phase-shuffle ΔΦ(SH−A)≤0
every seed · ABLATION (B−ablate)≥+0.02 every seed (mechanism-OFF 가 lift 를 죽여야 = 그 메커니즘이 LIVE;
ablate==B 면 INERT). 세 렌즈(+R8 sanity anchor):

- **LA RE-ENTRANT LOOP GATE** — content 는 thalamo-cortico-thalamic 재검증 pass 를 살아남아야 workspace
  접근: gate = max(0, cos(θ_i − θ_consensus)) (재검증된 workspace consensus 와의 일치, free carrier 아님).
  SHUF = feedback pairing 교란. ABLATE = feedback OFF → pass-through == ARM A.
- **LB PHASE-DIFFERENCE COHERENCE** — RELATIVE locking 으로 gate, ZERO mean carrier bias 로 re-center:
  gate = 0.5*(1 + cos(θ_i − θ_consensus)). shuffle 시 E[cos]=0 → gate→0.5 uniform → lift collapse 강제.
  ABLATE = coherence const 0.5. (R8 대비 fix: 절대-phase multiplicative carrier 제거.)
- **LC ADDITIVE ORDER-PARAM BIAS** — multiplicative carrier 없음: sal = e + λ·R·e_mean, R = Kuramoto
  order parameter |mean e^{iθ}| (phase-locked 일 때만 큼). SHUF 가 R 파괴. ABLATE R:=0 → sal=e == ARM A.

## Result — 🧱 CONFIDENT-TERMINAL (verbatim, `.verdicts/1423_thalamus_reentrant_phase/result.txt`, mean seeds [7,8,9], deterministic run1==run2)

| lens | c2 (ΔΦ≥+0.02 every seed) | c4 SHUFFLE (ΔΦ_sh≤0 every seed) | ABLATION | verdict |
|------|---|---|---|---|
| R8-ORIGINAL (sanity anchor) | PASS (+1.466/+0.845/+0.709) | **FAIL** (+0.026/+0.380/+0.296 positive) | LIVE | 🧱 (reproduces known engine c4-fail byte-exact) |
| LA RE-ENTRANT LOOP GATE | **FAIL** (seed7 −0.098) | PASS | **INERT** (seed7) | 🧱 WALL |
| LB PHASE-DIFFERENCE COHERENCE | **FAIL** (seeds7,8 −0.081/−0.073) | **FAIL** (seed8 +0.077) | INERT | 🧱 WALL |
| LC ADDITIVE ORDER-PARAM BIAS | PASS (+0.261/+0.198/+0.243) | **FAIL** (seeds7,8 +0.325/+0.065 positive) | LIVE | 🧱 WALL |

→ **VERDICT: 🧱 CONFIDENT-TERMINAL** — engine-native timing 벽이 세 원리적 렌즈 + R8 anchor 모두에서 자기
통제에 기각되며 선다. (c2 ∧ c4 ∧ ablation-LIVE) every-seed 를 통과하는 렌즈 0개 → engine-wired 0개
(올바름 — 배선할 GREEN 없음, `a_verified_must_wire`).

## Why the controls have teeth (the failing lenses validate the test)

- **R8-original**: c4 shuffle 가 정확히 FIRE (positive) — 알려진 engine-native 실패를 byte-exact 재현. anchor 유효.
- **LC additive order-param**: c2 PASS(robust lift)이지만 c4 shuffle 가 양수(+0.325/+0.065) — R8 와 **동일한
  carrier-variance 병리**. amplitude bias 가 Φ 를 들어 올리되 shuffle 에 collapse 안 함. shuffle 통제가 결정적.
- **LA / LB**: variance-free 로 설계된 두 렌즈는 robust lift 자체가 없음(c2 FAIL) — gate 가 amplitude 변조를
  주지 않으면 engine-native 에서 Φ-lift 가 안 난다. ablation 은 seed7 에서 INERT 로 메커니즘 비기여 확인.

오직 (c2 AND c4 AND ablation-LIVE)를 동시에 통과해야 BIND — 어느 렌즈도 못 함. 강화된 `a_break_the_wall`
가 요구하는 규율(MULTI-LENS + shuffle + ablation, 단일-렌즈 천장 박제 금지)을 충족한 정직한 🧱.

## What the wall names (the finding, c9)

`a_break_the_wall` taxonomy = **(a) measurement-faithful / (d) engine-substrate ceiling, MEASURED not
assumed**: engine-native LCG 기질 위에서 faithful-IIT4 Φ(n=4, T=64 toy)는 salience read-out 의 임의의
amplitude MODULATION(multiplicative carrier R8/LC, additive order-param LC)을 "integration" 으로 읽는다 —
lift 가 phase-shuffle 를 살아남는 이유는 그 변조의 **VARIANCE**(TIMING 구조가 아니라)가 MIP 를 구동하기
때문. variance-free 로 만든 두 렌즈(LA agreement gate, LB zero-DC coherence)는 올바르게 robust lift 가
없다(c2 FAIL). 따라서 이 engine 기질엔 Φ 를 **둘 다** 들어 올리고(c2) shuffle 에 collapse(c4)하는 timing-gate
read-out 이 없다 — R8 numpy-mirror 를 정직하게 만든 그 성질이 engine LCG 기질로 전이되지 않는다. 벽은
"발견 후 기각된 메커니즘"이 아니라, 작은 n=4/T=64 계에서 faithful-IIT4 의 amplitude/variance 민감성이라는
engine 기질 천장이다. R8 numpy-mirror 🟢 는 DIRECTIONAL 로 유지; engine-native 는 DEFERRED.

## Honest scope (c9)

TOY 4-module/dim-8/64-tick, 3 seeds [7,8,9], deterministic, $0 CPU. faithful IIT4 leg 은 REAL(exact MIP-EI
via hexa, proxy 아님). numpy-mirror engine-transfer 는 R8 에서 이미 DEFERRED 였고 이 카드가 그 wall 을 세
렌즈로 재확인. 더 큰 n(>8 exact 불가)/더 긴 T/실코퍼스/다른 oscillator coupling 은 UNVERIFIED
(`a_scale_honest_scope`·`a_toy_scale_recheck`). relay-CONTENT 축은 H_1283 에서 이미 🧱, 본 카드는 TIMING
축의 engine-native 만 다룸. NO bar moved (frozen-first). 배선 0 (배선할 GREEN 없음).

## Cross-links

H_1283 (thalamus, the wall) · H_1421 (multi-lens precedent that BROKE a wall — LC complementary-tail bound) ·
H_1419/H_1420 (correctly-stayed-🧱 ablation-INERT precedents) · h1227 · h1231 · h1280 · h1199 · h1205 ·
`a_phi_iit4_tool` · `a_break_the_wall` · `a_no_llm_frame_trap` · `a_engine_native_learning` ·
`a_verified_must_wire` · `a_core_engine_map` · `a_scale_honest_scope` · `a_toy_scale_recheck` ·
p1·p2·p3·p6·p7·p8·c9·c15·c16
