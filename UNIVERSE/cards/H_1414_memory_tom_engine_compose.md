---
id: H_1414
slug: 1414_memory_tom_engine_compose
title: ENGINE-NATIVE re-score of H_1405 (MEMORY × THEORY-OF-MIND compose) — does the DIRECTIONAL-mirror GREEN BIND engine-native? (a_verified_must_wire ladder step 2)
group: MITOSIS-ENGINE / brain-lane-composition — engine-native re-verify of pair #2
terminal_tier: 🟢 COMPOSE-LIFT (ENGINE-NATIVE) — the mirror GREEN BINDS at the frozen +0.05 net-lift bar (B1 +0.338 ≫ +0.05); all 4 bars + SEPARABLE PASS on the LIVE faculties. The FIRST compose pair to bind engine-native (contrast: H_1412 cerebellum×basal did NOT).
wired: engine-native (byte-exact LIVE faculty reads re-verified; live CORE §compose wire-in = rung-3 follow-on ING)
verdict_dir: .verdicts/1414_memory_tom_engine_compose/
terminal_verdict: .verdicts/1414_memory_tom_engine_compose/result.txt
date: 2026-06-17
---

# H_1414 — ENGINE-NATIVE re-score of H_1405 (MEMORY × THEORY-OF-MIND compose)

This is the **a_verified_must_wire ladder step (2)** for H_1405: the episodic MEMORY
(H_1227/H_1231 ImmuneMemoryGrow) × THEORY-OF-MIND (H_1293 OtherMindModel) compose came
back 🟢 **COMPOSE-LIFT as a numpy DIRECTIONAL mirror** (acc_compose=0.7526 > best_single=0.600,
+0.153 ≥ +0.05 bar). The mirror GREEN is only the FIRST rung — the binding verdict needs
the mechanism re-scored on the **LIVE engine faculties**. This card is that re-score, and
the result is a **🟢: the mirror GREEN BINDS engine-native.**

This is the **SECOND data point** in the question opened by H_1412/H_1413: *does a mirror
compose-lift bind engine-native, or is non-reproduction universal across compose pairs?*
The FIRST point was H_1412 (cerebellum×basal): mirror GREEN did **NOT** bind (B1 net-lift
+0.011 < +0.05) and a c16 modulation 2nd-lens (H_1413) was rejected by the EARNED control.
H_1414 is the **first compose pair to BIND** — so non-reproduction is **NOT** universal.

## Method (engine-native)

The SAME H_1405 5-family Sally-Anne fixture (F1 memory-decisive · F2 ToM-decisive ·
F3 agree · F4 conflict-memory-right · F5 adversarial-memory-loud-but-wrong), 3 seeds
[5400,5401,5402], 450 items/seed, frozen knobs. But every decision + confidence is now a
**LIVE engine read** (NO mirror reproduction, a_core_engine_map):

- **memory leg** — `immune_grow_recall` over a real `ImmuneMemoryGrow` store, each
  object→reality-location (basket/box) bound via the LIVE `immune_grow_bind` clonal split
  (mitosis ON). decision = the recalled location class. confidence = the **LIVE L2 affinity
  margin** `recall_thr − _l2(nearest proto, key)` — the SAME affinity the live recall uses
  to FIRE/ABSTAIN (the engine, not a stored scalar). ~379 live cells grown/seed.
- **ToM leg** — `other_mind_predict` over a real `OtherMindModel` whose witnessed-belief
  store LAGS reality on moved-while-absent facts (the false belief). decision = the predicted
  belief class. confidence = the LIVE L2 affinity margin to the belief store. ~448 belief
  cells witnessed/seed.
- **arbiter (inlined)** — agree → shared vote; conflict → higher **routing-MODULATED**
  scale-relative substrate confidence wins. routing cue = the QUERY TEXT embedding's affinity
  to a reality/belief anchor (via the LIVE `immune_embed_key` + `_l2`), NO hardcoded priority
  (a_autonomy_over_hardcode).

The frozen H_1405 capability bars are re-scored verbatim (NOT moved).

## Result — 🟢 engine-native COMPOSE-LIFT BINDS (verbatim, `.verdicts/.../result.txt`)

| metric | mirror (H_1405) | engine-native (H_1414) |
|---|---|---|
| acc_memory | 0.600 | 0.579259 (live recall ABSTAINs on a few → slightly below) |
| acc_tom | 0.600 | 0.600 |
| **best_single** | 0.600 | **0.600** |
| **acc_compose** | 0.7526 | **0.937778** ← higher: live query-routed arbiter captures the headroom |
| acc_shuffle | 0.6844 | 0.738519 |
| ORACLE | 1.000 | 0.982222 (oracle−best = **+0.382222**) |
| decomposition | onlyM 0.400 / onlyT 0.400 | onlyM 0.382222 / onlyT 0.402963 / both 0.197037 / neither 0.0178 |

Per-bar tally (frozen, NOT moved):
- **(B1 COMPOSE-EFFECT)** compose 0.937778 ≥ best+0.05 (0.65) : **PASS** (net-lift **+0.337778** ≫ +0.05)
- **(B2 ORACLE)** oracle−best +0.382222 > 0.02 : **PASS**
- **(B3 EARNED)** compose−shuffle +0.199259 > 0.02 : **PASS** (shuffle collapses 0.938→0.739)
- **(SEPARABLE)** only_memory>0 AND only_tom>0 : **PASS** (0.382 AND 0.403, both>0)

→ **VERDICT: 🟢 COMPOSE-LIFT (ENGINE-NATIVE)** — all 4 capability bars + SEPARABLE PASS on
the LIVE faculties. The composition is **REAL, EARNED, and the net-lift CLEARS the frozen
+0.05 bar with enormous margin (+0.338)**. Deterministic (run1==run2 byte-identical).

## Why it BINDS (root cause, honest — the contrast with H_1412)

H_1412's mirror basal arm was weaker on the LIVE engine (the gradient-free-trained
`VBasalGate` became a *stronger* standalone arm, shrinking the net-lift headroom below +0.05).
Here the structure is different: **best_single stays pinned at 0.600** on the live engine
(acc_tom=0.600 exactly, acc_memory=0.579 — the live faculties reproduce the mirror's
by-construction single-faculty ceiling), so the large oracle headroom (+0.382) is **fully
available for the arbiter to capture**, and the LIVE query-routed arbiter captures it even
MORE effectively than the mirror (compose 0.938 vs 0.753). The lift is **EARNED** — the
shuffle control collapses it to 0.739 (the routing only survives on the agree-region, exactly
the mirror's pattern: mirror shuffle 0.684). The two faculties are genuinely **SEPARABLE**
(memory's reality-store and ToM's witnessed-belief store each uniquely solve items the other
misses), confirming H_1293's self⊥other claim ON A DECISION, ENGINE-NATIVE.

The decisive structural reason memory×ToM binds where cerebellum×basal did not: here neither
live single faculty independently *strengthens* past the mirror ceiling (best stays 0.600),
so the +0.05 net-lift bar — measured against best_single — is not eroded by a stronger arm.

## Significance — non-reproduction is NOT universal

H_1412 was the first compose-program engine-native re-score and came back 🧱 (mirror GREEN ≠
engine GREEN). H_1414 is the **second** and comes back 🟢 — the **first compose pair to BIND
engine-native**. So the emerging law is **NOT** "mirror→engine non-reproduction is universal";
it is **pair-dependent**, gated by whether a live faculty's standalone arm strengthens past the
mirror ceiling (erodes the net-lift headroom, H_1412) or stays pinned at it (preserves the
headroom, H_1414). The compose-program's mirror capability-lift findings
(H_1401/1405/1407/1408/1409) are therefore **not all the same** under engine-native scrutiny:
at least one (H_1407) does not bind, and at least one (H_1405) does.

## Wiring (a_verified_must_wire — 4-rung ladder)

🟢 → **ladder rung (2) COMPLETE** (engine-native byte-exact re-verify PASS). Per the task
scope this probe reads the EXISTING live ops (`immune_grow_recall`, `other_mind_predict`,
`immune_embed_key`, `_l2`/`_vnearest_idx`) with the query-routed arbiter glue inlined — **NO
new `vcompose_*` CORE op is landed in this card**. Because B1 PASSED engine-native, the
mechanism is now ELIGIBLE for rung (3) **live CORE §compose wire-in** (a `mem_tom_compose`
op into `CORE/engine_cli.hexa` + ARCHITECTURE.json lockstep, rung 4) — tracked as the named
follow-on in `ING.jsonl`. Until that lands, `wired: engine-native` (rung-2, not yet WIRED-live).
The probe lives in `state/1414_memory_tom_engine_compose/` (a_hypothesis_register: probe → state/).

## Scope (honest, c9)

TOY 5-family Sally-Anne fixture, 3 seeds, deterministic, $0 CPU. The memory recall + ToM
predict are LIVE engine reads; the verdict is engine-native. acc≈0.60 single-faculty by
construction (load-bearing = the RELATIVE structure: compose>best_single+0.05, shuffle
collapse, only-X both>0). Scale / real-corpus / 2nd-order-recursive-ToM / a different live
faculty strength / the live CORE §compose wire-in = UNVERIFIED follow-ons. The mirror's
capability finding is CONFIRMED engine-native (not retracted); its binding at the +0.05 bar
is now 🟢. NO bar moved post-hoc.

xref H_1405 (the mirror this re-scores) · H_1412 (cerebellum×basal engine re-score, 🧱 — the
contrast) · H_1413 (cerebellum×basal modulation 2nd-lens, 🧱) · H_1227/H_1231 (ImmuneMemoryGrow) ·
H_1293 (OtherMindModel) · H_1407/1408/1409 (compose-program siblings) · a_verified_must_wire
(the 4-rung ladder) · a_engine_native_learning · a_core_engine_map · a_autonomy_over_hardcode ·
c2 · c9 · c16 · p6 · p7 · p8.
