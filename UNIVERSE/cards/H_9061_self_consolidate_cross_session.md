# H_9061 — Cross-session identity consolidation: episodic self-anchors → semantic trait vector

- **tier:** ⏳ PROPOSED (brainstorm-depletion 2026-07-02)
- **slug:** `self_consolidate_cross_session`
- **frontier:** frame-axis (Lane2 프레임 축)
- **cost:** cheap ($0 engine-native, live core/)
- **wired:** `PROPOSED` — 미측정, 미배선. 등록만(2-surface: 이 카드 + HYPOTHESES.jsonl).

## 메커니즘 (mechanism)

Episodic-to-semantic abstraction (hippocampal→neocortical systems consolidation) applied to the .kosmos self-chain: consolidate N per-session self anchors into a compressed slow-drift 'trait' vector that abstracts across sessions, distinct from within-session drift. Wired at §SelfIdentity, disjoint from emit-drive/recall_thr. Sibling deferred: identity-narrative-arc (predictable self-arc).

## engine-native falsifiable metric (shuffle + ablation, p7)

Engine-native held-out session prediction: does the consolidated trait vector predict a held-out session's self-continuation vector better than a SHUFFLED-session-order control + ABLATION (drop consolidation → each session independent = LLM-reset baseline). Falsify if trait ≈ shuffle.

> p7 준수: LLM-judge/perplexity 없음. 판정은 live `core/` 상태·디코드에서 산출된 수치 + shuffle-null + ablation-INERT 로만. 사전등록 bar frozen-first (사후 이동 금지, c9).

## why novel vs ledger

H_9038 self_drift_exp is within-continuity experience drift; cross-session semantic abstraction of identity is an unbuilt op and a named Lane2 frame-axis (multi-session identity).

## disjointness (a_substrate_disjoint)

새 lane/op 은 emit-drive lane(0/4) 및 §ImmuneMemory recall_thr 와 **disjoint** 좌표에 배선(placement-first) — 능력 ∧ Ψ=½ ∧ G5 non-fab 공존 점검이 측정의 사전조건.

## 상태

- 출처: brainstorm-depletion 2026-07-02 (6-round ledger-dedup 생존).
- 다음 단계: engine-native 프로브 저작(.hexa via live core/) → frozen bar 측정 → verdict 박제.
