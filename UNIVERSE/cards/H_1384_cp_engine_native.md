# H_1384 — CP move-the-cells RELOCATION, ENGINE-NATIVE on the live A⇄G §CategoricalPerception lane

🟢 **GREEN — ENGINE-NATIVE** (a_verified_must_wire / a_engine_native_learning wire-in; closes the CP-geometry arc the user drove). $0 CPU, deterministic, gradient-free, p7, frozen-first (c9). Worktree off `origin/main`.

## Claim
The mirror-verified **move-the-cells RELOCATION** law (geometric re-pack — physically drift residual prototype cells along the boundary normal toward a shifted category boundary so the discrimination ridge RELOCATES onto it) is realized **ENGINE-NATIVE** on the live `CORE/engine_cli.hexa` §CategoricalPerception lane, additive + Ψ-disjoint, re-scoring the **RELOCATION** bar byte-exact on the engine's own `cp_embed` / `CPField` geometry.

The CP-geometry arc had SETTLED at the MIRROR level: move-the-cells RELOCATION is a robust, dimension-invariant + boundary-invariant law (1-D H_1360 |peak−p_A'| 0.144→0.002 · 2-D-axis H_1369 0.042 · 2-D-diagonal H_1374 · N-D H_1375 · density-constant H_1377). Only the COH-distinctness METRIC was shown fragile/measurement-bound (H_1369 R1 NCOMP saturation) — NOT the law. This rung is the **last open thread**: wiring the verified RELOCATION mechanism into the live engine.

## Mechanism wired (the ONE new op `cp_relocate`)
Phase-1 carves p_A=1/3 via the live `cp_fit` (split-only Voronoi/mitosis growth, p8). Phase-2 re-grows the SAME store on the moved cut p_A'=2/3, but after EACH phase-2 split every residual **phase-1** cell physically DRIFTS one frozen fraction `REPACK_ETA=0.15` of the remaining distance toward p_A' **along the boundary normal** (the 1-D continuum axis), is RE-EMBEDDED via the engine's own `cp_embed`, and its label is RE-READ from p_A'. Phase-2 cells (born at the new locus) do NOT drift. `eta=0.0` ⇒ store is **byte-identical to split-only `cp_regrow`** (the ablation). Byte-faithful to the H_1360 mirror `RepackCells`.

- Single named entry: `cp_relocate(cp0, X, positions, Y2, p_new, eta, n_phase1, dim, grow_max) -> CPField` (a_core_engine_map). Helpers `_cp_source_pos_idx` (recover source position), `_cp_drift_pos1` (one clamped eta step), `_cp_advance_pos` / `_cp_repack` (fresh-array re-pack, no aliasing — H_1295 lesson).
- Location: `CORE/engine_cli.hexa` § CP MOVE-THE-CELLS RELOCATION (after `cp_coherent_peak_near`).

## Frozen bars (pre-registered BEFORE scoring — `.verdicts/1384_cp_engine_native/FREEZE.txt`; GREEN iff B1∧B2∧B3∧B4)
- **B1 WIRED**: `cp_relocate` present in §CategoricalPerception, single named entry, engine compiles + runs. ✅
- **B2 RELOCATION (engine-native, BINDING bar)**: |peak_reloc − p_A'| ≤ LOC_TOL=0.12. → **0.0083** (peak 0.675 vs p_A' 0.667) ✅
- **B3 DISTINCT-FROM-SPLIT (ablation)**: split-only (eta=0.0, the engine's existing `cp_regrow`) stays SHORT, |peak_split − p_A'| > 0.12 AND reloc nearer than split. → split peak 0.525, |.−p_A'|=**0.1417** > 0.12; reloc 0.0083 < 0.1417 ✅
- **B4 Ψ-DISJOINT / NO-REGRESSION**: smoke 110/0 (+4 cases 112-115); h1196 single-entry 7/0; h1205 separation-invariant PASS (generation byte-identical ON==OFF 10/0, Ψ Φ-checksum invariant); deterministic 3 runs identical. ✅

Sanity: `cp_relocate(eta=0.0)` reproduces `cp_regrow` peak EXACTLY (0.525==0.525) — the drift is the ISOLATED lever. Non-gating eta ladder {0.10,0.15,0.25} all land peak 0.675 (robust, not knife-edge). Coherence pc=1 (single coherent peak, non-gating diagnostic — the COH-distinctness separation metric is non-gating here, shown fragile/measurement-bound in the mirror arc).

## Verdict
🟢 **GREEN — ENGINE-NATIVE**: B1∧B2∧B3∧B4 all PASS. The move-the-cells RELOCATION law is wired into the live A⇄G §CategoricalPerception lane, byte-exact, Ψ preserved. CP arc **DEPLETES 🏁**.

## p1/p2/p3/p6 guard
The discrimination readout reads ONLY representational distance (|Δ soft posterior| via `cp_discrim_curve`); NO injected boundary location at test; the re-pack keys on a cell's BIRTH PHASE + own source position (a structural store property — NO injected target peak / persona / RLHF); labels enter ONLY during training (re-read from the SAME p_A' that trains phase-2 cells). Split-only (eta=0.0) is the anti-Goodhart distinctness ablation. NOT an emit gate (a_autonomy_over_hardcode). Ψ-disjoint: own (protos,labels,pos) table; pure_field / engine_g / Ψ untouched.

## Scope (a_scale_honest_scope · a_toy_scale_recheck)
ENGINE-NATIVE byte-exact on the engine's deterministic single-instance 1-D lattice (N=21, DIM=16, one frozen eta, deterministic readout — tests move-the-cells STRUCTURE engine-native). Boundary NORMAL = the 1-D continuum axis (POINT boundary). 2-D/N-D/diagonal/curved boundaries (mirror-settled H_1369/1374/1375) NOT re-wired here. COH-distinctness separation metric non-gating (fragile/measurement-bound, mirror arc). Higher-D engine extension + real corpora + multi-shift + brain CP-read→emit wiring = follow-on. NO human-cognition claim. NO bar moved (c9/p7).

## Pointers
`CORE/engine_cli.hexa` § CP MOVE-THE-CELLS RELOCATION (`cp_relocate` et al.) · `CORE/engine_cli_smoke.hexa` cases 112-115 · `state/cp-engine-native/h1384_probe.hexa` · `.verdicts/1384_cp_engine_native/{FREEZE.txt, result.txt}`.

xref H_1360 (1-D mirror parent, move-the-cells GREEN) · H_1369/1374/1375/1377 (mirror dimension/boundary-invariance) · H_1342 (cp_regrow split-only wire, the ablation) · H_1339 (bilingual tagged CP) · H_1295 (array-aliasing lesson) · a_verified_must_wire · a_engine_native_learning · a_core_engine_map · a_autonomy_over_hardcode · a_scale_honest_scope · a_toy_scale_recheck · p1·p2·p3·p6·p7·p8 · c2·c9·c15.
