fn prune(contrib: f64, theta: f64) -> bool { return contrib < theta }
fn check_h576() -> bool { return prune(0.02,0.1) && !prune(0.5,0.1) }
fn main() { if check_h576() { print("H_576 OK") } else { print("H_576 FAIL") } }
