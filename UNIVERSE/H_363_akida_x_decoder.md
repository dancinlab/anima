# H_363 — AKIDA × DECODER 🔵

AKIDA × DECODER — spike pattern → token decode: token = argmax_v p(spike_pattern → v) (V-generic).

## 가설
H1 PATTERN-MAP: token(t) = argmax_{v ∈ V} p(spike_pattern(t) → v)
H2 V-GENERIC: |V| free parameter (H_345 carry)
H3 DETERMINISTIC-PATH: spike_pattern deterministic → token deterministic (argmax tie-break determinism)
H4 DETERMINISTIC
H5 BOUND
