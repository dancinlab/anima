# H_403 — BRIDGE × DECODER 🔵

BRIDGE × DECODER — capability bounds vocab subset: V_allowed = filter(V, cap), monotone in cap_strength.

## 가설
H1 SUBSET: V_allowed(cap) ⊆ V
H2 MONOTONE-IN-CAP: cap_str ↑ → |V_allowed| ↑ (more tokens accessible)
H3 NULL-CAP-EMPTY-VOCAB: cap_str = 0 → V_allowed = ∅
H4 DETERMINISTIC
H5 BOUND
