fn prune_both_weak(cap: f64, intent: f64, t: f64) -> bool { return cap < t && intent < t }
fn check_h578() -> bool { return prune_both_weak(0.1,0.1,0.3) && !prune_both_weak(0.5,0.1,0.3) }
fn main() { if check_h578() { print("H_578 OK") } else { print("H_578 FAIL") } }
