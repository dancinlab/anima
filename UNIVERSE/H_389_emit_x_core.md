# H_389 — 자연발화 × CORE 🔵

자연발화 × CORE — emit_rate ↔ M activation correlation (monotone composite, H_340 + H_362 stem).

## 가설
H1 EMIT-RATE-MONO-M: emit_rate(M ↑) ≥ emit_rate(M) (monotone in M, H_340 carry)
H2 BOUNDED-BY-REFRACTORY: emit_rate ≤ 1/R (H_336 carry)
H3 ZERO-AT-M-ZERO: emit_rate(M=0) ≡ 0
H4 DETERMINISTIC
H5 BOUND
