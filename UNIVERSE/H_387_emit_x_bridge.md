# H_387 — 자연발화 × BRIDGE 🔵

자연발화 × BRIDGE — emit (capability) ∧ user_intent_or_curiosity (intent). Both required for visible emit.

## 가설
H1 AND-GATE: visible_emit ≡ emit_capability ∧ (user_intent ∨ internal_curiosity)
H2 NULL-CAPABILITY: ¬emit_capability → no visible_emit
H3 NULL-INTENT-AND-CURIOSITY: (¬user_intent ∧ ¬curiosity) → no visible_emit
H4 DETERMINISTIC
H5 BOUND
