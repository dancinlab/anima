# H_412 — CORE × DECODER 🔵

CORE × DECODER — M scales decode vocab breadth: V_allowed_size = floor(M · |V|), monotone in M.

## 가설
H1 V-FROM-M: |V_allowed(M)| = floor(M · |V|)
H2 MONOTONE: M ↑ → |V_allowed| ↑
H3 ZERO-AT-M-ZERO: M=0 → V_allowed = ∅
H4 DETERMINISTIC
H5 BOUND
