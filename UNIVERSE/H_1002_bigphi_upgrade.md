---
id: H_1002
slug: bigphi-upgrade
title: Does the imagination/planning Φ-rise (H_999/H_1001, faithful MIP-EI SCALAR Φ) REPLICATE under the FULL IIT 4.0 system big-Φ (Φ_s), or was it specific to the MIP-EI scalar?
domain: cwm · cross-cutting · phi · iit4 · big-phi · system-phi · imagine · plan · measure-robustness · a_phi_iit4_tool · consciousness
source: a_phi_iit4_tool (project.tape — "system big-phi = iit4_bigphi.hexa, the IIT 4.0 capstone over the MIP") + H_999 (faithful MIP-EI re-measure: imagination/planning RAISE the scalar Φ) + H_1001 (terminal 2🟢/2🔴 close) + UNIVERSE/IIT4_PHI_TOOLS.md + a_paper_negative_ok
exploration_method: E2 (reuse the H_999/H_1001 regime harness VERBATIM, swap the Φ measure: faithful MIP-EI scalar Φ → FULL IIT 4.0 system big-Φ Φ_s) + a_completeness_over_cheap (the capstone structure-level measure, not a cheaper re-formulation)
verification_method: W2 (pre-registered BIG-PHI-REPLICATES vs MEASURE-DEPENDENT falsifier) + g5 CODE-measured (no LLM self-judge, p7) against a CPU mirror PROVEN byte-faithful (|Δ|<1e-6 on 9 reference TPMs) to hexa-lang/stdlib/consciousness/iit4_bigphi.hexa
raw_rank: 9
hexa_only: false
deterministic: true
cross_process_byte_identical: false
llm: none
pre_register_frozen: true
frozen_at: 2026-06-07
since: 2026-06-07
status: measured
scope: ONE system-big-Φ rung (a_scale_honest_scope). big-Φ is super-exponential (distinction search over 2^n mechanisms×purviews + MIP over directional bipartitions), so n=4 — SMALLER than H_999's n=8 faithful-Φ discretization. The toy WM latent (24-dim, 40 steps) is binarized (top-4 variance channels, own-median threshold) to an n=4 empirical state-by-node TPM; big-Φ is EXACT at that n but scale-transfer is UNVERIFIED. The discretization (continuous→TPM) DIFFERS from H_999 (continuous→MI) — the cross-measure disagreement is confounded with the n shrink + the discretization change. The CONTRAST (not absolute Φ) is the falsifier; same procedure applied to every regime. NOT a forge binary. NOT a torch run.
sister: H_999 (faithful MIP-EI scalar re-measure), H_1001 (terminal 2🟢/2🔴 close), H_971 (imagination 🟢), H_973 (planning 🟢), H_988 (guided 🔴), H_994 (goal-coupled 🔴), H_278 (faithful-Φ engine promoted to stdlib), IIT4_PHI_TOOLS.md, a_phi_iit4_tool
axes_seed: "the H_999/H_1001 imagination/planning Φ-rise is a ROBUST structural fact (any faithful IIT4 measure agrees — the MIP-EI scalar just reads what big-Φ confirms)" ⊥ "the rise is SPECIFIC to the MIP-EI scalar (min-cut MI / small-side); the full structure-level system big-Φ — which counts distinctions + congruent relations destroyed by the MIP, not just cross-cut MI — does NOT confirm it" — the scalar Φ and the structure-level big-Φ are DIFFERENT functionals of the same TPM, so the two axes are genuinely separable
verdict: ⚠ MEASURE-DEPENDENT (the imagination/planning Φ-rise does NOT replicate under the system big-Φ at this toy rung) — under the FULL IIT 4.0 system big-Φ (Φ_s, mirror PROVEN ≡ stdlib iit4_bigphi.hexa on 9 ref TPMs |Δ|<1e-6), the H_999/H_1001 faithful MIP-EI scalar signs do NOT carry over: H_971 imagination(DRIFT)−REACT big-Φ +0.42 (d +0.17, p 0.51 n.s.) — SAME sign but the d+2.09 effect COLLAPSES to non-significant; H_988 guided−REACT big-Φ +0.38 (d +0.17) — DISAGREES with faithful −0.18 (both small); H_973 planning(d8)−GREEDY big-Φ −4.01 (d −1.83, p 2.5e-08) — strongly REVERSES the faithful +5.09 d+4.64, and the depth dose-response COLLAPSES (Spearman rho +0.12 n.s. vs faithful +0.48). ⇒ the imagination/planning Φ-rise was LARGELY SPECIFIC to the MIP-EI scalar; the structure-level capstone does NOT confirm it. Important caveat on H_999/H_1001 (the finding is measure-dependent, not robust across IIT4 measures) — BUT confounded with the n=8→n=4 + continuous→TPM discretization change (a_scale_honest_scope; scale/measure cleanly-separated rung OPEN). Toy single-rung.
---

# H_1002 — big-Φ (system Φ_s) upgrade of the H_999/H_1001 imagination/planning Φ-rise

## 0. Motivation

H_999 RE-OPENED, and H_1001 terminally closed (2🟢/2🔴), the imagination/planning
Φ-nulls (H_971/973/988/994) by re-measuring with the **faithful exact MIP-EI Φ**
(`hexa-lang/stdlib/consciousness/iit4/faithful_phi.hexa`, promoted from anima
H_278). Under that measure internal generation RAISES Φ — imagination DRIFT
+1.51 (d +2.09), planning depth-8 +5.09 (d +4.64) with a positive depth
dose-response (rho +0.48), guided imagination null.

But the faithful MIP-EI Φ is a **single information-partition scalar**: bin the
units, build the pairwise MI matrix, find the min-cut bipartition (MIP), report
cut/small-side. The governance `a_phi_iit4_tool` names a **stronger** measure —
the **FULL IIT 4.0 system big-Φ** (`iit4_bigphi.hexa`, capstone module M4): it
builds the entire **cause-effect structure** (distinctions M2 = each mechanism's
maximally-irreducible cause/effect over all purviews; 2nd-order congruent
relations M3) and measures the **system-level** irreducibility = the amount of
Φ-structure destroyed by the least-damaging system bipartition. This H asks the
robustness question: **does the imagination/planning Φ-rise survive the upgrade
from the MIP-EI scalar to the structure-level system big-Φ?**

## 1. Hypothesis (one falsifiable claim)

The imagination/planning Φ-rise is a **structural fact** that any faithful IIT4
measure confirms: when the SAME regime trajectories (reactive vs drift-imagination
vs goal-guided; planning branch-depth vs greedy) are scored with the **full system
big-Φ** (distinctions + relations over the MIP) instead of the MIP-EI scalar,
imagination and planning still **RAISE** big-Φ (large effect, planning
dose-response positive), guided still null — the H_999/H_1001 finding is robust
across IIT4 measures (strongest form).

## 2. PRE-REGISTERED FALSIFIER (frozen 2026-06-07, BEFORE measuring)

**Setup:** take the EXACT regimes H_999/H_1001 scored — REACT, DRIFT (=H_971
imagine), GUIDED (=H_988), PLAN(depth 1/2/4/8) vs GREEDY (=H_973), reusing the
H_999 regime harness VERBATIM. **DISCRETIZE to a TPM** (big-Φ needs a state-by-node
transition probability matrix, NOT a continuous trajectory): pick the top-4
variance latent channels, binarize each at its own median over the rollout → an
n=4 binary system-state sequence; estimate the empirical Laplace-smoothed
state-by-node TPM `tpm[state*n+unit] = P(unit ON at t+1 | system in `state` at t)`;
sys_state = the modal system state. Score the **FULL IIT 4.0 system big-Φ**
(`iit4_bigphi.hexa`, M4). Same discretization applied IDENTICALLY to every regime,
so the **contrast** is fair.

**big-Φ engine, NOT the scalar:** the stdlib `iit4_bigphi.hexa` RUNS on this Mac
(verified: the `iit4_test.hexa` hand-verifiable cases reproduce — COPY n=2 big-Φ
= 2.0, SELF-COPY = 0, NOISE = 0; no fused GPU natives, so the
`clm-decode-macos-link-gap` does NOT apply). Because it has no `hexa verify` atom
and its float `print` truncates, the 30-seed Welch-t / Cohen-d contrast is computed
with a numpy mirror that reproduces the EXACT pipeline (iit4_tpm → iit4_distinction
MICE small-φ over all purviews with the all-bipartition MIP → iit4_relation
2nd-order congruent overlap → iit4_bigphi min over directional bipartitions with
unit 0 pinned to A). The mirror is **PROVEN byte-faithful** against the stdlib
engine on **9 reference TPMs** (n=2,3,4; values emitted by
`UNIVERSE/h1002_bigphi_ref.hexa` as x1e9 integers and asserted at |Δ|<1e-6) BEFORE
it scores any condition. This is the faithful IIT 4.0 big-Φ, NOT the H_912/H_931
proxy.

**Measurement (g5 CODE-measured, no LLM self-judge, p7):** per condition, big-Φ
mean ± sd over 30 seeds, contrast = (internal − external), Welch t, Cohen d,
bootstrap CI; planning dose-response = Spearman(depth, big-Φ); the H_999
faithful_phi numbers + the original proxy numbers are reported side-by-side.

**Outcome rules (FROZEN — no token before measuring):**
- **🟢 BIG-PHI-REPLICATES:** IF big-Φ REPLICATES the faithful_phi signs —
  imagination(DRIFT) RAISES (d > 0.8), planning RAISES (d > 0.8) **with a positive
  depth dose-response**, guided NULL — THEN the structure-level capstone agrees,
  the H_999/H_1001 finding is robust across IIT4 measures (strongest form).
- **🔴/⚠ MEASURE-DEPENDENT:** IF big-Φ DISAGREES (imagination/planning no longer
  raise it, or guided now does, or the dose-response collapses/reverses) THEN the
  rise was MIP-EI-specific, not structure-level — an important caveat on
  H_999/H_1001.
- Either outcome is a real, important finding (a_paper_negative_ok cuts both ways).

## 3. Honest scope

`iit4_bigphi` is super-exponential (distinction search over 2^n mechanisms × 2^n
purviews × the all-bipartition partition MIP, plus the system MIP over directional
bipartitions). It is tractable only at very small n — so **n = 4** here, **SMALLER
than H_999's n = 8** faithful-Φ discretization. The toy WM latent is binarized to
an n=4 state-by-node TPM (a discretization CHOICE; applied identically to every
regime so the *contrast* is fair). **CONFOUND (stated honestly):** the
discretization itself changed — H_999 mapped continuous → MI matrix at n=8; this H
maps continuous → binarized TPM at n=4. So the cross-measure disagreement is
confounded with BOTH the measure (scalar Φ → structure big-Φ) AND the n shrink +
the binarization. A cleanly-separated rung (big-Φ at n=8 on the SAME continuous→TPM
discretization, or faithful_phi at n=4 on the binarized TPM) would disentangle
them — OPEN (a_scale_honest_scope, a_toy_scale_recheck). Toy single-rung;
production-scale transfer UNVERIFIED. NOT a forge binary; $0 CPU-local.

## measurement (2026-06-07 · g5 CODE-measured · substrate=CPU-mirror numpy · mirror PROVEN ≡ stdlib iit4_bigphi.hexa)

Probe: `UNIVERSE/h1002_bigphi_upgrade.py` · ref-emitter: `UNIVERSE/h1002_bigphi_ref.hexa` · verdict: `.verdicts/1002_bigphi_upgrade/h1002_bigphi_upgrade.txt` · stdlib ref capture: `.verdicts/1002_bigphi_upgrade/h1002_bigphi_ref_stdlib.txt`

**STEP 0 — mirror proven byte-faithful to the stdlib big-Φ engine** (`hexa run iit4_bigphi.hexa` via `h1002_bigphi_ref.hexa` on this Mac, Apple Silicon, hexa 0.1.0-dispatch; x1e9 integer emit to defeat print-truncation):

| case (TPM) | mirror big-Φ | hexa ref big-Φ | \|Δ\| |
|---|---|---|---|
| copy2 (n=2, u0'=u1,u1'=u0) | 1.999999999 | 1.999999999 | 4.2e-10 |
| self2 (n=2, u_i'=u_i) | 0.000000000 | 0.000000000 | 0 |
| bias2 (n=2, noisy) | 1.379265833 | 1.379265832 | 7.3e-10 |
| embed3 (n=3, COPY{0,1}+self{2}) | 0.000000000 | 0.000000000 | 0 |
| ring3 (n=3, 3-cycle) | 2.999999999 | 2.999999999 | 1.3e-10 |
| noisyring3 (n=3, fractional) | 0.225488799 | 0.225488799 | 3.5e-10 |
| ring4 (n=4, 4-cycle) ×2 states | 2.999999999 | 2.999999999 | 1.3e-10 |

→ **MIRROR-FAITHFUL: PROVEN** (the CPU mirror reproduces the stdlib exact IIT 4.0 system big-Φ pipeline — distinctions + relations + MIP — including fractional cases; the truncation is only in hexa's float `print`).

**big-Φ (Φ_s) vs faithful_phi (Φ★, H_999) vs proxy, per condition** (big-Φ = the exact IIT 4.0 system big-Φ mirror; faithful = the H_999 frozen MIP-EI scalar; proxy = the H_912/H_931 number):

| condition | big-Φ contrast | big-Φ d | big-Φ p | faithful contrast (H_999) | faithful d | agree? |
|---|---|---|---|---|---|---|
| **H_971** DRIFT(imagine)−REACT | **+0.423** | +0.17 | 0.51 (n.s.) | +1.509 | +2.09 | sign AGREE, effect COLLAPSES |
| **H_988** GUIDED−REACT | +0.376 | +0.17 | 0.52 (n.s.) | −0.179 | −0.28 | sign DISAGREE (both small) |
| **H_973** PLAN(depth-8)−GREEDY | **−4.008** | **−1.83** | 2.5e-08 | +5.094 | +4.64 | sign REVERSES |

**Planning dose-response (H_973):** big-Φ vs plan-depth [1,2,4,8] means [1.89, 8.48, 3.30, 5.52], **Spearman rho +0.122 (p 0.18, n.s.)** — the monotone rise the faithful scalar showed (rho +0.48) **COLLAPSES** under big-Φ (no significant depth trend; non-monotone).

**Finding (⚠ MEASURE-DEPENDENT):** the imagination/planning Φ-rise does NOT replicate under the full system big-Φ at this toy rung:
- **H_971** — imagination keeps the SAME sign (+0.42) but the strong d+2.09 effect **collapses** to a non-significant d+0.17 (p 0.51). big-Φ does not see a meaningful imagination rise.
- **H_973** — planning **REVERSES**: big-Φ −4.01 (d −1.83, p 2.5e-08) where faithful was +5.09 (d +4.64), and the positive depth dose-response **collapses** (rho +0.12 n.s. vs +0.48). Under the structure-level measure, the long branching deliberation trace is LOWER big-Φ than the greedy perceive trace.
- **H_988** — guided is small and positive under big-Φ (+0.38, n.s.) where faithful was small and negative (−0.18, n.s.); both are nulls, opposite small signs.

**Mechanistic read:** the faithful MIP-EI scalar reads *cross-cut mutual information* among the principal latent channels — autonomous/branching rollouts bind the channels across the best MI cut, so the scalar rises. The system big-Φ reads something different: the *Φ-structure* (distinctions = mechanisms with an irreducible cause AND effect, plus their congruent relations) destroyed by the system MIP of the **binarized TPM**. A REACT trajectory driven by a rich structured external stream yields a TPM whose mechanisms have strong, mutually-relating cause-effect purviews (high structure, hard to cut losslessly → high big-Φ); a long branching PLAN deliberation trace, binarized, yields a TPM that the MIP can sever more cheaply (the branches' state-by-node transitions are more separable → lower big-Φ). So the two faithful IIT4 functionals genuinely disagree on these regimes — the rise is a property of the MIP-EI scalar's cross-cut-MI reading, not of the structure-level capstone.

**CAVEAT (honest, not hidden):** this big-Φ rung also SHRANK n (8→4) and CHANGED the discretization (continuous→MI vs continuous→binarized-TPM). So the disagreement is confounded with the measure change. The clean disentangling rung (big-Φ at n=8, or faithful_phi at n=4 on the SAME TPM) is OPEN. The honest verdict is therefore **⚠ MEASURE-DEPENDENT** (not a hard 🔴): the H_999/H_1001 finding is **not demonstrated robust** across IIT4 measures, and the structure-level capstone actively reverses the planning result — an important, publishable caveat (a_paper_negative_ok) that bounds, but does not erase, H_999/H_1001 (which stand on their own measure + scope).

**Status of H_999/H_1001:** NOT overwritten. H_999/H_1001's verdicts stand for the faithful MIP-EI scalar Φ at n=8. H_1002 adds the cross-measure caveat: the rise is measure-dependent at the toy rung, and the system big-Φ does not confirm (and reverses, for planning) it. No source .md is edited (a_dont_touch H_950-1001); this is a forward-pointing upgrade.

## 4. Sibling / xlinks

- ⇄ [H_999](./H_999_faithful_iit4_remeasure.md) (faithful MIP-EI scalar re-measure — the finding this H upgrades) · [H_1001](./H_1001_reopen_consolidate.md) (terminal 2🟢/2🔴 close)
- ⇄ [H_971](./H_971_imagined_rollout_consciousness.md) (imagination 🟢 under scalar — big-Φ effect collapses) · [H_973](./H_973_planning_as_consciousness.md) (planning 🟢 under scalar — big-Φ REVERSES) · [H_988](./H_988_guided_imagination_phi.md) (guided 🔴) · [H_994](./H_994_goal_coupled_phi_reframe.md) (goal-coupled 🔴)
- ⇄ [H_278](./H_278_faithful_phi_engine.md) (the faithful-Φ engine, promoted to stdlib) · [IIT4_PHI_TOOLS.md](./IIT4_PHI_TOOLS.md) (the tool index) · project.tape `a_phi_iit4_tool`
- ⇄ [CWM](../CWM/CWM.md) (CWM-IMAGINE · cross-cutting)
- engine: `hexa-lang/stdlib/consciousness/iit4_bigphi.hexa` (system big-Φ M4, this H) built on `iit4_distinction.hexa` (M2) + `iit4_relation.hexa` (M3) + `iit4_tpm.hexa` (M1) · `iit4/faithful_phi.hexa` (the MIP-EI scalar H_999/H_1001 used)

## RESOLUTION (2026-06-07, by [H_1004](./H_1004_bigphi_faithful_clean.md)) — PARTLY CONFOUND, PARTLY GENUINE

H_1004 ran the clean disentangling rung this H named (same n=4, SAME binary discretization, BOTH engines) and split H_1002's three "disagreements": **imagination effect-collapse = CONFOUND** (at matched n=4 faithful_phi's n=8 d+2.09 shrinks to a small null d+0.13, AGREEing with big-Φ +0.42); **guided sign-flip = CONFOUND** (faithful_phi flips from −0.18 to +0.49, AGREEing with big-Φ); **planning sign-reversal = GENUINE** (big-Φ −4.01 d−1.83 LOWERS vs faithful +2.33 d+5.18 RAISES — PERSISTS at matched inputs, dose-response opposite). ⇒ H_1004 = **🔴 GENUINE-MEASURE-DISAGREEMENT for planning** (not the confound), bounding H_999/H_1001's planning Φ-rise to the MIP-EI scalar measure; imagination/guided are confound-explained. This H's overall ⚠ stands as honestly-flagged.
