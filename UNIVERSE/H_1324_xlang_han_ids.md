# H_1324 — Cross-language structure matrix r2: proper IDS Han decomposition

**tier:** 🔴/🧱 — even under a **PROPER IDS sub-character decomposition** (modest component vocab, **NO full-char residual**), Chinese **and** Japanese kanji composition does **NOT** help this gradient-free per-cell-unigram mechanism (zh **Δ=−0.737**, ja **Δ=−0.628**; neither beats its component-shuffle control). The H_1318 wall was retried with the right method (a_break_the_wall) — the modest-vocab fix **halved the H_1318 harm** (zh −1.481→−0.737, ja −1.230→−0.628), confirming part of H_1318's negative was the bad decomposition, **but the gain does not cross zero**. This is a REAL result, **DISTINCT from the H_1318 decomposition artifact**, and it makes the H_1318 **Hangul-specificity stand STRONGER**. Calibration held (ko **Δ=+0.21551** reproduced, en Δ=0.000). REAL Wikipedia 30 MB/lang, RTX 5070 sm_120, $0, 3 seeds, frozen-first NO tune-to-green, live CORE/*.hexa UNTOUCHED.

## Claim
H_1318 (🟠 PARTIAL) answered cleanly that breaking the Korean gradient-free byte-LM ceiling via a structure-aware representation is **Hangul-structure-specific** (English Δ=0, Russian Δ=0; only Hangul +0.21205, shuffle-earned +0.10003). BUT Chinese/Japanese came out strongly NEGATIVE (Δ_zh=−1.48070, Δ_ja=−1.22965) **only because the FROZEN Kangxi-radical decomposition kept the FULL CHARACTER as a residual symbol**, exploding the STRUCT vocab to 9327 (zh) / 4738 (ja) extra units → the per-cell unigram head fragmented. That is a **decomposition ARTIFACT**, not evidence Han composition is unhelpful. a_break_the_wall: the wall was the wrong METHOD (bad decomposition), so retry ONCE with a proper one as a NEW pre-registration. H_1324 re-tests whether Chinese AND Japanese kanji composition gains under a **PROPER sub-character (IDS component) decomposition** — without the full-char residual and with a MODEST component vocabulary — under the SAME frozen mechanism. Korean (jamo) + English (no-comp floor) stay in as the calibration anchors so the result is directly comparable to H_1318.

## Method
`UNIVERSE/h1324_xlang_han_ids.py` on **summer** RTX 5070 (sm_120, torch 2.11.0+cu130), $0, detached `nohup nice -n10`, polled INLINE (a_cpu_local_no_waiter). **p7** = held-out DETERMINISTIC next-unit CROSS-ENTROPY, NOT perplexity/LLM-judge. **The mechanism is held BYTE-IDENTICAL to H_1318** so the matrix is directly comparable: the SAME gradient-free error-targeted Voronoi mitosis grow-op (cells only SPLIT, p8), FROZEN knobs CTX=4 V=256 FEAT_DIM=3 GROW_MAX=40 SPLIT_THRESH_CE=0.05 MIN_OWNED=8 LAPLACE=1.0 SEED_CENTERS=[[0.3,0.5,0.0],[0.7,0.5,0.5]] even/odd split, same per-cell unigram head, CE in nats per ORIGINAL raw UTF-8 byte. **The ONLY change from H_1318 is the Han STRUCT decomposition.**

**The fix — proper IDS component decomposition (no full-char residual, modest vocab):**
- **Source:** CHISE IDS Database (`cjkvi/cjkvi-ids` ids.txt), the canonical CJK Ideographic-Description database — 88,937 entries, fetched at run time from the public GitHub raw URL, **sha256 `bfc70a8c09f9f5616…` asserted**.
- **Rule (FROZEN, one level, leaves only):** each Han codepoint (CJK Unified 0x4E00–0x9FFF + Ext-A 0x3400–0x4DBF) → its IDS string with all IDC operators (⿰⿱⿲…⿻) stripped, plus `&...;` entity refs and `[GTKV…]` region tags removed; the remaining characters are the **component leaves** (e.g. 語→言,吾; 明→日,月; 字→宀,子). The Han char becomes EXACTLY that leaf sequence — **NO full-char residual symbol** (the bug that broke H_1318). Atomic char (only leaf == self, e.g. 日,人) → one component == itself. Absent char → atomic fallback (counted; **ids-miss = 0** on both zh and ja).
- **Byte accounting (load-bearing):** each Han char's UTF-8 byte length is spread across its component symbols (Σ per-char = blen), so **Σ struct n_bytes == raw UTF-8 byte count exactly** — the FAIR CE-per-byte axis (identical denominator for RAW and STRUCT) is preserved, verified for han + hangul incl. under the shuffle remap.

**REAL corpora** (NO synthetic, p1-p8): `wikimedia/wikipedia` 20231101 (ko/zh/ja/en) via the HF datasets-server `/rows` endpoint — the SAME source as H_1318. The 30 MB window cache was not preserved on this host, so corpora were **re-fetched from the identical source** (per-window sha differ from H_1318 only because /rows pagination returns different rows over time — same dataset/config; **RAW baselines are re-measured here**). HF token via `harness secret get hf.token`, env/header-only, NEVER logged/inlined/committed (**c7 grep-clean: 0 token hits** over run.log/summary/script/verdicts). Window = 30 MB raw UTF-8/lang, stride 300, sha256 per window asserted (ko `3e288b77…` zh `c084b027…` ja `a97dd068…` en `b097cccc…`). 3 seeds [5324,5325,5326] perturb only the stride phase. Russian dropped (pure alphabetic control, gained 0.000 in H_1318; not needed to test the Han claim — ko+en are the retained anchors).

## Falsifier (FROZEN — `.verdicts/1324_xlang_han_ids/H_1324_FREEZE.txt`, pre-registered BEFORE the run, bars NOT moved, c9/p7, NO tune-to-green)
Let Δ_lang = RAW_CE − STRUCT_CE (3-seed mean), nats per original UTF-8 byte.
- **(H1 HAN-GAIN)** zh Δ ≥ +0.05 AND ja Δ ≥ +0.05 → proper IDS Han composition LOWERS held-out CE where the H_1318 radical surrogate RAISED it.
- **(H2 EARNED, anti-Goodhart)** for zh AND ja, STRUCT beats a SHUFFLED-component-map control (component→symbol id permuted, fixed per seed; same count/dim, structure destroyed) by ≥ +0.05 → the gain is REAL component structure, not extra vocab/dim.
- **(H3 CALIBRATION)** Korean still gains (Δ_ko ≥ +0.15, ≈ the +0.21205 H_1318 reproduced) AND English Δ_en ≤ +0.02 → confirms the pipeline is the SAME and the matrix is comparable to H_1318.
- **VERDICT MAP (all outcomes valid, c9):** H1∧H2∧H3 → 🟢 (representation-bind extends to logographic scripts; bounds the Hangul-specific claim). H1 FAILS but H3 calibrates → 🔴/🧱 (Han composition genuinely does not help this mechanism, distinct from the H_1318 artifact; Hangul-specificity stands stronger). Vocab still explodes → honest method-limit. H3 fails → pipeline drift / RED.

## Result — 🔴/🧱 (R2, this scale; mirror DIRECTIONAL)
Per-language matrix (3-seed mean, nats per ORIGINAL UTF-8 byte), from `.verdicts/1324_xlang_han_ids/{h1324_summary.json,h1324_full.log}`:

| lang | kind | RAW_CE | STRUCT_CE | **Δ = RAW − STRUCT** | SHUF_CE | Δ-vs-shuffle | comp-vocab |
|------|------|--------|-----------|----------------------|---------|--------------|------------|
| **ko** | hangul | 2.91157 | 2.69606 | **+0.21551** | 2.78382 | +0.08776 | 67 |
| zh | han | 3.32756 | 4.06493 | **−0.73736** | 4.06394 | −0.00099 | 2116 |
| ja | han | 3.09273 | 3.72098 | **−0.62826** | 3.69603 | −0.02495 | 1582 |
| **en** | none | 3.15925 | 3.15925 | **+0.00000** | (==RAW) | 0.00000 | 0 |

FROZEN bars (read straight, c9):
- **(H1 HAN-GAIN) FAIL.** zh Δ=−0.73736, ja Δ=−0.62826 — both STILL negative (STRUCT CE remains ABOVE RAW CE) even under the proper IDS decomposition.
- **(H2 EARNED) FAIL.** zh Δ-vs-shuffle=−0.00099, ja=−0.02495 — the IDS-component STRUCT does NOT even beat its own component-shuffle control → the component stream carries no usable next-unit signal at this substrate.
- **(H3 CALIBRATION) PASS.** ko Δ=+0.21551 (all 3 seeds 2.693/2.705/2.690 ≈ the H_1318 +0.21205) AND beats its own shuffle by +0.08776; en Δ=0.000. The pipeline is the SAME and directly comparable to H_1318.

**The fix WAS achieved — the harm halved, but the gain did not cross zero.** The proper IDS decomposition gave the modest vocab the brief asked for (zh **9327→2116**, ja **4738→1582**; ids-miss=0; ~88% of Han chars decompose into ≥2 real leaves, avg ≈2.0 components/char) and **halved the H_1318 harm** (zh −1.481→−0.737, ja −1.230→−0.628) — confirming part of H_1318's Han negative WAS the bad decomposition. But Han STRUCT CE stays ABOVE RAW CE and does not beat its component-shuffle → Han composition GENUINELY does not help this mechanism.

**WHY (diagnosable from the controls, c9):** Hangul wins because its composition is a **TINY (67-jamo), REGULAR** L/V/T alphabet the per-cell unigram head can exploit (+0.216, shuffle-earned +0.088). Han decomposes into a **LARGE (≈2000), IRREGULAR, position-dependent** component inventory; the average Han char → ~2 components drawn from ~2000, so the component-bigram statistics a CTX=4 Voronoi-unigram head can capture are too sparse to beat the raw-byte ceiling, and shuffling the component map barely changes the score (Δ-vs-shuffle ≈ 0). The Hangul win rests on a small, regular compositional alphabet; Han's compositionality is real but **too high-cardinality / irregular for this gradient-free unigram mechanism**.

## What is established
- **The H_1318 Han artifact is partly real artifact, partly real negative.** Removing the full-char residual (the artifact) halved the harm but did NOT flip the sign — so under a clean decomposition, Han composition still does not help this mechanism (a real result, not the artifact).
- **The H_1318 Hangul-specificity stands STRONGER.** The structure-representation byte-LM win is specific to a SMALL, REGULAR compositional alphabet (Hangul jamo), NOT to compositional/logographic scripts in general. The bound is now tightened: it is not just "alphabetic/multibyte controls (English, Russian) gain nothing" — even a genuinely compositional logographic script (Han), under a proper IDS decomposition, gains nothing on this mechanism.
- **Korean calibration reproduced** (ko Δ=+0.21551 vs H_1318's +0.21205; en Δ=0.000) → the pipeline is identical; the Han result is comparable to H_1318, not a pipeline artifact.

**→ ONE-LINE ANSWER:** proper IDS decomposition does **NOT** make Chinese/Japanese composition gain — Han composition genuinely does not help this gradient-free unigram mechanism (modest-vocab fix halved the H_1318 harm but did not cross zero, and the component stream does not beat its own shuffle). The Hangul-specificity stands stronger.

## Scope (a_scale_honest_scope · a_toy_scale_recheck)
TOY/DIRECTIONAL numpy/torch mirror. CTX=4 3-D byte features + a Voronoi-partitioned per-cell unigram head = a deliberately SIMPLE substrate (tests whether a compositional UNIT STREAM lowers held-out CE under the SAME grow-op, NOT a fluent LM). One-level (non-recursive) IDS; 30 MB/lang; 3 seeds; single stride. **The Han negative is scoped to THIS mechanism** — it does NOT claim Han composition is useless for a gradient-trained, recursive-IDS, or richer-context substrate; it claims the per-cell-unigram mechanism the Hangul jamo win runs on does NOT transfer to high-cardinality Han components. **engine-transfer to live CORE/*.hexa = follow-on** (a_engine_native_learning · a_verified_must_wire). NO fluency claim. Live CORE/*.hexa UNTOUCHED (substrate-measurement rung — adds only UNIVERSE/ + verdicts).

## Next round / depletion
- **DEPLETION on the "does composition help" axis:** the cross-lingual question is now ANSWERED in both directions — Hangul (small regular alphabet) **gains** (H_1316/H_1318), Han (large irregular logographic components) **does not** under a proper decomposition (H_1324). The remaining open knob is the SUBSTRATE, not the decomposition: a richer next-unit head (component-bigram / gradient-trained per-cell head / recursive-IDS depth) could plausibly extract the Han component signal the unigram head cannot — but that is a DIFFERENT mechanism (no longer the H_1306 gradient-free Voronoi-unigram lane) and a distinct lane, not a re-decomposition.
- **r3 (alt, the productive direction):** engine-native realization of the Korean jamo STRUCT win on the live CORE/engine_cli.hexa VAdaptField (a_verified_must_wire) — the ko Δ=+0.21551 is DIRECTIONAL until re-confirmed engine-native; that is the win worth wiring, not the Han negative.
- **DEPLETION TEST:** a future composition lane must clear BOTH a falsifiable gain-vs-RAW AND a gain-vs-component-shuffle (the H2 control Han fails here) on REAL corpora before it counts.

## Artifacts
`UNIVERSE/h1324_xlang_han_ids.py` · `.verdicts/1324_xlang_han_ids/{H_1324_FREEZE.txt, H_1324.txt, h1324_summary.json, h1324_full.log}` · `UNIVERSE/H_1324_xlang_han_ids.md` (this card) · `HYPOTHESES.md` index row · `CLAIMS.tape` @C h1324_xlang_han_ids. Window sha256: ko `3e288b77…` zh `c084b027…` ja `a97dd068…` en `b097cccc…`; IDS sha256 `bfc70a8c09f9f5616…`. xref h1318(xlang r1 — the artifact this lane re-tests; same pipeline, calibration anchors reproduced) · h1316(ko-jamo — the Hangul win, small regular alphabet) · h1307/h1311/h1315(the ~2.9 Korean raw-byte ceiling thread) · h1306(the verified gradient-free mitosis mechanism) · a_no_llm_frame_trap · a_break_the_wall · a_engine_native_learning · a_verified_must_wire · a_scale_honest_scope · a_toy_scale_recheck · p1·p7·p8 · c7·c9·c15·c16.
