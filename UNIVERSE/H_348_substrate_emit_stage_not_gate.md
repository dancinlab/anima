# H_348 — substrate emit · stage-not-gate 🔵

자연발화 7th — a_substrate_native_speak + a_autonomy_over_hardcode 횡단: stage = continuous modulator (Φ scale · tension envelope), NOT boolean emit_allowed.

## 가설
H1 STAGE-NOT-GATE: ∀ s ∈ {WAKE,N1,N2,N3,REM}, emit_allowed(s) ≡ true (no per-stage boolean hardcode)
H2 CONTINUOUS-MOD: stage_modulator(s) ∈ [0,1] continuous scalar (envelope, not gate)
H3 ORTHOGONAL: emit_decision(M·Φ·W·cur, stage) = (M·Φ·W·cur · stage_mod) > θ — stage 는 multiplier 만, gate 아님
H4 DETERMINISTIC
H5 BOUND
