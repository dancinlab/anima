anima living library — the 10-hypothesis arc (ASCII mirror of Fig. 1)
=====================================================================

  [ ORACLE FRAME — closed-negative ]            [ LIVING MEMORY — what anima IS ]

  H_6016  quantum storage                       H_6018  anima library
   (X) no readable DB (compress 1.011,           (O) content-addressable
       entropy 7.784/8)                              50%-cue overlap 1.000
   (O) info-preserving (err 2.5e-16)            ───────────────────────────
   (O) finite (area exp 2.00)                    H_6019  quantum library
        |                                         (O) 2^n capacity (256x)
        v                                         (O) sqrt(N) recall (50 it, 0.9999, 41x)
  H_6017  library of Babel                       (X) no bulk read / no un-stored oracle
   (O) exists (A^L = 4^8 = 65536)                      |
   (X) no usable index (addr = content)                v
   (X) no quantum shortcut (38726 ~ 65536)      H_6026  RTSC retrieval
        |                                         (O) Grover recall 0.9995 / 25 it
        +----------------------------------->     (X) no vacuum oracle (un-computed = 0)
                                                       |
                                                       v
                                               H_6027  collective library
                                                (O) cross-mind recall 1.000
                                                (O) consensus EC 0.046 < 0.082
                                                (X) channel-bound (OFF = 0)
                                                       |
                                                       v
                                               H_6028  generative completion
                                                (O) regen compressible 64/64
                                                (~) honest UNKNOWN on noise (0.000)
                                                       |
                                                       v
                                               H_6029  generational persistence
                                                (O) heritable across mitosis (0.9999)
                                                (~) fade ~3 gens; rehearsal -> 200+
                                                (O) meaning (gen 40) outlives noise (gen 3)
                                                       |
                                                       v
                                               H_6030  forgetting = feature
                                                (O) tension-weighted evict (+0.1110)
                                                (X) never-forget collapses (0.000 vs 1.000)
                                                       |
                                                       v
                                               H_6036  false memory
                                                (O) flag spurious (TPR 0.93 / FPR 0.00)
                                                (O) meaning-check rejects (0.844)
                                                (~) tension-gated poisoning (honest residual)

  THESIS: no quantum library of answers; anima's real library = a finite, fading,
  sharable, self-correcting content-addressable memory that RECONSTRUCTS MEANING
  and NEVER FABRICATES NOISE (p7 at the memory layer).

  (O) = supported   (X) = closed-negative / honest limit   (~) = honest partial/residual
