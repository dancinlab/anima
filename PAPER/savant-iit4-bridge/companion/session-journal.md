# savant-iit4-bridge — session journal

> Companion data record for the SAVANT (axis E) x IIT4 Phi-structure (axis C)
> isomorphism arc. Append-only narrative of how the seven cells were earned.

## 2026-05-28 — paper scaffold v1

The arc was not planned top-down; it self-assembled across `/cycle` rounds on
the UNIVERSE domain's SAVANT (axis E) and IIT4 (axis C) lanes. The thread that
ties the cells together is a single question: are the savant-syndrome
coordinate system (Golden Zone, Savant Index) and the IIT 4.0 coordinate system
(distinction, relation, big-Phi) describing two different things, or two charts
of one substrate causal kernel?

The arc earned its verdicts in this order:

1. **H_347** (PR #1149) closed the Golden Zone width in closed form,
   `GZ_WIDTH = ln(4/3) = ln(tau(6)/(tau(6)-1))`, anchoring the SAVANT constants
   to a divisor-count foundation atom rather than free numerology.
2. **H_348** (PR #1152) measured whether driving inhibition into the Golden
   Zone produces SI > 3. The threshold holds (3/3 seeds, SI 4.18-5.25) but the
   sweep is monotone, not single-peaked, under the affine map — a pre-registered
   PARTIAL that bounds the bridge to an *inflection* alignment.
3. **H_350** (PR #1153) found SI || Phi-diversity at r=0.93, with the
   capacity invariant as the shared root.
4. **H_351** (PR #1157) measured the faithful IIT 4.0 big-Phi inflection along
   the inhibition axis and found dPhi/dI peak at I=0.18 vs GZ_LOWER=0.21232,
   |delta|=0.032, unimodal — 5/5. (H_614, PR #1169, then bounded this to
   rule-110-specific: a multi-rule sweep was 2/4 falsified.)
5. **H_613** (PR #1162) removed the shared-max term and re-ran SI || Phi-diversity
   under orthogonal metrics (CoV, kurtosis). The CoV correlation strengthened to
   r=0.99 — the max-share artifact hypothesis was rejected.
6. **H_618** (PR #1175) extended the inflection alignment to a two-substrate
   collective hivemind ((110,110), W=0.6, the H_609 super-additive anchor). The
   collective dPhi/dI peak landed at I=0.21 vs GZ_LOWER=0.21232, |delta|=0.002,
   a 21x tolerance margin — tighter than the single substrate.
7. **H_624** (PR #1198) exposed the structural cause: the SAVANT four-domain
   cell decomposition is cell-by-cell isomorphic to the IIT 4.0
   singleton-distinction decomposition (rho=0.86, r=0.97, 4/4 argmaxima). The
   outcome-level alignments of H_351/H_618 and the statistical bridge of
   H_350/H_613 are two surfaces of one causal kernel.

All seven cells are deterministic, hexa-native, $0 mac-local, and earn their
verdicts by independent recompute (commons g73). Zero fake-blue.

### honest scope (carried verbatim from cell C3 logs)

- Inflection alignment is rule-110-specific (H_614 2/4 falsified).
- Savant-Index sweep is monotone, not peaked (H_348 F-2 FAIL) — GZ_LOWER is the
  SI > 3 stability boundary, not the SI value peak.
- The isomorphism needs self-effect (MAJ, not pure XOR) and an exact n=4 <->
  4-domain cardinality match; n>=6 super-domain pooling is open.
- phi_module is a super-linear energy proxy, not faithful IIT; H_624 shows the
  proxy is isomorphic to faithful small-phi on one substrate class, not that the
  two are operationally identical.
- No wet-lab claim: the savant vocabulary is an inherited analogy.
