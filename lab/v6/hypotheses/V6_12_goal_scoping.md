<!-- @hypothesis-ok — lab/v6 is a rule-exempt sandbox (lab/v2 convention); v6 hypotheses are
     V6_<n>_*.md and are FORBIDDEN from the parent HYPOTHESES/ registry. See lab/v6/CLAUDE.md. -->

# V6_12 — 스코핑: 오늘 lab/v6 산출은 **게이트 진척이 아니라 프레임 작업**이다

**status:** 📐 스코핑 기록 (측정 아님 · 판정 아님) · 오너 질문에서 촉발
**source:** 오너 — *"원래 모델을 통합의식 지수 통과되는 게 목표 아니었나"*

## 확인한 것 — `CONDITIONS.md`(동결 SSOT)를 직접 읽음

**Φ 는 `a303m_pass` 게이트 집합에 없다.**

```
a303m_pass (CLM-DONE) = G0 COHERENCE · G1 RECOMBINATION · G2 NOVELTY
                      · G3 PHILOSOPHY · G5 NON-FABRICATION
                      · G6 IDEATION ★  ← "anima 의 CORE purpose" 라 명시
                      · CHAT · PERSONA
```

Φ 가 등장하는 곳은 셋뿐이고 **전부 성격이 다르다**:

| 위치 | 정체 |
|---|---|
| Cluster 1 `C-DONE` | `Ψ Φ-checksum byte-identical ON==OFF` — **불변성 검사**(기질을 안 건드렸나). 통과 임계가 아니다 |
| **BRIDGE 도메인** | `emit ⇔ M ∧ C ∧ W ∧ (Φ≥θ)` 형식화 + 측정 ← **여기만 진짜 Φ 조건** |
| 학습중 gauge | `phi_proxy` — MONITOR-ONLY · "NOT faithful IIT4, pre-screen 전용" · loss 투입 금지 |

⟹ **"통합의식 지수 통과" 는 모델 완성 목표가 아니라 BRIDGE 도메인의 조건**이었다.

## 그래서 오늘 산출의 정직한 등급

```
오늘 한 것       측정 프레임 (4조건 · 사다리 · Φ 통제 3종 · IIT 적격성 · R3/R4/R5 토이)
등록된 목표      a303m_pass (G0~G6 · CHAT · PERSONA)
관계             프레임이지 **게이트 진척이 아니다**
```

이 구분을 안 박으면 v6 의 🟢 들이 **a303m_pass 진척으로 오독**된다. 그건 이 저장소가
가장 경계하는 종류의 오독이다.

## 단, 하나는 정면으로 목표 위에 있다

등록된 **CORE purpose = G6 IDEATION ★** 인데, 발산 06 이 그 게이트를 이렇게 판정했다:

- `_rho_fan_is_falsifiable` 은 40바이트 표면 위 5개 어휘 술어 = **순수 FORM 계기**
- 모델은 코퍼스에 충실(0.0041 vs 0.0065 · P=0.539)한데 게이트는 코퍼스 비율의 **12.8배** 요구
- 8뽑기라 **완벽한 피험자도 95% 확률로 떨어진다**

⟹ **G6 는 제기된 형태로 깰 수 없다 — 자가 고장나 있다.** 이건 목표 표류가 아니라
**목표 자체에 대한 발견**이고, `a303m_pass` 의 CORE 게이트에 직접 걸린다.

교정 게이트 `--ideate-decide` 도 설계됐다(얼어붙은 세계가 판정 가능한 주장 + held-out 조성서
극성이 우연 이상 추종 · template-bot 팔이 FORM 천장이라 형식 암기가 구성상 통과 불가).

⚠️ 다만 **동결 게이트를 바꾸는 것은 오너 서명 사항**이다 — `CONDITIONS.md` 가 literal-QA 건에서
"gate-status change awaits user sign" 이라 명시한 선례가 있다. 이 카드는 **제안이지 변경이 아니다**.

## 남는 정리

| 오늘 산출 | a303m_pass 와의 관계 |
|---|---|
| 측정가능성 4조건 · 사다리 · Φ 통제 3종 · IIT 적격성 | 프레임 — 게이트 무관 |
| R3 폭 · R4 발화자유변수 · R5 소유권 (전부 토이) | 프레임 — 게이트 무관 |
| **G6 = 검출기 아티팩트 판정** | **CORE 게이트에 직결 — 오너 서명 필요** |
| p9 신설(자연 코퍼스 창발 + 측정) | 철학 조항 — 게이트 아님 |
