<!-- @hypothesis-ok — lab/v6 is a rule-exempt sandbox (lab/v2 convention); v6 hypotheses are
     V6_<n>_*.md and are FORBIDDEN from the parent HYPOTHESES/ registry. See lab/v6/CLAUDE.md. -->

# V6_7 — P1 통과: 항수-2 벽은 **표현 분할**이었다 (계산량이 아니라 설계)

**status:** 🟢 **ARITY-2 LEARNED (screen)** · 통제 전부 제자리 · **DIRECTIONAL 천장**
**cost:** $0 · numpy · 113초 · 3 seed
**runs:** `python3 lab/v6/lanebus_p1_shared_index.py` (exit 0)
**supersedes:** [[V6_6]] 의 실패 판정 — 원인이 격리됐다

## 결과 (우연 0.1250 유도)

| arm | acc | 역할 |
|---|---|---|
| workspace · SEEN | **1.0000** | 양성통제 ✅ |
| **workspace · HELD-OUT** | **0.6875** | ← **측정치 · 우연의 5.5배** |
| staple(1-slot 둘) · SEEN | 0.3542 | 용량 맞춘 가법 |
| staple · HELD-OUT | **0.0000** | 바닥 유지 ✅ |
| address-permute | **0.1042** | 붕괴 ✅ |

**결합 마진 = 0.6875 − 0.0000 = 0.6875** (요구 0.25 초과) ⟹ 가법 두 개를 스테이플한 게
아니라 **진짜 결합**이다.

## 무엇이 진범이었나 — 7축이 먼저 전부 음성이었다

[[V6_6]] 이 held-out 0.0000 을 낸 뒤, 원인을 찾아 다음을 전부 돌렸고 **전부 음성**이었다:

```
결합 형태     concat / sum / product
최적화기      평이한 SGD / AdamW
weight decay  0 → 3.0
step          20k → 200k
사영          공유 / 역할분리 / 학습가능
store 코드    iid-무작위 / 직교화
주소지정      연성 / 온도조절 / 경성 argmax
```

그 사이에 **그로킹 양성통제**를 나란히 세웠다 — 표준형 순환군 덧셈(one-hot 피연산자 ·
학습 임베딩)은 n=23 에서 held-out **0.9100**(우연 0.0435)에 도달했다.
⟹ **"조성을 못 배운다" 는 애초에 사실이 아니었다.**

실패한 과제와 통과한 통제 사이에 남은 **구조적 차이는 하나**뿐이었다:

```
통제(통과)   두 피연산자가 **같은** 임베딩 표를 인덱싱  → 순환 구조를 한 번 배워 둘이 재사용
P1(실패)     역할마다 **자기 표**를 가짐                → 48 예제로 두 독립 표 사이의 정렬을
                                                          발견해야 하고, 끝내 못 하고 암기한다
```

그 분할을 없앤 것이 **변경의 전부**다. 회수는 여전히 **얼어붙은 바이트 키로 내용주소**라
H_9775 를 일반화시킨 store 성질은 그대로다.

```
역할분리 표    held-out 0.0000
공유 인덱스    held-out 0.6875
```

## ⚠️ 여기서 나온 부수 확인 — 저장소 진단의 독립 재현

주소 온도를 재다가 `H_9672` 가 기록한 현상을 그대로 만났다:

```
tau 1.0   addr_top1 0.9375   addr_mass 0.1519   (균등 0.0625)
tau 0.1   addr_top1 0.9375   addr_mass 0.8486
```

**포인터는 맞는데(0.9375) 질량이 균등 바로 위** — 읽기가 굶주린 상태. 그래서 초기 슬롯-임베딩
판이 훈련조차 못 맞췄다(0.27). 이건 저장소가 이미 `addr_top1 high ∧ addr_mass low` 로 적어둔
것과 **같은 병**이고, 다른 코드베이스에서 독립으로 재현된 셈이다.

## 주장하지 않는 것

토이 하나 · 표 계열 하나 · 3 seed · **DIRECTIONAL**(lab/v6 법). 그리고 이 과제는 위장된
**순환군**이라 **가능한 가장 친절한 조성**이다 — 실제 관계 표가 그 구조를 가질 이유는 없다.

여기서 세워지는 것은 더 좁고, 그래도 값어치가 있다:

> **P1 의 항수-2 벽은 조성 불능이 아니라 표현 분할이었고, 계산량이 아니라 설계 선택으로
> 제거된다.**

## 계획에 대한 함의

[[V6_6]] 이 빚졌던 **two-substrate escape** 는 지금 필요 없다 — 같은 기질 위에서 통과했다.
설계에 들어갈 제약이 하나 늘었다: **workspace 의 피연산자 슬롯은 하나의 인덱스 공간을 공유해야
한다.** 역할별 표는 조성을 구조적으로 막는다.

다음: P2(잔차 게이트 대 시계).
