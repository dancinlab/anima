#!/usr/bin/env bash
# installed by 'sidecar init'; engine resolved via $HOME/.sidecar (location-independent — survives the
# repo being moved, e.g. standalone anima-v5 -> anima/lab/v5, which broke the former ../../ relative path).
exec bash "$HOME/.sidecar/cli/bin/sidecar" "$@"
