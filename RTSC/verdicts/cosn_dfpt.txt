QE 7.5 DFPT el-ph attempt — CoSn baseline (undoped, magnetic kagome metal)
host: summer pool (CPU-only, $0), QE micromamba env "qe" PWSCF/PHONON v.7.5
co-tenant rbfe_hsp90.py UNTOUCHED + alive at end (PID 2138). p7. 2026-06-14.
prefix=cosn, ibrav=4 celldm(1)=9.9760 celldm(3)=0.80680 nat=6 (Co 3f + Sn 1a/2d),
nspin=2 magnetic (SCF total mag ~1.4-2 muB/cell), US pseudos (rrkjus_psl PBE).
========================================================================

RESULT: NO lambda / omega_log / Tc obtained. DFPT did not complete in the box.
The blocker is NOT an nspin/USPP rejection (ph.x ACCEPTED the magnetic
ultrasoft setup) — it is (a) the prompt's ph.in is workflow-misconfigured, and
(b) the heavy DFPT linear-response solve cannot finish on a 2.6x-oversubscribed
shared host within the time-box.

------------------------------------------------------------------------
FINDING 1 — electron_phonon='interpolated' (the prompt's ph.in) is REJECTED:
it requires a pre-computed DeltaVscf (verbatim, ph_probe.out):

 %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
     Error in routine phq_readin (1):
     El-ph needs a DeltaVscf file
 %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
     stopping ...

FINDING 1b — electron_phonon='simple' gives the SAME error (ph_probe2.out):
     Error in routine phq_readin (1):
     El-ph needs a DeltaVscf file
=> In QE 7.5, el-ph is a TWO-PASS workflow: pass 1 = a plain DFPT phonon run
   (no electron_phonon, with fildvscf set) that computes+saves DeltaVscf; pass 2
   = re-run ph.x with electron_phonon reading those potentials. The single-pass
   ph.in given in the task spec cannot work by construction.

------------------------------------------------------------------------
FINDING 2 — ph.x DOES support nspin=2 (magnetic) + ultrasoft DFPT. The plain
DFPT pass-1 (electron_phonon unset, fildvscf='dvscf') passed input validation,
read the magnetic wavefunctions, built the perturbation problem, and BEGAN the
q=Gamma linear-response solve. Verbatim (ph_dvscf.out), np=4:

     Program PHONON v.7.5 starts on 14Jun2026
     Reading xml data from directory: ./out_probe/cosn.save/
     Exchange-correlation= PBE
     Dynamical matrices for ( 2, 2, 2)  uniform grid of q-points
     (   4 q-points):
       N         xq(1)         xq(2)         xq(3)
       1   0.000000000   0.000000000   0.000000000
       2   0.000000000   0.000000000  -0.619732276
       3   0.000000000  -0.577350269   0.000000000
       4   0.000000000  -0.577350269  -0.619732276
     Number and degeneracy of irreps per q-point
       1 (Gamma)                                       N irreps = 12
       2   0 0 -0.6197                                 N irreps = 12
       3   0 -0.5774 0                                 N irreps = 18
       4   0 -0.5774 -0.6197                           N irreps = 18
     Saving dvscf to file.
     Calculation of q =    0.0000000   0.0000000   0.0000000
     There are   12 irreducible representations
     Representation     1      1 modes -  To be done
     ... (Representations 1-12, 18 modes total) ...
     Representation    12      2 modes -  To be done

=> NO nspin/USPP limitation hit. The task's hypothesized "ph.x may refuse el-ph
   with magnetism" is REFUTED for this build: QE 7.5 ph.x runs magnetic
   ultrasoft DFPT. (Caveat: el-ph DOUBLE-DELTA accuracy for a magnetic metal
   would still need a much finer el-ph k-mesh than computed here.)

------------------------------------------------------------------------
BLOCKER (actual) — DFPT linear-response COST on a saturated shared host.
After ~7 min wall on q=Gamma, ZERO self-consistent linear-response iterations
('iter #') had printed — the first representation's solve had not converged once.
Host contention (verbatim /proc/loadavg samples): 31.37 / 33.00 / 34.42 on a
12-core box (2.6x oversubscription) — two FOREIGN live QE jobs (a doping nscf +
an scf_q4.0 doping sweep, both other sessions, left untouched per
a_dont_kill_live_compute) + the rbfe co-tenant were time-slicing my ph.x ranks
to near-zero throughput. 4 q-points x 12-18 irreps x nspin=2 ultrasoft
self-consistent linear response for a 6-atom metal is genuinely heavy and is
beyond a ~40-min box on a contended CPU host. NO lambda/Tc fabricated.

PATH TO COMPLETION (not done here): dedicated cores (or a GPU/runpod pod, no
contention) -> (1) dense el-ph SCF; (2) plain DFPT phonon pass with fildvscf to
build DeltaVscf over the 2x2x2 q-grid; (3) ph.x electron_phonon='interpolated'
pass reading DeltaVscf on a fine el-ph k-mesh; (4) lambda / omega_log /
Allen-Dynes Tc (mu*=0.10) from the a2F summary. Magnetism + the Co-3d flat band
at E_F (RTSC_21) make the el-ph integration delicate; a non-magnetic control and
a converged el-ph k-mesh are advised.

HONEST: no value of lambda, omega_log, or Tc was produced. The two concrete,
verbatim findings are (1) the two-pass DeltaVscf requirement and (2) that QE 7.5
ph.x does NOT reject magnetic ultrasoft DFPT. rbfe_hsp90 alive at end; all my
QE workers stopped; out_probe/out_elph scratch removed.
