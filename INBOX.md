# INBOX — anima 현재 상태

> 현재 열린 cross-project handoff 만 기록 (`- [ ]`). 처리 이력 전체는 `INBOX.log.md` (append-only 로그). sidecar Decision 7 의 INBOX 도메인 쌍(스냅샷 + 로그) 모델.

## 열린 handoff

- [ ] **life-domain-stale-untracked-ssot-shadow** — working-copy fix 적용(LIFE.md main 동기화 + DOMAINS.tape 등록), **publish-gap 잔여**: orphan-recover 브랜치가 root SSOT(LIFE.md·BRAIN.md)를 stale 사본으로 main 을 shadow 중 + DOMAINS.tape·AGENT/CORE/DECODER SSOT origin/main 미추적. clean anima 세션에서 격리 worktree off main 로 1회 publish 필요. (상세 이력 → `INBOX.log.md`)
