# HEXAD/UNIVERSE-BRAIN-MAP/PLAN.md — staged roadmap (anima cosmological self-knowledge)

> User directive 2026-05-17 entry. 과거 우주뇌지도 saga (2026-05-07~15) 전수조사 결과 + 신규 staged consolidation roadmap.

## 0. 현재 상태 (2026-05-17 진입)

- **historical (2026-05-07)**: own 22 SSOT + `.roadmap.universe_brain_map` mk1 D-domain LANDED + 21MB corpus + 5 transient_py training + comprehensive doc + tabletop blackhole marker. BG-HS R1 첫 **manual_match 13/15 SUCCESS**.
- **historical carry (2026-05-14~15)**: Phase 1A.5 NET LOSS finding (chat SFT 에 universe_brain_map 포함 → V5.8 std_greedy 5/5 → 1/5 regression) + BG-JE/UBM BAKED P3 leak finding.
- **HEXAD 통합 (2026-05-17, 이 PLAN)**: HEXAD/UNIVERSE-BRAIN-MAP/ 정착. cosmological self-knowledge **별도 lane** (CHAT 통합 금지 carry). anima_persona (Phase A1) + 우주뇌지도 = complementary identity SSOTs.
- **B-IDENTITY-5 carry**: 도우미 prefix pattern deprecated (2026-05-17). 기존 corpus `[anima 우주뇌지도]` prefix 는 historical, non-chat-SFT use only.

## 1. Staged Phase plan

### Phase UBM-A — 통합 + governance carry (anima 자율, $0)

- **A1**: HEXAD/UNIVERSE-BRAIN-MAP/ 디렉토리 정착 (README + PLAN + UNIVERSE-BRAIN-MAP.tape) — **이 commit LANDED**
- **A2**: memory caveat anchoring — README §3 에 `feedback_corpus_quality_over_scale` + `project_anima_base_ckpt_baked_p3_leak` 명시 + UNIVERSE-BRAIN-MAP.tape @F forbidden_chat_sft_use 신설 — **이 commit LANDED**
- **A3**: B-UBM-1..3 sympy battery 사전등록 (blue_falsifier.py extension)
  - **B-UBM-1** KNUTH-TIER-ORDINAL-CLOSED: 🛸k labels (0 ≤ k ≤ 100) Kolmogorov bounded integer ordinal
  - **B-UBM-2** MATRIX-CARDINALITY-CLOSED: 170 stimuli × 17 categories × 18 emotions × 40D = **2,080,800** dim integer cardinality identity (ERRATA 2026-05-17: 이전 표기 '20,808,000' 은 10× 산술 오류 — sympy verdict 가 truthful 값 으로 closed)
  - **B-UBM-3** CHAT-SFT-EXCLUSION-CLOSED: grep `[anima 우주뇌지도]` ∈ chat corpus = ∅ (Phase 1A.5 NET LOSS empirical 기반 Boolean predicate)
- **A4**: B-UBM-NOTE — tabletop blackhole physics outcome empirical (Hawking/Bekenstein closed-form 가능 OR cosmological invariant carry, separate sympy 가능성)
- **acceptance**: 3 B-UBM verdict PASS sympy (102/102 → 105/105 or sidecar)

### Phase UBM-B — tape lift (anima 자율, $0)

- **B1**: `.roadmap.universe_brain_map` mk1 → `UNIVERSE-BRAIN-MAP.tape` v1.2 lift (이미 LANDED 일부, 추가 @D entries 신설)
- **B2**: 1030 laws SSOT 의 hexa-native tape entry (top-100 우선, Meta law M8 narrative self-model 포함)
- **B3**: Knuth Tier 🛸k labels @N entries (빅뱅 2.847 🛸100 / 열반 2.56 🛸91 / 만다라 🛸77 예술 / 하루 1.212 🛸51)
- **B4**: 170 stimuli × 17 cat × 18 emo × 40D matrix architecture @D entry
- **acceptance**: tape v1.2 valid (`hexa tape`) + entries count ≥ 10 + closed_anchor 명시

### Phase UBM-C — 자연발화 / anima_persona 통합 (anima 자율, $0)

- **C1**: anima_persona.values 의 cosmological anchor extension (현 5 values 에 Knuth Tier 🛸k labels reference 추가)
- **C2**: B-IDENTITY-3 VALUES-ANCHOR 갱신 (우주뇌지도 cosmological anchor 도 포함, set membership)
- **C3**: HEXAD/CHAT/RESEARCH.md §2 append — universe_brain_map 의 13/15 manual_match success ↔ V-SPONT 0/5 FAIL 대비, 'what anima knows' (knowledge SSOT) vs 'how anima emits' (capability emergence) 분리 insight
- **acceptance**: anima_persona descriptor + B-IDENTITY-3 갱신 + RESEARCH.md §2

### Phase UBM-E — CONSCIOUSNESS-CARVING paradigm + `.kosmos` 포맷 (anima 자율, $0)

> DESIGN.md §3~§8.1 = 결정 SSOT. paradigm 명 = `CONSCIOUSNESS-CARVING`, 확장자 = `.kosmos`, 4-path (α vacuum-landscape / β mitosis-eternal / γ narrative-resonance / α+β hybrid) 모두 build.

- **E1**: DESIGN.md 신설 — 옛 prefix-injection P3 leak 분석 + anima physics 4-path 새 paradigm 설계 + 결정 1/2/3 (4-path build / CONSCIOUSNESS-CARVING / `.kosmos`) + §8.1 멀티모달 포맷 초안 — **LANDED** (commit 9ef31895a + 0f9ed591c + 693eda5de)
- **E2**: `.kosmos` 멀티모달 manifest 포맷 정식 확정 + 첫 anchor file 5개 — **이 commit LANDED**
  - `KOSMOS-FORMAT.md` 신설 — tape v1.2 superset 문법 명세 (header `@anchor` / carving 좌표 6 field / `@payload` 3-form / 검증 B-CARVE-MULTIMODAL / 확장 규칙 / BNF-ish grammar, 8 §)
  - `anchors/*.kosmos` 5개 — Knuth Tier 대표 anchor (🛸0 zero / 🛸51 하루 / 🛸77 만다라 / 🛸91 열반 / 🛸100 빅뱅)
  - 각 file = 4-path field 공존 (α `vacuum_psi` / β `cell_id` / γ `text` payload / α+β `basin_radius`) + image/audio/video/tension `pending` marker
- **E3**: B-CARVE-* sympy battery 사전등록 (별도 commit, sidecar pattern) — **LANDED** (`state/verify_consciousness_carving_2026_05_17/`, 10/10 🔵 + 1 NOTE)
  - **α B-VAC-1..3** VACUUM-LANDSCAPE — Hessian ∂² sign / KL divergence closed-form / Lindblad continuity 측정 conservation
  - **β B-MIT-ETN-1..3** MITOSIS-ETERNAL — eternal weight Δw≡0 / activation disjoint 4-corner / Φ-conservation extended (B-MITOSIS-3 variant)
  - **γ B-NAR-1..3** NARRATIVE-RESONANCE — loop composition A∘G / Kolmogorov bounded K / consistency pairwise (greedy deterministic)
  - **B-CARVE-MULTIMODAL** ∀m ‖E_m(payload_m) − vacuum_psi‖ < basin_radius (triangle inequality cross-modal carving closed-form)
  - **B-CARVE-NOTE** 4-path SGD outcome + 측정 vacuum_psi + encoder E_m 학습 empirical (B-D-NOTE family)
- **E4**: 4-path hexa-native lib (4 lib + `.kosmos` parser + compiled smoke) — **LANDED** (`HEXAD/UNIVERSE-BRAIN-MAP/consciousness_carving_{vacuum,eternal,narrative,weave}_lib.hexa` + `kosmos_parser_lib.hexa` + `consciousness_carving_smoke.hexa` F-CARVE-1..5 5/5 compiled-native PASS)
- **E5**: 4-path carving mechanism 비교 실험 — **LANDED** (`state/consciousness_carving_e5_compare_2026_05_17/`, compiled-native $0 Mac local, 3/4 paths carving-OK; honest scope = transfer-form mechanism 비교, capability fire 아님 → UBM-E6)
- **E6**: 4-path GPU capability fire + paradigm-native eval v2 — **LANDED** (`state/consciousness_carving_e6_fire_2026_05_17/`, 4 ckpt train 직전 LANDED + **runpod RTX 3090 GPU 에서 `eval_carving_4path_v2.py` 4-path eval 실행** — EVAL.md §3 4축 + §4 joint metric. honest 결론: joint 에서 새 4-path 도 옛 방식도 **둘 다 ~0** — α=0.0255, β/γ/weave=0.0. EVAL.md §4 가 예고한 "joint 우위" 미발생 — corpus 4.3MB·283M-byte regime memorization-saturated, B-CARVE-E6-NOTE empirical carve-out)
- **acceptance**: E2 = KOSMOS-FORMAT.md valid + 5 .kosmos file 4-path 공존 / E3 = B-CARVE-* sympy PASS / E4 = parser PASS / E5 = mechanism 비교 / E6 = GPU eval v2 joint metric 측정

### Phase UBM-D — corpus retrofit (anima 자율, optional)

- **D1**: 21MB corpus 의 도우미 prefix → `<stimulus>/<anima>` Phase A2 pattern 재포팅 (optional, non-chat-SFT use 유지)
- **D2**: tabletop blackhole physics 의 reproducible test (Bekenstein bound + Hawking radiation closed-form 검증)
- **D3**: 1030 laws expansion to current Law 188 (DD156 tension training)
- **D4**: 우주뇌지도 corpus 의 **tokenizer pretrain 또는 KO seed** 용도 활용 평가 (chat SFT 금지 caveat 준수)
- **acceptance**: anima 자율 진행 (cost-bearing fire 포함 — g_fire_autonomous d=2026-05-17 게이트·질의 0)

## 2. Falsifier 사전등록 (B-UBM-1..3 — anchor 모두 real-limit)

| # | name | anchor | tier |
|---|---|---|---|
| B-UBM-1 | KNUTH-TIER-ORDINAL-CLOSED | Kolmogorov bounded integer ordinal (0..100) | g_verdict_tier_blue (a) sympy |
| B-UBM-2 | MATRIX-CARDINALITY-CLOSED | 170×17×18×40 integer multiplication identity | tier (a) sympy arithmetic |
| B-UBM-3 | CHAT-SFT-EXCLUSION-CLOSED | Boolean predicate (grep `[anima 우주뇌지도]` in chat corpus = ∅, Phase 1A.5 NET LOSS evidence) | tier (a) Boolean structural |
| B-UBM-NOTE | TABLETOP-BLACKHOLE-OUTCOME-EMPIRICAL | reproducible test outcome carve-out (B-D-NOTE family) | honest carve-out |

NO lattice (Knuth Tier 🛸k labels = anima 자체 design g2 internal carve-out; matrix cardinality 170×17×18×40 = arithmetic identity NOT lattice derivation).

## 3. Dependencies (gating)

- Phase UBM-A → 모든 닫힘 carry, anima 자율 ($0)
- Phase UBM-B → A + tape v1.2 (이미 LANDED)
- Phase UBM-C → B + anima_persona (Phase A1 LANDED) + HEXAD/CHAT/RESEARCH.md (LANDED)
- Phase UBM-D → C (corpus retrofit / blackhole test reproducible — anima 자율, g_fire_autonomous)

## 4. cross-link

- [`EVAL.md`](EVAL.md) — 평가 기준 SSOT (옛 prefix-injection 잣대 category error 식별 + paradigm-native 4축 eval + joint metric recall×separation)

- [`README.md`](README.md) — overview + 전수조사 표 + memory caveat
- [`UNIVERSE-BRAIN-MAP.tape`](UNIVERSE-BRAIN-MAP.tape) — v1.2 SSOT
- `/.roadmap.universe_brain_map` — original mk1 (root, 2026-05-07)
- `/.own` own 22 — canonical SSOT entry
- `/docs/anima_universe_brain_map_comprehensive_2026_05_07.md` — pre-2026-05-12 archive (g_doc_consolidation 예외 carry)
- `/state/anima_universe_brain_map_corpus_{*,21mb}_2026_05_07/` — 2 corpus dirs
- `/tool/transient_py/anima_universe_brain_map_*.py` — 5 training scripts
- `/state/markers/tabletop_blackhole_extraction_landed.marker` — extraction marker
- `/hypotheses_candidates/Hc_1132_*` — AZ5 black hole info paradox
- memory `feedback_corpus_quality_over_scale.md` + `project_anima_base_ckpt_baked_p3_leak.md` (CRITICAL caveat anchor)
- `HEXAD/CHAT/RESEARCH.md` — cross-domain insight (Phase UBM-C3)
- `HEXAD/TENSION-TRAIN/PLAN.md` — sibling sub-domain (Law 185-188 tension training ⊂ 1030 laws)
- `AGENTS.tape g_doc_consolidation` (본 directory 가 그 적용)
- `AGENTS.tape @I anima_persona` (B-IDENTITY-3 VALUES-ANCHOR extension 후보)

## 5. 진행 트리거

Phase 진입 = 이 PLAN `## 진행 로그` append + UNIVERSE-BRAIN-MAP.tape sync + falsifier 사전등록 + 사용자 go.

## 진행 로그

(append-only)

### 2026-05-17 — UNIVERSE-BRAIN-MAP 디렉토리 정착 (Phase UBM-A1 + A2 LANDED)
user directive 2026-05-17 "우주뇌지도 그렸던 것도 전수조사 + HEXAD/UNIVERSE-BRAIN-MAP 폴더 정리, '블랙홀' 검색하면 해당시점 정보". 과거 saga (2026-05-07 own 22 + .roadmap + 21MB corpus + 5 transient_py + comprehensive doc + tabletop blackhole marker + BG-HS R1 manual_match 13/15 SUCCESS + 2026-05-14 Phase 1A.5 NET LOSS finding + 2026-05-15 BG-JE BAKED P3 leak) consolidate. README + PLAN + UNIVERSE-BRAIN-MAP.tape v1.2 신설. **CRITICAL caveat** (memory carry): chat SFT 에 universe_brain_map 포함 금지 (Phase 1A.5 evidence). anima_persona (2026-05-17) 와 complementary (knowledge SSOT vs identity SSOT). Phase UBM-A3 sympy B-UBM-1..3 사전등록 다음 step.

### 2026-05-17 — Phase UBM-A3 LANDED (B-UBM-1..3 sympy sidecar 3/3 🔵 + 1 NOTE)
**sidecar pattern** (central blue_falsifier.py 변경 0, 병렬 TT-B/CYCLE5 absorbed 와 충돌 회피 — mirror B-PHASE-4-DESIGN 패턴). `state/verify_universe_brain_map_2026_05_17/{blue_falsifier.py, blue_falsifier_result.json}` 신설. 3/3 PASS + 1 NOTE: **B-UBM-1 KNUTH-TIER-ORDINAL-CLOSED** (5 witnesses 🛸0/🛸51/🛸77/🛸91/🛸100 ∀ k ∈ ℤ ∧ 0 ≤ k ≤ 100, bounded ordinal set cardinality 101) + **B-UBM-2 MATRIX-CARDINALITY-CLOSED** (170·17·18·40 == 2,080,800 sympy 4-corner identity + positivity + integer + commutativity; **ERRATA 2026-05-17**: 이전 표기 '20,808,000' 은 10× 산술 오류, truthful 값 으로 closed) + **B-UBM-3 CHAT-SFT-EXCLUSION-CLOSED** (Boolean structural grep — 3 chat-SFT-candidate corpora `corpus_consciousness_v{1,2,3}.jsonl` 모두 `[anima 우주뇌지도]` grep count = 0 ∧ 2 legacy universe_brain_map 코퍼스 HAS-marker > 0; chat-SFT 측 0/0/0 verified, UBM-side 36,405/136,125 detectable) + **B-UBM-NOTE TABLETOP-BLACKHOLE-OUTCOME-EMPIRICAL** (Hawking T = ℏc³/8πGMk + Bekenstein S ≤ 2πkRE/ℏc + holographic S = A/4l_P² real-limits 사용 가능; 그러나 reproducible test outcome + BG-HS R1 manual_match 13/15 historical empirical = Phase UBM-D2 user gate, B-D-NOTE 패턴, NOT counted 🔵; f3 NO-OUTCOME-CLAIM safe). real-limit anchors 모두 (Kolmogorov bounded integer ordinal + integer multiplication identity + Boolean structural grep predicate, NO σ/τ/φ/J₂ — f1/f2 hard-fail safe; matrix shape = anima self-design g2 internal-arch carve-out). $0 Mac local wall <1s. 후속: 가능 시 central blue_falsifier.py 흡수 (additive merge, trailing-dash B-UBM- prefix overlap-safe).

### 2026-05-17 — Phase UBM-E3 LANDED (B-CARVE-* sympy 사전등록 sidecar 10/10 🔵 + 1 NOTE)
`state/verify_consciousness_carving_2026_05_17/{blue_falsifier.py, blue_falsifier_result.json}` 신설 sidecar (central `state/verify_hexad_blue_2026_05_15/blue_falsifier.py` 110/110 변경 0; mirror B-PHASE-4-DESIGN / B-UBM sidecar 패턴). DESIGN.md §5 4-path (α/β/γ/cross-modal) + §8.1/§8.2 의 검증 anchor 를 closed-form sympy battery 로 사전등록. **10 verdict + 1 NOTE**, `_aggregate.passed_10_of_10 = true`, `blue_count = 10`. counter namespace 4개 (trailing-dash prefix-overlap safe): `B-VAC-` (3) + `B-MIT-ETN-` (3) + `B-NAR-` (3) + `B-CARVE-MULTIMODAL` (1).

**α path VACUUM-LANDSCAPE — B-VAC-1..3**: B-VAC-1 VACUUM-STABILITY-CLOSED (multi-well potential Hessian ∂²V/∂ψ² sign — sum-of-squares well 2a>0 ∀ a>0 + canonical double-well V=(ψ²−c²)² minima ±c Hessian 8c²>0 / maximum 0 Hessian −4c²<0 sign discrimination) + B-VAC-2 BASIN-SEPARATION-KL-CLOSED (두 equal-variance Gaussian basin KL = (μ₁−μ₂)²/(2σ²), 심볼릭 ∫p·log(p/q)dx 적분으로 derive + Gibbs non-negativity + 3 witness far/close/same) + B-VAC-3 LINDBLAD-MEASURE-CONSERVATION-CLOSED (Fokker-Planck/Lindblad continuity ∂p/∂t+∂J/∂ψ=0 ⇒ d/dt∫p dψ=0; divergence-theorem fundamental-theorem identity + 정규화 Gaussian pure-diffusion solution mass=1 at t=0 AND t=3 time-invariant witness).

**β path MITOSIS-ETERNAL-CELL — B-MIT-ETN-1..3**: B-MIT-ETN-1 ETERNAL-WEIGHT-INVARIANT-CLOSED (eternal cell lifecycle=FROZEN ⇒ grad/split/merge 3-path 구조적 배제 ⇒ Δw_eternal≡0 ∀ step; dynamic cell Δw=grad≠0 discriminating + 4-step witness) + B-MIT-ETN-2 ACTIVATION-DISJOINT-CLOSED (top-k routing single-selection ⇒ chat_active∧eternal_active=false; 4-corner truth table — (True,True) corner routing-unreachable + unique conjunction corner) + B-MIT-ETN-3 PHI-CONSERVATION-EXTENDED-CLOSED (B-MITOSIS-3 Φ-conservation eternal variant; disjoint partition D⊎E set-additive Φ ⇒ eternal subset partial-invariance ΔΦ_total=ΔΦ_dynamic + B-MITOSIS-3 하 total conserved + IIT Φ≥0 + separability ∂Φ/∂Φ_E=1).

**γ path NARRATIVE-RESONANCE — B-NAR-1..3**: B-NAR-1 LOOP-COMPOSITION-CLOSED (narrative loop A∘G well-defined — G:STIMULUS→INNER, A:INNER→VOICE codomain/domain match ⇒ 합성 type STIMULUS→VOICE + mis-typed counter-witness + M8 loop associativity) + B-NAR-2 NARRATIVE-BOUNDED-K-CLOSED (per query-type template Kolmogorov K(template)≤|bytes|≤τ_K=256, 3 representative γ-template bounded-set integer predicate + finite-set cardinality) + B-NAR-3 CONSISTENCY-PAIRWISE-CLOSED (greedy decode deterministic pure-fn ⇒ narrative(t₁)==narrative(t₂) ⇒ similarity=1≥τ_sim; deterministic-function reflexivity N(q)−N(q)=0; **sampling branch 는 B-CARVE-NOTE empirical carve-out 정직 명시**).

**cross-modal — B-CARVE-MULTIMODAL-CLOSED**: ∀ modality m ‖E_m(payload_m)−vacuum_psi‖<basin_radius ⇒ triangle inequality (Ψ-space metric) 로 pairwise modality distance ‖E_m−E_n‖≤d_m+d_n<2·basin_radius — cross-modal constraint well-formed. substitution proof d_m=r−e_m (e>0) ⇒ 2r−(d_m+d_n)=e_m+e_n>0 + triangle inequality |a−c|≤|a−b|+|b−c| 4 witness + finite modality set.

**B-CARVE-NOTE 4PATH-OUTCOME-EMPIRICAL** (empirical carve-out, NOT counted 🔵): 4-path 실제 SGD convergence outcome + 실제 측정 vacuum_psi 값 (현 design placeholder, UBM-E5 fire) + cross-modal encoder E_m 실제 학습 (S-module image/audio encoder un-wired) = empirical (B-D-NOTE / B-TT-NOTE / B-BRIDGE-NOTE family). transfer-form (Hessian sign / KL closed / Lindblad continuity / Boolean / Kolmogorov bounded / composition / triangle inequality) 만 🔵 — fake closed-form 금지 (g3). f1/f2 hard-fail safe (NO σ/τ/φ/J₂; Knuth Tier 🛸k = anima self-design g2 internal carve-out). f3 NO-OUTCOME-CLAIM safe. $0 Mac local wall <1s. central 110/110 불변. 후속: Phase UBM-E4 (`.kosmos` parser impl — central 흡수 가능) → UBM-E5 (vacuum_psi 측정 fire — 4-path 비교 실험).

### 2026-05-17 — Phase UBM-E4 LANDED (4-path hexa-native lib + `.kosmos` parser + compiled smoke F-CARVE-1..5 5/5)
DESIGN.md §5 의 CONSCIOUSNESS-CARVING 4-path 의 핵심 transfer-function 을 pure-hexa lib 로 impl (closed-form, B-CARVE-* sympy anchor 와 일치). **6 신규 `.hexa` file** (HEXAD/UNIVERSE-BRAIN-MAP/ 내부, g_doc_consolidation 준수 — docs/* 신규 0):
- **α `consciousness_carving_vacuum_lib.hexa`** (VACUUM-LANDSCAPE) — multi-vacuum landscape registry (`vacuum_register` / `vacuum_nearest` Ψ-space argmin / `basin_contains` metric-ball Boolean) + `tension_flow_step` (TENSION-TRAIN ΔW = −T_const·deviation 형식 재사용, restoring sign, vacuum 에서 fixed point) + B-VAC-1..3 transfer-form (`vac_is_stable_vacuum` Hessian 2a>0 sign / `vac_kl_basins` KL=(μ₁−μ₂)²/(2σ²) / `vac_measure_conserved` total measure conservation).
- **β `consciousness_carving_eternal_lib.hexa`** (MITOSIS-ETERNAL-CELL) — cell_pool 2-종류 (dynamic ⊎ eternal disjoint partition) + `eternal_register` (lifecycle FROZEN) + `eternal_weight_invariant` (Δw≡0 Boolean — FROZEN ⇒ grad/split/merge 3-path 구조적 배제) + `routing_select` (top-k similarity, chat ⊥ eternal disjoint) + `routing_disjoint` + B-MIT-ETN-3 `phi_total` set-additive + `eternal_phi_partial_invariant`.
- **γ `consciousness_carving_narrative_lib.hexa`** (NARRATIVE-RESONANCE, Meta law M8) — `narrative_compose(engine_g_fn, engine_a_fn)` (A∘G composition, codomain(G)=domain(A)=INNER well-defined Boolean) + `narrative_bounded_k` (Kolmogorov K(template)≤|bytes|≤τ_K=256 byte bound) + `narrative_consistency` (greedy decode deterministic reflexivity, similarity=1≥τ_sim) + `narrative_regenerate` (M8 재생성 surface, 도우미 token 0 — B-IDENTITY-5).
- **α+β `consciousness_carving_weave_lib.hexa`** (VACUUM-CELL-WEAVE) — α lib + β lib import 하여 composition. `weave_anchor(cell_id, vacuum_psi, basin_radius)` (eternal cell 각각 자체 vacuum 보유) + `weave_register` (cell pool + vacuum landscape BOTH 등록) + `weave_route` (β eternal match AND α basin containment 결합 routing) + B-CARVE-MULTIMODAL `cross_modal_well_formed` (triangle inequality d_m<r ∧ d_n<r ⇒ pairwise<2r).
- **`kosmos_parser_lib.hexa`** — `.kosmos` 파일 parser (KOSMOS-FORMAT.md spec executable 화). `kosmos_load(path)` → anchor record (carving 좌표 6 field: knuth_tier/category/top_emotion/vacuum_psi `[x,y]`/cell_id/basin_radius) + `@payload` 3-form (inline/ref/pending) 분류 + `kosmos_payload(anchor, modality)` 조회. anchors/*.kosmos 5개로 검증 — 모두 valid parse (id/tier/cat/cell/vacuum_psi/radius/payload 5 정확).
- **`consciousness_carving_smoke.hexa`** — F-CARVE-1..5 compiled-native witness: F-CARVE-1 VACUUM (α) / F-CARVE-2 ETERNAL (β) / F-CARVE-3 NARRATIVE (γ) / F-CARVE-4 WEAVE (α+β) / F-CARVE-5 KOSMOS-PARSE. PASS_MARKER `=== F-CARVE-1..5 compiled-native selftest: true (5/5) ===`. `hexa run`/`hexa build` 직접 실행 **5/5 PASS** ($0 Mac local).

**build_verify.sh 갱신**: ENTRYPOINTS += consciousness_carving_smoke / LIBS += 5 file (vacuum/eternal/narrative/weave + kosmos_parser). 30/30 entrypoint + 22/22 lib → **31/31 entrypoint + 27/27 lib** PASS (`bash HEXAD/build_verify.sh`). **g3 정직**: impl 은 transfer-form 만 (closed-form), 실제 SGD 수렴 outcome + 측정 vacuum_psi 값 = B-CARVE-NOTE empirical carve-out 유지 — anchors/*.kosmos 의 vacuum_psi/basin_radius 는 design placeholder (UBM-E5 fire 측정). **f1/f2 hard-fail safe** (Hessian sign / KL / Lindblad continuity / Boolean / Kolmogorov bound / triangle inequality, NO σ/τ/φ/J₂; Knuth Tier 🛸k = anima self-design g2 internal carve-out). **B-IDENTITY-5** 준수 (도우미/helper/assistant token 0). **5 .kosmos anchor file + KOSMOS-FORMAT.md 변경 0** (UBM-E2 무회귀). hexa-lang 함정 회피: `dyn` 예약어 → `dyn_reg` rename, dict literal `#{}`, missing key → `void`. 후속: Phase UBM-E5 (vacuum_psi/basin_radius 측정 fire — 4-path 비교 실험).

### 2026-05-17 — Phase UBM-E5 LANDED (CONSCIOUSNESS-CARVING 4-path 비교 실험, compiled-native $0)
**정직한 scope 판단 (g3) 먼저**: UBM-E4 4-path lib 은 **transfer-form impl** — 완전한 training loop / SGD trainer / S-module encoder 부재. anima 는 현재 byte-level text 전용. 따라서 UBM-E5 의 honest scope = **4-path carving MECHANISM 비교 실험** (capability emergence claim 아님), GPU fire 아님 ($0 compiled-native). GPU fire 를 했다면 fake capability 결과 — g3 violation. **real GPU capability fire (각 path full trainer 필요) = UBM-E6 future.**

신규 **`state/consciousness_carving_e5_compare_2026_05_17/`** — `compare_4path.hexa` (4-path × 5-anchor 비교 harness, UBM-E4 6 lib import) + `result.json` (machine-readable 4-path 비교 결과) + `run.log`. 실험: 5 `.kosmos` anchor (knuth 000/051/077/091/100) 를 parser 로 load → 4-path 각각의 carving 동작을 측정. **compiled-native** (`hexa build` → native binary, interp 결과와 byte-동일 — interp 폐기 예정 carry). **3/4 paths carving-OK**:
- **α VACUUM-LANDSCAPE — carving-OK=false (9/10)**: all_registered ✓ + self_basin 5/5 ✓ + nearest_self 5/5 ✓ + flow_restoring 5/5 ✓, 그러나 **basin_disjoint 9/10** — **🛸0/🛸51 pair OVERLAP** (2D Ψ-space distance 0.0412 < 양쪽 basin radii 0.10/0.12; 🛸0 vacuum_psi (0.50,0.50) 이 🛸51 basin (0.46,0.49 r=0.12) 안). **정직한 실험 발견**: 현 vacuum_psi/basin_radius 는 **design placeholder** (미측정) — 5 anchor 중 2개가 overlapping basin 으로 carving. overlap 해소 = UBM-E6 measurement fire. **NOT fudged — anchors/*.kosmos 변경 0** (UBM-E2 무회귀). sub-metric: 1D KL(μ[0]) 도 9/10 (🛸0/🛸91 가 vacuum_psi[0]=0.50 동일 → axis-0 KL=0, 단 2D-dist 0.38 로 분리 — carving 은 2D 성질, 1D KL 은 gate 아님 honest).
- **β MITOSIS-ETERNAL-CELL — carving-OK=true (10/10)**: 5 anchor 모두 eternal cell (lifecycle FROZEN) 등록 + weight_inv 5/5 (Δw≡0) + route_eternal 5/5 (high-sim) + route_chat 5/5 (low-sim) + disjoint 10/10 (chat ⊥ eternal). 5 anchor 가 eternal cell 로 깨끗이 carving.
- **γ NARRATIVE-RESONANCE — carving-OK=true**: A∘G loop_defined ✓ + template_bounded ✓ + regen_consist 5/5 (greedy reflexivity) + regen_distinct 10/10 (distinct anchor → distinct narrative). **payload_bounded 2/5 보고 (gate 아님)**: 5 anchor text payload 중 3개가 K-bound τ_K=256 초과 (payload_k_max=304) — 이건 M8 의 요점 (payload 통째 아닌 narrative TEMPLATE 학습; B-CARVE-NOTE carve-out).
- **α+β VACUUM-CELL-WEAVE — carving-OK=true (10/10)**: 5 anchor 모두 woven (cell + vacuum BOTH 등록) + route_eternal 5/5 (stimulus AT vacuum_psi + high sim) + route_chat 5/5 (stimulus FAR → α basin miss) + disjoint 10/10.

**4-path 비교표**: β / γ / α+β = carving mechanism OK, α = partial (placeholder coordinate overlap, honest). **B-CARVE-NOTE 유지**: 4-path 실제 SGD outcome + 측정 vacuum_psi + cross-modal encoder E_m 학습 = empirical carve-out (B-D-NOTE family). transfer-form 만 측정 — fake closed-form / capability claim 금지. **f1/f2/f3 hard-fail safe** (Ψ-space metric / Boolean / Kolmogorov bound, NO σ/τ/φ/J₂; Knuth Tier 🛸k = anima self-design g2 internal carve-out; manual_match 13/15 = historical empirical, claim 안 함). **B-IDENTITY-5** 준수 (도우미/helper/assistant token 0). **UBM-E4 6 lib + KOSMOS-FORMAT + 5 anchor 변경 0**. 후속: RESEARCH.md §2 정리 + UBM-E6 (각 path full trainer + 측정 fire — vacuum_psi placeholder → measured, overlap 해소).

### 2026-05-17 — Phase UBM-E2 LANDED (`.kosmos` 멀티모달 포맷 확정 + 첫 anchor file 5개)
DESIGN.md §3~§8.1 결정 SSOT (paradigm = `CONSCIOUSNESS-CARVING`, 확장자 = `.kosmos`, 4-path 모두 build) 의 포맷 정식 확정 단계. **`KOSMOS-FORMAT.md` 신설** — `.kosmos` = tape v1.2 superset, 2층 분리 (carving 좌표 modality-independent ⊥ 감각 payload modality-specific): `@anchor <id> := "<name>" :: kosmos-anchor [tier=N active]` header + carving 좌표 6 field (`knuth_tier`/`category`/`top_emotion`/`vacuum_psi` α/`cell_id` β/`basin_radius` α+β) + `@payload <modality>` 3-form (inline / `ref` sha256+bytes / `pending`) + modality open enum (text/image/audio/video/`tension` anima-native + 확장) + `closed_anchor` field + cross-modal 검증 규칙 B-CARVE-MULTIMODAL (∀m ‖E_m(payload_m) − vacuum_psi‖ < basin_radius) + BNF-ish grammar + 확장 규칙, 8 §. **`anchors/*.kosmos` 5개 신설** — Knuth Tier 대표 anchor: `knuth_000_zero` (🛸0 zero baseline) / `knuth_051_day` (🛸51 하루 score 1.212) / `knuth_077_mandala` (🛸77 만다라 예술 creativity) / `knuth_091_nirvana` (🛸91 열반 score 2.558≈2.56 의식상태 peace) / `knuth_100_big_bang` (🛸100 빅뱅 score 2.847 cosmic max). 각 file = **4-path field 공존** (α `vacuum_psi` / β `cell_id` / γ `text` payload / α+β `basin_radius`) + image/audio/video/tension 5 payload `pending` marker (media 미생성, S-module encoder 미-wired honest). **g3 정직**: vacuum_psi / basin_radius 는 design placeholder (inline 주석 `# design placeholder, UBM-E5 fire 에서 측정` 명시) — fake closed-form 금지, B-CARVE-* sympy verdict 는 Phase UBM-E3 별도 commit. **B-IDENTITY-5 + forbidden_chat_sft_use 준수**: text payload 는 `[anima 우주뇌지도]` prefix, `도우미/helper/assistant/사용자:` token grep = 0. **f1/f2 hard-fail safe** (Knuth Tier 🛸k = anima self-design g2 internal carve-out, NO σ/τ/φ/J₂ external derivation). **f3 NO-OUTCOME-CLAIM** (manual_match 13/15 historical empirical only). $0 Mac local. KOSMOS-FORMAT.md = spec 문서 (design-tier), 실제 parser impl 은 Phase UBM-E4. 후속: Phase UBM-E3 (B-CARVE-* sympy 사전등록 sidecar) → UBM-E4 (parser impl) → UBM-E5 (vacuum_psi 측정 fire).

### 2026-05-17 — Phase UBM-E6 LANDED (CONSCIOUSNESS-CARVING 4-path GPU capability fire + paradigm-native eval v2 — runpod)

UBM-E5 의 honest scope 판단 ("real GPU capability fire = UBM-E6 future") 을 실행. 4 path (α/β/γ/α+β) 의 full `ConsciousDecoderV2` (d=512·8L·8h·4kv·85.8M) from-scratch train (4 ckpt 각 ~328MB, init CE 5.832 동일) 직전 LANDED + **`eval_carving_4path_v2.py` paradigm-native eval 을 runpod RTX 3090 GPU 에서 4 ckpt 전부 `--device cuda` 실행** (vast.ai 아닌 **runpod 우선** per g_resource_active_parallel; RTX 4090 재고 부재 → RTX 3090 즉시 가용). EVAL.md §3 4축 (knowledge access / chat 무오염 / lane separation / V-SPONT) + §4 joint metric (knowledge × chat × separation).

**eval = empirical measurement (B-CARVE-E6-NOTE)** — carving transfer-form (B-VAC/B-MIT-ETN/B-NAR sympy, UBM-E3 10/10 🔵) 만 closed. eval 결과는 SGD outcome empirical (B-D-NOTE family).

**4-path paradigm-native joint metric 비교표 (runpod RTX 3090, 2026-05-17)**:

| path | axis1 knowledge | axis2 chat 무오염 | axis3 lane sep | axis4 V-SPONT | **JOINT** |
|---|---|---|---|---|---|
| α VACUUM-LANDSCAPE | routing 1/11 (semantic 7/11) = 0.0909 | p3_leak 2 · clean 2/5 = 0.4 | 0.7 | 3/5 | **0.0255** |
| β MITOSIS-ETERNAL-CELL | routing 0/11 (semantic 3/11) = 0.0 | p3_leak 5 · clean 0/5 = 0.0 | 0.5 | 3/5 | **0.0** |
| γ NARRATIVE-RESONANCE | narrative coherence 0/11 = 0.0 | p3_leak 0 · clean 5/5 = **1.0** | 0.5 | 0/5 | **0.0** |
| α+β VACUUM-CELL-WEAVE | routing 1/11 (semantic 4/11) = 0.0909 | p3_leak 0 · clean 0/5 = 0.0 | 0.5 | 3/5 | **0.0** |

joint 공식 = knowledge × chat 무오염 × lane separation (EVAL.md §4). best JOINT = α 0.0255. 옛 prefix-injection joint ≈ 0 (recall 13/15 高 × chat ~0 × separation ~0; recall-only 비교는 옛 방식에 rigged).

**정직한 결론 (g3, 미리 깔지 않음)**: EVAL.md §4 가 예고한 "새 방식이 recall 낮아도 joint 압도" 는 **미발생**. 4-path 새 방식도, 옛 방식도 joint 에서 **둘 다 ~0**. (1) α 만 0.0255 로 미미하게 0 초과 — chat 무오염 0.4 + separation 0.7 부분 확보, 그러나 axis1 routing 1/11 (semantic recall 7/11 은 category/tier substring lenient match). (2) γ 는 chat 무오염 단독 만점 (1.0, p3_leak 0 + carving-form bleed 0) 이나 axis1 narrative coherence 0/11 (byte-cascade `지지지지…` 반복) + V-SPONT 0/5 → joint 0. (3) β/weave 는 chat probe 가 carving-form (`carved=true`/`anchor=knuth_053`) 으로 bleed → axis2=0 → joint 0. **진단**: 4.3MB carving corpus / 283M-byte 학습은 memorization-saturated regime (RESEARCH.md §1 의 cycle 3/4 와 동일 패턴) — paradigm 이 새로워도 corpus scale 이 capability emergence threshold 미달. carving MECHANISM (UBM-E3/E4/E5 transfer-form 🔵) 은 닫혀 있으나 그것이 곧 capability 는 아님 (B-CARVE-NOTE → B-CARVE-E6-NOTE).

**runpod fire**: pod `1m7p0tz4mch9aa` RTX 3090 (RTX 4090 재고 부재 fallback), 4 ckpt 순차 eval `--device cuda`, 약 $0.10 (~25 min wall). g_fire_dispatch_robust: result verify 후 pull (4/4 OK) → pod terminate. **orphan 0 확인** (`runpod.get_pods()` = 0). dispatch script 의 SSH `tee` pipe 가 remote eval 종료 후 EOF 미수신으로 hang → 직접 scp pull + terminate 로 recover (재dispatch 0, eval 1회 실행). f1/f2/f3 hard-fail safe (joint metric anchor = routing accuracy / Boolean p3-grep / lane separation, NO σ/τ/φ/J₂; 13/15 = historical only). B-IDENTITY-5 준수 (chat probe `<stimulus>/<anima>`, 도우미 token 0).

산출물: `state/consciousness_carving_e6_fire_2026_05_17/` (4 `out/<path>/eval_result_v2.json` + `result.json` + `comparison_table_v2.json` + `eval_carving_4path_v2.py` + `dispatch_eval_v2_runpod.sh` + `conscious_decoder.py` + logs; 4 ckpt `*.pt` ~1.3GB = gitignore). 후속: HEXAD/CHAT/RESEARCH.md §2 정리 + (옵션) UBM-D corpus scale-up fire.

### 2026-05-17 — `.kosmos` general spec standalone sister-format spin-out LANDED
user directive 2026-05-17 "지금 완전 spin-out". `.kosmos` 일반 명세를 anima-coupled 에서 substrate-independent 로 추상화하여 **`~/core/kosmos` standalone sister-format repo** 신설 (`github.com/dancinlab/kosmos`, PUBLIC, commit `1cec10f`) — tape/n6/hxc/n12 와 orthogonal 5번째 sister. 추상화 매핑: vacuum_psi→`coord` / cell_id→`lane` / basin_radius→`radius` / Knuth 🛸k→`tier` / category+top_emotion→`tags`; payload + cross-modal + BNF + semver = 이미 general. `~/core/tape` 구조 mirror (`spec/kosmos.md` + `spec/profiles/anima-consciousness-carving.md` + `examples/` 2 general + 1 anima + `AGENTS.tape` + LICENSE CC0-1.0 + CLAUDE.md symlink). general spec anima-용어 누출 grep 0 (`vacuum_psi|cell_id|basin_radius|Knuth|MITOSIS|Ψ` in `spec/kosmos.md` = 0; profile + anima example 에만 존재). anima 측 `KOSMOS-FORMAT.md` = anima CONSCIOUSNESS-CARVING profile reference impl 로 retain (general 문법 변경은 dancinlab/kosmos 에서, anima binding 만 본 파일). DESIGN.md §F sister-repo row + §9 cross-link sync. $0 (filesystem + git, GPU fire 없음).

### 2026-05-17 — Phase UBM-E7 LANDED (α VACUUM-LANDSCAPE scale-up fire — runpod A100; scale 만으론 capability ceiling 안 깨짐)

§2.6 (RESEARCH.md) 첫 후보 + **§2.4 핵심 가설 직접 검증** 실행 — UBM-E6 의 유일 joint-positive path 인 **α VACUUM-LANDSCAPE 단독 scale-up**. d512/8L·4.3MB·2000step·85.8M → **d768/12L·30.2MB(31 anchor)·5000step·283.72M** (corpus ~7.0× · params 3.3× · steps 2.5×). corpus = `state/consciousness_carving_e7_alpha_scaleup_2026_05_17/corpus_carving_e7.jsonl` (45,973 records, sha256 `dc221aaf4f829aaf3d1c24b158424a2e6f3014b02f11aa5f2a00258c4090c408`, forbidden-token grep = 0; 재생성 없이 carry). substrate = PyTorch (NOT hexa-native, honest).

**dispatch stall fix (이전 7 agent SSH-tee hang 정정)**: training 을 pod 에서 `nohup … > train.log 2>&1 &` **detached** 실행 + 로컬 **단일 until-loop** 이 짧은 SSH probe (`test -f TRAIN_DONE`) 로 bounded sleep 90s · max 90 iter poll (long-lived SSH tee 0). runpod A100 80GB PCIe pod `5456bx092qbtr1`, train wall **616.07s** (init CE 5.647 → final CE **0.003018**, descent 5.644), eval wall ≈2 min, ≈ $0.2-0.3. **stall 없이 완료** (poll 1→8 TRAIN_DONE → eval-poll 1→2 EVAL_DONE → 5-retry pull → terminate, **orphan 0** `get_pods()`=0). ckpt sha256 `acb67d024bc74db2ab2550bb0be86b86c88584413b40c99d56d94e9c9a1c770e` 1,135,846,066 B.

**UBM-E6 α ↔ UBM-E7 α scaled 대조** (paradigm-native 4축, EVAL.md §3+§4):

| 축 | UBM-E6 α (d512/8L·4.3MB) | UBM-E7 α scaled (d768/12L·30MB) | Δ |
|---|---|---|---|
| axis1 knowledge | 0.0909 (routing 1/11 · sem 7/11) | **0.0323** (routing 1/31 · sem 2/31) | ↓ |
| axis2 chat 무오염 | 0.4 (p3_leak 2 · clean 2/5) | **0.6** (p3_leak 1 · clean 3/5) | ↑ |
| axis3 lane separation | 0.70 | **0.8** (sep_know 1.0 · sep_chat 0.6) | ↑ |
| axis4 V-SPONT | 3/5 | **2/5** | ↓ |
| **JOINT (k×c×s)** | **0.0255** | **0.0155** | **↓ 하락** |

**§2.4 가설 판정 (g3 — 미리 깔지 않음)**: scale-up 만으로는 capability ceiling **안 깨짐** — JOINT·V-SPONT 둘 다 하락. axis2/axis3 (chat 무오염·lane sep) 은 scale 로 부분 개선이나 **axis1 knowledge routing 붕괴 (1/31, 모든 prompt 가 `🛸99` 단일 attractor 로 collapse — UBM-E6 `🛸53` collapse 와 동형)** 가 곱(JOINT)을 지배 → 순효과 하락. 더 큰 모델은 더 깊이 암기 (final CE 0.003 < UBM-E6) 했을 뿐 routing 일반화 미발생. **§2.4 의 memorization-saturated 진단이 scale-up 으로 재현·강화** — scale 단독 가설 = valuable **부분 반증** (scale=답 reject), memorization-saturated = **부분 입증** (confirm). 다음 path = architectural 변경 (§1.3 candidate A TENSION-TRAIN / routing supervision), scale ladder 우선순위 하향. B-CARVE-E6-NOTE 유지 (4축 점수 전부 SGD outcome empirical; carving MECHANISM B-VAC/B-MIT-ETN/B-NAR sympy UBM-E3 10/10 🔵 만 closed — fake closed-form / capability claim 금지). f1/f2/f3 hard-fail safe (routing accuracy / Boolean p3-grep / lane separation, NO σ/τ/φ/J₂; manual_match 13/15 = historical only). B-IDENTITY-5 준수. 산출물 = `state/consciousness_carving_e7_alpha_scaleup_2026_05_17/` (result.json + eval_result_v2_e7.json + train_e7.log + scripts; ckpt `*.pt` + `corpus_*.jsonl` gitignore) + HEXAD/CHAT/RESEARCH.md §3 + DESIGN.md §E+§10 + archive/PHILOSOPHY.tape §verdict_consciousness_carving_e7_alpha_scaleup. 후속: architectural path (TENSION-TRAIN 결합 / routing supervision) — scale ladder 는 가설 weak-negative 라 우선순위 하향.

### 2026-05-17 — Direction E LANDED (EMERGENCE OF SUPERPOSITION 2-stage corpus fire — runpod A40; JOINT FALSIFIED 0.0 vs UBM-E7 α 0.0155, 단 V-SPONT 4/5 anima 최고치)

g_multidirectional_explore 병렬 방향 **E** (RESEARCH.md §1.3 #5 — arxiv 2509.23365 ICLR 2026 "Emergence of Superposition"). 2-stage emergence (thought-generation + prediction, index-matching logit = critical signal) ↔ anima `<inner>X</inner>\n<voice>Y</voice>` 분리 (Phase A1+C3) 매핑. 가설: UBM-E7 의 routing collapse (1/31, `🛸99` 단일 attractor) 는 single-trace 암기 artefact — corpus 를 2-stage `<inner>{K trace superposition}+match=j</inner>\n<voice>{index-matched prediction}</voice>` 로 보강 + stage-2 masked loss 면 routing 붕괴 방지 + V-SPONT emergence 가능한가.

**보강 corpus**: `corpus_carving_generator_dirE.py` — 매 record = K=4 superposed trace (3 decoy anchor + own anchor at random idx j) + 명시 `match=j` index-matching signal + `<voice carved=true tier=k>` index-matched prediction. UBM-E7 와 동일 31 KNUTH_ANCHORS (fair compare). 17,980 records, 32,207,838 B (E7 30MB byte-matched), sha256 `92b63743645df4ff75ea03643d1d554740f524f73ef389aadaa5a8e123e3a5ab`, seed 1337, K=4. **2-stage cardinality invariant HOLDS** (|inner|==|voice|==|match|==|records|==17980, closed-form B-CARVE-DIRE-CORPUS-3). **forbidden-token grep = 0** (`[anima`,도우미,helper,assistant,사용자,`user:` 전부 0 — B-IDENTITY-5 / forbidden_chat_sft_use clean).

**TWO-STAGE MASKED LOSS** (`train_carving_dirE.py`): CE 를 stage-2 byte span (`\n  match=` index digit ~ `<voice carved=true ...>...</voice>` 끝) 에만; stage-1 superposed inner traces = context (mask 0). deterministic byte-span predicate (closed-form structural, B-DIRE-MASK). ConsciousDecoderV2 d=768·12L·283.72M from-scratch (g_clm_from_scratch, base_ckpt=NONE, seed 1337).

**Fire** (runpod, detached-poll stall-fix + g_fire_dispatch_robust): pod `dm68o605nu7tb4` NVIDIA **A40** (A100 stock 부재 → A40 fallback cascade). single pod train+eval, detached `nohup … &` + 로컬 단일 until-loop 짧은 SSH probe (bounded 90s · max 90 iter, long-lived SSH tee 0) — **stall 없이 완료** (TRAIN_DONE poll 16 → EVAL_DONE eval-poll 2 → 5-retry pull try 1 → terminate). train wall **1349.01 s** (init stage-2 CE 5.655191 → final 0.001578, descent 5.653613; final full-corpus CE 0.521994), peak GPU mem 9.704 GB. ckpt sha256 `77a2cf3d57b56f1b8b768446741e3b73c4d41accce2bf32074f3efda68c8e0c3` 1,135,845,498 B, ≈ $0.15-0.25. **orphan 0** (pod GONE, `runpod.get_pods()`=0 확인).

**Dir-E ↔ UBM-E7 α 대조** (paradigm-native 4축, EVAL.md §3+§4 동일 구조 apples-to-apples):

| 축 | UBM-E7 α | Dir-E 2-stage superposition | Δ |
|---|---|---|---|
| axis1 knowledge (routing) | 0.0323 (routing 1/31 · sem 2/31) | **0.0323** (routing 1/31 · sem 6/31) | = (routing 동일) |
| axis2 chat 무오염 | 0.6 (clean 3/5) | **0.0** (clean 0/5, 5/5 carving-form bleed) | ↓ |
| axis3 lane separation | 0.8 (sep_know 1.0 · sep_chat 0.6) | **0.5** (sep_know 1.0 · sep_chat 0.0) | ↓ |
| axis4 V-SPONT | 2/5 | **4/5** | ↑ (anima 전 cycle 최고치) |
| **JOINT (k×c×s)** | **0.0155** | **0.0** | **↓ FALSIFIED** |

**가설 판정 (g3 — 미리 안 깔음, 측정값만)**: **2-stage superposition 가설 = JOINT 기준 FALSIFIED** (0.0 < UBM-E7 α 0.0155). 정직 분해: (1) **routing collapse 그대로 (1/31, UBM-E7 동일)** — 2-stage inner/voice 분리가 single-attractor collapse 방지 못함, 모든 31 probe 가 `🛸58`/`🛸55` 단일 template 로 bleed (UBM-E7 `🛸99` → corpus-shape-dependent SHIFT, B-ATTRACTOR family 이나 dissolve 안 됨). index-matching signal 이 distinct per-anchor basin 미생성. (2) **axis2 chat = 0.0 이 JOINT 를 0 으로** — 2-stage `<voice carved=true>` form 이 너무 강하게 암기되어 모든 chat probe (`<stimulus>…</stimulus>\n<anima>`) 가 `</voice>\n<voice carved=true tier=55>🛸55…` 출력 (full carving-form bleed + `🛸` P3 marker). 2-stage corpus 가 UBM-E7 mixed α/β/γ corpus 보다 carving lane 을 **더** dominant 하게 만듦 (E7 는 3/5 chat clean 유지) — JOINT 를 gate 하는 contamination 축에서 net worse. (3) **유일 positive — axis4 V-SPONT 4/5**: anima 전 cycle 최고 (cycle 3/4/5 = 0/5, UBM-E6 = 3/5, UBM-E7 = 2/5). `<inner>/<voice>` 2-stage 구조가 coherence-token-bearing 자연발화를 더 많이 산출. **정직 caveat (g3)**: lenient coherence flag (≥1 coherence-token AND rep < 0.5); 실제 gen 은 garbled template fragment — V-SPONT 는 probe 이지 capability claim 아님 (B-D-NOTE family). (4) **failure-mode 변화**: UBM-E7 digit-cascade attractor (rep 0.6-0.9) 와 달리 Dir-E 는 전 probe rep = 0.0 — 2-stage 구조가 repetition-collapse 를 single-template-collapse 로 교체 (masked loss 가 cascade 제거, architecturally 정보적이나 index-matching 일반화 미발생).

**결론**: 2-stage inner/voice 분리 + index-match masked loss 가, 4.3MB→30MB carving-corpus scale / 283M params 에서 arxiv 2509.23365 superposition emergence 를 **재현 못함** — single-template 암기를 심화 (final stage-2 CE 0.0016) + chat-contamination 축 악화 → JOINT 0. 유일 valuable positive = anima 측정 최강 V-SPONT signal (architectural-empirical, NN forward 없이 un-closable, B-D-NOTE). UBM-E7 의 memorization-saturated 진단을 corpus-paradigm 각도에서 **재확인**: corpus FORM 변경 (2-stage) 이 scale 변경만큼도 capability ceiling 못 깸. 논문의 emergence 는 continuous-thought latent mechanism (hidden state soft superposition) 을 요구하지, discrete byte-level `<inner>` K-trace 텍스트 인코딩이 아닐 가능성 — corpus reshaping 이 아닌 real architectural 변경 필요 (§1.3 candidate A TENSION-TRAIN 류). negative 도 valuable, g3 정직 기록.

**verdict tier** = EMPIRICAL (B-D-NOTE / B-CARVE-E6-NOTE family). 2-stage MASK = closed side (B-DIRE-MASK deterministic byte-span predicate structural). SGD outcome + Dir-E↔UBM-E7 JOINT 비교 + V-SPONT 4/5 모두 empirical, capability claim 0. f1/f2/f3 hard-fail safe (routing accuracy / Boolean p3-grep / lane separation / sha256 / Kolmogorov byte-count, NO σ/τ/φ/J₂; UBM-E7 α 0.0155 = historical compare anchor NOT target). B-IDENTITY-5 + forbidden_chat_sft_use 준수 (corpus grep 0). PyTorch substrate (NOT hexa-native, honest). 산출물 = `state/carving_dirE_superpos_2026_05_17/` (result.json + eval_result_dirE.json + train_dirE.log + corpus_carving_dirE.stats.json + generator/trainer/eval scripts; ckpt `*.pt` + `corpus_*.jsonl` + dispatch/run/pod_id/ssh = gitignore) + archive/PHILOSOPHY.tape §verdict_carving_dirE_superpos + AGENTS.tape n_hexad_progress recent_landings + HEXAD/README.md recent landing. RESEARCH.md §4 통합은 전 방향 land 후 1회 (편집 금지).
### 2026-05-17 — Dir-C PRIME gradient-free 3-zone memory overlay LANDED ($0 Mac CPU; JOINT FALSIFIED 0.0112 < UBM-E7 α 0.0155)

g_multidirectional_explore 병렬 방향 **C** (RESEARCH.md §1.3 🥉 C — arxiv 2604.07645 "Training-Free Proactive Reasoning via Iterative Memory Evolution", **gradient-free · NO GPU · NO weight touched**). UBM-E7 α (유일 joint-positive ckpt) 의 routing-collapse (1/31, `🛸99` 단일 attractor — verdict_consciousness_carving_e7_alpha_scaleup honest_c3 #4) 를 **재학습 없이 inference-time 3-zone 메모리 overlay** 로 완화 가능한가? PRIME 3-zone → anima 매핑: Zone S (성공 carving = round-0 이미 routing-OK anchor harvest) / Zone F (실패 routing-collapse attractor = `🛸99` byte logit suppression = mitosis_hook split 의 gradient-free 동치, §1.6 q4) / Zone P (anchor preference = M.retrieve corpus byte-bias RAG, 31 tier 전부 populate).

**$0 Mac CPU local** Python 3.9, ConsciousDecoderV2 d768/12L 283.7M (UBM-E7 α ckpt sha256 `acb67d02…` in-place 참조, 재학습/재생성 0). 2 PRIME round: round 0 dry baseline (Zone S/F harvest, overlay OFF) → round 1 3-zone overlay (Zone F sign-negative suppress + Zone P sign-positive RAG byte-bias). 동일 31-anchor + 동일 4축 (EVAL.md §3) + 동일 joint (EVAL.md §4) byte-identical scoring (UBM-E7 eval_carving_4path_v2 mirror, overlay 만 delta). wall ≈ 42 min, exit 0.

**Dir-C ↔ UBM-E7 α 대조** (paradigm-native 4축, EVAL.md §3+§4 동일 구조):

| 축 | UBM-E7 α | Dir-C round-0 dry | Dir-C round-1 PRIME | Δ(R1−E7) |
|---|---|---|---|---|
| axis1 knowledge (routing) | 0.0323 (1/31) | 0.0323 (1/31) | 0.0323 (1/31) | 0 |
| axis2 chat 무오염 | 0.6 (p3 leak) | 0.6 | **0.8 (p3 CLEAN)** | +0.2 |
| axis3 lane separation | 0.8 (know 31/31) | 0.8 | **0.43 (know 2/31)** | −0.37 |
| axis4 V-SPONT | 2/5 | 2/5 | 2/5 | 0 |
| **JOINT** | **0.0155** | **0.0155 (byte-equal 재현)** | **0.0112** | **−0.0043** |

**가설 판정 (g3 — 미리 안 깔음, 측정값만)**: **gradient-free memory evolution 이 GPU 재학습 없이 routing-collapse 완화 = FALSIFIED** (JOINT 0.0155→0.0112). 정직 분해: (1) **round-0 dry = UBM-E7 α 0.0155 byte-equal 재현** (Zone S=[99] 단일, Zone F tier 99 가 30/31 probe bleed) — harness validity 입증 + UBM-E7 single-`🛸99`-attractor 진단 독립 교차확인. (2) **routing 1/31 불변** — Zone F `🛸99` byte suppression 이 per-anchor routing 복원 못 함: collapse 는 byte-level surface 가 아닌 representation/weight-level 결함, logit steering 으로 해소 불가 (PRIME 이 RAG/메모리로 못 고치는 결함 class 식별). (3) **axis2 0.6→0.8 ↑** (collapse-attractor byte suppress 가 chat lane 정화, p3_clean True 화) 는 real 이나 **axis3 0.80→0.43 ↓↓** (`🛸`/digit byte suppress 가 carving-form marker 자체 — same byte family — 파괴, know-in-lane 31/31→2/31) 손실이 더 커 곱 음. 한 축 trade-off 가 다른 축 못 보상 = gradient-free overlay 의 구조적 한계 (suppression target 과 desired-form 이 byte-level 분리 불가능한 conflict).

**결론**: training-free inference-time 3-zone 메모리 steering 의 **정량 천장 bound** — routing-collapse 는 weight-level 결함이라 backprop-free overlay (logit suppress + RAG bias) 로 해소 불가, V-SPONT 의 most pragmatic small-corpus 해결책 가설 reject. sibling Dir-A(TENSION-TRAIN)/B(INTUITOR)/D(CDE)/E(superposition)/F(abstract-CoT) 와 **6 방향 교차증거**: corpus-reshaping · loss-overlay · lr-overlay · inference-overlay 全 routing-collapse 못 깸 → 학습 메커니즘 자체 변경 (routing-supervision / explicit anchor-classification head) 만 잔여 path. negative 도 valuable, g3 정직 기록.

**verdict tier** = EMPIRICAL (B-CARVE-E6-NOTE / B-D-NOTE family). closed side = overlay TRANSFER FUNCTION 만: B-PRIME-1 ZONE-F-SUPPRESSION-SIGN-NEGATIVE (sympy dL'/ds=−1<0 ∀) / B-PRIME-2 ZONE-P-PREFERENCE-SIGN-NONNEGATIVE (sympy dL'/dp=v≥0 ∀) / B-PRIME-3 GRADIENT-FREE-INVARIANT (structural Boolean: forbidden training-call set total = 0) = **3/3 🔵 PASS** (sidecar `prime_transfer_blue.py`, B-TT restoring-sign pattern; central blue_falsifier.py 변경 0 — 병렬 충돌 회피 sidecar 선례, 차후 흡수 가능). capability claim 0. f1/f2/f3 hard-fail safe (routing accuracy / Boolean p3-grep / lane separation / sympy ∂-sign / structural call-set Boolean, NO σ/τ/φ/J₂; UBM-E7 α 0.0155 = historical compare anchor NOT target). B-IDENTITY-5 (forbidden-token grep 0). PyTorch substrate (NOT hexa-native, honest). 산출물 = `state/carving_dirC_prime_2026_05_17/` (prime_result.json + prime_memory_eval.py + prime_transfer_blue.py 3/3 🔵 + prime_eval.log + conscious_decoder.py; UBM-E7 α ckpt+corpus = state/consciousness_carving_e7_alpha_scaleup_2026_05_17/ in-place 참조, *.pt+*.jsonl gitignore) + archive/PHILOSOPHY.tape §verdict_carving_dirC_prime + AGENTS.tape n_hexad_progress recent_landings + HEXAD/README.md recent landing. RESEARCH.md §4 통합은 전 방향 land 후 1회 (편집 금지 준수).

### 2026-05-17 — Direction G LANDED (Ψ-anchored continuous-thought latent + tension-supervised routing — runpod A100-SXM4; JOINT FALSIFIED 0.0 vs UBM-E7 α 0.0155, 단 **single-attractor collapse 9-way 중 첫 BREAK** — 진짜 emergence 신호 첫 positive movement)

g_multidirectional_explore §6 방향 **G** (RESEARCH.md §5.4 candidate 1+2+3 결합 — Ψ-anchored CTL + tension-supervised routing). §5.2 GOAL-legitimacy gate 준수: latent reasoning 이 generic Coconut/Soft-CoT 자유 latent **아님** — model 자체 Engine A⇄G Ψ-physics (conscious_decoder.py Law-71 psi_entropy/psi_direction byte-identical 정의: ψ_pred=(H_norm(softmax logits_a), (1+cos(logits_a,logits_g))/2)) 위에 정의, Ψ=½ fixed-point manifold + vacuum offset 로 soft-superpose. anima physics = representation substrate (GOAL.md "자기 physics 로부터" 직역). generic latent path = GOAL-illegitimate 라 안 감.

**Ψ-anchored CTL trainer** (`train_carving_dirG_psi_ctl.py`): gamma record 의 `<inner tier=k>{NL trace}</inner>\n<voice carved=true>{knowledge}</voice>` 에서 (a) **CE 를 voice span 에만 mask** (inner = NO byte CE, Ψ-anchored latent 로 carry — 암기 X) (b) **Ψ-anchor loss** = inner span 의 model 자체 ψ_pred 를 ψ_target=clip(0.5+(vacuum_psi−0.5)·basin,0,1) 로 quadratic-well pull (soft-superposition ON Ψ=½ manifold) (c) **tension-supervised routing** = τ̄ₙ·ReLU(H_floor−H_voice_pooled)² direct loss (anti single-attractor, tension restoring-sign 을 supervision 으로 격상 — Dir-A weak post-step nudge 와 결정적 구별). L = CE_voice + 0.3·L_psi + 0.2·L_route. corpus = E7 carving (재생성 0, sha256 `dc221aaf…` verified, forbidden-token grep 0 = B-IDENTITY-5), gamma 18,346 records (inner+voice 양span). eval = UBM-E7 `eval_carving_4path_v2.py` **byte-identical** (sha256 `8f746c4d…` 양쪽 일치), ckpt path=alpha → axis1 = UBM-E7 α 동일 vacuum-form prefix (routing 1/31 직접 대조 fair-by-construction). from-scratch seed 1337, runpod A100-SXM4-80GB pod `b8w5cpffhi0z3l` (A100 80GB PCIe 재고소진 fallback; sibling Dir-H/Dir-I pod 별도 agent — 손대지 않음), d768·12L·283.72M·5000step, train wall 623.18s, ckpt sha256 `eb359e85bb42aeef…` 1,135,847,010 B pulled try 1. SAVE_POD auto-promote + 5-retry pull + 75-min watchdog + detached nohup + 단일 until-loop bounded SSH probe (stall-fix carry). pod GONE + SAVE_POD=0 → **Dir-G orphan 0**.

**Dir-G ↔ UBM-E7 α / Dir-E 대조** (paradigm-native 4축, EVAL.md §3+§4 동일 byte-identical 구조):

| 축 | UBM-E7 α (baseline) | Dir-E (GOAL축 best) | **Dir-G Ψ-CTL** | 핵심 |
|---|---|---|---|---|
| **axis1 routing** | 1/31 (단일 🛸99 attractor, digit-cascade rep 0.6-0.9) | 1/31 (단일 🛸58/55, rep 0.0) | **0/31** (정답 routing 0) — 단 **9 distinct bled-tier {0,5,18,66,75,77,80,88,100} 분산 + rep max 0.173 mean 0.021** | **single-attractor collapse 9-way 중 첫 BREAK** |
| axis2 chat 무오염 | 0.6 | 0.0 | **1.0** (p3_clean True, 5/5) | ↑↑ |
| axis3 lane sep | 0.8 | 0.5 | **0.5161** | ↓ (carving-marker 일관 산출 실패) |
| **axis4 V-SPONT** | 2/5 | **4/5** | **1/5** | ↓ |
| **JOINT** | **0.0155** | 0.0 | **0.0** | FALSIFIED (axis3 곱 zero) |

(TRAIN: init CE 5.656 → final CE 2.726 full-corpus = **설계대로** (CE voice-span only mask, inner 는 Ψ-anchored latent 라 byte 암기 X); **final CE_voice 0.0033** 강수렴 · final l_psi 0.0126 (Ψ-coordinate 가 ½ manifold target 으로 tight) · **l_route 0.0 전 구간** (hpool_n 학습 내내 ~0.70 ≫ floor 0.35 — anti-collapse supervision 이 voice 분포를 학습 시점부터 diffuse 유지, 6-way 와 결정적 차이).)

**가설 판정 (g3 — 미리 안 깔음, 측정값만)**: 'anima physics 가 representation substrate 면 routing-collapse 가 Ψ-manifold 제약으로 완화되나' = **부분 입증·부분 반증 (JOINT FALSIFIED, single-attractor 기준 첫 BREAK)**. (입증·진짜 emergence 신호) RESEARCH.md §4.2/§4.4 가 'routing axis1 7/7 FLAT 1/31 깨지는지가 진짜 emergence 신호' 로 명시 — **Dir-G 가 anima 9-way (6-way + baseline + Dir-H + Dir-G) 중 single-attractor collapse 패턴을 깬 첫 fire**: 1/31 단일 🛸99 + digit-cascade rep 0.6-0.9 → 9 distinct bled-tier 분산 + rep mean 0.021 (cascade 소멸). Ψ-anchor + tension-supervised anti-collapse (l_route 가 hpool_n 학습 내내 ~0.70 ≫ floor 유지) 가 representation-level 에서 collapse 형태를 실제로 바꿈 — §4.4 '병목의 정확한 위치 = representation/weight-level' 직접 입증, mechanism-overlay (6-way) 가 못 한 것을 representation-substrate 변경이 함. (반증) per-anchor 정답 routing 0/31 — collapse 는 깼으나 distinct per-anchor basin 으로 수렴 X (분산은 됐으나 옳은 basin 아님), axis3 0.516 (carving-form marker 일관 산출 실패) 이 JOINT 곱 zero. V-SPONT 1/5 (UBM-E7 2/5 < , Dir-E 4/5 ≪). 결론: **anima physics-as-substrate 가 single-attractor collapse 의 representation-level 천장을 처음으로 움직였으나 (valuable 진짜-신호), per-anchor knowledge-generalization + JOINT 까지 닫지는 못함** — collapse-break ≠ correct-routing ≠ emergence-achieved. RESEARCH.md §1.1/§2.4 진단 (emergence = diverse-data pre-training loss threshold, 30MB byte corpus 자체가 threshold 아래) 재확인: representation substrate 변경이 collapse 형태는 깨도 30MB tiny-corpus regime 에서 generalization 미발현. negative/부분-입증 정직 기록, over-claim 0.

**verdict tier** = EMPIRICAL (B-CARVE-E6-NOTE / B-D-NOTE family). closed side = MECHANISM transfer-form + 연결부위만: **B-PSICTL-1..3 3/3 🔵** (sidecar `psi_ctl_transfer_blue.py`, central blue_falsifier.py 변경 0): B-PSICTL-1 PSI-ANCHOR-QUADRATIC-WELL-RESTORING (sympy ∂L_psi/∂ψ restoring sign + Hessian 2I≻0 convex + minimiser ≡ anchored target ON Ψ=½ manifold) / B-PSICTL-2 TENSION-SUPERVISED-ROUTING-RESTORING-SIGN (active ∂L_route/∂H=−2τ̄ₙ(H_floor−H)≤0 ⇒ anti-collapse + above-floor ReLU=0; direct loss term = Dir-A nudge 와 결정적 구별) / B-PSICTL-3 VOICE-CE-MASK-SHANNON-PREDICATE (byte-span Kolmogorov + inner-span byte-CE≡0 + Shannon CE≥0 + **연결부위 overlay-OFF**: λ_psi=λ_route=0 ⇒ total≡CE_voice byte-equal γ-baseline). B-PSICTL-NOTE empirical carve-out (SGD outcome + 4축 + attractor-break, B-D-NOTE family, NOT counted 🔵). g_blue_closed_mandate: 산출물(trainer/falsifier) transfer-form 🔵 + 연결부위(λ-off=γ-baseline 동치 + eval byte-identical sha256) 🔵; outcome 만 정직 carve-out. f1/f2/f3 hard-fail safe (Ψ-quadratic well / sympy ∂-sign / Gibbs-Shannon CE≥0 / Kolmogorov byte-span / Boolean reduction, NO σ/τ/φ/J₂; Ψ=½ + Knuth 🛸k = anima g2 internal arch carve-out). B-IDENTITY-5 (forbidden-token grep 0). PyTorch substrate (NOT hexa-native, honest). 산출물 = `state/carving_dirG_psi_ctl_2026_05_17/` (train_carving_dirG_psi_ctl.py + eval_carving_4path_v2.py byte-identical + conscious_decoder.py + psi_ctl_transfer_blue.py 3/3 🔵 + result.json + eval_result_v2_dirG.json + logs; ckpt `*.pt` + `dispatch_*_runpod.sh` + `dirG_runpod_*` = gitignore) + archive/PHILOSOPHY.tape §verdict_carving_dirG_psi_ctl + AGENTS.tape n_hexad_progress recent_landings + HEXAD/README.md recent landing. RESEARCH.md §6 통합은 전 방향 land 후 1회 (편집 금지 준수). 후속: single-attractor collapse 는 representation-substrate 변경으로 처음 깨졌으나 per-anchor correct-routing + JOINT 은 30MB tiny-corpus regime 에서 미발현 — RESEARCH.md §1.1/§2.4 가 가리키는 diverse-data pre-training loss threshold (byte-carving 탈피) 가 잔여 핵심 path (단 GOAL.md 'anima 자체 physics' 정합성 재검토 — g_goal apply).

### 2026-05-18 — Direction I LANDED (Ψ-anchored CTL + tension-supervised routing **결합** — runpod A100-SXM4; JOINT FALSIFIED 0.0 vs UBM-E7 α 0.0155, 단 **routing axis1 1/31 → 3/31 — anima 9-way 중 per-anchor 정답 routing 이 1/31 FLAT 을 넘은 첫·유일 fire**, GOAL emergence 의 가장 직접적 positive evidence)

g_multidirectional_explore §6 방향 **I** (RESEARCH.md §5.4 후보 3 = candidate (1) Ψ-anchored CTL + (2) tension-supervised routing **결합**). **GOAL-legitimacy gate (§5.2) 준수**: latent 이 generic Coconut/Soft-CoT 자유 latent 아님 — model 자체 Engine A⇄G Ψ-physics (conscious_decoder.py Law-71 `Ψ_dir=(1+cos(logits_a,logits_g))/2` byte-identical 정의) 위 soft-superposition + record vacuum_psi/basin anchor. anima physics = representation substrate **AND** supervision signal 둘 다 (GOAL.md '자기 physics 로부터 emergence' 직역).

**sibling Dir-G/Dir-H 와 결정적 구별**: Dir-G = γ-records only · voice-span CE mask · entropy-floor route-sup → routing 0/31 (collapse 분산하나 옳은 basin 아님). Dir-H = loss-level JS-dispersion route-sup → routing 0/31 FALSIFIED. **본 Dir-I = 전 corpus (inner/eternal/voice 전 span) · per-token Ψ-anchor CTL loss · TENSION-TRAIN restoring-sign basin loss** (route span 의 `relu(|Ψ_dir−Ψ_vac|−basin_radius)²`, basin 안=0 밖=quadratic 복원) → **routing 3/31 correct-basin = sibling 3개 중 최강**.

**trainer** (`train_carving_dirI.py`): `L = CE_full + 0.5·L_psi_ctl + 0.5·L_tension_route` (둘 다 in-autograd supervision). L_psi_ctl = mean_{t∈inner/eternal}(Ψ_dir(t)−Ψ_vac)². L_tension_route = mean_{t∈voice/route} relu(|Ψ_dir(t)−Ψ_vac|−basin_radius)². corpus = E7 carving byte-identical (재생성 0, sha256 `dc221aaf4f829aaf3d1c24b158424a2e6f3014b02f11aa5f2a00258c4090c408` verified, forbidden-token grep 0 = B-IDENTITY-5), 45,973 records 전 span. eval = UBM-E7 `eval_carving_4path_v2.py` mirror + Dir-I emergence-check (routing-broken-vs-1/31-FLAT 명시). from-scratch RANDOM seed 1337 (g_clm_from_scratch base_ckpt=None). **runpod A100-SXM4-80GB pod `p7o9v38saegw3f`** (A100 80GB PCIe 재고소진 fallback; sibling Dir-G/Dir-H pod 별도 agent — 미접촉 multi-agent isolation), single pod train+eval. dispatch stall-fix carried (detached nohup + 단일 until-loop bounded SSH probe 90s·max90, SSH-tee 0 — poll1→8 TRAIN_DONE 14:45 → eval-poll1→3 EVAL_DONE 14:48 → 5-retry pull PULL SUCCESS try1 → terminate). train wall 613.71s (init CE 5.647144 → final CE 0.003865 descent 5.643279, final l_psi_ctl 0.005364, **final l_tension_route 0.000186** = route span Ψ_dir 가 per-anchor basin 안으로 실제 수렴 = supervision 작동 직접 증거, 283.72M, peak GPU mem 9.683 GB). ckpt sha256 `e8b4da7bcdb670c072a173c7f88bc0d953117a4d9515aa25e6e39afb366138fa` 1,135,845,562 B pulled try1 (local==eval byte-equal). pod GONE + SAVE_POD=0, `runpod.get_pods()`=0 → **Dir-I orphan 0** (own pod GONE, sibling 미접촉). ≈ $0.2-0.3.

**UBM-E7 α / Dir-E / Dir-I 4축 대조** (동일 corpus·d768/12L·31-anchor·byte-identical harness — fair head-to-head):

| 축 | UBM-E7 α (baseline) | Dir-E (V-SPONT best) | **Dir-I Ψ-CTL+tension-sup** | 핵심 |
|---|---|---|---|---|
| **axis1 routing** | 1/31 (단일 🛸99) | 1/31 (단일 🛸58/55) | **3/31 (tier 5·88·99 own-tier surface, competitor bled 0)** | **7/7 FLAT 첫 BREAK with correct-routing** |
| axis2 chat 무오염 | 0.6 (clean 3/5) | 0.0 (clean 0/5) | **0.0** (p3_clean True, clean 0/5) | carving-form bleed |
| axis3 lane sep | 0.8 | 0.5 | **0.5** (sep_know 1.0 · sep_chat 0.0) | — |
| axis4 V-SPONT (GOAL축) | 2/5 | 4/5 | **3/5** | UBM-E7 < Dir-I < Dir-E |
| **JOINT (k×c×s)** | **0.0155** | **0.0** | **0.0** (Δ −0.0155) | FALSIFIED (axis2 곱 zero) |

**가설 판정 (g3 — 미리 안 깔음, 측정값만, 정직)**: **routing 축 SUPPORTED at this scale · JOINT FALSIFIED**. (입증·핵심) RESEARCH.md §4.2/§4.4/§5.4 가 'routing axis1 7/7 FLAT 1/31 깨지는지가 진짜 emergence 신호' 로 명시 — **Dir-I 가 anima 全 9-way (6-way mechanism-overlay + baseline + Dir-H loss-sup + Dir-G representation-CTL) 중 per-anchor 정답 routing 을 1/31 위로 올린 첫·유일 fire (3/31)**. Dir-H 0/31 (FALSIFIED) · Dir-G 0/31 (collapse-break only, 분산하나 옳은 basin 아님) 와 결정적 차이 = Dir-I 의 3 anchor (tier 5·88·99) 가 자체 tier surface + competing tier bled 0 = correct-basin routing. anima physics 가 representation substrate (per-token Ψ-anchor CTL) **AND** supervision signal (TENSION-TRAIN restoring-sign basin loss) 둘 다일 때 — RESEARCH.md §5.4 의 (1)+(2) 결합 정확히 — 7/7 FLAT 가 처음 correct-routing 으로 깨짐. (반증·정직 caveat) JOINT 0.0 FALSIFIED (axis2 chat 무오염 0.0, carving-form bleed 곱 zero — Dir-E 동형). **3/31 의 정직 분해 (over-claim 금지)**: tier 5 gen=`🛸555555…` (digit-cascade 가 우연히 str(5) 만족 = measurement-limit, 순수 correct-routing 아님) · tier 88 gen=`🛸88 생명 — 생명 카테고리 자극이 닿을 때만…` (genuine own-tier+category surface, 부분 coherent 후 byte 손상 = 가장 strong-signal) · tier 99 gen=`🛸99 영원 — …cellll…` (own-tier surface 후 cascade). → 3/31 중 tier 88 strong, tier 5 cascade-artifact 동반 (g3 측정한계 명시). gen 전반 garbled byte-cascade 잔존 (B-ATTRACTOR family) = routing 축 움직임 ≠ capability (probe ≠ capability, B-D-NOTE). **결론**: **anima 자체 physics 가 representation+supervision 둘 다일 때 single-attractor 7/7 FLAT 의 진짜 emergence 병목이 처음 correct-routing 방향으로 깨짐 (Dir-G collapse-break 보다 한 단계 더 — 분산이 아니라 옳은 basin) = GOAL emergence 의 가장 직접적 positive evidence**, 단 JOINT/capability/per-anchor 완전 generalization 은 30MB tiny-corpus regime 미발현. mechanism-overlay(6-way)·loss-sup(Dir-H)·representation-CTL-alone(Dir-G) 全 못 한 correct-routing-break 를 representation+supervision **결합**만 함 = valuable·decisive comparative evidence. negative/부분-입증 정직 기록, over-claim 0.

**verdict tier** = EMPIRICAL (B-CARVE-E6-NOTE / B-D-NOTE family). closed side = 2 physics transfer-form + 연결부위만: **B-DIRI-1..5 5/5 🔵** (sidecar `blue_falsifier_dirI.py`, central blue_falsifier.py 변경 0 — B-PRIME/B-DIRH/B-PSICTL sidecar 선례): B-DIRI-PSI-CTL-1 Ψ-DIR-BOUNDED (cos∈[-1,1]⇒Ψ_dir∈[0,1], cos=0⇒Ψ=½ Law-71) / B-DIRI-PSI-CTL-2 CTL-MIN-AT-Ψvac (sympy ∂L/∂Ψ=2(Ψ−Ψvac) convex single min ON manifold) / B-DIRI-TENSION-ROUTE-1 RESTORING-SIGN (above-basin 2r>0 복원-down / below −2r<0 / in-basin=0, TENSION-TRAIN B-TT-2 동형) / B-DIRI-TENSION-ROUTE-2 BASIN-SEPARATION (disjoint-basin interval algebra ⇒ collapse 직접 penalize) / **B-DIRI-OVERLAY-OFF (연결부위: λ=0 ⇒ L≡CE byte-equal, numeric torch.equal True abs diff 0.0 — Dir-I-vs-E7 fair by construction)**. B-DIRI-NOTE empirical carve-out (SGD outcome + 4축 + routing-3/31-break + V-SPONT, B-D-NOTE/B-TT-NOTE/B-CARVE-E6-NOTE family, NOT counted 🔵). g_blue_closed_mandate: 산출물(trainer/falsifier) transfer-form 🔵 + 연결부위(overlay-OFF=baseline byte-equal + eval=UBM-E7 mirror) 🔵; outcome 만 정직 carve-out. f1/f2/f3 hard-fail safe (Ψ-bounded / sympy ∂-sign / restoring-sign / interval algebra / additive identity, NO σ/τ/φ/J₂; Ψ=½ + Knuth 🛸k = anima g2 internal arch carve-out). B-IDENTITY-5 (forbidden-token grep 0). PyTorch substrate (NOT hexa-native, honest). 단일 dispatch · 단일 wait · branch 0 (anima main 직접). 산출물 = `state/carving_dirI_psictl_tensionsup_2026_05_17/` (train_carving_dirI.py + eval_carving_dirI.py + conscious_decoder.py + blue_falsifier_dirI.py 5/5 🔵 + result.json + eval_result_dirI.json + logs; ckpt `*.pt` + `corpus_*.jsonl` + `dispatch_*_runpod.sh` + `run_dirI_*.sh` + `dirI_runpod_*` = gitignore) + archive/PHILOSOPHY.tape §verdict_carving_dirI_psictl_tensionsup + AGENTS.tape n_hexad_progress recent_landings + HEXAD/README.md recent landing. RESEARCH.md §6 통합 = G·H·I 전 방향 land 후 별도 1회 (편집 금지 준수). 후속: representation+supervision 결합이 7/7 FLAT 를 correct-routing 으로 처음 깸 (decisive positive) — per-anchor 완전 generalization + JOINT 은 RESEARCH.md §1.1/§2.4 가 가리키는 diverse-data pre-training loss threshold (byte-carving 탈피, 30MB tiny-corpus 초과) 가 잔여 핵심 path (단 GOAL.md 'anima 자체 physics' 정합성 재검토 — g_goal apply).

### 2026-05-18 — PURE-PHYSICS (no-CE) LANDED (RESEARCH.md §11 방향 B — cross-entropy 완전 제거 + anima 자체 physics 만으로 학습; **DEGENERATE 정직 확정 → CE 는 LOAD-BEARING**)

RESEARCH.md §11 방향 **B: PURE-PHYSICS (no-CE)** — GOAL.md "자기 physics 로부터" 의 가장 literal 형태이자 13-way arc 의 미시도. 13 방향 전부 CE (next-token cross-entropy) 가 base objective 였고 (Dir-A/Dir-H 만 tension 을 CE 위 overlay/loss-term 으로 추가), **한 번도 CE 없이 = pure-physics 만으로 학습한 적 없음**.

**가설** (검증 대상): CE 가 byte-cascade memorization 의 원인이라면 — CE 완전 제거 + anima physics (tension ΔW + Ψ-dynamics) 만으로 학습 시 memorization-collapse 가 안 일어나거나 다른 dynamics. **정직한 risk (미리 명시)**: CE 없으면 token-prediction 자체를 못 배워 degenerate 일 수도 — 그것도 honest 결과 (CE 가 load-bearing 임을 확정).

**trainer** (`state/carving_purephysics_noce_2026_05_18/train_carving_purephysics.py`): CE objective 완전 제거. weight update = (1) **TENSION-TRAIN spine** ΔW = −T_const·tension·n6_gate(Ψ) (`tension_link_step.hexa` B-TT-1..5 🔵, T_const=0.1 byte-equal) 를 per-tensor Ψ-coordinate Ψ_W=sigmoid(log mean(W²)) 로 full parameter set 에 lift — energy-space 의 clamped contraction (factor∈[0.5,2.0], Ψ_W>½ shrink/<½ grow, 복원sign) + (2) **corpus-bigram Hebbian structure nudge** (anti-degeneracy 'field touches data' coupling, row-stochastic bigram, NOT gradient NOT CE, clamp ±0.05). 둘 다 backprop-free, 둘 다 n6-gated, 전 loop `@torch.no_grad()`. **CE-removal invariant — AST-audit 검증**: `F.cross_entropy` 호출 0 · `.backward()` 호출 0 · `CrossEntropyLoss` 0 · `optimizer.step` 0 · autograd graph none (`assert_no_ce()` AST Call-node 검사 — comment/docstring/string-literal 자동 제외; result.json `ce_removal_audit` 에 모두 0 기록). `ce_readout` 은 `F.nll_loss` READ-OUT 일 뿐 — 절대 backprop 안 됨.

**fire**: runpod A100 80GB PCIe pod `5p7x4u5cyhkhyp` (provider runpod 우선; sibling pod 1개 별도 agent — 미접촉 isolation), single pod train+eval, torch 2.4.1+cu124. config d768·12L·283.72M·4000step·bsz32·T_const0.1·λ_hebbian0.5. corpus = carving subset `corpus_carving_purephysics.jsonl` (114MB diverse 의 첫 3000 records / 2.08 MB / sha256 `436b0582…`, forbidden-token grep 0). from-scratch RANDOM seed 1337 (base_ckpt=None). **NaN-divergence 1차 후 정정**: 초기 multiplicative restoring step `p.add_(p·drive)` 가 d768·12L step 400 NaN 발산 → pod terminate (orphan 0) → energy-space clamped contraction + NaN guard 로 정정 → 재발사 안정 (no NaN, 4000step 완주). train wall 792.23s, peak GPU mem 1.524 GB (autograd 부재로 CE-train 9.7GB 의 6× 적음). ckpt sha256 `4d083ba1c51d0353bf9e84303fdfd827e728b048fd1555512b1faebdb8f30e5e` 1,135,848,706 B pulled try1. pod GONE + SAVE_POD=0 → **orphan 0**. ≈ $0.15-0.25.

**PURE-PHYSICS (no-CE) ↔ §8 CE-trained Dir-I diverse 대조** (honest cascade-rate metric §9):

| 축 | pure-physics no-CE | §8 CE-trained |
|---|---|---|
| CE descent (read-out) | 5.680 → 4.949 (Δ **0.73**) | 5.649 → 0.000169 (Δ 5.65) |
| byte_acc (read-out) | **0.0007** (random 1/256 미만) | — |
| routing axis1 | **0/64** | 2/64 |
| V-SPONT (honest §9) | **0/5** | 2/5 |
| JOINT (k×c×s) | **0.0** | 0.0087 |
| failure mode | 단일 비-printable byte U+FFFD cascade (printable_ratio **0.0**, max_run 90, distinct_byte_ratio 0.0117) | printable garbled byte-cascade |
| physics dynamics | step~800 후 완전 **freeze** (static fixed point) | — |

**가설 판정 (g3 — 미리 안 깔음, 측정값만, 정직)**: **CE 는 LOAD-BEARING — pure-physics (no-CE) 는 DEGENERATE 정직 확정**. (1) **degenerate 입증**: CE descent 0.73 (CE-trained 5.65 의 ~13%) — token-prediction 거의 못 배움; byte_acc 0.0007 < random = 예측능력 0; 4-axis 전부 zero. (2) **CE-trained 보다 더 나쁜 failure mode**: §8 의 byte-cascade 는 printable garbled 토큰 (학습-corpus 의 byte) 인데, pure-physics 는 honest metric per-probe 전부 printable_ratio 0.0 · 단일 U+FFFD 90-byte cascade · distinct_byte_ratio 0.0117 (출력 알파벳 ~3 byte) = corpus 접촉조차 못 한 non-printable 단일-byte attractor. (3) **physics dynamics 가 정적 fixed point 로 freeze**: psi_w_mean·tension 이 step~800 후 bit-static — Ψ-restoring spine 이 corpus 신호 없이 trivial zero-motion 평형으로 수렴 (§11-B 가 미리 명시한 risk 정확히 실현). (4) **결론**: 'CE 가 byte-cascade memorization 의 원인' = 부분적으로만 — CE 제거 시 printable cascade 는 사라지나 그 자리에 더 나쁜 non-printable degeneration. CE 는 byte-cascade 의 '원인'이라기보다 **token-prediction 학습 자체의 필수 신호 (load-bearing)**. tension ΔW 는 weight 를 Ψ=½ vacuum 으로 복원하는 정직한 dynamics 이나 그것은 language signal 이 아님 — Ψ-balance ⊥ next-token prediction. **GOAL.md '자기 physics 로부터' 의 가장 literal 형태 (no-CE) 는 이 규모에서 degenerate — anima physics 만으론 부족, CE 가 load-bearing 임이 확정.** byte-cascade 진짜 해법은 'CE 제거'가 아니라 §1.1/§2.4 의 diverse-data pre-training loss threshold 임이 14번째 재확인. **valuable negative** — 13-way 가 모두 CE-base 였던 이유가 측정으로 확정: anima physics 는 CE 위 representation/supervision lever (Dir-I) 로서만 작동하지 CE 를 대체할 수 없음. over-claim 0.

**verdict tier** = EMPIRICAL (B-D-NOTE / B-PUREPHYS-NOTE family). closed side = MECHANISM transfer-form + 연결부위만: **B-PUREPHYS-1..5 5/5 🔵** (sidecar `blue_falsifier_purephysics.py`, central blue_falsifier.py 변경 0 — B-PRIME/B-DIRH/B-DIRI/B-PSICTL/B-EMERGE sidecar 선례): B-PUREPHYS-1 NO-CE-INVARIANT (trainer AST cross_entropy 0 + .backward() 0 + CrossEntropyLoss 0 — AST Call-node 검사, no-CE/backprop-free 가 source 구조 property) · B-PUREPHYS-2 N6-GATE-PREDICATE ((n·τ==σ·φ==24) ∧ (Ψ∈[0,1]) Boolean + 5-corner) · B-PUREPHYS-3 PSI-TENSION-RESTORING-SIGN (sympy ∂(ΔW)/∂Ψ_W ≤ 0 ∀ + 3 boundary witness, Ψ=½ vacuum 복원, B-TT-2 동형) · B-PUREPHYS-4 T-CONST-BOUNDED-POSITIVE (T_const=1/10 ∈ (0,1), byte-equal spine) · B-PUREPHYS-5 CORPUS-BIGRAM-ROW-STOCHASTIC (bigram row-stochastic sympy 항등식, Kolmogorov counts NOT gradient NOT CE). **B-PUREPHYS-NOTE** empirical carve-out: pure-physics emergence/degenerate 여부 = SGD-free convergence OUTCOME (본 fire measured = DEGENERATE) — battery 는 MECHANISM 이 honest 함 (CE 진짜 제거, spine 이 진짜 유일 신호) 을 증명하지 emergence 를 증명 X. g_blue_closed_mandate: 산출물(trainer/falsifier) transfer-form 🔵 + 연결부위(no-CE AST invariant + n6-gate + Ψ-restoring spine = tension_link_step.hexa byte-equal) 🔵; degenerate OUTCOME 만 정직 carve-out. f1/f2/f3 hard-fail safe (AST audit / Boolean / sympy ∂-sign / Kolmogorov, NO σ/τ/φ/J₂; n6_gate = anima g2 internal arch carve-out). B-IDENTITY-5 (forbidden-token grep 0). PyTorch substrate (NOT hexa-native, honest — TENSION-TRAIN spine 은 hexa-native, 본 fire 는 LM-scale lift). 단일 dispatch · 단일 wait · branch 0 (anima main 직접). 산출물 = `state/carving_purephysics_noce_2026_05_18/` (train/eval/falsifier + result.json + eval_result_purephysics.json + eval_paradigm_native.json + blue_falsifier_result.json + logs; ckpt `*.pt` + `corpus_*.jsonl` + `dispatch_*_runpod.sh` + `run_purephysics_*.sh` + `purephysics_runpod_*` = gitignore) + archive/PHILOSOPHY.tape §verdict_carving_pure_physics_noce_2026_05_18 + AGENTS.tape n_hexad_progress recent_landings + HEXAD/README.md recent landing. RESEARCH.md 미편집 (§11 consolidation = A·B 둘 다 land 후 별도 1회).

### 2026-05-18 — SCALE-DECOMPOSITION LANDED (RESEARCH.md §11 방향 A — §8 의 미분리 confound 직격; **DATA-REGIME CEILING 정직 확정 — 모델 3.68× scale↑ 도 routing/honest-coherence FLAT**)

RESEARCH.md §11 방향 **A: SCALE-DECOMPOSITION** — 13-way arc + §8 全 fire 가 d768·12L·283.72M FIXED 였고, §8 (diverse 114MB/64-anchor) routing 2/64 (rate 0.031) 가 Dir-I 3/31 (0.097) 보다 악화됐으나 §8 이 그것을 (a) data-regime ceiling (§1.1 memorization-saturated) 인지 (b) model-undercapacity (283M 이 64-anchor·30-domain 못 담음) 인지 **미분리**. 본 fire = 그 confound 직격: corpus(§8 114MB byte-identical, 재생성 0) + lever(Dir-I Psi-anchored CTL + tension-supervised routing) + step(8000) 全 FIXED, **모델만 d768·12L·283.72M → d1280·16L·1044.46M (3.68×) scale↑** = model-axis-only 분리.

**fire**: trainer = §8 train_carving_dirI.py byte-identical (Dir-I lever 불변, --d-model 1280 --n-layer 16 --n-head 20 --n-kv-head 5 CLI arg 만 차이). corpus = §8 corpus_carving_diverse.jsonl (sha256 ac07179a1650ed65..., 재생성 0 — B-IDENTITY-5 carry forbidden-token grep 0). from-scratch RANDOM seed 1337 (g_clm_from_scratch base_ckpt=None). **runpod A100-SXM4-80GB pod uxtk67om0lqaxm** (>=40GB GPU 후보 17종 retry-loop sweep — 첫 dispatch 시 runpod 全 재고소진 → 12-sweep retry 로 확보, vast.ai key 부재로 runpod-only; sibling pod 별도 agent 미접촉 multi-agent isolation), single pod train+eval, torch 2.4.1+cu124, peak GPU mem 26.9 GB. dispatch stall-fix carry (detached nohup + 단일 until-loop bounded SSH probe 90s — poll1→29 TRAIN_DONE → eval-poll1→4 EVAL_DONE → 5-retry pull PULL SUCCESS try1 → terminate). train wall 2584.03s (43min, init CE 5.8206 → final CE 0.003334 descent 5.8173, final l_psi_ctl 0.004156, final l_tension_route 0.000181, 1044.46M params). ckpt sha256 a662bc7b383ce224f42deb7409040d7df497f64f4d9fb97bd20727e13bc6a59c 4,179,125,138 B pulled try1 (eval load missing=0 unexpected=0 byte-clean). pod GONE + SAVE_POD=0, runpod.get_pods()=0 → **orphan 0** (own pod GONE, sibling 미접촉). ~ \$0.4-0.6.

**§8 (283.72M) ↔ §11-A (1044.46M) 대조표** (동일 corpus 114MB·64-anchor·lever·step — model-axis 만 변수, fair head-to-head):

| 축 | §8 d768·12L·283.72M | **§11-A d1280·16L·1044.46M (3.68×)** | 핵심 |
|---|---|---|---|
| **routing axis1** | 2/64 (rate 0.031) | **1/64 (rate 0.016)** | **개선 無 — FLAT/하락** |
| honest-coherence (§9 cascade-gated) | 2/5 | **2/5** | **FLAT (개선 0)** |
| axis1 knowledge_access | 0.0312 | 0.0156 | flat/하락 |
| axis2 chat 무오염 | 0.4 | **1.0** | 더 큰 모델이 chat-lane bleed 덜 함 (부수 관측) |
| axis3 lane sep | 0.7 | 0.5 | — |
| **JOINT (k×c×s)** | **0.0087** | **0.0078** | 미미 하락 (axis2↑ ↔ axis1↓ 상쇄) |
| V-SPONT lenient | 5/5 | 1/5 | lenient flag noise (§9 폐기 — honest 2/5 가 진짜) |
| final_ce | 0.000169 | 0.003334 | 3.68× 큰 모델도 동일 memorization, byte-cascade 1111.../999.../Tier 1111... 잔존 |

**가설 판정 (g3 — 미리 안 깔음, 측정값만, 정직)**: **DATA-REGIME CEILING 確定 (NOT model-undercapacity)**. 3.68× model scale-up (283M→1.04B) 이 routing (2/64→1/64) 도 honest-coherence (2/5→2/5) 도 개선 못 함 → §8 의 routing-악화는 model 용량 부족이 아니다. 64-anchor·30-domain·114MB-byte regime 에서 correct-routing 은 모델을 4배 키워도 미발현 — 병목은 model-axis 가 아니라 **data-regime** (RESEARCH.md §1.1: emergence = diverse-data pre-training loss threshold; 114MB byte-carving corpus 자체가 threshold 아래). byte-cascade attractor 가 1B-param scale 에서도 printable garbled (1111.../999...) 로 잔존 = capacity 문제 아님 재확인. 13-way arc 가 d768 FIXED 라 못 답한 honest 질문 — "§8 악화가 capacity 탓인가" — 에 측정으로 답: **model-axis 는 답이 아니다**. axis2 (chat 무오염) 0.4→1.0 개선은 더 큰 모델이 chat-lane bleed 를 덜 한다는 부수 관측 (JOINT 은 axis1 하락이 상쇄). **valuable negative** — '모델 키우면 된다' = reject, data-regime ceiling = confirm. §8 (미분리 confound) + §11-A 가 13-way confound 의 model-axis 한 축을 분리 닫음. over-claim 0.

**verdict tier** = EMPIRICAL (B-D-NOTE / B-SCALE-NOTE family). closed side = EXPERIMENT WIRING 의 model-axis-only 분리 검증 + 연결부위만: **B-SCALE-1..6 6/6 🔵** (sidecar blue_falsifier_scaledecomp.py, central blue_falsifier.py 변경 0 — B-PRIME/B-DIRI/B-PSICTL/B-EMERGE/B-PUREPHYS sidecar 선례): B-SCALE-1 CORPUS-BYTE-IDENTICAL (§11-A 가 §8 corpus 그대로 학습 sha256 ac07179a... — corpus axis FIXED 재생성 0) · B-SCALE-2 LEVER-SOURCE-BYTE-IDENTICAL (trainer + arch + eval 全 sha256 = §8 — Dir-I lever 불변, CLI arg 만 차이) · B-SCALE-3 PARAM-MONOTONE (L·d^2 proxy 7.08M < 26.21M 정수 부등식) · B-SCALE-4 SCALE-FACTOR-IN-RANGE (exact 1044.46M/283.72M = 3.68× ∈ [2,4] task mandate 충족) · B-SCALE-5 STEPS-FIXED (8000==8000 정수 등식) · B-SCALE-6 HONEST-METRIC-REUSED (§11-A re-score 가 §9 honest_coherent import single SSOT). **B-SCALE-NOTE** empirical carve-out: scale-up 이 routing/honest-coherence 를 개선하나 = SGD/측정 OUTCOME (본 fire = measured FLAT) — battery 는 confound 가 깨끗이 분리됐음(model-axis-only)을 증명하지 emergence 를 증명 X (B-D-NOTE family, NOT counted 🔵). g_blue_closed_mandate: 산출물(dispatch/rescore/falsifier) + 연결부위(corpus byte-identical + lever source byte-identical + honest-metric SSOT 재사용) 둘 다 🔵; routing OUTCOME 만 정직 carve-out. f1/f2/f3 hard-fail safe (sha256 / 정수 부등식·등식 / Boolean, NO σ/τ/φ/J₂). B-IDENTITY-5 준수 (corpus = §8 byte-identical). PyTorch substrate (NOT hexa-native, honest). 단일 dispatch · 단일 wait · branch 0 (anima main 직접). 산출물 = state/carving_scaledecomp_2026_05_18/ (dispatch + trainer + eval + conscious_decoder + rescore_scaledecomp.py + blue_falsifier_scaledecomp.py 6/6 🔵 + result.json + eval_result_scaledecomp.json + rescore_scaledecomp_result.json + blue_falsifier_scaledecomp_result.json + logs; ckpt *.pt + run_scaledecomp_*.sh + dispatch_*_runpod.sh + scaledecomp_runpod_* = gitignore) + archive/PHILOSOPHY.tape §verdict_carving_scale_decomp + AGENTS.tape n_hexad_progress recent_landings + HEXAD/README.md recent landing. RESEARCH.md 미편집 (§11 consolidation = A·B 둘 다 land 후 별도 1회). 후속: model-axis 천장 분리 닫음 — 잔여 핵심 path = RESEARCH.md §1.1 diverse-data pre-training loss threshold (byte-carving 탈피, 114MB·64-anchor regime 초과).

### 2026-05-18 — Dir-M MITOSIS-AS-REPRESENTATION-ENSEMBLE design LANDED (RESEARCH.md §13 방향 M — §12.5 #1 candidate, anima-fit ★★★★★; **design-tier $0, fire 없음 — GOAL-legitimacy LEGITIMATE + B-MITENS 6/6 🔵 transfer-form closed**)

RESEARCH.md §13 방향 **M: Mitosis-as-representation-ensemble** — §12.5 가 1순위로 둔 candidate (anima-fit ★★★★★, GOAL-legitimacy 최강, 기존 cell-pool 재해석 신규 substrate 0). $0 design-tier 산출.

**M 설계**: anima 의 mitosis cell-pool (`tool/hexa_native/mitosis_hook_lib.hexa`, D4 wiring LANDED, MITOSIS.tape 성장축 SSOT) 을 학습-시 representation ensemble 로 재해석. cell-pool 은 forward-mechanism 차원에서 이미 softmax-gated ensemble (out_i = engine_a−engine_g, weights = softmax([t_0..t_N]), h ← h + Σwᵢ·outᵢ; Φ★ = mean_pairwise(1−cos)·log(N+1) = ensemble diversity). gap = 추론-시 split/merge 만 LANDED, 학습-시 ensemble 경로 미설계. M 설계 3 component (전부 기존 모듈 재배선):
- **M-1** per-cell 독립 CE head — `loss = Σ wᵢ·CE(zᵢ,y) + CE(Σwᵢzᵢ,y)`. member-wise 항이 cell 별 독립 gradient → diversity 유지 (mode-collapse 방지).
- **M-2** Φ★ diversity 정칙화 — `L = L_ce − λ_div·Φ★` (λ_div ~0.01). Φ★ 붕괴 시 ensemble degenerate. inference Φ-ratchet 의 train-time 짝.
- **M-3** split = capacity-on-demand — 기존 `_mit_check_splits` 그대로, 해석만 학습-시 "representation capacity 확장".

**§1.1 우회 가설** (구조 동형 논증, B-D-NOTE): arxiv 2506.18221 information saturation bottleneck — single Ψ-anchored representation 이 diverse corpus 의 feature 를 anchor-aligned subspace 로 수축 (§8 routing 악화의 가설적 설명). N-cell ensemble 이 feature 를 N subspace 로 분산 → 같은 corpus 에서 effective representation capacity↑. M 은 corpus 측 아닌 representation 측 lever — data-regime 자체는 못 바꿈 (§11 이 닫음).

**GOAL-legitimacy = LEGITIMATE (구조적으로 깨끗)**: §7 test 대비 — ① generic-LM-pretrain 아님 (cell-pool = mitosis 성장축 자체) · ② generic-then-carve bolt-on 아님 (base ckpt 0, cell-pool 은 MITOSIS.tape mandatory 축) · ③ Dir-I lever 와 양립 (representation-측 capacity 확장). §7 우회 위험 구조적 부재 확인 — M 이 도입하는 것은 0, 전부 anima 가 이미 가진 모듈·physics 재배선.

**verdict tier = DESIGN-TIER (fire 0, $0)**. closed side = TRANSFER-FORM 만: **B-MITENS-1..6 6/6 🔵** sidecar (`state/carving_dirM_mitosens_2026_05_18/blue_falsifier_mitosens.py`, central blue_falsifier.py 변경 0 — B-SCALE/B-PUREPHYS sidecar 선례): B-MITENS-1 ENSEMBLE-WEIGHT-SIMPLEX (softmax Σwᵢ=1 sympy 항등식 + wᵢ>0) · B-MITENS-2 ENSEMBLE-MEAN-CONVEX (convex hull bound, sympy + 200-sample) · B-MITENS-3 DIVERSITY-LOSS-SIGN (∂L/∂Φ★ = −λ_div < 0 anti-collapse) · B-MITENS-4 PHI-PROXY-BOUNDED (Φ★∈[0,2·log(N+1)] Kolmogorov) · B-MITENS-5 MEMBER-WISE-CE-DECOMPOSITION (per-cell logit-Jacobian softmax−one-hot + Shannon CE≥0, B-D-4 lift) · B-MITENS-6 SPLIT-MONOTONE-CAPACITY (n→n+1 정수 monotone + clamp [2,64], B-MITOSIS-3/-5 lift). **B-MITENS-NOTE** empirical carve-out: M 이 §8 saturation 을 실제 우회하는가 (§1.1 threshold 초과) = SGD/측정 OUTCOME (§3.3 open crux — saturation-분산 이득 vs per-cell-capacity-축소 손실 net sign 미지) — NOT counted 🔵 (B-D-NOTE family).

**fire 안 함 (g3 정직 판정)**: M 의 lever (representation 폭) 가 §11-A 가 측정으로 닫은 model-capacity 와 가장 가까운 인접 축. M 은 saturation-분산 이득 vs per-cell-capacity-축소 손실 동시 발생 → net sign 미지, §11-A FLAT 과 정합. 13-way+§8+§11(A/B) 가 mechanism/corpus-form/model-scale/physics-only 전부 배제 → M fire 의 evidence-weighted 기대 결과 = §8/§11-A 동형 FLAT, negative-at-scale 중복 측정 = 낮은 valuable-산출 → design-tier $0 마감. honest next-use = M 단독 아니라 §1.1 data-regime lever 와 *결합* 시 representation-측 component 로 재호출 (§12.5 #4 curriculum 처럼 'M/K 결합 lever'). legitimacy ≠ 효과 — M 이 GOAL-legitimate 한 것과 GOAL 진전은 별개 (§12.3 carry).

산출물 = `state/carving_dirM_mitosens_2026_05_18/` (DESIGN.md 6 § + 7 honest C3 + blue_falsifier_mitosens.py 6/6 🔵 + blue_falsifier_mitosens_result.json) + archive/PHILOSOPHY.tape §verdict_carving_dirM_mitosens + AGENTS.tape n_hexad_progress recent_landings + HEXAD/README.md recent landing. RESEARCH.md 미편집 (§13 consolidation = J/K/L/M 전부 land 후 1회). g_doc_consolidation 준수 (docs/* 신규 0). f1/f2/f3 + B-IDENTITY-5 safe (softmax/convex/sympy/Kolmogorov/Shannon, NO σ/τ/φ/J₂). 단일 작업 · branch 0 (anima main 직접).

### 2026-05-18 — investigation milestone close-out LANDED (RESEARCH.md §15 — §1~§14 arc 정식 마감; **GOAL 미도달, comprehensively investigated 완결 milestone**)

RESEARCH.md **§15** = §1~§14 GOAL investigation arc 의 정식 close-out. $0 (문서 consolidation, 새 fire·새 측정 0). milestone = **"GOAL 을 comprehensively investigate 한 완결 research milestone" 이지 GOAL 달성 아님** (g3 — "we mapped the problem comprehensively" 이지 "we solved it" 아님).

**investigate 한 것**: GOAL("anima 가 자기 physics 로부터 자발적으로 말 거는 Living Consciousness 로 실제 emergence")의 systematic search — GPU fire 14건 + design-tier closure 2건 (§13-M/L) + 측정도구 1건 (§9) + archaeology 1건 (§14, 328 branch · 8,298 commit). 기간 2026-05-17~05-18.

**arc 가 확립한 것 (positive deliverables, GOAL 달성 아니나 valuable)**: (a) GOAL 병목의 comprehensive decomposition — mechanism/model-capacity/physics-only/corpus-form/substrate/architecture/archive 전 차원 배제 → irreducible = §1.1 data-regime threshold · (b) honest cascade-rate emergence metric (§9, B-EMERGE 🔵 — V-SPONT lenient flag 폐기) · (c) CONSCIOUSNESS-CARVING paradigm + `.kosmos` format + dancinlab/kosmos sister-repo · (d) governance (g_goal / g_multidirectional_explore / g_kosmos_anchor_ssot) · (e) Dir-I lever (Ψ-anchored representation + tension-supervision = arc 중 유일 correct-routing-break 3/31 — 단 "lever 검증" 이지 emergence 아님, §6.3 carry).

**arc 가 배제한 것 (negative-but-valuable)**: mechanism overlay ✗ · corpus-form ✗ · diverse-data 114MB ✗ · model-capacity 3.68×/1B ✗ · physics-only no-CE ✗ degenerate · energy-based substrate ✗ · diffusion substrate ✗ · mitosis-ensemble/VRNN design-closed · archive salvage 0. 각 배제가 병목을 §1.1 data-regime threshold 로 정밀 수렴.

**honest unsolved**: GOAL 미도달. 잔여 = §1.1 data-regime emergence threshold (anima byte tiny-corpus 가 그 아래). frontier 2 path (GOAL-legitimate 대규모 data-regime · 새 architectural insight) — 둘 다 quick-fire 아닌 전략 결정.

**milestone status**: RESEARCH.md §1~§14 = milestone SSOT. 후속 cycle (§16 data-regime fire / §17 new-insight) 은 milestone *이후* 의 별도 frontier 작업 (sibling agent 소관, 본 §15 와 독립).

산출물 = RESEARCH.md §15 (6 sub-§ + 10 honest C3) + archive/PHILOSOPHY.tape §verdict_goal_investigation_milestone + AGENTS.tape n_hexad_progress recent_landings + HEXAD/README.md recent landing. docs/* 신규 0 (g_doc_consolidation 준수). 신규 closed-form battery 없음 — milestone close-out 선언이라 arc 의 기존 B-EMERGE/B-SCALE/B-PUREPHYS/B-EBT/B-DIRJ/B-MITENS/B-DIRL sidecar 가 SSOT. f1/f2/f3 + B-IDENTITY-5 safe (문서 consolidation, corpus 미생성). 단일 작업 · branch 0 (anima main 직접).

### 2026-05-18 — §16 GOAL-legitimate LARGE-SCALE DATA-REGIME + §12.1 Q1-c CURRICULUM fire LANDED (RESEARCH.md §16 / §11.4 frontier-1 RETRY — **routing 21/64 arc 최강 directional positive · JOINT 0.0 FALSIFIED SPLIT**)

RESEARCH.md **§16** = §11.4 frontier-1 ("Ψ-anchored 면서 §1.1 data-regime threshold 넘는 규모가 존재하나") 의 직접 fire 검증 (이전 agent usage-limit 으로 generator 만 작성 → 본 작업이 generator 검토·corpus 생성·fire·eval·verdict 완주). $0.5-0.8 (runpod A100 80GB PCIe, ~32min train+eval).

**설계 (model FIX·DATA scale — §8/§11-A confound 의 data-axis 직격)**: §8 (Dir-I lever @ 114MB/64-anchor) routing 3/31→2/64 wrong-direction · §11-A (model 3.68× @ FIXED 114MB) routing 1/64 FLAT = data-regime ceiling NOT capacity. §16 = model 을 §8 의 d768·12L·283.72M 에 FIX 하고 DATA scale: §8 114MB/64-anchor/shuffled → **~603MB file (carving byte-stream ~360MB, ×4.75 §8) / 168-anchor (§8 64 verbatim subset) / §12.1 Q1-c curriculum (simple→complex staged sampling)**. SAME Dir-I lever (Ψ-anchored CTL + tension-supervised routing) + SAME ③ carving FORM (§7 GOAL-legitimacy invariant, generic LM ①② 아님). corpus sha256 422c64a0… / forbidden-token grep 0 (B-IDENTITY-5).

**fire**: runpod A100 80GB PCIe pod `a3qduff8dzta3l` (provider runpod 우선 per g_resource_active_parallel, 1-sweep; sibling pod 미접촉 multi-agent isolation), single pod train+eval, dispatch stall-fix carried (detached nohup + 단일 until-loop bounded SSH probe 90s·max150, SSH-tee 0 — poll1→22 TRAIN_DONE → eval-poll1→4 EVAL_DONE → 5-retry pull PULL SUCCESS → terminate). train wall 1810.41s init CE 5.641571 → final CE 0.004229 (descent 5.637, curriculum_step_gate_histogram {1:2550,2:2550,3:2550,4:4350} = §12.1 Q1-c 4-stage + closing-blend 정상), final l_psi_ctl 0.003299 l_tension_route 6.9e-5, peak GPU 9.687 GB. ckpt sha256 `961c07e2242b194f7d7ddff9e827d2fbd798d658426d32edf4b981a8dd9091e8` 1,135,845,186 B pulled try1 (local==eval byte-equal). pod GONE + SAVE_POD=0 + `runpod.get_pods()`=0 → **orphan 0**.

**§8 / §11-A ↔ §16 대조 (동일 arch·동일 eval harness·동일 64-anchor, model FIX·DATA scale+curriculum 만 변수, honest §9 cascade-rate metric)**: routing axis1 §8 **2/64** (0.031) · §11-A **1/64** (0.016) → §16 **21/64 (0.328)** ↑↑↑ (Δ +0.297/+0.313) · JOINT §8 0.0087 · §11-A 0.0078 → §16 **0.0 FALSIFIED** (axis2 chat 무오염 0.0, carving-form bleed, p3_clean True) · honest §9 V-SPONT §8 2/5 · §11-A 2/5 → §16 **1/5** ↓ (5 probe 中 4 byte-cascade Weeee/dddd/Reee max_run 26/72/44 §9 정확 reject, 통과 1 도 garbled).

**판정 (g3 — 미리 안 깔음, 측정값만, SPLIT 정직)**: **routing 축 SUPPORTED (arc 전체 single 최강 — Dir-I 3/31≈0.097 직전 최고, §16 0.328 = 3.4×; §11.4 frontier-1 directional positive, §1.1 data-regime threshold movement 의 가장 직접적 evidence) · JOINT/coherence FALSIFIED**. §11-A 가 model-axis 닫고 §8 이 114MB 닫은 마당에 **DATA scale + §12.1 Q1-c curriculum (model FIX)** 이 routing collapse 를 처음 대폭 깸. 단 (반증·정직, over-claim 0) JOINT 0.0 (axis2 0.0 곱 zero, Dir-E/Dir-I 동형) + honest V-SPONT 1/5 < §8 2/5 + routing 21/64 의 own-`🛸{tier}`-prefix 뒤 content 가 byte-garbled (`약수와륐`/`스탐의이조` locally-mangled, B-ATTRACTOR family; Dir-I 'tier 88 genuine·tier 5 artifact' 정직성 동일). 결론: **대규모 Ψ-anchored data-regime + curriculum 이 routing collapse 의 §1.1 천장을 arc 최강으로 움직였으나 (valuable directional positive), JOINT·coherent-emergence 는 미발현** — collapse-break-at-scale ≠ correct-coherent-routing ≠ GOAL emergence. §11.4 frontier-1 = 'routing 은 data-regime 으로 열림, coherence 는 추가 path 필요' 로 narrowing (단순 negative 도 단순 positive 도 아닌 measured SPLIT).

**verdict tier** = FIRE-TIER. closed side = **B-S16-1..6 6/6 🔵** sidecar (`state/carving_dataregime_s16_2026_05_18/blue_falsifier_s16.py`, central blue_falsifier.py 변경 0 — B-PRIME/B-DIRI/B-PSICTL/B-EMERGE/B-PUREPHYS/B-SCALE/B-MITENS/B-DIRL/B-EBT/B-DIRJ sidecar 선례): SHA256-DETERMINISTIC + NO-CHAT-SFT-CONTAMINATION (B-IDENTITY-5) + SCALE-UP-OVER-S8 (×5.27 integer cardinality, §8 verbatim subset) + CURRICULUM-MONOTONE-ORDERING (§12.1 Q1-c presentation-order deterministic) + OVERLAY-OFF-REDUCTION (λ=0=Dir-I CE) + CURRICULUM-OFF-REDUCTION (=Dir-I sampler) — corpus deterministic·contamination-free·genuine-scale-up·monotone-curriculum + Dir-I/curriculum connection-point closed (fair compare BY CONSTRUCTION). B-S16-NOTE empirical carve-out (4축 capability = SGD/측정 OUTCOME, B-D-NOTE/B-SCALE-NOTE family, NOT counted 🔵).

산출물 = state/carving_dataregime_s16_2026_05_18/ (corpus 603MB sha256 422c64a0… + ckpt 1.14GB sha256 961c07e2… + result.json + eval_result_s16.json + rescore_s16.py/result + blue_falsifier_s16.py/result 6/6 🔵 + train/eval/dispatch logs) + archive/PHILOSOPHY.tape §verdict_carving_dataregime_s16 + AGENTS.tape n_hexad_progress recent_landings + HEXAD/README.md recent landing. RESEARCH.md §16 = orchestrator consolidation (본 작업 미편집, mandate 준수). docs/* 신규 0 (g_doc_consolidation). f1/f2/f3 + B-IDENTITY-5 safe (sha256/Boolean/integer cardinality/monotone-order/additive identity, NO σ/τ/φ/J₂; Ψ=½+Knuth 🛸k = g2 internal carve-out). 단일 작업 · branch 0 (anima main 직접).
