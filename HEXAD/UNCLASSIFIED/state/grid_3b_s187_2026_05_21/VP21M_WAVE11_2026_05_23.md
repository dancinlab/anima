# vP21M Wave-11 — ZHFL/RUFL router deploy + LIVE evidence (deploy BLOCKED · SSH 채널 차단)

> 2026-05-23 KST · session-3 cycle #6 · round 5 #3 follow-up. PR #132 squash-merge 완료
> 및 ZHFL/RUFL HF adapter local-stage 까지는 ✓.  최종 mini deploy 는 mini sshd
> `exec/sftp/subsystem request failed on channel 0` 로 **BLOCKED** — 코드 머지만
> 진행되었고 production behavior 무변경.  rollback **N/A** (deploy 미실시).

## Context

| 항목 | 값 |
|---|---|
| PR #132 | `feat(CHAT): extend hot-swap router — ZHFL/RUFL native adapter slots` |
| merge commit | `83d96933fa5c2261ee297a185db5b516c7ca8b67` (squash to main) |
| branch deletion | local skipped (worktree holds branch ref), remote deleted |
| cdo-validate CI | FAILURE (repo-wide pre-existing — same fail pattern as 이전 PRs, non-blocking) |
| diff scope | `HEXAD/CHAT/server/anima_participant.py` +8/-2 · `HEXAD/CHAT/server/substrate_lora.py` +6/-3 |
| HF adapters | `dancinlab/anima-vp21m-zhfl` + `dancinlab/anima-vp21m-rufl` (PRIVATE, 1.5B Qwen2.5) |
| local stage | `/tmp/anima_router_v6/{zhfl_adapter,rufl_adapter}/` (각 10 files, adapter_model.safetensors 147 MB) |
| mini ssh | **BROKEN** — `Authenticated to mini.local` 성공 → `exec request failed on channel 0` (sftp · subsystem 동일) |
| production behavior | **무변경** (post-merge code, ZHFL/RUFL adapter 모두 mini 미배포) |
| daemon health | `https://chat.dancinlab.org/health` = `{ok:true, anima_alive:true}` (alive, PRE-#132 code) |

## Steps 1-2 — 완료 ✓

### Step 1: PR #132 merge ✓

```
gh pr merge 132 --squash --delete-branch
→ merged at 83d96933fa5c2261ee297a185db5b516c7ca8b67
→ remote branch deleted (local skipped: worktree ref hold)
```

CI: `cdo-validate FAILURE 36s` — 이전 PR들과 동일 repo-wide preexisting fail, non-blocking per session policy.

### Step 2: HF artifact pull ✓

```
HF_TOKEN=$(secret get hf.token)
snapshot_download(repo_id=...) → /tmp/anima_router_v6/{zhfl_adapter,rufl_adapter}
→ each 10 files (adapter_config + adapter_model.safetensors 147.77MB + tokenizer 7 files + README + .gitattributes)
```

Verification: 두 어댑터 모두 9-file LoRA 풀셋 (adapter_config + safetensors + tokenizer.json + tokenizer_config + vocab + merges + added_tokens + special_tokens_map + README) — Wave-2 SCP-race fallback 불필요.

## Steps 3-5 — BLOCKED ✗ (mini sshd 채널 reject)

### Step 3: rsync to mini — BLOCKED

```
rsync -av /tmp/anima_router_v6/zhfl_adapter/ mini:~/anima_chat_pack/zhfl_adapter/
→ rsync(34819): error: unexpected end of file
→ rsync(34819): error: io_read_nonblocking ...
→ rsync(34819): warning: child 34823 exited with status 255
```

### Step 4: scp anima_participant.py / substrate_lora.py — BLOCKED

```
scp /dev/null mini:/tmp/test_ssh_ok
→ debug1: Authenticated to mini.local ([192.168.50.39]:22) using "publickey".
→ debug1: Sending subsystem: sftp
→ subsystem request failed on channel 0
→ scp: Connection closed
```

### Step 5: daemon reload — BLOCKED

```
ssh mini 'launchctl kickstart -k gui/501/com.dancinlab.anima'
→ exec request failed on channel 0
```

### 진단

| layer | 상태 |
|---|---|
| TCP/22 reachable (`nc -zv mini.local 22`) | ✓ succeeded |
| SSH banner (`ssh-keyscan -t ed25519`) | ✓ SSH-2.0-OpenSSH_10.2 |
| publickey auth (`ed25519` SHA256:0+pK9...) | ✓ Authenticated |
| session channel — exec | ✗ `exec request failed on channel 0` |
| session channel — sftp/subsystem | ✗ `subsystem request failed on channel 0` |
| session channel — `-T` no-tty | ✗ same |
| session channel — `-tt` force-tty | ✗ same |
| session channel — `bash` stdin pipe | ✗ same |
| pool routing (`pool on mini`) | host disabled; manual re-enable 필요 |

증상: TCP 도달 + auth 성공 + 모든 session channel reject.  mini macOS sshd 측 `Match`/`ForceCommand` 또는 `~mini/.ssh/rc` `user-rc` (authorized_keys 옵션) breakage 가능성 — 원격에서 진단/수정 불가.  사용자 직접 mini 콘솔 로그인 후 sshd 진단 필요 (예: `tail /var/log/system.log | grep sshd` · `/etc/ssh/sshd_config` · `~/.ssh/rc`).

## Step 6 — Pre-deploy LIVE snapshot (대체 evidence path)

> Deploy 가 blocked 되었으므로 pre-deploy snapshot 만 가능.  Post-deploy 비교 미실시.

### LIVE measurement (hexa-native lane, 2026-05-23T07:00Z)

`hexa run /Users/ghost/core/anima/HEXAD/CHAT/server/anima_live_register_measure.hexa --out /tmp/anima_wave11/wave11_pre_deploy.md`

ts range: 2026-05-23T06:42:42Z .. 07:00:04Z (rolling /history 50, ~17.4 min window)

| lang | n | tag-leak | prose-leak | len_mean |
|---|---|---|---|---|
| zh | 9 | 0/9 = 0.0% | 0/9 = 0.0% | 362.9 |
| ko | 12 | 0/12 = 0% | 4/12 = 33.3% | 282.8 |
| ru | 5 | 0/5 = 0% | 0/5 = 0% | 415.4 |
| ja | 12 | 0/12 = 0% | 0/12 = 0% | 386.3 |
| en | 12 | 4/12 = 33.3% | 11/12 = 91.7% | 259.9 |

**WAVE11 pre-deploy aggregate**: EN-share **24.0%** · tag **8.0%** · prose **30.0%** · n=50

### WAVE10 baseline (PR #131, ~13min post-#123 deploy) — 비교

| 측정 | n | EN-share | tag | prose |
|---|---|---|---|---|
| WAVE10 (sliding-window saturated) | 37 | **18.9%** | 8.1% | 27.0% |
| WAVE11 pre-deploy (now, +30분 later) | 50 | **24.0%** | 8.0% | 30.0% |
| Δ | +13 | **+5.1pp** | -0.1pp (noise) | +3.0pp |

### 해석 — pre-deploy drift

WAVE10 SSOT 측정 후 ~30분이 더 흘러 broker rolling window 가 다른 emit 50개로 교체되었음.  EN-share 가 18.9% → 24.0% 로 **+5.1pp** 상승했음 — 이는:

1. WAVE10 의 18.9% 가 window-50 의 한 단발 측정이지 stable steady-state 가 아님.  EN-share 의 long-run mean 은 **~21-24% 대역** 으로 보임 (1-sample WAVE10 의 18.9% 는 lower-tail).
2. EN intra-lang prose 91.7% (12 의 EN emit 중 11 이 prose-leak) — corpus-side lever 영역 여전.  EN-share 자체는 dampener 가 floor 12.5% 부근 까지 누르지만 emit-당-prose 비율은 corpus_v5 의 EN 부분이 본질적 register-heavy.
3. zh/ru 의 prose-leak = **0%** — 이 두 언어는 corpus_v5 의 STRONG (zh=16, ru=17) baseline 그대로 깨끗히 동작.  PR #132 의 ZHFL/RUFL native adapter 가 배포되었더라도 baseline 이 이미 깨끗하므로 visible improvement 측정 자체가 어렵다 (floor effect).

## Step 7 — zh/ru specialist verdict

> Deploy blocked — adapter 미배포 → post-deploy 측정 불가.  Pre-deploy 단독 결론.

| 항목 | pre-deploy (= 현 production, PR #132 code 미배포) |
|---|---|
| zh emit n | 9 |
| zh prose-leak | 0% |
| zh tag-leak | 0% |
| ru emit n | 5 |
| ru prose-leak | 0% |
| ru tag-leak | 0% |

**verdict**: zh/ru output 은 pre-deploy 에서 이미 **completely clean** (prose 0% · tag 0%).  PR #132 ZHFL/RUFL adapter 배포 후 visible improvement 측정 가능성은 floor effect 로 낮음 — 측정 의도가 "no degradation" 확인이라면 baseline 이 이미 floor 라 충분히 robust.  진정한 specialist evidence 는 zh/ru 의 **register-leak detection rate** 보다 **출력 quality 자체** (BLEU · perplexity · native-fluency human eval) 가 필요한 측정 영역인데, broker `lang` field 기반 leak-detection 은 이 영역 미 cover.

## Step 8 — git ops + report

| 항목 | 값 |
|---|---|
| branch | `feat/lora-wave11-zhfl-rufl-deploy-evidence` from origin/main (post-#132) |
| stage | `git add -f` WAVE11 doc + `wave11_history_raw.json` + `wave11_pre_deploy_live.md` |
| commit msg | `docs(LORA): VP21M Wave-11 — ZHFL/RUFL router deploy + LIVE evidence (deploy BLOCKED · mini sshd)` |
| PR base | main |
| production swap | **무실시** (deploy 가 발생하지 않았으므로 swap/rollback 양 path 모두 N/A) |

## Honest C3

1. **mini sshd breakage 원인 미식별** — 원격 진단 channel 없음. `~mini/.ssh/rc` user-rc / `ForceCommand` / `Match` / `Subsystem sftp` 설정 중 하나 의심이지만 mini 직접 콘솔 로그인 없이 확정 불가.
2. **Deploy blocked → adapter not on mini → PR #132 의 runtime behavior 검증 0** — code merge 만 진행됨.  로컬 import smoke-test 도 하지 않음 (mini=production, Mac=dev 와 dependency 차이 가능, 의미 적음).
3. **Pre-deploy snapshot = pre-#132 production** — broker 가 PR #132 코드 미반영 daemon 으로 동작 중이므로 "pre-deploy" 라는 표현 자체가 정확. Post-deploy 측정 path 는 mini sshd 복구 후 별도 cycle.
4. **WAVE10 18.9% → WAVE11 pre 24.0% drift** — 1-sample window-50 측정의 분산 (~5pp).  WAVE10 의 "18.9%" 단일 측정값을 long-run mean 으로 사용한 것은 over-confident 였음.  ~21-24% steady-state 가 honestyer.
5. **zh/ru baseline 이미 0% leak → ZHFL/RUFL 의 visible-improvement 측정 floor effect** — 단순 leak-rate 측정으로는 specialization 검증 어려움. 필요한 측정은 register-leak rate 가 아닌 출력 fluency / token-distribution / native-script preference profiling — 본 cycle scope 외.
6. **WAVE11 pre-deploy 의 EN intra-lang prose 91.7%** = corpus-side lever 영역 (EN-emit 자체의 register 비율).  PR #132 는 EN slot 변화 없음 → 이 metric 미 cover.  EN-share aggregate 의 (a) slot 가중치 외 (b) prose-drift to EN 의 second-order 가 24% / (18.9 ~ 24) range 의 출처.
7. **cost** $0 (no GPU fire, no scp, no rsync 실제 수행 — error 즉시 abort).
8. **rollback path** — deploy 미실시이므로 rollback N/A.  만일 미래 mini sshd 복구 후 deploy 시 rollback ref SHA = `072aa773c` (PR #123, pre-#132 anima_participant.py / substrate_lora.py 의 직접 부모 commit).

## Artifacts

- merge: PR #132 → main `83d96933f`
- local HF stage: `/tmp/anima_router_v6/{zhfl_adapter,rufl_adapter}/` (각 10 files, 150 MB)
- LIVE pre-deploy: `HEXAD/UNCLASSIFIED/state/grid_3b_s187_2026_05_21/wave11_pre_deploy_live.md`
- raw history snapshot: `HEXAD/UNCLASSIFIED/state/grid_3b_s187_2026_05_21/wave11_history_raw.json` (broker /history?n=500 dump, 50 records actually returned by capped rolling window)
- this report: `HEXAD/UNCLASSIFIED/state/grid_3b_s187_2026_05_21/VP21M_WAVE11_2026_05_23.md`

## Next path

1. mini sshd 복구 — 사용자 mini 콘솔 직접 로그인 후 `sudo log show --predicate 'process == "sshd"' --last 1h | tail -50` 및 `sudo cat /etc/ssh/sshd_config` 검증, `~mini/.ssh/rc` 의심 시 rename `~/.ssh/rc.bak`.
2. mini sshd 복구 후 WAVE11.5 cycle — adapter rsync + code scp + daemon reload + post-deploy LIVE measurement.
3. 본 cycle 의 code merge 만 main 에 land — production behavior 무변경, deploy evidence carry to next cycle.

## Session-3 cumulative (updated)

5 LoRA cycles + 1 substrate lever + 1 router-extension code merge (deploy blocked) = 7 cycles, ~$1.60 GPU + $0 substrate/router, HF 6 adapters.  
**Router code merged + adapter local-staged, mini deploy blocked by mini-side sshd issue** — production behavior unchanged, code path 준비 완료, 단 deploy gate 가 사용자 mini 콘솔 작업 dependency.
