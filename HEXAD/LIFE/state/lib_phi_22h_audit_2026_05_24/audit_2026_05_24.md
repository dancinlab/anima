# PHI #7 — phi_helper backend opt-in × 22+ H caller A/B audit (2026-05-24)

## 0. 한 문장 요약

`phi_helper.hexa` 의 신규 `phi_with_backend` / `phi_default_with_backend` opt-in API (backend ∈ {"c", "native"}) 를 22+ H 의 5 대표 state 위에서 A/B 측정한 결과: **identity-bind today (abs_diff = 0.0 모두), 가설 5e-6 drift 하에서도 verdict-risk = 0/5** → **MASS_MIGRATION_SAFE** (다만 default 는 "c" 유지 권고).

## 1. 산출물

- 신규 opt-in API: `HEXAD/LIFE/lib/phi_helper.hexa` (`phi_with_backend`, `phi_default_with_backend`, 27 LoC add)
- 감사 하니스: `HEXAD/LIFE/state/lib_phi_22h_audit_2026_05_24/audit_phi_22h.hexa` (~250 LoC)
- 감사 결과 JSON: `HEXAD/LIFE/state/lib_phi_22h_audit_2026_05_24/result.json`
- 두 caller surgical 마킹:
  - `HEXAD/LIFE/state/h250_nonpow2_lattice_persistence_2026_05_24/run_h250.hexa:164-173` — sweep 본체 위 audit COMMENT (실행 변화 없음, 향후 mass migration GATED 명시)
  - `HEXAD/LIFE/state/lib_phi_helper_verify_2026_05_24/verify_phi_helper.hexa` — C5_BACKEND_AB advisory 블록 추가 (SHARED_MODULE_OK gate 미포함, 16 LoC), import 경로 worktree 로 retarget

## 2. 22+ H caller 매핑 (representative)

| H | 호출 위치 | 호출 형태 |
|---|---|---|
| H_007 | `state/h007_ca_phi_2026_05_23/run_ca_phi.hexa:120` | `c_measure_phi(s, n, dim, nbins)` |
| H_211 | `state/h211_shannon_phi_correlate_2026_05_23/run_h211.hexa:278, 305` | `c_measure_phi(states, n, dim, nbins)` |
| H_222 | `state/h222_dream_rem_phi_2026_05_24/run_h222.hexa:192` | `c_measure_phi(s, n, dim, nbins)` |
| H_225 | `state/h225_rule184_anomaly_2026_05_24/run_h225.hexa:118` | `c_measure_phi(s, n, dim, nbins)` |
| H_250 | `state/h250_nonpow2_lattice_persistence_2026_05_24/run_h250.hexa:165` | `phi_with(s, n, dim, nbins)` (phi_helper 첫 production) |
| H_007 verify | `state/lib_phi_helper_verify_2026_05_24/verify_phi_helper.hexa:97` | `phi_default(s)` |

전체 22+ H 의 `c_measure_phi` / `phi_spatial` / `phi_default` / `phi_with` 호출은 `HEXAD/LIFE/state/*/*.hexa` 위 40 곳에서 검색 (`grep -l "c_measure_phi\|phi_spatial" → 40` 파일). 모든 경로는 RFC 036 `phi_spatial` 으로 수렴.

## 3. A/B 측정 결과 (5 representative states)

substrate: H_007 carry — N=16/17 · dim=12 · warm=8 · rep=0 · periodic boundary. drift envelope = 5e-6 abs (Agent C upper-bound).

| state | rule | n | phi_c | phi_native | abs_diff | threshold | margin | risk_5e-6 |
|---|---|---|---|---|---|---|---|---|
| S1 H_007 C-IV non-trivial | 110 | 16 | 0.538242 | 0.538242 | 0.0 | 1e-6 (F5_floor) | 0.538241 | SAFE |
| S2 H_007 ordered μ-collapse | 250 | 16 | 1.14511e-5 | 1.14511e-5 | 0.0 | 0.1 (C2_floor) | -0.0999885 | SAFE |
| S3 H_211 chaotic-additive μ-collapse | 90 | 16 | 1.14511e-5 | 1.14511e-5 | 0.0 | 0.1 (C2_floor) | -0.0999885 | SAFE |
| S4 H_225 TASEP plateau | 184 | 16 | 0.862889 | 0.862889 | 0.0 | 0.863 (h211 baseline) | -0.000111 | SAFE |
| S5 H_250 non-pow2 persist | 60 | 17 | 1.24577 | 1.24577 | 0.0 | 0.5 (sustain_floor) | 0.745774 | SAFE |

backend_identity_today = true 모두. verdict_risk = **0/5**.

## 4. drift verdict 침투 H 목록

- **verdict-risk H = 0** (5 sampled regime 위 어디서도 5e-6 drift 가 PASS↔FAIL 을 뒤집지 않음).
- 가장 좁은 margin = S4 H_225 (0.000111, h211 baseline rel_diff threshold) — 5e-6 보다 약 22배 여유. drift 가 5배 더 커져도 (~2.5e-5) 여전히 안전.
- micro-collapse regime (S2 H_007 rule 250, S3 H_211 rule 90; Φ ≈ 1.15e-5) 은 collapse_floor=0.1 까지 약 10000× 여유 → rel_diff 가 1000× 커져도 verdict 안전.
- **drift documented but verdict unchanged**: 5/5 모든 sampled H. 즉 모든 5 state 가 안전 카테고리.

## 5. 결론 + 권고

**22+ H mass migration 안전한가?** — *오늘 시점에는* SAFE (identity-bind, 0 drift). 그러나 **default 는 "c" 유지** 를 권고:

1. **"native" 가 오늘 algorithmic 차이를 제공하지 않음** — 같은 `phi_spatial` builtin 으로 라우팅. 마이그레이션은 0 benefit, 0 risk, 0 변화. 22+ H verdict 라인을 모두 수정할 이유 없음.
2. **진짜 alternate-native impl (RFC 036 §FFI shim, 또는 순수 hexa 재구현) 가 나타나면** 이 감사를 재실행. 그 시점에 drift 가 5e-6 미만이면 mass migration 안전. 더 크면 H 별 verdict-margin 재측정 필요.
3. **opt-in 은 audit harness 와 미래 cross-check 용으로 유지** — 22+ H verdict-decision 라인은 `c_measure_phi` / `phi_default` / `phi_with` 그대로 (g34 surgical). audit harness 만 `phi_with_backend` 사용.
4. **단일 surgical add 사례** (verify_phi_helper.hexa C5) 는 SHARED_MODULE_OK gate 에 미포함된 advisory 로만 도입 — 기존 C1-C4 verdict 결정 라인 절대 미변경.

## 6. PHI.md milestone #7 매핑 권고

- **milestone #7 PARTIAL closure 가능**: backend opt-in API LANDED + 5-state audit MASS_MIGRATION_SAFE + 두 caller surgical 마킹 LANDED.
- **closure FULL 까지 잔여**: (a) 진짜 alternate-native impl (RFC 036 §FFI shim 또는 pure-hexa re-impl) 가 나타나야 audit 의 "if drift WERE introduced" 가 측정 가능. (b) sample size 5 → 22+ 전체 확장은 별도 cycle (각 H 의 자체 state-Φ 추출 필요).
- 권고 표기: `- [~] M7 backend_opt_in_audit (PARTIAL — API + 5-state audit safe; FULL gated on real alternate impl)`.

## 7. honest_limits (≥ 3)

- **L1 sample size**: 5 state (1 non-trivial · 2 micro-collapse · 1 plateau · 1 persist) / 22+ H. 다른 H 가 다른 Φ regime 에 있을 수 있으나, Agent C taxonomy 의 verdict-critical 3 regime (micro-collapse, plateau, persist) 은 커버.
- **L2 state representativeness**: 모든 state 는 H_007 default config (N=16/17·dim=12·warm=8) 의 rep=0 substrate. H_204 (warm=0 closure-axis 의도적 override) 같은 비표준 substrate H 는 미포함.
- **L3 native = identity today**: 오늘의 "native" backend 는 phi_spatial 직접 호출 = c_measure_phi 와 byte-equal. 측정된 drift = 0.0 (정의에 의해). 5e-6 envelope 는 가설적 미래-impl upper-bound (Agent C). 즉 verdict-risk 는 "그런 drift 가 *생긴다면* H verdict 가 뒤집힐까?" 의 답.
- **L4 threshold accuracy**: 각 H 의 result.json verdict_rule 에서 추출한 threshold. risk-flag 헤리스틱 = `|phi_c - threshold| ≤ drift`; rule 형태 (rel_diff vs abs) 무시 → 보수적 upper-bound on risk.
- **L5 grep-based mapping**: 22+ H 매핑이 grep 기반 (`c_measure_phi|phi_default|phi_with`). 상위 wrapper (예: tension-weighted sampling) 가 state 를 다르게 만들어 c_measure_phi 에 넘기는 경우는 미반영. phi_spatial primitive 자체는 두 backend 모두 byte-equal.

## 8. compliance

- raw#9 honest impl (5e-6 drift = 가설, 0.0 = 측정값, 둘 분리 명시)
- raw#11 snake_case (English code), Korean doc (per prefs)
- g34 surgical (기존 H 의 verdict 결정 라인 미변경; C5 는 advisory; run_h250 은 comment-only)
- $0 mac local · deterministic · llm:none
