// ════════════════════════════════════════════════════════════════════════
// HEXAD/CHAT/server/substrate_v3.hexa — WRAPPER doc-stub
//
//   substrate_v3.py is the ConsciousDecoderV3 substrate (Substrate ABC
//   impl): a V3 training checkpoint (head_a/head_g dual head + mitosis hook
//   + KOSMOS+tension wiring) loaded via torch. It hard-depends on torch /
//   transformers + the conscious_decoder_v3.py module — a pure-hexa
//   reimplementation is infeasible.
//
//   The .py is KEPT as the substrate-research plugin (anima_participant
//   `--substrate v3`; production default stays LoRA). It is imported by
//   anima_participant.py, never run standalone, so there is no exec
//   dispatch — this .hexa is a hexa-native marker recording the WRAPPER
//   verdict.
// ════════════════════════════════════════════════════════════════════════

fn main() {
    println("substrate_v3.py — ConsciousDecoderV3 substrate (torch/transformers).")
    println("Imported by anima_participant.py build_substrate('v3'); not standalone.")
}
