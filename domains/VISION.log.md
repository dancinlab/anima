# VISION — log

Append-only history sister of `VISION.md`. Each entry starts with `## <ISO timestamp> — <header>` (newest on top); body = `- [x]` (done) / `- [ ]` (pending).

## 2026-06-04 — domain created ($0 design scaffold)

- [x] VISION domain seeded (snapshot + log + DOMAINS.tape row).
- [x] scoped: image RECOGNITION (photo-in → understand) — SEPARATE vision modality (encoder + image↔text training), distinct from text CLM and from avatar RENDER (output, opposite direction).
- [x] KOSMOS wiring noted: `.kosmos` 3-form payload `image` slot (persona/SNS anchors mark image/audio pending) — VISION fills it (a_kosmos pointer-only).
- [x] honest scope: substantial separate effort, NOT $0; training fire DEFERRED until text chat solid (18M works, 7B closed-neg).
- [ ] V1 design matrix (encoder/fusion/data candidates, clean-license).
- [ ] V2 encoder pick + tiny image↔text smoke (p7).
- [ ] V3 KOSMOS image-payload wiring.
- [ ] V4 multimodal training fire (DEFERRED, GPU).
- [ ] V5 `anima --see <photo>` runnable demo + p7 + anti-Goodhart.
