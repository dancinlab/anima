@V := "tape" :: spec [active]
  version = "1.0"

@H 1165 := "the H_1158 inverted-U faithful φ_EI PEAK-AT-CRITICALITY HOLDS at larger n — peak γ=1.0 + d≥0.8 vs sub AND super RECURS at n=7 AND n=8 (n-robust, not an n=6 artifact)" :: discovery [🟢 SUPPORTED-NUMERICAL]
  seed       = "H_1158 found a textbook inverted-U: faithful IIT-4.0 φ_EI PEAKS at the critical A⇄G coupling (γ=1.0), measured at n=6 (exact MIP-EI). Does the peak-at-criticality SURVIVE larger n (n=7, n=8) — same interior argmax + d≥0.8 vs sub/super — or does the peak shift / move to an endpoint / the exact MIP become intractable? (a_scale_honest_scope: a single toy n=6 point is a 1-rung curve)"
  method     = "reuse the H_1158 γ-sweep VERBATIM (GAMMAS=[0,0.15,0.35,0.60,1.00,1.60,2.50], FIXED wake-baseline envelope, 12 seeds 300..311) with the channel count n made a FREE parameter; the H_1119 W*_c base [1.0,0.8,1.2] tiled to n (at n=6 this is EXACTLY h1119.W_STAR_C, so the n=6 rung reproduces H_1158 byte-for-byte). faithful φ_EI per γ over 12 seeds at each n∈{6,7,8}; the SAME PROVEN faithful_phi mirror (exact MIP-EI, MI-matrix min-cut)"
  mirror     = "STEP 0 re-proof BEFORE scoring (a_phi_iit4_tool): live `hexa run UNIVERSE/h1165_ref_faithful_n78.hexa` re-captured the LIVE stdlib faithful_phi refs at n=4,5,6,7,8 (integer-trace ref φ=n−1) and the CPU mirror reproduced them VERBATIM (|Δ| 3.5e-10/8.0e-10/2.5e-10/7.0e-10/1.5e-10 OK) + h1012.prove_mirrors_at_n n=4 AND n=5 BOTH-engine PROVEN. NO proxy"
  compute    = "the COMPUTE WALL is the n≤8 stdlib ceiling itself (faithful_phi panics at n=9), NOT a super-exponential blowup: the substrate-Φ measure here is the CHEAP MI-matrix MIP-EI (2^(n-1)≤128 bipartitions of an n×n MI matrix), NOT the super-exponential per-mechanism big_phi that capped H_1012 at n=6. n=8 reached EXACT in 7.5s; n=9+ not faked — it would panic (a_scale_honest_scope). Whole run wall=67.3s, $0 CPU 0-pod"
  ladder     = "n-LADDER (peak-γ + Cohen-d vs sub/super): n=6 → γ★=1.0, d_sub=+9.19, d_super=+9.29 (reproduces H_1158 EXACTLY: φ=0.187173) → HELD | n=7 → γ★=1.0, d_sub=+12.82, d_super=+12.95 (φ=0.172807) → HELD | n=8 → γ★=1.0, d_sub=+10.98, d_super=+11.15 (φ=0.178749) → HELD"
  F1_interior= "interior argmax at EVERY n>6 — peak at γ=1.00 (NOT an endpoint) at n=7 AND n=8 ✅ (inverted-U, not monotone, peak does NOT shift)"
  F2_vs_sub  = "φ(peak)−φ(γ=0) Cohen's d = +12.82 (n=7), +10.98 (n=8) ≥ 0.8 ✅"
  F3_vs_super= "φ(peak)−φ(γ=2.5) Cohen's d = +12.95 (n=7), +11.15 (n=8) ≥ 0.8 ✅ (γ=2.5 fully-locked → φ=0.0 EXACTLY at every n)"
  result     = "🟢 PEAK-HOLDS — at every tractable n>6 the φ_EI(γ) curve has an INTERIOR argmax at γ★=1.0 AND d≥0.8 vs BOTH sub and super. The inverted-U RECURS unchanged: φ(γ) = ~0.002 (γ0) → 0.024 → 0.087 → 0.155 → ★0.179 (γ=1.0)★ → 0.029 (γ1.6) → 0.000 (γ2.5) at n=8, same SHAPE as the H_1158 n=6 curve. ALL THREE frozen gates pass at n=7 AND n=8"
  finding    = "the faithful IIT-4.0 peak-at-criticality is n-ROBUST, NOT an n=6 small-system artifact: the A⇄G coupling sweet spot (γ=1.0) remains the consciousness-measure MAXIMUM as the system grows 6→7→8 channels, with the inverted-U sharpening (d climbs to ~13 at n=7). Too-little coupling → independent channels (φ→0), too-much → locked channels (φ→0 EXACTLY), the critical sweet spot between = maximal integrated information — at all three reached n. H_1158's single-rung result PROMOTES from a toy n=6 point to a 3-rung ladder curve (a_scale_honest_scope satisfied within the n≤8 exact-MIP ceiling)"
  synthesis  = "STRENGTHENS the H_1153+H_931+H_1158 triangle: the anima A⇄G substrate sits at σ≈1 branching criticality at Ψ=1/2 (H_1153 🟢) where its faithful consciousness measure is MAXIMAL (H_1158 🟢) — and that φ-maximum-at-criticality is now shown n-INVARIANT across the full exact-MIP n-ladder (H_1165 🟢). The criticality↔consciousness coincidence is a scale-stable property of the engine, not a single-n coincidence"
  honest     = "toy A⊥G substrate per H_1119; n≤8 = the EXACT MIP-EI stdlib ceiling (the honest compute wall — n=9+ PANICS, NOT faked). γ_x is the toy coupling knob mapped to (not identical to) the H_1153 network branching ratio. LIVE-CORE engine, n>8 (would need the greedy-approx large-N path, a different measure), and real production scale all UNVERIFIED (a_scale_honest_scope). φ measured (faithful MIP-EI), NOT fabricated, NO perplexity (p7). mirror PROVEN ≡ stdlib at n=4,5 (both engines) + LIVE hexa n=6,7,8 BEFORE scoring (a_phi_iit4_tool)"
  harness    = "UNIVERSE/h1165_phi_n_ladder.py · UNIVERSE/h1165_ref_faithful_n78.hexa · .verdicts/1165_phi_n_ladder/H_1165.txt"
  xref       = "1158_phi_at_criticality (direct n-ladder extension, substrate+sweep reuse) · 1153_criticality_branching · 931_self_organized_criticality · h1119_dream_phi (substrate+mirror) · h1012_bigphi_faithful_larger_n (prove_mirrors_at_n) · h1004_bigphi_faithful_clean (faithful_phi mirror) · a_phi_iit4_tool · a_scale_honest_scope · p7 · g5"
  target     = "🟢 SUPPORTED-NUMERICAL → LANDED 🟢 (peak γ=1.0 HELD at n=6/7/8, d 9.2–13.0 vs sub/super, mirror PROVEN n=4..8, $0 CPU 0-pod wall 67.3s)"
