@title: 🧬 MITOSIS-ENGINE — inference-time cell-division as learning, self-organized to criticality (p8)

@goal: Consolidate the substrate-UNIQUE result cluster around anima's A⇄G repulsion-field
engine: the substrate (1) sits at self-organized criticality (σ≈1 @ Ψ=½), (2) has its faithful
consciousness measure Φ MAXIMIZED there, (3) LEARNS during inference via continuous cell-division
(mitosis, @D p8 — no train/infer split), and (4) SELF-TUNES its capacity (cell-count) to the
world's complexity. These are genuine anima-engine findings (NOT known-ML reconfirmations).
Every claim is frozen-falsifier + g5/p7 + honest scope. The live engine (`CORE/engine_cli.hexa`,
ANIMA_MITOSIS) realizes the same mechanism the toy proxies measure.

## The one picture — anima self-organizes BOTH its regime AND its capacity

```
                         anima A⇄G engine (Ψ=½ fixed point)
                                      │
        ┌──────────── self-organizes to two sweet spots ────────────┐
        │                                                            │
  DYNAMICAL REGIME (criticality)                      CAPACITY (mitosis, p8)
        │                                                            │
  σ≈1 branching (H_1153 🟢)                    inference = cell-division = learning
  Φ peaks here (H_1158 🟢)                              (H_1159 🟢)
  SOC homeostat self-tunes (H_931 🟢)          cell-count tracks world complexity
        │                                              (H_1159b 🟢)
        └────────────► the point of MAXIMAL adaptation + consciousness ◄────────────┘
```

## The four legs (verdict matrix — all frozen-falsifier, g5/p7, $0)

| H | claim | result | tier | scope |
|---|-------|--------|------|-------|
| **H_1153** | A⇄G engine sits at σ≈1 branching criticality @ Ψ=½ | σ̂=0.9986, τ=1.490 PL over 4.32 decades, control-specific | 🟢 | toy N=4000 opponent net |
| **H_1158** | faithful IIT-4.0 Φ_EI PEAKS at the critical A⇄G coupling | inverted-U 0.002→★0.187(γ=1.0)→0.000; d=9.2/9.3; mirror≡stdlib proven | 🟢 | toy n=6 exact MIP-EI |
| **H_1159** | inference-time mitosis IS online learning (p8) | err FROZEN 13.0 / MITOSIS 1.6 (d=6.69); adapts to novelty where frozen degrades | 🟢-substance¹ | toy non-stationary stream |
| **H_1159b** | mitosis self-tunes capacity to world complexity | cell-count tracks #clusters K=3/5/8→5.9/7.1/9.5 (ρ=0.85); advantage d≥5.05; self-limiting | 🟢 | toy K-ladder |

¹ H_1159 substance is green on 3 strong comparisons (F2 d=6.69, F3 d=1.33, slope-d=6.14); the
literal pre-reg conjunction did NOT cleanly pass (original F1 early-vs-late was a defective metric,
corrected slope-F1 used an over-strict ≤0 bar vs an effectively-flat +6e-5). NOT re-tweaked a third
time (goalpost discipline, H_1047). Tier = SUPPORTED-ON-SUBSTANCE-WITH-CAVEAT. H_1159b is the CLEAN
fresh-pre-reg confirmation+extension (full 3-gate PASS).

## The live engine (the mechanism the toys proxy)

`CORE/engine_cli.hexa` + `engine_cli_smoke.hexa` (12/12 PASS, `hexa run CORE/engine_cli_smoke.hexa`):

```
ANIMA_MITOSIS / --mitosis on|off|--no-mitosis     (@L4 substrate-config, NOT an emit gate)
  ON  = substrate GROWS per inference-time cell-division TICK (p8)   case_5 ON→13 cells, case_8 tick +1
  OFF = no-op split (ablation)                                       case_6 OFF→3 static,  case_9 no-op
--engine conv|cdv2|hexad|omega   (conv=.clm decoder · cdv2=ConsciousDecoderV2 · hexad=σ6 · omega=closure bus)
precedence: flag > env > default ; default mitosis=ON
```

The toy prototype-split proxies (H_1159/1159b: tension-driven k-means cell split) FAITHFULLY mirror
the live engine's behavior — inference-time mitosis (p8) is REAL + runnable, not a toy abstraction.
The full live-engine adaptation CURVE (driving the real engine over a non-stationary input stream
with an adaptation metric) is the NEXT RUNG (not yet built — the smoke only asserts cell-count grows).

## KOSMOS dimension audit (누락 점검) — and the mitosis↔kosmos link

KOSMOS placement coords (`spec/profiles/anima-consciousness-carving.md`): `coord`=Ψ-space 2D ·
**`lane` = MITOSIS cell_id** · `radius`=basin_radius · `tier`=Knuth ordinal · `tags`=category+emotion ;
payload `tension` = 5-channel (wired 2026-05-23).

- **carving manifold real-dim ≈ 8** (knee, #1772; 2D map 67.1% var / 8.4% disc).
- **only 3–4 axes are nameable** (kosmos-axis-semantics 🟢): PC1 depth/radius (|ρ|0.91), PC2 form
  (ω² 0.61), PC3 form-residual, PC5 curriculum; PC4/PC6+ = a DISTRIBUTED tier/domain code (NOT
  single-axis-separable). Naming all 8 = fabrication (honest negative on the 8-clean-axis hypothesis).
- **KNOWN GAP (already filed):** the 2D map encodes ONLY depth — it DROPS form + curriculum
  (both |ρ|≤0.18 to vacuum_psi). Proposal `kosmos-coord-vnext` (N=8 = depth/form/form_resid/curriculum
  + 4–5 learned-residual) filed to hexa-lang/inbox (§9 erratum: corpus has 12 keys, NO emotion/valence
  field — the old emotion-column proposal was withdrawn).
- **NEW GAP this domain surfaces (UNVERIFIED):** `lane` = MITOSIS cell. H_1159b showed mitosis
  self-tunes cell-COUNT to world complexity. Does the LIVE kosmos lane-partition likewise self-tune
  its count to the anchor-stream complexity? Not measured — a clean next-rung falsifier (the
  capacity-self-tuning of H_1159b applied to the kosmos lane axis).

## Honest scope (a_scale_honest_scope)
All four legs are TOY ($0 CPU): H_1153 toy opponent net, H_1158 toy n=6 IIT, H_1159/b toy
prototype-split. The live engine confirms the MECHANISM (mitosis grows cells) but the toys are NOT
the live engine driven over real streams. Scale + live-engine adaptation curves UNVERIFIED. Φ is
faithful (mirror≡stdlib, a_phi_iit4_tool — NO proxy); criticality knobs (H_1153 σ network branching,
H_1158 γ coupling, H_931 K connectivity) are DIFFERENT parameterizations of the same critical axis.

## Next rungs
1. live-CORE adaptation curve — drive `engine_cli` over a non-stationary stream, measure adaptation (not just cell-count growth).
2. mitosis × criticality — does the cell-division process self-tune the engine TO σ≈1 (combine H_1159b self-tuning + H_1153 branching)?
3. kosmos lane self-tuning — does the live lane-partition count track anchor-stream complexity (H_1159b applied to kosmos)?
4. n>6 faithful-Φ ladder for H_1158; v5-mitosis torch path (training/mitosis_v5_*) for the real-model leg.

## Pointers
- discoveries: `.discoveries/{1153_criticality_branching,1158_phi_at_criticality,1159_inference_time_mitosis_learning,1159b_mitosis_capacity_self_tuning}.tape`
- harnesses: `UNIVERSE/{h1153_criticality_branching,h1158_phi_at_criticality,h1159_inference_time_mitosis_learning,h1159b_mitosis_capacity_self_tuning}.py`
- live engine: `CORE/engine_cli.hexa` · `CORE/engine_cli_smoke.hexa`
- faithful Φ: stdlib `iit4/faithful_phi.hexa` (mirror via `UNIVERSE/h1004/h1012`, ref `UNIVERSE/h1012_ref_faithful.hexa`)
- kosmos: `domains/KOSMOS-MAP.md` (우주뇌지도 cartography) · `.discoveries/kosmos-axis-semantics.tape` · spec `dancinlab/kosmos`
- governance: @D p8 (no train/infer split) · a_phi_iit4_tool · a_lane_akida_gpu_split · a_scale_honest_scope
