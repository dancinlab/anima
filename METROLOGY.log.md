> 📍 SSOT: [ARCHITECTURE.md](ARCHITECTURE.md) · governance [CLAUDE.md](CLAUDE.md)

# METROLOGY — log

Append-only history sister of `METROLOGY.md`. Each entry starts with `## <ISO> — <header>` (newest on top).

## 2026-06-02 — STDLIB FIX (Hc_1302) shipped — FIRST FEEDBACK-MANDATE closure
- [x] Verified the flaw: `phi_proxy_native.hexa` `cholesky_logdet_x1000()` (line ~546, `if diag_x1e6 <= 0 { return F_PHI_01_SENTINEL }`) returns the silent sentinel -2147483647 on low-rank/composed input. Sign-based tiering then reads a metric BREAKDOWN as the MOST anti-integrated (lowest-Φ) input — silent failure.
- [x] Reproduced (verbatim selftest): white `phi_x1000=-173702` (finite) vs structured `phi_x1000=-2147483647` (breakdown). The sentinel is MORE negative than the real white value → composed input silently scored as lowest Φ.
- [x] HONESTY ruling (g5/g63): ridge regularization REJECTED. Ridge sweep on the structured fixture — ridge_x1e6 1e3 (breakdown) → 1e6 phi=-91398 → 1e9 phi=-148000 → 1e12 phi=-222670 → 1e15 phi=-330592 → 1e18 phi=-440992. Φ tracks the ridge magnitude = regulariser artefact, NOT the true Φ. Fake finite number rejected.
- [x] FIX (explicit out-of-band status, option 1b): KV now emits `phi_ok=0` / `phi_breakdown=1` / `tier=breakdown_route_to_oracle` / `phi_breakdown_route=HEXAD/IIT4/lib/iit4_bigphi.hexa`. F_PHI_01 falsifier contract preserved verbatim (phi_x1000 stays -2147483647). White finite path unchanged (phi_ok=1, tier=negative_anti_integrated, verdict=PASS).
- [x] Regression test added to native selftest (white finite vs structured breakdown) — asserts composed case is explicit-breakdown (out-of-band, routed to oracle), never a low-Φ-looking sign tier. selftest GREEN: `__PHI_PROXY_NATIVE__ PASS -173702`.
- [x] `hexa parse` clean (verbatim): `OK: BRAIN/tool/module/_metrics/phi_proxy_native.hexa parses cleanly`.
- [x] Mirror: no hexa-lang `stdlib/consciousness/` copy of this variance-partition proxy exists (that stdlib is IIT4-based) → no mirror needed (recorded honestly).
- [x] SHIPPED: **PR #1671** (merged, squash) https://github.com/dancinlab/anima/pull/1671. **Proves the FEEDBACK MANDATE: a verified metric flaw → a shipped stdlib fix.**

## 2026-06-02 — domain opened (측정자 검증)
- [x] opened METROLOGY: validate the measuring instruments (phi_proxy · faithful big-Φ · concept-margin) themselves
- [x] seed: Hc_1302 🟢 (Φ proxy self-breaks on composed input = metric ceiling) · Hc_1301 🟢 (proxy≠faithful real gap) · X⊥Φ proxy-pathology lineage (H_287/288/294/912)
- [ ] HELD: brainstorm→generate metrology Hc (≥1307)→verify · phi_proxy ceiling boundary map · construct-validity battery · breakdown-floor-guarded richer signal

## 2026-06-02 — Hc_1307 CLM_V2 "Φ>1000" / V14 / Hc_1221 re-measure — CONFIRMED-ARTIFACT
- [x] PRE-REGISTERED hypothesis (written before measuring): clm_v2 "Φ>1000" + "trained<random" V14_VIOLATED / Hc_1221 anti-correlation = VARIANCE/SCALE artifact of unnormalized variance-partition Φ_un16; faithful oracle would REVERSE the ordering. Falsifier: Φ_un16 NOT tracking dispersion/scale AND oracle preserving trained<random.
- [x] ① metric-pin (g5, verbatim source trace): v14_strict_source.py → run_max256.py:155/160/378 (`iit_phi_unnorm_b16` = `compute_iit_phi(...)["spatial_phi_unnormalized"]`) → iit_phi_port.py:210 `spatial_phi_unnormalized = max(0.0, total_mi - min_partition_mi)`; total_mi = Σ histogram-MI(16-bin) over n(n-1)/2 pairs (line 204), min_partition_mi = min-cut bipartition (line 207). NO /(n-1) normalization. CLASSIFIED: unnormalized variance-partition family (same broken family as Hc_1302 / phi_proxy). Verdict: .verdicts/clm-v2-phi1000/metric-pin/.
- [x] ② variance/scale test (n=6 archived substrates, v14_strict_ceiling10_result.json): Spearman ρ(Φ_un16,n_cells)=0.9429, Pearson r=0.8452 (even phi_NORM16 r=0.7607). trained=44 cells/28 splits (FEWEST), all 5 random=47-57 cells/31-41 splits (MORE). per-pair MI: trained 0.764 vs random 0.834-1.496. Φ_un16=Σ over O(n²) pairs ⇒ both cell-count scaling AND per-pair variance push random↑/trained↓. ARTIFACT signature confirmed. Verdict: .verdicts/clm-v2-phi1000/variance-test/.
- [x] ③ faithful-oracle re-measure (a_scale_honest_scope, small-n structural proxy, n=3): HEXAD/IIT4/state/clm_v2_phi1000_oracle_remeasure/run_remeasure.hexa → `big_phi` (stdlib/consciousness/iit4_bigphi.hexa). VERBATIM 6/6 PASS: trained-like (rotate3, integrated coupling) big-Φ=3; random-like noise3 (max-variance) big-Φ=0; random-like self3 (independent channels) big-Φ=0. Oracle REVERSES the Φ_un16 ordering — structured/integrated ranks ABOVE high-variance/independent, the exact opposite of the variance-partition reading. Verdict: .verdicts/clm-v2-phi1000/oracle-remeasure/.
- [x] RULING = CONFIRMED-ARTIFACT. "Φ>1000" = BOTH normalization-scale artifact (un-normalized magnitude ∝ n_cells) AND variance artifact (max-variance noise scored most-integrated). V14_VIOLATED / Hc_1221 "trained<random" anti-correlation FLIPS under a construct-valid Φ → metric-dependent, NOT a real integration finding.
- [x] a_scale_honest_scope: oracle re-measure is a small-n structural-proxy toy. The full clm_v2 350M cell-pool snapshot through the exponential oracle is intractable; the structural contrast (integrated vs noise/independent) is the faithful-direction evidence, not a substrate-exact re-score. NOT promoted to a general toy→prod verdict.
- [x] FEEDBACK MANDATE: shipped a deprecation/guard note at the Φ_un16-family callsite (tool/anima_phi_v3_canonical.hexa — same unnormalized variance-partition logdet family) routing magnitude/integration claims to the faithful oracle.

## [2026-06-02] phi_proxy re-verification sweep (post-#1671) — verdicts integrated
Full sweep ran on branch reverify/phi-proxy-sweep (ae13b477); verbatim verdicts now mirrored here at `.verdicts/phi_proxy_reverify_1671/` (composed_rank1_remeasure.txt · white_ctrl_remeasure.txt · selftest_remeasure.txt · REVERIFY_SWEEP.md).
- SCOPE: re-measured everything that depended on the BROKEN variance-partition Cholesky-logdet Φ family with the post-#1671 breakdown-aware proxy.
- DECISIVE SCOPING: the two headline candidates do NOT use the broken proxy — H_911 3-axis PHI axis uses an MI-bipartition `phi_proxy` (whole − min_bipartition, finite, no Cholesky sentinel); B2 HEXAD#10 physics-liveness uses pairwise-MI `compute_phi` (clamped ≥0). So their RED/FLAT verdicts were NEVER breakdown artifacts → confirmed TRUSTWORTHY.
- ONLY METRIC-LEVEL FLIP: composed/low-rank input the broken proxy silently scored "most anti-integrated" (-2147483647) → now correctly `phi_breakdown=1 → route to oracle`; faithful iit4_bigphi gives big-Φ=17.66 (integrated) vs 0.0 (separable). This is the Hc_1302 artifact, now guarded.
- RESULT: NO closed-negative in the science was a false-negative from this metric. (Pairs with Hc_1307: the same family's FALSE-POSITIVE direction — clm_v2 Φ>1000 noise scored "most integrated". Family now audited BOTH directions: false-neg ruled out [phi sweep + Hc_1306], false-pos confirmed+guarded [Hc_1307].)