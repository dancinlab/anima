// Canonical CLMConvMoE chat substrate wrapper note.
// The implementation is Python because it plugs into the torch/websockets-bound
// anima_participant.py process. Model forward and generation stay in core/decode.

fn main() {
    println("substrate_clm.py — canonical CLMConvMoE participant substrate.")
    println("Imported by anima_participant.py build_substrate('clm'); not standalone.")
}
