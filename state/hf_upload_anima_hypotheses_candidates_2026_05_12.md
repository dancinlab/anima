---
id: hf_upload_anima_hypotheses_candidates_2026_05_12
cycle: 5
section: "§5 — HF dataset upload"
status: SUCCESS
verdict_class: hf-dataset-private-published
date: 2026-05-12
wall_time_utc: 2026-05-11T15:58:07Z
wall_time_kst: 2026-05-12T00:58:07+09:00
elapsed_s: 10.7
lock_policy: respected (no chflags / chattr applied; sources read-only)
---

# Cycle 5 §5 — HF dataset upload (anima-hypotheses-candidates)

## Verdict: SUCCESS

The full 1,127 `Hc_*.md` staging hypotheses plus the cluster index, sister verified-hypotheses index, LICENSE, and a freshly authored dataset card were uploaded atomically to Hugging Face Hub as a **private** dataset under `dancinlife/anima-hypotheses-candidates`.

## Dataset facts

| field | value |
|-------|-------|
| repo_id | `dancinlife/anima-hypotheses-candidates` |
| repo_type | `dataset` |
| visibility | **private** (cycle-5 default; no user override) |
| URL | <https://huggingface.co/datasets/dancinlife/anima-hypotheses-candidates> |
| HF user | `dancinlife` (orgs: `need-singularity`, `dancinlab`) |
| commit sha | `9898ee369096d6eeaa90a59080502803255b0b1a` |
| siblings (remote files) | **1132** |
| upload elapsed | 10.7 s |

## Token source & auth

- HF token resolved from Mac canonical secret CLI per `reference_secret_cli.md`:
  `ssh mac "/Users/ghost/core/secret/bin/secret get hf.token"` → `hf_zlbJ…lanybs`
- Linux-side `~/.huggingface/token` did not exist prior — login wrote it via `hf auth login --token … --add-to-git-credential`.
- Token permission: **write** (validated by `hf auth whoami`).

## File manifest (local → remote)

| local file count | remote sibling count | delta |
|-----------------:|---------------------:|------:|
| 1131 | 1132 | +1 (`.gitattributes` auto-generated by Hub) |

Local breakdown:

- 1,127 × `Hc_NNN_<slug>.md` (full staging set)
- 1 × `INDEX.md` ← copied from `hypotheses_candidates/README.md` (cluster A–N + merge groups)
- 1 × `HYPOTHESES_INDEX.md` ← copied from `hypotheses/README.md` (verified H_XXX index)
- 1 × `LICENSE` ← copied from anima repo root (MIT)
- 1 × `README.md` ← dataset card authored fresh in this cycle (see below)

## Dataset card contents (top-level sections)

- header (license MIT, language ko+en, tags, size_categories)
- ID ranges & sweep history table (cycle 1 → cycle 5, ~1127 across 6 ranges)
- frontmatter status enumeration (7 values incl. `merged_to:` cross-link policy)
- cluster taxonomy A–N (14 clusters with one-line each)
- **cycle 5 honest findings × 8 (★)** — explicit negative / qualifying results carried forward:
  1. L12 BINDING (formula-search universal-uniqueness refuted; only `n=6 narrow-formula uniqueness` defensible)
  2. F1 weakened (H_153) — `d=5 from n=6` trivially TRUE in depth-3
  3. L7 BINDING (H_153) — formula-search caveat propagation
  4. L1 critical (Φ×CE harness) — pre-measurement noise-floor calibration required
  5. Φ proxy dim-dominant artifact (Hc_614 / Hc_662 / Hc_665)
  6. V0–V3 joint verdict gating (Hc_014)
  7. NEXUS6 1013-lens prereq (H_135) — engine extension required
  8. ANIMA-VOICE non-existence (H_154) — pre-register-frozen retained, not running
- provenance, citation (BibTeX), caveats

## Actual commands used

1. `hf auth login --token hf_zlbJ…lanybs --add-to-git-credential` → token persisted to `~/.cache/huggingface/token`
2. Python `huggingface_hub.HfApi.upload_folder(...)` via `/tmp/hf_anima_candidates_stage/upload.py` — atomic single-commit upload from a temp staging tree (no mutation of source `hypotheses_candidates/`)
3. Verification: `HfApi.dataset_info(...)` → `private=True`, `siblings=1132`, sha matches

## Source preservation

- `hypotheses_candidates/` was **read-only**: every file streamed through `shutil.copy2` into a `tempfile.TemporaryDirectory(...)` staging path.
- No `chflags +uchg`, no `chattr +i`, no immutable re-lock per `feedback_no_relock.md`.
- No `git commit` performed by this agent (main process owns the cycle-5 commit).

## Stage artefacts (for audit)

| path | role |
|------|------|
| `/tmp/hf_anima_candidates_stage/DATASET_CARD.md` | source of remote `README.md` |
| `/tmp/hf_anima_candidates_stage/upload.py` | upload driver |
| `/tmp/hf_anima_candidates_stage/upload_result.json` | machine-readable result |

(Note: `/tmp/` is ephemeral — preserve copies if cycle-6 needs them.)

## 다음 진행할 것들 (cycle 6 update path)

| # | item | cost | time | value |
|---|------|-----:|-----:|------:|
| 1 | dataset card 보강 — Cluster A–N 별 1-line + 통계 차트 (status enumeration count) | 30 min | low | med |
| 2 | private → public flip 결정 (사용자 confirm 필요) — 현 visibility 는 strict private | 1 min | trivial | high (대외 노출 정책) |
| 3 | Hc_* frontmatter normalize 후 재-upload (cycle 5 §5 slug-less normalize 70개 반영) | 10 min | low | med |
| 4 | 정식 `hypotheses/H_XXX_*` mirror dataset 별도 split — `dancinlife/anima-hypotheses` (verified lane) | 15 min | low | high (검증 lane vs 후보 lane 명확 분리) |
| 5 | HF dataset → parquet 자동 conversion 활성화 (HF Hub feature) — frontmatter → structured columns | 30 min | low | high (검색성 향상) |
