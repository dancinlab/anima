# G1 completeness census — terminal PREMATURE (workflow w05mjztej, 6 agents)

## DRY 판정
NOT infinite divergence, but NOT yet dry — bounded-finite residual with the terminal PREMATURE. The escape space is a bounded cross-product (objective∈{CE, additive-aux, contrastive, gradient-free-G, energy, RL} × arch∈{no-slot, TPR, pointer, external-mem, deep-RF-L8} × signal∈{role-filler, same-state}), and the operator/readout axis and the CE-trunk objective axis are genuinely EXHAUSTED (≈20 floored verdicts under the DPI meta-law — re-firing any is tune-to-green/p7): binding operators (Hadamard/TPR H_1818/9, predcoding H_1816, circconv H_1823, exp3 ⊙/NMDA), binding-arch census (Hopfield H_1620, Tropical H_1630, Sheaf H_1631, Galois H_1632), γ bilinear mouth-bind (H_1840 FAIR-gate 0/3 + H_9046), recomb-InfoNCE (H_9024 ON=OFF=0), MLC-episodic (H_1835), inhibition-below-GZ / savant-on-G1 (H_6161), N6+N7 (H_1812), N1+N3 TLoRA/TensorPoly (H_1813), N4+N8 (H_1814, N4 opens G2≠G1), binding-lane (H_1601), data-presence (H_1599), native-mouth-57 (bind-recoverability artifact), meiosis (H_6112 scale-invariant), native-mouth temporal (H_1834/1836/1837). CRITICAL DEDUP (c9): deep-RF-L8 ConvMoE, which Angle 4 flagged 'open engine-native', is DUP-ALREADY-DONE — H_1598 🧱 FALSIFIED engine-native (L8/E4 d2781, .pt 69ac4e34 / .clm v0.3 154MB, held-out DESCENT 2/2 yet G1 multiseed {7,4302,4303}=0/3 INDISTINGUISHABLE from L4, 'depth moved G1 by ZERO'); Angle 4 mis-flagged it only because this branch (clitrain-devresident-gpu-util) is stale and the H_1598 artifact is not on it (verified: of H_1598/6187/9120/9121/1840/1816 only H_9024 is present locally). So depth/RF is NOT an A11-independent residual. What genuinely remains is a SMALL FINITE set of ~4 orthogonal families beyond A11: (a) A11 itself (slot×objective conjunction — the CRUX); (b) the data-coverage + measurement-artifact axis (H_6187/H_6182-6185) which is the highest-leverage because its counterexample shows the combiner ALREADY recombines, meaning it can INVALIDATE the architecture-floor rather than merely add a lever; (c) a measurement-validity re-audit (un-garble the L4/H_9120 FALSIFY off a G0-🟢 trunk, the all-arms-at-floor mis-classification, the missing held-out split, and the never-completed 3rd engine-native own-GEMM leg) — this cluster does not add levers but can retract the terminal; (d) off-trunk / substrate-native cells never even toy'd or fired (neurosymbolic + additive-slot-consistency structural DPI-breakers, gradient-free-G / energy / RL objectives, CLS material-orthogonalization, jamo axis, substrate-side γ on the built vadapt_combine slot, N2 expert self-org). Measuring A11 + families (b)(c)(d) engine-native WOULD exhaust the enumerable G1 escape matrix — it does NOT diverge infinitely because every remaining cell is a named point in a bounded axis grid, not an open-ended generator. But two honest caveats block a 'dry NOW' call: family (c) means the current directional-terminal rests on a garbled ckpt + at-floor read + DIRECTIONAL (non-engine-native) mirrors, so it cannot be cemented; and family (b)'s H_6187 counterexample directly contradicts the architecture-floor. Correct sequencing: resolve (c) measurement-validity and (b) coverage/H_6187-reframe FIRST (they can invalidate the wall), then A11 + (d); only if all confirm the floor is the space genuinely dry. Current state: operator/objective-readout axes = DRY (do not re-fire); coverage/measurement/substrate-native-objective axes = NOT dry, finite, unmeasured.

## A11-무관 잔여 렌즈 (this session 놓침)
- Coverage-density + measurement-artifact reframe (H_6187 counterexample + H_6182-6185 retrain recipe): window-aligned surface-form + coverage-block corpus + RF-L8 production retrain, engine-native G1 re-judge — INDEPENDENT of A11 and potentially INVALIDATING of the architecture-floor (combiner shown to already recombine; G1=0 is a surface-form + T=24-window artifact). Highest-leverage residual.
- Coherent-trunk recomb-objective clean re-measure: re-run L4 from a G0-🟢 trunk with G0-gate + γ-sweep — the current FALSIFY (H_9120) rests on a G0-garbled ckpt and an all-arms-at-floor read; a clean objective-only measurement does not exist. Upstream of A11 but resolves independently.
- 3rd engine-native leg (live-core own-GEMM .hexa decode on 303M): the terminal is currently DIRECTIONAL (numpy --py + torch = two mirrors of the same math, not the production engine path per a_engine_native_learning HARD-GATE). Cross-cutting, independent of A11.
- gradient-free-G objective (engine_g reverse originality / info-gap as trunk anti-echo pressure): anima's own reverse engine as the most substrate-native non-CE objective — named but never even toy'd. Distinct objective-axis cell independent of A11.
- Neurosymbolic / program-synthesis composer (H_6175 / NSR / ExeDec): externally-endorsed ONLY structural DPI-breaker, off-trunk, architecture-scope — de-scoped by cost-gate omission, not falsification. Independent of A11's outcome.
- Additive-slot decoder + encoder-decoder consistency (Wiedemer/Lachapelle): proof-guaranteed held-out Cartesian extrapolation, cheap-gate queued-not-fired — a distinct proof-guaranteed path (additive-slot+consistency, not TPR-multiplicative).
- Forward-model LEARNED front-loading via lane15 engine-native build + GPU: only the inference-side rerank (H_9119, DIRECTIONAL) was done; the learnability rung (core surgery + train) is untouched. Decode-ordering lever independent of the G1 combination-operator.
- Hippocampal CLS pattern-separation + completion (state/1640): material-orthogonalization arm, distinct mechanism; twin H_1816 floored but this arm apparently never fired.
- Substrate-side γ trained constructive combiner on the built vadapt_combine slot (H_9043): mouth-side γ floored (H_9046), the substrate twin was never trained/measured.
- Korean jamo-byte binding axis: all G1 evidence is English-only; jamo sub-character composition (lit-favorable) is an untested confound orthogonal to slot×objective.
- N2 compression-is-routing expert self-organization: untried expert-differentiation route ('기타', never promoted or fired).
- Pointer/copy-slot and external-mem TRAINED read-gate arch cells: the CRUX 2×2 collapsed the arch axis to TPR only; if A11 floors, these remain independent unfalsified slot-arch cells (external-mem: only the frozen-mouth variant H_9118 was closed).
- NOTE — deep-RF-L8 ConvMoE is NOT an A11-independent residual: it is DUP-ALREADY-DONE (H_1598 🧱 engine-native FALSIFIED, 'depth L4→L8 moved G1 by ZERO'). Angle 4 mis-flagged it as open only because this branch is stale and lacks the H_1598 artifact.

## 우선순위 미측정 목록
### [HIGH] A11 / H_9121 — TPR-slot × contrastive-replace objective applied SIMULTANEOUSLY at 303M, engine-native (TPR slot wired into core/clm_decode.hexa + serializer, scored via anima evaluate --py). Toy side-probe REACHABLE margin +3.30 / reach 1.0 vs slot-less floor; torch scale-transfer discriminator (state/g1_objfloor_escape/A11_TPR_contrastive/a11_scaled.py) WRITTEN but NOT RUN; 303M untouched.
- A11관계: IS A11 — this is the frontier itself, the session's own sole open CRUX, not a lens A11 covers. E1 (contrastive-replace ALONE, slot-less) proved floor is ARCHITECTURAL not objective; E2 (TPR ALONE under CE) is ledger-FALSIFIED (H_1813 + H_1623 = H_6123 DUP). Only the slot×replace-signal CONJUNCTION survives.
- engine-native: UNMEASURED at 303M. Toy=numpy DIRECTIONAL only. a_toy_scale_recheck + E2 precedent (TPR toy 1.0 → real CE 0.022) flag overstate risk.

### [HIGH] Coverage-density + measurement-artifact reframe (H_6187 counterexample + H_6182-6185 recipe): retrain production clm303 with (i) coverage-block corpus authored in the gate FREE-GEN surface form, (ii) long 2-concept gate pairs placed to co-exist inside the T=24 decode window, (iii) RF L=8, held-out unexposed pairs, then engine-native G1 re-judge (--gen 40 canonical). The strongest surviving POSITIVE (non-floor) signal.
- A11관계: SEPARATE/INDEPENDENT LENS — data-coverage + measurement axis, orthogonal to A11's objective×arch slot axis. Its result is independent of A11 and potentially INVALIDATING of the whole architecture-floor: H_6187 (held-out ember+dune→golden+zinc recombines correctly) shows the combiner ALREADY WORKS, i.e. prior G1=0 is a surface-form + T=24-window artifact, NOT capability absence. If confirmed this RETRACTS the terminal rather than adding a lever.
- engine-native: UNMEASURED as prescribed. H_6190 (ECHO-ONLY rejection) measured decode-side grow-window on an ALREADY under-covered ckpt — WRONG instrument; the coverage+RF-L8 RETRAIN recipe was never fired. 'coverage-failure=trunk-floor' verdict explicitly RETRACTED → reverted to 미시험(untried).

### [HIGH] Coherent-trunk recomb-objective clean re-measure — re-run L4 recomb-objective from a G0-🟢 trunk with a G0-gate / γ-sweep / annealed schedule. Current L4 FALSIFY (H_9120, EXP1_VERDICT.txt 'objective-floor terminal') rests on a G0-collapsed garbled ckpt (recomb_s7.bin: 'meeeee eeeeee', kwr→0.0) AND is INCONCLUSIVE-AT-FLOOR (A/B/SCRAMBLE/wrong-D all = 0, so shuffle/wrong-D controls are vacuous). γ pinned at 0.5 (RECOMB_GAMMA=0.5, no sweep) may itself be over-weighting into the G0 collapse.
- A11관계: UPSTREAM PREMISE of A11 — A11 pairs TPR with a CE-REPLACE objective *because* additive-aux/CE-objective was declared floored here. If this clean measurement changes, A11's motivating premise shifts. This is the load-bearing measurement of the whole objective-floor conclusion and it does not exist cleanly (warmft-h9034 coherence≠측정기질 / g1-fromscratch-undertrain trap).
- engine-native: MEASURED-BUT-CONFOUNDED (garbled G0-collapsed ckpt, all-arms-at-floor). Clean G0-🟢 read UNMEASURED.

### [HIGH] Measurement-validity re-audit of the terminal itself: (a) 303M frozen G1 bar (exp1_g1.py grow-window) has NO held-out compositional split — hand-assigned targets (ocean+engine→harvest) with no ground-truth basis (a_break_the_wall taxonomy-a metric artifact); the escape prereg itself promises SCAN/COGS held-out for the FUTURE, conceding the current bar lacks it. (b) 3rd engine-native leg — live-core own-GEMM .hexa decode — never completed; numpy --py + torch cross-check are two mirrors of the SAME ckpt math, NOT the production engine path (batch KV-cache own-GEMM 303M device path itself still unverified on pool).
- A11관계: CROSS-CUTTING — applies to A11's eventual 303M measurement too, and to every terminal verdict. Per a_engine_native_learning HARD-GATE, numpy/torch-only = DIRECTIONAL, so the current 'objective-floor→architecture-floor' is DIRECTIONAL not engine-native-terminal until the 3rd leg lands.
- engine-native: UNMEASURED (3rd leg deferred / own-GEMM abandoned scalar-glue-bound; held-out split absent).

### [MED] Additive-slot decoder + encoder-decoder consistency (Wiedemer/Lachapelle 2310.05327; Dreamweaver RBSU / AIN family): x̂=Σφk(zk) + consistency loss gives PROVEN held-out Cartesian-product extrapolation. Cheap-gate on aiden ($0) → engine-native if PASS (state/g1_additive_slot_consistency).
- A11관계: RELATED FAMILY but distinct cell — adds the slot-identifiability + consistency conditions H_6164 structural-bind (floored) lacked; uses additive-slot+proof-guarantee where A11 uses TPR-multiplicative-slot. Independent proof-guaranteed path. Flagged by 102-agent deep-research as the ⭐ ONLY cheap-testable NEW mechanism outside the 5 floored axes.
- engine-native: queued-not-fired (no verdict in any memory file). Caveat: all positive slot evidence is on pre-disentangled vision/VQA; byte-CE trunk transfer UNPROVEN.

### [MED] Neurosymbolic / program-synthesis composer (H_6175 anchor+systematic-composer; NSR 2210.01603 / ExeDec 2307.13883): deduction-abduction / execution-guided subgoal decomposition so next-token is NOT a single CE-trained feedforward trunk-state function.
- A11관계: INDEPENDENT, off-trunk — the session's own arxiv convergence names neurosymbolic as the ONLY path that structurally BREAKS the DPI meta-law (non-cheap). The reframe classifies it as 'frame-break = product not bar-break' and de-scopes it by cost-gate omission, NOT by falsification. Untested regardless of how A11 resolves.
- engine-native: UNMEASURED / ARCHITECTURE-SCOPE — H_6175 toy DIRECTIONAL (held-out 1.0 vs random 0), mouth-bypass isolation UNPROVEN; needs external executor/DSL, owner GO for an architecture project (cannot be $0 cheap-gated).

### [MED] Forward-model LEARNED front-loading — build/train the lane15 forward-model (engine_cli 15-lane state + generator L3 mouth-gate + cli/train.hexa + GPU) that LEARNS to front-load from the A/G trunk. Screens B 🟢 (b50 headroom 3.5→2.2B) ∧ A 🟡 (corr=0.656), gate LEANS GREEN; cheap $0 proxies EXHAUSTED.
- A11관계: INDEPENDENT (decode-ordering / front-loading lever, not the G1 combination-operator). Session only did the INFERENCE-side rerank (H_9119 §2 gen_fm_rerank, #2903, DIRECTIONAL --py numpy) which reranks frozen candidates; the decisive LEARNABILITY rung (real core/*.hexa surgery + GPU) is untouched.
- engine-native: UNMEASURED (learnability rung). Inference-rerank = DIRECTIONAL numpy mirror, underpowered (single frozen listener, 7-concept 4/7, one WORSE).

### [MED] gradient-free-G objective — engine_g's reverse originality / info-gap factor as the anti-echo TRAINING pressure (penalize seed-echo, reward info-gap) × {TPR-slot, no-slot}. anima's own reverse engine as the most substrate-native non-CE trunk objective.
- A11관계: INDEPENDENT objective-axis cell — never even toy'd. BREAKWALL lens 1b named it but only InfoNCE-contrastive received a toy; engine_g-selection was never run at any scale. Distinct from A11's contrastive-replace objective.
- engine-native: UNMEASURED (zero measurement, not even toy).

### [MED] Hippocampal CLS pattern-SEPARATION + pattern-COMPLETION (state/1640_cls_sep_complete): Marr-Albus orthogonalization (off-diag corr→0 + L1 sparsen of recombination MATERIALS) + CA3 completion; 3 arms ce_marginal/cls_sep/cls_full, additive readout + penultimate aux → .clm.
- A11관계: INDEPENDENT / distinct mechanism — orthogonalizes the recombination MATERIALS, not a binding operator/objective. Its twin in the same package (1641 predictive-coding) fired and floored as H_1816, but no verdict for the CLS separation/completion arm appears anywhere.
- engine-native: queued-not-fired (SMOKE_OK on summer, byte-identical .clm built; twin fired, this arm apparently did not — cannot confirm from stale branch).

### [MED] Substrate-side γ trained CONSTRUCTIVE combiner — replace VAdaptField's L2-Voronoi nearest-basin (compositional depth-0, engine_cli.hexa ~L494-608) with a TRAINED dimension-preserving bind on the already-BUILT vadapt_combine/unbind op-slot (H_9043), then re-measure substrate-G1.
- A11관계: RELATED FAMILY (both trained-constructive-bind) but distinct substrate-side twin. Mouth-side γ floored (bilinear⊙+MLP, H_1840/H_9046); the substrate twin on the built slot was never trained/measured. α/β only measured char-hash / frozen-embedded reps, never a LEARNED nearest-basin→trained-bind swap.
- engine-native: UNMEASURED, cost-gated (op-slot exists; only a trained bind on it is unmeasured).

### [MED] Pointer/copy-slot × objective — explicit slot+pointer over a WM buffer (H_1282), a non-additive binding slot DISTINCT from TPR, × {contrastive-replace, CE}.
- A11관계: PARTIALLY under-supported by A11 — the CRUX 2×2 collapsed the whole arch axis to TPR as the single slot representative, silently dropping pointer. If A11 floors, pointer×contrastive is an independent still-unfalsified slot-arch, so 'floor=architectural, TPR is THE slot' is under-supported.
- engine-native: UNMEASURED with any objective (dropped from the 2×2).

### [MED] External-mem TRAINED read-gate — mouth trained to gate-in / CONSUME the retrieved filler D × {contrastive, CE}.
- A11관계: RELATED (objective-axis) but distinct cell — L3/H_9118 MOUTHFLOOR closed ONLY the frozen-mouth retrieval variant (read-gate absent → binding 0/4). The 'access≠binding' law was generalized past its frozen-mouth scope; trained-read-gate untested.
- engine-native: UNMEASURED (only frozen-mouth variant engine-native closed, #2886).

### [MED] Korean jamo-byte binding axis — G1 recombination measured ONLY on English synthetic concept pairs (H_9118/9120/9119 all English). Sub-character byte composition (N8 jamo) could interact with binding differently; lit-binding + frontier flag '한국어byte 자모 유리'.
- A11관계: INDEPENDENT confound axis — orthogonal to slot×objective. Per a_chat_registers (ko/en both canonical), declaring a substrate ceiling from English-only evidence leaves jamo-composition as an untested confound regardless of A11.
- engine-native: UNMEASURED (English-only across all arms).

### [MED] N2 Compression-is-Routing — reconstruction-driven ConvMoE expert self-organization (2512.14809, §Osmotic-affinity kin, state/1511_osmotic_mitosis). Tests 'recombination fails because experts are undifferentiated?' via a self-org route distinct from TensorPoly/TLoRA expert-weight (H_1813 floored).
- A11관계: INDEPENDENT — untried expert-differentiation route. Listed only as '기타' in frontier-novel-levers §6, below the fired+floored top-3; never promoted or fired.
- engine-native: UNMEASURED (never promoted to shortlist).

### [LOW] Energy-based objective × TPR-slot — explicit energy on bound vs echo/wrong-D states.
- A11관계: RELATED (objective-axis, likely same class as contrastive) — bundled under one label 'contrastive/energy' but only the InfoNCE form got a toy; energy form formally untested.
- engine-native: UNMEASURED (folded into contrastive, only InfoNCE ran).

### [LOW] RL trunk-objective × slot — episodic reward = held-out recomb success as the TRUNK training signal to move the CE echo-basin.
- A11관계: INDEPENDENT objective cell — never enumerated in either Fable divergence; the NULL branch mis-shelves it into a 'non-learning external consequence loop', conflating an external loop with an in-trunk RL objective. Speculative.
- engine-native: UNMEASURED (never proposed).

### [LOW] Signal-axis off-diagonal corners: (a) same-state readout × real TPR-slot (DPI-inert readouts H_1834/1836/1837/exp3 were measured only on no-slot/additive arch); (b) role-filler × no-slot (asymmetric additive with role-position).
- A11관계: RELATED — the signal axis was implicitly tied to arch (role-filler≡TPR, same-state≡no-slot-readout), so off-diagonal corners never separated. Both prediction-only (predicted INERT/floor).
- engine-native: UNMEASURED (prediction-only).

### [LOW] Scale-ladder coverage d768-converged confirmation (H_6176) of the d256→d512 held-out 1→3 trend.
- A11관계: INDEPENDENT but low value — scale is an amplifier of a working lever (scale-303m-1b-7b), and the underlying coverage-density lever is itself unfired engine-native (item #2).
- engine-native: UNMEASURED (d768 undertrained/invalid; converged refire never done).

### [LOW] Native-mouth suture/VDJ (H_6139 discrete select+splice) + kosmos persistent recursive Merge (H_6143, storage-side) real-trunk A/B on live CLMConvMoE.
- A11관계: INDEPENDENT but low — meiosis twin (H_6112) real-trunk COLLAPSED scale-invariant (0.022→0.0), predicting numpy-reachability won't transfer; H_6143 needs a NEW decode/storage-side eval rung (standard G1 gate can't verify it), storage-property caveat (recover≠capability, H_9027).
- engine-native: UNMEASURED (numpy REACHABLE only; real-trunk build never done).

### [LOW] HE (representational-homomorphism-error) as a $0 cheap G1-PREDICTOR on production clm303 penultimate; external-consequence/interlocutor loop (consequence_return_design) as a substrate-native lens.
- A11관계: INDEPENDENT — HE is tooling/diagnostic value only (would cheapen screening), NOT a capability lever (HE-as-objective H_6162 + HE-tracks-inhibition H_6161 both floored). The consequence-loop is foreclosed by scoping (a_no_llm_frame_trap) not measurement; worth a falsifiable prediction rather than definitional exclusion.
- engine-native: UNMEASURED (diagnostic never validated; loop out-of-scope by assertion).


## 5각 census 원본 missed_items
### G1-recombination census: which non-terminal lenses (engine-native unmeasured / toy-only / cost-gated) survived this sess
- **H_9121-A11** [HIGH] A11 CRUX = TPR-slot × contrastive-replace objective applied SIMULTANEOUSLY (not either alone). Toy side-probe shows REACHABLE margin +3.30 / reach 1.0 vs additive-no-slot floor; 303M engine-native UNM — The 6 PRs falsified E1 (CE-replace ALONE, floor=architectural) and E2 (TPR ALONE under CE, ledger-dup H_1813/1623) SEPARATELY, then declared objective-floor directional-terminal. But the reframe expli
- **H_6182-6185** [HIGH] Combination-coverage-density corpus lever (🟢 DIRECTIONAL-POSITIVE, arch-independent across attention+ConvMoE+dilated-conv). H_6185 gives an explicit unfired 2-step engine-native recipe: ① RF L=4→8 (RF — The reframe card (H_9121) folds 'coverage' into the basin-preserving-rejected bucket via H_6190 (ECHO-ONLY). But H_6190 measured decode-side grow-window on an ALREADY under-covered ckpt — it did NOT r
- **H_9116+H_9117** [MED] Forward-model LEARNED front-loading via lane15 engine-native build (engine_cli 15-lane state + generator L3 mouth-gate + cli/train.hexa + GPU). Screens: B 🟢 (b50 headroom 3.5→2.2B real) ∧ A 🟡 (corr=0. — This session only did the INFERENCE-side rerank (H_9119 §2 gen_fm_rerank, #2903, DIRECTIONAL, --py numpy mirror). That reranks frozen candidates; it does not build/train the lane15 forward-model that 
- **H_6175** [MED] Neurosymbolic anchor+systematic-composer: held-out recombination 1.0 vs random-ctrl 0 (numpy toy). External arxiv convergence (memory: g1-novel-mechanism-deepresearch) says neurosymbolic is the ONLY p — Session stayed inside the CE-trunk objective/escape frame (E1/E2 are both trunk-objective variants). The neurosymbolic composer is an off-trunk architecture the reframe classifies as 'frame-break = pr
- **H_1840** [DUP-ALREADY-DONE] γ trained-constructive HRR bind (invertible circular-conv trunk combiner with additive-bypass architecturally DENIED, bilinear bottleneck). — Memory long called this 'the sole surviving lever', but the FAIR (non-rigged) cheap-gate on a bypass-denied bilinear target killed it before this session — bypass-denial was NOT load-bearing and the g
- **H_6176** [LOW] Scale ladder for coverage/recombination: converged rung d256→d512 showed held-out 1→3 (positive scale signal); d768 was undertrained/invalid so unconfirmed. — Session treated scale as settled ('amplifier not lever', scale-303m-1b-7b) and focused on 303M objective levers. The specific converged-d768 confirmation of the 1→3 coverage-scale trend was never fire
- **H_6171+H_6186** [LOW] H_6171 generation-modality (small AR model recombines held-out combos, struct 0.95 vs random 0 → G1=0 is training/objective not substrate) and H_6186 G6 targeted-coverage form-priming + G6-bind gate t — Both are pre-reframe generation/measurement-instrument probes. The session's objective-floor terminal absorbed the 'training/objective' framing (H_6171) as falsified, but neither the generation-modali

### Memory census of the anima G1 (C2 RECOMBINE) recombination-wall lever space: which levers/combinations/lenses remain GEN
- **G1-U1** [HIGH] H_6187 follow-on: surface-form + decode-window-ALIGNED coverage retrain of production clm303, then engine-native G1 re-judge. Coverage blocks re-authored in the gate free-gen surface form (not 'the A  — The L8+coverage retrain (clm303_deep_L8_cov, ckpt ~/anima-weights/g1_l8_cov sha 2c565ad4) fired but was ruled INCONCLUSIVE (not floored): the 'coverage-failure=trunk-floor' verdict was explicitly RETR
- **G1-U2** [HIGH] Additive-slot decoder + encoder-decoder consistency (Wiedemer/Lachapelle 2310.05327; Dreamweaver block-slot RBSU / AIN per-attr same family). Additive-per-slot decoder x̂=Σφk(zk) + consistency loss gi — Flagged by the 102-agent deep-research (g1-novel-mechanism-deepresearch) as the ⭐ ONLY cheap-testable NEW mechanism outside the 5 floored axes (objective/readout/reg/data/scale). Queued as 'cheap-gate
- **G1-U3** [HIGH] Hippocampal CLS pattern-SEPARATION + pattern-COMPLETION bio-lens (state/1640_cls_sep_complete package): Marr-Albus separation (off-diag corr→0 orthogonalization + L1 sparsen of recombination materials — Built as a 303M launch-ready package (bio-lens-g1-packages, SMOKE_OK on summer, byte-identical .clm) to fire AFTER H_1602 objrun landed. Its TWIN in the same package (1641 predictive-coding binding) f
- **G1-U4** [MED] γ substrate-side trained CONSTRUCTIVE combiner: replace VAdaptField's L2-Voronoi nearest-basin (compositional depth-0, 8-D winner-take-all k-means, engine_cli.hexa ~L494-608) with a TRAINED dimension- — Named the 'γ substrate twin' in substrate-framebreak (4-corner convergence) and the last untried substrate lever. α/β only measured char-hash / frozen-embedded representations, never a LEARNED nearest
- **G1-U5** [MED] Neurosymbolic / program-synthesis procedure change (NSR 2210.01603 · ExeDec 2307.13883, both ICLR2024): deduction-abduction / execution-guided subgoal decomposition so next-token is NOT a single CE-tr — deep-research flagged it as one of 3 live families but explicitly de-scoped: not a CE-trunk drop-in, needs an external executor/DSL at inference, cannot be $0 cheap-gated → 'owner-scope architecture p
- **G1-U6** [MED] N2 Compression-is-Routing: reconstruction-driven expert self-organization (2512.14809), where ConvMoE expert differentiation emerges from a recon/compression objective (anima §Osmotic-affinity kin, cf — Listed only as '기타'(other) in frontier-novel-levers §6, below the top-3 shortlist (N6+N7, N1+N3, N4+N8) which all fired and floored. Never promoted to a shortlist or fired — an unmeasured expert-diffe
- **G1-U7** [LOW] Native-mouth H_6139 suture/VDJ (discrete select+splice) real-trunk A/B on live CLMConvMoE, engine-native. numpy REACHABLE 0→18/18 shuffle-earned (wrong-lib=0). — Only the meiosis twin (H_6112) got the real-trunk A/B + scale-ladder (0.022→0.011→0.0, scale-INVARIANT FLOORED). H_6139 was flagged '동일 전이위험, 미측정'(same transfer risk, unmeasured) — a discrete-splice A
- **G1-U8** [LOW] Native-mouth H_6143 kosmos persistent recursive Merge (compositional memory, STORAGE-side): depth-3 24/24, additive=HRR=0 collapse. Needs a decode/storage-side eval rung, NOT the standard anima-evalua — Classified as (c) off-trunk storage/decode-side — 'anima evaluate G1 로 검증 불가'(cannot be verified by the standard G1 gate). No decode-side rung was ever built for it, so it sits outside the measured la
- **G1-U9** [LOW] HE (representational-homomorphism-error, An&Du NeurReps 2025 #142, OOD-compositional R²=0.73) computed on REAL clm303 penultimate via numpy, as a $0 pre-screen predictor of G1 BEFORE expensive eval —  — lit-binding flagged it as 'NEW lever B (cheap diagnostic)'. HE-as-OBJECTIVE (H_6162) and HE-tracks-inhibition (H_6161) both fired+floored, but HE-as-a-cheap-G1-PREDICTOR on the production clm303 penul
- **G1-DONE** [DUP-ALREADY-DONE] FLOORED axes — DO NOT re-fire (tune-to-green, p7): binding-readout operators [Hadamard/TPR H_1818/1819, predcoding H_1816, circconv H_1823, exp3 ⊙/NMDA]; binding-architecture census [Hopfield H_1620,  — Not missed — explicitly falsified/floored across the census. Enumerated here so the reader does not mistake a floored lever for an open one; the operator and trunk-objective axes are exhausted.

### Escape-matrix cross-product coverage {objective:6 × arch:5 × signal:2} placed against what H_9121 (BREAKWALL+FLEETFULL F
- **A11-303M-engine-native** [HIGH] (objective=contrastive-replace × arch=TPR-slot × signal=role-filler) at 303M, engine-native. The one cell whose measurement decides confident-ceiling vs escape-exists. E1 side-probe gave TOY REACHABLE — Cost-gated (303M pool train, explicit-go) + toy-overstate risk flagged by E2 precedent (TPR toy unbind 1.0 → real CE 0.022). The design correctly identified it as the crux but left it unmeasured; even
- **pointer-slot-x-objective** [MED] arch=pointer/copy-slot (explicit slot+pointer over a WM buffer, H_1282) × {contrastive-replace, CE}. A non-additive binding slot DISTINCT from TPR, proposed in BREAKWALL lens 2b, never measured with a — The CRUX 2×2 collapsed the whole arch axis to a single slot representative = TPR, so pointer/copy was silently dropped. If A11 lifts, pointer is the cheaper/deployable slot; if A11 floors, pointer×con
- **external-mem-trained-read-gate** [MED] arch=external-mem × a TRAINED read-gate (mouth learns to CONSUME the retrieved D) × {contrastive, CE}. L3/H_9118 MOUTHFLOOR closed ONLY the frozen-mouth retrieval variant (read-gate absent → binding 0 — L3 deliberately froze the mouth to isolate the architecture axis, which by construction excludes a trained read-gate. FLEETFULL flagged it '부분 REACHABLE (read-gate는 objective로 회귀)' but never measured 
- **gradient-free-G-objective** [MED] objective=gradient-free-G — engine_g's reverse originality/info_gap factor as the anti-echo TRAINING pressure (penalize seed-echo, reward info-gap) × {TPR-slot, no-slot}. anima's own reverse engine as — BREAKWALL lens 1b listed it, but FLEETFULL grouped it under 'non-CE signal' and only InfoNCE-contrastive received a toy. engine_g-selection was never run — not even toy — so the objective axis is unme
- **deep-RF-L8-depth-sweep** [MED] arch=deep-RF-L8 — engine-native depth sweep L∈{1,4,8} (recombination has only ever been measured at L1) × any objective, with slot-bypass and depth-vs-width ablation. Pre-registered as FLEETFULL P1 bu — Dismissed by an ANALYTIC coverage argument ('RF≈513 already covers the seed → RF is not the wall') and only torch-DIRECTIONAL via H_6184 (dilated-conv). No engine-native L∈{1,4,8} ablation. I found no
- **energy-objective-x-TPR** [LOW] objective=energy-based (explicit energy on bound vs echo/wrong-D states) × TPR-slot. Named alongside contrastive throughout BREAKWALL/FLEETFULL but only InfoNCE-contrastive got a toy discriminator. — Bundled with contrastive under one label ('contrastive/energy'); the toy operationalized only the contrastive (InfoNCE) form, leaving the energy form formally untested (likely same class, but not meas
- **RL-trunk-objective-x-slot** [LOW] objective=RL — episodic reward = held-out recomb success / downstream consequence, used as the TRUNK training signal to move the CE echo-basin × {TPR-slot, pointer}. A non-basin-preserving, non-differ — Never enumerated in either Fable divergence. The NULL branch punts to a 'non-learning external consequence loop (outside the G1 recomb axis)', conflating an external loop with an IN-TRUNK RL objective
- **signal-axis-corners** [LOW] Two signal-axis corners never isolated: (a) same-state readout × TPR-slot — the same-state DPI-INERT readout ops (H_1834/1836/1837/exp3) were measured only on no-slot/additive arch, never with a real  — The signal axis was implicitly tied to the arch axis (role-filler ≡ TPR-slot, same-state ≡ readout-on-no-slot), so the off-diagonal signal×arch corners were never separated; both are prediction-only, 
- **E2-CE-x-TPR** [DUP-ALREADY-DONE] (objective=CE × arch=TPR-slot × role-filler). TOY REACHABLE (unbind 1.0 vs additive 0.242, ablation-causal) but engine-native 303M FALSIFIED via ledger: H_1813 (TPR expert-weight reparam, CE, --py) NO — Not missed — already closed; flagged to prevent redundant re-fire. CE does not use the TPR slot compositionally (INERT-under-CE), which is precisely WHY A11 pairs TPR with a CE-REPLACE objective.

### Completeness critic of this session's G1 conclusion (H_9118→9121: "objective-floor → architecture-floor, DIRECTIONAL-TER
- **G1-1** [HIGH] Coherent-trunk recomb-objective (L4) was NEVER cleanly re-measured — the FALSIFY verdict rests on a garbled ckpt — recomb_s7.bin output is G0-collapsed garble (out_np_recomb_s7.tsv: 'meeeee eeeeee', 'reeree reearee', kwr as low as 0.0). Comparing a broken model's novel=0 against coherent trunk_baseline's novel=0 C
- **G1-2** [HIGH] H_9120 is INCONCLUSIVE-AT-FLOOR, not a clean falsification — every arm reads 0 so the shuffle/wrong-D controls are vacuous — EXP1_VERDICT.txt: A.novel=0, B=0, C(SCRAMBLE)=0, D(wrong-D)=0. The memory repeatedly names this exact pattern INCONCLUSIVE-AT-FLOOR (binding-arch-census, exp3-bind, g1-fromscratch). 'SCRAMBLE control 
- **G1-3** [HIGH] The 303M frozen G1 bar (exp1_g1.py grow-window) has NO held-out compositional split — metric-validity / ill-posed-target gap — The verdict bar scores 'composed novel-only' against hand-assigned target keywords (ocean+engine→harvest, forest+music→voyage …) that have no ground-truth basis — nothing tells the model that ocean+en
- **G1-4** [HIGH] 3rd engine-native leg (live-core own-GEMM .hexa decode) never completed; 'numpy=torch byte-identical' is two mirrors of the same math agreeing, not the production engine path — H_9120 canonical = numpy --py (owner override a_eval_py_canonical) + torch cross-check. Both numpy-hand-rolled and torch-library forward are re-implementations of the SAME ckpt math — their byte-ident
- **G1-5** [MED] Architecture-floor reframe ('additive ConvMoE has no multiplicative binding slot → no objective can attach') is asserted from toy+theory, not from a coherent 303M objective-cannot-descend measurement — The reframe's evidence is (i) H_1816 additive L_bind step-550 collapse (prior), (ii) E1 toy InfoNCE-on-ADD plateauing at 1.20 (numpy N=24), (iii) Fable breakwall reasoning. No 303M run shows a coheren
- **G1-6** [MED] A11 CRUX (TPR-slot × contrastive) — the sole 'open' door — rests entirely on a numpy toy in the DOCUMENTED toy-overstate regime — E1 side-probe TPR margin +3.30 / reach 1.0 and E2 TPR held-out 1.0 are exactly the abstract-REACHABLE→303M-FAIL pattern the card itself cites: H_1466 (binding leg 1.0, 🧱 wall), H_1623 (abstract 1.0 → 
- **G1-7** [MED] Single frozen γ=0.5 for the recomb aux — no γ sweep or golden-zone schedule tested — recomb_objective.py pins RECOMB_GAMMA=0.5. Given a_savant_train's golden-zone inhibition-scheduling lens and H_1816's additive collapse being a magnitude/basin phenomenon, γ=0.5 may simply be over-wei
- **G1-8** [MED] Neurosymbolic escape — flagged by the session's own arxiv convergence as the ONLY structural DPI-breaker — was not registered as an escape candidate — H_9121 background cites external convergence 'neurosymbolic만 DPI를 구조적으로 깸(비-cheap)' and g1-novel-mechanism-deepresearch names neurosymbolic + additive-slot+consistency as the non-cheap breakers, yet t
- **G1-9** [MED] G1 recombination measured ONLY on English synthetic concept pairs — Korean jamo-byte binding axis untested despite lit flagging it favorable — All arms (H_9118 concept pairs, H_9120 exp1_g1, H_9119 b50 concepts) are English. lit-binding-objective-external-arxiv + frontier-novel-levers note '한국어byte 자모 유리' (N8 jamo lever) — sub-character byte
- **G1-10** [MED] H_9119 forward-model rerank result is underpowered — single frozen listener oracle, floor-dominated mean (half concepts at 999), 4/7 decodable subset — b50 = one frozen listener (ideation-303m-d5000); no listener ensemble, so the 3-byte mean improvement (505→502) could be listener-specific. Mean is floor-dominated (7/14 pinned at 999); the real signa
- **G1-11** [LOW] External-consequence loop (non-learning path) dismissed as 'G1 재조합축 밖' rather than probed — H_9121 NULL clause parks the consequence/interlocutor loop outside the recombination axis by definition. Given preserved design docs (consequence_return_design, llm_interlocutor_design, loopclose_desi
- **G1-12** [DUP-ALREADY-DONE] Escape-3 tension-loop rejected via self-verification (H_1834/1837 재포장) — This self-rejection is well-supported by the ledger (h1834/h1837/h1837-tension-mouth-iterated-dpi: native-mouth temporal readout = DPI-INERT, same-state ablation inert). Listed only to confirm it is N

### G1/recomb/ConvMoE/savant/mitosis/deep-L8 ING census cross-checked against HYPOTHESES.jsonl verdicts. Central finding: EV
- **#42492882** [DUP-ALREADY-DONE] ConvMoE G1벽 돌파 ab75착륙 — deep ConvMoE L≥8 이 RF-reachable 이라 G1 재조합 통과가능 재프레임 (🟢 GREEN-DIRECTIONAL, deep 미실행). 4 lens카드 H_1581(binding lane)·H_1582(savant-on-G1)·H_1583(expert-routing)·H_1584(depth L≥8) — Board never `ing done`-scrubbed after resolution. The deep-ConvMoE L8 training got executed (via follow-on #42492905) and scored → H_1598 🧱 FALSIFIED: L8/E4 d2781 held-out DESCENT 2/2 PASS (generalize
- **#42492883** [DUP-ALREADY-DONE] STANDING-GO (user explicit 2026-06-26): ae99 GPU-util 검증 착륙시 → 즉시 deep-ConvMoE 303M 발사 pool. 레시피=anima train --canon --L 8 --d 2960 --savant --mitosis --corpus <4칸> --heldout-corpus <held>. — The STANDING-GO fired: #42492905 executed exactly this recipe (L8 d2781, savant dropout anneal, mitosis E0=2→Emax=4) on summer GPU to step 12000. Serialized .clm v0.3 (154MB sha 5777c506…), scored eng
- **#42492905** [DUP-ALREADY-DONE] deep-ConvMoE 303M L8 d2781 학습 PARTIAL+finalize 발사 (#42492883 STANDING-GO 현 상태). summer clm303_deep_L8_d2781.pt step12000/15000 저장 후 ~83%서 정지. finalize 서브에이전트 a0854800 발사: serialize→held-out DESCENT→en — The finalize completed: verdict = still-walled. H_1598 🧱 FALSIFIED landed on origin/main with the full evidence (.pt sha 69ac4e34…, .clm v0.3 round-trip PASS, held-out ko 1.600/en 1.942 both < uniform
- **#42492881** [DUP-ALREADY-DONE] savant/mitosis-ByteGPT 프로그램 a01f착륙: PART-A savant→ByteGPT 배선 DONE (DIRECTIONAL) · PART-B mitosis-analog H_1580 MIXED. NEXT=cost-gated 303M pool savant-ByteGPT 학습→engine-native G0-G6+G1 bar(깊이중요)+held- — Both levers are already falsified AS G1 levers, and neither is a combination-operator (which is what G1 requires). savant-on-G1 = H_6161 🧱 DIRECTIONAL-FLOOR ('inhibition-as-compositional-noise', trunk
- **#43053596** [DUP-ALREADY-DONE] 레버발견 캠페인 — 결합-연산자 floor 구조적 확정. 유일 live 레버=trunk-objective (recomb-objective InfoNCE) ByteGPT-303M A/B, ckpt PULLed (off sha b55f73·on sha 5c93b1), engine-native G1 eval LIVE via cli/evaluate.py, poll — Resolved → H_9024 🧱 NOT-SUPPORTED CONFIRMED (DIRECTIONAL): recomb-objective InfoNCE on ByteGPT-303M attention trunk gives G1 best_distinct ON=0 == OFF=0, LIFT=0, both arms 4/4 held-out DESCENT (fair).
- **#42492884** [DUP-ALREADY-DONE] ByteGPT-303M G0-G6 py-engine 측정 — ab899가 pool detached 잡 띄우고 결과없이 rest. headline=ByteGPT G1 재조합 통과? a7b_pass GREEN/FAIL. — ByteGPT-303M G1 was measured repeatedly downstream: the clean-G1-wall reference is ByteGPT (single=2, memory g1-py303-single-floor-vs-bytegpt-lever), and the recomb-objective ByteGPT A/B (#43053596→H_
- **A11 / H_9121** [HIGH] THIS SESSION's escape — G1 objective-floor escape CRUX. TPR-slot × contrastive-replace SIMULTANEOUS. TOY REACHABLE margin +3.30 (numpy N=24 D=96), 303M UNMEASURED (cost-gate, explicit-go 대기). worktree — Not missed — this is the frontier the task references. Recorded here to close the census: A11 is NOT one-of-several remaining lenses; it is the SOLE survivor. E1 (contrastive-replace alone) = TOY-AT-F
- **board-hygiene** [LOW] savant cli/train 303M LEARNING fire follow-ons (#41421039 H_1567, #41421046 H_1567-canon, #41421052-class, #42201294/#42201295 savant-g6 decode, #41421026-family) — many ⏳10d cost-gated savant/mitosis — These are savant/mitosis TRAINING (capacity/expression axis) follow-ons, not G1-combination lenses — orthogonal to the G1 wall per a_substrate_disjoint. Several sub-items already have WIRED-live done 

