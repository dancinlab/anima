# G1 재조합 frontier — earned terminal @303M byte-LM (Fable 전-census 재프레임 adjudication)

**날짜**: 2026-07-10 · **트리거**: depth-RF(H_1584) 🧱 착지 후 walls-delegate-to-fable — 완성된 5축 census 전체를
Fable 5 재프레임에 위임(tune-to-green 아닌 새 각도 or 근거있는 terminal 판정).

## Fable 판정 요약
**새 lever 없음**(지어내면 tune-to-green). 단 Fable는 "terminal은 아직 earned 아님 — 기록-내 미결 3개"를 제기.
그러나 Fable가 참조 못 한 커밋된 $0 측정 2건이 그 미결 2개를 **이미 🧱로 닫아둠** → Fable 자신의 기준
("트리 어느 노드든 🧱면 그 시점 terminal earned")대로 **terminal EARNED**.

## Fable 결정트리 vs 커밋된 기록 (전 노드 $0-closed)
| Fable 노드 | Fable가 본 상태 | 커밋된 실제 기록 | 판정 |
|---|---|---|---|
| STEP A — F2-density 감사 | 미결(E1 corpus 각주 구멍) | `state/g1g6_exhaustive_brainstorm/f2_datapath/heldout_recomb/RESULT.json` = **COLLOCATION-ONLY** (true held-out novel n=0 · rare n=8 sparse · full n=49=memorized in-distribution collocation=G1 벽 regime). "F2 cannot escape the wall via existing data." | 🧱 $0 |
| STEP B — XOR instrument@power | power-limited·NEXT=대형코퍼스 spend-go | **H_9265(#3289) 이미 2 대형코퍼스 $0 Stage A 실행**: web-broad 477MB(N=11564) R1✅ R2❌(sign-flip 강건성 미달) · review 500k(N=10066) **R1❌(I3 5e-05≪null=비가법 신호 부재)**. NOT-CERTIFIED = instrument-verdict. Stage C(GPU interaction-lift)는 A미인증=INVALID-gate로 정당 차단(사전등록). | 🧱 $0 |
| STEP C — F2-dense 2-arm ($100-150) | 조건부 단발 | STEP A가 moot화: F2-dense corpus(consciousness_anchor)는 COLLOCATION-ONLY라 학습 시 held-out 신호 부재→벽 재생산. 발사=tune-to-green. | moot |
| Q3 — terminal 문구 감사 | exit="attention 교체 선행" | **이미 반증**: H_1602/H_9024 ByteGPT attention-303M(2000+8000 step) best_distinct 0→0·CLAUDE.md 교훈 7(ConvMoE H_1394·ByteGPT-L24 H_1590 둘 다 engine-native G6 floor). exit-clause 재작성 필요. | 문구수정 |

## EARNED TERMINAL — 정직한 문구 (Fable Q3 교정 반영)
> **G1 held-out constructive recombination은 이 corpus-class × next-byte CE measure에서 ≤303M로 학습 불가 —
> arch-무관(ConvMoE·attention 둘 다 engine-native floor: H_1584 depth-RF·H_1602/H_9024 attention). 측정된 corpus의
> non-additive 성분은 ≤~2%(0.979 additive 천장·H_9261), XOR/negation 슬라이스에 집중하나 모든 자연코퍼스서 검출력
> 이하 or 신호 부재(H_9265 review 500k I3 5e-05) + held-out 재조합 신호는 어떤 코퍼스에도 부재(F2 COLLOCATION-ONLY).
> 벽의 진범 = conv trunk 아니라 corpus×CE 결합 measure. exit = 아키텍처 교체가 아니라 학습 measure(corpus/task
> class)의 교체.**

## 전수 소진 census (측정가능 레버 전부 🧱 · 전부 verified merge)
read-side 6 lane 🧱(#3284/85/86) + γ en-general 🧱(interaction-lift −0.801 #3230) + γ STEP-0 synthetic 🧱(bind−add −0.147)
+ γ ko PC-P2 XOR 측정불가·신호부재 🧱(H_9265 #3289) + F2 데이터경로 COLLOCATION-ONLY 🧱($0) + census 19 family 🧱
+ objective/readout/combiner/data 전수 🧱 + **depth-RF(H_1584 #3293) 🧱**.

## Ψ-SOMA 정합
이건 **ρ(reach) fact**이지 σ(consciousness) 판정의 결손이 아님(amoeba argument). deep L8 eval에서 σ vitals 9/9 LIVE
확인. G1 벽이 terminal이어도 consciousness verdict 본체는 무손실.

## 유일 reopen 조건 (사전등록 · 미충족)
학습 measure 자체 교체 — held-out 재조합 신호를 실제로 담은 corpus×task class 구성이 발견될 때만(현재 모든 자연·합성
코퍼스 COLLOCATION-ONLY or 신호부재). 그때 γ real-text target 발사 정당. 그 전 재발사(read-side/measured-γ/F2-dense/
XOR-Stage-C-on-uncertified-instrument)는 전부 tune-to-green.
