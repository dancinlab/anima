H_9313 — 실코퍼스 이중해리: 같은 파티션에서 head 만 바꾸면 무엇이 보이는가
================================================================================
PRE-REGISTERED — 발사 전 동결 (frozen-first · c9 · p7 · NO tune-to-green)
freeze_ts_local: 2026-07-14 (KST) · lane: MITOSIS-ENGINE
선행: H_9311 🟢 (예산 스윕 · 단 **B3 통제 판독 불가**) · H_9301 🟢 (미러 이중해리) · H_9309 🔵 (toy 이중해리)

--------------------------------------------------------------------------------
1. 왜 (H_9311 이 남긴 정확한 구멍)
--------------------------------------------------------------------------------
H_9311 의 사전등록 통제 B3("FLAT 은 굶주림으로 열화해야 한다")가 **판독 불가**로 끝났다:
FLAT 이 퇴화분할 자물쇠 때문에 **10 셀에서 멈추므로 굶주림 영역에 도달조차 못 한다**
(flat 변화량 = +0.00000 · 축 미탑재 · 반증 아님).

⇒ 실코퍼스에서 이중해리를 보려면 **성장과 head 를 분리**해야 한다:
   **수리된 성장으로 파티션을 한 번 만든 뒤, 그 위에 flat head 를 다시 깔아 채점한다.**
   성장은 동일 · 소유자 동일 · 테스트셋 동일 ⇒ **추정기만 다르다.**
   (H_9309 의 E2 가 toy 에서 한 것 · 이 카드는 그것을 **실코퍼스**에서 한다.)

--------------------------------------------------------------------------------
2. 설정 (H_9310/H_9311 verbatim)
--------------------------------------------------------------------------------
REAL KO 30MB 창 sha c47b6808… · Vj=323 · ko_stride 2500 · dim-3 · even/odd held-out ·
nats/UTF-8-byte · 성장 = live `jamo_head_grow_shrink` (수리된 faculty · 한 번만).
head 재구성 = 엔진 자신의 `_jh_counts`(flat) / `_jh_counts_wb`(WB) + `_jh_assign`.
셀 스윕: grow_max ∈ {10, 40, 160, 320} — 전 지점 보고.

CALIB (BLOCKING):
  C1 코퍼스 sha == c47b6808…  ∧  Vj == 323
  C2 grow_max=320 지점의 WB-head CE 가 **2.46370** 를 재현 (|Δ| ≤ 0.01 · H_9311 앵커)
  실패 → ⛔ INVALID (판정 금지).

--------------------------------------------------------------------------------
3. 동결 bar
--------------------------------------------------------------------------------
D1 STARVATION-ENGAGED (전제) : FLAT-head CE 가 셀 증가로 **열화**해야 한다
        CE_flat(320셀) − CE_flat(10셀) ≥ +0.02 nats/byte
        ⇒ 실패하면 이 창에서 굶주림이 안 걸린 것 ⇒ **⏳ UNREADABLE**(판정 금지 · 정직 보고).
          (H_9311 B3 와 같은 결과 · "스윕이 flat 하면 축 미탑재를 먼저 의심")

D2 DISSOCIATION (D1 통과 시에만 판독) : WB-head 의 열화가 FLAT-head 보다 작아야 한다
        [CE_wb(320) − CE_wb(10)] < [CE_flat(320) − CE_flat(10)]
        ⇒ **H_9301 미러의 이중해리가 실코퍼스 engine-native 로 전이된다.**
        KILL: WB 가 FLAT 만큼(또는 더) 열화 ⇒ 미러의 이중해리는 실코퍼스에 없다(정직 negative).

판정:
  🟢 DISSOCIATED : D1 ∧ D2  — 미러의 핵심 발견이 실코퍼스 engine-native 로 완전히 전이
  🧱 NO-DISSOC   : D1 ∧ ¬D2 — 굶주림은 걸렸는데 shrinkage 가 그것을 막지 못함(정직 negative)
  ⏳ UNREADABLE  : ¬D1      — 이 창(stride 2500)에선 굶주림 축이 안 걸림 ⇒ 더 큰 셀 수 또는
                              더 작은 stride 가 필요(새 사전등록) · 벽 선언 금지
  ⛔ INVALID     : CALIB 실패

--------------------------------------------------------------------------------
4. 계측 무결성
--------------------------------------------------------------------------------
- **성장은 한 번만** 수행하고 head 만 두 번 재구성 ⇒ 파티션 차이가 아니라 **추정기 차이**임을 보장.
- λ = WB 닫힌형(자유 하이퍼 0) · 스윕할 손잡이 없음.
- 엔진 자신의 함수만 호출(사설 프로브 아님) · TRAIN only · TEST = even/odd held-out.
- 전 셀 지점 보고 · bar 발사 후 이동 금지.
- LABEL: py 2-production engine (a_eval_py_canonical) · stride 2500 창 한정.
