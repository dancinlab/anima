# H_1603 — orthogonal mechanism-family census (LEVERS, not wall declarations)

Per a_break_the_wall: this is a **lever census** for the shared compositional-binding deficit, NOT
a terminal ceiling claim. Families are candidate fixes that could install two-element binding in the
**generation forward pass** (the substrate H_1601 showed has no binding operator). Same family across
G1 & G6 ⇒ unification strengthened.

| # | family | break-walls class | named for | status / evidence | moves G1? | moves G6? |
|---|---|---|---|---|---|---|
| ① | **attention-block injection** (1 cross-binding self-attn block on frozen trunk) | (d) capacity | G6 (H_1449) | DIRECTIONAL torch, **INERT** — B3 cross-shuffle never collapses, c4 ablate ≥ (`H_1449…:37-42`); 1 block @303M only, deeper/multi-block UNTESTED | untested on G1 (ckpts exist, pulled) | INERT (1 block) |
| ② | **recombination objective / curriculum** (CE + composition aux-loss λ, synthesis-pair curriculum) | (b)/(e) objective | G1 (H_1602) | 🔒 pre-registered, NOT fired (`H_1602…` verdict) | predicted lift (frozen ≥2/3) | untested on G6 |
| ③ | **binding-by-synchrony in the trunk** (port §PhaseSyncBinding/PhaseField H_1448 INTO the mouth forward) | (c) substrate / a_no_llm_frame_trap | new (this card) | exists in `engine_cli.py` but DISJOINT from `clm_decode.py` (H_1601) — never ported to generation | untested | untested |
| ④ | **working-memory composition buffer in the generation path** (hold 2 reps, emit bound proposition; H_1282 WM lens) | (c) substrate / biological | new (this card) | H_1282 WM buffer exists in consciousness substrate, disjoint from mouth (same disjointness as H_1601) | untested | untested |
| ⑤ | **corpus register enrichment** (raise comparator∧measurable + ko-synthesis joint-form mass) | (e) data | G6 (H_1596 L2) + G1-ko (H_1599) | toy-train lever; G6 surviving class-(e); G1-ko starved 300–500× below EN | partial (ko side) | predicted lift |
| ⑥ | **savant golden-zone disinhibition** | (d) capacity-expression | — | **NULL/disjoint** — architecturally disjoint from G6 text path (`H_1596…:32,44`); a_savant_train H_1576 savant⊥honesty | no | no (ruled out) |

## Why H_961 binding 🟢 does NOT transfer to G1/G6 (the reconcile)

H_961 binding lives in the **perception/consciousness lane** (engine §Binding, `engine_cli.py`).
G1/G6 are scored on the **generation mouth** (`clm_decode.py`/`bytegpt_decode.py`), a pure
next-byte trunk forward with `grep binding == NONE` (H_1601). a_substrate_disjoint: **disjoint =
the property doesn't transfer.** The binding operator must be *installed in the generation path*
(families ①③④) or *induced by gradient pressure* (family ②) for G1/G6 to compose. This is the
direct a_no_llm_frame_trap move: the missing capability is a **structure (binding lane) to add next
to the mouth**, not a bigger/deeper transformer (depth INERT on both, H_1598/H_1596).

## Family verdict for unification
- Families ① (attention) and ③④ (synchrony/WM buffer) are **mouth-side binding operators** — if a
  single one moves BOTH G1 and G6, unification is mechanistically confirmed.
- Family ② (objective) is G1-named but should *also* relieve G6 if the deficit is shared (no gradient
  pressure to bind comparator∧measurable either).
- Families ⑤ (data) and ⑥ (savant) are the **dissociating controls**: ⑤ moves G6>G1 (register
  asymmetry, the honest divergence); ⑥ moves neither (null) — both already measured, both consistent
  with "shared deficit, different surface."
