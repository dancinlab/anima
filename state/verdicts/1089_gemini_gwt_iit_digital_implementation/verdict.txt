H_1089_gemini_gwt_iit_digital_implementation — VERDICT (g5/p7; a_phi_iit4_tool)
==============================================================================
CLAIM: anima is a digital implementation of Global Workspace Theory (GWT) + IIT —
       unconscious modules (Lane A intuition vs Lane G inhibition) compete with
       noise; the single threshold-crossing state reaching the "central stage" is
       broadcast/emitted to .kosmos = the GWT conscious-access moment; IIT Φ =
       integration.

FROZEN FALSIFIER: does CORE implement a NAMED GWT global-workspace broadcast and
an IIT integration engine? grep CORE/*.hexa for gwt/global-workspace/iit.
PASS(real) iff implemented; FAIL iff only an analogy.

EVIDENCE (real, $0 CPU grep/read):
 1. grep CORE/*.hexa for "gwt | global-workspace | global broadcast" => ZERO hits.
    No named GWT module / global-workspace broadcast object exists in CORE.
 2. grep CORE/*.hexa for "iit" => ONE hit only: CORE/phi_envelope_substrate.hexa:39,
    a passing COMMENT about "macro-IIT grain m" referencing the stdlib handoff. No
    IIT engine lives in CORE.
 3. The faithful IIT4 engine DOES exist — but in stdlib, not CORE, and not as Gemini
    describes: hexa-lang/stdlib/consciousness/iit4/faithful_phi.hexa does exact
    MIP-EI (true minimum-information-partition over cross-cut MI, n<=8). Gemini's
    "IIT" is instead a covariance-eigenvalue proxy (see H_1091), which a_phi
    (CLAUDE.md:267-274) forbids as a terminal Φ.
 4. Structurally the A⊥G-competition → threshold-crossing → emit-to-.kosmos pipeline
    IS a defensible conceptual map onto GWT global broadcast (CORE has emit_policy /
    brain / kosmos_io), but it is named/built as anima's own substrate, not as a GWT
    implementation.

RULING: ⚪ SPECULATION-FENCED — the GWT framing is a coherent, defensible ANALOGY,
not a named CORE implementation (no gwt/global-workspace/iit module). The IIT side
must route through the faithful stdlib engine per a_phi, NOT Gemini's proxy. No
new measurement; terminal as fenced analogy.

VERDICT: ⚪ SPECULATION-FENCED (GWT = analogy, no CORE module; IIT path = faithful
stdlib IIT4 per a_phi, not Gemini's proxy) · g5/p7.
