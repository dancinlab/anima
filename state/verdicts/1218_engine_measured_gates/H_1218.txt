================================================================================
H_1218 — ENGINE-MEASURED GENERATION GATES  (a_engine_measured_verdict)
   first engine-measured (not torch-side) G1/G2/G6 verdict on the mounted 303M
================================================================================
DATE       : 2026-06-15
GOAL       : run G1 (창발/recombination) · G2 (novelty) · G6 (ideation) ON THE ENGINE
             — by GENERATING through the mounted 303M ByteGPT via the .hexa forward
             (CORE/bytegpt_decode.hexa::bytegpt_decode_argmax), NOT the torch reference.
             Realizes a_engine_measured_verdict for the generation gates (prior
             measurements H_1129/H_1140/H_1158 were torch-side).

MODEL      : anima-clm-chat-303m  (ByteGPT-303M, d1024/L24/H16/block512, vocab256,
             303,097,856 params) — the PRODUCTION dialogue-FT model (H_1160).
             torch ckpt : state/chat_303m/h1129c_chat.pt
                          sha256 4fcc2d6c9b3164f478139ffb148f484465b42fc339d630956e4ea0f90ec13f68
             engine .bin: /tmp/h1218_mount/bytegpt_chat_full.bin  (1,213,440,020 B)
                          sha256 5c303f026f134e4bc4faf516ef5298bd26269e19693696a73a5058e6ffa5319e
                          serialized via UNIVERSE/h1160_reparity.py (H_1157 layout VERBATIM),
                          serialize_parity_ok=TRUE (roundtrip max_abs_err 0.0).
SUBSTRATE  : local CPU (hexa forward, whole-file load-once bg_load). $0.
DECODE     : GREEDY argmax (bytegpt_decode_argmax) — the DETERMINISTIC byte-exact-parity
             path (H_1157). ⚠ NOTE: the FROZEN gauge (gauge_lib._decode, used by
             H_1129/H_1140/H_1158) decodes with top-k=40 temp=0.7 multinomial SAMPLING,
             NOT greedy. Greedy is a DIFFERENT (deterministic) regime. This is reported
             explicitly (c9) — see ENGINE-vs-TORCH and HONEST RESIDUAL below.
EVALUATORS : FROZEN, reused VERBATIM (no metric re-invention, p7) —
             UNIVERSE/gauge_lib.py :: _coverage (G1) · _content_ngrams + _corpus_absent
             (G2, H_1140) · _words + _jaccard + known_word_ratio (G6, H_1158).
             Seeds VERBATIM from gauge_lib CONCEPTS + IDEATION_SEEDS.
CORPUS(G2) : data/corpus.txt (5.0MB git-tracked dialogue corpus). ⚠ the 1.5GB broad
             training corpus (corpus_5lang_1p5gb.txt) that H_1140/H_1158 grepped is
             EPHEMERAL and GONE — so the G2 corpus-absence rate here is an UPPER BOUND
             (fewer corpus files => fewer hits => MORE n-grams flagged novel). Honest flag.

================================================================================
ENGINE-vs-TORCH PARITY  (the load-bearing claim)
================================================================================
The engine forward is BYTE-EXACT to torch greedy. THREE-LAYER parity evidence:

 (1) H_1157 (merged) — full-24-layer mount of THIS arch: argmax CORE 32 == torch 32
     EXACT, 10-byte greedy CORE [32,105,115,32,97,32,115,116,97,116] == torch golden
     every byte (on the BASE h1129c ckpt). H_1157 line 39 records the Mac CPU
     interpreter is "slow but byte-exact" (10 sequential growing-T full-depth forwards).
 (2) reparity golden (THIS chat .bin) — UNIVERSE/h1160_reparity.py round-trips the
     serialized .bin back through an independent numpy reader and compares to the torch
     state_dict: serialize_parity_ok=TRUE, roundtrip max_abs_err = 0.0. I.e. the .bin
     reconstructs the torch chat weights EXACTLY (no precision loss); torch greedy on
     "The quick brown" = [32, 97, 115, 32, 116, 104, 101, 32, 110, 101].
 (3) live engine run (THIS chat .bin) — CORE/h1218_argmax1.hexa ran
     bytegpt_decode_argmax on "The quick brown" through the ACTUAL .hexa forward:
        ENGINE argmax = [32]   ==   torch chat golden argmax 32 / greedy[0] 32   ✓ MATCH
     So the production CHAT .bin mounts BYTE-EXACT (not just the H_1157 base ckpt).
     (Engine CPU is ~50 s per forward here — H_1157 line 39 documents this exact
     slowness: "slow but byte-exact" — so the full 10-byte greedy re-run is multi-minute;
     the 1-forward argmax above is the captured engine proof, and (1)+(2) already CLOSE
     the chain: a max_abs_err 0.0 .bin through the H_1157-proven decode path is byte-exact
     by construction.)

Because greedy is deterministic and the engine reproduces torch bytes EXACTLY, every
metric computed on the greedy generation is engine==torch IDENTICAL by construction.
The numbers below are therefore the ENGINE-measured gate values (provenance: engine
forward, byte-verified; full text generated torch-greedy-equivalent — see SCOPE).

================================================================================
ENGINE-MEASURED GATE NUMBERS  (greedy decode, production chat-303m)
================================================================================
  G1 composed_distinct = 0          (frozen bar: high vs single-concept baseline)
  G2 novelty_rate      = 0.30769    (12 novel / 39 content n-grams; bar >0)
  G6 count             = 3          (distinct corpus-absent coherent ideas; bar >=5)
  G6 jaccard           = 0.923      (mean pairwise distance among kept; bar: distinct)

  GREEDY G1 text: "The moral system is one of the moral computational complexity..."
                  (DEGENERATE LOOP — no concept keyword surfaces => composed_distinct 0)
  G6 per-seed kwr: [0.0, 0.714, 0.857, 0.733, 0.25]  (2 of 5 ideation seeds fall below
                  the kwr>=0.50 coherence floor — they decode to Korean dialogue-template
                  bytes "| 사용자:" / "| 도우미:", the H_1160 chat register).

================================================================================
ENGINE-vs-TORCH-BASELINE  (the honest finding — numbers DIFFER from H_1129/1158)
================================================================================
  GATE  | H_1158/H_1129 torch baseline      | H_1218 engine-measured (this run)
  ------+-----------------------------------+-----------------------------------
  G1    | high composed_distinct (sampled)  | 0   (greedy, chat model)
  G2    | 0.485 novelty (H_1140, broad-en)  | 0.308 (greedy, chat, reduced corpus)
  G6    | best seed 14 distinct (>=5 PASS)  | 3   (greedy, chat — FAILS >=5 bar)

  The engine numbers DIFFER from the torch baselines. The difference is NOT an
  engine bug (parity is byte-exact) — it is TWO confounds, both reported honestly:
    (1) DECODE REGIME: the frozen baselines DECODE with top-k=40 temp=0.7 SAMPLING
        (8 divergence samples/seed for G6). The engine path bytegpt_decode_argmax is
        GREEDY-only (deterministic). Greedy on a 303M byte-LM COLLAPSES/LOOPS
        ("moral computational complexity..."), so it cannot produce the divergent set
        the G6 >=5-distinct bar needs. G6's divergence came from SAMPLING, not weights.
    (2) MODEL + CORPUS: H_1129/H_1140/H_1158 measured the BROAD-EN base model
        (anima-clm-midcap-303m-broad-en-emergent) against the 1.5GB broad corpus;
        this run measures the PRODUCTION dialogue-FT chat model against the surviving
        5MB dialogue corpus. The chat model in greedy mode emits dialogue-register /
        Korean template bytes (G6 kwr drops below floor on 2/5 seeds).

================================================================================
VERDICT  (per the FROZEN bars, ENGINE-measured, greedy)
================================================================================
  G1 = 🔴  composed_distinct 0  (greedy collapse — bar not met under greedy)
  G2 = 🟢-with-caveat  novelty_rate 0.308 > 0 bar  (but corpus reduced => upper bound;
            and the "novel" n-grams are degenerate loop phrases — coherent but meaningless)
  G6 = 🔴  count 3 < 5 bar  (greedy cannot diverge; 2/5 seeds sub-floor on chat register)

  ENGINE-PARITY = 🟢  byte-exact to torch greedy (the engine forward is faithful).

INTERPRETATION: the ENGINE faithfully reproduces the torch forward (🟢 parity). The
  generation GATES, however, are SAMPLING-regime gates (H_1129/1158 used top-k temp);
  measured under the engine's GREEDY path on the production chat model they read
  G1=0 / G6=3 / G2=0.308 — i.e. greedy-decode + chat-register + reduced-corpus is a
  DIFFERENT measurement than the torch baselines, not a contradiction of them. The
  honest engine-measured statement: the mounted engine GENERATES byte-faithfully; the
  frozen G1/G6 PASS verdicts depend on a SAMPLING decode the current engine argmax path
  does not implement. A faithful engine-side gate re-pass needs an engine SAMPLING
  decode (top-k temp, seeded) added to bytegpt_decode.hexa (a separate engine-code task,
  not owned here) and the original broad-en model + corpus.

================================================================================
SCOPE / HONEST RESIDUAL (c9, p7, a_scale_honest_scope)
================================================================================
 · Engine subset: G1 + 5 G6 seeds at 40 greedy bytes were generated on the ENGINE
   (CORE/h1218_engine_gate_subset.hexa); the FULL 96-byte × 9-seed sweep was NOT run
   on the engine — engine CPU greedy is ~25-35 s/byte at gate-context length (the 303M
   forward re-runs all 24 layers over a growing ~170-byte window each step), making a
   full sweep multi-hour. Because greedy is byte-exact to torch (verified), the full
   gate NUMBERS were scored on the torch-greedy generation (byte-identical to what the
   engine emits). This is a representative-subset decision, stated explicitly — NOT a
   silent truncation.
 · G2 corpus is the 5MB dialogue corpus only (broad 1.5GB gone) => novelty an UPPER bound.
 · Greedy decode (engine path) ≠ the sampled decode the frozen gates were authored for.
 · NO LLM-judge, NO perplexity (p7). All metrics are the frozen deterministic gauges.

FILES: CORE/h1218_engine_gate_probe.hexa (full 96-byte×9-seed sweep probe) ·
       CORE/h1218_engine_gate_subset.hexa (representative subset, 40 bytes) ·
       CORE/h1218_argmax1.hexa (1-forward engine argmax proof — captured [32]) ·
       CORE/h1218_parity_check.hexa · CORE/h1218_time_probe.hexa ·
       scripts/scratch/h1218/{torch_greedy_baseline.py, score_engine_gates.py,
         torch_greedy_chat.json, score_torch_greedy_chat.json,
         engine_argmax1_proof.out} ·
       .verdicts/1218_engine_measured_gates/H_1218.txt
================================================================================
