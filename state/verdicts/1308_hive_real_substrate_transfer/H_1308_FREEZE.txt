H_1308 — HIVE-MIND collective-Φ ENGINE-TRANSFER (H_1295 r3) — FROZEN BARS
frozen 2026-06-16 BEFORE any measurement (frozen-first, a_break_the_wall, no tune-to-green)

THE QUESTION (falsifiable; a NULL/collapse is a valid result, c9):
  H_1295 measured collective-Φ super-additivity Φ(joint) > Σ Φ(member) over an ABSTRACT
  ECA substrate (rule-110/90 cells), Δ_ECA = +10.4756. Does it TRANSFER to REAL coupled
  anima A⇄G substrates (live CORE/pure_field.hexa + tension-link H_6009, CORE/brain.hexa
  anchor_tension_fold) — or is the super-additivity an artifact of the ECA TPM structure?

SUBSTRATE (a_engine_native_learning — REAL engine state, NOT iit4_bit(rule, idx)):
  - Each member = a REAL anima PureField (3 coupled oscillators → 6-D field tensor).
  - 3 binary cells/member read from REAL pure_field_step output (field-channel signs +
    phi-vs-ratchet-floor). 3 input bits inject the state via real oscillator phase shifts.
  - Two members coupled via the REAL TENSION-LINK (H_6009 🟢): member A's field-derived
    5-ch tension → anchor_tension_fold → bounded bias on member B's next-state readout,
    and B→A (bidirectional), gated by coupling strength W. W=0 ⇒ no tension-link.
  - Φ = faithful IIT-4 ONLY (a_phi_iit4_tool) via big_phi_bounded. n=3 member cap=3 exact,
    joint n=6 cap=2 lower-bound, sys_state=0. n≤8 faithful-Φ ceiling respected (NO n=9+).

ECA REFERENCE (H_1295, .verdicts/1295_hive_collective_phi/H_1295.txt):
  Σ = 4.99209 · Φ_joint = 15.4677 · Δ_ECA = +10.4756 · coherence 0.58125.

FROZEN BARS (W=0.6, MARGIN=+1.0, 3 seeds [1300,1301,1302]):
  B1 PRESENCE  : Δ_real = Φ(real-coupled) − Σ Φ(member) ≥ +1.0 every seed.
  B2 TRANSFER  : Δ_real > 0 (same SIGN as ECA Δ>0) ⇒ super-additivity REPRODUCES on the
                 real A⇄G substrate. Δ_real ≤ 0 ⇒ HONEST NULL (ECA artifact, no transfer).
  B3 DECOUPLE  : Δ(W=0) ≤ 0 (the lift IS the real tension-link coupling, not an artifact
                 of joining two state spaces).
  B4 SHUFFLE   : permute the real-state→cell readout map → per seed
                 (Δ_real − Δ_shuffle ≥ +1.0) OR (Δ_shuffle ≤ 0): the lift is the real
                 structured coupling, not generic variance.

VERDICT RULE:
  GREEN ENGINE-TRANSFER iff B1 ∧ B2 ∧ B3 ∧ B4 (4/0) → the hive result is REAL on live
    coupled anima A⇄G, not an ECA TPM artifact → update H_1295 card + FINDINGS hive row.
  HONEST NULL iff B1 fails or B2 fails (Δ_real ≤ 0) → collective-Φ super-additivity is an
    ABSTRACT-substrate property that does NOT transfer to real coupled anima (first-class
    closed-negative, c9). Record honestly; do NOT promote the ECA result as engine-verified.
  Otherwise NOT GREEN (a control failed) — report honestly, no over-claim.

p7: faithful IIT-4 only, never perplexity. $0 CPU mac-local. deterministic.
NO tune-to-green: these bars are frozen here, before any Φ is computed.
