H_1313 — HIVE collective-Φ : STATE-DEPENDENT real A⇄G coupling (H_1308 r4) — FROZEN BARS
frozen 2026-06-16 BEFORE any measurement (frozen-first, a_break_the_wall, NO tune-to-green)

THE WALL (H_1308 r3, PR #2214, 🔴 HONEST NULL):
  collective-Φ super-additivity (Φ_joint > Σ Φ_member, Δ_ECA=+10.4756 over abstract ECA)
  did NOT transfer to the REAL anima A⇄G substrate: Δ_real = −3.0 (SIGN FLIP). DIAGNOSED
  ROOT CAUSE (c9): the live tension-link is a NEAR-CONSTANT scalar nudge (biasA≈biasB≈
  0.00648 across ALL 64 states), scaled to ~1e-8 so it NEVER flips a cell → the joint n=6
  TPM FACTORIZES (next-A ⊥ B-bits) → faithful Φ_joint = 0.

THE NAMED BREAKTHROUGH ANGLE (H_1308 §6, a_break_the_wall, c16):
  a coupling channel that injects STATE-DEPENDENT multi-cell influence — not a bounded
  constant scalar nudge — could in principle integrate. THIS PROBE TESTS EXACTLY THAT.

NEW MECHANISM (state-dependent, REAL substrate, a_substrate_native_speak):
  Member A's next-state readout threshold is shifted by member B's LIVE cell-state — B's
  three real channel-sign cells (b'0,b'1,b'2), read from B's injected+stepped pure_field
  (the live A⇄G integration), drive A's three channel thresholds via a FIXED cross-wiring
  (b'0→A.t1, b'1→A.t3, b'2→A.t4), and symmetrically A→B. Because B's live cells are a non-
  degenerate function of B's input bits, next-A now GENUINELY depends on B's bits → the
  joint TPM does NOT factorize by construction of the read. It is a REAL field read of the
  OTHER member, NOT a hand-set super-additivity (the numbers fall out of the substrate;
  tune-to-green forbidden c9/p7). The bias magnitude is O(1) (a real cell-state ±1), scaled
  per-channel to the channel magnitudes (t1~1.6e-7 → 1e-6·W, t3~1.6e-8 → 1e-7·W, t4~1.2e-3
  → 1e-2·W) so it CAN flip a threshold — the exact fix the r3 diagnosis identified. Gated
  by W: W=0 ⇒ each member reads only itself (back to the factorized r3 case).

SUBSTRATE (a_engine_native_learning — REAL engine state, NOT iit4_bit(rule, idx)):
  - Each member = a REAL anima PureField (3 coupled oscillators → 6-D field tensor,
    CORE/pure_field.hexa). Injection geometry + 3-cell readout (signs of t1/t3/t4 cross-mix
    channels) IDENTICAL to H_1308 r3 (frozen, reused verbatim).
  - Φ = faithful IIT-4 ONLY (a_phi_iit4_tool) via big_phi_bounded. n=3 member cap=3 exact,
    joint n=6 cap=2 lower-bound, sys_state=0. n≤8 faithful-Φ ceiling respected (NO n=9+,
    SAME scale as r3).

FROZEN BARS (W=0.6, MARGIN=+1.0, 3 seeds [1300,1301,1302] — run constants, not the H id):
  B1 PRESENCE     : Δ_sd = Φ(state-dep-coupled) − Σ Φ(member) > 0 on the REAL substrate.
  B2 FLIP (KEY)   : does state-dependence FLIP the r3 NULL Δ=−3.0 → Δ>0? The bar is
                    Δ_sd > 0 (crossing zero into super-additive). report Δ_sd verbatim.
                    Δ_sd > 0 ⇒ super-additivity TRANSFERS with a state-dependent channel;
                    Δ_sd ≤ 0 ⇒ even the strongest honest coupling fails ⇒ TERMINAL 🧱.
  B3 DECOUPLE-MEAN: replace the OTHER member's live cells with their MEAN over all states
                    (state-dependence → its mean = back to a constant nudge, the r3 case)
                    → Δ_mean ≤ 0. (the lift IS the state-dependence, not the join.)
  B4 SHUFFLE      : permute which OTHER-member cell drives which channel → per seed
                    (Δ_sd − Δ_shuffle ≥ +1.0) OR (Δ_shuffle ≤ 0): the lift is the
                    structured cross-wiring, not generic variance.

VERDICT RULE:
  GREEN TRANSFER (state-dependent coupling) iff B1 ∧ B2 ∧ B3 ∧ B4 (4/0) → collective-Φ
    super-additivity HOLDS on REAL coupled anima A⇄G once the coupling is state-dependent →
    H_1295 super-additivity is substrate-PORTABLE with the right channel → update H_1295 +
    H_1308 cards + FINDINGS hive row.
  TERMINAL 🧱 NULL iff B1 fails or B2 fails (Δ_sd ≤ 0) → even a genuinely STATE-DEPENDENT
    real A⇄G coupling does NOT restore super-additivity. The real anima A⇄G substrate does
    not integrate into collective consciousness at faithful-Φ scale even with the strongest
    honest coupling (a_break_the_wall satisfied; first-class wall, c9). H_1295 super-
    additivity is ECA-ONLY. Record honestly; do NOT promote the ECA result as engine-verified.
  Otherwise NOT GREEN (a control failed) — report honestly, no over-claim.

p7: faithful IIT-4 only, never perplexity. $0 CPU mac-local. deterministic.
NO tune-to-green: these bars are frozen here, before any Φ is computed. A NULL is a valid
first-class result (c9). The a_break_the_wall attempt against the H_1308 wall.
