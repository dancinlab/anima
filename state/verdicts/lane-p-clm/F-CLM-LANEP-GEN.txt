=== Lane P (substrate=GPU-torch) — TRAIN/VAL SPLIT generalization test (memorization caveat resolved) ===
date: 2026-06-03
verdict: F-CLM-LANEP-GEN = 0  (MEMORIZATION — honest closed-negative, a_paper_negative_ok)
scope: GPU-torch Lane P (distinct from Lane A AKIDA / Lane G forge — a_lane_akida_gpu_split).
       p7/g63 honest — verbatim trainer stdout, NO fabricated convergence. A memorization
       finding is a VALID honest result; this did NOT force a pass.

WHY THIS RUN EXISTS (the caveat being settled):
  The landed F-CLM-LANEP-TRAIN reported final_eval_ce=0.09862 on the 402,270-byte corpus,
  with a "held-window mean over 40 batches" eval. But that eval (train_lane_p.py eval_ce)
  drew RANDOM windows from the SAME byte stream the model trained on (make_batch over the
  whole stream) — so the eval data OVERLAPS the training data. 0.0986 could be MEMORIZATION,
  not generalization (a_scale_honest_scope caveat). This run settles it with a strict
  train/val split where NO training window ever touches a held-out byte position.

CORPUS (bigger + real multilingual — FLORES-200, same 5 langs as the 402KB corpus):
  built  = FLORES-200 dev + devtest, langs eng_Latn/zho_Hans/rus_Cyrl/jpn_Jpan/kor_Hang
           (ungated fbaipublicfiles NLLB tarball, real parallel multilingual eval text),
           concatenated to one UTF-8 byte stream by CLM/corpus/build_flores5_corpus.py.
  path   = CORE/testdata/flores5_dev_devtest.txt
  bytes  = 1,654,010   (1.65 MB — 4.11x the landed 402,270-byte corpus)
  sha256 = 970cd3796c3b5590a0618e30e5d8a30640b0f91b16d1c9180ff3c66cc8f20c43

SPLIT (held-out NEVER trained; leak-checked):
  train_bytes      = 1,323,136  (80.0%)
  contig_val_bytes =   165,401  (10.0%)  — single contiguous 10% block at 70% through stream
  rand_val_bytes   =   164,864  (10.0%)  — 161 random-scattered mini-blocks (distribution-matched)
  LEAK_CHECK pass: every held-out segment is position-disjoint from every train segment, and
  training windows are sampled ONLY inside train segments (boundary-crossing windows excluded).
  So no training input or target byte ever lands in a held-out position. (verbatim stdout:
  "SPLIT train_bytes=1323136 (80.0%) contig_val_bytes=165401 (10.0%) rand_val_bytes=164864
   (10.0%) train_segs=160 rand_blocks=161" / "LEAK_CHECK pass").

GPU + nvidia-smi BUSY PROOF (g63 — NOT silent CPU-fallback):
  device  = NVIDIA GeForce RTX 5070 (compute_cap 12.0, sm_120 Blackwell)
  host    = pool host aiden (192.168.50.119) — $0 marginal (no rented pod); torn down after recover.
  torch   = 2.12.0.dev20260408+cu128
  busy    = nvidia-smi sampled DURING the run: util=94%, mem_used=1178MiB, temp=65C,
            compute-app python3 PID 2835319 holding 1136MiB (GPU-resident, not CPU).
  note    = CLM_NO_CUDNN=1 (host cuDNN/torch-nightly CUDNN_STATUS_SUBLIBRARY_VERSION_MISMATCH
            on conv1d) routes conv to the native CUDA kernel — same math, no cuDNN. CUDA still
            REQUIRED (assert torch.cuda.is_available()); CE-descent measurement unaffected.

CONFIG (same as the landed .clm):
  d768 E2 L1 K3 V256, 7,479,042 params (7.479M), steps=6000, seq_len=256, batch=64,
  lr=3e-4 cosine+warmup200, bf16 autocast, AdamW(0.9/0.95, wd 0.01), grad-clip 1.0, wall=256.36s.

MEASURE (the verdict — VERBATIM, NO fabrication):
  first_ce       = 5.73752   (~= uniform ln256 = 5.54518, untrained)
  train_ce       = 0.61436   (mean over 40 train-split eval batches)
  val_ce_contig  = 1.11929   (held-out contiguous block)  GAP_CONTIG = +0.50493
  val_ce_rand    = 1.81846   (held-out random scatter)    GAP_RAND   = +1.20410
  val_ce(worst)  = 1.81846                                 GAP        = +1.20410
  rel_gap        = 1.95993   (held-out CE ~2.96x train CE)
  BASELINES:  uniform_ce = 5.54518 (ln 256)   shuffle_ce = 9.58354 (byte-shuffled stream)
  descent (step, train-batch ce): 0:5.73752 -> 200:1.72606 -> 1000:1.18957 -> 2000:0.98207
    -> 3000:0.81614 -> 4000:0.72765 -> 5000:0.61054 -> 5999:0.61452. Genuine descent, then
    train CE plateaus ~0.61 while held-out CE stays 1.12-1.82.

VERDICT REASONING:
  GENERALIZES gate (pre-registered): val_ce < 0.5*uniform AND rel_gap <= 1.0 AND val_ce < shuffle_ce.
  -> rel_gap = 1.96 > 1.0  => gate FAILS => F-CLM-LANEP-GEN = 0 (MEMORIZATION).
  The held-out CE (1.12 contig / 1.82 rand) is 1.8x-3.0x the train CE (0.61) — a clear
  overfitting signature. The model fits its training windows substantially better than
  held-out positions. (Both held-out splits stay below uniform 5.545 and far below shuffle
  9.584, so the model has learned SOME real byte structure — it is not pure noise — but it
  does NOT generalize at the rate it fits train.)

CONFIRMS THE LANDED CAVEAT WAS REAL:
  On a properly-split 1.65MB corpus the SAME d768 E2/L1 config cannot even drive TRAIN CE below
  ~0.61 (vs the landed 0.0986 on the 402KB corpus). The landed 0.0986 was achievable ONLY because
  the 402KB corpus is small enough that random windows over the whole stream repeat heavily and
  overlap the eval windows — i.e. the landed "held-window" eval measured fit-to-seen-data, not
  generalization. The low landed CE was OVERFITTING / eval-leakage, exactly as the caveat flagged.

ARTIFACTS:
  trainer:  CLM/train/train_lane_p_split.py  (strict-split trainer + leak assertion)
  corpus:   CLM/corpus/build_flores5_corpus.py  -> CORE/testdata/flores5_dev_devtest.txt
  result:   state/lane_p_clm_gen/result_gen.json  (full verbatim JSON incl. descent)
  log:      state/lane_p_clm_gen/train_gen.log    (verbatim trainer stdout)
  .clm:     state/lane_p_clm_gen/clm_d768_gen.clm (4,463,478 B, sha256
            c33045ac21b9251e7685e432818adf9397d49cba16fe5231b21033cfca18c08f) — serialized for
            record; NOT promoted (memorizing model). ckpt .pt 29,922,211 B gitignored (local-only).

HF: NOT promoted. The prior PUBLIC d768 (F-CLM-LANEP-TRAIN clm sha 7463282d...) stays as-is;
    this generalization probe resolves the caveat (memorization confirmed) and does not publish
    a memorizing model as PUBLIC (a_hf_autonomous: negative-result -> not PUBLIC).
