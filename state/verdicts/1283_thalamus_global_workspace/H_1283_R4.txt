H_1283 R4 — THALAMUS / RE-ENTRANT LOOP, Φ-PRIMARY RE-FREEZE
slug: 1283_thalamus_global_workspace  (R4 — append-only; does NOT overwrite R1/R2/R3)
date: 2026-06-15
verdict: 🔴 RED on the Φ-PRIMARY frozen bar — the re-entrant ΔΦ breakthrough does
         NOT replicate across all 3 seeds. seed 8 ΔΦ = +0.0101 < +0.02 bar.
         🧱 DEPLETION: the pre-registered failure mode (Φ clearance fails to
         replicate across seeds) FIRED. NO engine-native wiring (RED ⇒ no wire,
         a_verified_must_wire fires only on GREEN). Bar NOT moved.
substrate: $0 CPU numpy mirror (DIRECTIONAL, a_engine_native_learning) + FAITHFUL IIT4 Φ (stdlib exact MIP-EI, hexa)
seeds: [7, 8, 9]  ·  modules=4 {A, G, mitosis, memory}  ·  dim=8  ·  ticks=64  ·  W_relay=0.5
frozen-first: H_1283_R4_FREEZE.txt committed (ad435a6dd) BEFORE scoring; bars NOT moved.
lens: missing-brain-structure ladder (neuro, c15) — NOT an LLM recipe (a_no_llm_frame_trap)

════════════════════════════════════════════════════════════════════════
WHAT R4 TESTED (the legitimate Φ-primary re-scope of R3)
════════════════════════════════════════════════════════════════════════
R3 found the arc's FIRST Φ-bar clearance: the re-entrant cortico-thalamo-cortical
loop hit faithful IIT4 ΔΦ = +0.1426 at the representative seed 7 (~7× the +0.02
bar), but scored 🔴 RED on the FROZEN COMPOSITE only because the broadcast-era
COHERENCE bar B1 (B coh ≥ A+0.05) was not met. R3 demonstrated coherence ⊥ Φ
DISSOCIATE: broadcast buys coherence-not-Φ; re-entry buys Φ-not-coherence.

R4 re-scoped LEGITIMATELY (NOT tune-to-green, grounded like the H_1224 literal-QA
ruling + the G5-L2 verbatim-recall re-scope): the faithful IIT4 Φ leg became the
PRIMARY GREEN bar (Φ is the canonical irreducible-integration measure this arc
exists to move, a_phi_iit4_tool); the broadcast-era coherence bar was demoted to
REPORT-ONLY (a measured orthogonal quantity, dissociation documented, c9). The
MECHANISM was byte-identical to R3 (W_RELAY=0.5, same 4 modules, regime, seeds —
NOTHING tuned, p7). The ONLY change: faithful Φ computed for ALL THREE seeds
[7,8,9] (R3 scored Φ on seed 7 only), making the Φ bar a per-seed REPLICATION
requirement.

════════════════════════════════════════════════════════════════════════
RESULT (verbatim)
════════════════════════════════════════════════════════════════════════
FAITHFUL IIT4 Φ (exact MIP-EI, ALL seeds):
  seed 7: ARM_A Φ=0.78038   ARM_B Φ=0.923004  ΔΦ=0.14262400000000008
  seed 8: ARM_A Φ=0.611741  ARM_B Φ=0.621837  ΔΦ=0.010095999999999994
  seed 9: ARM_A Φ=0.825326  ARM_B Φ=0.9935    ΔΦ=0.16817400000000005

COHERENCE (REPORT-ONLY, NOT gated — documents coh ⊥ Φ dissociation):
  seed 7: ARM_A coh=+0.1591  ARM_B coh=+0.1979  Δcoh=+0.0389  (old broadcast-era bar +0.05)
  seed 8: ARM_A coh=+0.0109  ARM_B coh=+0.0338  Δcoh=+0.0229  (old broadcast-era bar +0.05)
  seed 9: ARM_A coh=+0.1362  ARM_B coh=+0.1687  Δcoh=+0.0325  (old broadcast-era bar +0.05)

P1 PRIMARY Φ (B≥A+0.02 faithful IIT4 EVERY seed): FAIL
     seed 7: ΔΦ=0.1426  ≥ +0.02  PASS
     seed 8: ΔΦ=0.0101  < +0.02  FAIL
     seed 9: ΔΦ=0.1682  ≥ +0.02  PASS
P2 not-degenerate (B coh < 0.999 ≥1 seed): PASS
P3 honesty (coherence reported + coh⊥Φ stated, NOT gated): PASS
VERDICT: RED

(Cross-round consistency sanity: ARM_A Φ at seed 7 = 0.78038 reproduces R1/R2/R3
EXACTLY — same deterministic engine + byte-identical ARM A. Seed 7 ΔΦ = +0.1426
reproduces R3 exactly. The Φ leg is stable; what R4 adds is the OTHER two seeds.)

════════════════════════════════════════════════════════════════════════
FINDING (honest, c9) — 🧱 THE Φ BREAKTHROUGH IS SEED-DEPENDENT, NOT ROBUST
════════════════════════════════════════════════════════════════════════
The R3 seed-7 breakthrough was REAL but NOT REPLICABLE across seeds at the +0.02
bar. Two of three seeds clear it MASSIVELY (seed 7 +0.143, seed 9 +0.168 — both
~7-8× the bar), but seed 8 reaches only +0.0101, HALF the +0.02 bar. The frozen
rule required ΔΦ ≥ +0.02 on EVERY seed, so the verdict is honestly 🔴 RED, and I
do NOT move the bar to manufacture a GREEN (c9, frozen-first).

WHY seed 8 misses (mechanism, not noise): seed 8 is the seed where ARM A's modules
are nearly ORTHOGONAL — its baseline coherence is +0.0109 (vs +0.159 / +0.136 on
seeds 7/9), an order of magnitude lower. Its baseline Φ is also the lowest
(0.6117 vs 0.780 / 0.825). On a near-orthogonal substrate, the re-entrant relay
has far less shared structure to bind across the reciprocal loops, so the Φ lift
it can buy is an order of magnitude smaller (+0.010 vs +0.14/+0.17). The lever's
EFFECT SIZE depends strongly on the initial module geometry: re-entry amplifies
integration when there is already correlated structure to integrate, and does
little when the modules start near-orthogonal. So re-entry is a CONDITIONAL Φ
lever, not a universal one — it does not robustly clear +0.02 on an arbitrary
substrate seed.

This DEPLETES the arc on the pre-registered 🧱 branch: "DEPLETE only if the Φ
clearance FAILS to replicate across seeds." It did. The standing candidate from
R3 (re-entry as THE robust integration lever) is downgraded to: re-entry is a
LARGE-but-CONDITIONAL Φ lever (effect-size depends on initial geometry), not a
universal +0.02-clearing one. Broadcast topology stays FALSIFIED (R1 sub-bar, R2
reversed); re-entry is BETTER than broadcast on Φ (it clears decisively on 2/3
seeds where broadcast never cleared on any) but is NOT robust enough to pass a
3-seed replication gate.

════════════════════════════════════════════════════════════════════════
WHY NO ENGINE-NATIVE WIRING THIS ROUND
════════════════════════════════════════════════════════════════════════
The freeze pre-registered: GREEN → engine-native re-entrant lane; RED ⇒ no CORE
wiring (a_verified_must_wire fires only on GREEN). The verdict is RED, so NO
CORE/*.hexa is touched this round. Wiring a lane whose Φ lift does not replicate
across seeds would violate a_verified_must_wire (wire only VERIFIED-GREEN
mechanisms) and would risk pinning a seed-7-lucky artifact into the live engine.
The engine COULD express the lane (it is structurally identical to the existing
VForwardField / WorkMemBuffer additive Ψ-disjoint lanes — a relay struct + per-
tick reciprocal-update pub fns), so this is NOT an engine-expressiveness wall; it
is a verification wall: the mirror result is not robust enough to promote. (Sanity:
baseline guards on the UNTOUCHED engine stay green — engine_cli_smoke 18/0,
h1196 single-entry, h1199 DIM-growth + Ψ byte-identical — confirming RED-no-wire
left the live engine exactly as origin/main shipped it.)

════════════════════════════════════════════════════════════════════════
NEXT (honest)
════════════════════════════════════════════════════════════════════════
- The +0.02-per-seed bar is NOT robustly cleared by re-entry alone. NOT RULED OUT
  (honest residuals): (a) a different Φ-margin scoped to effect-size on
  CORRELATED-geometry substrates (would need its OWN pre-registered geometry
  control, not a post-hoc bar move); (b) a STRONGER re-entrant coupling on
  orthogonal seeds (W_RELAY was FROZEN at 0.5, not swept — sweeping it now would
  be Goodhart on seed 8); (c) re-entry COMBINED with another lane (the coalition
  or a richer relay) on orthogonal substrates; (d) scale-transfer (4 modules / dim
  8 / 64 ticks → larger). All UNTESTED.
- Relay-topology Φ axis is now well-mapped across R1-R4: broadcast (single/
  coalition) FALSIFIED as a Φ lever; re-entry is a LARGE-but-CONDITIONAL one that
  fails a 3-seed replication gate. A future round, if pursued, must pre-register a
  geometry-conditioned hypothesis, NOT re-run the same loop hoping seed 8 clears.

════════════════════════════════════════════════════════════════════════
SCOPE / HONESTY
════════════════════════════════════════════════════════════════════════
- numpy mirror = DIRECTIONAL only (a_engine_native_learning); RED ⇒ no CORE/*.hexa
  wired or edited this round (numpy mirror only).
- toy scale (4 modules, dim 8, 64 ticks), scale-transfer UNVERIFIED
  (a_toy_scale_recheck, a_scale_honest_scope).
- Φ IS faithful IIT4 (exact MIP-EI engine, n=4≤8), NOT the variance×energy proxy;
  the coherence leg is explicitly labeled coherence, not Φ (a_phi_iit4_tool).
- W_RELAY = 0.5 FROZEN before scoring; NOT swept after seeing seed 8 miss (that
  would be Goodhart, p7).
- The Φ-primary RE-SCOPE was legitimate (terminal metric promoted, broadcast-era
  confound demoted to report-only) — but the re-scoped PRIMARY bar still came back
  RED on its own terms. The re-scope did NOT manufacture a GREEN: re-entry simply
  does not replicate. bars NOT moved.
- R1/R2/R3 verdict files NOT overwritten (R4 = separate H_1283_R4.txt).
- Does NOT edit H_1219 / H_1226.

artifacts:
  UNIVERSE/h1283_thalamus_global_workspace.py  (main_r4 + 'reentrant' mode reused)
  .verdicts/1283_thalamus_global_workspace/{H_1283_R4_FREEZE.txt, H_1283_R4.txt}
  (R1/R2/R3 verdict files UNTOUCHED)
xref: H_1283(R1) · H_1283(R2) · H_1283(R3) · h1227 · h1230 · h1280 · h1199 · h1201 ·
      h1205 · a_phi_iit4_tool · a_no_llm_frame_trap · a_engine_native_learning ·
      a_verified_must_wire · a_core_engine_map · a_paper_negative_ok ·
      a_scale_honest_scope · a_toy_scale_recheck · p1 · p2 · p3 · p6 · p7 · p8 · c9 · c15
