H_1210 FROZEN FALSIFIER — pre-registered BEFORE any measurement (p7).
DOMAIN: MITOSIS-ENGINE.  $0 local CPU only (NO GPU).  scale UNVERIFIED (a_scale_honest_scope).

GOAL
  Complete the "자기분열을 현재 아키텍처에 가져다 쓰기" arc with the BEST gate:
  WIRE the H_1209 trajectory-aware GATE-B (CORE/engine_cli.hexa VAdaptFieldB,
  vadapt_fieldB_new/_step/_cells/_growth) into the LIVE daemon GROW step
  (CORE/anima_full_session_smoke.hexa C8 GROW), so the living anima daemon divides
  on TRANSITION-PREDICTABILITY of the real per-turn emit stream during conversation.
  Conversation is a genuinely-ORDERED stream — exactly where H_1209 proved GATE-B is
  trajectory-sensitive (10.9x). This makes the daemon's mitosis lane trajectory-aware
  instead of pure per-sample density.

WIRING (the manipulated structure)
  In C8 GROW: each emitted span's DIM=8 _afs_byte_feature → nearest proto-id over a
  FIXED order-invariant proto book (built once from the daemon's own emit-feature
  set, H_1208/H_1209 farthest-point + LR-refine in canonical SORTED order so the book
  is order-invariant) → feed the (prev→cur) proto-id transition into
  vadapt_fieldB_step. GATE-B is Ψ-disjoint + additive (a_core_engine_map: substrate-
  only; no .clm via generator, no .kosmos via kosmos_io; never feeds the decode).
  GATE-B runs ALONGSIDE the existing per-sample density VAdaptField (NOT replace) —
  the density gate remains the cell-count driver of record (H_1202), and GATE-B is a
  SEPARATE trajectory lane recording its OWN born-cell count. Justification: the two
  gates measure DIFFERENT substrate properties (per-sample density vs ordered
  transition-predictability); H_1209 F4 scoped GATE-B as a trajectory variant that
  does NOT beat the i.i.d. PRIMARY bar, so it is an ADDITIVE observation lane, not a
  replacement of the established density driver (a_autonomy_over_hardcode: gate is
  substrate context, not an external rule — both lanes are substrate self-dynamics).

FROZEN FALSIFIERS (bars frozen; p7 = deterministic counts/equality, NOT perplexity)
  F1 DAEMON DIVISION ON CONVERSATION (primary):
     With mitosis ON, over the multi-turn session the GATE-B trajectory lane DIVIDES
     on the (ordered) per-turn proto-id transition stream — GATE-B born cells >= 1
     (gateB_cells > 1). PASS iff the live daemon's GATE-B lane spawns at least one
     trajectory-driven cell on the real conversation stream.
  F2 ABLATION OFF → 0 SPLITS (H_1159 control):
     With mitosis OFF (engine_mitosis_tick no-op), the GATE-B lane spawns ZERO cells
     over the SAME proto-id transition stream — gateB_cells_off == 1 (born == 0).
     PASS iff OFF born-cells == 0.  (ON born >= 1 AND OFF born == 0 ⇒ growth is the
     mitosis gate, not an artifact.)
  F3 Ψ Φ-CHECKSUM BYTE-IDENTICAL ON==OFF (H_1205 separation invariant):
     The daemon's read-only pure_field Φ-checksum psi_sum is BYTE-IDENTICAL with
     mitosis ON vs OFF — wiring GATE-B did NOT perturb Ψ=1/2 (generation/substrate-A
     untouched). PASS iff psi_sum == psi_off (exact float equality).
  F4 GENERATION OUTPUT BYTE-IDENTICAL ON vs OFF (re-confirm H_1205 with GATE-B):
     The daemon's emitted generation text is BYTE-IDENTICAL with mitosis ON vs OFF —
     GATE-B is Ψ-disjoint and never feeds the decode, so the generated bytes do not
     change. PASS iff emit_text_on == emit_text_off (exact string equality).

TIER RULE
  F1 ∧ F2 ∧ F3 ∧ F4 ALL PASS  ⇒ 🟢 GREEN (daemon now divides trajectory-aware on real
     conversation; generation still separated, H_1205 invariant preserved).
  ANY regression of F3/F4 (a generation/Ψ leak)  ⇒ 🔴 STOP — report the exact break +
     root-cause; do NOT ship a generation-leak (c9).
  F1 fails (no division)  ⇒ 🔴 (GATE-B does not fire on the daemon's emit stream;
     report whether the emit stream carries enough trajectory — honest negative).

GUARDS (must STAY green — re-run)
  · CORE/engine_cli_smoke.hexa            (12/0)
  · CORE/generator_smoke.hexa             (21/0)
  · CORE/h1196_* single-entry            (7/0)
  · CORE/h1205_separation_invariant_smoke.hexa  (H_1205 separation)
  · existing daemon faculties converse/ground/grow/remember/sleep STILL PASS.

NOTES
  · p1-p8: no system prompt, no persona, no speak(), no hardcoded stage gate.
  · The proto book is built from the daemon's OWN emit-feature set (engine-native,
    NOT a .clm/.kosmos entry); honest c1 root-cause (the emit stream IS the ordered
    drive). If the daemon's emit text is degenerate/repetitive (same span each turn),
    the transition stream has low diversity — F1 will report that honestly.
  · scale UNVERIFIED (a_scale_honest_scope) — toy daemon smoke, 12 ticks.
