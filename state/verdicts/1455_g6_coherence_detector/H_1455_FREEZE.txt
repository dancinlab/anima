H_1455 FREEZE — G6 COHERENCE-DETECTOR upgrade (clause-cosine), frozen BEFORE scoring
slug: 1455_g6_coherence_detector · date: 2026-06-19
goal: make the FROZEN H_1305 FALS detector a STRICTER superset so cross-shuffle (random comparator×measurable pairing) actually COLLAPSES — the missing discriminator that left H_1431/1434 PARTIAL (FALS survives shuffle = template, not binding).
mechanism (p7, anima-native, anti-Goodhart STRICTER): keep _g6_is_falsifiable (a)(b)(c) VERBATIM, ADD (d) COHERENCE: embed the comparator-clause and the measurable-clause via the engine's OWN immune_embed_key (byte-trigram FNV-1a, DIM=64, the immune store geometry) and require cosine >= τ. A single coherent claim's two clauses share subject geometry; a cross-shuffle weld of two different ideas does not.
EXPLORATORY STEP (this run): MEASURE clause-cosine for the 5 designed-falsifiable calibration claims vs cross-shuffle welds. FROZEN PASS CRITERION for the upgrade to be VIABLE: mean(coherent cosine) − mean(cross-shuffle cosine) >= +0.10 AND a τ exists keeping all 5 calibration pos >= τ while all cross-shuffle < τ. If NO separation → embedding-coherence does NOT discriminate → honest negative, name next lens (NO τ tuned to manufacture it, c9).
