H_1430 — CONCEPT / CATEGORY PROTOTYPE-ABSTRACTION (HD33). R1 numpy MIRROR RESULT.
Run on aiden pool host (c17 — NOT mini). $0 CPU numpy 2.4.6, gradient-free, deterministic.
Frozen designs: H_1430_FREEZE.txt (R1) · H_1430_R1b_FREEZE.txt · H_1430_R1c_FREEZE.txt (final).
================================================================================
VERDICT (verbatim): 🧱 MEASUREMENT-WALL / PARTIAL (DIRECTIONAL numpy mirror).
  The prototype-abstraction faculty is REAL and demonstrated — it shows the signature
  Posner-Keele prototype-enhancement effect (c2b PASS), beats the exemplar/item-store on
  novel instances (c2 PASS), and BOTH anti-Goodhart controls collapse (c3 shuffle, c4
  ablate, PASS) — distinct from ImmuneMemory item-binding (H_1227/1231/1288) and SpatialMap
  metric positions (H_1296). But the FROZEN 5-bar conjunction does NOT all-pass: c1 PRESENCE
  FAILS because B's STRONG absolute novel accuracy (≥0.55) and the prototype-enhancement
  SIGNATURE (c2/c4, which need the exemplar store to fail the clean prototype) require
  OPPOSITE noise regimes and are structurally anti-correlated. Honest 🧱 (c9, c16 type-a
  measurement/operationalization wall) — VALID result, not a defeat. NOT engine-wired
  (5-bar not all-green). a_engine_native_learning R2 binding verdict not reached.

--------------------------------------------------------------------------------
R1c FINAL BAND (Posner-Keele high-distortion, multi-dim sphere prototypes) — 3 seeds
--------------------------------------------------------------------------------
seed 4430: NOVEL A=0.250 B=0.469 Bshuf=0.062 Babl=0.250 | PROTO A=0.750 B=1.000 Babl=0.750 | TRAIN A=1.000 B=0.781 Babl=1.000
seed 4431: NOVEL A=0.188 B=0.344 Bshuf=0.062 Babl=0.188 | PROTO A=0.750 B=1.000 Babl=0.750 | TRAIN A=1.000 B=0.781 Babl=1.000
seed 4432: NOVEL A=0.219 B=0.312 Bshuf=0.188 Babl=0.219 | PROTO A=0.750 B=1.000 Babl=0.750 | TRAIN A=1.000 B=0.750 Babl=1.000
MEAN NOVEL: A=0.219 B=0.375 Bshuf=0.104 Babl=0.219  (chance=0.250)
MEAN PROTO: A=0.750 B=1.000 Babl=0.750
MEAN TRAIN: A=1.000 B=0.771 Babl=1.000
B far-foil abstain rate: 1.000

  c1 PRESENCE      B-chance>=0.30 (each+mean): FAIL  (B=0.375, chance=0.250; need B>=0.55)
  c2 DISTINCT      B-A>=0.10 & A no-enhance  : PASS  (B-A=+0.156; A_proto=0.750<A_train=1.000)
  c2b PROTO-EFFECT B_proto>=B_train          : PASS  (B_proto=1.000 >= B_train=0.771)
  c3 EARNED-CAT    Bshuf-chance<=0.10        : PASS  (Bshuf=0.104)
  c4 EARNED-ABSTR  ablate no-enhance & drop  : PASS  (Babl_proto=0.750<Babl_train=1.000;
                                                      Babl_novel=0.219<=B-0.05=0.325)
  c5 NO-FAB        far-abstain>=0.90         : PASS  (abstain=1.000)
  => 5/6 PASS; c1 FAIL => NOT all-green => 🧱.

--------------------------------------------------------------------------------
THE BINDING FINDING (why 🧱, not 🟢) — c1 ⊥ prototype-effect, measured
--------------------------------------------------------------------------------
A diagnostic sweep on aiden (recorded) shows the anti-correlation is STRUCTURAL, not a
band accident:
  - LOW noise (R1 band, NOISE=1.0): the task is trivial; a 1-NN EXEMPLAR store ALSO nails
    the clean central prototype (A_proto=1.000) so the exemplar does NOT fail the
    enhancement test -> c2/c4 sub-clauses FAIL by saturation (B has no headroom).
  - HIGH noise (R1c band, NOISE=2.5): training items scatter so widely that no stored
    exemplar lands near the clean central prototype (A_proto=0.750 < A_train=1.000) -> the
    exemplar FAILS the enhancement test (c2/c4 PASS), the centroid's noise-averaging
    advantage appears (B_proto=1.000, B beats A on novel) -> but B's ABSOLUTE novel
    accuracy is only modestly above chance (0.375) -> c1 FAILS.
  - Scaling SEP / N_TRAIN to push B above 0.55 (clear c1) ALSO lets exemplars cover the
    prototype again (A_proto -> 1.000), re-breaking c2/c4 (measured: SEP=8 N_TRAIN=16
    gives B=0.969 but A_proto=A_train so A_fail_enh=False).
The two conditions live in mutually exclusive noise regimes. The frozen conjunction
(c1 ∧ c2 ∧ c2b ∧ c3 ∧ c4 ∧ c5) is UNSATISFIABLE in any well-posed regime of this
synthetic geometry.

WHAT IS GENUINELY ESTABLISHED (the positive content of the 🧱):
  1. The prototype/centroid abstraction GENERALIZES to novel never-seen instances above
     what the exemplar store achieves (B-A=+0.156, c2 main clause) — generalization-by-
     abstraction is real and DISTINCT from item-binding.
  2. The Posner-Keele PROTOTYPE-ENHANCEMENT EFFECT holds for the abstraction (c2b PASS):
     the never-shown centroid is classified BETTER than the trained noisy instances
     (B_proto=1.000 > B_train=0.771). This is the decisive prototype-effect bar.
  3. The lift is EARNED: shuffle (label permutation) collapses it to chance (c3), ablating
     the centroid to exemplar mode kills the enhancement AND drops novel accuracy (c4).
  4. NO-FAB: far foils are abstained 100% (c5).
WHAT FAILS: the STRONG-PRESENCE absolute bar (c1) cannot co-occur with the cross-model
enhancement signature in this geometry — a measurement-axis wall, not a missing faculty.

--------------------------------------------------------------------------------
R1 + R1b (degenerate slices — recorded, frozen-first honesty)
--------------------------------------------------------------------------------
R1  (NOISE=1.0, axis-floor protos): all arms 1.000, c2/c4 FAIL by saturation -> RED.
R1b (NOISE_TEST=4.0, ABSTAIN_D=10): all novel abstain to 0.000, only B_proto=0.917 survives
    (the abstraction recovers the clean prototype where exemplars at radius ~14.7 abstain) -> RED.
Both surfaced the well-posedness defects fixed (geometry only, bars unchanged) in R1c.

DISTINCT axes (frozen, confirmed against live lane cards):
  - vs ImmuneMemory item-binding (H_1227/1231/1288): the immune store binds each instance
    INDEPENDENTLY (FNV-trigram key affinity); it memorizes trained items and abstains/guesses
    on a NOVEL one; it CANNOT abstract a category centroid. Mirrored by arm A (exemplar 1-NN):
    A memorizes (train=1.000) but fails novel-far + the clean prototype.
  - vs SpatialMap metric positions (H_1296): holds positions of SPECIFIC landmarks for a
    relational distance query; it is not a learned class boundary over a feature DISTRIBUTION
    and abstracts no centroid.
The centroid abstraction is orthogonal to both — confirmed by c2/c2b/c4 (the abstraction does
what neither item-binding nor a metric map does), even though the full 5-bar conjunction walls.

SCOPE (a_scale_honest_scope · a_toy_scale_recheck): TOY synthetic gaussian prototypes / 4 cat
/ DIM 24 / 3 seeds / deterministic centroid readout (tests the ABSTRACTION STRUCTURE, not a
trained net). scale / real-corpus / high-D / hierarchical categories / engine-native transfer
UNVERIFIED. R1 mirror = DIRECTIONAL; an engine-native R2 would be the binding verdict but is
NOT pursued (5-bar walls at the mirror). p1/p2/p3/p6: every arm reads ONLY learned
centroids/exemplars + the test vector; NO injected label/persona/identity/RLHF; geometry,
scored only.
================================================================================
