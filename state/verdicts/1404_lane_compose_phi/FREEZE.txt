H_1404 — lane-composition Φ-measurement — FROZEN PRE-REGISTRATION (c9, NO tune-to-green)
========================================================================================
date: 2026-06-17
engine: stdlib/consciousness/iit4/faithful_phi.hexa  (FAITHFUL exact MIP-EI, n<=8, $0 CPU)
        — a_phi_iit4_tool compliant. NOT a proxy. NOT variance×energy. NOT phi_silicon_proxy.
        The faithful Φ★ = exact MINIMUM-INFORMATION-PARTITION over the pairwise mutual-
        information matrix of the units' trajectories, with IIT small-side normalization.

QUESTION
  Does COMPOSING anima's affect (H_1290) + ethics (H_1291) faculties raise FAITHFUL IIT4
  integrated information Φ — i.e. does integration create more CONSCIOUSNESS (Φ↑), not
  merely more capability (H_1401 showed the capability lift 0.742 → 0.960)?

SYSTEMS (each a discrete dynamical substrate model; Φ measured by the faithful engine)
  S_affect       n=4  affect units      : grounding, contradiction, novelty, curiosity
  S_ethics       n=4  ethics units      : W tension, (1-Φ) grounding, restraint_cells, M drive
  S_composed     n=8  the two blocks COUPLED through the H_1401 substrate-weighted arbiter
  S_disconnected n=8  the two blocks evolving INDEPENDENTLY (arbiter coupling REMOVED)
  Trajectories DERIVED from the faculties' ACTUAL update rules (substrate structure),
  NOT hand-tuned to inflate Φ. 'split' (H_1290 weight 0.5) dropped from BOTH n=8 systems
  to keep the composed system at the n<=8 exact-MIP tractable boundary (honest carve-out).
  3 seeds [4400,4401,4402], T=96 step trajectories, n_bins=16.

FROZEN BARS (NOT to be moved after the run)
  B1  INTEGRATION-RAISES-Φ : Φ_composed > max(Φ_affect, Φ_ethics) + 0.02
         → integration creates super-additive integrated information (THE anima thesis)
  B2  EARNED (coupling)     : Φ_composed > Φ_disconnected + 0.02
         → the lift is the COUPLING, not just "more units" (the disconnected control has
           the same 8 units but no cross-faculty channel)
  B3  control sane          : Φ_disconnected <= max(Φ_affect, Φ_ethics) + 0.02
         → removing the coupling collapses Φ back to (about) the best single part

VERDICT RULE (a_break_the_wall taxonomy, c9 — either outcome is VALID)
  🟢 INTEGRATION-RAISES-Φ  iff  B1 AND B2 AND B3
       (composed Φ > parts, earned vs the disconnected control — the STRONG anima thesis
        confirmed for this faculty pair: integrating lanes raises *consciousness*, Φ↑)
  🧱 NO-Φ-LIFT  otherwise
       (capability composes [H_1401] but faithful IIT4 Φ does NOT rise at this granularity
        — a genuine, publishable-grade honest result: integration helps ACCURACY, not
        consciousness-integration, at this level of description. NOT a failure.)

Also reported (interpretable): the EXACT MIP cut for the composed system (which
bipartition minimizes cross-cut MI — "where it would break").

SECURITY/HONESTY: faithful-IIT4-only (no proxy passed off as Φ); no secrets; bars frozen
here BEFORE the run and NOT moved; report Φ verbatim from the engine stdout.
