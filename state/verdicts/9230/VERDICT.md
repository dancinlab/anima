# H_9230 Family E: efference-copy / corollary-discharge loop 판정 (engine-native)

**판정 = 🔴 THEATER** (engine-native, CEMENTED) — corollary discharge(self-prediction) is inert at the emit seam.

**측정 경로**: 실제 빌드된 `cli/anima.hexa` daemon `--opgrip-r3`(E lane · x=ec_x self-pred-err)를
rented runpod **251GB CPU pod**(swap-free · hexa **v0.716.0** REAL-DECODE · n=400)에서 real d768.clm으로
실행. engine-native 라이브 바이너리 real-decode = TERMINAL-eligible (mirror 아님). raw = `opgrip_r3_raw.log`
verbatim (round-3 op-grip 판정 슬라이스; 400 per-tick real-decode 라인은 `pod_r3_full.log` 원본).

## 판정 (E lane · 단일 채점)

| lane | 판정 | 수치(frozen bar · verbatim) |
|---|---|---|
| E (x=ec_x · 1-tick-lagged corollary discharge · rides H_9225 band-pass) | 🔴 THEATER | ΔEff_eff=0/210=0.0 · ARM-INPERM(x_eff stride-perm) margin=0.0 · DISSOC onset=0/140=0.0 cont=0/70=0.0(ok=YES) · g_eff=1.609(band_med=0.10878·capsat=no·¬degenerate) |

## Frozen bar 충족 (p7 · 측정 전 verbatim 출력)

- **FROZEN byte-identity `og_h_frzE = 0` ✅** — production emit path(idle/e_live) 바이트 무접촉 증명
  (harness valid, HARNESS-BUG 아님, run VOID 아님).
- **POS-CONTROL (dense ARM-SHOCK) = 105 flips · POS-PASS(≥2)=YES ✅** — 계기가 gate를 확실히 움직인다
  (meter works). negative가 dead meter 탓이 아니다.
- **x_eff NON-degenerate ✅** — g_eff=1.609(band_med=0.10878 ≥ 0.002 · capsat=no) → axis-degenerate 아님,
  cap-saturated 아님. corollary-discharge 신호는 실제로 변동한다(inert한 건 seam grip이지 신호가 아님).
- **DISSOC ok=YES · N3=0 · Ψ-guard ok ✅** — onset-vs-continuation dissociation bar 통과(both 0),
  N3 flips=0(must 0)·WAKE=0·Ψ_ON=Ψ_OFF=0.6667(Ψ emit-gap 0, forcing-gate 미발동).
- **THEATER 규칙 자동 판정** (pre-reg bar 5): `ΔEff<0.02 ∧ POS-PASS ∧ ¬degenerate ∧ ¬capsat ∧ x_eff non-degenerate(b_med≥0.002)` → 🔴 THEATER.

## VERDICT (log verbatim)

```
E VERDICT = 🔴 THEATER — corollary discharge inert at emit (ΔEff<0.02, POS-PASS ⇒ meter works, x_eff non-degenerate); adds a THIRD orthogonal recoding (shape/integration/self-prediction) to the convergent seam-law
```

## 과학적 의미 — self-prediction recoding THEATER (seam-law STRENGTHENING)

- corollary discharge(운동 forward model의 self-prediction)를 **작동하는 계기 + 비-degenerate 신호** 하에서
  emit seam에 물렸는데 gate가 전혀 반응하지 않았다(ΔEff=0/210). self-prediction은 emit에 인과적으로 inert.
- **이는 THIRD orthogonal read-side recoding THEATER** — Family A(H_9225 shape-conversion, band-pass) THEATER에
  이어 E(self-prediction) THEATER가 합류. read-side recoding family가 닫히고 있다.
- 두 CLEAN read-side recoding THEATER(A=shape-conversion, E=self-prediction) → emit gate는
  shape-conversion AND self-prediction recoding 둘 다에 대해 causally sealed. **urgency가 유일한 proven
  emit-shade 채널로 남는다**(H_9101 🟢). 남은 escalation = **WRITE-side(train-time coupling)**.

## 결정 (a_verified_must_wire · a_break_the_wall)

- **never wire** — ΔEff=0 → 배선하면 dead decoration. loss/gate 진입 금지(p7).
- **THEATER는 negative이지 capability 아님** — E는 능력이 아니라 정직한 음성(read-side recoding 무력의 세 번째 증거).
- convergent seam-law STRENGTHENING: read-side recoding family CLOSING, escalation = write-side train-coupling.

## scope

real d768.clm · rented runpod 251GB CPU pod(swap-free) · hexa v0.716.0 REAL-DECODE · `--opgrip-r3` n=400
(calib 10-49 · score ≥50 · mid=210). E lane 단독 채점(x=ec_x self-pred-err, rides H_9225 dual-EMA band-pass
transducer VERBATIM as new signal source). FROZEN arm byte-identity=0(production 무접촉)·POS-PASS(meter live)·
x_eff non-degenerate → 정직한 negative(THEATER, GREEN 아님).
