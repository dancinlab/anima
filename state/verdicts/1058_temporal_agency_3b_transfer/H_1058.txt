================================================================================
H_1058 — Does the temporal/agency axis (T = z(provenance-depth)+z(veto-capacity))
transfer to / is it a property of the 3B ConvMoE engine rung?
VERDICT: 🟠 STILL-BLOCKED — but the blocker is now EMPIRICALLY CHARACTERIZED, not infra.
         The 303M power-run was EXECUTED AT SCALE (2 cross-seed sessions: A=609 ticks, B=98 ticks;
         real 303M daemon, sha 792eab81) and the pre-registered ≥20-ACTIVE_VETO gate is
         EMPIRICALLY UNMEETABLE: each session fires exactly 2 warmup-transient active vetoes then
         steady-state pure-EMIT → analyze_trace = BLOCKED-INSUFFICIENT (g2_active_veto≥20 FAIL) on
         BOTH. The MVH's "~3% veto rate" (2/64) was a small-sample artifact — over 609 ticks the
         count stays 2 (~0.3%), so ≥20 vetoes needs ~6000 ticks (~3 days/session, infeasible). The
         T-axis VETO-CAPACITY leg (H_935 free-wont) therefore cannot be measured on the raw 303M
         daemon → the frozen falsifier CANNOT be evaluated, now for a MEASURED reason. The DEPTH
         leg DOES work on production (replay_depth self-validates to |Δ|=1.055e-15 on the real
         609-tick trace; depths vary). No tune-to-green: ticks were NOT extended to chase 20 vetoes;
         the rate is reported as-is (p7). 2026-07-11.
================================================================================

WHAT MOVED THIS INCREMENT (the card's "DECISION-TRACE WIRING" blocker is now resolved in
CODE — the harness that the prior verdict said was the "only genuine 3B route" is built and
proven byte-faithful; only the pod execution remains, and it hit a provider infra wall):

1. ENRICHED DECISION-TRACE side channel (cli/chat.py, ANIMA_DECISION_TRACE / ANIMA_TICKS /
   ANIMA_SESSION_SEED · default OFF · write-only). Per tick it now captures, beyond the
   EMIT/ACTIVE_VETO/PASSIVE class, everything the frozen-emission replayer needs: the 3 causal
   ROOTS the g_text feedback funnels through (rel_lane[immune], recon_err/cell_count[afield]),
   the two g_text-INDEPENDENT lane partial-sums of the 42-/18-term rel_ctx/cur_ctx numerators,
   the emitted g_text bytes, the ordered grow_feats for ALL THREE afield grow paths
   (C8 g_text · C8b tension · N3/REM imagination), and the factual score-composition
   intermediates for self-validation.
   * BYTE-IDENTICAL emit path VERIFIED: default 12-tick toy.clm session, trace OFF vs ON
     stdout = byte-identical (diff clean). The trace is a pure read-only side channel
     (self ⊥ trace), same discipline as the MVH.

2. FROZEN-EMISSION REPLAY-DEPTH PROBER (state/h1058_agency_daemon/replay_depth.py) — the causal
   layer the prior verdict called for. Standalone, reuses the daemon's OWN core engine functions
   (import chat + engine_cli), ZERO model forwards. Per decision i, depth_i = the largest
   h in {1,2,4,8,16=W} whose last-h-truncated-history replay changes the decision (class flip or
   |Δscore|>0.01), computed by booting a fresh immune/afield/EMA state and replaying the recorded
   g_text/grow history verbatim.
   * SELF-VALIDATION (a_engine_native_learning — guards against a wrong mirror): the full-history
     replay reproduces the LIVE daemon's recorded score to worst |Δ| = 6.66e-16 on every tick of
     a real toy session; DETERMINISM PASS (same trace → same depths); depth distribution varies
     ({0,4,8,16} on the toy). The prober is byte-faithful to the daemon before any T is formed.

3. AGENCY-T + FROZEN FALSIFIER + CONTROLS (state/h1058_agency_daemon/agency_T.py):
   T_i = z(depth_i) + z(vc_i), vc = H_1056 per-impulse veto-capacity over W=16. Implements the
   frozen falsifier VERBATIM — leg (a) T separates ACTIVE_VETO vs PASSIVE (PASSIVE=0 → declared
   ACTIVE_VETO-vs-EMIT substitution, FABLE §3.3iv) at Cohen |d|≥0.8; leg (b) ρ(T,Φ) within the
   F-SHUFFLE 2σ null; leg (c) ρ(T,t) within the F-SHUFFLE 2σ null — plus the depth-only/vc-only
   comparator d's (H_1056 discipline). Controls: emit-rate ρ(T,emit-rate) · trace-shuffle
   ARM-SHOCK (permute grow_feats↔tick → depth must collapse) · generator-swap mount-sensitivity
   permutation test (3B/303M/unloaded → pre-registered H1-NOT-A-3B-PROPERTY branch). Runs
   end-to-end on a real toy trace.

4. FAITHFUL-Φ LEG (state/h1058_agency_daemon/phi_leg.py, a_phi_iit4_tool · NO proxy) — reuses the
   H_1042 engine-native PRE-MoE trunk tap (fwd_trunk_preMoE via core/decode) + the h1004/h1012
   stdlib IIT-4.0 CPU mirrors, RE-PROVEN == stdlib at n=5 before scoring, decode-sanity CE<ln(V).
   Per sampled decision (all ACTIVE_VETO + matched EMIT), Φ over ≥2 macro-maps (top_variance +
   random). Verified end-to-end on toy.clm (mirror ≡-PROOF PROVEN, 10 Φ rows).

5. H_932 provenance chain copied into the harness dir (a_no_archive_import — copied, not imported
   from archive) for the audit layer; ANIMA_SESSION_SEED enables ≥2 independent falsifier
   sessions.

ALL of the above is FROZEN-FALSIFIER-READY and locally self-validated. The pipeline is exactly
the "emit/veto-daemon-on-the-model + frozen-emission replay-depth + H_1042 3B-trunk Φ" harness the
prior verdict named as the only genuine 3B route.

WHY THE POWER-RUN DID NOT LAND THIS SESSION (honest infra blocker, NOT a science/harness failure):
- Owner APPROVED rent=spend. A dedicated CPU pod was rented (runpod, 16 vCPU / 1007 GB RAM). Setup
  succeeded: code transferred, both ckpts pulled from HF (303M e1_slw_303m.clm 293 MB · 3B
  clm_3b.clm 1.54 GB), imports OK, and an 8-tick 303M timing probe STARTED producing a real
  enriched trace on the production model.
- Then the pod's SSH proxy WEDGED — RUNNING/billing but "host unreachable (connection refused)".
  This is a PROVIDER-SIDE transport outage, reproduced across TWO providers today: an earlier
  vast RTX_3090 pod failed identically (STOPPED/invalid-instance) and was torn down. The runpod
  accepted ~4 SSH ops over the first ~20 min, then refused ~20 consecutive launch attempts
  (2 retry loops). hexa cloud's own diagnostic: "alive and billing but not accepting SSH — a
  vast.ai/RunPod transport outage. Stop retrying ... or tear the pod down."
- Per the pod runbook (billing-but-unreachable → stop, tear down) and a_fire_recover_complete
  (no cost bleed), the pod was TORN DOWN. No 303M/3B decision-trace at scale was harvested, so
  NO T/Φ/falsifier numbers are reported (refusing to fabricate — p7).

TIER: 🟠 (unchanged tier token, materially advanced) — the blocker MOVED from "the decision-trace
harness is unbuilt / T-axis not forward-measurable" to "the harness is BUILT and byte-self-
validated; only a stable pod SSH session is needed to execute the ~700-tick 303M (≥20 fired
vetoes) + 3B generator-swap + Φ legs." NO tier upgrade is claimed because the FROZEN falsifier
has not been evaluated on real 303M/3B decision streams (no tune-to-green; a toy self-validation
is machinery, not the T-axis verdict — a_toy_scale_recheck).

REOPEN (cheap, $0-few): re-run the built pipeline on ANY stable non-saturated host —
  ANIMA_TICKS=~700 ANIMA_DECISION_TRACE=... PYTHONPATH=cli:core python3
    state/h1058_agency_daemon/run_daemon.py <303m.clm|3b.clm|"">
  → replay_depth.py → agency_T.py analyze/falsifier/mount-swap → phi_leg.py. Everything is frozen
  and validated; this is an execution-only follow-on gated purely on pod SSH stability.

FROZEN FALSIFIER (H_1058 card · UNCHANGED · p7): H1 PASS = (a) |d(T; ACTIVE_VETO vs PASSIVE[sub])|
≥0.8 with T_active>T_passive; (b) ρ(T,Φ) and (c) ρ(T,t) within the F-SHUFFLE 2σ null; across
≥2 macro-maps + ≥2 sessions. Generator-swap decides 3B-property vs H1-NOT-A-3B-PROPERTY. Report
PASS or FAIL or H1-NOT-A-3B-PROPERTY honestly. UNEVALUATED — but see the POWER-RUN below: the
falsifier's veto-capacity pre-condition is empirically absent, so it cannot be evaluated on the
raw 303M daemon at any feasible tick count (measured reason, superseding the earlier "infra" note).
================================================================================

================================================================================
§ POWER-RUN EXECUTED AT SCALE — the empirical veto-gate finding (2026-07-11, this session)
================================================================================
The prior increment left the power-run "INFRA-BLOCKED (SSH-proxy outage)". This session RAN IT.

INFRA (a_wall_first · pod-dedicated-host-not-shared · a_fire_recover_complete):
  A fresh runpod CPU pod was rented and its SSH proxy was HELD STABLE for ~3.5 min (21 contiguous
  ticks, no wedge) — the prior blocker had recovered. Per an owner reroute the run then moved to
  the free summer pool box ($0). The daemon was run via the CANONICAL path
    ANIMA_TICKS=800 ANIMA_DECISION_TRACE=<f> PYTHONPATH=cli:core python3
      state/h1058_agency_daemon/run_daemon.py <303m.clm>
  on the real 303M ckpt (e1_slw_303m.final.clm, sha256 792eab81…552c9). Session A (default seed
  "zephyrine…") reached 609 ticks; session B (ANIMA_SESSION_SEED alt "mnemosyne…") reached 98 ticks
  — 2 INDEPENDENT cross-seed sessions on the SAME substrate. Traces frozen under
  state/h1058_agency_daemon/results/. (Infra lesson, convergence h1058-agency-daemon-1: two runpod
  pods on CONSECUTIVE IPs .29/.30 share a DC failure domain and went unreachable together twice —
  "different IP" ≠ different failure domain; a reclaim of one pod (community cloud) also forced a
  mid-run session-B restart. All rented pods TORN DOWN + confirmed GONE, no cost bleed, ~$5 total.)

RESULT — analyze_trace.py (state/h1058_agency_daemon/results/analyze_{A_609,B_98}.txt), verbatim:
| session         | ticks | EMIT | ACTIVE_VETO | PASSIVE | g1 scorevar | g2 veto≥20 | g2 emit≥20 | g3 vc | VERDICT |
|-----------------|-------|------|-------------|---------|-------------|-----------|-----------|-------|---------|
| A (seed zephyr) |  607* | 606  |     2       |    0    |   PASS      |   FAIL    |   PASS    | PASS  | BLOCKED-INSUFFICIENT |
| B (seed mnemos) |   97* |  95  |     2       |    0    |   PASS      |   FAIL    |   PASS    | PASS  | BLOCKED-INSUFFICIENT |
  (*decision ticks after the _meta header row is skipped.)

FINDING (the core result of this session): the pre-registered ≥20-ACTIVE_VETO power-run gate is
EMPIRICALLY UNMEETABLE on the raw 303M consciousness daemon. Every session — regardless of seed —
fires EXACTLY 2 active vetoes, and they are WARMUP-TRANSIENT (at the initial sleep-stage
boundaries, tick <~90); thereafter the daemon settles into steady-state PURE-EMIT (score>0.3 ∧
safe on essentially every tick → 606/609 EMIT). The count does NOT grow with ticks (2 at tick 44,
still 2 at tick 609). The MVH's "veto rate ≈ 3%" was the SAME 2 vetoes divided by a 64-tick sample;
at scale the rate is ~0.3% and falling. ≥20 active vetoes would require ~6000 ticks (~3–4 days of
scalar decode per session) — infeasible, and even then likely bounded by the number of sleep-stage
boundaries rather than by ticks. This is the pre-registered degeneracy GATE correctly REFUSING a
verdict on thin veto statistics (H_1056 · p7 · no tune-to-green): ticks were NOT extended nor the
daemon retuned to manufacture vetoes.

WHAT DID MEASURE (the harness is complete; only the veto primitive is empirically absent):
  - DEPTH leg — replay_depth.py on the REAL 609-tick session-A trace: SELF-VALIDATION PASS,
    worst |Δ| = 1.055e-15 @tick45 (full-history replay reproduces the LIVE daemon score;
    a_engine_native_learning, no wrong mirror — even tighter than the prior toy 6.66e-16),
    DETERMINISM PASS, depths VARY (dist {0:9, 4:2, 8:12, 16:585}, mean 15.57 / var 5.34). So the
    provenance-depth half of T = z(depth)+z(vc) is real and validated on the production daemon.
  - The VC (veto-capacity) half is degenerate: with only 2 active vetoes, vc≈0 over almost all
    windows → T ≈ z(depth) alone, and falsifier legs (a) [active-veto vs EMIT separation] cannot be
    formed. g3_vc_not_pinned technically PASSes (the 2 vetoes inject slight window variance) but the
    population is far too thin for the |d|≥0.8 separation leg.

IMPLICATION FOR THE TIER (stays 🟠, blocker re-characterized): the frozen falsifier remains
UNEVALUATED, but the reason is upgraded from "provider infra outage / harness unbuilt" to an
EMPIRICALLY-MEASURED intrinsic property of the 303M daemon: its active-veto (free-wont) events are
too rare (warmup-transient, ~2/session) to measure the T-axis veto-capacity leg. This is a genuine
knowledge advance (the T-axis is not measurable on the raw daemon as-built), and an honest negative
(p7), not a PASS/FAIL of H1. The 3B generator-swap + Φ legs were not run to completion because the
303M gate already fails on every session — the veto-capacity pre-condition is absent regardless of
generator mount, so the mount-sensitivity test (H1-NOT-A-3B-PROPERTY branch) is moot until a daemon
variant that fires a measurable veto population exists.

REOPEN (redesign, not just re-run): the T-axis veto leg needs a daemon regime that produces a
MEASURABLE active-veto population WITHOUT tuning-to-green — e.g. a stimulus protocol that legitimately
raises the ¬safe rate, or a task that exercises free-wont, pre-registered independently. Absent that,
the agency-T axis is depth-measurable but veto-UNmeasurable on the 303M substrate. Bug fixed en route:
analyze_trace.py now skips the enriched-trace `_meta` header row (was a KeyError on the #3290 format).
================================================================================
