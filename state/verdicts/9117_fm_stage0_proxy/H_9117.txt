=== H_9117 — §2 Stage-0 in-engine proxy validation ($0, no oracle) — VERDICT ===
tier=DIRECTIONAL (lexical listener = $0 LOWER-BOUND surrogate of the CLM mouth-backend fable specified). Reuse H_9115 fixture.

VERDICT: 🔴 LEXICAL-PROXY-WEAK (for the specific surrogate tested) — with a VALUABLE REDIRECT.
  corr(d̂ lexical-contrastive-prefix, oracle-decodability) = −0.21  (FAIL — my cheap surrogate does not predict decodability)
  corr(filler_prefix surface baseline, oracle-decodability) = +0.688 (the TRIVIAL surface proxy predicts it WELL)

READING (c9 honest — bar NOT moved):
- fable's §2 Stage-0 prescribed validating an in-engine "frozen-listener CONTRASTIVE prefix decodability" proxy d̂ (the
  CLM/bytegpt mouth-backend run backwards) with a corr≥0.656 gate BEFORE any .hexa/GPU. I could not run the real CLM
  backend cheaply, so I tested a $0 LEXICAL surrogate (IDF-contrastive prefix margin). It FAILED: r=−0.21, dominated by
  total discriminative-word MASS (cactus d̂=37 vs volcano 25) rather than front-loading — a bad approximation of the
  contrastive listener.
- This does NOT refute fable's ACTUAL proxy (the real CLM-listener sees genuine byte-level discriminability my lexical
  bag-of-words misses). It only shows my cheap lexical STAND-IN is inadequate → the true CLM-listener test is unresolved
  and would need the real backend (heavier, engine-native).
- REDIRECT (the valuable finding): the TRIVIAL surface proxy filler_prefix (bytes of non-discriminative prefix before the
  first content word) predicts oracle-decodability at r=+0.688 — GOOD. So an in-engine modulation signal for Stage-1 does
  NOT need fable's heavy contrastive-CLM-listener; a simple front-loading measure (how early discriminative content
  starts) already carries the signal. This is a CHEAPER Stage-1 proxy than the design assumed.
- Consistency: H_9116 Screen-A found corr(filler_prefix, compression-GAIN)=0.656; here corr(filler_prefix, oracle-
  decodability)=0.688 — the SAME surface feature (front-loading) is the robust, cheap, consistent signal across both
  screens. The contrastive-listener elaboration is not (yet) shown to add value; the simple front-loading proxy is.

CONCLUSION: §2 Stage-0 outcome = the cheap in-engine proxy question RESOLVES toward the SIMPLE end: front-loading degree
  (filler_prefix / discriminative-content onset) is a viable, cheap, in-engine-computable modulation target (r≈0.69),
  making Stage-1's mouth-gate rerank scoreable WITHOUT a CLM-listener contrastive pass. fable's contrastive proxy is an
  untested (possibly unnecessary) elaboration. Net: Stage-1 .hexa (rz_forward_model resolver + mouth-gate K-rerank scored
  by an in-engine front-loading measure) remains the next rung — a real core/*.hexa build (engine-surgery, multi-turn) +
  eventual external b50 re-measure. Cost boundary unchanged; the proxy is now cheaper than designed.
Data: stage0_proxy.py · RESULT.md · DESIGN_fable_s2impl.md · ../9115_forward_model_screen/screenb_fixture.jsonl.
