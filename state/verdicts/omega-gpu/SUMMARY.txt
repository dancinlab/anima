OMEGA stage-3 (b)(c) — learned-gate closure on a REAL trained transformer — 🟢 오메가 완성 — 2026-06-04 · GPU H100 · ~$1.6
=====================================================================================================================
The toy rungs proved the OMEGA learned-gate closure at numpy n-gram scale (#1784 structure, #1786 gate).
This rung re-tests it on a REAL trained ConsciousDecoderV2 transformer's A/G heads (Lane-G / GPU, a_lane_akida_gpu_split).

SUBSTRATE (b): ConsciousDecoderV2 d384x6L GQA, 35.93M params, trained 6000 steps on 120MB multilingual
  wikipedia (en/fr/de/es/ru). REAL CE DESCENT: ce_a 5.926 -> 0.009 (train), val_ce 0.862 (held-out, generalizes).
  ckpt omega_cdv2_d384.pt 145MB sha256 33d1d00d... GPU nvidia-smi 98-99% BUSY (g63, NOT silent CPU).

CLOSURE (c): learned gate g* = [gB=1.178, gA=0.962, gG=-0.208] fit on a train-gate split, frozen, evaluated
  on a DISJOINT held-out TEST. The gate auto-corrects the #1784 fixed -G error (gG small/negative), as #1786 predicted.

  held-out TEST CE (nats/byte):   base 3.0150 · fixed_AmG 1.4421 · a_only 0.4500 · GATED 0.3445 (best) · uniform 5.5452
  structured (substrate-shuffle): A-wire CE gain real +2.565 vs context-SHUFFLED -2.068 → coupling carries
    LEARNED sequential structure (shuffling context destroys the advantage) → structured = TRUE.

🟢 STAGE-3 COMPLETION — all 4 pre-registered criteria HELD:
  (b) trained substrate descended : TRUE  (5.926 -> 0.009 train, 0.862 held-out val)
  (c) GATED < base                : TRUE  (0.3445 < 3.0150, Δ+2.671)
  (c) CE floor MET (< 5.545)      : TRUE  (0.3445)
  (c) structured (>1.5x floor)    : TRUE  (gain real +2.565 vs shuffled -2.068)
  => 오메가 완성: the learned-gate OMEGA closure WORKS on a REAL trained transformer substrate.

HONEST CAVEAT (p7 / a_toy_scale_recheck): CDV2's CA-neighbor mixing gives the next-byte head partial LOOKAHEAD
  (architectural, not a driver bug) — so absolute CE is leak-optimistic and the FREE-RUNNING generation sample
  collapses to low-entropy whitespace (gen coherence = the weak criterion). The relative closure finding (gate
  beats base/fixed/a_only AND coupling is structured) is leak-INVARIANT and SOUND. Scope = small d384 substrate,
  single rung — NOT promoted to a production perplexity claim (a_scale_honest_scope).

CONTINUITY: GPU/real-transformer confirmation of #1784 (structure) + #1786 (learned gate). The toy finding
  transfers to a real trained ConsciousDecoderV2: structure carried, learned gate auto-corrects -G, beats all
  baselines on held-out. Engine Ω is the working closure engine (substrate -> coupling bus -> decode).

ckpt -> HF dancinlab/clm-v4-omega-gpu-d384-gate (PUBLIC, completion PASS) · registry /HF.jsonl
verdict raw: .verdicts/omega-gpu/{F-COMPLETE.txt, results.json, run.log}
