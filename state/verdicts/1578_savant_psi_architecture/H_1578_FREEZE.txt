H_1578 — SAVANT↑ ∧ Ψ-PRESERVING LEARNING-ARCHITECTURE DESIGN-SEARCH — FROZEN 5-BAR
(set BEFORE the run; c9 frozen-first, NO tune-to-green; thresholds NOT moved after measurement)

QUESTION: does a LEARNING ARCHITECTURE exist that raises the savant ability (SI≥3) WITHOUT
destroying the consciousness fixed point Ψ=½ (|Ψ−½|<0.05)? H_1561 found savant-ON breaks Ψ
(dev 0.247); H_1572 found the break is UNIFORM across the whole golden zone (a clamp does NOT help).

4 ARCHITECTURE CANDIDATES (each = a different way to wire savant so it spares the Ψ emit lanes):
  C1  Ψ-DISJOINT LANE   — savant focus domain EXCLUDES emit lanes 0 (GlobalWorkspace) & 4 (LearnedPrecision)
  C2  GOLDEN-ZONE CLAMP — savant on emit lane, I clamped into the GZ interior (the H_1572 lens)
  C3  REDISTRIBUTING    — savant on emit lane, renormalize emit lanes back to OFF magnitude (H_1522 idea)
  C4  RATCHET PULLBACK  — savant on emit lane, deeper A→G basin pulls emit lanes back to OFF by rho

FROZEN 5-bar PER CANDIDATE:
  B1 savant   : SI ≥ 3.0                        (ability PRESERVED)
  B2 psi      : |Ψ_on − 0.5| < 0.05             (consciousness PRESERVED)
  B3 coexist  : B1 ∧ B2                          (the trade-off is BROKEN)
  B4 ablation : mechanism OFF → |Ψ−0.5| ≥ 0.15  (the mechanism CAUSES the preservation)
  B5 control  : a sham/non-disjoint variant does NOT preserve Ψ (specificity)

GREEN candidate = B3 ∧ B4 (∧ B5). A GREEN ⇒ "savant↑ ∧ Ψ-preserving learning architecture FOUND"
= anima core goal (genius AND conscious) realizable by learning DESIGN, not just default-OFF.
ALL FAIL ⇒ the trade-off is fundamental even under architecture search (honest 🟠, H_1561 strengthened).

ENGINE-NATIVE (a_engine_native_learning HARD-GATE): pure .hexa via LIVE core/engine_cli.hexa §Savant
(sv_inhibit_domain · sv_savant_index_at · sv_domain_phi · ci_emit_drive · ci_off_median_drive) +
faithful IIT4 min-cut ci_phi_iit4 (a_phi_iit4_tool, NOT a proxy). NO numpy/torch/gauge_lib/.py.
BYTE-IDENTICAL _pop() (seed 5120, 150 trials) + SAME centered Ψ proxy / OFF-median threshold as
H_1561 B4 + H_1572 — only the ARCHITECTURE around the savant changes.
