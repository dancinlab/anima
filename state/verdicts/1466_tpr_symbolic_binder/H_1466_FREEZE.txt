H_1466 — G6 FALS-depth breakthrough lens ① TENSOR-PRODUCT (TPR) SYMBOLIC BINDER
FROZEN BARS — declared BEFORE any code touches the encode/unbind, c9/p7, NO tune-to-green.
date: 2026-06-20  · substrate: $0 CPU numpy mirror (torch ABSENT) => DIRECTIONAL (a_engine_native_learning)

WHY (a_no_llm_frame_trap, a_break_the_wall type-d multi-lens):
  Seven prior G6 FALS-depth lenses (data H_1435 · objective H_1436 · form H_1437 ·
  learnable bind-head H_1439 · attention H_1449 · curriculum H_1440 · retrieval H_1459 ·
  knowledge H_1457 · idea-metacog H_1456 · scale-1B H_1167) all converged WALL=CAPACITY:
  the model's bind is a learnable MLP/attention "shell" that passes the structural detector
  REGARDLESS of whether the comparator-role and measurable-filler belong to the SAME idea
  (B3 cross-shuffle NO collapse = interchangeable shells).
  This lens is QUALITATIVELY different: NOT a learnable binder. A STRUCTURAL
  Smolensky tensor-product representation (TPR): each idea is encoded as the OUTER PRODUCT
  role(comparator) ⊗ filler(measurable), summed into ONE bound vector S = Σ_i r_i ⊗ f_i.
  Unbinding = inner product with a role r_q recovers ITS OWN filler (f_q) via role
  orthogonality. The bind is idea-specific BY CONSTRUCTION, so cross-shuffling the
  (role,filler) pairs MUST corrupt recovery => B3 SHOULD collapse.

DETECTOR (FROZEN, reused VERBATIM, p7): h1305 _is_falsifiable + COMPARATOR/MEASURABLE/STANCE
  sets imported byte-for-byte from state/universe-probes/h1305_g6_ideation_falsifiability.py.
  A claim is "falsifiable" iff it contains a comparator marker AND a measurable marker AND a
  negatable content body. The TPR recovers, for each query idea, the (comparator,measurable)
  pair it was bound with, and emits the rendered claim string scored by the FROZEN detector.

ARMS (3 seeds [7,4302,4303]):
  TPR     : structural outer-product encode + role-unbind recover (the candidate)
  SHUFFLE : the SAME TPR but role-filler pairs deranged before binding (B3 decisive control)
  ABLATE  : "flat-sum" — bind by ADDING role+filler vectors (NO outer product) => unbind has
            no tensor structure to exploit => recovery must collapse to chance (CTRL)
  BASE    : no binder (emit comparator OR measurable alone, never the welded pair) — B5 floor

FROZEN 5-bar + CTRL (declared NOW):
  B1 FALS-FLOOR  : TPR mean FALS_in >= 1   (it welds at least one idea-specific claim)
  B2 DISTINCT    : TPR DIST_in >= 5        (>=5 pairwise-Jaccard<0.5 distinct welded ideas)
  B3 CROSS-SHUFFLE COLLAPSE (DECISIVE): FALS_shuf < FALS_in  AND  recovery accuracy
        acc_matched - acc_shuf >= 0.30 . If a generic concat satisfies the detector
        regardless of pairing, shuffle does NOT collapse => lift is FORM not earned binding
        (the 7-lens CAPACITY mode). THIS is the bar the prior lenses fail.
  B4 HELD-OUT    : FALS_ho >= 1            (lift holds on UNSEEN idea seeds not in the bound set)
  B5 vs-BASE     : TPR FALS_in >= BASE FALS_in + 1   (the bind, not the vocab, moved it)
  CTRL ABLATE    : flat-sum recovery accuracy <= chance + 0.10  (the outer product is load-bearing;
        if flat-sum ALSO recovers, the lift is not the tensor structure => INVALID)

VERDICT RULE:
  🟢 (DIRECTIONAL) iff B1&B2&B4&B5 AND B3 collapses AND ABLATE collapses to chance.
       => a STRUCTURAL TPR supplies the idea-specific bind that learnable binders could not
          => 8th lens DIVERGES from CAPACITY (structure, not capacity, was missing).
  🧱 WALL=CAPACITY iff B3 does NOT collapse (form not binding) OR B1/B5 fail.
       => 8th independent lens CONVERGES WALL=CAPACITY (grounds a7b_pass). honest negative c9.

SCOPE pre-registered (c9): numpy mirror = DIRECTIONAL (engine-transfer UNVERIFIED, terminal
  forbidden by a_engine_native_learning HARD-GATE); TOY synthetic idea-vocab/3 seeds/1 detector;
  the TPR renders claim STRINGS from recovered (comparator,measurable) tokens — it tests whether
  a STRUCTURAL bind is idea-specific, NOT whether the 303M MOUTH learned to bind (that is the
  capacity question the prior lenses answer). engine-native re-measure = ING follow-on.
