# .verdicts/savant-torch — SAVANT torch-cuda lane verdicts

Verbatim run outputs (g5/g63, p7, NO fabrication) for the SAVANT 5-lang 7B
torch-cuda reference lane (Lane-G / GPU). Distinct from `.verdicts/savant/`
(the forge-native SAVANT-7B lane).

- `rung0_descent.txt` — small (~85M) validation descent curve verbatim + PASS?
- `7b_launch_state.txt` — 7B durable launch: pod id, persistent vol, ckpt
  cadence, ETA to a competent CE, harvest plan.
