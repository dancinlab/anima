H_1463 — PHASE-BINDING / BINDING-BY-SYNCHRONY (G6 FALS-depth lens ③) — FROZEN BAR DECLARATION
Declared BEFORE the honest (content-derived) result was read (c9 anti-tune-to-green, p7).
================================================================================================
LENS (a_no_llm_frame_trap — neuroscience binding-by-synchrony, von der Malsburg / Lisman
theta-gamma phase coupling): features of the SAME object fire at the SAME phase; binding is
read as PHASE-COHERENCE, not a learned weight-space weld. The 7 prior G6 lenses all used
content-addressed welds (MLP/attention/retrieval) and ALL failed B3 cross-shuffle (FALS_shuf
== FALS_in = interchangeable shells = WALL=CAPACITY). H_1463 asks: does a RELATIONAL phase
read-out make B3 collapse where the content-addressed welds could not?

HONEST DESIGN (c9 — the choice that makes this a REAL test not a tautology): phase tags are
derived FROM CONTENT carrying the H_1431 emission profile — only ~25% of each slot's content
is idea-specific, ~75% is generic falsifiable-clause FORM shared by every measurable. A
FOREIGN measurable (cross-shuffle) keeps the FORM-shell and only flips the IDEA-signal. So B3
collapses ONLY IF the idea-signal dominates the phase, i.e. ONLY IF synchrony recovers
idea-identity over generic form. FORM_RATIO=0.75 is FROZEN at the measured profile, NOT tuned;
a full sweep 0.5->0.95 is logged.

SEEDS [1463,1464,1465]. DIM=64. COH_THR=0.55. B3_MARGIN=0.30. $0 CPU numpy.

FROZEN 5 BARS (🟢 iff all + phase-scramble control collapses):
  B1 FALS-FLOOR  : SYNC mean FALS_in >= 1
  B2 COUNT       : SYNC mean DIST_in >= 5
  B3 X-SHUFFLE COLLAPSE (DECISIVE): COH_matched - COH_mismatched >= 0.30
  B4 HELD-OUT    : SYNC mean FALS_ho >= 1
  B5 vs-BASE     : SYNC FALS_in >= base FALS_in + 1
CTRL phase-scramble (ablate): COH_matched_ablated <= 0.55 (random phase -> chance).

VERDICT RULE:
  🟢 ROOT-FIX iff B1&B2&B3&B4&B5 AND phase-scramble collapses.
  🧱 WALL=CAPACITY iff B3 does NOT collapse at the measured FORM_RATIO (interchangeable shells)
     OR B1/B5 fail -> synchrony is a structural template, converges with the 7 prior lenses.

DIRECTIONAL (a_engine_native_learning): numpy mirror -> AUTOMATIC DIRECTIONAL; terminal read =
engine-native re-measure on live core/ decode = ING follow-on.

------------------------------------------------------------------------------------------------
RESULT (verbatim run.local.log, mean/3 seeds, FORM_RATIO=0.75):
  base   FALS_in=2.000
  SYNC   FALS_in=2.333  DIST_in=5.333  FALS_ho=2.000
  COH    matched=0.328  mismatched(cross)=0.216  ablate=0.323
  B1 FALS-FLOOR  2.333>=1                 : True
  B2 DIST        5.333>=5                 : True
  B3 X-SHUFFLE   0.112>=0.30 (COLLAPSE)   : FALSE  <-- DECISIVE FAIL
  B4 HELD-OUT    2.0>=1                   : True
  B5 vs-BASE     2.333 vs 2.0+1           : FALSE
  CTRL phase-scramble  0.323<=0.55        : True (control fires correctly)

SENSITIVITY SWEEP (B3 gap vs FORM_RATIO — B3 NEVER passes):
  fr=0.50 gap=+0.159  fr=0.60 +0.128  fr=0.70 +0.079  fr=0.75 +0.112
  fr=0.80 +0.112  fr=0.85 +0.107  fr=0.90 +0.105  fr=0.95 +0.086   (all < 0.30 -> B3=False)

VERDICT: 🧱 WALL=CAPACITY (DIRECTIONAL). B3 cross-shuffle does NOT collapse — the foreign
measurable shares the generic FORM-shell so its phase stays coherent (interchangeable shells,
H_1431/1434/1449 mode). The phase-scramble control collapses to chance (0.323) = the synchrony
read-out itself works; it is the mouth's CONTENT that lacks separable idea-identity. 8th
independent lens converging WALL=CAPACITY -> grounds 7B (a7b_pass).
