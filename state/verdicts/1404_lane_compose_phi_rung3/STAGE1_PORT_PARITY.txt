H_1404/1407/1408 lane-composed Φ — rung-3 live-core wire · STAGE-1 port-parity (2026-07-07)

WIRED: core/engine_cli.hexa + .py — ethics_units / lane_compose_step / lane_composed_phi
(typecheck PASS · py STAGE-1 port-parity PASS).

STAGE-1 (fixture-equivalent margin/contradiction/exposure → new ported functions, rent-0):
  phi_composed=0.1484  phi_disconnected=0.0799  lift=0.0684  → INTEGRATION-RAISES-Φ 재현 ✓
  ethics_units(m=0.6,c=0,nov=0.4,cur=0.2,exp=3.0) = [0.882, 0.400, 0.200, 0.881]

ESTIMATOR SPLIT (a_phi_iit4_tool): READOUT = prod ci_phi_iit4 (Gaussian-MI MIP·live context signal).
Φ VERDICT tier stays stdlib faithful_phi (binned-MI) via `hexa verify` over a dumped live trajectory
— the two estimators DIFFER; never cement a Φ tier off ci_phi_iit4.

FOLLOW-ON (a_toy_scale_recheck): live-cell re-measure over a real decode window. prod contradiction
is BINARY (fixture continuous) + live exposure from last_used → live Φ may differ; a negative live
ordering is a RESULT (no tune-to-green). H_1407/1408 = shared Φ readout done · own 8-unit builder pending.
