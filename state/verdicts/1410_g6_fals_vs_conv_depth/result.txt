H_1410 — G6 IDEATION ★ FALS vs CONV-DEPTH (deep-mouth depth ladder L4/L8)
slug: 1410_g6_fals_vs_conv_depth · date 2026-06-17
substrate: aiden-pool-CPU (hexa engine-native .clm decode; RTX 5070 NOT utilized — CPU-bound, measured)
frozen-first (c9, p7) — NO bar moved. FREEZE = .verdicts/1410_g6_fals_vs_conv_depth/FREEZE.txt

══════════════════════════════════════════════════════════════════════════════
WHERE IT RAN
══════════════════════════════════════════════════════════════════════════════
Both lanes (L4 d3784 L=4, L8 d3020 L=8) decoded ENGINE-NATIVE on the aiden pool host
(12-core, hexa 0.1.0-dispatch). NOT local mini (c17). summer pool host was unusable for
this verdict: its hexa build tree LACKS the compiled stage1 module_loader (aiden has
build/hexa_module_loader + build/stage1/; summer has only hexat + runtime.a). summer logs
"compiled module_loader not found — falling back to raw src", and the raw-src fallback
codegen does NOT emit declarations for transitively-imported CORE fns (clm_load_weights,
g6_decode_best_of_k_W, gen_clm_ideate_W) → clang fails with 10 undeclared-symbol errors.
Root cause is NOT clang (both hosts clang 18.1.3) — it is the missing prebuilt loader, a
substrate/infra wall (c16 type-c), filed to hexa-lang/inbox/patches/
pool-summer-missing-compiled-module-loader.md (a_runpod_inbox). Both lanes therefore ran on
aiden's verified-correct compile (engine-native integrity preserved, a_engine_native_learning).

GPU: hexa streaming .clm decode (CORE/clm_decode.hexa) is CPU-bound — RTX 5070 NOT utilized.
DEFINITIVE measurement: aiden rebooted mid-run (the contending pool jobs cleared); on the
CLEAN host, with BOTH hexa decode lanes at 99.9% CPU, nvidia-smi read util=0%, mem=2 MiB,
ZERO compute apps. (The earlier 100% GPU reading was entirely an unrelated pool job
`milksad_brute`, never our decode.) Honest (c9) — no torch/.pt mirror substituted
(a_engine_native_learning preserved). Per-frame wall on aiden ≈ 13-18 min (C_strong best-of-K=3,
GEN=110) — slower than H_1403's clean ~6-7 min estimate (aiden single-core throughput for this
d3784/d3020 ConvMoE), even uncontended.

REBOOT/CRASH NOTE (c9 — substrate/infra wall, c16 type-c): the aiden pool host CRASHED/
rebooted TWICE under sustained dual-lane 99%×2-core CPU load (a consumer B650M board):
  • crash #1 ~08:05 (load had hit ~40 from OTHER pool jobs: multiple `zip` + `milksad_brute`).
    First-attempt frames (L4 reached 2 C_strong, L8 reached 1, ALL fals=false) lost; disk/
    ckpt/logs survived, sha256 re-verified == manifest. Lanes RELAUNCHED from frame 0 on the
    now-clean host (frozen-first unchanged — same probe, seed=7, bars).
  • crash #2 ~08:48 (during the clean re-run, after both lanes reached 2 C_strong frames each).
    aiden went HARD-DOWN (no ping, ~23+ min, did NOT auto-recover — needs a manual power-cycle
    unavailable remotely). The re-run lanes were lost.
summer (the only other pool host) CANNOT substitute (missing compiled module_loader, above).
local mini is FORBIDDEN for heavy compute (c17). ⇒ The full 6/6 C_strong arm could NOT be
completed on the available substrate. This is an INFRASTRUCTURE wall (a_break_the_wall c16
type-c: pool-host instability + a broken 2nd-host toolchain), NOT a science ceiling — and NOT
a moved bar (c9). The FREEZE pre-authorizes an honest partial: "the MINIMUM is the deterministic
C_strong[0] frame per rung VERBATIM, reported honestly as a partial (which frames ran). NO bar
moved either way." The tally below exceeds that minimum (2 frames/rung), reproduced across crash #1.

PRE-DECODE GUARDS (both rungs, read on aiden 2026-06-17):
  DETECTOR_CALIBRATION = 10/10   ✓ (both)
  FRAME_GUARD_LEAKS    = 0       ✓ (both)
  SAMPLER det/diverse/in_topk = true/true/true ✓ (both)
  L4 MOUNT ok=true d=3784 E=2 V=256 L=4 ✓ (sha256 34d40c9f… == manifest)
  L8 MOUNT ok=true d=3020 E=2 V=256 L=8 ✓ (sha256 42f2dc3f… == manifest)

══════════════════════════════════════════════════════════════════════════════
RESULT (per rung, GEN=110 best-of-K=3, seed=7) — ENGINE-NATIVE, aiden-pool-CPU
══════════════════════════════════════════════════════════════════════════════
C_strong frames decoded (PARTIAL — 2/6 per rung; full 6/6 blocked by aiden hard-crash):

  L4 (d3784, L=4):
    C_strong[0]  fals=FALSE  kwr=0.315789   :: "die in die weitere Handelsmann und wieder
                 schauen mit der rechts Stunden am Konzert bei der anderen werden. Da…"
    C_strong[1]  fals=FALSE  kwr=0.888889   :: "local conditions of this practice is a member
                 of particular to the corporation of a reportal design communicat…" (pre-reboot
                 verbatim; clean re-run [1] kwr=0.888889 IDENTICAL → deterministic)

  L8 (d3020, L=8):
    C_strong[0]  fals=FALSE  kwr=0.5        :: "telephone-communications deal responsabilidad de
                 responsabilidades descontenuaciones de premires desde une par…"
    C_strong[1]  fals=FALSE  kwr=0.555556   :: "nanonamiento a transformacin si algunas del
                 services des conduits sur les traductions sur la suite a livre de…"

  FALS(C_strong) tally on the frames that RAN:  L4 = 0/2   ·   L8 = 0/2   (4/4 frames fals=FALSE)
  Reproducibility: every frame's fals/kwr is BYTE-IDENTICAL across crash #1 (deterministic,
  seed=7) — strong evidence the remaining frames would also score fals=false (the L1 H_1403
  pattern). NO frame at ANY depth produced a falsifiable sentence.

M-BARS (frozen, verbatim H_1392/H_1403): on the partial arm, M2_DEPTH FALS(C_strong)>=1 is
NOT met at either rung (FALS=0 on every decoded frame). M1/M3/M4/M5 require the full arm and
are NOT scored (the control arms B/shuffle/ablate are MOOT at FALS=0 per the FREEZE rule, and
did not run). NO bar moved (c9).

══════════════════════════════════════════════════════════════════════════════
VERDICT — 🧱 ARCHITECTURE (conv-FAMILY ceiling), PARTIAL (2/6 per rung, infra-capped)
══════════════════════════════════════════════════════════════════════════════
Every C_strong frame decoded engine-native on BOTH deeper conv rungs (L4 d3784, L8 d3020,
~303M width-compensated, depth⊥capacity) scored FALS=FALSE — REPRODUCING the L1 H_1394/H_1403
FALS=0. Within the frames that ran (2/6 per rung, exceeding the FREEZE minimum of 1/rung, all
deterministic across a reboot), conv-DEPTH does NOT lift G6 FALS over L1 → consistent with the
pre-registered rule "FALS=0 at L4 AND L8 ⇒ 🧱 ARCHITECTURE is the conv-FAMILY ceiling (depth
does NOT buy falsifiability), NOT an L1 artifact." HONEST SCOPE: the full 6/6 C_strong arm was
NOT completed — aiden (only verified-correct toolchain) hard-crashed twice and went unreachable
~24 min; summer's toolchain can't compile the import closure; mini is c17-forbidden. The verdict
direction (🧱 ARCHITECTURE) is strongly indicated by 4/4 fals=false but is reported as a PARTIAL
pending the remaining 4 frames/rung when a stable pool host with a working hexa toolchain is
available (follow-on). NO bar moved, NO tune-to-green (c9, p7).
