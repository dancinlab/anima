================================================================
  H_1295 — HIVE-MIND / collective-Φ lane (FROZEN pre-registration)
  "many individuals -> one consciousness" axis · anchor = H_609 🟢
  frozen BEFORE any measurement · frozen-first (a_break_the_wall · c9 · p7)
  (id 1295: H_1294 was taken by the hierarchical-PFC PR #2181 landed concurrently)
================================================================

ANCHOR (the genuinely-GREEN claim to realize, NOT the falsified ones)
  H_609 🟢 SUPPORTED-NUMERICAL — coupling N anima substrates raises faithful
  IIT-4 big-Φ ABOVE the sum of the individual Φ (super-additivity) for SOME
  coupling regime: Φ(AB) > Φ(A)+Φ(B). Peak at rule-pair (110,110), W=0.6:
  Φ(AB)=15.4677 vs Φ(A)+Φ(B)=4.99209  (Δ=+10.4756).

DO-NOT-WIRE (explicitly falsified mechanisms — c9, a_verified_must_wire GREEN-only)
  H_611 🔴 cross-substrate TRANSFER-ENTROPY alignment with collective Φ — FALSIFIED.
  H_617 🔴 SAVANT × hive SI-ratio cross-link (GZ_LOWER knob) — FALSIFIED.
  975  🔴 multi-agent SHARED WORLD-MODEL — RED.
  => wire ONLY the super-additivity of collective faithful-Φ; no TE/SI/shared-WM.

Φ ENGINE (a_phi_iit4_tool — faithful IIT-4 ONLY, numpy NEVER computes Φ)
  stdlib/consciousness/iit4_bigphi.hexa :: big_phi_bounded(tpm, n, sys_state, cap)
  n=3 -> cap=3 (no-op, faithful exact) · n=6 -> cap=2 (M4b SSOT lower-bound).
  Substrate builder = H_609 build_tpm_ab (boundary-edge W-blend), reused verbatim;
  shuffle variant permutes the cross-half boundary wiring (seeded).

SEEDS (3): [1295, 1296, 1297]. The CORE super-additive regime (110,110)@W=0.6 is
  deterministic and seed-INVARIANT (an ECA TPM has no RNG). Seeds drive the angle-1
  SHUFFLE — the random permutation of WHICH boundary cell couples to WHICH across
  the A|B cut.

────────────────────────────────────────────────────────────────
PRE-REGISTERED BARS (frozen — adjust ONLY with honest rationale BEFORE the run)
────────────────────────────────────────────────────────────────

B1  SUPER-ADDITIVITY (the anchor claim)
    (110,110) at coupling W=0.6, every seed:  Δ_coupled = Φ(joint) - [Φ(A)+Φ(B)] >= +1.0.
    MARGIN = +1.0  (conservative; H_609 measured Δ=+10.48, ~10x margin headroom).

B2  ANGLE-1 EMERGENCE CONTROL — TOPOLOGY SHUFFLE (informational, frozen)
    Permute which boundary cell couples to which (same coupling MAGNITUDE, scrambled
    topology). PASS iff Δ_coupled - Δ_shuffle >= +1.0 every seed. [angle-1; see amendment]

B3  NO-COLLAPSE (anti-degenerate-clone)
    coherence := 1 - mean_over_states_cells(|nextA - nextB|).
    PASS iff coherence < 0.999 on >=1 seed (halves functionally distinct, not a clone).

────────────────────────────────────────────────────────────────
ANGLE-2 amendment (a_break_the_wall / c16) — frozen BEFORE the angle-2 run
────────────────────────────────────────────────────────────────
ANGLE-1 result (honest, kept): the topology-shuffle B2 FAILS — a RANDOM equal-magnitude
  cross-wiring produces Δ_shuffle EXCEEDING the structured H_609 coupling on all seeds.
  So super-additivity is NOT specific to the H_609 boundary topology; ANY cross-coupling
  of these substrates integrates (often more). Angle-1 B2 (topology-specificity) is the
  WRONG emergence control — it tests "is THIS wiring privileged" (no), not "is COUPLING
  itself load-bearing" (the real anchor claim). The angle-1 number is REPORTED verbatim
  as honest context but is NOT a GREEN gate.

ANGLE-2 reframed bars (the genuine emergence control for super-additivity):
  B2a DECOUPLE-NULL: at W=0 the joint substrate is sub-additive (Δ(W=0) <= 0). The
      super-additive lift REQUIRES coupling.
  B2b RULE-CLASS DISSOCIATION: the SAME coupling on a STERILE pair (90,90) does NOT
      super-add. PASS iff Δ_coupled(110,110) - Δ_coupled(90,90) >= +1.0 (substrate-CONTENT
      dissociation that a pure variance/magnitude story cannot produce).
  B1, B3 unchanged.
  🟢 GREEN iff B1 ∧ B2a ∧ B2b ∧ B3.

VERDICT RULE
  🟢 GREEN  iff B1 ∧ B2a ∧ B2b ∧ B3. On GREEN: realize a CollectivePool struct + faculty
  in CORE/engine_cli.hexa (N coupled substrate slots + read collective integration via
  faithful IIT-4), re-run the frozen bars ENGINE-NATIVE, prove the existing lanes + decode
  are byte-unchanged (smoke). On RED: report wall, no wiring (a_verified_must_wire GREEN-only).

SCOPE (honest, a_scale_honest_scope / a_toy_scale_recheck)
  TOY: n_a=n_b=3, n_ab=6, cap=2 bounded lower-bound, ECA rule substrate, 3 seeds,
  sys_state=0 anchor only, n=2 members. The experiment is hexa-native (faithful Φ, no
  numpy Φ) but COLLECTIVE-Φ engine-transfer to a live multi-anima deployment is
  UNVERIFIED; this realizes the STRUCTURE, not a scaled multi-substrate daemon. N>2
  scaling = follow-on.
================================================================
