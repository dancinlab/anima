================================================================================
H_1421 — MULTI-LENS breakthrough attempt on the cerebellum×memory engine-BIND WALL
  (H_1417 P4: 🧱 net-lift +0.0437 < +0.05). FROZEN-FIRST pre-registration.
  date: 2026-06-17
================================================================================

Per the STRENGTHENED a_break_the_wall (commit 3304edc65): a (d)-ceiling/subsumption
WALL is NOT confidently terminal until ≥2-3 genuinely DIFFERENT principled lenses
(a_no_llm_frame_trap, real cerebello-hippocampal neuroscience) each fail their OWN
shuffle AND ablation control. ABLATION is decisive — if the lens's mechanism turned
OFF scores the SAME as the lens ON, the mechanism is INERT ⇒ ceiling evidence
(exactly how H_1416 proved its sequential pipeline INERT).

--------------------------------------------------------------------------------
FIXTURE + BARS — FROZEN VERBATIM from H_1417 P4 (cerebellum×memory). NOT moved.
--------------------------------------------------------------------------------
  pair  : cerebellum = vforward_err (fx=3) × memory = immune_grow_recall (fy=0)
  seeds : [5530, 5531, 5532]  (the H_1417 _SEEDS for P4: base 5500+3*10=5530)
  fixture: 5-family structure (F0 X-decisive · F1 Y-decisive · F2 agree ·
           F3 conflict-X-right · F4 adversarial-X-loud-but-wrong), N_PER_FAMILY=90
           → 450 items/seed, deterministic. Each leg = a LIVE engine read.
  E_THR = 0.50 (cerebellum err threshold, == H_1417 P4)

  FROZEN COMPOSE BARS (IDENTICAL to every landed pair H_1407/1414/1415/1417):
    (B1 COMPOSE-EFFECT) acc_compose >= best_single + 0.05
    (B2 ORACLE)         acc_oracle  - best_single > 0.02
    (B3 EARNED/p6)      acc_compose - acc_shuffle  > 0.02
    (SEPARABLE)         only_X > 0 AND only_Y > 0
  NEW gate (a_break_the_wall ablation tool, frozen pre-measure):
    (ABLATION)          acc_lens - acc_ablate > 0   (the lens mechanism is LIVE,
                        NOT inert; ablate = that lens's mechanism turned OFF)
  A lens is 🟢 BOUND iff (B1 AND B2 AND B3 AND SEP AND ABLATION-LIVE) ; else 🧱.

--------------------------------------------------------------------------------
THE 4 LENSES (pre-registered; each scored with shuffle AND ablation):
--------------------------------------------------------------------------------
  L0 BASELINE      — the H_1417 P4 symmetric confidence-routing arbiter. SANITY:
                     must reproduce the H_1417 P4 number (+0.0437, 🧱). No ablation.
  LA SURPRISE-GATE — cerebellar forward-model ERROR signals surprise; surprise GATES
                     the route: low err (model sure) → trust cerebellum vote; high err
                     (surprise) → trust memory recall. ABLATE: freeze reliability at 0.5.
  LB MEMORY-PRIME  — memory supplies the retrieved PRIOR; the forward model adjudicates
                     ONLY when memory abstains/low-conf. memory-grounded → memory, else
                     cerebellum. ABLATE: ignore memory grounding (always cerebellum).
  LC COMPLEMENTARY-TAIL — complementary-learning-systems: the forward model is loud-but-
                     wrong on the adversarial tail; DETECT cerebellar over-confidence
                     (loud AND memory grounded in conflict) → DEFER to the episodic store;
                     else baseline route. ABLATE: tail-deferral OFF → baseline route.

PRE-REGISTERED VERDICT RULE: if ≥1 lens BINDS (all 5 gates) → 🟢, wire it (rung-3+4).
If ALL lenses fail (B1 and/or ablation-INERT) → 🧱 CONFIDENT-TERMINAL after ≥2-3 lenses
(a valid result, c9). NO bar moved, NO tune-to-green (c9/p7). $0 CPU, deterministic.
================================================================================
