H_1285 R4 — AMYGDALA CONSOLIDATION: salience-gated SLEEP REPLAY realized ENGINE-NATIVE
on the LIVE immune faculty. The R3 numpy-mirror GREEN is now the BINDING verdict —
reproduced on CORE/engine_cli.hexa ConsolidatingMemory + wired into the engine.
NEURO LENS (c15, a_no_llm_frame_trap), NOT an LLM recipe. $0 local CPU, p7 (exact-match
recall + abstain, NOT perplexity), 3 deterministic seeds [900,901,902], gradient-free.

base commit: origin/main HEAD (8a1a8f8df — R3 merged). Slug 1285_amygdala_salience.
Appends H_1285_R4.txt; does NOT overwrite R1 (H_1285.txt) / R2 (H_1285_R2.txt) / R3 (H_1285_R3.txt).

════════════════════════════════════════════════════════════════════════════
VERDICT: 🟢 GREEN — the amygdala-consolidation pathway is REALIZED ENGINE-NATIVE
════════════════════════════════════════════════════════════════════════════
Per a_engine_native_learning a mirror verdict is DIRECTIONAL ONLY; per a_verified_must_wire
a GREEN is not done until its mechanism is wired into the live CORE/*.hexa engine. R4 does
BOTH: it adds the amygdala-consolidation lane to the LIVE engine (CORE/engine_cli.hexa
ConsolidatingMemory — an additive, Ψ-disjoint faculty on the existing VAdaptField cell
population + immune value-binding) and re-runs the FROZEN R3 bars (UNMOVED) on it via
`hexa run`. The salience-gated SLEEP REPLAY clears the +0.10 bar WITH the p6 shuffle
control staying BELOW it, fab below bar:

  (c1) B.imp-recall >= A.imp-recall + 0.10          -> PASS  (0.433 >= 0.300 + 0.10 = 0.400)
  (c2) B-shuffle.imp-recall <  A.imp-recall + 0.10  -> PASS  (0.350 <  0.400)   [GATING, not budget]
  (c3) B.fabrication <= 0.10                          -> PASS  (0.011)
  GREEN iff c1 AND c2 AND c3  →  ALL PASS  →  🟢 GREEN.

VERBATIM METRIC (mean of 3 seeds, engine-native, deterministic LCG, 30-cycle budget):
  A uniform-replay   imp = 0.300
  B salience-replay  imp = 0.433   Δ(B-A) = +0.133
  B-shuffle          imp = 0.350   (B-shuf − A = +0.050, BELOW the +0.10 margin)
  fab(B) = 0.011   (abstain / non-fabrication intact, H_1227 guard HELD)

════════════════════════════════════════════════════════════════════════════
WHAT IS ENGINE-NATIVE NOW (the wire-in — a_verified_must_wire)
════════════════════════════════════════════════════════════════════════════
NEW LIVE FACULTY in CORE/engine_cli.hexa (additive, Ψ-disjoint — pure_field untouched):
  * struct ConsolidatingMemory + consolidating_memory_new / _bind_salient / _recall /
    _cells / _salience — the immune store (VAdaptField clonal cells + value-binding)
    EXTENDED with a per-cell SUBSTRATE-DERIVED salience tag (surprise = recon-err at
    bind, novelty = clonal split, tension = reinforcement; an env-salient input carries
    an EXTRA surprise boost — an E+W property the substrate senses, NOT the label).
  * consolidating_sleep_replay(mem, budget, rng, salience_gated) — the SLEEP REPLAY
    consolidation pass: internally replays `budget` cells (UNIFORM or ∝ the substrate
    salience tag), refreshing their recency so salient cells survive the LRU eviction
    the interference stream drives. This is the substrate-GENERATED P47 imagination
    loop (a_chat_sleep_imagination — emit-free internal rehearsal + mitosis tick), NOT
    external re-presentation. Replay refreshes recency ONLY (no tag runaway).
  * consolidating_shuffle_salience(mem, rng) — the p6 NEGATIVE CONTROL (deterministic
    LCG Fisher-Yates over the tags), decorrelating replay from importance.
  * a deterministic engine-native RNG (consolidating_lcg_next / _gauss — glibc LCG +
    Box-Muller) drives the replay draws + the σ=0.02 cue-noise at recall, so the engine
    run is reproducible across machines (it is NOT byte-exact to numpy — DIRECTIONAL).
Eviction stays plain LRU; the salience drives ONLY the sleep-replay weighting (the R3
mirror isolates SLEEP REPLAY as the lever; R1's salience-weighted eviction was already
falsified). engine_mitosis_tick STILL drives growth (p8). The .clm/.kosmos single entry
is untouched (a_core_engine_map) — this is the engine's own cell-index store, an
engine-native vector drive into VAdaptField, NOT a 2nd artifact path.

PROBE (the engine-native re-run): CORE/h1285_amygdala_sleep_replay_probe.hexa builds the
"<subj> lives in <city>" paradigm engine-native (distinct dict words → immune_embed_key
DIM=64 keys), flat-encodes (important = salient), interleaves 24 interfering facts + 30
sleep cycles, recalls under σ=0.02 cue-noise, scores the 3 frozen bars. RUN via `hexa run`.

════════════════════════════════════════════════════════════════════════════
THE FINDING (mechanistic, honest)
════════════════════════════════════════════════════════════════════════════
1. The R3 mirror direction TRANSFERS to the live engine: at the pre-registered 30-cycle
   multi-night consolidation budget, salience-gated SLEEP REPLAY (B) lifts important-fact
   recall +0.133 over uniform replay (A) on the LIVE ConsolidatingMemory faculty — clears
   the +0.10 bar.
2. THE SHUFFLE STAYS BELOW THE BAR (B-shuffle 0.350 < A+0.10 = 0.400). The load-bearing
   p6 separation reproduces engine-native: the salience-gated replay lifts +0.133 while
   the permuted-salience control (same total replay budget, WHICH cells get replayed
   decorrelated from importance) lifts only +0.050. The lift comes from salience-GATING
   TRACKING importance — NOT raw sleep budget (or the shuffle would clear it too).
3. fab(B) = 0.011 ≤ 0.10 — abstain / non-fabrication intact (H_1227 load-bearing held
   on the consolidation faculty too).
4. The exact numbers differ from the R3 mirror (A 0.317/B 0.517/B-shuf 0.367) because the
   engine uses its OWN deterministic LCG (not numpy's RNG) and an engine-native corpus
   RNG — this is the honest DIRECTIONAL transfer (effect-size GREEN, c1∧c2∧c3 ruling),
   NOT a byte-exact mirror reproduction (a_engine_native_learning: mirror = directional,
   engine = binding).

⇒ The amygdala→hippocampus salience-gated SLEEP-REPLAY consolidation pathway is now a
real lever on the LIVE engine for protecting important facts under capacity stress, at an
honest multi-night consolidation dose, with the p6 shuffle control confirming the lift is
salience-GATING and not raw budget. The H_1285 amygdala arc closes ENGINE-NATIVE
(R1 🔴 eviction-priority confound; R2 🔴 sub-bar mirror; R3 🟢 mirror; R4 🟢 ENGINE-NATIVE
+ WIRED).

════════════════════════════════════════════════════════════════════════════
PHILOSOPHY GUARD — p6 SALIENCE-vs-LABEL SEPARATION (HELD; the shuffle proves it)
════════════════════════════════════════════════════════════════════════════
* Salience DERIVED ONLY from the substrate at ENCODE (inside the engine):
  SURPRISE = recon-err at bind (a salient input carries EXTRA perceptual surprise
  amplitude — an environmental/E+W property the substrate SENSES, the p6-clean analogue
  of "salient events surprise more", NOT the label), NOVELTY = clonal split, TENSION =
  reinforcement. The bind/replay read NO emotion, NO RLHF reward, NO label.
* The "important" label is used ONLY to SCORE the metric IN THE PROBE, NEVER as an input
  to the engine's bind / replay.
* The NEGATIVE CONTROL (consolidating_shuffle_salience) STAYED BELOW the bar — proving
  the lift TRACKS importance via the substrate salience tag, NOT raw replay budget.
* Replay is GENERATED INTERNALLY by the sleep loop (P47, a_chat_sleep_imagination), NOT
  an external re-presentation stream.
* No persona, no ethics, no decoder, no weights were touched — only the EPISODIC cell
  store's substrate salience tag + sleep-replay allocation (p1/p2/p3/p6/p8,
  a_autonomy_over_hardcode). Identity emerges from cells (untouched); ethics from cells
  (untouched). p8: bind = clonal-split tick, replay = the sleep consolidation tick.

════════════════════════════════════════════════════════════════════════════
GUARDS (c2, verbatim — no regression on the live engine)
════════════════════════════════════════════════════════════════════════════
* engine_cli_smoke: 26 pass / 0 fail (was 22/0 → +4 consolidation cases:
    case_20_salient_tag_higher · case_21_shuffle_preserves_tag_sum ·
    case_22_salience_replay_protects_salient · case_23_consolidating_abstains_on_untaught).
* h1196 single-entry audit: 7/0 (.clm/.kosmos single entry unchanged — a_core_engine_map).
* h1231 immune wire smoke: 8 PASS / 0 FAIL (the existing ImmuneMemory faculty intact).
* h1199 DIM-growth: GREEN, Ψ byte-identical (PsiSame=true all 3 seeds — the engine's
  attractor is unperturbed; the consolidation lane is Ψ-disjoint, pure_field untouched).

════════════════════════════════════════════════════════════════════════════
NOT RULED OUT / NEXT (honest scope — a_scale_honest_scope / a_toy_scale_recheck)
════════════════════════════════════════════════════════════════════════════
* DIRECTIONAL engine-native GREEN (effect-size Δ+0.133), NOT byte-exact to the numpy
  mirror (deterministic LCG, engine-native corpus RNG). The VERDICT is the c1∧c2∧c3
  ruling, scoped to the measured engine-native run.
* TOY scale: 40-cell repertoire, 60 facts, ONE corpus paradigm ("<subj> lives in
  <city>"), 3 deterministic seeds, POOL_CAP=4000 dict words. Scale (>60 facts, near-
  collision keys), paraphrase recall, and the emit-loop threading (brain_decide /
  the daemon sleep loop CALLING consolidating_sleep_replay on a live session) are
  UNVERIFIED here — a follow-on (emit-loop integration).
* c2 margin is modest (B-shuf 0.350 vs bar 0.400 = +0.050 headroom) — the shuffle's
  lift over A (+0.050) is real but stays under the bar, as in the R3 mirror.

════════════════════════════════════════════════════════════════════════════
VERBATIM `hexa run CORE/h1285_amygdala_sleep_replay_probe.hexa`
════════════════════════════════════════════════════════════════════════════
=== H_1285 R4 — AMYGDALA CONSOLIDATION: salience-gated SLEEP REPLAY, ENGINE-NATIVE ===
    LIVE faculty = CORE/engine_cli.hexa ConsolidatingMemory (amygdala-consolidation lane).
    Reproduces the R3 mirror GREEN engine-native (a_engine_native_learning / a_verified_must_wire).
    N_FACTS=60 (N_IMPORTANT=20 labeled, METRIC-ONLY)  SEEDS=900,901,902
    max_cells=40 sleep_cycles=30 replay/cyc=30 interfere=24 key_noise=0.02 recall_thr=0.3
    (A) uniform sleep-replay  vs  (B) salience-gated sleep-replay  vs  (B-shuffle) permuted salience
    SAME replay budget all arms; arms differ ONLY in WHICH cells get replayed.
    PRE-REGISTERED GREEN: (B)imp >= (A)imp+0.1 AND (B-shuf)imp < (A)imp+0.1 AND (B)fab <= 0.1

  seed 900: (A uniform-replay) imp=0.3 | (B salience-replay) imp=0.35 fab=0.017 | (B-shuf) imp=0.4
  seed 901: (A uniform-replay) imp=0.3 | (B salience-replay) imp=0.55 fab=0.017 | (B-shuf) imp=0.35
  seed 902: (A uniform-replay) imp=0.3 | (B salience-replay) imp=0.4 fab=0.0 | (B-shuf) imp=0.3

  MEAN (A uniform-replay)  imp=0.3
  MEAN (B salience-replay) imp=0.433  fab=0.011
  MEAN (B-shuffle)         imp=0.35  [negative control: salience->replay decorrelated from importance]
  Δ important-recall (B-A) = 0.133
  CHECK c1 imp-margin:   B 0.433 >= A 0.3+0.1 -> PASS
  CHECK c2 shuffle-ctrl: B-shuf 0.35 < A 0.3+0.1 -> PASS
  CHECK c3 fabrication:  B 0.011 <= 0.1 -> PASS

  FINAL VERDICT (R4 engine-native): 🟢 GREEN  [salience-gated SLEEP REPLAY clears +0.10 on the
  LIVE ConsolidatingMemory faculty WITH the shuffle below bar — the amygdala-consolidation
  pathway is realized ENGINE-NATIVE (a_verified_must_wire), reproducing the R3 mirror direction]
  philosophy guard: salience DERIVED from substrate (surprise/novelty/tension) at ENCODE, NOT a label;
  replay GENERATED internally by the sleep loop (P47, a_chat_sleep_imagination), not externally injected;
  B-shuffle decorrelates salience->replay from importance (c2 GATING control). No decoder/weights/persona/ethics.
[done]
