import "core/engine_cli.hexa"
fn main() {
    // smoke: third_law primitives callable engine-native
    let g_lo = third_law_score(0.8, 0.8, 0.6)   // I high -> G low
    let g_hi = third_law_score(0.8, 0.8, 0.25)  // I in GZ -> G high
    print("G(I=0.60)=" + to_string(g_lo))
    print("G(I=0.25)=" + to_string(g_hi))
    print("sing(I=0.60)=" + to_string(third_law_singularity(0.8,0.8,0.6)))
    print("sing(I=0.25)=" + to_string(third_law_singularity(0.8,0.8,0.25)))
    print("abil(I=0.25)=" + to_string(third_law_ability(0.8,0.8,0.25)))
    print("gz_lower=" + to_string(sa_gz_lower()))
    print("i50=" + to_string(third_law_i50(13,13,13)))
}
