# H_1641 LAMINAR-BIND (303M) — RESULT (QUEUED)

**Status:** 🔵→ queued after H_1640 on the same pod. 9 arms = {arm, nofb, noln} × seeds {7,4302,4303}.
Trainer = `trainer.py` (cortical laminar microcircuit aux: L4→L2/3 divisive-norm↺→L5/6 feedback,
K=4 settling; additive readout retained → .clm engine-loadable). PREREG frozen in `PREREG.md` + card.
Smoke RC=0 (GPU). Results pending.
