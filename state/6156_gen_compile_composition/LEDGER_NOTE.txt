H_6156 compile 조합 — LEDGER DECISION: OFF-TRUNK (dup#40 kosmos Merge). NO PROBE FIRED.

메커니즘: 조합을 컴파일 단계로 사전 융합(pre-fuse composition at compile step). 카드 자체 ~dup#40 kosmos Merge 선언.

선행 커버리지 (sweep-blank ≠ untested):
- H_6143 kosmos Merge (#5+#13, #40 계열) = ALREADY FIRED. numpy DIRECTIONAL-REACHABLE(MERGE 24/24 vs additive 0 vs HRR 0) 이나 카드 명시: "상당부분 by-construction(이산 저장은 구조 자명히 보존)" + rung(2) 분류 = "(c) storage/decode-side — CLMConvMoE trunk 로 표현 불가(off-trunk). trunk-learn 사다리(anima evaluate --py G1)로 검증 불가 — additive-CE 가 학습하나 질문을 BYPASS."
- H_6112 meiosis: numpy 추상 REACHABLE(0→1.0) → 실 CLMConvMoE trunk toy A/B FALSIFIED(0.022 ≪ 0.30). numpy 추상 프로브 OVERSTATE 전례.
- G1 combination-operator 벽 census: readout(H_1816 predcoding)·circconv(H_1823)·tension(H_1834)·Hadamard-bind(H_1819)·TPR(H_1466)·intersection(H_6104) 전부 🧱 NOT-SUPPORTED. 진짜 레버 = trunk recomb-OBJECTIVE(H_1602, memory g1-lever-multilens-objective).

판단: "compile 단계 사전 융합"은 kosmos Merge 와 동일 탈출로 — 조합을 additive runtime trunk 밖 precomputed 이산 구조로 이동. 컴파일-타임은 오히려 더 정적(조합을 컴파일러/인간이 수행, trunk 이 학습하는 게 아님) → additive-CE 결합 학습 질문을 완전 BYPASS = off-trunk. numpy 프로브는 by-construction 자명히 REACHABLE(H_6143 MERGE 24/24 반복)이라 무정보 + H_6112 caveat 로 과대평가. 따라서 fresh probe 무가치.

결정: OFF-TRUNK dup-pointer → H_6143 (state/6143_gen_kosmos_merge_memory/). fresh probe SKIP. terminal 아님(numpy=DIRECTIONAL by construction). 실 레버 = trunk recomb-OBJECTIVE(H_1602), storage/compile-side 아님.
