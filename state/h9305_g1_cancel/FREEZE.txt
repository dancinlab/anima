H_9305 — G1-CANCEL: pooled 0 이 층화 상쇄(Simpson/mixture cancellation)를 숨겼는가
================================================================================
PRE-REGISTERED — 데이터(층별 효과) 판독 前 동결 · frozen-first · c9 · p7 · NO tune-to-green
freeze_ts_local: 2026-07-14 (KST) · lane: g1-crack-natural-emergence
설계: Fable 5 (카드 A · state/fable_killshots/w1_G1재조합.out.md) · 집행: 메인 세션
선행: H_9304 🧱 DATA-ADDITIVE (pooled held-out EARNED +0.00233 · TOST(±0.02) PASS)

--------------------------------------------------------------------------------
1. 가설
--------------------------------------------------------------------------------
H_9304 의 pooled +0.0023 nats 는 층(register/도메인) 내부의 **반대부호 시너지가 상쇄된 평균**이다.
(부정이 극성을 뒤집는 층 vs 반어·완서법·관용구가 되뒤집는 층이 섞이면 정확히 이 모양.)

--------------------------------------------------------------------------------
2. 계기 — H_9304 인증계기 그대로 재사용 (1바이트도 수정 금지)
--------------------------------------------------------------------------------
state/h9304_g1_earned/h9304_g1_earned.py 의 build/fit_and_score/earned/make_heldout/
_stratified_shuffle_B/synth 를 **import** 해서 쓴다. 추정량·통제·순열 정의 변경 0.
바뀌는 것은 **판독 granularity 뿐** (corpus-global → 층별).
코퍼스: NSMC train sha256 e03b7d14... (H_9304 와 동일 · provenance gate 유지)
어간 vocab: min_occ=100 **전역 1회** 고정 (층별 재선택 금지 = post-hoc selection 차단)

--------------------------------------------------------------------------------
3. DV
--------------------------------------------------------------------------------
층 k 의 held-out EARNED  I_k  (H_9304 계기 연산 그대로 · 층 내부에서 α,γ,δ,held-out 전부 재적합)
상쇄지수  C = Σ_k w_k·|I_k| − |Σ_k w_k·I_k|      (w_k = 층 k 의 held-out 셀 수 / 총합)
   C ≈ 0  → 상쇄 없음 (pooled 0 = 층별 0)
   C ≫ 0  → pooled 0 이 반대부호 층효과를 숨김

--------------------------------------------------------------------------------
4. 층화 스킴 (사전 고정 · 사후 추가 금지)
--------------------------------------------------------------------------------
⚠️ **설계 편차 (정직 선언)**: 도시에의 "HF 4-cell register(ko/en×general/sns)" 층은
   **적용 불가**다. 계기는 토큰 외부 라벨 T 를 요구하는데 T 를 가진 코퍼스는 NSMC 뿐이고,
   HF 4-cell register 코퍼스는 T 라벨이 없다(그리고 NSMC 엔 register 메타데이터가 없다).
   ⇒ register 축은 이 계기에 **교차 자체가 불가능**. 4-cell 을 억지로 쓰면 T 가 사라져 계기가 죽는다.
   대체: NSMC **내부**에서 공짜로 얻는 사전등록 자연 층 3종 (라벨 튜닝 0):

   S_CLUST  (PRIMARY · 도시에 명시)  = 비지도 클러스터 k=8
              char-3gram hashing(256d) → L2 정규화 → k-means(10 iter · seed 9305)
   S_LEN    (SECONDARY)             = 바이트 길이 4분위 (4층)
              register 대리 (짧은=SNS 어투 / 긴=서술형) · a_korean_byte_budget: 문자 아닌 **바이트**
   S_MARKER (MECHANISM 진단)        = 부정 마커 계열 6종 (지않·지못·안·못·아니·없)
              ⚠️ 분할(partition)이 아님 — B=0 행을 공유하므로 w_k 합 ≠ 1 ⇒ **C 지수 계산 안 함**.
              대신 "부정 종류마다 연산자 방향이 달라 상쇄된다"는 기전을 δ̂_m 부호로 직접 본다.
              마커 m 아암 = B_m(그 마커만 1) + B=0 행 풀 공유 + 다른 마커 행은 제외.

   총 검정 층 수 = 8 + 4 + 6 = **18**

--------------------------------------------------------------------------------
5. Arms (4)
--------------------------------------------------------------------------------
EXP    : 진짜 층화 (위 3 스킴)
CTRL1  : **크기-매칭 무작위 층** — 각 스킴의 층 크기 분포를 그대로 두고 행을 무작위 재배정.
         구조만 파괴 · 소표본 추정편향은 보존 (매개 공변량 = 층 크기).
         전 층 |I_k| ≤ 0.02 여야 함 (아니면 층별 판독 자체가 소표본 artifact ⇒ INVALID).
CTRL2  : **층내 라벨-순열 귀무** k_perm=500 (층별 참값 0 PEDESTAL)
         = H_9304 의 T-층화 B-셔플 (P(B,T) 정확 보존 · A–B 짝만 파괴 = 시너지만 파괴).
POS    : **양성대조 = XBIND ⊕ 부호반전-XBIND 50/50 혼합** (참 pooled ≈ 0 · 층내 ≈ +5.3)
         어간 풀 공유 · 층 P: T=xor(pol_A,B) · 층 N: T=1−xor(pol_A,B).
         pooled 로 적합하면 α_A 가 두 층에서 상쇄되어 ≈0 ⇒ pooled EARNED ≈ 0.
         층별로 적합하면 각 층이 완전 flip 연산자를 회수 ⇒ I_k ≈ +5.3, C ≫ 0.
         **이 아암이 실패하면 verdict = INVALID** (계기가 상쇄를 애초에 검출 못 함).

--------------------------------------------------------------------------------
6. 검정력 (데이터 보기 前 계산 · power-before-negative)
--------------------------------------------------------------------------------
pooled: n_heldout=35,911 · sd_null=0.000197 · MDE(3σ)=0.00059 (H_9304 실측)
층별 예측: n_k ≈ 35,911/8 ≈ 4,489 ⇒ sd_k ≈ 0.000197·√8 ≈ 0.00056 ⇒ MDE_k(3σ) ≈ 0.0017 ≪ 0.02 ✅
   ⇒ 층당 등가마진 ±0.02 판정은 **사전 계산상 powered**.
   실측 sd_null_k 를 층마다 다시 재고, **MDE_k = 3·sd_null_k > 0.02 인 층은 그 층에 한해
   NOT-POWERED 로 보고**하고 등가(TOST) 주장에서 제외한다.

--------------------------------------------------------------------------------
7. PASS / FAIL / INVALID (사전 고정 · 1바이트 사후 변경 금지)
--------------------------------------------------------------------------------
등가마진 Δ_eq = **±0.02 nats** (H_9304 와 동일 · 상속)
다중검정 보정: 18 층 ⇒ PEDESTAL bar 는 Bonferroni 조정 양측 5% 귀무분위수
   ped_k = quantile( |null_k − mean(null_k)| , 1 − 0.05/18 )
   층 유의 bar_k = max(ped_k, 0.02)   ← 절대 floor 0.02 는 어떤 경우에도 하향 금지

PASS  = (≥1 층에서 |I_k| > bar_k)  ∧  (CTRL1 전 층 |I_k| ≤ 0.02)  ∧  (POS 검출: 층별 I ≥ +0.30
        이고 POS pooled |I| ≤ 0.02 이고 C_POS > 1.0)
FAIL  = 전 층 TOST(±0.02) 통과 (CI 가 (−0.02,+0.02) 안) ∧ POS 검출 ∧ CTRL1 clean
        ⇒ H_9304 가 층 granularity 까지 강화됨 · 각도① 사망
INVALID = POS 미검출 (계기가 상쇄를 못 봄) 또는 CTRL1 오염(무작위 층이 |I|>0.02 를 뱉음)
NOT-POWERED = 다수 층에서 MDE_k > 0.02

C 지수 부수 판정: C_EXP 를 CTRL1 의 C 분포(무작위 층)와 비교 — C 자체는 |·| 때문에 0 이 하한이라
   **양수인 것만으로는 근거가 안 된다**(순서통계량/절대값 편향). CTRL1 대비 초과분만 인정.
⚠️ max(controls) 순서통계량 금지 — 층별 비교는 전부 paired (같은 층 index · 같은 seed).

--------------------------------------------------------------------------------
8. 반증되면
--------------------------------------------------------------------------------
상쇄-artifact 탈출구 사망 — H_9304 의 DATA-ADDITIVE 가 층 granularity 까지 강화되고,
G1 재조합벽의 "자연이 연산자를 공급하지 않는다"가 corpus-global 뿐 아니라 **층 내부에서도** 참.
남는 자연-데이터 다리 = 카드 B(병렬쌍) · 카드 C(대화 턴) 뿐.

비용: $0 CPU 로컬 (GPU/pod 발사 없음)

================================================================================
AMENDMENT 1 — 양성대조가 계기 결함을 잡음 (주 bar 판독 前 · bar 무이동)
amend_ts_local: 2026-07-14 (KST)
================================================================================
POS 아암 실측 (주 bar 판독 前 · 스크립트는 층 판독 전에 hard-exit):
    POS pooled    I = +0.00033   δ̂ = −0.90      ← 참 상쇄 실재 (pooling 은 눈이 멂)
    POS stratum 0 I = +4.42251   δ̂ = −3.00      ← 층별로 읽으면 회수됨
    POS stratum 1 I = +4.41661   δ̂ = −3.00
    C_POS = Σw|I| − |Σw·I| = 4.4195 − 4.4195 = **0.00000**   (bar > 1.0 ⇒ 게이트 FAIL)

진단 — **C 지수(Fable 카드 A)가 구조적으로 틀렸다** (계기가 아니라 요약통계량의 결함):
  추정량 EARNED = CE_add − CE_op 는 δ 를 **데이터에서 적합**하므로 (H_9304 가 하드코딩 flip 을
  버리고 고친 바로 그 지점) **연산자 회수 '크기'** 다 — 연산자가 **어느 방향이든** 존재하면
  I ≥ 0 이다. 반대 방향 연산자(flip vs 되flip)는 **I 의 부호반전이 아니라 δ̂ 의 부호**로 나타난다.
  ⇒ C = Σw|I| − |Σw·I| 는 I_k 가 음수여야만 커지는데, 이 추정량은 그런 I 를 만들 수 없다.
  ⇒ C 는 **이 추정량이 실제로 만들어내는 상쇄를 영원히 표현 못 한다.** (POS 로 증명됨)
  ⚠️ C 를 그대로 뒀다면 "C≈0 ⇒ 상쇄 없음"을 **모든 코퍼스에서 자동으로** 얻었을 것 —
     참값이 상쇄인 POS 에서조차 C=0.00000. **정확히 tune-to-green 의 반대 방향 결함**
     (내 주장에 유리한 게 아니라, 판정 자체를 무의미하게 만드는 결함).

수리 (bar 1바이트 이동 없음 · 주 PASS/FAIL 논리 무변경):
  C → **D = Σ_k w_k·I_k − I_pooled** (층화 판독 − pooled 판독) 로 교체.
  D 는 두 상쇄 채널을 **모두** 표현한다:
    (a) 주효과 상쇄 (α_A 가 층간 반대 → pooled α 붕괴 → pooled I≈0, 층별 I≫0)  ← POS 가 이것
    (b) 연산자 방향 상쇄 (δ̂ 가 층간 반대 → pooled δ 는 평균이라 양쪽 다 손해)
  둘 다 D > 0 을 낳는다. D_POS = 4.4195 − 0.0003 = **+4.4192 > 1.0** (동일 수치 bar 1.0 유지).
  POS 검출 = (pooled |I| ≤ 0.02) ∧ (층별 I ≥ +0.30) ∧ (**D_POS > 1.0**)   ← C_POS 만 D_POS 로 교체

  D 의 통제: 무작위 층도 층당 자유파라미터가 늘어 D > 0 을 **소표본 과적합만으로** 만든다.
  ⇒ CTRL1(크기-매칭 무작위 층)이 정확히 그 공변량이다. 인정 근거 = **D_exp vs D_ctrl1**,
     그리고 pooled anchor 는 두 D 에서 상쇄되므로 실질 대비는 **Σw·I_EXP vs Σw·I_CTRL1**,
     **층 index 로 paired** (같은 k · 같은 seed · 크기 매칭) + **paired-t**.
     max(controls) 순서통계량 금지 준수.

**주 PASS/FAIL 판정 논리는 1바이트도 안 바뀐다** (§7 그대로):
  PASS = (≥1 층 |I_k| > max(ped_k, 0.02)) ∧ (CTRL1 전 층 |I_k| ≤ 0.02) ∧ (POS 검출)
  FAIL = 전 층 TOST(±0.02) ∧ POS 검출 ∧ CTRL1 clean
  D 는 §7 이 이미 "부수 판정"으로 강등해 둔 C 의 자리를 그대로 승계한다.

판독 무결성: 이 수정 시점에 **자연 층 수치는 하나도 안 봤다** (스크립트가 POS 게이트에서
  exit). 본 세션이 본 자연 수치는 pooled anchor +0.00232 뿐이고 이는 H_9304 가 이미 발표한 값.
  선례: H_9304 자신이 참값-0 PEDESTAL 로 계기 결함 2건을 주 bar 판독 前에 잡아 수리(bar 무이동).

================================================================================
AMENDMENT 2 — V5 SEED 게이트가 **사전등록 층별 bar 자체를 무효화**함 (사후 · 판정 하향)
amend_ts_local: 2026-07-14 (KST)
================================================================================
동결 스크립트는 4/4 seed 에서 문자 그대로 **PASS** 를 출력했다 (≥1 층 |I_k| > bar_k).
그 PASS 를 **채택하지 않는다.** 이유 — 사전등록 bar 가 잘못 계측됐다 (내 주장에 **불리한** 방향의 수정):

§6 의 검정력 예측이 40배 틀렸다. sd_k 를 pooled 순열 sd 를 √8 배 해서 0.00056 으로 잡았는데,
그건 **순열 성분만** 스케일한 것이고 지배적 분산 성분인 **held-out 어간 추출(make_heldout) 분산**을
빠뜨렸다. 동일 층(S_LEN k0 = 결정론적 바이트 4분위 · seed 무관하게 같은 층)을 4 seed 로 재추출:
      I = +0.02124 / −0.01434 / −0.02430 / +0.01689   ← **부호가 뒤집힌다**
      sd(seed 간, 진짜 표집분산) = 0.02260
      sd(순열 귀무 = bar 가 쓴 것)= 0.00090     ⇒ **25배 과소** ⇒ 진짜 MDE(3σ) = **0.0678 ≫ 0.02**
⇒ ped_k·MDE_k 는 추정량의 실제 불확실성의 4% 만 반영 ⇒ **|I_k| > max(ped_k,0.02) 는 유효한
   유의성 검정이 아니다.** 절대 floor 0.02 를 넘는 것은 순열 귀무가 **구조적으로 볼 수 없는 잡음**이다.

실증: "유의한" 층 집합이 seed 마다 **완전히 다르고 부호도 무작위**다.
   seed 9305 [CLUSTk3 −0.067, LENk0 **+0.021**] / seed 1 [CLUSTk3 −0.031]
   seed 2    [CLUSTk7 −0.021, LENk0 **−0.024**] / seed 3 [CLUSTk2 +0.021, MARK-지않 +0.021]
   같은 LENk0 이 +0.021 과 −0.024 를 오간다 ⇒ 층 구조가 아니라 held-out 추출 잡음.
⇒ **층별 판독은 NOT-POWERED.** PASS 도 FAIL(TOST)도 licensed 되지 않는다
   (sd 0.0226 자체가 등가마진 0.02 보다 크므로 어떤 층도 TOST 통과 불가).

**단, 검정력이 살아있는 판독이 하나 있다 — §7 이 사전등록한 상쇄 잉여 (CTRL1 대비 초과분):**
   surplus = D_exp − D_ctrl1  (paired · seed index 로 매칭 · max(controls) 미사용)
   상쇄 가설의 방향성 예측 = surplus > 0 (층화가 pooling 이 파괴한 걸 회수한다)
   실측  S_LEN   : −0.00585 ± 0.00212  paired-t = **−5.51** (n=4) p≈0.0004   ← 예측과 **반대**
         S_CLUST : −0.00302 ± 0.00279  paired-t = **−2.17** (n=4) p≈0.030    ← 예측과 **반대**
   D_exp(S_LEN) 는 4/4 seed 전부 **음수** (층화 판독이 pooled 보다 **적게** 회수) 이고
   무작위 층(CTRL1)보다도 **낮다**. ⇒ 숨겨져 평균화된 신호는 **없다**. 상쇄 기전은 **반증**.

계기 무결성 (4/4 seed): POS 발화 True (pooled ±0.0007 · 층내 +4.42~+4.52 · D +4.42~+4.50) ·
   CTRL1 오염 0 · 참값0 pedestal 정상.

**최종 판정 = NOT-POWERED** (층별 bar 무효 ⇒ 층 granularity 에서 PASS/FAIL 둘 다 못 찍음)
   + 부수 결론 (powered): **상쇄 잉여 = 0/음수 ⇒ 상쇄 기전 반증 · H_9304 pooled 판독은 안 뒤집힘.**
정직: seed 9305 에서 멈췄으면 **거짓 PASS 를 출고했을 것이다.** V5 seed 게이트가 그걸 막았다.
재개 조건 = 층별 bar 를 **held-out 재추출 분산 포함**으로 재구축(seed≥20 부트스트랩) 후 재발사.
