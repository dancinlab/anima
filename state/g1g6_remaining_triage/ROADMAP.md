# G1/G6 Residual Lever Triage -> Single Conditional GPU Roadmap

**Date:** 2026-07-05 · **Scope:** synthesize 6 residual lever triages + H_9200 program-closure into ONE
GPU roadmap forked on the PENDING FiLM-303M crux. $0 read+reason only (no GPU/303M/train).

**Ledger verification (this session, against live repo):**
- H_1598 depth-as-G1-lever = FALSIFIED TERMINAL (archive/state/verdicts/1598...), byte-identical L4==L8 wall, re-fire banned -> convmoe_deep_g1 CLOSED. verified in HYPOTHESES.jsonl.
- H_1595 h1129 g6_multiseed = GENUINE G6 WALL TERMINAL (all 3 seeds fals=0) -> g6_multiseed_h1129 SUPERSEDED.
- H_6167 = NO OPERATOR LEVER (bilinear-additive delta <= +0.026, recombination=task-structure-bound) + H_1623 frozen FiLM-readout de-risk FAIL 0/3 -> film_readout_303m frozen form walled.
- H_1601/H_1602 = binding-lane INERT / recombination-objective NOT-SUP -> operator/objective axes floored.
- state/g0g6_premise_b_derisk/E1_303M_handoff_prompt.md EXISTS -> slot_cement_303m OWNED by parallel E1 lane (4/4 de-risk GO).
- state/transfer_mechanism_sweep/film303/ = ZERO artifacts -> the FiLM-303M crux is genuinely PENDING, not landed. This is the only live fork-gate.
- Transfer sweep (state/transfer_mechanism_sweep/SWEEP_SYNTHESIS.md): corrected pure-bilinear target, escape=MULTIPLE-ESCAPES (bilinear/multiplicative class: hypernet/TPR/FiLM/slot); additive/linear-SSM collapse; recurrent=capacity!=transfer (GRU cross memorize). Task is synthetic pure-bilinear (additive floor forced to ~0) -> conditional; REAL-303M crux required.

---

## (1) LIVE-GPU-CANDIDATE list + priority

**Honest headline: ZERO independent GPU fires are justified NOW.** Every mechanism axis (operator,
depth, binding-lane, objective, frozen-readout, mitosis, savant) is already engine-native walled. The
only live artifact is the $0 FiLM-303M crux (a pool 303M-forward probe, NOT a GPU build). All GPU
candidates below are strictly downstream of and gated on a MECHANISM-SIDE crux landing.

Conditional priority (fires ONLY if crux = MECHANISM-SIDE):

| # | candidate | owner | trigger | cost |
|---|-----------|-------|---------|------|
| 0 | FiLM-303M crux ($0 REAL-rep probe: load 303M, extract joint pair-rep, fit FiLM vs additive linear head, cross-distribution R2) | this frontier | fire NOW, fork-gate | ~$0 pool |
| 1 | E1 gated-write forward-slot 303M cement (TRAINED gate + F2 order-dense corpus) | parallel E1 lane (handoff authored, DO NOT self-fire) | crux MECHANISM-SIDE | $0 pool / ~$5-10 rent (rent=spend -> owner GPU-go) |
| 2 | end-to-end FiLM-conditioned trunk train (gamma(a)*enc(b)+beta(a) -> core/generator.hexa L3 slot; NOT frozen readout -- frozen form=H_1623 FAIL) | this frontier | crux MECHANISM-SIDE AND E1 slot underperforms OR as parallel arm | ~$0.6-2 one cost-gated fire |
| 3 | TPR 1-shot contrast · hypernet ceiling (remaining bilinear-class members) | follow-on | after #1/#2 lift confirmed | pool $0 |

**Do NOT fire independently: film_readout_303m (re-runs H_1623+H_6167), convmoe_deep_g1 (H_1598
re-fire BANNED), g6_multiseed_h1129 (H_1595 cemented), savant_mitosis_303m (mitosis DPI-FLOOR),
h6163_trained_lane (additive form collapses per #3031).**

---

## (2) SUPERSEDED / CLOSED (re-fire prohibited)

- convmoe_deep_g1 -- CLOSED-WALLED. H_1598 FALSIFIED depth-as-G1-lever, engine-native TERMINAL, ARCHITECTURE re-fire ban. #3031 sweep confirms mechanism (depth=capacity!=transfer, no multiplicative operator). Residue: scrub STALE ING #42492882/883/881.
- g6_multiseed_h1129 -- SUPERSEDED. H_1595 GENUINE G6 WALL cemented; frozen verdict archive/state/verdicts/1595...; mini re-measure resolved the aiden infra-wall (SPEC premise = stale ING #42492907 snapshot). Orthogonal to crux either way.
- film_readout_303m (frozen-readout form) -- SUPERSEDED. H_1623 frozen FiLM de-risk FAIL 0/3 + H_6167 NO OPERATOR LEVER + gamma-B3 TRANSFER FAIL TERMINAL + C2 20-family census 0 survivors. This session's pure-bilinear sweep (FiLM cross R2=0.512 vs additive -0.034) is the H_6166/6167 measurement-INVALID artifact regime. Reopens ONLY end-to-end (not frozen) on MECHANISM-SIDE.
- slot_cement_303m -- SUPERSEDED (deferred to owning E1 lane). H_9200 Family-E E1, 4/4 de-risk GO, build spec authored (E1_303M_handoff_prompt.md). Load-bearing precedent A11: CE-deleted FIXED-role forward-slot ALREADY floored on real 303M --py (0/5, H_9121) -> E1 MUST use a TRAINED gate + F2 corpus. E1's own confirmed bottleneck = corpus density, not mechanism.
- h6163_trained_lane -- SUPERSEDED. #3028 trained frozen-rep readout transfers BELOW chance (rep_cross 0.4286 < 0.65 bar) -> DIRECTIONAL-WALL; DESIGN.md additive form is exactly the collapsing class per #3031. Folds into F2 data lever.
- savant_mitosis_303m -- SUPERSEDED (retire as G1 lever). Mitosis=DPI-FLOOR (H_9131 trunk-obj family CLOSED); savant in closed G1 census; neither introduces a bilinear binding form = the non-escaping capacity/schedule class. If savant wanted: wire PART-A as default-OFF disjoint faculty ($0 CPU-local, no G1 claim), like L5 store GREEN #2996.

---

## (3) FiLM-303M crux (PENDING) -- conditional true OPEN frontier

The 74-item H_9200 program has no $0 crack left (SWEEP_SYNTHESIS section5: 8 WALL, 12 GREEN-confirmatory,
4 INCONCLUSIVE). It converges to exactly one live fork-gate: does REAL 303M joint pair-rep carry a
transferable bilinear form that a bilinear (FiLM) readout can extract cross-distribution, better than
additive? The synthetic sweep says bilinear escapes on a forced-additive-floor task -- the crux tests
whether that survives on real reps where linear terms are NOT zeroed out.

TRUE OPEN = conditional. It is OPEN only in the MECHANISM-SIDE branch. TARGET-SIDE closes it into the
already-known F2 authored-data problem (no new mechanism frontier). The crux is $0 (single 303M forward +
linear head fit on pool summer/aiden), so fire it FIRST, before committing any GPU budget.

Frozen crux bar (pre-register, no tune-to-green): MECHANISM-SIDE iff FiLM cross-distribution R2 beats
additive+linear-term baseline by a real margin (>= the +0.15 H_6167 threshold, NOT the artifact +0.5) on
held-out DISJOINT concept split, with shuffle/order control collapse. If FiLM ~= additive+linear on real
reps -> TARGET-SIDE.

---

## (4) GPU roadmap -- MECHANISM-SIDE vs TARGET-SIDE

### Branch X -- MECHANISM-SIDE (real 303M rep has transferable bilinear form)
Escape is in the mechanism -> the operator matters after all (against H_6167's synthetic-regime
finding, but on real reps). Sequence (a_wall_first, parallel where possible):
1. Crux already positive -> promote per MULTIPLE-ESCAPES law: cheapest first.
2. E1 gated-write SLOT 303M cement (owned lane) = the #1 build -- TRAINED gate (NOT walled A11 fixed-role) + F2 corpus. Coordinate via handoff; do not self-fire.
3. end-to-end FiLM-conditioned trunk train (this frontier) as #2 build/parallel arm -- gamma(a)*enc(b)+beta(a) into core/generator.hexa L3 slot, O(d) DPI-native GLU, disjoint from emit + section-ImmuneMemory G5 gate (a_substrate_disjoint), unified core/decode+core/serialize, .hexa engine-native. Retrain on cross-distribution held-out concept split.
4. Measure = anima evaluate --py <clm> single canonical path, G0-G6 ladder, heavy 303M decode on pool summer/aiden (NEVER mini -- swap OOM rc=137).
5. Frozen GREEN bar: G1 composed_distinct bd>=2 > max_single AND coherent AND shuffle-bind control drop >=0.15 held-out AND slot-ablation collapse AND FLAT-control FAIL; >=2/3 seeds {7,4302,4303}; G0 kwr>=0.5 4/5 preserved, threshold-shift 0. KILL if margin <=0 or shuffle non-collapse.
6. Then TPR 1-shot contrast, hypernet ceiling. G1 engine-native ladder lift -> H_9200 program GREEN closure.
- GPU cost: all owned pool ($0 rent) OR ~$0.6-2/one cost-gated fire; E1 ~1-3 GPU-h/seed x3.

### Branch Y -- TARGET-SIDE (real reps: additive ~= FiLM; wall = data/target, mechanism-independent)
Escape is in the data (section4 + F2 convergence). Any readout floors on the current collocation-only
corpus -> firing E1/FiLM now = wasted GPU floored like the A11 fixed-role slot (--py 0/5).
1. NO mechanism GPU-go. Redirect budget mechanism-build -> authored-data-build.
2. F2 authored transferable-form corpus first -- held-out re-combination requires derivation-trace / order-dense authored data (H_9124), NOT any readout operator. $0 tiebreaker first: F2 held-out disjoint-vocab check.
3. E1 slot / FiLM trunk become contingent on the new corpus (retrain on F2, not current). Re-measure via anima evaluate --py.
4. h6163_trained_lane folds entirely into this data lever (no separate core/ GPU lane); savant_mitosis still floors on collocation corpus -> stays retired.
5. Frozen bar unchanged (same G1 ladder, on F2-trained ckpt). No tune-to-green.

**Both branches:** convmoe_deep / g6_multiseed / frozen-FiLM-readout stay CLOSED; scrub STALE ING board
(#42492882/883/881/907). The program is now build + 1 crux frontier, not a lever brainstorm.
