{"type":"result","subtype":"success","is_error":false,"api_error_status":null,"duration_ms":249148,"duration_api_ms":250407,"ttft_ms":179657,"ttft_stream_ms":4727,"time_to_request_ms":28,"num_turns":3,"result":"Ledger check: no prior H tests morpheme atomization against G1; H_1388/1390 (BPE-on-jamo) built unsupervised ko-segmentation infra for an emit-bias lever — reusable as the segmenter here, never pointed at recombination. Design follows.\n\n---\n\n# Morpheme-lever spec: atomicity vs. identity — the leak is separable\n\n## 0. Resolution of the key design question\n\n**Yes, the lever is distinguishable from the leak, and the separating line is *identity vs. atomicity*.**\n\n- **Leak** = handing the model the *equivalence class*: 안/않/못/아니 → one shared `⟨NEG⟩` ID. Transfer to a held-out stem is then trivial (it literally is the trained token).\n- **Non-leak** = handing only *unit boundaries*: an **unsupervised** segmenter (BPE-on-jamo, H_1388 infra — no linguistic labels) applied uniformly to the whole lexicon, giving every frequent morpheme — including each of the 4 negation stems — a **distinct** atomic ID. Nothing marks the 4 stems as related. The equivalence class must then be *induced* from unlabeled distributional co-occurrence during plain-CE continued pretraining (CPT). That induction step is exactly the abstraction NBIND-FC showed the byte substrate never performs; if it happens here, it was earned, not handed.\n\nThe causal channel is provable with two arms: an **ablate-the-induction control** (remove the held-out stem from CPT → its code has no distributional geometry → transfer must floor) and a **hand-the-identity ceiling** (shared-`⟨NEG⟩` arm = positive control proving the eval can see transfer at all). If the main arm lands between floor and ceiling, the class was induced. So the lever is falsifiable at 303M — no scale punt needed. The one residual unfalsifiable-at-this-budget branch is \"CPT too short for clustering to form\", and the design below converts that from a silent FAIL into an explicit **PENDING** via a mid-run gate.\n\nCritically, **no architecture change is needed**: a morpheme tokenizer is expressible as a **reversible byte codec** — map each segmenter-induced morpheme to a distinct short code in unused byte space, retrain on the remapped stream. The 303M CLMConvMoE engine, `anima-py train`/`evaluate`, and the XOR-grid protocol all run unmodified; the intervention is a documented corpus-side codec (leak-auditable in ~50 lines).\n\n## 1. (a) $0 frozen probe — SPAN-GEOM (run first; can kill the lever for free)\n\nThe earn-seal (PROBE_A) tested a stem-invariant **flip axis** in *sentence mean-pool* — a consumption-adjacent readout. The lever's actual premise is one level lower: *\"byte tokenization prevents the substrate from forming a stem-level NEG equivalence class at all.\"* That premise is testable on the frozen base ckpt at the **span** level:\n\n- **Data**: ≥300 wild occurrences per stem (NSMC + general ko), Korean-aware disambiguation filters (안 = pre-verbal negation only, exclude locative noun; 못 exclude nail/pond noun; 아니 in 아니-/아니라 forms). Controls: position-matched pre-verbal adverbs (잘/좀/다시/자주/많이) + frequency-matched random syllable spans.\n- **Representation**: trunk activations **span-pooled over the stem's own bytes** (layer sweep L1–L4 + penultimate; ±0/±3-byte window ablation) — not sentence mean-pool.\n- **Tests**: (i) LOSO span classifier NEG-vs-adverb: train on 안/않 spans vs. half the adverbs, test on 못/아니 spans vs. *held-out* adverbs (byte-identity features can't transfer under LOSO, so a hit requires shared NEG-like features). (ii) RSA Δ = cos(within-NEG) − cos(NEG↔adverb) vs. permutation null. Controls: label-shuffle + the earn-seal's adv-misfire gate (novel adverbs must not classify as NEG — else the probe is a novelty detector). Stats per probe-defect census: no max(controls), paired bootstrap, MDE precomputed (n=300/stem → bacc SE≈0.03, detectable Δ≈0.08).\n- **Discriminating logic** — the branch is decisive either way:\n  - **CLASS EXISTS** (bacc ≥0.75 + RSA Δ > null, misfire clean) → the byte substrate *already represents* the morpheme abstraction and fails only to **consume** it. Consumption is the read/route side — already EARNED TERMINAL (#3293/#3294). ⟹ the morpheme-tokenizer premise is refuted; the architectural lever **closes at $0** and the wall verdict stands as corpus×CE-measure, now with the representation stratum mapped (exists-at-span, absent-at-readout).\n  - **CLASS ABSENT** (bacc ≈ chance, both controls clean) → premise alive: atomicity plausibly gates class formation → license the pod run.\n\n## 2. (b) 1-pod run — MORPH-ATOM codec (fire only on SPAN-GEOM = ABSENT)\n\n**Codec**: BPE-on-jamo (~4–8k merges) trained on the CPT corpus itself; every learned morpheme → distinct 2-byte code from unused byte space; lossless/reversible; the 4 negation stems each get their own code *because they're frequent, not because they're negation*. Audit gate: assert no two negation stems share a code, and stems get no annotation the top-4k non-negation morphemes don't.\n\n**Corpus**: CPT = ~60–100MB natural ko (4-cell registers, proportional sample), **sentiment-label-free, plain CE**. Drill = NBIND-FC XOR grid remapped through the codec, **drilled stems 안/않/못 only**; the held-out stem (아니; second fold 못) appears in **zero** drill rows. Eval = held-out (predicate × stem) cells: F2 = held-out-stem flip bacc; F1 = drilled-stem × held-out-predicate (sanity).\n\n**Staged pipeline with early-exit gates** (self-diagnosing; ~95min RTX4090, `anima-python[gpu]`, pip-bootstrap hard-gate per pod-fire memory):\n\n1. **CPT** warm-start from base 303M on remapped corpus (~50min).\n2. **Gate G-a (post-CPT geometry, ~$0)**: rerun SPAN-GEOM on the stem *codes* (now atomic — trivial to probe). Class not formed → **STOP, verdict = PENDING(CPT-budget)**, never a FAIL — this is the V-gate that keeps outcome-3 from masquerading as a lever kill.\n3. **Gate G-a2 (pre-drill zero-shot flip, ~$0)**: XOR-eval the held-out stem *before any drill*. If it already flips, CPT taught the flip directly from natural text (a real confound the balanced XOR only partially blocks) → record; the drill-transfer claim then needs the C2 arm to decompose.\n4. **Drill + eval** (~25min): fine-tune on the drilled-stem grid, serialize, `anima-py evaluate` F2/F1.\n\n**Arms** (M + 3 controls; stage C2 only if M lifts — cheap-first):\n- **ARM-M**: codec + CPT + drill (above).\n- **ARM-C1** (compute/data control): identical CPT tokens + drill, **no codec** (raw bytes). Kills \"more Korean data alone closes F2\" and the CPT-direct-supervision confound in one arm.\n- **ARM-C3** (leak ceiling / V1 liveness): shared-`⟨NEG⟩` codec. Must PASS big (expect F2 ≥0.9); if it doesn't, the measurement is dead → INVALID, not FAIL.\n- **ARM-C2** (mechanism ablation, conditional): codec + CPT with held-out-stem sentences removed → held-out code has no geometry → F2 must floor. M-lift + C2-floor = transfer causally rides the *induced* class.\n\n**Thresholds** (Δ vs. each control paired over held-out cells, bootstrap CI excluding 0; no order statistics): **PASS** = F2(M) ≥0.70 **and** F2(M)−F2(C1) ≥0.15 **and** C3 ≥0.90 **and** (if run) C2 ≤0.55. Baseline anchor: current byte F2 = 0.20–0.45. Single seed = DIRECTIONAL; PASS candidate → 1 replication seed before TERMINAL (V5). Honest scope: verdict is scoped to codec-granted atomicity at this CPT budget; **outcome \"class forms (G-a PASS) but F2 still floors\" is itself decision-grade** — it earns the architecture-lever terminal, because deeper morpheme architectures that also only deliver atomicity inherit the null.\n\n## 3. (c) Go/no-go\n\n**GO on SPAN-GEOM now — it's $0, runs on existing base-ckpt activation dumps + one new span-pooled extraction, and every branch pays**: CLASS-EXISTS closes the last named lever for free and completes the wall verdict; CLASS-ABSENT is the license the pod run needs. **Pod run = conditional GO** (≈$0.6–1.0, 1×RTX4090 ~95min per arm, 3 arms parallel per a_wall_first ≈ $2–3 total; rent=spend → owner go per fleet caveat). At ~8% crack prior the pod's expected value is carried not by the crack but by the terminal-quality of its nulls: G-a-PASS+F2-floor upgrades \"corpus×CE measure\" from best-explanation to controls-proven against the architecture alternative, and the G-a/G-a2 gates guarantee the one cheap failure mode (CPT starvation) reports as PENDING rather than a false 🧱. Register as one H (2 surfaces) with SPAN-GEOM as stage-1 and the codec run as stage-2, gated.","stop_reason":"end_turn","session_id":"83638e72-a127-473e-b2ab-6958f11398b2","total_cost_usd":1.6922929999999998,"usage":{"input_tokens":6,"cache_creation_input_tokens":40770,"cache_read_input_tokens":113255,"output_tokens":15233,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":40770,"ephemeral_5m_input_tokens":0},"inference_geo":"not_available","iterations":[{"input_tokens":2,"output_tokens":3626,"cache_read_input_tokens":54939,"cache_creation_input_tokens":638,"cache_creation":{"ephemeral_5m_input_tokens":0,"ephemeral_1h_input_tokens":638},"type":"message"}],"speed":"standard"},"modelUsage":{"claude-haiku-4-5-20251001":{"inputTokens":1818,"outputTokens":22,"cacheReadInputTokens":0,"cacheCreationInputTokens":0,"webSearchRequests":0,"costUSD":0.001928,"contextWindow":200000,"maxOutputTokens":32000},"claude-fable-5":{"inputTokens":6,"outputTokens":15233,"cacheReadInputTokens":113255,"cacheCreationInputTokens":40770,"webSearchRequests":0,"costUSD":1.690365,"contextWindow":1000000,"maxOutputTokens":64000}},"permission_denials":[],"terminal_reason":"completed","fast_mode_state":"off","uuid":"dfe75bbc-47bf-4dc5-9ee6-98463b18029d"}
