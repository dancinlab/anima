// H_640 timing probe — ONE shard: n=6 ring, rule110, cap=1 (cheapest).
// Goal: confirm a single (rule,cap,state) shard finishes < 60s at small cap.
import "/tmp/u-h640/HEXAD/IIT4/lib/iit4_eca.hexa"
import "stdlib/consciousness/iit4_bounded.hexa"
let N = 6
fn main() {
    let tpm = eca_tpm(110, N)
    let r = big_phi_bounded(tpm, N, 21, 1)   // state st=21 (010101), cap=1
    println("PROBE n6 r110 st21 cap1 phi=" + str(r[0]))
    println("PROBE_DONE")
}
