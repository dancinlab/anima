// H_640 timing probe — generic (RULE, CAP) shard at n=6, state st=21.
import "/tmp/u-h640/HEXAD/IIT4/lib/iit4_eca.hexa"
import "stdlib/consciousness/iit4_bounded.hexa"
let N = 6
let RULE = 90
let CAP = 3
fn main() {
    let tpm = eca_tpm(RULE, N)
    let r = big_phi_bounded(tpm, N, 21, CAP)
    println("PROBE n6 r" + str(RULE) + " st21 cap" + str(CAP) + " phi=" + str(r[0]))
    println("PROBE_DONE")
}
