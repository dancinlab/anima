H_9311 — 예산을 풀면 성장은 어디서 멈추는가 (engine-native · 실코퍼스)
================================================================================
PRE-REGISTERED — 발사 전 동결 (frozen-first · c9 · p7 · NO tune-to-green)
freeze_ts_local: 2026-07-14 (KST) · lane: MITOSIS-ENGINE
선행: H_9310 🔵 (실코퍼스 engine-native · FLAT 10셀/2.82046 vs SHRINK 40셀/2.71886)

--------------------------------------------------------------------------------
1. 왜 새 카드인가 (사후 확장 금지)
--------------------------------------------------------------------------------
H_9310 에서 SHRINK 가 **사전등록 예산 상한(grow_max=40)에 도달**했다. 즉 그 카드는
"40 셀까지는 좋아진다"만 말했고, **어디서 멈추는지는 측정하지 않았다**.
같은 카드에서 예산을 올려 다시 읽는 것은 **사후 확장 = tune-to-green** 이므로 금지했다.
⇒ 별도 카드로 **새 bar 를 발사 전에 동결**하고 예산 축을 연다.

이것이 중요한 이유: H_9301 미러가 이미 경고한 것 —
   "성장은 **무해**해졌을 뿐 **생산적**이 되지는 않았다(G3 ✗)."
   미러에서는 셀을 320 까지 늘려도 shrinkage head 가 **평평**했다(열화 0, 그러나 이득도 없음).
⇒ 실코퍼스 engine-native 에서도 같은가? 아니면 계속 좋아지는가? **이것이 미측정이다.**

--------------------------------------------------------------------------------
2. 설정 (H_9310 verbatim · 예산 축만 연다)
--------------------------------------------------------------------------------
REAL KO 30MB 창 sha c47b6808… · Vj=323 · ko_stride 2500 · dim-3 · even/odd held-out ·
nats/UTF-8-byte · live 엔진 faculty (`jamo_head_grow` / `jamo_head_grow_shrink`).
예산 스윕: grow_max ∈ {10, 20, 40, 80, 160, 320} — **전 지점 그대로 보고**(silent truncation 금지).

CALIB (BLOCKING): grow_max=40 지점의 FLAT CE 가 **2.82046** 를 재현 (|Δ| ≤ 0.01)
                  AND SHRINK CE 가 **2.71886** 를 재현 (|Δ| ≤ 0.01)  ← H_9310 앵커 2개
                  실패 → ⛔ INVALID (판정 금지).

--------------------------------------------------------------------------------
3. 동결 bar
--------------------------------------------------------------------------------
B1 GROWTH-PAYS  : CE(SHRINK @ 최대예산) ≤ CE(SHRINK @ 40셀) − 0.02 nats/byte
        ⇒ 예산을 풀면 **계속 좋아진다** = 성장이 **생산적**이다.
        ⇒ 이것이 참이면 H_9301 미러의 G3 ✗("무해할 뿐 생산적이지 않다")가 **실코퍼스에서 뒤집힌다**.

B2 SATURATION  : SHRINK 의 CE 가 어디서 평탄해지는가 (사전등록 진단 · bar 아님)
        평탄화 정의: 인접 두 지점의 |ΔCE| < 0.01 이 처음 발생하는 셀 수.
        전 지점 보고. cherry-pick 금지.

B3 FLAT-CONTROL : 같은 예산에서 FLAT arm 도 함께 보고 (열화 여부).
        미러 예측: FLAT 은 굶주림으로 **열화**해야 한다. 열화 안 하면 → 이 창(stride 2500)에선
        굶주림이 안 걸린 것(정직 보고 · 축 미탑재).

판정:
  🟢 GROWTH-PAYS   : B1 참 ⇒ 성장이 생산적 · H_9301 G3 실코퍼스서 반전
  🧱 SATURATES     : B1 거짓 ∧ SHRINK 가 평탄 ⇒ **성장은 무해할 뿐 생산적이 아니다**(H_9301 G3 확증)
                     — 정직 negative. "더 쪼개도 좋아지지 않는다"가 실코퍼스 engine-native 사실.
  🔴 DEGRADES      : SHRINK 가 예산 증가로 **열화** ⇒ shrinkage 로도 못 막는 무언가가 있다(신규 발견)
  ⛔ INVALID       : CALIB 실패

--------------------------------------------------------------------------------
4. 계측 무결성
--------------------------------------------------------------------------------
- λ = WB 닫힌형(자유 하이퍼 0) · 스윕할 손잡이 없음.
- 두 arm 모두 live 엔진 faculty 호출 · TRAIN only 성장/head · TEST = even/odd held-out.
- 전 예산 지점 보고. bar 발사 후 이동 금지.
- 스윕이 flat 하면 "효과 없음"이 아니라 "축 미탑재"를 먼저 의심(convergence mitosis-estimator-1).
- LABEL: py 2-production engine (a_eval_py_canonical) · stride 2500 창 한정.
