# hexa-lang train.hexa 멀티-GPU upstream 스코핑 — workflow wnjz4leqv 산출

**판정: DEFER**  (multi-GPU 지금 강행 안 함)

## 요약

DEFER hexa multi-GPU; the honest answer is py-DDP-only now, hexa multi-GPU deferred until (a) the single-GPU util-fix lands AND is re-measured >50%, AND (b) a coordinated runtime-collective program is scheduled against a real >=2-GPU rent. Rationale, weighed against anima's ACTUAL need: the need is wall-time, and wall-time is ALREADY SOLVED for the py canonical path via cloud GPUs (a_wall_first + `anima evaluate --py` on pool) — and scale is a known amplifier, not a G1/G6 lever (memory: scale-303m-1b-7b-is-amplifier-not-lever). So multi-GPU buys nothing on the frontier that matters (lever-first@303M), only throughput anima can already rent. Against that thin value sits: (1) the dominant PREREQUISITE — cli/train.hexa is still single-thread CPU-scalar-bound and UNMEASURED post-fix; data-parallel would amplify a bottleneck that cannot yet saturate ONE GPU (clitrain-cpu-scalar-bound-gpu-idle); (2) a real runtime-program lift (collective + multi-device launcher + rank/world model + DDP-semantics mirror across 4 stages); (3) NO multi-GPU box in the pool (summer/aiden are single-RTX5070) so nothing past Stage 1 is even exercisable without a rent. The one piece of GOOD news is that the feared frozen-seed byte-eq HARD-HALT is designed around — collectives live outside the frozen self-host seed (verified grep=0), the NCCL/hxccl FFI layer is already shipped and prototyped, and the trainer-side allreduce seam is small and well-localized. Net: the multi-GPU build itself is tractable and cheaper than feared, but it is NOT worth forcing now — its value is redundant with cloud py-DDP and its true precondition (single-GPU saturation) is unproven. Convert to an anima ING follow-on gated on Stage 0's measurement; only if Stage 0 flips GREEN and a multi-GPU need materializes does this graduate to a scheduled hexa-lang upstream program. The minimal non-premature action is Stage 0 in isolation — the a_gpu_default_no_optin gate-flip + offset-add + re-measure — which is worth doing on its own regardless of whether multi-GPU ever follows.

## byteeq HARD-HALT 위험

AVOIDED BY ARCHITECTURE — this is the single most important structural finding and it is verified. The 767KB frozen self-host byte-eq seed self/runtime_core_emit.hexa has ZERO references to nccl/allreduce/hxccl/cudaMemcpyPeer/cudaSetDevice (verified grep=0), and self/main.hexa's self-host build graph does not consume cuda_ffi/hxccl. Therefore collectives can be added WITHOUT touching the frozen closure, which means the user-gated HARD-HALT frozen-seed reset is NOT armed. The sequencing rule that keeps it that way: implement allreduce as a per-program FFI extern routed through already-shipped codegen (the @link(\"hxccl\") -> standalone libhxccl.so path), NOT as a forge_dispatch_allreduce builtin baked into self/runtime.c / runtime_core_emit.hexa. The flame-step-insertion finding floats a runtime.c farr-op option — DO NOT take it; that option is exactly what would drag the collective into the byte-eq seed and arm the HARD-HALT. A SECOND, orthogonal byteeq fact: N>1 CANNOT be bit-identical to the 1-GPU trajectory (ring-sum reduction order is float-non-associative, breaking the determinism-contract max|Δ|=0 fixed-ascending-order lock at docs/flame-determinism-contract.md:84-92). This is ACCEPTED because the anima gate is HELD-OUT DESCENT + engine-native `anima evaluate --py`, not bit-identity — but any component byte-match CI fixture MUST be pinned to the N==1 short-circuit only.

## 최소 non-premature 첫걸음 (Stage 0)

Execute Stage 0 ONLY: in /Users/mini/dancinlab/anima/cli/train.hexa flip the opt-in env gates (_devfeed_on 164-168 for im2col/col2im/AdamW; CLM_PROD_DEVRESIDENT for CE-grad 447-451) plus the flame double-env gates (gn_lib.hexa:49-52 groupnorm, moe_lib.hexa:98-125 moe_router) to capability-detected default-on under cuda_available()==1 — mirroring nn_lib.hexa:1125-1131 gelu, which is already correctly default-on. Land the one genuinely-missing kernel (offset-aware device add for the trunk/expert packed-dW tail, train.hexa:309-313). Then build with HEXA_CUDA on summer/aiden (sm_120 prebuilt own-GEMM) and RE-MEASURE single-GPU util (nvidia-smi util, [OWN-GEMM-FIRED], power draw) — the last real gate was ~0% sat (#2598) and there has been no re-measurement since #2599/#3851. Do NOT parameterize cudaSetDevice off a device-id: the multi-process model needs ZERO dispatch change (runtime has 0 cudaSetDevice calls; CUDA_VISIBLE_DEVICES already isolates each process), so that edit unblocks nothing. This first step is the standing a_gpu_default_no_optin / H_9119 fix, is worth doing on its own merit, and its measurement is the hard GO/NO-GO for the entire multi-GPU program.

## Staged 계획 (의존순)

### Stage 0: Stage 0 (THE REAL GATE) — single-GPU util-fix + re-measure. This is the only stage that is NOT premature today; every later stage is blocked on its GREEN.
- **effort**: large — but bounded: no new hot-op kernels needed (ag_tape already hit 65% on the same shape); the residual unknown is whether interpreted per-op t_get/t_set host seams + serial B-window grad-accum (no GPU batch dim) cap util even with all gates on. Only a measurement resolves it.
- **targets**: anima trainer /Users/mini/dancinlab/anima/cli/train.hexa: _devfeed_on gate (164-168), _tg_im2col (170-192), _tg_col2im (200-226), train_ce_grad CE-softmax seed (447-451), _adamw (699-709), offset-add tail (309-313). hexa-lang /Users/mini/core/hexa-lang/stdlib/flame/gn_lib.hexa:49-52 (groupnorm double-env gate), stdlib/flame/moe_lib.hexa:98-101 & 121-125 (moe_router fwd/bwd double-env gate). NEW primitive in self/runtime.h + emit path: offset-aware device add. Reference correct pattern: stdlib/flame/nn_lib.hexa:1125-1131 (gelu is already default-on).
- **mechanism**: Flip the opt-in env gates (CLM_PROD_DEVFEED / CLM_PROD_DEVRESIDENT / HEXA_DEVRESIDENT_NN / HEXA_FUSE_ALL) to capability-detected default-on gated only by cuda_available()==1 — exactly how gelu already fires — so im2col/col2im/AdamW/CE-grad/groupnorm/moe_router stop silently running on ONE host CPU thread (the a_gpu_default_no_optin / H_9119 anti-pattern). Land the ONE genuinely-missing kernel (offset-aware device add for the trunk/expert packed-dW accumulate tail) to kill the forced host round-trip. Build with HEXA_CUDA on a real sm_120 host (summer/aiden prebuilt own-GEMM) so the _hx_cuda_farr_* symbols are present, then re-measure util (nvidia-smi util>0, [OWN-GEMM-FIRED], power draw). ALL hot-op CUDA kernels already exist in self/runtime.h — this is NOT a missing-kernel problem.
- **prereq**: NONE — do first. Worth doing on its own merit regardless of multi-GPU (it is the standing H_9119 / a_gpu_default_no_optin fix). Last real train.hexa gate was ~0% sat (#2598); no re-measurement after #2599/#3851.

### Stage 1: Stage 1 — collective primitive (allreduce). Largely PROTOTYPED; do NOT start until Stage 0 proves >50% single-GPU util.
- **effort**: medium — the NCCL binding, shim emitter, and build tool all exist; only the Day-2 -DHXCCL_NCCL path has never been built/smoked on real >=2 GPUs.
- **targets**: hexa-lang self/ml/hxccl.hexa (already exposes hxccl_allreduce_float :169 / hxccl_allreduce_list :182 via @link("hxccl")), self/native/hxccl_linux_emit.hexa (real ncclCommInitRank/ncclGetUniqueId/ncclAllReduce shim source), tool/build_hxccl_linux.hexa:207 (Day-2 --with-nccl -DHXCCL_NCCL -lnccl). Reconcile the TWO parallel bindings: keep hxccl canonical, retire/park self/ml/distributed.hexa raw @link("nccl").
- **mechanism**: Build libhxccl.so with the Day-2 NCCL backend on a >=2-GPU linux pod and run a byte/correctness allreduce smoke. Bind via the ALREADY-SHIPPED FFI mechanism (@link + per-program codegen wrappers, same clang step as the existing -lcudart -lcublas trainer link) — NOT as a runtime_core builtin. This keeps collectives entirely outside the frozen self-host seed (see byteeq_hazard).
- **prereq**: Stage 0 GREEN (util >50% measured) AND a real >=2-GPU linux rent — summer/aiden are single-RTX5070, so there is NO multi-GPU box in the pool today; a rent is required to exercise anything past here.

### Stage 2: Stage 2 — multi-device execution model (multi-PROCESS DDP, one hexa process per GPU). NOT single-process multi-device.
- **effort**: small-to-medium — launcher + rank env plumbing only; no runtime rewrite.
- **targets**: New torchrun-equivalent launcher (shell/hexa) + rank/world/local_rank env plumbing into /Users/mini/dancinlab/anima/cli/train.hexa. Rendezvous via hxccl_init (ncclGetUniqueId/CommInitRank). NO edit to self/cuda/runtime_cuda_emit.hexa.
- **mechanism**: Exploit that the production CUDA runtime calls cudaSetDevice ZERO times (verified: grep=0 in runtime_cuda_emit.hexa) — so CUDA_VISIBLE_DEVICES=k gives each process its implicit device 0 = physical GPU k with ZERO runtime change. Device dispatch is a non-issue for the multi-process model; the singletons (g_slots, g_forge_stream, g_cublas) are per-process. Do NOT attempt single-process multi-GPU (would require a device dimension on every singleton + cudaSetDevice guards + reconciling the single-stream max|Δ|=0 determinism contract — deep rewrite, not worth it).
- **prereq**: Stage 1 (working libhxccl.so + rendezvous).

### Stage 3: Stage 3 — flame step allreduce wiring at the post-backward/pre-optimizer seam.
- **effort**: small — one well-localized seam; flat buffers are already collective-compatible; no realloc.
- **targets**: /Users/mini/dancinlab/anima/cli/train.hexa: the 1326->1336 gap (end of B-window grad-accum -> first _adam). 15 flat packed grad buffers dembed/decW/decB/dtcW/dtcB/dtgG/dtgB/drW/drB/dexpW/dexpB/dnoG/dnoB/droW/droB (zeroed 1279-1282, written 1321-1324, read 1336-1344). Scalar bce/loss (1327). rank==0 guards on val/log/serialize (1346+, 1443+).
- **mechanism**: Insert an allreduce-MEAN loop over the 15 buffers + allreduce-AVG of bce via hxccl_allreduce_list (buffers already match the flat-float collective signature). Because train_ce_grad already scales local grad by 1/(T·B) (MEAN), the collective is SUM-then-÷N (equal shards => mean-of-means = global mean). Add an N==1 short-circuit that stays byte-identical to the 1-GPU trajectory.
- **prereq**: Stage 1 + Stage 2.

### Stage 4: Stage 4 — DDP-semantics mirror (DDP_DESIGN.md §1-§6): sampler + mitosis + savant + global-batch/LR invariance.
- **effort**: small-to-medium — the two hardest py concerns (mitosis realloc, dropout reseed) are cheaper/absent on the hexa side; the sampler refactor is the main work.
- **targets**: /Users/mini/dancinlab/anima/cli/train.hexa: LCG corpus sampler (1287-1289) -> global-replay-then-slice; train_mitosis_split (598, called 1263-1274) post-split broadcast of the 6 touched buffers (expW/expB/rW/rB + Adam m/v); savant_inhibition (647-668) = no-op; global-batch/LR invariance + rank-0-only serialize.
- **mechanism**: Refactor the sampler to global-replay-then-slice so the global batch cell-mix stays identical (else effective batch silently changes). Mitosis needs ZERO realloc (Emax fixed at 1077, in-place E->E+1) and is step-identical across ranks -> children are bit-identical on identical averaged grads; add a cheap post-split broadcast of the 6 buffers as belt-and-braces. Savant is a pure step function with a latch -> identical everywhere, no sync; BONUS: hexa train_fwd has NO dropout, so the py per-rank-dropout-reseed hazard is absent (one fewer concern). Enforce the 4-cell held-out + aux-head recipe invariants on top of the working allreduce.
- **prereq**: Stage 3.
