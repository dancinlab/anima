H_9316 — G1 reopen 스크리너: 역접/양보는 자연 코퍼스에 전이되는 비가법 정보를 담는가
================================================================================
PRE-REGISTERED — 발사 전 동결 (frozen-first · c9 · p7 · NO tune-to-green)
freeze_ts_local: 2026-07-14 (KST) · lane: g1-crack-natural-emergence (프런티어)
계기 설계 = H_9304 (인증 완료) · 스크리닝 설계 = Fable 5 · 실행 = 메인 세션
선행: H_9304 🧱 DATA-ADDITIVE (NSMC 부정-플립 · EARNED +0.00233 · TOST 0 과 등가)

--------------------------------------------------------------------------------
1. 왜 (H_9304 가 명시한 유일한 reopen 조건)
--------------------------------------------------------------------------------
H_9304 카드 verbatim: "reopen = held-out 로 전이되는 비가법 정보를 **실제로 담은** 자연
corpus × task class 의 발견. **이 측정이 그 탐색의 $0 사전 스크리너**($0 · 95초/건)."

⇒ 이 카드는 그 스크리너의 **첫 발사**다.

**Fable 의 구조적 논증(이 카드가 검정하는 프레임)**:
   NSMC 가 +0.002 를 낸 이유는 XOR 이 없어서가 아니다 — δ̂ = −0.35 로 **방향은 맞았다**.
   이유는 **주효과(α_A + γ_B)가 라벨 분산을 거의 다 먹어서 잔차 상호작용이 티끌**이라는 것이다.
   그리고 그 구조적 원인: **언어는 이해되기 위해 쓰인다** — 화자는 청자가 XOR 를 풀지 않아도 되게
   **가법 단서를 선불로 깐다**(redundancy/comprehensibility 압력). 즉 marginal 무력화
   (interaction-dominant) 구조는 **소통 언어가 구조적으로 기피**한다.
   ⇒ NSMC 의 +0.002 는 코퍼스 버그가 아니라 **이 압력의 서명**일 수 있다.

이 카드는 그 프레임을 **다른 사영**에서 친다.

--------------------------------------------------------------------------------
2. 왜 역접/양보인가 (중복 발사 아님)
--------------------------------------------------------------------------------
γ census 는 **"진짜 비가법 = XOR-class 뿐(부정/역접)"** 이라고 명시했다. 그런데 이 레인에서
**부정만 측정됐고 역접은 한 번도 직접 측정된 적이 없다**. 다른 사영이다.

기전: "좋지만 비싸다" — 양보 표지 뒤의 절이 문서 라벨을 지배한다 ⇒ 앞절 극성 × 양보의 상호작용.
   δ < 0 이면 양보가 어간의 극성 기여를 **뒤집거나 약화**시킨다는 뜻.

데이터: NSMC 150,000 행 중 **21,401 행(14.3%)** 이 역접/양보 표지 포함 ⇒ held-out 셀 충분
   (F2 의 n=0 함정 회피).

--------------------------------------------------------------------------------
3. 조작화 (H_9304 템플릿 verbatim · 렉시콘 의존 0)
--------------------------------------------------------------------------------
  A = 어간 (숨은 극성 = 학습되는 계수 α_A · 비-양보 출현에서 학습)
  B = **양보/역접 비트** ∈ {지만, 는데, ㄴ데, 하지만, 그러나, 그런데} — **폐집합 · 사전등록**
  T = NSMC 인간 별점 라벨 (0/1) — **토큰 스트림 외부** (동어반복 함정 구조적 회피)
  held-out 셀 = (A, B=1) 중 그 A 가 TRAIN 엔 **비양보로만** 등장한 것
  연산자 = logit P(T=1|A,B) = α_A + γ_B + δ·(B·α_A) 의 **전역 δ 1개**
           (δ=0 가법 · δ=−2 완전 flip). δ 는 **다른 어간들의 B=1 셀에서 학습**(전이되는 연산자).

코퍼스: NSMC train 149,995 행 · sha256 e03b7d14e9e41be8d464a28057cd25d7396c53e67aa7fd5f7e552c59b0ee2940

--------------------------------------------------------------------------------
4. 계기 인증 게이트 (BLOCKING · 주 bar 판독 前 계산조차 안 함)
--------------------------------------------------------------------------------
H_9304 의 3 게이트를 **그대로** 상속한다 (이 lane 에서 계기가 죽은 전례 2건: H_9265 · H_9303 —
"죽은 프로브의 침묵은 아무것도 증명하지 않는다"):

  G-ALIVE    (합성 XOR 양성대조) : EARNED ≥ **+0.30 nats** · 미달 → 계기 실명 → 판독 금지
  G-PEDESTAL (합성 가법 · 참값 0) : |EARNED| ≤ **0.02** · 초과 → 계기 편향 → 판독 금지
  G-POWER    (held-out 인구조사) : MDE(3σ) ≤ 0.02 · 미달 → ⏳ INVALID (DATA-SPARSE · KILL 아님)

--------------------------------------------------------------------------------
5. 동결 bar (Bonferroni · 다중후보 p-hacking 차단)
--------------------------------------------------------------------------------
⚠️ **이 스크리너는 여러 후보를 훑는다**(P1 반어 · P2 역접 · P3 패리티 …). 하나가 유의하면
   p-hacking 이므로 **사전등록 Bonferroni**: 계획 후보 수 **N = 3** ⇒ 유효 α = 0.05/3 ≈ **0.0167**
   ⇒ CI 를 **95% → 98.3%** 로 조인다. Δ_eq = **±0.02 nats** (H_9304 와 동일 · 데이터 前 고정).
   후보를 데이터 보고 고르지 않는다 (peek-then-pick 금지).

  🟢 INFO-PRESENT   : held-out EARNED 의 98.3% CI 하한 > +0.02
                      ⇒ **역접이 전이되는 비가법 정보를 담는다** ⇒ G1 reopen · 재학습 정당화
                        (⚠️ H_9306 상속: 정보 존재는 spend 의 **필요조건이지 충분조건 아님** —
                         되산 양 < 벽 높이면 벽은 그대로 선다)

  🧱 DATA-ADDITIVE  : TOST PASS (CI ⊂ ±0.02) ∧ seen 시너지도 ≈ 0
                      ⇒ 역접도 NSMC 부정과 같다 ⇒ **Fable 의 구조적 논증이 한 사영 더 확증**
                        (소통 언어의 가법 압력)

  🧱 OPERATOR-ABSENT: TOST PASS ∧ seen 시너지 ≫ +0.02
                      ⇒ 비가법성은 있으나 전이 안 됨 (collocation-only)

  ⏳ NOT-POWERED    : CI 가 0 과 +0.02 를 모두 관통 ⇒ N 부족
  ⛔ INVALID        : 인증 게이트 실패

--------------------------------------------------------------------------------
6. 효과크기 보고 의무 (검정력 인플레 차단)
--------------------------------------------------------------------------------
n ~ 수만이면 **티끌도 p<.05** 가 된다(power-before-negative-verdict).
⇒ **p 값을 판정 근거로 쓰지 않는다.** 판정은 TOST(Δ_eq) + **XBIND 자(尺) 대비 효과크기 비율**
   (earned / 5.29653)로 보고한다. H_9304 의 NSMC 부정은 0.00233/5.29653 = **0.00044 (0.044%)** 였다.

--------------------------------------------------------------------------------
7. 계측 무결성 (H_9304 상속 + Fable 함정 목록)
--------------------------------------------------------------------------------
- **T 외부성**: T = 인간 별점 (토큰 스트림 밖). 양보 표지는 토큰 안에 있으나 그것이 **B**(설명변수)이지
  T 가 아니다 — H_9304 와 동일 구조.
- **held-out 순수성**: A 가 TRAIN 엔 **비양보로만** 등장하도록 강제(누수 = 동어반복).
- **통제**: B 를 **T-층화 셔플**(같은 T 안에서만 순열) ⇒ P(B,T) 정확 보존, A–B 짝만 파괴
  (평범 순열은 B→T 채널까지 파괴해 참값 0 아암이 −0.6 을 읽는다 — H_9304 에서 실측된 결함).
- **추정량 중심화**: δ 는 **데이터에서 학습**(하드코딩 금지 — 방향 가설을 측정량에 심으면 참값 0 아암이
  −0.7 을 읽는다 · H_9304 에서 실측된 결함).
- **레지스터 정합**: A 와 held-out 셀이 같은 코퍼스/레지스터(NSMC 단일 도메인 ⇒ 자동 충족).
- bar 발사 후 이동 금지. 전 지점 보고.

--------------------------------------------------------------------------------
8. 후속 후보 (같은 Bonferroni 가족 · 미발사)
--------------------------------------------------------------------------------
P1 SARCASM (반어) — **Fable 이 지목한 극단 leverage**: 소통 언어 중 marginal 이 **능동적으로 오도**
   하는 유일한 자연 구조(긍정 단어 → 부정 의미). 반어에서조차 +ε 이면 **"소통적 자연 언어 =
   DATA-ADDITIVE" 를 부류 수준에서 종결**할 수 있다.
   ⛔ **데이터 블로커(실측 확인)**: **감성 라벨 ∧ 반어 라벨을 함께** 가진 코퍼스가 필요하다
   (하나만 있으면 B 와 T 가 같아져 퇴화). 확인한 공개 반어 코퍼스(ETS twitter shared task)는
   `label`(SARCASM/NOT) **하나뿐** ⇒ 사용 불가. 필요 사양 = 트윗/리뷰 + **감성 점수** + **반어 플래그**
   (예: SemEval-2015 Task 11 계열). 확보되면 이 FREEZE 의 Bonferroni 가족으로 발사.

P3 PARITY (이중부정) — 이론상 가장 순수한 XOR 이나 자연 텍스트에 2+ 부정문이 극희소
   ⇒ held-out n≈0 위험(F2 함정 직격). 발사 시 **G-POWER 가 먼저 죽으면 INVALID 로 정직 종결**.
