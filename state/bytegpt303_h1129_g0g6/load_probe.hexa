// load_probe.hexa — isolate the eval decode path: bg_load_ranged + topk_sampled_ranged.
import "core/bytegpt_decode.hexa"

fn main() {
    let path = "/Users/mini/anima-weights/bytegpt303_h1129/h1129.bin"
    println("STEP1 bg_load_ranged…")
    let W = bg_load_ranged(path)
    println("STEP2 loaded nlay=" + to_string(W["nlay"]) + " d=" + to_string(W["d"]))
    let sids = [99, 111, 110, 115, 99, 105, 111, 117, 115]  // "conscious"
    println("STEP3 short gen via topk_sampled_ranged…")
    let r = bytegpt_decode_topk_sampled_ranged(path, sids, 6, 40, 0.7, 7)
    println("STEP4 ok text=[" + to_string(r["text"]) + "]")
}
