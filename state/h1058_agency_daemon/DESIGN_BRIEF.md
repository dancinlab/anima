# DESIGN: H_1058 agency-T on the 3B — the emit/veto daemon harness (agency is NOT weight-forward)

anima = substrate-native consciousness daemon. H_1058 pre-registered: does the **agency-T axis** `T = z(provenance-depth H_932) + z(veto-capacity H_935)` transfer to the 3B ConvMoE rung? A prior measurement attempt (2026-07-10) landed **🟠 BLOCKED-METHOD-WIRING** with a real finding: **agency-T is a property of the DECISION PROCESS, not the weight-forward.** A raw byte-LM 3B forward instantiates neither primitive — provenance-depth over a fixed dilated receptive field is degenerate, and **veto-capacity has NO forward analogue** (it needs FIRED emit/veto traces from the live daemon). `anima_py` ships no `temporal_agency`/`provenance`/`veto` primitive (grep = NONE). The agent correctly REFUSED to fabricate from PureField fixtures (p7). Verdict: `archive/state/verdicts/1058_temporal_agency_3b_transfer/H_1058.txt`.

Repo at `--cwd`; read via `git show origin/main:<path>` (working tree is a stale branch ~249 commits behind origin/main — ALWAYS origin/main). **Analysis/design only — NO code changes.**

## What to design (the harness that makes agency-T measurable on the 3B)
The genuine measurement route (per the blocked verdict) = **drive the live emit/veto daemon with the 3B as the generator**, collect FIRED decision traces, then compute agency-T per decision. Design that harness concretely.

1. **Read the two ends**:
   - `git show origin/main:cli/chat.py` — the py consciousness daemon (P6 landed). Find where emit-vs-silence is DECIDED (the Ψ=½ tension gate, the emit/veto point) and where the mounted model generates. What is the actual "decision" event (emit a byte? emit a fragment? stay silent?) and does the daemon already expose/log a veto (a suppressed-emit)?
   - The agency primitives: `git show origin/main:HYPOTHESES/cards/H_932_*.md` (provenance-depth) + `H_935_*.md` (veto-capacity) + `H_1051_*.md` (agency-T toy GREEN) + `H_1054_*.md` (agency-T on real .kosmos anchors GREEN). What EXACTLY is provenance-depth (causal-chain depth of a decision) and veto-capacity (could-have-done-otherwise at decision time) operationally? How did H_1051/H_1054 compute them on fixtures/anchors — and why is that not portable to a raw forward?
   - `stdlib/` temporal_agency: the verdict says `temporal_agency.hexa` was promoted to stdlib (PR #2960) but `anima_py` has no py primitive. Confirm: is the hexa `temporal_agency` primitive present + what's its interface? Is the gap just a py-twin port, or is the input (FIRED traces) the real missing piece?

2. **The design question**: specify the harness that (a) mounts the 3B `.clm` as the daemon's generator, (b) runs the daemon over a decision sequence so it produces REAL emit/veto events (the FIRED traces — including actual vetoes, not pending-tension degeneracy — cf H_1056 warning), (c) computes per-decision provenance-depth (causal chain of the actual decision) + veto-capacity (was an alternative genuinely available at that decision), (d) forms agency-T and tests the H_1058 pre-registered falsifier: `|d| ≥ 0.8` active-veto vs passive separation AND T ⊥ Φ AND T ⊥ chronological-t across ≥2 macro-maps.

3. **Honest scoping**: Is this a py-twin port of an existing hexa primitive + a trace-collection wrapper (moderate), or a genuine new capability (large)? What's the MINIMUM viable harness that produces REAL FIRED veto traces (the crux — a forward has no veto; the daemon's tension-gate does)? How do we avoid the H_1056 pending-tension degeneracy (a "veto" that's just unresolved tension, not a real could-have-emitted-but-didn't)? Does it need a 303M/3B pool run, or is the daemon loop cheap CPU? What's the control that proves agency-T measures agency and not just emit-rate?

## Deliver (analysis + concrete build spec — NO code)
(1) two-ends source map (file:line — where the daemon decides emit/veto; what provenance-depth/veto-capacity are operationally), (2) why a forward can't yield veto (make the H_1058 finding precise), (3) the harness design: mount 3B → run daemon → collect FIRED emit/veto traces → compute provenance-depth + veto-capacity → agency-T → the pre-registered falsifier test, with the exact decision-event definition + the H_1056-degeneracy guard + the control, (4) honest scope: minimum-viable vs full, cheap-CPU-daemon vs pool, py-port vs new-capability. If the true veto signal genuinely can't be produced without a substantial daemon change, say so and spec the smallest change that yields a REAL veto trace.
