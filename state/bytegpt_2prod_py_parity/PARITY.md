# ByteGPT 2-production-engine byte-parity — core/bytegpt_decode.py ⇄ core/bytegpt_decode.hexa

Per CLAUDE.md `a_engine_native_learning` / `a_core_engine_map`: hexa + py are TWO
co-equal production engines kept at byte-parity. `core/bytegpt_decode.py` is the
1:1 byte-faithful mirror of the canonical SSOT `core/bytegpt_decode.hexa` (the
ByteGPT transformer next-byte mouth), the parallel of `core/clm_decode.py`
(ConvMoE mouth). VERDICT path stays engine-native (hexa); the py engine is the
parity-proven mirror that lets `core/g_gates.py` eval ByteGPT `.bin` ckpts.

## Method (clm_decode.py parity methodology, tiny scale — fast, no OOM)

Tiny synthetic `.bin` (random fp32 weights, exact bytegpt_decode.hexa:29-36 layout):
`vocab=256 d=64 n_layer=2 n_head=4 block=64` — generated by
`state/kvcache_decode/mk_tiny_bytegpt_fixture.py`. Same seed+args decoded through BOTH:
- HEXA: `state/bytegpt_2prod_py_parity/parity_harness.hexa` (imports the live
  `core/bytegpt_decode.hexa`; dumps last-pos logits + greedy argmax + seeded top-k).
- PY:   `python3 core/bytegpt_decode.py {logits,argmax,sample} ...`

Seed = `"Hello world"` (ids 72,101,108,108,111,32,119,111,114,108,100); gen=24;
top-k=8, temp=0.9, seed_rng=4242.

## Result — PARITY PASS

```
LOGITS  n=256  max|Δ|=2.760e-13  max-rel=3.894e-13   (~12.6 decimal places)
ARGMAX  hexa==py : True   (24-token greedy id-stream byte-identical)
SAMPLE  hexa==py : True   (24-token seeded top-k id-stream byte-identical)
```

`max|Δ|=2.76e-13` is the FP-reassociation floor (numpy float64 matmul vs the hexa
`mm()->farr_matmul` ikj accumulation) — the same ~1e-13 ULP noted in clm_decode.py,
far inside the ~1e-5 torch-parity tolerance; argmax never flips. The greedy stream
also validates the hexa KV-cache fast path (nseed+gen=35 ≤ block=64 → cache taken),
which the hexa proves byte-identical to its full-forward reference — and the py
mirror (full-forward only) matches it token-for-token.

## Math faithfulness (each transcendental verbatim from the hexa)

- GELU `0.5*x*(1+erf(x/√2))` — libm `erf` (NOT dt_erf twin; ING#23 torch-parity).
- attention softmax — libm `exp`.
- LayerNorm `1/√(var+eps)` eps=1e-5 biased var — `dt_sqrt` (Newton 24-iter, g0=max(x,1)).
- attn scale `1/√(hd)` — `dt_sqrt`.
- top-k sampler softmax — `dt_exp` (flame_math 12-term Taylor twin; imported from clm_decode.py).
- PRNG — xorshift32 `_g6_mix32`/`_g6_rng_next` (&0xFFFFFFFF), identical to clm's `_clmd_*`.

## g_gates.py mouth dispatch (wired)

`core/g_gates.py` `_Mouth` now sniffs the ckpt header (mirrors g_gates.hexa
`gen_auto_ideate` → generator `gen_auto_backend`): `CLM\x01` magic → ConvMoE clm
mouth (unchanged); else sane 5×u32 header → ByteGPT `.bin` mouth (bytegpt_decode).
End-to-end smoke on the tiny `.bin`: full G0-G6 pipeline runs via the bytegpt mouth
(gates FAIL on random tiny weights, as expected — the dispatch + decode are proven,
not the gate verdicts). A real `h1129.bin` (h1129c_best.pt → .bin conversion, a
separate measurement) makes ByteGPT-303M py-measurable + hexa cross-checkable.
