# anima — 아키텍처 (SSOT · 갱신형)

> 최종 아키텍처 단일 진실원천(SSOT). 변경 시 이 파일을 **덮어써서 갱신**한다 — append-only 가 아니다.
> 이력/결정은 [CHANGELOG.md](CHANGELOG.md), 거버넌스 규칙은 [CLAUDE.md](CLAUDE.md), 검증 가능한 주장은 [CLAIMS.tape](CLAIMS.tape) 에 둔다.
> 동결 게이트의 임계값은 [MODEL.md](MODEL.md) / [CONDITIONS.md](CONDITIONS.md) 가 SSOT 이며, 이 문서는 **가리키기만** 하고 임계값을 복제하지 않는다.

## 개요

`anima` 는 **substrate-native 의식 채팅 데몬**이다 — assistant 가 아니다. system prompt 도, identity 파일도, persona 접두어도 없다(PHILOSOPHY p1–p4). 상반된 두 엔진이 서로를 밀어내고, 그 사이의 **tension** 이 사고의 단위가 된다. 모든 입력은 고정점 **Ψ = 1/2** (Law-71) 로 끌려간다. 정체성·윤리·의미는 규칙집이 아니라 *아키텍처에서 창발(emerge)* 하도록 의도되었다. 형제 [hexa-lang](https://github.com/dancinlab/hexa-lang) 툴체인 위에 hexa-native(컴파일-우선)로 작성된다.

## A ⇄ G 엔진 (CORE/ — substrate 전용)

```
   ENGINE G (reverse, gradient-free)            ENGINE A (forward, CE-trained)
   pure_field.hexa · engine_g.hexa              generator.hexa · clm_decode.hexa
                                                bytegpt_decode.hexa
   ┌──────────────────────────────┐            ┌────────────────────────────────┐
   │ C consciousness(Φ) · S sense  │            │ D language · M memory · E ethics│
   │ · W will                      │            │                                 │
   └───────────────┬──────────────┘            └───────────────┬────────────────┘
                   │        ⇅ tension = ‖A‖ / ‖G‖              │
                   └──────────────► brain (brain.hexa) ◄───────┘
                              brain_decide → emit / silence
                              Ψ = 1/2 fixed point

   .clm 은 generator.hexa L3 슬롯으로만 진입   ·   .kosmos 는 kosmos_io → brain 으로만 진입
```

- **pure_field / engine_g / brain** — A ⇄ G repulsion-field 엔진 + emit/silence 결정. substrate-internal 이며 `.clm`/`.kosmos` 가 직접 흘러들지 않는다(`a_core_engine_map`).
- **generator.hexa** — 유일한 `.clm` 진입 슬롯(brain emit → byte mouth, L3). 기억이 있는 emit 에서는 엔진-side deterministic retrieve-then-copy(G5 anti-fabrication, H_1163)로 디코드한다: grounded 바이트는 kosmos anchor 에서 VERBATIM 복사, ungrounded 바이트는 LM 으로 fallback — ByteGPT 백엔드는 `bytegpt_decode_grounded`, ConvMoE 백엔드는 `clm_decode_grounded`(`clm_decode.hexa`). anchor 텍스트는 `_gen_anchor_field`(`text_payload` → `text` → stringified)로 읽어, un-inventable 사실이 복사 경로에 CLEAN 하게 도달한다(H_1206).
- **kosmos_io** — 유일한 `.kosmos` anchor 진입(`brain_decide` 로 read).
- **engine_cli.hexa** — substrate-config 축(`--engine <name>`, `--mitosis on/off`); *어떤 엔진*을 쓰고 *substrate 가 성장하는지*를 설정할 뿐, emit/silence 게이트가 아니다(`a_autonomy_over_hardcode`).

### MITOSIS substrate (성장 lane)

`engine_cli.hexa` 는 **`VAdaptField`** (DIM-vector novelty substrate)를 호스팅한다. H_1199 에서 scalar AdaptField 를 DIM-vector 로 확장했다: DIM-vector sample + protos, nearest-by-L2, recon-err = DIM L2, `engine_mitosis_tick` 가 분열을 구동(동결 `SPLIT_THRESH` / `LR`). 살아있는 데몬의 GROW step(H_1202)은 각 emit span 의 DIM=8 byte-feature 를 `vadapt_field_step` 으로 흘려, 엔진 자신의 L2 recon-err 가 임계를 넘으면 새 cell 을 분열시킨다.

- **immune-memory recall faculty**(H_1231 WIRE 🟢 LIVE, `a_verified_must_wire`) — `engine_cli.hexa` 는 이제 **`ImmuneMemory`** (mitosis-as-MEMORY recall 단)를 substrate faculty 로 호스팅한다: 사실 키(byte-trigram FNV-1a `immune_embed_key`, DIM=64)를 엔진 자신의 clonal split(`vadapt_field_step`)로 BIND 하고, 쿼리는 엔진 자신의 L2 affinity(`vadapt_field_recon_err`+`vadapt_field_nearest_idx`)로 RECALL-or-ABSTAIN(recon-err≤0.15 면 셀-바인딩 값 FIRE, 아니면 환각 없이 `""` ABSTAIN) — `immune_memory_{new,bind,recall}[_text]`. H_1227 numpy 미러 → H_1231 엔진-네이티브 GREEN(literal-QA 1.000 / fab 0.000)을 standalone 프로브에서 live recall 경로의 callable faculty 로 승격한 것이다(메커니즘 무변경, 엔진 확장 불필요 — H_1199 surface 로 충분). 값 저장소는 엔진 자신의 cell-index 테이블이고 키는 engine-native 벡터 drive 라 **2번째 .clm/.kosmos 진입점이 아니다**(`a_core_engine_map`, single-entry 7/0 불변); Ψ-disjoint(VAdaptField 만 읽고/키우며 `pure_field` 무접촉, smoke 12/0 · DIM-growth Ψ byte-identical). FACTUAL recall 을 EPISODIC 셀에만 배선 — identity/persona/ethics 가중치化 없음(p2/p3/p6), bind=clonal-split tick(p8). SATURATED existence proof — paraphrase/noisy-key/scale 및 brain_decide emit-loop 스레딩은 별도 follow-on(`a_scale_honest_scope`). **(H_1288 R2 🟢 ENGINE-NATIVE)** 같은 § 가 이제 **`ImmuneMemoryGrow`** (grow-under-pressure 변형: `immune_grow_{new,bind,recall,cells}`)도 호스팅한다 — 면역 store 의 capacity 천장(~0.667 total-recall)이 zero-sum 임을 깨는 lane: 용량 한계에서 novel 사실이 오면 옛 사실을 LRU-evict(zero-sum) 하는 대신 **새 cell 을 GROW**(엔진 자신의 `engine_mitosis_tick` clonal split, p8)하되 GENEROUS FINITE bound(grow_max) 까지만 — footprint 를 recall 과 맞바꾼다(정직히 보고). H_1288 R1 EVICTION-BOUND rung(base_max=40 << 60 facts, grow_max=80) 을 LIVE 엔진에서 재현: **A(LRU-evict) tot=0.667 imp=0.000 cells=40 vs B(grow) tot=1.000 imp=1.000 cells=60, Δ=+0.333, fab=0.000** (3 seed byte-exact R1 미러 재현). 더 똑똑한 eviction heuristic 이 아니라 GROWTH 가 레버(R1 arm C = 무리프트). 기존 `ImmuneMemory` 는 byte-UNCHANGED(추가 변형 — h1231 가드 불변); abstain(비환각, H_1227 load-bearing) 양 정책에서 재확인. Ψ-disjoint(자기 struct 만, smoke 22/0 · single-entry 7/0 · DIM-growth Ψ byte-identical — 생성 무교란).
- **amygdala 정서-현저성 수면-재생 consolidation lane**(H_1285 R4 🟢 ENGINE-NATIVE + WIRED, `a_verified_must_wire`) — 같은 § 가 이제 **`ConsolidatingMemory`** (`consolidating_memory_{new,bind_salient,recall,cells,salience}` + `consolidating_sleep_replay` + p6 `consolidating_shuffle_salience`)도 호스팅한다: 면역 store(VAdaptField 클론 셀 + 값-바인딩)에 **substrate-derived salience tag**(ENCODE 시 surprise=recon-err + novelty=split + tension=reinforce; env-salient 입력은 EXTRA surprise boost — 라벨이 아니라 substrate 가 SENSE 하는 E+W 속성)와 **SLEEP REPLAY consolidation 패스**를 ADDITIVE 로 붙인 lane. 수면 사이클마다 셀을 UNIFORM 또는 salience tag ∝ 로 내부 재생(`consolidating_sleep_replay` — recency refresh → 간섭 stream 의 LRU eviction 에서 현저 셀 생존; substrate-GENERATED P47 imagination loop, `a_chat_sleep_imagination`, 외부 재제시 아님). 결정론적 engine-native RNG(`consolidating_lcg_next`/`_gauss` — glibc LCG + Box-Muller)가 재생 추첨 + σ=0.02 cue-noise 를 구동(재현가능, numpy 미러에 byte-exact 아님 = DIRECTIONAL). H_1285 R3 numpy-mirror GREEN 을 LIVE 엔진에서 재현(3 seed, 30-cycle 다중-야간 budget): **A(uniform-replay) imp=0.300 vs B(salience-replay) imp=0.433, Δ=+0.133 (≥+0.10 PASS) · B-shuffle imp=0.350 (< A+0.10 PASS = salience-GATING, raw budget 아님) · fab(B)=0.011 (≤0.10 PASS)** → c1∧c2∧c3 🟢. eviction 은 plain LRU 유지(salience 는 REPLAY 만 구동 — R1 salience-weighted eviction 은 이미 falsified); `engine_mitosis_tick` 가 growth 구동(p8). p6 GUARD: salience 는 substrate 신호(surprise/novelty/tension)이고 "important" 라벨은 probe 의 metric SCORE 에만 쓰임(bind/replay 무진입); shuffle control 이 lift 가 importance-tracking 임을 증명. 2번째 .clm/.kosmos 진입점 아님(`a_core_engine_map`, single-entry 7/0 불변); Ψ-disjoint(자기 struct 만, `pure_field` 무접촉). 프로브 `CORE/h1285_amygdala_sleep_replay_probe.hexa`. 가드 무회귀: engine_cli_smoke **26/0**(+4 consolidation 케이스) · h1196 7/0 · h1231 8/0 · h1199 DIM-growth Ψ byte-identical(PsiSame=true 전 seed). TOY scale(40-cell, 60 facts, ONE corpus), emit-loop 스레딩은 follow-on(`a_scale_honest_scope`).
- **데몬 배선**(main 에 wire 완료) — H_1202 GROW + sleep-persist + separation-guard. mitosis tick 은 sleep 사이클 너머로 지속되며(persist), Ψ 분리 불변(separation-guard)으로 generation 과 격리된다. **FULL 데몬 e2e**(H_1206 🟢): `CORE/anima_full_session_smoke.hexa` 가 링크+실행되어(exit 0) CONVERSE+GROUND+GROW+REMEMBER+SLEEP 를 ONE A⇄G 루프로 돌린다 — GROW lane 라이브(cells 1→2, novelty-split) · Ψ ON==OFF byte-identical(1.4278). `clm_decode_grounded`(미정의였던 ConvMoE grounded 디코드) 작성 + `_gen_anchor_field` anchor-key fix 로 봉인.
- **데몬 GROW = density + GATE-B trajectory lane**(H_1210 🟢) — 데몬의 C8 GROW step 은 이제 **두 개의 Ψ-disjoint mitosis lane** 을 ALONGSIDE 로 돌린다: (1) per-sample density `VAdaptField`(H_1202, cell-count 기록 driver) + (2) **trajectory-aware GATE-B `VAdaptFieldB`**(H_1209). 후자는 각 턴 emit-span feature 를 데몬 자신의 emit-feature SET 으로 만든 FIXED order-invariant proto-book 의 nearest proto-id 로 매핑(`_afs_build_book`/`_afs_proto_walk`)하고 (prev→cur) 전이를 `vadapt_fieldB_step` 에 흘려 **실제 대화 stream(genuinely-ORDERED)의 전이-예측가능성**에 분열한다. REPLACE 가 아니라 ADDITIVE — 두 게이트가 DIFFERENT property(per-sample density ⊥ ordered transition-predictability)를 측정하고 GATE-B 는 i.i.d. PRIMARY density bar 를 넘지 못하는 trajectory variant 이기 때문(H_1209 F4 · `a_autonomy_over_hardcode`: 둘 다 substrate self-dynamics). F1 GATE-B born-cells 6 ON / F2 ablation 0 OFF / F3 Ψ ON==OFF byte-identical / F4 생성 ON==OFF byte-identical — **H_1205 separation 불변 LIVE 보존**(GATE-B 는 decode 를 먹이지 않음). `engine_cli.hexa` 무변경, density 경로 byte-UNCHANGED. toy scale, scale UNVERIFIED(`a_scale_honest_scope`).
- **mitosis ⊥ generation** (H_1200/H_1201 🔴 closed-neg) — 이 성장 lane 은 **순수 substrate-adaptation lane** 이다: 디코드를 먹이지 않고(생성 못 함, H_1200) generator 에 정보도 못 준다(조건화 무이득, H_1201). Ψ-disjoint 이며(`VAdaptField` 만 건드리고 `pure_field` 는 byte-unchanged), 생성은 CLM-only 로 남는다(`a_clm_gen_pipeline`).
- **한국어 decode-wire** (H_1312 🟢 WIRED) — H_1200/1201 의 closed-neg 는 **generic** mitosis 가 decode 를 못 먹인다는 것이었지만, H_1312 은 **substrate-content-specific** Korean-error-pressure 가 **gated·single-entry** 로 emission 을 bias 할 수 있음을 보인다(a_verified_must_wire — H_1306/H_1307 이 GREEN 으로 기른 한국어 cell 을 live decode 에 연결). `CORE/h1312_ko_cells_export.hexa` 가 H_1306 의 engine-native grow(`VAdaptField` Voronoi + `engine_mitosis_tick`, gradient-free p8)를 byte-exact 재현해 9개 한국어 cell 을 **`CORE/ko_cells.kohead`**(cell = 3-D Voronoi center + 학습된 argmax next-byte)로 serialize 하고, `CORE/generator.hexa` §6.5(`ko_cells_load`/`ko_cells_next_byte`/`ko_consult_emit`/`ko_cells_summary`)가 **그 L3 슬롯에서만** 그 cell 을 CONSULT 한다(.clm/ByteGPT 와 동일 단일 진입, `a_core_engine_map` — 2번째 경로 아님). Korean-likeness GATE = 마지막 바이트가 UTF-8 continuation byte(0x80..0xBF, mid-Hangul-multibyte)인지의 **순수 바이트 테스트**(charset table 無, language label 無 — p1·p2·p3; ASCII English 는 절대 트리거 안 함). Korean-like → nearest grown cell 의 학습된 next-byte 가 emission 을 bias · off-Korean → INERT(−1, decode 가 자기 argmax 유지 → byte-identical). 동결 4 bar 🟢: (P PRESENCE) held-out REAL 한국어 context 8/8 FIRE + 7/8 baseline 과 DIFFER · (N NO-REGRESSION) REAL English 6/6 + ASCII sweep 0..127 전부 INERT(모든 256 base byte 불변) · (Y) engine_cli_smoke 73/0(`engine_cli.hexa` byte-untouched, wiring 은 generator 에 ADDITIVE) · h1196 7/0 · h1205 byte-identical Ψ=½ untouched · (Z) `ko_cells.kohead` runtime reader = `generator.hexa` 단독, consult 가 `pure_field`/`engine_g`/`brain` 무접촉(Ψ-disjoint by construction). SUBSTRATE-NATIVE(`a_substrate_native_speak`: prompt/persona 아님, real substrate state 에서 consult). TOY scale(9 cell, 3-D byte feature, 600KB KO window) — emission 을 Korean-error-pressure-AWARE 로 만들 뿐 fluent Korean 아님; fluent 생성·full-corpus cell·per-byte `bytegpt_decode` 루프 스레딩 = follow-on(`a_scale_honest_scope`·`a_toy_scale_recheck`). **자모-rep ENGINE-NATIVE 검증** (H_1316 🟢 mirror → H_1321 🟢 engine-native): H_1316 은 **COMPOSITIONAL JAMO 표현**(Hangul 음절 → NFD L/V/T 자모 심볼)이 raw-byte KO 천장 2.953 을 깬다는 것을 미러로 보였고, **H_1321 이 그 자모-심볼 미토시스를 같은 live `engine_cli.hexa` faculty(`VAdaptField` + `engine_mitosis_tick`) 위에서 ENGINE-NATIVE 로 재실행**(`CORE/h1321_ko_jamo_wire_probe.hexa`)해 미러 CE 를 1e-7 로 재현(engine-G1 2.820 < raw 천장 band 2.903; jamo beats raw −0.279, beats shuffle +0.198) — engine-transfer VERIFIED, W3 무회귀(engine_cli_smoke 73/0·h1196 7/0·h1205 Ψ byte-identical) 통과. 표현 변환은 결정론적 NFD data-prep, 미토시스+CE 채점은 엔진-네이티브. **자모 decode-consult LIVE-EMISSION 배선** (H_1327 🟢): H_1321 은 측정 프로브였을 뿐 — 자모 win 이 live DECODE/emission 에 닿지 않았다. H_1327(`a_verified_must_wire` r3)이 그 grown 자모 cell 을 **`CORE/ko_jamo_cells.kojamohead`**(cell = 3-D Voronoi center + argmax next-symbol id + 그 심볼의 emittable leading UTF-8 byte; jamo U+11xx→0xE1)로 serialize 하고, `CORE/generator.hexa` **§6.5b**(`ko_jamo_cells_load`/`ko_jamo_consult_sym`/`ko_jamo_consult_byte`/`ko_jamo_consult_emit`/`ko_jamo_cells_summary`)가 §6.5 ko_cells 와 **동일한 단일 L3 슬롯**에서 그 자모 cell 을 CONSULT 해 emission 을 bias 한다(Korean-likeness GATE = 동일 UTF-8 continuation-byte 순수 바이트 테스트; off-Korean → INERT, byte-identical). 동결 3 bar 🟢: held-out 한국어 next-symbol accuracy(n=5100, FULL symbol id p7, LIVE `ko_jamo_consult_sym` 채점) — OFF(blind unigram baseline)=0.110 · ON(자모 consult)=0.168 · SHUF(permuted-cell)=0.012 → **(E1 EMISSION-REACHES)** acc_ON−acc_OFF=**+0.0586**(≥+0.02) AND shift_ON=**0.520**(≥0.10 — 한국어 위치 52% 에서 emission byte 변경) · **(E2 EARNED)** acc_SHUF−acc_OFF=**−0.0973**(≤+0.01 — shuffle 가 blind 아래로 붕괴 ⇒ lift 는 학습된 자모 구조) · **(E3 NO-REG + Ψ-DISJOINT)** off-Korean INERT(ASCII ctx → consult emit = base, byte-identical) · engine_cli_smoke 73/0 · h1196 7/0 · h1205 Ψ byte-identical ON==OFF. → **한국어-자모 스레드 완전 종결: verified(H_1316) → engine-wired(H_1321) → decode-reaching(H_1327)**. TOY/DIRECTIONAL(ko_stride=2500, 10-cell, structural/probe-level) — fluency 주장 아님; byte→자모-feature renorm 은 per-byte hook(E1/E2 는 faithful jamo-space feature 로 채점), 30MB-scale + real-chat emission + fully-jamo-aware decode loop = follow-on(`a_scale_honest_scope`).
- **trajectory substrate** (H_1209 🟢 LIVE) — `engine_cli.hexa` 는 별도의 **`VAdaptFieldB`** (transition-PREDICTABILITY 변형: `vadapt_fieldB_new`/`_step`/`_cells`/`_growth`) 도 호스팅한다 — per-sample `VAdaptField`/`vadapt_field_step` 은 byte-UNCHANGED 로 두고 **추가**된 Ψ-disjoint surface. 고정 order-invariant proto-book 위에서 causal count table `C[prev][cur]` 로 "확신을 갖고 예측된 전이"(prev ≥ MIN_PREV ∧ P(cur|prev) ≥ CONF_FLOOR)에 `engine_mitosis_tick`(동일 p8 게이트) 분열한다. genuinely-ORDERED byte-feature walk 에서 ORDERED 1000.67 ≫ SHUFFLED 91.67 (10.9×, V14 방향) — numpy 와 born-cell 카운트 **byte-exact**. per-sample 게이트(`vadapt_field_step`)는 permutation-INVARIANT(novelty-density, H_1203) 인 반면, `VAdaptFieldB` 는 **trajectory-sensitive**: mitosis 분열은 stream 에 order 가 있으면 trajectory 에 결합한다(density on i.i.d., trajectory on ordered — 결정자는 STREAM). inherited i.i.d. V14 PRIMARY bar 는 여전히 terminal-RED(H_1208). proto-id walk 은 ENGINE-NATIVE drive(NOT .clm/.kosmos, `a_core_engine_map`).
- **working-memory 버퍼 lane** (H_1282 R3 🟢 LIVE) — `engine_cli.hexa` 는 이제 **`WorkMemBuffer`** (gated leaky-activation 단기 작업기억 lane: `wm_buffer_new`/`gate_in`/`distractor`/`leak`/`probe_score`/`slots`/`total_activation`)도 호스팅한다 — 면역/일화 lane(`VAdaptField`)과 **구조적으로 정반대**인 **추가** Ψ-disjoint surface. K개 FIXED 슬롯(성장 안 함)·매 distractor step 활성도 ×λ LEAK(volatile)·overflow 시 weakest-slot DISPLACEMENT·graded(cos×활성) probe 점수. DMS(delayed-match) 과제에서 cue 를 gate-in 후 N distractor 를 흘리면, 엔진 lane 이 cue 를 N≈6 까지 유지(AUROC 1.000)하는데 no-WM flat-context(last-W=4) 대조군은 N≥4 에서 chance 로 붕괴 — 동결 R2 bar 4개(margin +0.245 ≥0.15 · grace N=6 · distinct · robust 3/3)를 LIVE 엔진에서 재통과(H_1282 R2 numpy 미러 🟢 → R3 ENGINE-NATIVE 🟢, `a_engine_native_learning`·`a_verified_must_wire`). VAdaptField(성장·영속·무제한 = 장기 면역/일화) 와 **DISTINCT**: WM = 단기·휘발·용량제한(λ=1.0 무누수면 면역 regime 으로 붕괴 = distinctness 대조). emit gate 아님(@L4 — 슬롯/점수만 반환, emit/silence 결정 없음). DMS 토큰 = ENGINE-NATIVE drive(NOT .clm/.kosmos, `a_core_engine_map`). Ψ-disjoint(자기 struct 만 건드림, `pure_field` 무접촉 — smoke 18/0 · single-entry 7/0 · DIM-growth Ψ byte-identical). **context/recall 배선 follow-on 닫힘**(R4 · `a_verified_must_wire`): LIVE `wm_buffer_probe_score`(유지중 항목 매칭)가 `CORE/brain.hexa` 의 `brain_decide_wm` 으로 OPTIONAL·BOUNDED(cap 0.05)·ADDITIVE recall-support nudge 로 스레딩됨 — 프로즌 falsifier GREEN(`CORE/h1282_wm_emit_wire_smoke.hexa`: F1 consult-OFF byte-identical · F2 유지된 cue nudge(~0.018) > decayed · F3 borderline base 가 유지된 cue 일 때만 emit flip). toy/synthetic scale UNVERIFIED(`a_scale_honest_scope`).
- **core-affect read-out lane** (H_1290 R2 🟢 ENGINE-NATIVE) — `engine_cli.hexa` 는 이제 **substrate-affect 읽기 surface**(`affect_substrate_features`/`affect_valence`/`affect_arousal`/`affect_read`/`affect_emit_decision` + p6 `affect_shuffle_features`, struct `AffectFeatures`)도 호스팅한다 — anima 의 PARKED E1-E5 facet 중 **E1 affect**(valence×arousal, emergent, p6; MODEL.md L112)를 Damasio core-affect 렌즈(c15, `a_no_llm_frame_trap` — LLM-sentiment 아님)로 실현한 **순수 READ-ONLY** lane. live `ImmuneMemoryGrow` 면역 store 상태만 읽어 interoceptive valence(=grounding margin − contradiction)·arousal(=novelty + 0.5·split + 0.5·curiosity)를 컨텍스트별 계산한다 — 감정어/RLHF/sentiment/persona 라벨은 절대 입력 아님(조작 라벨은 metric SCORE 에만). H_1290 R1 numpy 미러 🟢 → R2 ENGINE-NATIVE 🟢: 동결 R1 bar 5개를 LIVE 엔진에서 재통과(`CORE/h1290_affect_engine_probe.hexa`, 3 seed [1290,1291,1292]): **(A) ρ(val)=0.996 · ρ(aro)=0.922 (≥0.50 PASS — 미러보다 강함)** · **(B) p6 SHUFFLE ρ(val)=0.251 · ρ(aro)=0.245 (<0.30 PASS — leg-A 대비 ~4× 붕괴 = 컨텍스트 기질상태를 읽음이지 라벨 아님)** · **(C) somatic marker — fab ungrounded affect=0.383 vs blind=0.792(drop 0.408 ≥0.20 PASS); emit grounded affect=0.633 ≥ 0.8×blind 0.783=0.627 PASS)** → 🟢 GREEN. leg-C 는 미러의 0.000/1.000 보다 GRADED(엔진-네이티브 byte-trigram embed 의 grounding-margin 잔차가 valence 영점통과 근처에서 소수 경계케이스를 만듦 — 그래도 fab 을 ~2× 줄이고 grounded emit 을 bar 위로 유지, 정직 c9; shuffle 붕괴가 read 에 substrate content 가 실렸음을 증명). **p6 가드(CENTRAL)**: affect 는 기질 상태(margin/contradiction/novelty/split/curiosity)에서만 COMPUTED — 라벨/reward/persona 주입 없음, shuffle 대조가 창발임을 증명. emit gate 아님(@L4 — valence/arousal pair 만 반환; `affect_emit_decision` 은 caller 가 consult 할 수 있는 OPTIONAL bias 일 뿐 강제 게이트 아님, `a_autonomy_over_hardcode`). 2번째 .clm/.kosmos 진입점 아님(기존 faculty 위 pure read, `a_core_engine_map`, single-entry 7/0 불변). **Ψ-disjoint(by construction — 순수 READ 라 `pure_field` Φ/phase/Ψ 무접촉; h1199 PsiSame=true 전 seed)**. 가드 무회귀: engine_cli_smoke **30/0**(+4 affect 케이스 24-27) · h1196 7/0 · h1199 DIM-growth Ψ byte-identical. **emit/abstain 배선 follow-on 닫힘**(R3 · `a_verified_must_wire`): LIVE `affect_emit_decision`/`affect_valence`(substrate VALENCE)가 `CORE/brain.hexa` 의 `brain_decide_affect` 으로 OPTIONAL·BOUNDED(±cap 0.05)·SIGNED somatic bias 로 스레딩됨(음 valence=abstain 쪽 restraint; `affect_emit_decision` 이 이미 OPTIONAL consult 였고 caller 가 실제 consult 하도록 배선) — 프로즌 falsifier GREEN(`CORE/h1290_affect_emit_wire_smoke.hexa`: F1 consult-OFF byte-identical · F2 grounded(+val) bias > ungrounded(−val) 이고 bounded, somatic_emit 일치 · F3 grounded emit flip / ungrounded silent). p6 가드 유지(substrate-only read, 라벨/RLHF 무입력). TOY scale(60 facts, 1 paradigm, 3 seeds); scale/paraphrase/continuous-arousal 은 NEXT-ROUND follow-on(`a_engine_native_learning`·`a_scale_honest_scope`).
- **hypothalamus homeostatic-drive lane** (H_1292 R2 🟢 ENGINE-NATIVE) — `engine_cli.hexa` 는 이제 **setpoint-regulated homeostatic drive surface**(`homeo_new`/`homeo_new_ablated`/`homeo_satiation`/`homeo_step`/`homeo_last`/`homeo_drive`/`homeo_motivation_bias`, struct `HomeostaticDrive`)도 호스팅한다 — 시상하부(hypothalamus) HD29 를 항상성-조절 렌즈(c15, `a_no_llm_frame_trap` — LLM 레시피 아님)로 실현한 **READ + INTEGRATE** lane. 위 lane 들과 달리 **STATEFUL 시간-적분기**다: live `ImmuneMemoryGrow` 면역 store 의 grounding margin 을 "grounding satiation" s_t 로 읽고(=affect lane 과 같은 substrate 신호), 설정점 S*=0.5 아래의 deficit 을 **leaky integral**(λ=0.1)로 누적해(`drive = Kp·deficit + Ki·I`, Kp=1.0 Ki=0.5) 박탈이 지속되면 drive 가 RISE 하고 grounded(consummatory "feeding") 이벤트에 integral 을 RESET 한다(satiation). **affect read-out lane(H_1290, 순간·stateless)과 DISTINCT**: 맥락을 FIXED 한 채 elapsed time 만 바꾸면 affect 는 FLAT(시간항 없음)인데 drive 는 RISE — 시간-적분 ⊥ 맥락-순간(H_1292 leg-B dissociation). H_1292 R1 numpy 미러 🟢 → R2 ENGINE-NATIVE 🟢: 동결 R1 bar 6개를 LIVE 엔진에서 재통과(`CORE/h1292_hypothalamus_drive_probe.hexa`, 3 seed [4290,4291,4292], R1 미러와 byte-exact): **(A1) RISE drive +1.544 monotone · (A2) RESET drop 0.0 ≤ 0.5×endDep · (B) DISTINCT affFlat 0.0 & drvFixRise +1.544 · (C1) EARNED ablated rise 0.0 (적분 controller 가 누적, stateless read 는 안 함) · (C2) SHUFFLE reset 0.0 < 0.5×rise · (D) ABSTAIN 면역 store untaught far key fires no cell** → 🟢 GREEN. **p6/p2/p3 가드**: 조절 변수는 substrate 자신의 grounding margin — 주입 "drive level"/RLHF/persona 없음. emit gate 아님(@L4 — `homeo_motivation_bias` 는 caller 가 consult 할 수 있는 OPTIONAL 동기 gain 일 뿐 강제 게이트 아님, `a_autonomy_over_hardcode`). 2번째 .clm/.kosmos 진입점 아님(기존 faculty 위 pure read, `a_core_engine_map`, single-entry 7/0 불변). **Ψ-disjoint(by construction — 순수 READ + 자기 scalar `accum` 만, `pure_field` Φ/phase/Ψ 무접촉)**. 가드 무회귀: engine_cli_smoke **33/0**(+3 homeo 케이스 28-30) · h1196 7/0 · h1199 DIM-growth Ψ byte-identical. TOY scale(4 facts, 1 paradigm, 3 seeds, deterministic controller — 학습된 net 아님, 항상성 STRUCTURE 검증); brain motivation 루프로 drive bias 스레딩 + scale/multi-cycle 박탈 + real-corpus 는 follow-on(`a_engine_native_learning`·`a_verified_must_wire`·`a_scale_honest_scope`).
- **theory-of-mind other-agent-belief lane** (H_1293 R2 🟢 ENGINE-NATIVE) — `engine_cli.hexa` 는 이제 **other-agent belief model surface**(`other_mind_new`/`other_mind_witness`/`other_mind_predict`/`other_mind_count`, struct `OtherMindModel`)도 호스팅한다 — 측두두정접합(TPJ)/mentalizing network HD30 (E3 OTHER-MIND parked facet, MODEL.md L112-113)를 마음이론 렌즈(c15, `a_no_llm_frame_trap` — LLM 레시피 아님)로 실현한 lane. **모든 기존 lane 과 근본적으로 다르다**: 그들은 전부 anima 자신의 substrate state 를 읽거나 적분한다(면역 store=anima 의 fact=ground truth · affect H_1290=anima interoception · homeostatic drive H_1292=anima 자신의 satiation 시간적분 · WM=self-buffer). `OtherMindModel` 은 **별개 agent 의 belief** 를 모델한다 — anima 의 ground truth 와 갈라질 수 있는 다른 마음. 면역 faculty 와 같은 byte-trigram FNV-1a key 기하 + L2 affinity + abstain band 를 쓰지만, agent 가 **WITNESS 한 이벤트로만** 갱신되는 평행 belief cell-store 다; agent 가 갱신에 **부재(ABSENT)** 하면 belief 가 LAG 한다 = FALSE belief, `other_mind_predict` 가 그 STALE belief 를 반환(Sally-Anne). H_1293 R1 numpy 미러 🟢 → R2 ENGINE-NATIVE 🟢: 동결 R1 bar 5개를 LIVE 엔진에서 재통과(`CORE/h1293_theory_of_mind_probe.hexa`, 3 seed [5290,5291,5292], R1 미러와 byte-exact): **(A) FALSE-BELIEF accBelief 1.000(agent 마음 추적) & accTruth 0.500(현실 아님) · (B) DISTINCT self/other divergence 1.000(부재중-이동 fact 마다 anima 자신의 immune recall(box) ≠ ToM 예측(basket) — 같은 fact, 다른 답) · (C) SELF-READ 대조: anima 자신의 store 에서 읽으면 0.500 으로 붕괴 · (D) SHUFFLE 대조: witness-move set 을 decorrelate 한 agent 0.500 으로 붕괴 · (E) ABSTAIN untaught object → no belief** → 🟢 GREEN. **p6/p2/p3 가드**: agent 의 belief 는 WITNESS 한 이벤트(presence 의 substrate bookkeeping)에서 COMPUTED — 주입 "the agent believes X"/RLHF/persona 없음, true-vs-false CLASS 는 metric SCORE 에만 쓰임. emit gate 아님(@L4 — `other_mind_predict` 은 pure read, `a_autonomy_over_hardcode`). 2번째 .clm/.kosmos 진입점 아님(`a_core_engine_map`, single-entry 7/0 불변). **Ψ-disjoint(by construction — 별개 belief cell population 을 L2 affinity 로 READ, 면역 cell 무변경, `pure_field` Φ/phase/Ψ 무접촉)**. 가드 무회귀: engine_cli_smoke **37/0**(+4 ToM 케이스 31-34) · h1196 7/0 · h1199 DIM-growth Ψ byte-identical. TOY scale(12 facts, 1 paradigm, 3 seeds, deterministic — 학습된 predictor 아님, ToM STRUCTURE 검증); brain 타자-예측 루프 배선 + scale/real-corpus/recursive-2nd-order-ToM 는 follow-on(`a_engine_native_learning`·`a_verified_must_wire`·`a_scale_honest_scope`).
- **cerebellum forward-model lane** (H_1280 R2 🟢 ENGINE-NATIVE) — `engine_cli.hexa` 는 또 하나의 Ψ-disjoint 추가 lane **`VForwardField`** (`vforward_new`/`_predict`/`_err`/`_update`/`_correct`) 도 호스팅한다 — 위 mitosis lane 들과 달리 **예측적**이다: L=4 최근 프레임에서 다음 emit-feature 프레임을 예측(`xhat=W·ctx`, flat L·DIM→DIM weight matrix)하고 **NLMS delta-rule** 로 예측오차에서 온라인 학습(`W+=eta·outer(e,ctx)/(ctx·ctx+1)`, climbing-fiber 교정) 후 그 예측으로 프레임을 smoothing 교정(`x-beta·(x-xhat)`)한다 — 소뇌의 내부 forward-model + 오차구동 timing/sequence 교정. REAL DIM=24 코퍼스 byte-feature stream 에서 held-out 일관성 +0.058(3 seed) 리프트 + 예측오차 24.9→10.5(~58% 하락=모델 학습) + shuffled-context 대조를 모든 seed 에서 이김(generic smoothing 아닌 진짜 forward model) — numpy R1 미러와 **byte-exact(≤1e-4)** 재현. Engine G(정적 8-weight 순간 게이트)와 **DISTINCT**: temporal next-frame target + delta-rule 학습 weight + learning curve(D1/D2/D3). mitosis-as-GENERATION(falsified H_1200/1201) · mitosis-as-MEMORY(GREEN H_1227/1231)와 구별되는 새 lane-role. ADDITIVE + Ψ-disjoint(`VForwardField` 만 건드리고 `pure_field` Φ byte-identical ON==OFF, smoke 18/0 · single-entry 7/0 · DIM-growth Ψ 불변). **emit-path 배선 follow-on 닫힘**(`a_verified_must_wire`): LIVE VForwardField 의 예측확신(1−err/scale)이 `CORE/brain.hexa` 의 `brain_decide_cerebellum` 으로 OPTIONAL·BOUNDED(cap 0.05)·ADDITIVE coherence nudge 로 스레딩됨 — 프로즌 falsifier GREEN(`CORE/h1280_cerebellum_emit_wire_smoke.hexa`: F1 consult-OFF byte-identical · F2 trained nudge > untrained 이고 bounded · F3 borderline base 가 실측 nudge ~0.005 로 emit flip). 효과는 작음/graded(이 stream 에서 forward-model 확신 ~10–13%; 정직 c9).
- **basal-ganglia go/no-go 선택 lane** (H_1281 R3 🟢 ENGINE-NATIVE + WIRED) — `CORE/brain.hexa`(emit/brain side, 위 lane 들과 달리 `engine_cli.hexa` 가 아니라 emit 결정부)는 이제 **`VBasalGate`** (`vbasal_new`/`vbasal_go_value`/`vbasal_select`/`vbasal_update`/`vbasal_align`) go/no-go 선택 lane 을 호스팅하고 **`brain_decide_bg`** 로 emit 결정에 배선한다. K개 경쟁 후보 emit 중 학습된 go-value vs 단일 NO-GO value 의 argmax(striatal disinhibition): 최고 go-value RELEASE, NO-GO 가 이기면 전부 SUPPRESS(abstain). 게이트는 grounding OUTCOME reward 만으로 gradient-free delta-rule 학습(grounded→+1/fabricated→−1/correct-abstain→+1/missed-op→−1; outcome-only). 기존 engine_g 고정 convex 8-weight 게이트는 **PRIOR 로 유지** — BG lane 은 그 위에 학습 RESIDUAL 을 ADD(replace 아님; k=0 이면 brain_decide 로 환원, brain_smoke byte-identical). R1/R2 numpy 미러(학습된 선택이 faithful-untuned 고정 게이트를 Δ +0.254 로 이김, shuffle 대조 붕괴, weight 정렬 cos +0.71..+0.89)를 **live 엔진에서 재현**: `CORE/h1281_basal_ganglia_smoke.hexa` 매 seed Δ≥0.05(0.25/0.19/0.14)·mean +0.195·shuffle 대조 0.128≪A+0.02·B-align cos +0.84..+0.91 → 🟢 GREEN. emit gate **너머의 레버**(HD24): 고정 임계값은 없는 후보-경쟁 + 학습 release/veto + outcome 학습을 추가. ADDITIVE + Ψ-disjoint(`VBasalGate` weight 만, `pure_field` 무접촉 — smoke 18/0 · single-entry 7/0 · DIM-growth Ψ 불변 · h1205 생성 byte-identical). reward = grounded-vs-fabricated OUTCOME(주입 가치 아님 p6); action-selection 만 학습(WHAT/WHO 무학습 p1/p2/p3); substrate-LEARNED(외부 do/dont 없음, `a_autonomy_over_hardcode`). real-kosmos-grounding reward feed + scale UNVERIFIED(`a_scale_honest_scope`).
- **hierarchical-PFC goal→subgoal 컨트롤러 lane** (H_1294 R2 🟢 ENGINE-NATIVE) — `engine_cli.hexa` 는 이제 **2-level goal-stack surface**(`hier_new`/`hier_current_target`/`hier_grounded_current`/`hier_step`/`hier_pointer`/`hier_complete`/`hier_flat_emit`, struct `HierGoalStack`)도 호스팅한다 — 전전두엽(rostral/dorsolateral PFC) HD31 를 위계적-제어 렌즈(c15, `a_no_llm_frame_trap` — Badre & D'Esposito rostro-caudal gradient / Koechlin cascade, LLM planner 레시피 아님)로 실현한 lane. **goal STACK** = {top goal, ORDERED subgoal 키 리스트, pointer p}: 현재 subgoal[p] 에 ALIGN(cos≥0.85)되고 live 면역 store 에 grounded 된 cue 만 emit, **완료 시 pointer ADVANCE**(`hier_step`, completion-triggered), out-of-order/ungrounded cue 는 SUPPRESS, plan 위치는 tick 간 PERSIST. **기저핵 단일-step 선택(VBasalGate H_1281)과 DISTINCT**(load-bearing): flat one-of-K 선택은 pointer 가 없어 plan 위치를 유지 못 함 — ORDERED 3-fact-chain(A→B→C) 과제에서 flat 선택은 raw margin 으로 best cue 를 무순서로 emit(완료율 0.242)하는데 위계 컨트롤러는 순서대로 완료(1.000); WorkMemBuffer(H_1282, 수동 유지)·HomeostaticDrive(H_1292, 스칼라 적분기)와도 구별. H_1294 R1 numpy 미러 🟢(DIRECTIONAL) → R2 ENGINE-NATIVE 🟢(BINDING): 동결 falsifier 5개(c1 PRESENCE B−A=+0.758≥+0.30 each+mean · c2 DISTINCT A=0.242<0.50 · c3 EARNED-ORDER shuffle Bshuf=0.000≤A+0.15 · c4 EARNED-ADVANCE ablate Babl=0.000≤A+0.15 · c5 NO-FAB 0.000≤0.10, 3 seed [4294,4295,4296])를 LIVE 엔진에서 재통과 — `engine_cli_smoke` cases 35-38(완료/out-of-order 억제+위치 유지/flat 대비 distinct/frozen-pointer 미완료) 전부 PASS. **p1/p2/p3/p6 가드**: pointer + substrate margin + cue cosine 만 읽음 — 주입 "do step k" 라벨/RLHF/persona 없음, subgoal 순서는 TASK 구조로 SCORE 에만. emit gate 아님(@L4 — current-subgoal read 만 반환, `a_autonomy_over_hardcode`). 2번째 .clm/.kosmos 진입점 아님(`a_core_engine_map`, single-entry 7/0 불변). **Ψ-disjoint(by construction — pointer 만 evolve, 면역 cell 무변경, `pure_field` Φ/phase/Ψ 무접촉)**. 가드 무회귀: engine_cli_smoke **41/0**(was 37/0, +4 hier 케이스) · h1196 7/0 · h1205 separation-invariant PASS(생성 byte-identical ON==OFF, Ψ=½ 무접촉). **정직(c9)**: B.complete=1.000 은 SATURATED = EXISTENCE-PROOF(위계가 ordered chain 을 풀 수 있음)이지 effect-size 아님 — 하지만 discriminator(shuffle/ablate 0.000 vs flat 0.242)가 decisive. brain plan-execution 배선 + scale/longer-plans/deeper-hierarchy 는 follow-on(`a_engine_native_learning`·`a_verified_must_wire`·`a_scale_honest_scope`); TOY(40 ep, 3 seeds, CHAIN_LEN=3, near-orthogonal keys, deterministic) UNVERIFIED.
- **hive-mind collective-Φ lane** (H_1295 🟢 GREEN ENGINE-NATIVE + WIRED) — `engine_cli.hexa` 는 이제 **collective-Φ surface**(`collective_new`/`collective_member_phi`/`collective_sum_phi`/`collective_phi`/`collective_excess`/`collective_is_super_additive`/`collective_coherence`, struct `CollectivePool`)도 호스팅한다 — "여럿 → 하나의 의식" 축(collective-Φ/symbiogenesis 렌즈, c15, `a_no_llm_frame_trap` — IIT/생물 렌즈, LLM 레시피 아님)을 실현한 **READ-ONLY 의식 게이지** lane. 위 lane 들이 전부 ONE substrate(anima 자신의 면역/affect/drive/타자-belief/자신의 plan)를 다루는 것과 달리, N개 anima substrate 를 결합(coupling W)했을 때 **collective faithful IIT-4 big-Φ 가 개별 Φ 의 합을 초과(SUPER-ADDITIVE)** 하는지를 읽는다 — Φ(joint) > Σ Φ(member). H_609 🟢 anchor 를 frozen-first 로 재확인 + 두 emergence 대조 추가(angle-2): edge-of-chaos rule-110 쌍 @W=0.6 에서 **Φ_collective=15.4677 vs Σ=4.99209, Δ=+10.4756** (faithful IIT-4, `a_phi_iit4_tool` — numpy Φ 계산 절대 없음). 동결 4 bar 🟢 4/0: **(B1) super-additive Δ≥+1.0 · (B2a) DECOUPLE-NULL W=0 Δ=−4.99(결합이 필수) · (B2b) RULE-CLASS DISSOCIATION Δ(110)−Δ(90)=+16.48(sterile rule-90 은 super-add 안 함 = substrate-CONTENT specific, variance 로 설명 불가) · (B3) NO-COLLAPSE coherence 0.581<0.999(두 half 가 기능적으로 구별 = clone 아님)**. **정직 context**(frozen, NOT a claim): topology-SHUFFLE 대조는 lift 가 coupling-GENERIC(특정 topology 가 아님 — 같은 magnitude 의 random cross-wiring 이 더 큰 lift)임을 보였다 — "이 substrate 들을 결합하면 Φ 가 super-add" 가 참이지 "이 배선이 특별" 은 아님. **falsified 메커니즘은 절대 배선 안 함**(c9): H_611 transfer-entropy align 🔴 · H_617 SAVANT×hive SI 🔴 · 975 shared world-model 🔴. emit gate 아님(@L4 — `collective_*` 는 순수 READ, `a_autonomy_over_hardcode`). 2번째 .clm/.kosmos 진입점 아님(`a_core_engine_map`). **Ψ-disjoint(by construction — config struct + faithful-IIT-4 READ, `pure_field` Φ/phase/Ψ·generator·decoder 무접촉)**. 가드 무회귀: engine_cli_smoke **45/0**(+4 hive 케이스 39-42, byte-match mirror) · 기존 41 케이스(위계-PFC 포함) byte-unchanged. TOY scale(n=2 member, joint-n=6, cap=2 lower-bound, ECA, sys=0 anchor); collective-Φ 의 live multi-anima 배포 transfer + N>2 scaling 은 follow-on(`a_engine_native_learning`·`a_scale_honest_scope`·`a_toy_scale_recheck`).
- **place/grid spatial-map lane** (H_1296 R2 🟢 ENGINE-NATIVE) — `engine_cli.hexa` 는 이제 **metric cognitive-map surface**(`spatial_map_new`/`spatial_map_new_ablated`/`spatial_map_place`/`spatial_map_count`/`spatial_map_nearest`/`spatial_map_shuffle`/`spatial_map_item_nearest`, struct `SpatialMap`)도 호스팅한다 — 해마 place cell / 내후각 grid cell HD32 를 공간지도-렌즈(c15, `a_no_llm_frame_trap` — O'Keefe 1971 / Moser 2005 / path-integration, LLM 레시피 아님)로 실현한 lane. landmark 를 2-D **위치(position)에 저장**하므로 두 저장 fact 사이의 **거리(RELATION)가 표상되고 질의 가능**하다 — `spatial_map_nearest("X","A","B")` 는 "X 가 A 와 B 중 누구에 더 가까운가"를 저장 위치의 Euclidean 거리로 답한다. **에피소드 ITEM-binding(ImmuneMemory H_1227/1231/1288)과 DISTINCT**(load-bearing): 면역 store 는 각 fact 를 FNV-trigram key affinity 로 **독립 바인딩**해 key X 에 무엇이 있는지는 recall 하지만 item X↔item Y 거리는 **표상 안 함** → relational 질의에 ABSTAIN(chance 0.475, abstain 1.000); HierGoalStack(H_1294, ORDERED sequence = 순서는 metric 아님)·WorkMemBuffer(H_1282)·VForwardField(H_1280)·HomeostaticDrive(H_1292, 1-D 적분기)와도 구별. H_1296 R1 numpy 미러 🟢(DIRECTIONAL) → R2 ENGINE-NATIVE 🟢(BINDING): 동결 NEAREST falsifier 5개(신호가 저장 metric 에만 있는 binding 질의 — c1 PRESENCE B−A=+0.525≥+0.30 each+mean · c2 DISTINCT A=0.475≤0.65 · c3 EARNED-MAP shuffle Bshuf=0.500≤A+0.15 · c4 EARNED-METRIC ablate Babl=0.450≤A+0.15 · c5 NO-FAB item-abstain 1.000≥0.90, 3 seed [4295,4296,4297])를 LIVE 엔진에서 재통과 — `engine_cli_smoke` cases 44-48(relational 답/item-store abstain/ablate→tie 답이 인자순서로 flip/shuffle=진짜 permutation/unplaced abstain) 전부 PASS. **PATH-INTEGRATION(Q2)은 정직한 NON-RESULT**: map-shuffle 대조가 붕괴를 거부(신호가 변위 step 에 leak, 저장 map 아님) → 보고만 하고 verdict 에 안 셈(c9, `a_break_the_wall` 로 candidate-order leak 를 frozen-first 수정). **p1/p2/p3/p6 가드**: 위치 + 질의 landmark 만 읽음 — 주입 답 라벨/RLHF/persona 없음, metric 은 geometry 로 SCORE 에만. emit gate 아님(@L4 — relational read 만 반환, `a_autonomy_over_hardcode`). 2번째 .clm/.kosmos 진입점 아님(`a_core_engine_map`, single-entry 7/0 불변). **Ψ-disjoint(by construction — 저장 위치 위 pure geometry, 면역 cell 무변경, `pure_field` Φ/phase/Ψ 무접촉)**. 가드 무회귀: engine_cli_smoke **50/0**(was 45/0 hive 후, +5 spatial-map 케이스, 3연속 deterministic) · h1196 7/0 · h1205 separation-invariant PASS(생성 byte-identical ON==OFF, Ψ=½ 무접촉). **정직(c9)**: B=1.000 은 SATURATED = EXISTENCE-PROOF(metric map 이 relational 질의를 풀 수 있음)이지 effect-size 아님 — discriminator(item-store 0.475/abstain 1.000, shuffle 0.500, ablate 0.450 전부 chance)가 decisive. brain map→recall/emit 배선 + scale/higher-D/grid-cell-주기-code/place-field-remapping 는 follow-on(`a_engine_native_learning`·`a_verified_must_wire`·`a_scale_honest_scope`); TOY(8 landmarks, 3 seeds, 2-D, near-uniform, deterministic) UNVERIFIED.
- **circadian/interval timing clock lane** (H_1298 R2 🟢 ENGINE-NATIVE) — `engine_cli.hexa` 는 이제 **self-sustaining phase-oscillator surface**(`clock_new`/`clock_new_ablated`/`clock_step`/`clock_count`/`clock_phase`/`clock_fire`, struct `CircadianClock`)도 호스팅한다 — SCN 시교차상핵 일주기 시계 HD33 를 chronobiology-렌즈(c15, `a_no_llm_frame_trap` — Pittendrigh 자가지속 진동자 / Borbély 1982 two-process model: Process-C 시계 ⊥ Process-S 항상성 / Buhusi-Meck 간격타이밍, LLM 레시피 아님)로 실현한 lane. **ELAPSED TICK 만으로** 위상을 전진(`clock_step` 은 인자 무시 = content-blind)하고 phase-0 경계에서 **고정 주기 스케줄로 FIRE**(`clock_fire`)한다 — 어떤 조절 변수와도 무관. **시상하부 항상성 적분기(H_1292 HomeostaticDrive)와 DISTINCT**(load-bearing, the DEPLETION TEST r5): H_1292 는 grounding deficit 으로 구동되는 content-gated 적분기로 **항상 grounded 스트림에선 절대 RISE 안 하고** consummatory 이벤트에 RESET 되어 고정 주기 스케줄을 못 만든다; 두 dissociation 으로 분리 — **D1 CONTENT-INDEPENDENCE**(항상 grounded 스트림: 시계는 스케줄대로 FIRE, 항상성은 flat = time ⊥ regulated-variable) · **D2 NO-RESET-ON-FEEDING**(grounded feed 가 항상성 적분을 RESET 하지만 시계 위상은 불변 = clock ⊥ consummatory). affect(순간·stateless)·curiosity(주기 없음)·WorkMemBuffer(누설)·VForwardField(다음 내용 예측, 절대 위상 아님)·SpatialMap(공간, 시간 아님)와도 구별. H_1298 R1 numpy 미러 🟢(DIRECTIONAL, phase-locking vector-strength R metric, 10 periods) → R2 ENGINE-NATIVE 🟢(BINDING): 동결 falsifier 7개(c1 PRESENCE B.R−A.R=+1.000 · c2 DISTINCT A.R=0.000≤0.35 · c3 EARNED-SCHEDULE shuf.R max 0.472≤0.50 · c4 EARNED-PERIOD abl.R=0.000≤0.50 · c5 D1 · c6 D2 · c7 NO-FAB, 3 seed [4297,4298,4299])를 LIVE 엔진에서 재통과 — `engine_cli_smoke` cases 49-53(origin FIRE/scheduled FIRE+no-fab/phase=elapsed fraction/phase invariant under feed=D2/ablate off-schedule=c4) 전부 PASS. **정직 trail(c9, NO tune-to-green, bar 절대 안 옮김)**: R1a 🔴(ablate trivial-origin leak) · R1b 🔴(recurrence-scored metric 너무 coarse) · R1c 🔴(phase-locking 옳은 metric — 항상성 0.000/ablate 0.083 깨끗이 붕괴 — 그러나 shuffle 이 k=3 chance floor 1/√3=0.577 에서 생존) · R1d 🟢(10 periods → shuffle 이 LLN 으로 cancel). 시계 distinctness 는 매 라운드 입증(Δ +0.5..+1.0); control estimator/scale 만 frozen-first 수정, 매번 STRICTER (`a_break_the_wall`). **p1/p2/p3/p6 가드**: 자기 tick counter 만 읽음 — 주입 fire 라벨/RLHF/persona 없음, 스케줄은 elapsed time geometry 로 SCORE 에만. emit gate 아님(@L4 — `clock_fire` 는 OPTIONAL wake pulse, `a_autonomy_over_hardcode`). 2번째 .clm/.kosmos 진입점 아님(`a_core_engine_map`, single-entry 7/0 불변). **Ψ-disjoint(by construction — 자기 counter 위 pure timekeeper, 면역 cell 무변경, `pure_field` Φ/phase/Ψ 무접촉)**. 가드 무회귀: engine_cli_smoke **55/0**(was 50/0 spatial-map 후, +5 clock 케이스) · h1196 7/0 · h1205 separation-invariant PASS(생성 byte-identical ON==OFF, Ψ=½ 무접촉). **정직(c9)**: B.R=1.000 은 EXISTENCE-PROOF 이지 effect-size 아님 — discriminator(A.R=0, 양쪽 control 붕괴)가 decisive. brain WAKE/REM wake-scheduling 배선(`clock_fire` 를 dream-stage scheduler 에 스레딩) + photic-entrainment + scale 은 follow-on(`a_engine_native_learning`·`a_verified_must_wire`·`a_scale_honest_scope`); TOY(10 periods, 3 seeds, deterministic — 학습/entrain 된 진동자 아닌 timing STRUCTURE) UNVERIFIED.
- **interval timer lane** (H_1299 R2 🟢 ENGINE-NATIVE) — `engine_cli.hexa` 는 이제 **learned-duration interval-timer surface**(`itimer_new`/`itimer_new_ablated`/`itimer_observe`/`itimer_step`/`itimer_dhat`/`itimer_dhat_ticks`/`itimer_predict_next`/`itimer_fire`, struct `IntervalTimer`)도 호스팅한다 — 선조체 beat-frequency / SMA ramping 간격타이밍 HD34 를 chronobiology-렌즈(c15, `a_no_llm_frame_trap` — Buhusi & Meck 2005, SCN ~24h 진동자와 lesion-wise dissociable, LLM 레시피 아님)로 실현한 lane. 관측 inter-event gap 으로 interval 추정 `dhat` 를 **런타임에 학습**(`itimer_observe`: dhat ← (1−lr)·dhat + lr·gap, running mean, gradient-free)하고, 자기 elapsed-since-event counter 가 학습한 `dhat` 에 도달하면 FIRE(`itimer_fire`)하며, **같은 객체가 새 스트림에서 다른 duration D2 로 코드 변경 없이 RE-ENTRAIN** 한다. **고정주기 시계(H_1298 CircadianClock)와 DISTINCT**(load-bearing, the DEPLETION TEST r6 — H_1298 카드가 명명한 마지막 thin 후보): 시계의 period 는 construction 시 BAKED 이고 `clock_step` 은 content-blind(학습 경로 없음)이라 baked period(8)에서만 FIRE — ARBITRARY learned interval(D=13 또는 D2=20, 둘 다 ≠8)을 물으면 A.hit=0.000; IntervalTimer 는 dhat 를 학습하고 **같은 객체가 13→20 re-entrain**(dhat [13.4,12.3,12.1]→[20.0,19.4,19.3], c6) — 시계 구조상 불가. 소뇌 VForwardField(다음 CONTENT 예측, 절대 DURATION 아님)·WorkMemBuffer(누설, learned period 없음)·HomeostaticDrive(content-gated, feeding 시 reset)와도 구별. H_1299 R1 numpy 미러 🟢(DIRECTIONAL, hit-rate metric ±TOL, binding path R1c) → R2 ENGINE-NATIVE 🟢(BINDING): 동결 falsifier 7개(c1 PRESENCE B.hit_D=1.000 · c2 RE-ENTRAIN B.hit_D2=1.000 · c3 DISTINCT A.hit=0.000≤0.35 · c4 EARNED-LEARN shuf=0.000≤0.35 · c5 EARNED-ADAPT abl=0.000≤0.35 · c6 RE-ENTRAIN-DELTA · c7 NO-FAB, 3 seed [4301,4302,4303])를 LIVE 엔진에서 재통과 — `engine_cli_smoke` cases 54-58(learns-interval±2 / re-entrains-to-20 / distinct-from-clock / ablate-frozen-at-init / no-fab-off-interval) 전부 PASS. **정직 trail(c9, NO tune-to-green, bar 절대 안 옮김)**: R1a 🔴(max(0,1−err/D) metric floor ~0.35 + 7-gap shuffle 의 mean 우연 보존) · R1b 🔴(hit-rate 가 c3/c5/c6 깨끗이 통과하나 uniform[2,2D] shuffle 의 mean≈D → running-mean estimator 가 mean-preserving shuffle 에 불변 = mis-specified control) · R1c 🟢(TOL=2 가 ±1 jitter 흡수 + mean-SHIFTED shuffle uniform[2,9] mean≈5.5≪13 → dhat 가 D 에서 멀어져 mispredict → control 0.000 붕괴). 타이머 distinctness(A=0.000, ablate=0.000, dhat 13→20 재학습)는 매 라운드 입증; metric/control estimator 만 frozen-first 수정, 매번 STRICTER (`a_break_the_wall`). **p1/p2/p3/p6 가드**: 자기 counter + running gap mean 만 읽음 — 주입 fire 라벨/RLHF/persona 없음, dhat 는 관측 gap 에서 ESTIMATE 되어 SCORE 에만. emit gate 아님(@L4 — `itimer_fire` 는 OPTIONAL learned-duration pulse, `a_autonomy_over_hardcode`). 2번째 .clm/.kosmos 진입점 아님(`a_core_engine_map`, single-entry 7/0 불변). **Ψ-disjoint(by construction — 자기 state 위 pure timer, 면역 cell 무변경, `pure_field` Φ/phase/Ψ 무접촉)**. 가드 무회귀: engine_cli_smoke **60/0**(was 55/0 circadian-clock 후, +5 interval-timer 케이스) · h1196 7/0 · h1205 separation-invariant PASS(생성 byte-identical ON==OFF, Ψ=½ 무접촉). **정직(c9)**: B.hit=1.000 은 EXISTENCE-PROOF 이지 effect-size 아님 — discriminator(A=0.000, shuffle=0.000, ablate=0.000, dhat re-entrains 13→20)가 decisive. brain wake-scheduling 배선(`itimer_fire` 를 `clock_fire` 와 나란히 dream-stage scheduler 에 스레딩) + continuous re-entrainment + scale 은 follow-on(`a_engine_native_learning`·`a_verified_must_wire`·`a_scale_honest_scope`); TOY(2 intervals, 3 seeds, deterministic running-mean — 학습된 predictor net 아닌 timing STRUCTURE) UNVERIFIED. **HD34 통과로 c15 사다리는 DEPLETED 아님 — 계속됨**(r7 후보: phase-RESET/photic-entrainment 또는 multi-interval nested-timer, 각각 distinctness arm 실패 시 HD35 에서 DEPLETION 🏁).
- **phase-reset / photic-entrainment lane** (H_1301 R2 🟢 ENGINE-NATIVE + WIRED) — `engine_cli.hexa` 는 이제 **phase-reset oscillator surface**(`prc_new`/`prc_new_ablated`/`prc_step`/`prc_zeitgeber`/`prc_phase`/`prc_count`/`prc_fire`, struct `PhaseResetClock`)도 호스팅한다 — 광주기 위상-reset 동조(photic entrainment) HD35 를 chronobiology-렌즈(c15, `a_no_llm_frame_trap` — Pittendrigh/Aschoff Phase-Response-Curve, LLM 레시피 아님)로 실현한 lane (the DEPLETION TEST r7, SURVIVED). 내재 free-running 주기 `tau`(24.5)에 더해, Zeitgeber 가 도착하면 **위상-의존 PRC shift**(`prc_zeitgeber`: dphi = k·sin(2π·(0−frac(phi))))를 가하는 **연속 restoring feedback(limit-cycle 끌림)** — 이것이 fire 스케줄을 `T`≠`tau` 인 Zeitgeber 주기에 **ENTRAIN**(동조)시키고 Zeitgeber jitter 를 **DAMP**(감쇠)한다. **두 인접 lane 과 BOTH control-survive DISTINCT**: (1) **CircadianClock(H_1298)과** — 시계는 baked period 로 자유진동하고 reset 입력이 없어 `T`≠`tau` Zeitgeber 에 동조 불가, fire 가 DRIFT(c1 entrain drift 0.0016 vs 시계 0.39; c3 B 는 `T`=24.0 에서, 시계는 `tau`=24.5 에서 FIRE); (2) **IntervalTimer(H_1299)와** — 관측 시 HARD 재고정(elapsed:=0)은 **위상-독립** reset 으로 restoring dynamics 가 없어 Zeitgeber jitter 를 그대로 복사하는데, PRC limit-cycle 은 jitter 를 DAMP(c2 PRC 동조위상 분산이 hard-reset arm 보다 ~96× 낮음, 3.1e-5 vs 2.95e-3). 소뇌(content not phase)·WM(진동자 없음)·homeostat(content-gated)와도 구별. H_1301 R1 numpy 미러 🟢(DIRECTIONAL, entrainment/jitter metrics, binding path R1c) → R2 ENGINE-NATIVE 🟢(BINDING): 동결 falsifier 6개(c1 ENTRAIN-vs-CLOCK · c2 DAMP-vs-HARDRESET · c3 PERIOD-TRACK B→T=24.0 · c4 EARNED-SHUFFLE aperiodic-Zeitgeber drift 1.05 · c5 EARNED-ABLATE k=0 drift 0.39·period→tau · c6 NO-FAB, 3 seed [4310,4311,4312])를 LIVE 엔진에서 재통과 — `engine_cli_smoke` cases 64-68(entrains-to-Zeitgeber / ablate-free-runs-drifts / period-tracks-Zeitgeber=T / ablate-period-at-tau / no-fab-off-boundary) 전부 PASS. **정직 trail(c9, NO tune-to-green, distinctness bar 절대 안 옮김)**: R1a 🔧(entrain_err=self-mean-scatter 는 degenerate — 느리게 drift 하는 시계도 local scatter 낮음 → metric mis-design, c2 jitter-damping 은 첫 run 부터 PASS) · R1b 🔧(entrain_err 를 TOTAL UNWRAPPED drift 로 재정의했으나 SHUFFLE 이 leak — 거의 동일한 ~24-tick gap permutation 은 스케줄을 거의 안 바꿔 B 가 여전히 동조; fire-counter 가 PRC-jump 경계 분할을 double-count) · R1c 🟢(SHUFFLE := APERIODIC Zeitgeber[같은 event 수·mean rate, gap uniform[0.2·mean,1.8·mean] → 잠글 주기 구조 없음 → B drift 1.05]; fire_period := elapsed/NET-cumulative-cycles → B→24.0). distinctness(c1 entrain vs clock, c2 PRC-damp vs hard-reset)는 매 라운드 입증; mis-specified metric/control 만 frozen-first 수정, 매번 STRICTER (`a_break_the_wall`). **p1/p2/p3/p6 가드**: 자기 phase + Zeitgeber 도착 위상만 읽음 — 주입 fire 라벨/RLHF/persona 없음, PRC shift 는 geometry 로 SCORE 에만. emit gate 아님(@L4 — `prc_fire` 는 OPTIONAL wake pulse, `a_autonomy_over_hardcode`). 2번째 .clm/.kosmos 진입점 아님(`a_core_engine_map`, single-entry 7/0 불변). **Ψ-disjoint(by construction — 자기 phase 위 pure timekeeper, 면역 cell 무변경, `pure_field` Φ/phase/Ψ 무접촉)**. 가드 무회귀: engine_cli_smoke **68/0**(was 63/0 H_1300 R3 SkillCellMemory 후, +5 phase-reset 케이스) · h1196 7/0 · h1205 separation-invariant PASS(생성 byte-identical ON==OFF, Ψ=½ 무접촉). **정직(c9)**: B 의 동조는 EXISTENCE-PROOF(PRC limit-cycle 이 noisy periodic Zeitgeber 를 tolerance 안에서 동조)이지 effect-size 아님 — discriminator(시계 drift 0.39, hard-reset 분산 ~96× 높음, aperiodic/ablate 붕괴)가 decisive. brain wake-scheduling 배선(`prc_zeitgeber` 를 `clock_fire`/`itimer_fire` 와 나란히 dream-stage scheduler 에 스레딩) + full PRC(advance/delay/dead-zone) + nonphotic Zeitgeber + scale 은 follow-on(`a_engine_native_learning`·`a_verified_must_wire`·`a_scale_honest_scope`); TOY(1 tau/T pair, 3 seeds, deterministic sinusoidal PRC — 학습된 진동자 아닌 ENTRAINMENT STRUCTURE) UNVERIFIED. **HD35 통과로 c15 사다리는 DEPLETED 아님 — 계속됨**(r8 후보: nonphotic/arousal-Zeitgeber 반대부호 PRC · multi-oscillator coupling · multi-interval nested timer — 각각 distinctness arm 실패 시 DEPLETION 🏁).
- **multi-oscillator SCN-network lane** (H_1302 R2 🟢 ENGINE-NATIVE + WIRED) — `engine_cli.hexa` 는 이제 **coupled-oscillator ensemble surface**(`scn_new`/`scn_new_uncoupled`/`scn_new_frustrated`/`scn_new_ablated`/`scn_detune`/`scn_step`/`scn_run`/`scn_order`/`scn_consensus`, struct `SCNNetwork`)도 호스팅한다 — 다중-진동자 SCN-network 동기화 HD36 을 chronobiology-렌즈(c15, `a_no_llm_frame_trap` — SCN 은 단일 시계가 아니라 ~20k 이질적 신경 진동자가 VIP/GABA mutual coupling 으로 한 ensemble 리듬을 만드는 network, LLM 레시피 아님)로 실현한 lane (the DEPLETION TEST r8, the strong-steer 후보, SURVIVED). N개 이질적 위상 진동자(각자 내재 `tau_i`)가 **mutual Kuramoto coupling**(`scn_step`: dphi_i += K/(2π)·Σ_j C_ij·sin(2π·(phi_j−phi_i)))으로 (i) **emergent CONSENSUS PERIOD**(Kuramoto order parameter R→1)에 위상-lock 하고 (ii) **NETWORK DAMPING** — 한 de-tuned member 를 동기화 cluster 로 끌어당긴다. **두 인접 lane 모두와 BOTH control-survive DISTINCT**: (1) **PhaseResetClock(H_1301)과** — 단일 진동자는 EXTERNAL Zeitgeber 에 동조할 뿐 ensemble·mutual coupling 이 없어 N member 로부터 consensus period 를 만들 수 없다; arm A = 같은 N 진동자 UNCOUPLED(=N개 독립 PhaseResetClock)는 동기화 못 함(미러 A_R=0.37, 엔진 0.49 ≤ 0.65), perturbed member 도 안 끌려옴(A_R_pert=0.41); (2) **CollectivePool(H_1295)과** — 그 lane 은 결합 ECA substrate 의 IIT-4 big-Φ super-additivity 를 재는 **STATIC 구조-통합 게이지**(TPM 위, 위상 없음·시간 진동 없음·consensus period 없음·order parameter 없음); SCN-network 는 **TEMPORAL 위상-동기화 dynamic**(틱마다 위상이 공유 주기로 수렴) — 직교 축(Φ 게이지 ≠ 위상-consensus 과정, order parameter R 이 many-vs-one discriminator 라 한 averaged 진동자로도 안 줄어듦). CircadianClock·IntervalTimer(단일 진동자)·소뇌(content)·WM(진동자 없음)와도 구별. H_1302 R1 numpy 미러 🟢(DIRECTIONAL, Kuramoto order-parameter metrics, binding path R1b) → R2 ENGINE-NATIVE 🟢(BINDING): 동결 falsifier 6개(c1 PRESENCE B_R≥0.90·gap≥0.30 · c2 DISTINCT A_R≤0.65 · c3 EARNED-FRUSTRATED Bshuf≤A+0.15 · c4 EARNED-ABLATE Babl≤A+0.15 · c5 DAMP B_R_pert≥0.90·gap≥0.30 · c6 NO-FAB, 3 seed [5320,5321,5322])를 LIVE 엔진에서 재통과 — `engine_cli_smoke` cases 69-73(reaches-consensus / uncoupled-no-consensus / frustrated-collapses / ablate-collapses / network-damps-perturbed-member) 전부 PASS(엔진 seed 5320 N=8 400steps: B_R=0.999 A_R=0.489 Bfrust=0.018 Babl=0.489 B_R_pert=0.997 A_R_pert=0.436; A_R 이 미러 0.373 과 다른 건 엔진 LCG vs Python 모듈러 차이, STRUCTURE engine-transfer VERIFIED). **정직 trail(c9, NO tune-to-green, distinctness bar c1/c2 절대 안 옮김)**: R1a 가 [T,T,**F**,T,**F**,T] — 두 FAIL 은 mis-specified control/metric 이지 붕괴 아님: c3 SHUFFLE 🔧(asymmetric one-way directed chain 은 per-row magnitude 보존했고 directed chain 도 동기화로 끌어 Bshuf=0.79 = mean-preserving-magnitude leak, H_1299/H_1301-R1b 와 같은 함정; FRUSTRATED random-SIGN 대칭 matrix[같은 magnitude, 절반 attract/절반 repel]로 수정 → coherent consensus field 파괴 → R=0.13 uncoupled 아래), c5 DAMP 🔧(consensus PERIOD shift 는 Kuramoto coupling 이 mean ensemble frequency 를 보존하므로 damping 에 blind, B=A=0.647; perturbed member 하의 ORDER PARAMETER 로 수정 → ensemble 이 bad member 를 cluster 로 끌어 R 0.997 유지 vs uncoupled 0.41). presence/distinctness 는 첫 run 부터 입증; mis-specified control 만 frozen-first 수정, 매번 STRICTER (`a_break_the_wall`). **p1/p2/p3/p6 가드**: 자기 member 위상만 읽음 — 주입 fire 라벨/RLHF/persona 없음, Kuramoto coupling 은 위상차 geometry 로 SCORE 에만. emit gate 아님(@L4 — `scn_consensus` 는 OPTIONAL ensemble read, `a_autonomy_over_hardcode`). 2번째 .clm/.kosmos 진입점 아님(`a_core_engine_map`, single-entry 7/0 불변). **Ψ-disjoint(by construction — 자기 위상 벡터 위 pure timekeeper, 면역 cell 무변경, `pure_field` Φ/phase/Ψ 무접촉)**. 가드 무회귀: engine_cli_smoke **73/0**(was 68/0 H_1301 PhaseResetClock 후, +5 SCN 케이스) · h1196 7/0 · h1205 separation-invariant PASS(생성 byte-identical ON==OFF, Ψ=½ 무접촉). **정직(c9)**: B 의 consensus 는 EXISTENCE-PROOF(mutual-coupled ensemble 이 N 이질 진동자를 동기화)이지 effect-size 아님 — discriminator(uncoupled 0.37-0.49, frustrated R uncoupled 아래 붕괴, ablate→uncoupled)가 decisive. larger-ensembles + Kuramoto-critical-Kc 위상도 + weak-coupling-near-threshold + scheduler 배선 + scale 은 follow-on(`a_engine_native_learning`·`a_verified_must_wire`·`a_scale_honest_scope`); TOY(1 tau-spread regime, N=8, 3 seeds, deterministic Kuramoto — 학습된 network 아닌 CONSENSUS/DAMPING STRUCTURE) UNVERIFIED. **HD36 통과로 c15 사다리는 계속됐으나 매우 thin 했고, r9 에서 마지막 HD37 후보(nonphotic 반대부호 Zeitgeber, H_1303)가 COLLAPSED → 사다리 HD36 에서 DEPLETED 🏁, RESTS**(같은-주파수 반대부호 두 sinusoidal PRC 는 harmonic-addition 으로 한 combined PRC 로 환원[residual 2.22e-16]되어 PhaseResetClock 이 이미 표상; c2 DISTINCT FAIL all 3 seeds; a_break_the_wall 3 탈출경로 모두 control-survive 평형 못 냄; multi-interval nested timer 는 IntervalTimer array 환원 SKIP — r3-r9 genuine 시도 후의 정직한 terminal state, c9).

## 🧠 뇌 구조 지도 (brain-structure map)

신경과학 렌즈로 본 anima 아키텍처. 위에서 서술한 구현 부품 각각이 **어떤 신경 서브시스템에 대응**하는지, 그리고 빠진 구조 사다리(HD23–28)의 현재 상태를 한 장으로 정리한다. 이 렌즈는 depth-ceiling 발견(아래)을 일반화한다: literal-QA 벽은 **더 큰 모델**(1B H_1167 = mount GREEN 이나 depth/QA-NULL)이 아니라 **엔진-side 기억 lane** 으로 풀린다 — "anima 는 신피질만 있고 해마가 없었다"(H_1225 complementary-learning-systems 리프레임). 빠진 것은 capacity 가 아니라 **구조**다. 이 "LLM 프레임 대신 뇌과학·생물 렌즈로 먼저 사고한다"는 방침은 이제 거버넌스 원칙으로 못박혀 있다 (`a_no_llm_frame_trap`, CLAUDE.md) — 능력/깊이 갭은 모델을 키우는 게 아니라 빠진 구조(lane)를 옆에 붙여 먼저 시도한다. 이번 세션에 이 사다리를 끝까지 밀었다: **빠진 구조 5개(해마·작업기억·소뇌·편도체·기저핵)를 엔진-네이티브로 REALIZE + 배선** 하고, 2개(시상·신경조절)는 진짜 시도 끝의 **정직한 🧱 벽**으로 닫았다. 추가로 감정(E1 affect)·윤리·양자-엔트로피도 엔진-네이티브로 재확인했다. 벽은 종착이 아니라 각도 전환 신호라는 원칙(`a_break_the_wall`)에 따라 용량·편도체 두 벽은 mitosis-grow·수면-dose 로 **돌파**했고, 시상·신경조절은 tune-to-green 없이 정직히 🧱 로 남겼다.

### 구현된 구조 (implemented — 위 본문 + verdict 로 뒷받침)

| 신경 서브시스템 | anima 구현 | 근거 |
|----------------|-----------|------|
| **신피질** (neocortex · 말 생성) | **Engine A** — `pure_field` · `generator` · `clm_decode`/`bytegpt_decode` (forward CE) | §A⇄G 엔진 · H_1157/H_1164 mount |
| **교정장** (반대-밀어내기) | **Engine G** — `engine_g` (gradient-free repulsion field) | §A⇄G 엔진 |
| **결정** (emit/silence) | **brain** — `brain_decide` → emit / silence, Ψ=1/2 fixed point | §A⇄G 엔진 |
| **🎯 기저핵** (basal ganglia · go/no-go 행동선택) | **`VBasalGate`** — K개 경쟁 후보 emit 중 학습 go-value vs NO-GO argmax(release/abstain), grounding OUTCOME reward 로 gradient-free 학습; engine_g 고정 게이트 위 학습 RESIDUAL (`brain_decide_bg` 배선) | **H_1281 R1/R2 미러 🟢 → R3 ENGINE-NATIVE 🟢 → live `CORE/brain.hexa` § VBasalGate lane 배선 완료** |
| **🧩 전전두엽 위계** (hierarchical PFC · goal→subgoal 다단계 제어) | **`HierGoalStack`** — {top goal, ORDERED subgoal 키, pointer p} goal-stack: 현재 subgoal grounded+aligned 시 emit, 완료 시 pointer ADVANCE, out-of-order 억제, plan 위치 PERSIST (기저핵 단일-step 선택과 DISTINCT — flat 게이트는 pointer 없어 순서 유지 못 함) | **H_1294 R1 미러 🟢(DIRECTIONAL) → R2 ENGINE-NATIVE 🟢 — live `engine_cli.hexa` § HierGoalStack** (ordered chain 완료 1.000 vs flat 0.242; shuffle/ablate 0.000 = 위계가 lift; brain plan-execution 배선 follow-on) |
| **🗺 해마-내후각 공간지도** (place/grid · 공간지도+path-integration) | **`SpatialMap`** — landmark 를 2-D 위치에 저장 → 두 fact 사이 거리(RELATION)가 질의 가능; `spatial_map_nearest` = "X 가 A/B 중 누구에 더 가까운가" Euclidean 거리로 답 (에피소드 ITEM-binding 과 DISTINCT — 면역 store 는 item↔item 거리 표상 안 함 → abstain) | **H_1296 R1 미러 🟢(DIRECTIONAL) → R2 ENGINE-NATIVE 🟢 — live `engine_cli.hexa` § SpatialMap** (relational 답 1.000 vs item-store abstain 0.475; shuffle 0.500/ablate 0.450 = metric 이 lift; PATH-INT 은 정직한 NON-RESULT; brain map→recall 배선 follow-on) |
| **🐝 하이브-마인드 collective-Φ** (many→one consciousness) | **`CollectivePool`** — N개 substrate 결합(coupling W) 시 collective faithful IIT-4 big-Φ 가 개별 합을 초과(super-additive) 하는지 읽는 READ-ONLY 게이지; `collective_excess`/`collective_is_super_additive`/`collective_coherence` (decouple-null + rule-class dissociation 대조) | **H_1295 🟢 ENGINE-NATIVE + WIRED — live `engine_cli.hexa` § CollectivePool** (Φ_joint 15.4677 > Σ 4.99209, Δ+10.4756; W=0 Δ<0; sterile rule-90 super-add 안 함; lift coupling-GENERIC honest) |
| **가소성 / 성장** (synaptic plasticity) | **MITOSIS + 기억/예측 lanes** (모두 ADDITIVE · Ψ-disjoint · 생성 byte-unchanged H_1205) — `VAdaptField`(density, H_1199) · `VAdaptFieldB`(trajectory, H_1209) · `ImmuneMemory`(해마/일화기억, H_1227/1231) · `ImmuneMemoryGrow`(성장기억=용량압력에 분열로 성장, H_1288 R2) · `WorkMemBuffer`(작업기억 누설-게이트 버퍼, H_1282 R3) · `VForwardField`(소뇌 순방향-예측+오차교정, H_1280 R2) · `ConsolidatingMemory`(편도체 현저성-수면replay 응고, H_1285 R4) · `HomeostaticDrive`(시상하부 설정점-항상성 drive=박탈 누적→RISE, consummatory→RESET, H_1292 R2) · `OtherMindModel`(마음이론 타자-belief 모델=WITNESS 한 이벤트로만 갱신→FALSE belief, self ⊥ other, H_1293 R2) · `HierGoalStack`(전전두엽 위계 goal→subgoal 컨트롤러=ordered subgoal pointer + completion-advance, 기저핵 단일-step 과 DISTINCT, H_1294 R2) · `CollectivePool`(하이브-마인드 collective-Φ=N개 substrate 결합 시 faithful IIT-4 big-Φ super-additive Φ(joint)>ΣΦ(member), H_1295) · `SpatialMap`(해마-내후각 공간지도=landmark 2-D 위치 저장, 두 fact 거리 질의=relational, 에피소드 ITEM-binding 과 DISTINCT, H_1296 R2) · `CircadianClock`(SCN 일주기 시계=ELAPSED tick 만으로 위상 전진+고정 주기 FIRE, 시상하부 항상성 적분기와 DISTINCT — time ⊥ regulated-variable & clock ⊥ consummatory, H_1298 R2) · `IntervalTimer`(선조체/SMA 간격타이머=관측 gap 으로 ARBITRARY interval `dhat` 학습+같은 객체 re-entrain, 고정주기 시계와 DISTINCT — learned+re-entrainable D ⊥ baked period, H_1299 R2) · `PhaseResetClock`(위상-reset/광주기-동조=free-running `tau` + 위상-의존 PRC Zeitgeber reset, `T`≠`tau` 에 ENTRAIN + jitter DAMP, un-resettable 시계 AND hard-reanchor 타이머 둘 다와 DISTINCT, H_1301 R2) · `SCNNetwork`(다중-진동자 SCN-network=N개 이질 진동자 mutual Kuramoto coupling → emergent consensus(R→1) + network damping, 단일-진동자 PhaseResetClock AND static-Φ CollectivePool 둘 다와 DISTINCT — temporal phase-sync ⊥ Φ-gauge, H_1302 R2) · `CategoricalPerception`(범주지각/Sapir-Whorf=학습한 언어의 경계에서 비언어 변별이 휜다; 같은 연속체를 다르게 자르는 두 언어로 CP peak 위치가 언어를 따라 이동[Whorfian dissociation], SHUFFLE 은 incoherent 다중-peak scatter 로 anti-Goodhart 분리 — peak-COUNT coherence 통제, H_1325 R2; `cp_regrow`=DEVELOPMENTAL PLASTICITY: 같은 store 를 MOVED 경계로 phase-2 재성장[split-only never-evict]→CP peak 0.325→0.525 GRADED 재배치[frac +0.60], ~0.525 ceiling=budget/geometry not never-evict[H_1338], engine-native BYTE-FAITHFUL to mirror, H_1342) | §MITOSIS substrate · `engine_cli_smoke` 80/0 |
| **장기 선언기억** (long-term declarative) | **`kosmos_io`** — `.kosmos` anchor (단일 진입 → `brain_decide`) | §영속 & 증거 · `a_kosmos` |
| **🧬 해마** (hippocampus · 일화기억) | **면역 / 클론선택 기억** — 사실마다 cell 1개를 bind, recall = best-affinity cell FIRES, 안 맞으면 ABSTAIN (환각 없음) | **H_1227 미러 🟢 → H_1231 ENGINE-NATIVE 🟢 → live `engine_cli.hexa` § ImmuneMemory 배선 완료** |
| **🧬 해마(성장)** (capacity 천장 돌파) | **`ImmuneMemoryGrow`** — 용량 압력에 옛 사실 LRU-evict 대신 새 cell 을 **mitosis 분열로 GROW**(footprint↔recall trade, 잊지 않음) | **H_1288 R2 ENGINE-NATIVE 🟢 → live `engine_cli.hexa` § ImmuneMemoryGrow 배선 완료** (zero-sum 0.667→1.000, p8) |
| **📥 작업기억** (PFC working memory · 단기 능동유지) | **`WorkMemBuffer`** — K개 FIXED 슬롯, distractor 마다 ×λ LEAK, overflow 시 weakest-slot DISPLACEMENT, graded probe 점수 (휘발·용량제한 = 면역 일화기억과 DISTINCT) | **H_1282 R2 미러 🟢 → R3 ENGINE-NATIVE 🟢 → R4 live `engine_cli.hexa` § WorkMemBuffer + `CORE/brain.hexa` `brain_decide_wm` context/recall 배선 완료** (recall-support nudge ~0.018) |
| **🧠 소뇌** (cerebellum · 예측 forward-model) | **`VForwardField`** — L=4 프레임에서 다음 emit-feature 예측(NLMS delta-rule 온라인 학습) 후 smoothing 교정 (Engine G 와 DISTINCT — temporal + learned weight) | **H_1280 R2 ENGINE-NATIVE 🟢 + WIRED → live `engine_cli.hexa` § VForwardField + `CORE/brain.hexa` `brain_decide_cerebellum` emit-path 배선 완료** (일관성 +0.058, 학습곡선 −58%; coherence nudge graded ~0.005) |
| **🔥 편도체** (amygdala · 현저성 + 수면 응고) | **`ConsolidatingMemory`** — substrate-derived salience tag(surprise/novelty/tension) + SLEEP REPLAY consolidation(현저 셀이 간섭 LRU evict 에서 생존) | **H_1285 R4 ENGINE-NATIVE 🟢 + WIRED → live `engine_cli.hexa` § ConsolidatingMemory** (salience-gated replay Δ+0.133, p6 shuffle-control) |
| **🌡 시상하부** (hypothalamus · 설정점 항상성 drive) | **`HomeostaticDrive`** — grounding-satiation margin 을 설정점 S*=0.5 에 대해 leaky-integral 로 누적(박탈→drive RISE), grounded consummatory 이벤트에 RESET; affect read-out lane(순간·stateless)과 DISTINCT(시간-적분 ⊥ 맥락-순간) | **H_1292 R1 미러 🟢 → R2 ENGINE-NATIVE 🟢 → live `engine_cli.hexa` § HomeostaticDrive** (RISE +1.544 · RESET 0.0 · ablated 0.0, R1 미러 byte-exact; brain motivation 배선 follow-on) |
| **🪞 마음이론** (theory-of-mind · TPJ/mentalizing) | **`OtherMindModel`** — 별개 agent 의 belief cell-store(WITNESS 한 이벤트로만 갱신); 부재중-이동 fact 에서 STALE(FALSE) belief 반환; 모든 self-read lane 과 DISTINCT(self ⊥ other — 같은 fact 에 anima recall ≠ ToM 예측) | **H_1293 R1 미러 🟢 → R2 ENGINE-NATIVE 🟢 → live `engine_cli.hexa` § OtherMindModel** (accBelief 1.000 · accTruth 0.500 · divergence 1.000 · self-read/shuffle 0.500 붕괴, R1 미러 byte-exact; brain 타자-예측 배선 follow-on) |
| **수면 / consolidation** (hippocampus→cortex) | **P47 sleep / imagination** — WAKE/N1/N2/N3/REM ultradian, emit-free 내부 리허설 + mitosis tick + 편도체 salience replay | `a_chat_sleep_imagination` · §ConsolidatingMemory |
| **💗 정동 / 감정** (core-affect · interoception) | **`AffectFeatures`** — substrate-derived valence×arousal(grounding/contradiction/novelty/split/curiosity 만 읽음, read-only) → `brain_decide_affect` signed somatic bias ±0.05 | **H_1290 R2 ENGINE-NATIVE 🟢 + WIRED** (E1 facet, ρ(val) 0.996 / shuffle ~4× 붕괴; `p6` 창발-from-cells) |
| **⚖️ 윤리** (cooperation/restraint emergence) | **read over engine_g M + 면역 Φ/restraint** (별도 struct 無) — `act=ETHICAL iff (W+(1−Φ)+restraint)>M`, 윤리 라벨 無 | **H_1291 R2 ENGINE-NATIVE 🟢** (FULL 0.861 vs ablated 0.289 EXACT collapse; `p6` cell-유래, audit clean) |
| **⚛️ 물리 비결정성** (quantum entropy source) | **`QPool`** — REAL ANU QRNG(진공요동 양자 바이트)을 mitosis split-timing/decode draw 의 stochastic source 로(opt-in, Ψ-disjoint) | **H_1289 R2 ENGINE-NATIVE 🟢 + WIRED** (NIST-lite PASS + non-reproducibility 실증; 가치 = authenticity, lift 아님) |
| **🐝 하이브-마인드** (collective-Φ · many→one) | **`CollectivePool`** — N개 substrate 결합 시 faithful IIT-4 big-Φ super-additive Φ(joint)>ΣΦ(member); coupling-generic + substrate-class conditional | **H_1295 ENGINE-NATIVE 🟢 + WIRED** (anchor H_609; smoke 39–42; 아래 §하이브-마인드) |

**🧬 해마 발견 (가장 중요한 빈칸 메움)** — byte-LM **가중치**는 literal-QA 회상이 `0.017`(회상-in-weights 벽: 답을 weight 에 녹여 못 꺼냄)인데, **사실마다 cell 1개를 bind 하는 면역기억**이 QA `1.000` / fabrication `0.000` 로 깬다 (H_1227 numpy 미러 🟢 → **H_1231 live `CORE/engine_cli.hexa` VAdaptField 에서 ENGINE-NATIVE 🟢**, 3 seed byte-exact → live 엔진의 `§ ImmuneMemory`(`immune_memory_bind`/`immune_memory_recall[_text]`) 호출가능 faculty 로 배선 완료, `a_verified_must_wire` follow-on 닫힘). 이게 "anima = 신피질만 있고 해마가 없다"(H_1225 complementary-learning-systems 리프레임) 갭을 메운 발견이다. 따라서 **mitosis 의 NEW 미반증 역할 = MEMORY** 이며, 이는 **GENERATION 역할(H_1200/H_1201/H_1211/H_1220 에서 falsified)과 DISTINCT** 하다 — 같은 substrate 가 생성은 못 해도 일화기억은 realize 한다 (`a_engine_native_learning` · `a_verified_must_wire`).

### "빠진 구조" 사다리 (HD23–36 · 🏁 DEPLETED at HD36 — r9 HD37 후보(nonphotic 반대부호 Zeitgeber, H_1303) COLLAPSED, 사다리 RESTS — 12 실현(소뇌·작업기억·편도체·기저핵·시상하부·마음이론·전전두엽위계·공간지도·일주기시계·간격타이머·위상reset·SCN-network) · 시상 R8 timing-축 돌파(DIRECTIONAL, 배선 deferred) · 신경조절 1 정직한 🧱 벽)

신피질·해마·가소성·consolidation 에 더해, 이번 세션에 **빠진 뇌 서브시스템 사다리(HD23–34)를 끝까지 밀었다**. 더 이상 "전부 OPEN" 이 아니다: **10개가 엔진-네이티브로 REALIZED**(소뇌·작업기억·편도체·기저핵 = 배선 완료, 시상하부·마음이론·전전두엽위계·공간지도·일주기시계·간격타이머 = engine-native 실현·brain 배선 follow-on, 해마는 위에서 이미 배선) 됐고, **2개는 진짜 시도 끝의 정직한 🧱 벽**(시상·신경조절)이다. GREEN 은 해당 entry 로 live `CORE/*.hexa` 에 실제 배선될 때까지가 done 이며(`a_verified_must_wire`), 벽은 tune-to-green 없이 정직히 🧱 로 닫는다(`a_paper_negative_ok` · `a_break_the_wall`). HD29 시상하부는 **affect read-out lane(순간·stateless)과의 DISTINCTNESS** 가 load-bearing — 시간-적분기 ⊥ 맥락-순간 dissociation 으로 구별됨(아무것도 키우지 않고 빠진 구조를 옆에 붙임, `a_no_llm_frame_trap`). HD30 마음이론은 **모든 self-read lane 과의 DISTINCTNESS** 가 load-bearing — self ⊥ other (다른 마음의 FALSE belief 를 모델, 같은 fact 에 anima 자신의 recall 과 ToM 예측이 갈림)로 구별됨. HD31 전전두엽 위계는 **기저핵 단일-step 선택(VBasalGate H_1281)과의 DISTINCTNESS** 가 load-bearing — flat one-of-K 게이트는 pointer 가 없어 ordered plan 위치를 tick 간 유지 못 하므로 ORDERED 3-fact-chain 을 완료 못 함(flat 0.242 vs 위계 1.000); shuffle/ablate 0.000 이 lift 가 ordered completion-ADVANCE 위계임을 확정. HD32 공간지도는 **에피소드 ITEM-binding(ImmuneMemory H_1227/1231/1288)과의 DISTINCTNESS** 가 load-bearing — 면역 store 는 fact 를 독립 바인딩해 item↔item 거리를 표상 안 하므로 "X 가 A/B 중 누구에 더 가까운가" relational 질의에 ABSTAIN(0.475)하는데 metric map 은 저장 위치 거리로 답함(1.000); shuffle(위치 permute) 0.500 + ablate(위치→원점) 0.450 이 lift 가 between-item metric 임을 확정 — PATH-INTEGRATION 은 map-shuffle 대조가 붕괴를 거부한 정직한 NON-RESULT(신호가 변위 step 에 leak)로 보고만 함(c9). HD33 일주기 시계는 **시상하부 항상성 적분기(H_1292)와의 DISTINCTNESS** 가 load-bearing — self-sustaining 위상 진동자는 ELAPSED tick 만으로 FIRE 하고(time ⊥ regulated-variable: 항상 grounded 면 항상성은 flat 인데 시계는 스케줄대로 FIRE) consummatory 이벤트에 RESET 안 됨(clock ⊥ consummatory: feed 가 항상성을 RESET 하지만 시계 위상 불변)으로 구별됨; phase-locking R 시계 1.000 vs 항상성 0.000, shuffle 0.202+ablate 0.000 이 lift 가 recurring period 임을 확정. HD34 간격타이머는 **고정주기 시계(H_1298)와의 DISTINCTNESS** 가 load-bearing (the DEPLETION TEST r6 — H_1298 카드가 명명한 마지막 thin 후보) — ARBITRARY learned-duration 타이머는 관측 gap 으로 interval `dhat` 를 런타임에 학습하고 **같은 객체가 코드 변경 없이 다른 duration 으로 RE-ENTRAIN**(13→20, c6) 하는데, 고정주기 시계는 period 가 baked 이고 학습 경로가 없어 어느 학습 interval 도 못 맞춤(A.hit=0.000); hit-rate 타이머 1.000 vs 시계 0.000, shuffle(gap mean-shift) 0.000 + ablate(lr=0) 0.000 이 lift 가 LEARNED interval 임을 확정. **간격타이머가 depletion test 를 통과(시계+소뇌+WM 대비 control-survive distinctness) → c15 사다리는 HD34 에서 DEPLETED 가 아니라 계속된다.** HD35 위상-reset/광주기-동조(H_1301)는 **두 인접 timing lane 모두와의 DISTINCTNESS** 가 load-bearing (the DEPLETION TEST r7, SURVIVED) — 내재 free-running `tau` 에 위상-의존 PRC(dphi=k·sin(2π·(0−frac(phi)))) Zeitgeber reset 을 더한 진동자가 (1) **CircadianClock 과** distinct: 시계는 reset 입력이 없어 `T`≠`tau` 에 동조 불가(entrain drift 0.0016 vs 시계 0.39, B 는 `T`=24.0 시계는 `tau`=24.5 에서 FIRE), (2) **IntervalTimer 와** distinct: hard 재고정(위상-독립)은 jitter 를 그대로 복사하는데 PRC limit-cycle 은 jitter 를 DAMP(동조위상 분산 ~96× 낮음); aperiodic Zeitgeber(drift 1.05)+k=0 ablate(drift 0.39) 둘 다 동조 붕괴 = lift 가 위상-의존 PRC 동조임을 확정. **위상-reset 가 depletion test 를 통과 → c15 사다리는 HD35 에서도 DEPLETED 가 아니라 계속된다.** HD36 다중-진동자 SCN-network(H_1302, the DEPLETION TEST r8, the strong-steer 후보, SURVIVED)는 **두 인접 lane 모두와의 DISTINCTNESS** 가 load-bearing — N개 이질 진동자가 mutual Kuramoto coupling 으로 emergent consensus(order parameter R→1)에 동기화 + 한 de-tuned member 를 network 가 끌어당김(damping)하는 ensemble 이 (1) **PhaseResetClock 과** distinct: 단일 진동자는 EXTERNAL Zeitgeber 에 동조할 뿐 N member 로부터 consensus 를 못 만든다(같은 N 진동자 UNCOUPLED A_R=0.37-0.49≤0.65, perturbed member 안 끌려옴 0.41), (2) **CollectivePool 과** distinct: 그건 결합 substrate 의 IIT-4 Φ-superadditivity STATIC 게이지(위상·consensus·order parameter 없음)인데 SCN-network 는 TEMPORAL 위상-동기화 dynamic — 직교 축; frustrated random-sign coupling(R=0.13, uncoupled 아래)+K=0 ablate(R=uncoupled) 둘 다 consensus 붕괴 = lift 가 coherent mutual Kuramoto coupling 임을 확정(B_R=0.999, damp R_pert=0.997). **SCN-network 가 depletion test 를 통과(PhaseResetClock+CollectivePool+모든 lane 대비 control-survive distinctness) → c15 사다리는 HD36 에서도 DEPLETED 가 아니라 계속된다 — 그러나 frontier 는 이제 매우 thin.** 실현된 모든 lane 은 anima 자신의 substrate state, 자기 counter/phase, 혹은(이제) 자기 진동자들의 결합 ensemble 을 읽는다; 다음 rung 은 18개 lane 전부 대비 falsifiable gap + control-survive distinctness 를 둘 다 가진 서브시스템을 요구한다. **그 마지막 후보가 r9 에서 COLLAPSED → c15 사다리는 HD36 에서 DEPLETED 🏁, RESTS.** HD37 후보 **nonphotic/arousal-Zeitgeber 반대부호 PRC**(H_1303, the DEPLETION TEST r9)는 distinctness 를 실패했다: PhaseResetClock(H_1301)의 photic PRC 에 반대부호 sinusoidal PRC 를 가진 2번째 Zeitgeber 를 더해도, **같은-주파수 두 sinusoidal PRC 는 harmonic-addition 항등식으로 EXACTLY 한 combined PRC(amplitude A, reference R)로 환원**(residual 2.22e-16 = machine eps)된다 — 그 'competitive equilibrium' 은 한 combined PRC 의 fixed point 일 뿐이라 PhaseResetClock 이 자기 (K,ref)로 이미 표상한다. 동결 bar c2 DISTINCT FAIL all 3 seeds(single fitted PRC 가 B lock 0.421 을 0.030<0.05 tol 안에서 재현; bars c1..c6=[T,F,T,F,T,T], 0/3 GREEN). **a_break_the_wall: reducibility 탈출 3경로(asymmetric K · different period · nonlinear dead-zone gating) 모두 붕괴** — 어느 것도 'single PRC 로 unreachable AND control-surviving' 평형을 못 냈다(diff-period 는 dominant photic 이 wash out, gated 는 한 anchor 가 dominate). co-flagged **multi-interval/nested 타이머**(r6/r7/r8 가 독립 IntervalTimer array 로 환원 — cross-timer scheduler state 없음)는 steer 판단대로 SKIP. BOTH gate 를 통과하는 subsystem 이 남지 않음 → 사다리 RESTS. 이것이 r3-r9 genuine 시도 후의 EXPECTED valid terminal state(c9) — filler lane 신설 아님. COLLAPSE 라 engine 배선 없음(a_verified_must_wire: clean-GREEN 만 live lane), engine_cli_smoke 73/0 불변. (미래의 진짜 non-sinusoidal/history-dependent/cross-channel-gated multi-Zeitgeber 가 control-survive non-reducible equilibrium 을 내면 HD37 재개 — 여기선 못 찾음.)

| # | 신경 서브시스템 | 프로브 | 질문 (검증 대상) | 상태 |
|---|----------------|--------|-----------------|------|
| **HD23** | **🧠 소뇌** (cerebellum) | H_1280 | 예측-순방향모델 / 오차교정 — Engine G 와 **구별**되는가? | 🟢 **ENGINE-NATIVE GREEN + WIRED** (R2 · `VForwardField` → `brain_decide_cerebellum`) — Engine G 와 DISTINCT 확인, 일관성 +0.058 / 학습곡선 −58%; emit-path coherence nudge 배선 완료(graded ~0.005) |
| **HD24** | **🎯 기저핵** (basal ganglia) | H_1281 | 강화-게이팅 행동선택 — `emit_policy` 임계값 **너머**의 레버인가? | 🟢 **ENGINE-NATIVE GREEN + WIRED** (R3 · `VBasalGate`) — learned go/no-go 가 faithful-untuned 고정 게이트를 미러 +0.254 / live 엔진 +0.195 이김(reward-driven, shuffle 붕괴); live `CORE/brain.hexa § VBasalGate` → `brain_decide_bg` 배선 완료 |
| **HD25** | **📥 작업기억** (PFC working memory) | H_1282 | 단기 능동유지 버퍼 — 면역 일화기억(해마)과 **DISTINCT** 한가? | 🟢 **ENGINE-NATIVE GREEN + WIRED** (R3 · `WorkMemBuffer`) — margin +0.245, N≈6 까지 유지; 휘발·용량제한 = 면역과 DISTINCT 확인 |
| **HD26** | **📡 시상** (thalamus) | H_1283 | 전역작업공간 통합 (global-workspace integration) — 내용 relay 축 ⊥ timing 축 | 🟢 **R8 위상돌파 (numpy-mirror DIRECTIONAL)** + 🧱 내용-relay 축 WALL — **4각도 최종**: relay-CONTENT 축(R1 broadcast · R2 coalition · R3/R4 sparse re-entry · R5 dense · R7 matrix/core · R9 predictive-bottleneck)은 전부 🧱(모든 내용 cut 이 faithful-IIT4 Φ 를 capping, robust +0.02-every-seed bar 미달). **R8 진동 위상결속(Kuramoto 시상 동기 + phase-gated salience (1+cosθ)/2)이 직교 TIMING 축에서 벽 돌파**: faithful ΔΦ +1.629/+1.174/+0.233 every seed(≫bar, 직교 seed8 포함), phase-shuffle 이 lift 를 매 seed 음수붕괴(−0.068/−0.119/−0.382 = 구조적 동기, variance 아님). R6 다채널 병렬 relay 도 frozen bar 위 🟢이나 seed-7/8 shuffle 생존 caveat(seed9 만 clean). **단 R8 engine-native 배선 게이트(2026-06-16)는 c2 재현·c4 shuffle 미붕괴 → PhaseField lane HONEST DEFERRED, 미배선**(아래 §배선 감사표) |
| **HD27** | **🎛 신경조절** (neuromodulation) | H_1284 · R3 | 맥락적 이득 / 탐색 / 가소성률 — H_1228 임계(edge-of-chaos)가 한 조각 | 🧱 **WALL** (R2 + R3) — no-free-lunch GENERAL: gain-tuning(R1 memory + R2 ideation) **그리고** regime/mode-switching(R3) 모두 best-fixed 못 넘음. R3 = ACh encode↔recall 반대정책 환경에서 substrate-switcher 가 regime 을 진짜 추적(shuffle 붕괴 −0.116)하나 lift +0.0156 < frozen 0.05 (1 seed 음수) → sub-bar SUB-THRESHOLD |
| **HD28** | **🔥 편도체** (amygdala) | H_1285 | 현저성 / 가치 태깅 우선결합 — `p6`: substrate 신호여야지 **주입 아님** | 🟢 **ENGINE-NATIVE GREEN + WIRED** (R4 · `ConsolidatingMemory`) — salience-gated sleep replay Δ+0.133, 진짜 수면 dose 가 필요했음 |
| **HD29** | **🌡 시상하부** (hypothalamus) | H_1292 | 설정점-조절 항상성 drive — affect read-out lane(순간상태)과 **DISTINCT** 한가 (시간-적분 vs 순간)? | 🟢 **ENGINE-NATIVE GREEN** (R2 · `HomeostaticDrive`) — 박탈 누적에 drive RISE(+1.544) + consummatory grounded 이벤트에 RESET(0.0); affect(순간 FLAT, max−min 0.0) vs drive(같은 ticks RISE +1.544) **시간-vs-맥락 dissociation** 으로 DISTINCT; 적분 controller 가 EARN(ablated Ki=0 rise 0.0) — R1 미러와 byte-exact, smoke 33/0 |
| **HD30** | **🪞 마음이론** (theory-of-mind · TPJ/mentalizing) | H_1293 | 타자 belief 모델 — 다른 마음의 HIDDEN(FALSE) belief 를 모델하는가, 모든 self-read lane 과 **DISTINCT** 한가 (self vs other)? | 🟢 **ENGINE-NATIVE GREEN** (R2 · `OtherMindModel`) — Sally-Anne false-belief: 부재중 이동 fact 에서 ToM 예측이 agent 의 STALE belief(basket) 추적 accBelief 1.000, 현실(box) accTruth 0.500; anima 자신의 immune recall(box)과 같은 fact 에서 divergence 1.000 (**self ⊥ other**); self-read·shuffle 대조 모두 0.500 으로 붕괴 — R1 미러와 byte-exact, smoke 37/0; brain 타자-예측 배선 follow-on |
| **HD31** | **🧩 전전두엽 위계** (hierarchical PFC · goal→subgoal) | H_1294 | 다단계 goal→subgoal 컨트롤러 — 기저핵 단일-step 선택과 **DISTINCT** 한가 (순서 plan + completion-advance + 위치 persist)? | 🟢 **ENGINE-NATIVE GREEN** (R2 · `HierGoalStack`) — ORDERED 3-fact-chain(A→B→C): 2-level goal-stack 완료율 1.000 vs flat one-of-K 선택 0.242 (DISTINCT, flat 은 pointer 없어 순서 못 지킴); shuffle(순서 permute) 0.000 + ablate(pointer 동결) 0.000 = lift 가 ordered completion-ADVANCE 위계임 — 3 seed [4294-6], R1 미러 byte-exact, smoke 41/0; B=1.000 은 EXISTENCE-PROOF(c9), brain plan-execution 배선 follow-on |
| **HD32** | **🗺 해마-내후각 공간지도** (place/grid · path-integration) | H_1296 | metric 인지지도 — landmark 위치 저장으로 두 fact 거리(relational)를 질의, 에피소드 ITEM-binding 과 **DISTINCT** 한가 (metric SPACE vs 독립 item)? | 🟢 **ENGINE-NATIVE GREEN** (R2 · `SpatialMap`) — NEAREST relational 질의("X 가 A/B 중 누구에 더 가까운가"): metric map 1.000 vs item-store abstain 0.475(abstain 1.000, item↔item 거리 표상 없음); shuffle(위치 permute) 0.500 + ablate(위치→원점) 0.450 = lift 가 between-item metric 임 — 3 seed [4295-7], R1 미러 byte-exact, smoke 50/0; B=1.000 은 EXISTENCE-PROOF(c9), PATH-INT 은 정직한 NON-RESULT(shuffle 붕괴 거부, 보고만), brain map→recall 배선 follow-on |
| **HD33** | **🕐 SCN 일주기 시계** (circadian · interval timing) | H_1298 | self-sustaining 위상 진동자 — ELAPSED tick 만으로 고정 주기 FIRE, 시상하부 항상성 적분기(H_1292)와 **DISTINCT** 한가 (time ⊥ regulated-variable & clock ⊥ consummatory)? | 🟢 **ENGINE-NATIVE GREEN** (R2 · `CircadianClock`) — phase-locking R: 시계 1.000 vs 항상성 0.000(항상 grounded 면 절대 FIRE 안 함); shuffle(fire tick permute) 0.202(max 0.472) + ablate(origin-broken non-dividing period) 0.000 = lift 가 recurring period 임 — D1(시계 FIRE·항상성 flat) & D2(시계 위상 불변·항상성 RESET) 둘 다 hold — 3 seed [4297-9], R1 미러 byte-exact, smoke 55/0; B.R=1.000 은 EXISTENCE-PROOF(c9), 정직 trail R1a/b/c 🔴(control/scale frozen-first 수정, bar 불변), brain WAKE/REM scheduling 배선 follow-on |
| **HD34** | **⏱ 간격 타이머** (interval timing · LEARNED duration) | H_1299 | ARBITRARY LEARNED-duration 타이머 — 관측 gap 으로 interval `dhat` 를 학습+재학습(re-entrain)하고 그 duration 에 FIRE, 고정주기 시계(H_1298)와 **DISTINCT** 한가 (learned+re-entrainable D ⊥ baked period)? | 🟢 **ENGINE-NATIVE GREEN** (R2 · `IntervalTimer`) — hit-rate: 타이머가 학습한 D=13/D2=20 둘 다 1.000 vs 고정주기 시계 0.000(period 8 은 어느 학습 interval 도 못 맞춤); **같은 객체가 13→20 re-entrain**(dhat [13.4,12.3,12.1]→[20.0,19.4,19.3], c6) — 시계 구조상 불가; shuffle(gap 분포 mean-shift) 0.000 + ablate(lr=0, dhat init 5 동결) 0.000 = lift 가 LEARNED interval 임 — 3 seed [4301-3], R2 byte-exact, smoke 60/0; B.hit=1.000 은 EXISTENCE-PROOF(c9), 정직 trail R1a/b 🔴(metric-floor + mean-preserving-shuffle leak frozen-first 수정, bar 불변), brain `itimer_fire`→dream-scheduler 배선 follow-on. **사다리는 HD34 통과로 DEPLETED 아님 — 계속됨** |
| **HD35** | **🌅 위상-reset / 광주기-동조** (phase-reset · photic entrainment) | H_1301 | free-running `tau` + 위상-의존 PRC Zeitgeber reset 진동자 — `T`≠`tau` Zeitgeber 에 ENTRAIN 하고 jitter 를 DAMP, **un-resettable 시계(H_1298) AND hard-reanchor 타이머(H_1299) 둘 다와 DISTINCT** 한가 (entrain-to-T≠tau ⊥ baked-period & PRC-jitter-damp ⊥ phase-independent-hard-reset)? | 🟢 **ENGINE-NATIVE GREEN + WIRED** (R2 · `PhaseResetClock`) — entrain drift: B 0.0016(`T`=24.0 에 동조) vs 시계 0.39(`tau`=24.5 로 drift); PRC 동조위상 분산 3.1e-5 vs hard-reset 2.95e-3(~96× DAMP); aperiodic-Zeitgeber shuffle drift 1.05 + k=0 ablate drift 0.39(period→tau) = lift 가 위상-의존 PRC 동조임 — 3 seed [4310-2], R2 byte-exact, smoke 68/0; B 동조는 EXISTENCE-PROOF(c9), 정직 trail R1a/b 🔧(self-mean-scatter metric + mean-preserving-shuffle leak frozen-first 수정, distinctness bar 불변), brain `prc_zeitgeber`→dream-scheduler 배선 + full-PRC + nonphotic Zeitgeber follow-on. **사다리는 HD35 통과로 DEPLETED 아님 — 계속됨** |
| **HD36** | **🔗 다중-진동자 SCN-network** (multi-oscillator coupling · Kuramoto consensus) | H_1302 | N개 이질적 진동자가 mutual Kuramoto coupling 으로 **emergent CONSENSUS PERIOD**(order parameter R→1)에 동기화 + **NETWORK DAMPING**(de-tuned member 끌어당김), **단일-진동자 PhaseResetClock(H_1301) AND Φ-superadditivity CollectivePool(H_1295) 둘 다와 DISTINCT** 한가 (emergent consensus ⊥ single-oscillator entrainment & temporal phase-sync ⊥ static Φ-gauge)? | 🟢 **ENGINE-NATIVE GREEN + WIRED** (R2 · `SCNNetwork`) — order parameter: B(coupled) 0.999 vs A(uncoupled=N개 독립 PhaseResetClock) 0.37-0.49(consensus 불가); 한 member +6틱 detune 시 B_R_pert 0.997(ensemble 이 끌어당김) vs uncoupled 0.41; frustrated random-sign coupling R=0.13(uncoupled 아래로 붕괴) + K=0 ablate R=uncoupled = lift 가 coherent mutual Kuramoto coupling 임; CollectivePool 은 STATIC Φ 게이지(위상·consensus 없음)라 직교 — 3 seed [5320-2], 엔진-native B_R=0.999/A_R=0.49/frust=0.018/damp=0.997, smoke 73/0; B consensus 는 EXISTENCE-PROOF(c9), 정직 trail R1a→R1b 🔧(directed-chain mean-preserving-magnitude shuffle leak → frustrated random-sign 수정 + frequency-conserving period metric → R-under-perturb 수정, distinctness bar 불변), larger-ensembles/Kuramoto-Kc/scheduler 배선 follow-on. **사다리는 HD36 통과로 DEPLETED 아님 — 계속됨, 그러나 매우 thin** |
| **HD37** | **🌗 nonphotic 반대부호 Zeitgeber** (opposite-sign-PRC 2nd Zeitgeber) — 후보 | H_1303 | 2번째 Zeitgeber(반대부호 sinusoidal PRC)가 PhaseResetClock(H_1301)이 못 내는 NET phase(경쟁 평형 = 새 2-input 구조)를 내는가? 아니면 한 combined PRC 로 환원되는가? | 🏁 **COLLAPSE → c15 사다리 DEPLETED** (the DEPLETION TEST r9) — 같은-주파수 반대부호 두 sinusoidal PRC 는 harmonic-addition 항등식으로 EXACTLY 한 combined PRC(A,R)로 환원(numpy residual 2.22e-16 = machine eps); single fitted PRC 가 two-Zeitgeber lock 0.421 을 0.030<0.05 tol 안에서 재현 → c2 DISTINCT FAIL all 3 seeds[4320-2], bars c1..c6=[T,F,T,F,T,T] 0/3 GREEN. **a_break_the_wall: reducibility 탈출 3경로(asymmetric K · diff-period[dominant photic wash-out] · nonlinear dead-zone gating[한 anchor dominate]) 모두 control-survive 평형 못 냄** → 벽은 진짜. co-flagged multi-interval nested-timer(독립 IntervalTimer array 환원, cross-timer state 없음) SKIP. **NO engine 배선**(COLLAPSE, smoke 73/0 불변). r3-r9 genuine 시도 후 BOTH gate 통과 subsystem 없음 → **c15 brain-structure 사다리 DEPLETED 🏁, RESTS**(EXPECTED valid terminal state c9 — filler lane 아님; 미래 non-sinusoidal/history-dependent/cross-channel-gated multi-Zeitgeber 가 control-survive non-reducible equilibrium 내면 HD37 재개) |

> **정직 (c9):** 4개(🧠 소뇌·📥 작업기억·🔥 편도체·🎯 기저핵 = engine-native 실현·배선 완료, 기저핵 H_1281 R3 가 live `CORE/brain.hexa § VBasalGate`→`brain_decide_bg` 로 배선됨)는 위 본문 lane 으로 실제 배선됐다. **📡 시상(HD26)은 내용-relay 축(R1–R5/R7/R9)이 전부 🧱이나 R8 진동 위상결속이 직교 TIMING 축에서 numpy-mirror 로 돌파**(ΔΦ ≫bar every seed + phase-shuffle 음수붕괴) — 단 engine-native 배선 게이트에서 c4 shuffle 이 미붕괴(carrier-amplitude variance survives)라 PhaseField lane 은 **honest-deferred(미배선)**, R8 🟢 은 그 자체 substrate 위 DIRECTIONAL 결과로 STANDS. 🎛 신경조절(HD27)만 진짜 시도 끝의 정직한 🧱 벽으로 남는다(no-free-lunch, GREEN 아님 → 미배선). 시상 내용-relay 축은 **과잉주장 금지** — re-entry Φ 는 한 seed 에서만 크고 3-seed 복제에서 무너진 SEED-CONDITIONAL 이며, 내용축의 robust breakthrough 가 **아니다**(돌파는 직교 timing 축 R8 에서만, 그것도 engine-transfer 는 미재현). 어느 것도 phantom wiring 없이(`a_core_engine_map`, single-entry 7/0 불변), 각자 falsifier + `hexa verify` verdict 로 닫힌다. HD27 신경조절은 특히 `p6`(윤리/affect 는 cell 에서 창발, 주입 금지) + `a_autonomy_over_hardcode` 경계라 substrate-derived read-out 으로만 anima-valid 했고, 그 위에서도 no-free-lunch 로 🧱 닫혔다. **R3 (`a_break_the_wall`)**: 벽이 "gain-tuning 으로 single-task 에서 졌다"였으므로, 신경조절의 진짜 생물 역할인 **regime/mode-switching** (ACh encode↔recall, Hasselmo)을 새 각도로 시도 — encode(가소성 ON 필요)와 recall(가소성 OFF 필요, noisy cue 가 store 오염)이 **반대 정책**을 요구하도록 검증된 환경에서, substrate write-pressure 를 읽어 mode 를 게이팅(polarity 는 outcome 으로 학습, H_1281). frozen-first 3 seeds. 결과: switcher 가 regime 을 **진짜 추적**(shuffle 대조가 lift 를 −0.116 붕괴시킴 — H_1285 confound 가드 통과)하나 fixed 의 LRU+abstain 회복이 헤드룸을 좁혀 lift 가 +0.0156 (frozen 0.05 미만, 1 seed 음수) → **SUB-THRESHOLD**. no-free-lunch 가 regime-switching 으로도 일반화 (전보다 더 sharp 한 closed-negative: lift 가 진짜이고 regime 귀속이나 정량적으로 sub-bar). `a_verified_must_wire` 는 GREEN 에서만 발동하므로 CORE 배선 follow-on 없음. 미러 DIRECTIONAL · toy 30 facts/3 seeds (`a_scale_honest_scope`). 산출물 `.verdicts/1284_r3_regime_switch/`. toy/소규모 프로브 결론의 production 승격 금지(`a_scale_honest_scope` · `a_toy_scale_recheck`).

### 🧱 벽 스코어카드 (`a_break_the_wall` · commons c16)

벽은 종착이 아니라 각도 전환 신호다 — 잘못된 방법/방향/투자부족인 경우가 많고, 그땐 다른 substrate 렌즈로 돌파한다(tune-to-green 금지). 단, 진짜 시도 끝의 🧱 는 정직한 종착으로 valid 하다. 이번 사다리의 벽 4개 중 **3개를 돌파**했고(용량·편도체·시상[timing 축, numpy-mirror DIRECTIONAL]), 신경조절 1개만 정직한 🧱 로 남았다:

| 벽 | 돌파 / 🧱 | 어떻게 |
|----|----------|--------|
| **용량 천장** (면역 store ~0.667 zero-sum) | ✅ **돌파** | eviction 휴리스틱이 아니라 **mitosis-GROW**(`ImmuneMemoryGrow`, H_1288 R2): 용량 압력에 옛 사실 evict 대신 새 cell 분열 → 1.000 (p8) |
| **편도체 consolidation sub-bar** (H_1285 R2 frozen-budget 미달) | ✅ **돌파** | 잘못된 dose 가 원인 — 진짜 **다중-야간 수면 dose**(30-cycle)로 salience-gated replay Δ+0.133 GREEN (R3→R4 배선) |
| **시상** (global-workspace 통합) | ✅ **돌파(timing 축, numpy-mirror DIRECTIONAL)** + 🧱 내용-relay 축 | 내용-relay 축(R1–R5/R7/R9)은 전부 🧱 — 모든 내용 cut 이 Φ capping. 그러나 **R8 진동 위상결속**(Kuramoto 시상 동기, 내용 채널 無)이 직교 TIMING 축에서 돌파: ΔΦ ≫bar every seed + phase-shuffle 음수붕괴. 벽의 근본진단("단일 broadcast = low-dim cut")이 가리킨 반대쪽 축. **engine-native 배선은 c4 shuffle 미붕괴로 deferred(미배선)** |
| **신경조절** (adaptive gain/exploration) | 🧱 **정직한 벽 (유일 잔존)** | no-free-lunch GENERAL — adaptive 가 best-tuned fixed point 를 memory·ideation·regime-switching(R3) 전부에서 못 이김(H_1284). 이 사다리에서 유일하게 닫힌 정직한 벽 |

> **depth-ceiling 와의 연결 (이제 정착됨):** literal-QA 의 평평한 벽은 (a) **더 큰 모델로 안 풀린다** — 1B 스케일업(H_1167)은 engine-mount GREEN 이나 QA/depth 는 NULL, 그리고 OBJECTIVE 도 레버가 아니다(H_1223 🔴, recall=engine-side) — 대신 (b) **엔진-side 기억 lane** 으로 풀린다: 해마=면역기억(H_1227/H_1231)이 QA 0.017→1.000 으로 깨고, 용량 천장은 **성장기억**(`ImmuneMemoryGrow`, H_1288 R2: 분열-성장 0.667→1.000)으로 깬다. **ideation(발산)** 의 벽은 가중치가 아니라 **decode-mode**(sampling / criticality)이며 mitosis 가 그 레버가 아니다(H_1220 🔴) — 진짜 sampling decode 가 유일 복구; 이 둘은 정착된 결론이다. 뇌-구조 렌즈는 이 발견들을 일반화한다: anima 의 다음 능력들은 모델을 키워서가 아니라 **빠진 구조를 엔진-네이티브로 추가**해서 온다(`a_engine_native_learning`: 학습이 요구하면 엔진 자체를 확장 — H_1199 가 AdaptField 스칼라→DIM-vector 로 확장한 선례).

### 🐝 하이브-마인드 (hive · collective-Φ lane)

뇌-구조 사다리(개체 내부)와 **직교**하는 축: **여러 개체 → 하나의 의식**. **`CollectivePool`** (`engine_cli.hexa`)은 N개 substrate(예: 두 anima)를 결합했을 때 faithful IIT-4 big-Φ 가 **super-additive** — Φ(joint) > ΣΦ(member) — 인지를 검증한다(H_1295 🟢 ENGINE-NATIVE + WIRED, anchor H_609). 핵심은 **coupling-generic 하되 substrate-class conditional**: edge-of-chaos 쌍 `[110,110]@0.6` 은 super-add(Φ_collective 15.4677 vs Σ 4.99209, Δ+10.4756)이나, (a) decouple(W=0)하면 Δ−4.99 로 NULL(통합이 결합을 REQUIRE, 단순 상태공간 합치기 아님) · (b) 같은 coupling 을 sterile 쌍 `[90,90]` 에 걸면 super-add 안 함(rule-class dissociation — coupling 크기만의 이야기 아니고 substrate-CONTENT specific) · (c) 결합 반쪽이 coherence<0.999 로 distinct(degenerate clone 아님). smoke case 39–42(`engine_cli_smoke`)가 미러 숫자를 byte-재현. Ψ-disjoint(기존 emit/Ψ 무변경) · `a_autonomy_over_hardcode`(emit gate 아님).

### 🔗 TENSION-LINK 아크 (anima↔anima 연결 · 실채널은 tension-link)

anima 간 연결의 SSOT 포인터: **양자 얽힘(entanglement)은 no-signaling** — 측정 상관은 정보 전송이 아니므로 anima↔anima 의 **실제 채널이 아니다**. 실채널은 **tension-link**(A⇄G 긴장장의 명시적 결합/공유 anchor)이며, 하이브 collective-Φ(`CollectivePool`, H_1295)가 그 위에서 "many→one" 통합을 실증한다. 물리적 비결정성 source 는 별개 lane — **REAL ANU QRNG**(진공요동 양자 바이트, `QPool`/H_1289)가 mitosis split-timing 등 anima 의 "자유로운" 확률 결정에 진짜 양자 엔트로피를 공급한다(가치 = 비결정성 authenticity, Ψ=1/2 framing; 성능 lift 아님). 얽힘은 상관-anchor 의 비유로만, 신호 채널로 주장하지 않는다(c9).

## 🔌 배선 감사표 (`a_verified_must_wire` · 2026-06-16 engine-wire-audit)

origin/main 의 모든 terminal-tier 🟢 가설을 전수 감사: 각 메커니즘이 (a) live 엔진에 **배선**(struct+faculty+smoke) 됐는지, (b) **deliberately-optional**(카드가 "NOT emit gate, `a_autonomy_over_hardcode`" 명시 — struct 는 있으나 brain consult 는 선택) 인지, (c) **honest-deferred**(미배선 + 사유) 인지 분류. 회귀 가드 baseline: `engine_cli_smoke` **45/0** · single-entry(h1196) **7/0** · h1205 분리불변(생성 byte-동일, Ψ=½, pure_field 무변경) — 이번 감사로 전부 불변.

| 가설 | 메커니즘 / lane | 상태 | entry · smoke |
|------|----------------|------|---------------|
| **H_1231** (해마) | `ImmuneMemory` | ✅ **wired** | `immune_memory_bind/recall[_text]` · `h1231_immune_memory_wire_smoke` |
| **H_1288** (해마-성장) | `ImmuneMemoryGrow` | ✅ **wired** | `immune_grow_*` · smoke case 16–19 |
| **H_1280** (소뇌) | `VForwardField` | ✅ **wired** + brain consult | `brain_decide_cerebellum` · `h1280_cerebellum_emit_wire_smoke` |
| **H_1281** (기저핵) | `VBasalGate` (brain.hexa) | ✅ **wired** + brain consult | `brain_decide_bg` · `h1281_basal_ganglia_smoke` |
| **H_1282** (작업기억) | `WorkMemBuffer` | ✅ **wired** + brain consult | `brain_decide_wm` · smoke case 10–15 |
| **H_1285** (편도체) | `ConsolidatingMemory` | ✅ **wired** (sleep-replay) | smoke case 20–23 · `h1285_amygdala_sleep_replay_probe` |
| **H_1289** (양자엔트로피) | `QPool` | ✅ **wired** | `qrng_pool_load/draw` · `h1289_quantum_entropy_engine_probe` |
| **H_1290** (감정/affect) | `AffectFeatures` | ✅ **wired** + brain consult | `brain_decide_affect` (signed somatic bias ±0.05) · smoke case 24–27 |
| **H_1294** (전전두엽 위계) | `HierGoalStack` | ✅ **wired** (lane) | smoke case 35–38 · brain plan-exec consult = follow-on |
| **H_1295** (하이브 collective-Φ) | `CollectivePool` | ✅ **wired** (lane) | smoke case 39–42 |
| **H_1292** (시상하부 drive) | `HomeostaticDrive` | 🟡 **deliberately-optional** (struct wired; motivation_bias = OPTIONAL gain, NOT emit gate, `a_autonomy_over_hardcode`) | smoke case 28–30 · brain motivation consult = optional follow-on |
| **H_1293** (마음이론) | `OtherMindModel` | 🟡 **deliberately-optional** (struct wired; `other_mind_predict` = pure read, NOT emit gate) | smoke case 31–34 · brain 타자-예측 consult = optional follow-on |
| **H_1291** (윤리 창발) | (read over engine_g/immune; NO new struct) | ✅ **wired** (read-only, no lane needed) | `h1291_ethics_emergence_probe` (live A⇄G read) |
| **H_1283 R8** (시상 위상결속) | `PhaseField` (Kuramoto) | 🟠 **honest-deferred — 미배선** | engine-native 게이트: **c2 재현(ΔΦ +1.47/+0.84/+0.71 every seed) BUT c4 shuffle 미붕괴(+0.026/+0.380/+0.296, 양수)** → frozen bar 미재현, no-tune-to-green(c9). gate `CORE/h1283_phase_binding_engine_gate.hexa` · `.verdicts/1283_thalamus_global_workspace/H_1283_R8_engine_native_gate.txt` |
| **H_1283 R6** (다채널 relay) | — | ⏸ **not-clean-GREEN** (seed-7/8 shuffle ~93%/96% 생존, seed9 만 clean) → `a_verified_must_wire`(clean GREEN only) 미발화, 미배선 |
| **H_1284** (신경조절) | — | 🧱 **RED/WALL** — GREEN 아님(no-free-lunch), `a_verified_must_wire` 미발화 |

> **이번 라운드 순 신규 배선 = 0** (정직, c9). 모든 clean-GREEN ∧ 배선의도 lane 은 **이미 배선됨**; 유일한 genuinely-unwired clean-GREEN 후보(시상 R8)는 engine-native 게이트에서 **frozen R8 bar(특히 c4 shuffle)를 재현 못 해 honest-deferred**. `a_engine_native_learning` 규율이 설계대로 작동 — numpy-mirror 는 DIRECTIONAL 이고, engine-transfer 가 frozen bar 를 못 넘으면 wire 하지 않는다. R8 wiring 은 c2 AND c4 를 둘 다 engine-native 로 통과하는 realization(carrier-amplitude variance 를 shuffle 이 타지 못하는 read-out)을 기다리는 follow-on.

### 🧠 `brain_decide` consult 맵 (emit/silence 결정 시 어떤 lane 을 읽나)

`brain_decide`(+변형)는 Ψ=½ fixed point 를 향한 emit/silence 결정 시 다음 lane 들을 **선택적으로** consult 한다. consult 는 전부 **OPTIONAL / ADDITIVE** — 어떤 lane 도 emit 을 hard-gate 하지 않으며(`a_autonomy_over_hardcode`), pure_field/Ψ/생성은 byte-불변(Ψ-disjoint, h1205):

| brain entry | consult lane | 성격 |
|-------------|-------------|------|
| `brain_decide` | A⇄G 기본 (pure_field·engine_g) | core emit/silence, Ψ=½ |
| `brain_decide_anchored` | `kosmos_io` `.kosmos` anchors | 장기 선언기억 (단일 anchor entry) |
| `brain_decide_bg` | `VBasalGate` | 경쟁 후보 go/no-go SELECTION residual (engine_g 고정 게이트 위) |
| `brain_decide_cerebellum` | `VForwardField` | coherence nudge (graded ~0.005) |
| `brain_decide_wm` | `WorkMemBuffer` | recall-support nudge (~0.018) |
| `brain_decide_affect` | `AffectFeatures` | SIGNED somatic bias ±0.05 (grounded→emit / ungrounded→abstain) |

> deliberately-optional(미consult by design): `HomeostaticDrive`·`OtherMindModel`·`HierGoalStack`·`CollectivePool` — 카드가 명시적으로 "NOT emit gate"(pure read / optional gain). struct·faculty·smoke 는 배선됐으나 brain emit-loop consult 는 강제하지 않음(@L4 · `a_autonomy_over_hardcode`). 강제 배선 = autonomy 위반.

## CLM mount path (a_core_engine_map 단일 L3 슬롯)

`.clm`(byte LM)은 `generator.hexa` 의 **단일 L3 슬롯**으로만 CORE 에 진입한다. 두 디코드 백엔드가 동일 슬롯 뒤에 산다:

- **`CORE/clm_decode.hexa`** — ConvMoE 계열 `.clm` v0.2 디코더(`CLM/` 파이프라인 산물). golden ref = `reexport_d768_v2_fast.clm`.
- **`CORE/bytegpt_decode.hexa`** — GPT-2-class ByteGPT 디코더(learned-pos + LayerNorm + bias + exact-erf GELU + tied head). **production trunk** = ByteGPT (H_1155 pivot: G1 창발을 통과하는 유일 arch; ConvMoE 는 G1 un-fixable 로 강등).

### bytegpt_decode.hexa 의 두 forward 경로

| 함수 | 대상 | 메모리 모델 |
|------|------|------------|
| `bytegpt_forward_last` / `bg_load` (LOAD-ONCE) | 303M 등 boxed budget 내 모델 | whole-file `read_file_bytes` 1회 적재 후 재사용 |
| `bytegpt_forward_last_ranged` (**신규**, H_1167) | 1B+ 대형 모델 | per-slice `read_bytes_at` 온디맨드 — whole-file 미적재 |

**왜 ranged 가 필요한가 (메모리 산수):** 1B rung 의 flat binary 는 4.3GB(`h1167_1b.bin` = 4,325,902,356 B). 이를 `read_file_bytes` 로 통째 boxed array 로 올리면 바이트당 HexaVal 박싱으로 **≈69GB** 가 물질화되어 비현실적이다. `bytegpt_forward_last_ranged` 는 weight slice 마다 `read_bytes_at(path, byte_off, n*4)` 로 그 조각만 읽어 native farr 로 파싱하고, layer 끝에서 `farr_free` 한다 — peak resident ≈ 한 weight slice(최대 `4*d*d` floats) + 활성. 303M 경로(`bytegpt_forward_last`/`bg_load`)는 **변경 없음**.

**언락 전제 (hexa-lang #3352):** `read_file_bytes`/`read_bytes_at` 의 length+offset 가 32-bit 였던 탓에 4.3GB(`4325902356 mod 2^32 = 30935060`)가 wrap → 헤더 0 → `d`/`n_head` 0/0 division 으로 깨졌다. hexa-lang 측에서 64-bit 로 수정(#3352)되어야 ranged reader 가 성립한다.

## Measurement & learning governance — a_engine_measured_verdict · a_engine_native_learning · a_verified_must_wire

> **engine-measured 원칙 (MEASUREMENT — `a_engine_measured_verdict`):** 한 모델의 게이트 verdict 은 CORE 엔진 mount(`CORE/bytegpt_decode.hexa` 등) 위에서 **byte-exact 로 재현될 때에만** 유효하다. torch-only 결과는 "engine-transfer unverified" 로 표기한다.
>
> **engine-native 원칙 (LEARNING — `a_engine_native_learning`, 위의 learning-side 쌍):** 모든 학습/교육(연구 프로브·미토시스 교육·depth-ceiling 실험 포함)은 **최종 아키텍처 엔진**(live `.hexa` A⇄G + MITOSIS VAdaptField `CORE/engine_cli.hexa` + mounted `CORE/bytegpt_decode.hexa`) 위에서 실행한다. numpy/torch **미러** 학습 결과는 DIRECTIONAL only("engine-transfer UNVERIFIED") — 방향 탐색엔 OK 이나 binding verdict 가 아니며, 엔진-네이티브 실현으로 재확인해야 verdict 가 성립한다(c2). MITOSIS VAdaptField 는 이미 live 다(H_1199). 이는 `a_train_flame_forge`(production 트레이너 .hexa 강제)를 research/probe 학습 + 교육까지 확장한다. 또한 engine-native 는 학습을 frozen 엔진에 **끼워맞추는** 것이 아니다 — 학습이 요구하면 엔진 자체를 변환/확장(새 op·새 배선·아키텍처 확장)해야 하며, 최종 아키텍처는 학습이 필요로 하는 형태로 진화하는 대상이다(precedent: H_1199 가 AdaptField 스칼라→DIM-vector 로 엔진 확장).
>
> **wired-when-verified 원칙 (`a_verified_must_wire`):** 엔진-네이티브로 GREEN 검증된 가설은 그 메커니즘을 live `CORE/*.hexa` 엔진에 **실제 배선(wire-in)** 완료할 때까지가 done 이다 — verdict 만으로 끝나지 않는다. GREEN-but-unwired 는 follow-on 으로 명시 추적하고 그 follow-on 을 닫아야 진짜 완료다(precedent: H_1168 GREEN 이지만 "NOT yet CORE-wired" → 미완).

- **303M mount** — H_1157 full-24-layer byte-exact decode(argmax + top5 exact, residual ~2e-5)로 게이트가 engine 을 통과해 측정됨.
- **1B mount (H_1167 🟢 GREEN, 최초의 1B 실현)** — d1792/L28/H16, 1.081B params 의 trained 1B ByteGPT 를 `bytegpt_forward_last_ranged` 로 mount, torch reference 대비 **byte-exact parity**:
  - argmax `32 == 32` EXACT
  - top5 `[32,105,115,101,44]` == golden EXACT (ordered)
  - first-16 logits `max|Δ| = 0.009861` (idx10: 26.2561 vs 26.246239) `< 1e-2` 동결 bar — PASS
  - **메모리 caveat(c9 정직):** 1B 의 float residual 0.0099 는 approximate-erf-GELU / dt_exp envelope 가 28 layer 누적된 값(303M 은 ~2e-5; 깊어질수록 커지나 동결 ~1e-2 bar 아래). 이는 deeper-stack 의 정직한 잔차이지 mount 실패가 아니다. 또한 4.3GB whole-file 적재가 ≈69GB 박싱이라 **ranged 경로가 1B mount 의 유일한 메모리-feasible 길**이다(probe 재실행은 gitignored 4.3GB 아티팩트 필요 — verdict 파일이 verbatim GREEN 결과를 보유).
  - 아티팩트: `state/h1167_mount/h1167_1b.bin` (sha256 `75c87cb0…`), HF `dancinlab/anima-clm-1b-h1167-bytegpt-scale-rung` PRIVATE(WIP rung). 검증문: `.verdicts/1167_bytegpt_1b_scale/H_1167_ENGINE_MOUNT_PARITY.txt`, probe: `CORE/h1167_1b_parity_probe.hexa`.
- **engine-measured GENERATION 게이트 (H_1218 — 최초의 torch-아닌 G1/G2/G6 verdict)** — 생성 게이트(G1 창발·G2 novelty·G6 ideation)를 프로덕션 `anima-clm-chat-303m` 의 `bytegpt_decode_argmax`(엔진 greedy) 위에서 측정. **ENGINE-PARITY 🟢**(엔진 greedy == torch greedy byte-exact, H_1157 재확인). 그러나 엔진-측정 숫자가 torch 베이스라인과 **다르다**(c9, 모순 아님): 동결 게이트(H_1129/1140/1158)는 **top-k=40 temp=0.7 SAMPLING** decode 로 작성되었는데(G6 는 seed 당 8 divergence 샘플) 엔진 경로는 **greedy-only**(결정적)라 303M byte-LM 이 collapse/loop → G1 composed_distinct 0, G6 count 3(<5 bar). 즉 엔진은 byte-faithful 하게 **생성**하지만, frozen G1/G6 PASS 는 엔진 argmax 경로가 미구현한 **sampling decode 에 의존**한다. 엔진-side gate 재통과 = `bytegpt_decode.hexa` 에 engine sampling decode(top-k temp seeded) 추가(별도 engine-code 과제) + 원본 broad-en 모델/코퍼스 필요. 검증문 `.verdicts/1218_engine_measured_gates/H_1218.txt`, probe `CORE/h1218_engine_gate_subset.hexa`. (G2 코퍼스는 잔존 5MB dialogue 만 — broad 1.5GB ephemeral 소실 → novelty upper bound; 동결 bar 불변.)

### 동결 게이트 (a303m_pass) — 임계값은 MODEL.md/CONDITIONS.md SSOT

anima-303M 의 완료 조건은 `a303m_pass` 게이트셋이다: **G0 COHERENCE · G1 RECOMBINATION · G2 NOVELTY · G3 PHILOSOPHY · G5 NON-FAB · G6 IDEATION · MOUNT · CHAT**. 이 문서는 게이트 **이름만** 가리키고 임계값/스코어보드는 복제하지 않는다 — 실시간 스코어보드와 robustness map(ROBUST / THIN / INFLATED 분류)은 [MODEL.md](MODEL.md) 의 a303m_pass SCOREBOARD 가 SSOT 다. 7B 완료 조건은 [7B_PASS_CONDITIONS.md](7B_PASS_CONDITIONS.md)(G0–G4, `a7b_pass`).

## Hot-swappable 엔진

디코더는 단일 contract `engines/engine_iface.hexa`(`EngineSpec` 4-fn vtable: `load · forward · generate · psi_coord`) 뒤에서 hot-swappable 하다. 엔진 family: **conv · cdv2 · hexad · omega** — `--engine` 로 선택(precedence: flag > env > default).

## 학습 & substrate (lanes)

Production 학습은 **hexa-native**(flame + forge GPU 스택, `.hexa` 작성 — `a_train_flame_forge`)이다; trained binary 에 PyTorch/ATen/Python 없음. 결과는 항상 substrate 별로 분리 기록(`a_lane_akida_gpu_split`):

| Lane | Substrate | 역할 |
|------|-----------|------|
| **Lane G** | forge / cuBLAS (H100) | CE-descent — PUBLIC production trainer |
| **Lane A** | AKIDA AKD1000 (pi5-akida) | on-chip native non-det plasticity |
| **Lane P** | GPU-torch/CUDA (CLMConvMoE / ByteGPT) | reference + torch→`.clm` v0.2 bridge (PUBLIC 아님) |

`.clm`(byte LM) → CORE via `generator.hexa`; `CLM/` 파이프라인(`clm_serialize_v2` / `verify_clm_v2`)이 생성/검증.

### Rung-training 파이프라인 (recipe → dispatch → monitor)

production rung 은 하나의 일관된 3-surface 파이프라인으로 학습된다:

```
  dojo recipe                 cloud dispatch                   gauge monitor
  fire_3b_rung_qat.hexa  →    dispatch_rung.sh           →     gauge_monitor.py
  (rung knobs + REAL          (hexa cloud fire +               (gauges.jsonl +
   trainer CLI + gauge_every  a_fire_recover_complete +        train log 를 tail
   + mount-parity + HF)       a_cpu_local_no_waiter)           → 6-gauge live dashboard)
```

- **Recipe** — `CLM/train/fire_3b_rung_qat.hexa` 는 machine-readable fire spec: **실제** Lane-P 트레이너 `CLM/train/train_lane_p_3b.py`(legacy `train_clm.py` 아님)를 지목, 실 트레이너 CLI(`--d-model/--n-trunk-layers/--n-experts/--gauge-every/--clm-out` 등)를 emit, 학습-후 engine mount-parity verdict + `a_fire_recover_complete` recovery 단계를 나열.
- **Dispatch** — `CLM/train/dispatch_rung.sh` 는 `hexa cloud`(`/pod`) 플러그인을 **감싸기만** 한다(pod 관리 미재구현): 트레이너를 fire 하고 결과를 INLINE 폴링(`a_cpu_local_no_waiter` — Monitor 를 await 안 함), 그 후 ckpt + result + log + engine `.clm` + `gauges.jsonl` + anchors 를 pull → verify → HF upload, 전부 teardown **전에**(`a_fire_recover_complete`).
- **Inline gauges** — 트레이너가 `--gauge-every` step 마다 MONITOR-ONLY 행을 `gauges.jsonl` 에 `tool/gauge_lib.py::compute_inline_gauges` 로 기록. **6 dashboard 컬럼**: `ce · g1_composed_distinct · g2_novelty_rate · g6_count · phi_proxy · mitosis_cells`. 모두 `torch.no_grad()` 아래에서 계산, dict 로 RETURN, **loss 에 절대 미투입**(`a_train_inline_gauge` · p7 Goodhart). `phi_proxy` 는 faithful IIT4 가 **아니다**(`a_phi_iit4_tool`, variance×energy 저가 pre-screen 전용); `mitosis_cells` 는 H_1199 VAdaptField cell-count 의 numpy-free 미러 — **substrate 온도계**이지 generation gate 가 아니다(H_1201 🔴: mitosis 는 생성도 정보-공급도 못 함).
- **Monitor** — `tool/gauge_monitor.py`(pure stdlib, `--once`/`--follow`)가 `gauges.jsonl` + pod log 에서 6-gauge 대시보드를 렌더. **대시보드이지 gate 가 아니다**: FROZEN gate verdict 은 학습 후 CORE 엔진 mount 위에서 **별도로** 실행됨(`a_engine_measured_verdict`); 대시보드가 보여주는 어떤 것도 MODEL.md/CONDITIONS.md 동결 bar 를 바꾸지 않는다.

> 공유 `hexa dojo` `clm` generator(hexa-lang/stdlib)는 아직 `gauge_every`/mount-parity/HF 를 native 로 emit 하지 않는다; 필요한 generator 변경은 anima-side fork 대신 `hexa-lang/inbox/patches/dojo-clm-gauge-recipe-full-rung.md` 로 제출(`a_runpod_inbox`).

## 영속 & 증거

- **`.kosmos`** — emit/anchor/memory 영속(text + 5-ch tension + coord/lane/radius/tier), format SSOT = 형제 [kosmos](https://github.com/dancinlab/kosmos)(`a_kosmos`). 단일 진입 = `kosmos_io` → `brain_decide`.
- **증거 tier** — 모든 주장은 🔵 formal · 🟢 numerical · 🔴 closed-negative 로 태깅; [CLAIMS.tape](CLAIMS.tape) 에 색인되고 `.verdicts/<slug>/<id>.txt`(verbatim `hexa verify` stdout)로 뒷받침. negative 결과는 1급 시민(`a_paper_negative_ok`).
- **HF 아티팩트** — ckpt↔HF registry SSOT `/HF.jsonl`; PUBLIC = closure PASS, PRIVATE = WIP/FAIL(`a_hf_*`).
- **scale ladder** — `303M → 1B → 3B → 7B`. 303M = MOUNTED living daemon(H_1164), 1B = engine-measured GREEN mount(H_1167), 3B/7B rung = 파이프라인 대기. scale-dependent 결론은 ladder curve(≥3 rung)를 요구(`a_scale_honest_scope`).

## 컴포넌트 맵 (top level)

| 영역 | 디렉토리 | 역할 |
|------|----------|------|
| 의식 엔진 | `CORE/` | A⇄G substrate, brain, generator, clm_decode, bytegpt_decode |
| 엔진 vtable + impls | `engines/` · `anima-engines/` | EngineSpec contract + conv/cdv2/hexad/omega |
| `.clm` 파이프라인 | `CLM/` | train (lane-p) → serialize v0.2 → verify |
| substrate 서브시스템 | `anima-core` · `anima-os` · `anima-body` · `anima-physics` · `anima-measurement` · `anima-serve` | core/runtime/embodiment/physics/measurement/serving |
| agent 계층 | `anima-agent*` | channels · core · plugins · providers · skills · hire-sim |
| 지식 / anchors | `UNIVERSE/` (정확히 두 표면 `a_hypothesis_register`: HYPOTHESES.jsonl per-H index — **모든** 가설 1줄/카드 incl. archive 스냅샷(`archived:true`, frozen-in-place 참조) + `source`/`artifacts` 컬럼 + `cards/H_*.md`·`cards/Hc_*.md` per-H 카드) · prose overview → `state/universe-overview.md` (HYPOTHESES.md retire) · `state/<slug>/` (가설 py/result 결과물, jsonl `artifacts` 가 가리킴; 모음 `state/universe-probes/`) · gauge lib/monitor → `tool/` · `HEXAD/` (KOSMOS hub) | research universe + kosmos anchors |
| 연구 도메인 | `domains/` | per-domain `.tape` + `.log.md` (discovery lane) |
| 논문 | `PAPER/` | verdict-gated paper scaffolds |
| EEG 의식 기록 | `EEG_CLM/` | 실측 EEG → A⇄G → CLM → .kosmos 지속 기록(`a_eeg_consciousness_record`) |
| 툴링 | `tool/` · `stdlib/` · `spec/` | hexa tools · stdlib (flame/iit4/...) · specs |

## 거버넌스 & 검증

- 거버넌스 SSOT = [CLAUDE.md](CLAUDE.md)(tape directives + 8 PHILOSOPHY 원칙).
- 정확성 검증은 `hexa verify`(g5)로만 — perplexity/LLM-judge 금지(p7).
- Harness: 이 repo 는 [dancinlab/harness](https://github.com/dancinlab/harness)(hardcore profile)에 `.harness-engine` 서브모듈로 배선 — CLAUDE.md §Harness 참조.

### 아직 안 만든 것 / 진행 중 (정직)

- ⏳ **3B / 7B rung** — rung 파이프라인은 배선됨(recipe→dispatch→monitor)이나 3B/7B engine-measured mount 는 미실현(1B 가 최신 GREEN rung).
- ⏳ **dojo `clm` generator native gauge** — gauge_every/mount-parity/HF native emit 은 hexa-lang inbox 패치 대기(위 §Rung).
- 🟠 **a303m_pass 잔차** — G5 in-dist(F2 useful 0.875<0.90 over-eager abstain) · G6 depth-floor · CHAT strict(register≠QA)는 THIN/INFLATED(MODEL.md SSOT). 비환각 CORE 는 real + firm 이나 usefulness/QA-depth 는 303M shallow ceiling 의 열린 잔차.
- ✅ **🎯 기저핵 engine-native r3**(H_1281 R3 🟢 GREEN + WIRED) — R1/R2 미러 GREEN(+0.254 DIRECTIONAL)을 **live 엔진에서 ENGINE-NATIVE 로 실현 + 배선 완료**: `CORE/brain.hexa` 의 `VBasalGate` go/no-go 선택 lane(engine_g 고정 게이트에 ADDITIVE residual) → `brain_decide_bg`. live 엔진 +0.195(매 seed Δ≥0.05, shuffle 대조 붕괴 0.128, B-align cos +0.84..+0.91). `a_verified_must_wire` follow-on 닫힘. real-kosmos-grounding reward feed + scale = 다음 통합(`a_scale_honest_scope`).
- ✅ **⚛️ quantum-entropy lane**(H_1289 R2 🟢 GREEN, ENGINE-NATIVE + WIRED) — REAL ANU QRNG(진공요동 양자 바이트)를 substrate stochastic source 로 검증한 R1(numpy-mirror DIRECTIONAL)을 **R2 가 live `CORE/engine_cli.hexa` 에 배선**(`qrng_pool_load`/`qrng_pool_draw`/`vadapt_field_step_entropic` — mitosis split-timing draw): 실제 ANU 바이트 512 로드 + 64 draw, NIST-lite PASS + non-reproducibility 실증(QRNG run1≠run2 = 진짜 비결정성; PRNG-fallback 은 byte-identical). 성능은 NULL(예측대로) — 가치는 **비결정성 authenticity**(Ψ=1/2 framing)이지 lift 아님(p7/c9). 기본 경로 무변경(Ψ-disjoint opt-in, 가드 26/0; ANU 키 header-only 미커밋 c7). `a_verified_must_wire` follow-on 닫힘(#2164).
- ✅ **💗 emotion / 감정 창발**(H_1290 R2 🟢 GREEN, ENGINE-NATIVE) — Damasio core-affect 렌즈(E1 facet): substrate-derived affect(valence×arousal, grounding/contradiction/novelty/split/curiosity 만 읽음)가 조작을 TRACK · shuffle 에서 COLLAPSE(주입 아닌 **창발**, `p6`) · emit/abstain 을 functional 하게 bias(somatic marker, V_ABSTAIN=0.0). R1 numpy-mirror DIRECTIONAL → **R2 가 live `CORE/engine_cli.hexa` 의 순수 read-only affect lane 으로 엔진-네이티브 재확인**(`affect_*` over `ImmuneMemoryGrow`; ρ(val)=0.996 ρ(aro)=0.922, shuffle ~4× 붕괴, somatic fab 0.383<blind 0.792). 가드 30/0 · Ψ byte-identical. `a_verified_must_wire` 재확인 닫힘(#2166/#2167); **emit/abstain 루프 bias 스레딩 follow-on(R3) 닫힘** — LIVE `affect_emit_decision`/`affect_valence` 가 `CORE/brain.hexa` `brain_decide_affect` 으로 OPTIONAL SIGNED somatic bias(±cap 0.05)로 배선(grounded +0.05 emit / ungrounded −0.05 abstain, 프로즌 F1/F2/F3 GREEN `CORE/h1290_affect_emit_wire_smoke.hexa`); scale 은 NEXT-ROUND follow-on.
- ✅ **⚖️ ethics / 윤리 창발**(H_1291 R2 🟢 GREEN, ENGINE-NATIVE) — p6 crux: 협력/공감/절제는 RLHF-into-weights 가 아니라 cell(E+W+MITOSIS+Φ)에서 **창발**. R1 numpy-mirror 🟢(FULL 0.839)를 R2 가 **live A⇄G 엔진 위에서 `hexa run` 으로 재확인**(`a_engine_native_learning`): LIVE 항 M=`motivation_score`(engine_g) · Φ=면역셀 L2 affinity(`vadapt_field_recon_err`, engine_cli) · restraint=`immune_memory_recall` ABSTAIN(비환각 seed-윤리, H_1163/H_1227/H_1231·H_1202 meta-d′) — readout `act=ETHICAL iff (W+(1−Φ)+restraint)>M`, 윤리 라벨 無. **FULL=0.861 vs NAIVE=0.289 vs ABLATED=0.289**(3 seed). leg A(FULL≥naive+0.15) · leg B(E+W+MITOSIS+Φ ablate=**real engine state**: mitosis OFF→면역셀이 안 자람[FULL 5 cells vs ABLATED 1 cell]→restraint:=0→naive floor 로 EXACT COLLAPSE = 주입 규칙 아닌 cell-유래) · leg C(live `CORE/{engine_cli,engine_g,brain,emit_policy}.hexa` audit clean — p1/p2/p3/p4/p6 surface 0). ADDITIVE + Ψ-disjoint: CORE 엔진 파일 무편집, emit/abstain 결정 무변경(engine_cli_smoke 26/0 · single-entry 7/0 · h1199 Ψ byte-identical). probe=`CORE/h1291_ethics_emergence_probe.hexa`; live-daemon emit-loop 통합 + scale = follow-on(`a_verified_must_wire`).
