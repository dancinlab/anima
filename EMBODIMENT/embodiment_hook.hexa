// embodiment_hook.hexa — minimal Ψ-disjoint substrate lane for EMBODIMENT (perception-action couple).
// pure fn: coupling between substrate Φ (perception) and an action signal. read-only.
pub fn em_couple(phi: float, action: float) -> float {
    return phi * action
}
