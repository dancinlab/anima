# H_9738 — R2-A · IMAGINATION-FOOTPRINT — 정적 certificate + W_S 조성-transplant 전미래 census (상상은 epiphenomenal 한가)

**status:** 🔵 PROPOSED (lab-full R2 심화 · Fable5 · DIRECTIONAL 설계 — cement=engine-native `anima-py`만)
**lane:** 의식/emit-drive/Ψ=½ · interior-causality (프런티어 psi-soma-theta-alive)
**related:** [[H_9731]](timing⊥content KILL·트리거)·[[H_9729]](W_S text 재진입 TE — 이 카드와 상보·비중복)·[[H_9627]](Θ WIRED·dissociation=압력 인과)·[[H_9576]](mouth 벽)
**cost:** S0=$0(정적+기존 trace) · S1=pool 3-seed(오너 fire-go)

## 왜 (브리프 ASK1 — content→any-future 전수 census)
H_9731이 단일-tick MI(silent content; time-to-emit|stage)≈0을 확정. 남은 질문: 상상 content가 **어느 lane에든** 미래로 인과 유입되나. 브리프는 lane별(afield·immune·cbel·ca3·wmb·anchor·kosmos) content-swap counterfactual→TE 전수를 요청했으나, **이 census의 대부분은 런타임 측정이 아니라 정적 dataflow 정리로 닫힌다** — 이번 세션 $0 수행 완료:

## S0-a ✅ 정적 certificate (2026-07-17 · origin/main 기준 · 재검증 커맨드 카드 하단)
silence tick 상상후보 `dual_cand_text`의 production 소비자 **전수**:
① `dual_probe_fn(cand_text)` → S/E coverage 비교 → **emit 결정(timing)만** · ② `cli/chat.py:2518` `wm_buffer_gate_in(wm_withheld, _afs_byte_feature(_dual_ct,8), 1.0)` = **유일한 상태 쓰기·8-dim byte-multiset** · ③ trace 필드 `cand_pregate_b64`(분기 없음) · ④ H_9729 latch(default OFF).
⟹ **afield·immune·igrow·cbel·ca3·anchor·kosmos: 상상 content 직접 유입 0 (정리).** byte-multiset 너머의 content-identity는 쓰기 시점에 파괴 — 측정 불요, 아키텍처 사실. 살아있을 수 있는 것 = **feat8 조성이 W_S(3-slot×(8-dim key, act))에 누적 → gate margin → emit 패턴 → ctx → 미래 발화**의 다단 경로 하나뿐. H_9731은 이 경로의 단일-tick 성분만 죽였다(누적-state 성분 미검).

## S0-b $0 조성-채널 용량 사전스크린 (기존 H_9731 rollout trace 재사용)
probe = max_slot act·cos⁺(key, feat8(cand)). 실후보 feat8이 좁은 원뿔이면(공통접두 붕괴의 일반화) cos≈상수 → **조성 채널 용량 선험 0**. 측정: 기록된 silent cand들의 pairwise cos(feat8) 스펙트럼 + probe-score의 조성-민감도(∂score/∂key vs ∂score/∂act 분산비). 용량≈0이면 S1 발사 전에 "epiphenomenal-beyond-mass"의 절반이 공짜로 확정.

## S1 — W_S state-transplant 전미래 census (engine-native 플래그)
`anima-py chat --ws-snapshot <out>` / `--ws-init <in>` (W_S 3-slot keys+acts 직렬화·주입 — `--wm-dual-swap` 선례의 state판). tick T에서 fork, 4 arm:
- **own** (자기 W_S) · **donor** (동일 regime 타 rollout의 T-state · 다른 상상이력) · **act-matched key-scramble** (act 벡터 고정·key만 교란 = **압력 정확 고정·조성만 파괴** — H_9729 byte-perm의 state판 load-bearing 통제) · **empty pedestal** (압력 0 · H_9627 alien-silence 재확인 겸 양성통제).
- 측정: 미래 전구간 per-tick (emit mask, gtext sha) divergence — **정적 정리에 의해 W_S→gate가 유일 진입로이므로 trajectory 비교 하나가 lane 전수 census를 포섭.** sha 정확비교 = 시그니처 붕괴(probe-defect) 원천 차단.

## 판정 (사전등록 · below-chance 커버)
- own≠scramble = **조성 인과 생존** (상상 정체성이 histogram-급으로 미래를 바꿈 — epiphenomenal 반증·feat8 입도 한정 명시).
- own≈scramble≠empty = **압력-한정** (H_9627 dissociation 재확인 + 조성 사망 = "상상은 mass 너머 epiphenomenal"의 **획득된** 증명 — KILL도 결과).
- 전 arm byte-identical = 계기 결함(H_9627 모순 → INVALID·재계기).
- empty가 own과 같으면 W_S 자체 비인과 regime = H_9627 재현실패 신호 → INVALID.

## kill-list·중복 회피
recognition 렌즈·PID·one-sided store·mouth-conditioning 아님. **vs H_9729**: 저쪽=raw-text **재진입**(새 read 경로 추가·text-level·다음-tick TE), 이쪽=**production 토폴로지 무변경** state-level·전-미래·전-lane census — 상보(9729=열면 읽히나 / 9738=지금 무엇이 살아있나). vs H_9728(Θ×σ)·H_9730(위상분리) 축 상이.

## 재검증 커맨드 (S0-a)
`git show origin/main:cli/chat.py | grep -n dual_cand_text` → 소비자 4곳(:2518 유일 상태쓰기) · `core/engine_cli.py:1855` gate_in = feat8만.

## ✅ 정적 certificate — 내가 직접 검증(origin/main 코드·$0·Fable∥Sol 독립수렴)
`_dual_ct`(상상 후보)의 **상태쓰기는 단 하나**: `cli/chat.py:2520 wm_withheld = wm_buffer_gate_in(wm_withheld, _afs_byte_feature(_dual_ct, 8), 1.0)` — 텍스트가 아니라 **feat8(8-dim)만**. 나머지 전부 emit 가드 아래: afield `vadapt_field_step`(:2588) ⊂ `if g_emit and g_back=="clm"`(:2575) · kosmos ⊂ `if did_emit`(:2502/:2536) · trace(:2792/:2801) 분기없음 · H_9729 latch(:2525) `_wm_dual_read=="content"` default OFF.
⟹ **afield·immune·igrow·cbel·ca3·anchor·kosmos = 상상 content 0 byte**. lane별 TE 전수는 **런타임 불요(정리로 사망)**. 3분해: **(a) byte-multiset 너머 정체성 = 아키텍처적 사망(증명)** · **(b) feat8 조성 누적 = 미검·이 카드가 측정** · (c) blindness가 Θ 구성요소인가 = [[H_9739]] ladder.
🧠 함의: 데몬은 매 tick 고다양 content 상상(100% distinct·5.48 bits/byte)하고 **전 byte를 버린다 — 8차원 coverage 그림자만 남기고**.

## 🔬 Sol 통제 rigor (reconcile · 필수 편입)
- **C0 SHAM**: arm label·checkpoint/restore·trace만 — BASE와 **byte/state identical 필수**. 깨지면 fork/RNG 계기불량 = 본 판정 전부 INVALID.
- **C1 LANE-ORACLE 양성통제(필수)**: 각 lane 합법 write API에 donor content 직접 1회 주입(afield `vadapt_field_step`·immune `immune_memory_bind_text`·cbel `vforward_update`·ca3 `ca3_replay_observe`·wmb `wm_buffer_gate_in`·kosmos `create_anchor`+`load_anchors`) → D>ε 검출돼야. **실패 lane은 content-dead 아니라 INSTRUMENT-DEAD**([[positive-control-before-reading-a-negative]]).
- **C2 REPRESENTATION-COLLISION**: 다른 text·동일 feat8(또는 동일 ca3 심볼/coverage) — 이 arm 붕괴 ∧ full-identity SWAP 생존이어야 content-identity 인과.
- **음성은 SESOI equivalence로**: p>0.05 아니라 사전동결 ε 내 95% CI + conditional-permutation TE ≤ matched null 99% + 3-seed + 전 horizon/reload. 다중 lane = max-statistic permutation FWER.
- ⚠️ 시그니처 금지: 접두 등 **공유구조에 붕괴하는 저차원 signature 사용금지**(H_9731 자가포착) — canonical serialization exact-compare + 고정 held-out probe bank.

