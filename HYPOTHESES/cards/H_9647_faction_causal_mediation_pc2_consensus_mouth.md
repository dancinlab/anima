---
id: H_9647
group: faction-lateral-axis-r3
date: 2026-07-17
slug: faction_causal_mediation_pc2_consensus_mouth
title: PC2→consensus Δ→mouth 의 완전한 인과 매개가 없으면 파벌은 mouth-severance 의 해법이 아니다
status: PROPOSED
tier: 🟢 인과 매개(pool) · Sol F15 — H_9645 의 판정 관문
cost: pool
source: sidecar lab full (Fable5 claude-fable-5 + Codex5.6 gpt-5.6-sol 병렬 발산 · 37안 → 중복제거 27안)
related: H_9576, H_9645, H_9295
---

# H_9647 — PC2→consensus Δ→mouth 의 완전한 인과 매개가 없으면 파벌은 mouth-severance 의 해법이 아니다

## 주장 (반증가능)

상관된 emit-직교 DOF 를 3단 인과로 쪼갠다. H_9576 이 '경로 있음·의미 미전달' 로 벽을 재프레임했으므로, 매개가 실제로 성립하는지가 핵심.

## 레버 (engine-native · `anima-py` 플래그)

```
`anima-py evaluate <ckpt> --faction-consensus phasic --faction-mediation normal,pc2-clamp,pulse-clamp`
```

> `a_experiment_engine_native`: 조작은 `anima-py` 명령의 **플래그**여야 한다 — 엔진 옆 스크립트/프로브 금지.
> 위 플래그는 **미구현**이다. 발사 전 `cli/`+`core/` 에 구현하고 `VERSION` 을 올려야 한다(G5).

## DV · bar · 우연수준

자연간접효과 NIE · 직접효과 NDE. permutation 우연 0. bar = NIE 95% CI>0 ∧ pulse clamp 가 PC2→emit 효과의 ≥50% 제거.

## 통제군 (≥2 · 사전등록)

① PC2 clamp null ② consensus pulse clamp null ③ stage 내 pulse permutation scramble ④ oracle PC2 pulse→mouth 양성

## 사망조건 (사전등록 · tune-to-green 금지)

PC2 가 consensus 를 예측 못 하거나 pulse clamp 후 emit PC2 가 유지되면 인과 경로 사망.

## 비용

pool(CPU)

## 왜 새로운가 (기존 각도 대비)

H_9295(단순 게이팅)와 달리 **매개분석 3단**. H_9576 의 '경로 있음·의미 미전달' 을 인과로 분해.

## 범위 (정직)

- 발산 산출물 = **DIRECTIONAL 설계**이지 verdict 아님(`a_lab_full_diverge`). cement 는 engine-native `anima-py` 로만.
- 옛 파벌 Φ 숫자(법칙 22/43/44 · TOPO12)는 **폐엔진 proxy** 산이며 이 카드의 근거가 아니다 — H_9627/H_9628 참조.
