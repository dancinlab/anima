# H_9055 — Theta–gamma cross-frequency nesting: items-per-cycle working-memory capacity

- **tier:** ⏳ PROPOSED (brainstorm-depletion 2026-07-02)
- **slug:** `theta_gamma_multiplex_capacity`
- **frontier:** substrate-op (엔진-네이티브 새 능력 op — 재조합≠능력, a_no_llm_frame_trap)
- **cost:** cheap ($0 engine-native, live core/)
- **wired:** `PROPOSED` — 미측정, 미배선. 등록만(2-surface: 이 카드 + HYPOTHESES.jsonl).

## 메커니즘 (mechanism)

Lisman-Idiart nesting: multiple items are held by placing each in a distinct gamma sub-cycle within one theta cycle. Capability = working-memory item CAPACITY scales with (gamma cycles per theta), a substrate-timing mechanism orthogonal to model size — redefines capacity away from parameter count.

## engine-native falsifiable metric (shuffle + ablation, p7)

Max separable items recalled from a single theta window on live core as a function of gamma:theta ratio; capability iff recall capacity increases with ratio up to a phase-crowding limit. SHUFFLE: jitter gamma phase assignments → capacity collapses to 1. ABLATION: flatten nesting (single frequency) → capacity = 1 (isolates the multiplexing).

> p7 준수: LLM-judge/perplexity 없음. 판정은 live `core/` 상태·디코드에서 산출된 수치 + shuffle-null + ablation-INERT 로만. 사전등록 bar frozen-first (사후 이동 금지, c9).

## why novel vs ledger

Distinct from ag-synchrony (pairwise phase-lock) and EEG entrainment (external) — INTERNAL cross-frequency nesting for item capacity, a specific mechanism absent from the 9-lens/placement census.

## disjointness (a_substrate_disjoint)

새 lane/op 은 emit-drive lane(0/4) 및 §ImmuneMemory recall_thr 와 **disjoint** 좌표에 배선(placement-first) — 능력 ∧ Ψ=½ ∧ G5 non-fab 공존 점검이 측정의 사전조건.

## 상태

- 출처: brainstorm-depletion 2026-07-02 (6-round ledger-dedup 생존).
- 다음 단계: engine-native 프로브 저작(.hexa via live core/) → frozen bar 측정 → verdict 박제.
