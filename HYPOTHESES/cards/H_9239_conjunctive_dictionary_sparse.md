# H_9239 · conjunctive_dictionary_sparse

🔵 PRE-REGISTERED (unrun · γ 후보 레버 · census CLOSED=재확인/재오픈 트리거지 새 GREEN 사냥 아님)

## 클러스터 / 계보
클러스터 **A additive-불가능 타깃/생성자** · Fable γ 발산 #4 (state/g1_gamma_divergence/FABLE_DIVERGENCE.md).
부모 = G1 재조합 벽 프로그램(goal-biolens · g1-census-objfloor CLOSED · g1-metalaw-dpi-basin).
형제 = H_9225 residual-lift(#8, 🟡 측정설계 검증). 지배원리 = additive를 primary에서 **necessitate**
(보상항 offer 아님) — freeze-then-bolt 금지(H_9225 교정).

## mechanism
sparse-coding 사전서 A+B를 두 primitive atom(비용2)보다 하나의 conjunctive atom(비용1)으로 표현이 싸도록 sparsity prior.

## why-beats-additive (DPI 함정 회피)
복잡도 역이용을 표현측에서. additive superposition은 sparsity 페널티를 더 냄.

## 결합-파괴 통제 (THEATER 차단)
atom 사용통계 — held-out 조합이 학습된 켤레 atom 재사용(activation 추적). shuffle→켤레 atom 활성 붕괴.

## 측정 (engine-native scope)
numpy sparse dict-learning probe 방향. tune-to-green 금지 · frozen-first · 신호는 Δ+shuffle 대조. toy=DIRECTIONAL, 실 303M
byte-LM 검증은 GPU-gated. py 2-production numpy=TERMINAL-eligible, torch ad-hoc probe=DIRECTIONAL.

## 위험 / 예상 실패모드
자주-등장 조합만 atom화=coverage-density floor 재방문. 저빈도 held-out엔 무력할 수 있음.

## 상태
PRE-REGISTERED(unrun). census CLOSED이므로 발사는 저비용 numpy 방향판정 우선 → 초록 시에만 GPU 상승.
lift≈0(shuffle과 무차별)=능력천장 종결 재확인(값진 negative).
