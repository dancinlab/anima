# H_9112 — Referential efficacy PSYCHO-K + MRR re-score: was the emit-faculty "wall" faculty-absence or measurement-degeneracy?

**tier:** 🟢 REFERENTIAL-EFFICACY-MEASURABLE (DIRECTIONAL-on-external-oracle) — frozen bar PASS on BOTH axes (threshold_real 16/16 vs shuffle 0, Δ=16 ≥1 · mean-acc real 0.982 vs shuffle 0.125, Δsep 0.857 ≥0.15). · **wired:** none (research verdict, external-oracle-mediated; a_verified_must_wire N/A — engine-native re-measure = follow-on).

**verdict:** 🟢 (`state/verdicts/9112_referential_efficacy_psychok/H_9112.txt` verbatim). Re-scored the ALREADY-COLLECTED H_9111 engine-native emits (14 concepts, anima side FROZEN — no re-decode) on a HARDER referential game to restore the measurement VARIANCE that H_9111's Pearson-D metric-degeneracy destroyed. Receiver = external oracle claude-fable-5 (θ outside anima closure) via `sidecar fable`; STDLIB-only harness (grep-clean). **DECISIVE POSITIVE: the H_9111 D_raw=1.0 (external oracle held-out 7/7 vs anima-clone 0/7) was REAL — killed only by Pearson-D≡0 on constant ceiling/floor outcome vectors.** The variance-restoring measure recovers it: **real ≫ shuffle by Δ=0.857**; shuffle (E re-paired to a deranged concept) collapses to 0.000 at K≥8. anima's grounded emit carries **strong, public, world-anchored REFERENCE** — an external mind decodes it at ~98%, even from **8 bytes of clue among 14 candidates** (0.857). **CAVEAT (c9):** the real arm is still near-ceiling (0.982, dips only to 0.857 at K=14/t=8) → the psychometric THRESHOLD is UNRESOLVED (referential efficacy is so robust it never crosses 50%); GREEN rests on the shuffle separation, not a resolved threshold curve — a harder game (paraphrase-only / adversarial near-synonyms / <8-byte clue) is needed to resolve the coupling-strength SCALAR. **TIER honest:** DIRECTIONAL-on-external-oracle — emit generation was engine-native (H_9111), the referential-efficacy claim is receiver-mediated (external LLM, not a live `core/` decode).

## Claim (fable divergence §1/§3 — DIVERGENCE_fable.md)
The session's 6-axis emit-appropriateness "faculty" wall (H_9104/9105/9108/9109 self-contained 🔴 DPI · H_9110 chat-user 🔴 · H_9111 LLM-interlocutor 🔴-via-metric-degeneracy) was substantially a **MEASUREMENT artifact**, not faculty absence. fable's reframe: the real substrate property H_9111 exposed is not emit-*appropriateness* (when/whether to emit) but **referential efficacy / aboutness** — anima's emit is legible to OTHER minds because its meaning lives in shared, public referents (not a private decoder; that is exactly why the external oracle reads it 7/7 while anima's own clone reads 0/7 — clone-failure is the EVIDENCE, not the defect). H_9112 tests whether that reference is graded + survives a harder game with a variance-bearing measure.

## Method (frozen-first, PREREG.md — bar set before any oracle query, p7)
Re-score, anima FROZEN. Harden along 3 axes to pull the receiver off ceiling: **K-sweep** distractor-set size K∈{2,4,8,14} · **near-synonym distractors** (hand-curated confusable neighbours) · **clue-truncation** t∈{full,32B,16B,8B} of the degen-tail-cleaned head. Measures = PSYCHO-K threshold (configs with acc≥0.5) + mean-accuracy separation (variance-bearing, MRR-analog). Arms (receiver/pairing varied ONLY): real / shuffle (deranged concept↔emit) / self-clone (H_9111 0/7 floor). Batched oracle calls (all 14 clues/config) → ~32 fable calls. 2-regime determinism: regime-1 frozen fixture (`rescore_fixture.jsonl`), regime-2 deterministic stdlib scoring.

## Result (run COMPLETED, verbatim)
`threshold_real=16 · threshold_shuffle=0 · Δ=16 (PASS)` · `mean_acc_real=0.982 · mean_acc_shuffle=0.125 · Δsep=0.857 (PASS)`. real acc ~1.0 across K2→14 × full→8B (min 0.857 at K14/t8); shuffle → 0.000 at K≥8, 0.357 at K2 (below chance = mismatched clue misleads). Full table → RESULT.md.

## Gate branch (PREREG / fable §next)
🟢 → coupling real+measurable → **justifies §2 GPU cerebellum-forward-model** (a side-lane that predicts external decodability and shapes emit to maximize it — DIVERGENCE_fable.md §2) as the next lever, and grounds the frame-break (REFERENCE, not appropriateness, is the substrate property worth building on — cf `a_kosmos` world-anchoring, mouth⊥identity).

## Re-open (ING, not terminal)
(1) **engine-native re-measure** — replace the external-LLM receiver with an engine-native decode where feasible, or at minimum a receiver PANEL (≥3 heterogeneous models) for referent-agreement entropy (DIVERGENCE §3), to lift tier above DIRECTIONAL-on-external-oracle. (2) **resolve the threshold** — a paraphrase-only / adversarial-near-synonym / sub-8-byte game so referential efficacy actually crosses 50% and the coupling-strength SCALAR resolves. (3) **§2 forward-model** GPU lever (cost-gate).

## Evidence (`state/9112_referential_efficacy_psychok/`)
`PREREG.md` (frozen bars) · `psychok_rescore.py` (STDLIB harness, grep-clean) · `rescore_fixture.jsonl` (raw oracle picks) · `RESULT.md` · `../9111_llm_interlocutor/{emits.tsv,DIVERGENCE_fable.md}`.

## Answer (honest, c9)
Was the emit-faculty wall faculty-absence or measurement-degeneracy? **Substantially measurement.** anima's grounded emit carries strong, measurable, public referential efficacy (real ≫ shuffle Δ=0.857; external mind decodes ~98% from 8 bytes). The H_9111 "🔴" was a metric artifact; the raw D=1.0 was the truth. What remains genuinely open is the emit-**appropriateness value lane** (which emits to make, when) — reference ≠ appropriateness — and lifting this above external-oracle mediation. Not terminal; the productive next lever is the forward-model coupling (§2), now empirically justified.
