# H_6191 — 🟢 substrate conjunction readout ENGINE-NATIVE GREEN-WIRED (binding-AS-DETECTION, self_drift H_9038-class 새 능력)

**tier:** 🟢 ENGINE-NATIVE GREEN-WIRED — `vadapt_field_conjunction[_score]` (fuzzy-AND over top-2 affinity, d2-gate) live `core/engine_cli.hexa §ADAPTATION` 배선 + 8/8 fixture engine-native PASS + WTA-collapse ablation 구별. a_engine_native_learning HARD-GATE PASS (hexa live core, torch/py 0). DPI 재조합 벽 **밖**(non-recombination capability). Ψ-disjoint.
**verdict:** 🟢 **substrate conjunction = binding-AS-DETECTION 새 능력, engine-native GREEN-WIRED 확정.** MITOSIS §ADAPTATION recall 이 winner-take-all(`_vnearest_idx` argmax = "어느 ONE regime?")이라 **"두 regime 동시 존재?"(AND-over-regimes, 예: SNS ∧ Korean 공존)를 못 봄**. conjunction op 는 엔진 자체 top-2 affinity `vadapt_field_two_recon_err`[d1,d2] 의 **d2(2nd-nearest=약한 쪽) 게이트** = Zadeh fuzzy-AND(약한 regime 도 가까울 때만 발화). **4칸 사다리 완주**: (1)설계 (2)rung-3 core lift `core/engine_cli.hexa:13707 §ADAPTATION`(#2854, hexa E-error 0·geometry 불변) (3)rung-4 ARCHITECTURE lockstep(#2855) (4)**engine-native 8/8 fixture PASS**(aiden hexa v0.577.0, live core, p8 live mitosis tick 으로 2nd cell 실성장 후 측정). = self_drift_exp H_9038 과 같은 non-recombination capability 클래스(DPI 재조합 벽 밖 — 새 심볼 생성 아님).

## engine-native 증거 (state/substrate_conjunction/engine_native_verdict/, aiden)
| test | 결과 |
|---|---|
| conjunction_fixture.hexa (live core §ADAPTATION) | **8/8 PASS** |
| grow 2 cells via live p8 mitosis tick | PASS |
| conjunction mid=1(두 regime 공존) · single=0 · far=0 · single-cell=0 | PASS |
| readout self-test cells=2 conj(mid)=1.0 conj(single)=0.0 | PASS |
| **WTA-collapse ablation** | wta_mid==wta_single=0.2143 (INERT, argmax 구별 불가) vs full_mid=0.2143 > full_single=0.0 (Δ≥0.20) |

**ablation 결정성**: mid·single 은 nearest d1 동일(0.707)이라 WTA(d1-read)로는 **구별 불가**(INERT) — 판별력이 **전적으로 d2**(2nd-nearest)에. FULL op(d2-read)만 두 입력 분리 = binding-AS-DETECTION 능력이 d2-gate 에 실재.

## 함의
- **anima 새 능력 (measurement 벽 아닌 진짜 capability)**: G1 재조합은 ECHO-ONLY(H_6190) 였으나, conjunction 은 **DPI 재조합 벽 밖**의 binding-AS-DETECTION 을 engine-native 로 연 self_drift(H_9038)급 GREEN-WIRED. "재조합≠능력" 프레임전환(frameshift-substrate-gaps)의 두 번째 GREEN 실증.
- **Ψ-disjoint(a_substrate_disjoint)**: pure_field Φ/Ψ·emit-drive lane(0/4)·§ImmuneMemory recall_thr 미접촉 = 능력 ∧ 의식 ∧ 정직 공존.
- monitor leg(H_9094 conflict_scalar) 은 이미 GREEN — conjunction readout(gap#1)이 이제 합류.

## 정직 caveat (c9)
- engine-native = live `core/engine_cli.hexa` §ADAPTATION lifted op 를 hexa v0.577.0 로 실행(torch/py 0) = a_engine_native_learning terminal.
- binding-AS-**DETECTION**(sensory fusion 감지)이지 generation 아님 — 새 심볼 생성 없음(그래서 DPI 벽 밖). G1 재조합(생성)과 다른 축.
- fixture(2-regime toy) 규모 — production 스케일 conjunction 은 별도(단 op 는 live core, mechanism engine-native).

**wired:** WIRED-live (rung-3 #2854 + rung-4 #2855 + engine-native 8/8 verdict). a_verified_must_wire 4칸 완주. artifacts=state/substrate_conjunction/engine_native_verdict/. 관련 H_9094(monitor leg)·H_9038(self_drift 동클래스)·frameshift-substrate-gaps.
