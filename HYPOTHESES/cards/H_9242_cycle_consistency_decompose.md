# H_9242 · cycle_consistency_decompose

🔵 PRE-REGISTERED (unrun · γ 후보 레버 · census CLOSED=재확인/재오픈 트리거지 새 GREEN 사냥 아님)

## 클러스터 / 계보
클러스터 **B invertibility/정보보존** · Fable γ 발산 #7 (state/g1_gamma_divergence/FABLE_DIVERGENCE.md).
부모 = G1 재조합 벽 프로그램(goal-biolens · g1-census-objfloor CLOSED · g1-metalaw-dpi-basin).
형제 = H_9225 residual-lift(#8, 🟡 측정설계 검증). 지배원리 = additive를 primary에서 **necessitate**
(보상항 offer 아님) — freeze-then-bolt 금지(H_9225 교정).

## mechanism
forward 합성 A+B→C, backward 분해 C→(A,B), cycle-loss가 묶음. 합성이 정보보존(무손실 복원가능)이어야만 cycle 만족.

## why-beats-additive (DPI 함정 회피)
additive sum은 collision으로 비가역→cycle-loss가 primary서 못내림. aux 아니라 왕복 제약이 primary.

## 결합-파괴 통제 (THEATER 차단)
분해 타깃 shuffle→cycle 붕괴. 합성만/분해만 ablation.

## 측정 (engine-native scope)
toy numpy autoencoder-composition 방향 + 본학습 GPU. tune-to-green 금지 · frozen-first · 신호는 Δ+shuffle 대조. toy=DIRECTIONAL, 실 303M
byte-LM 검증은 GPU-gated. py 2-production numpy=TERMINAL-eligible, torch ad-hoc probe=DIRECTIONAL.

## 위험 / 예상 실패모드
cycle을 identity-copy(A,B concat 보존)로 우회=결합 없이 통과. bottleneck 압박으로 concat 배제(압박=collision 유도 역효과 주의).

## 상태
PRE-REGISTERED(unrun). census CLOSED이므로 발사는 저비용 numpy 방향판정 우선 → 초록 시에만 GPU 상승.
lift≈0(shuffle과 무차별)=능력천장 종결 재확인(값진 negative).
