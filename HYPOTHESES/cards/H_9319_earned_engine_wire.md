# H_9319 — EARNED 계기를 엔진에 배선 (`anima-py evaluate --earned`)

**Tier: 🔵 ENGINE-NATIVE WIRED (4/4 재현) + ⚠️ H_9318 순위 주장 철회 · 2026-07-14**

- 배선 → `cli/earned.py` + `cli/evaluate.py` (dispatch · `_KNOWN_FLAGS` · `--help` 3종 세트) · VERSION 0.13.17 (G5)
- 산출 → `state/h9319_earned_wire/` (RESULT.md · corpora/ · results/)
- 오너 지시 → **"모든 실험은 엔진네이티브여야 한다"** (`a_experiment_engine_native`)

## 위반 사실 (정직)

G1 스크리너 4점(H_9304·H_9316·H_9317·H_9318)은 전부 `state/*.py` 에 **자체 구현된 로지스틱 모델**이었다 — `core/` 를 한 번도 호출하지 않는 **미러**. 규칙이 금지하는 바로 그것이다: *"a number a mirror produces carries no guarantee it reproduces on the production path."*

## 배선 경로 재측정 — 4/4 재현

| 점 | 미러 | **배선 경로** | ruler | |
|---|---|---|---|---|
| 부정 (H_9304) | +0.00233 | **+0.00233** | 0.044% | ✅ byte-exact |
| 역접 (H_9316) | +0.00908 | **+0.00907** | 0.171% | ✅ |
| 패리티 (H_9317) | +0.00318 | **+0.00319** | 0.060% | ✅ |
| 반어 (H_9318) | +0.00127 | **+0.00127** (seed 9318) | 0.024% | ✅ byte-exact |
| 인증 3게이트 | — | G-ALIVE **+5.29653** · G-PEDESTAL **+0.00285** · G-POWER 전점 PASS | — | ✅ |

⇒ **네 점 전부 배선된 엔진 경로에서 재현된다.** 수치는 이제 cement 자격을 갖는다.

## ⚠️ 배선이 드러낸 결함 — 반어의 "가장 작다"는 **시드 잡음**이었다 (H_9318 부분 철회)

같은 계기 · 같은 코퍼스 · **시드만 다름**:

| 반어 (held-out ~2.5–3k 셀 · **n 작음**) | EARNED | ruler |
|---|---|---|
| seed 9318 (미러가 보고한 값) | +0.00127 | **0.024%** |
| seed 9304 (계기 기본값) | +0.00323 | **0.061%** |

**2.5배 흔들린다.**

⇒ **철회**: H_9318 카드의 *"반어가 네 점 중 가장 작다 — 이론상 가장 커야 하는 지점에서!"* 는 **순위 주장이 시드 잡음**이었다. seed 9304 에선 패리티(0.060%)와 사실상 같다.

⇒ **불변**: 두 시드 모두 TOST 등가마진(±0.02)보다 **한참 작다** ⇒ **H_9318 의 CLASS-CLOSED 판정과 그 논증(marginal 이 능동 오도하는 극단에서조차 전이 정보 없음)은 그대로 선다.** 흔들린 것은 *순위*이지 *등가성*이 아니다.

### 이것이 배선이 사준 것이다

미러의 단일-시드 수치는 **깨끗한 순위처럼 보였다**. 배선된 경로에서 네 점을 하나의 계기·하나의 시드 규약으로 다시 돌리자 **그 순위가 잡음임이 드러났다.** 미러로 끝냈다면 그 순위를 사실로 카드에 남겼을 것이다.

## 후속 (미발사)
- **n 이 작은 코퍼스는 단일 시드 금지** — 반어 점은 다중-시드 효과크기 CI 로 재보고.
- 나머지 3점은 n 이 커서(28k–39k 셀) 시드 민감성이 낮으나 **정책상 다중 시드 재보고** 대상.

## HONEST
계기는 배선됐으나 **코퍼스-사실 측정**이라 `core/` forward 를 호출하지 않는다(모델 무관 · 학습 무관). `a_experiment_engine_native` 가 요구하는 것은 **manipulation 이 canonical CLI 안에서 측정되는 것**이며, 이 조건은 충족한다(dispatch·flags·help·byte-audit 상속). bar 이동 0.
