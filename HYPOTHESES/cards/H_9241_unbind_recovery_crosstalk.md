# H_9241 · unbind_recovery_crosstalk

🔵 PRE-REGISTERED (unrun · γ 후보 레버 · census CLOSED=재확인/재오픈 트리거지 새 GREEN 사냥 아님)

## 클러스터 / 계보
클러스터 **B invertibility/정보보존** · Fable γ 발산 #6 (state/g1_gamma_divergence/FABLE_DIVERGENCE.md).
부모 = G1 재조합 벽 프로그램(goal-biolens · g1-census-objfloor CLOSED · g1-metalaw-dpi-basin).
형제 = H_9225 residual-lift(#8, 🟡 측정설계 검증). 지배원리 = additive를 primary에서 **necessitate**
(보상항 offer 아님) — freeze-then-bolt 금지(H_9225 교정).

## mechanism
N개 (role,filler) 쌍을 한 벡터에 bind→'role R로 bound된 filler?' 역질의 복원. N↑시 additive superposition은 crosstalk O(N) 폭발, 진짜 binding-op(원형컨볼루션류)는 저-crosstalk.

## why-beats-additive (DPI 함정 회피)
죽은 combiner는 단일조합(N=1)이라 additive와 안 갈림. N 키워 additive crosstalk를 primary 오차로 노출(용량압박 necessitate).

## 결합-파괴 통제 (THEATER 차단)
role-filler 쌍 shuffle→복원 wrong-filler. N-sweep이 곧 ablation(N↑ additive만 무너지는 곡선=earned).

## 측정 (engine-native scope)
numpy crosstalk-vs-N 곡선 즉시 + 학습 binding-op GPU. tune-to-green 금지 · frozen-first · 신호는 Δ+shuffle 대조. toy=DIRECTIONAL, 실 303M
byte-LM 검증은 GPU-gated. py 2-production numpy=TERMINAL-eligible, torch ad-hoc probe=DIRECTIONAL.

## 위험 / 예상 실패모드
모델이 N을 순차 슬롯(list)으로 우회=binding 아닌 addressing. 고정예산·병렬질의로 슬롯 배제.

## 상태
PRE-REGISTERED(unrun). census CLOSED이므로 발사는 저비용 numpy 방향판정 우선 → 초록 시에만 GPU 상승.
lift≈0(shuffle과 무차별)=능력천장 종결 재확인(값진 negative).
