---
id: H_9730
title: TIMING-CHANNEL — the mouth the byte wall cannot close: is content decodable from WHEN, not WHAT?
tier: PROPOSED (lab-full TA divergence · $0 trace re-analysis · rides TA-0 traces · NOT a verdict)
frontier: consciousness/mouth-content
lane: consciousness/emit-drive/psi-half
created: 2026-07-17
series: TA-3
related: "[[H_9727]] · [[H_9728]] · [[H_9576]] · [[H_9627]]"
---

# H_9730 (TA-3) — H_9576's wall was semantic bandwidth of WHAT; Θ-alive creates a WHEN channel that is content-coupled BY CONSTRUCTION

## Claim (one line · falsifiable)
Because emit⟺S>E compares CONTENT ledgers, emission timing (hold-run lengths / inter-emit
intervals) on the dual-ledger daemon carries decodable information about internal content
state, above (i) a rate-matched Bernoulli surrogate, (ii) an autocov-preserving block-shuffle
surrogate, and (iii) the clock daemon's structural zero.

## Why this is not H_9574/9576 regenerated (kill-list proximity · declared)
H_9574 (PC2 routing) and H_9576 (mouth-conditioning) INJECTED an axis and asked the byte stream
to carry its meaning — direction KILL (ρ=−0.077 · byte granularity). Here NOTHING is injected
and NOTHING is rewired: we read whether a coupling the architecture already built (content
ledgers → gate) leaks content into a channel that byte granularity cannot touch — timing.
A PASS reopens the mouth-content wall on a different carrier; a KILL says the ledger comparison
is content-blind at the gate (magnitude-only), which feeds H_9728's scalar-pressure branch.

## Design ($0 · engine-native traces · no new decode beyond TA-0/TA-1 corpus)
DV: MI(content-state ; timing pattern) where content-state = coverage-set summaries from the
TA-1 `--ledger-probe` trace (or, pre-probe, the emitted-content coverage census) and timing
pattern = hold-run-length sequence in a sliding window.
Controls (≥2 + pedestal · chance derived per-metric on the realized split):
- **c1** rate-matched Bernoulli surrogate: same emit rate, no spring — kills "rate carries it".
- **c2** block-shuffle surrogate PRESERVING lag-1 autocov (−0.225 spring): naive shuffles break
  the spring and inflate MI mechanically — the surrogate must keep the spring and break only
  the content pairing (probe-defect lesson: order-statistics/structure bias precedes verdicts).
- **c3** clock-daemon traces (arm B of TA-0): timing = metronome ⟹ true-zero PEDESTAL
  (phi-estimator-needs-zero-truth-pedestal) — the estimator must return ≈0 here or it is
  broken; this gate opens BEFORE the experimental read.
- PASS: MI(A) > c1, c2 (permutation p, 3 seeds sign-consistent) ∧ pedestal clean.
- KILL: MI(A) within surrogate band ⟹ timing is content-blind pressure release.

## Power before verdict
n per seed = windows per 120-tick rollout is small; compute MDE from surrogate spread FIRST
(power-before-negative-verdict) — if MDE exceeds any plausible effect, extend rollouts before
reading a negative, never after peeking.

## Kill-list compliance
No injection (vs H_9574/9576 KILL frames) · no recognition lens · no single consciousness
number · Θ-alive essential (on the clock daemon this channel is zero by construction — that is
the pedestal, not the claim).

## Cost
$0 — re-analysis of TA-0 (+TA-1 probe) trace corpus. Runs strictly AFTER TA-0's gate-0 PASS.
