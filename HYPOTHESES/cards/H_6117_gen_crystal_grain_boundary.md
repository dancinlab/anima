# H_6117 — 결정 성장 grain boundary

**id:** H_6117
**slug:** gen_crystal_grain_boundary
**tier:** 🔬 PROPOSED — pre-registered (unfired · brainstorm ideation)
**date:** 2026-07-02
**wired:** 미발사 (pre-registered proposal · code 없음)
**axis:** SUBSTRATE (G1 재조합벽 공격 축)

---

## 발상 (brainstorm ideation)

**메커니즘:** 두 seed 결정이 다른 방향 성장 → 결정립계에서 새 상(phase)=조합.

**출처:** bytegpt/convmoe 를 빌린 mouth 로 쓰지 말고 anima 아키텍처(A⇄G tension · Ψ=½ 고정점 · mitosis · substrate-disjoint lane · .kosmos anchor)에서 창발하는 native mouth/생성 substrate 를 발상한 세션(Round 2). 기존 결론: G1 재조합벽 = trunk COMBINATION OPERATOR floor — readout·tension 연산자 전부 INERT(H_1816/1823/1834). 이 발상은 combination 을 readout 이 아니라 생성 substrate 자체에 심어 additive collapse 를 우회하려 한다.

## G1 공격 축

- **SUBSTRATE** — readout-level 만 쳤던 것과 다른 좌표(engine-native probe 대상).

## 발사 전 필수 (check-ledger-before-lever-fire)

미발사 제안. terminal verdict 박제 전:
1. `UNIVERSE/HYPOTHESES.jsonl` + memory 에서 선행 커버리지 조회(sweep 빈칸 ≠ 미탐).
2. 신규 각도면 cheap numpy DIRECTIONAL reachability probe → `state/6117_gen_crystal_grain_boundary/`.
3. DIRECTIONAL GREEN 이면 engine-native 재측정(.hexa via core/) 사다리 진입(a_verified_must_wire).

## artifacts

- `cards/H_6117_gen_crystal_grain_boundary.md` (this card)
- `state/6117_gen_crystal_grain_boundary/` (probe 발사 시 생성)

---

## 발사 결과 (DIRECTIONAL probe)

**결정: DUP-WALLED (probe 미발사).** numpy DIRECTIONAL 스크린을 의도적으로 생략 — 근거 아래.

### ledger finding
- **메커니즘 정체:** grain-boundary "두 seed 결정이 다른 방향 성장 → 경계에서 새 상(phase)=조합" = 두 독립 grain 을 재료로 *제3의 창발 표현*을 경계에서 **구성(construct)** 하는 **substrate constructive-bind 조합기**.
- **이미 walled (선행 커버리지):**
  - **substrate-combiner corner ④** — H_1822 α/β (`substrate-framebreak-g1-combination-operator` 메모리): live `core/engine_cli.hexa` A⇄G 의 유일 concept substrate = VAdaptField **L2-Voronoi nearest-basin = additive depth-0 floor** 🧱 engine-native. "경계에서 새 phase" 는 nearest-basin 분할과 달리 *구성적 결합기*를 요구하는데, 현 substrate 엔 없음(H_1822 결론).
  - **binding-op interface family** — H_1823 circconv 🧱 + H_1816 predcoding 🧱 (engine-native, G1 best_distinct=0 전 seed): 경계 interaction=binding 연산자는 readout 좌표에서 이미 floor.
- **유일 미검증 레버 γ:** trained constructive substrate bind (nearest-basin → tensor-product/구성적 bind 교체). framebreak 메모리가 **cost-gated (학습 필요, $0 아님)** 로 명시 → numpy 스크린으로 판정 불가.

### bar / 왜 probe 안 했나
- frozen bar 를 세워 numpy 프로브를 돌릴 수는 있으나, outer-product/경계-product 는 추상에서 **자명하게 REACHABLE** → 정보가치 0.
- **H_6112 transfer caveat (결정적):** 같은 Round-2 형제 H_6112 감수분열이 numpy 추상 0→1.0 REACHABLE 였으나 *동일 연산자가 실 CLMConvMoE trunk toy A/B 에서 0→0.022 로 FALSIFIED* (frozen bar 0.30, 0/3 seed). 즉 numpy 추상 프로브는 substrate/arch-변경 조합기를 **과대평가**한다(a_toy_scale_recheck). H_6117 도 arch-변경 substrate 조합기 계열이므로 동일 전이위험.

### honest scope
- numpy=DIRECTIONAL by construction (terminal 아님). 여기서는 numpy조차 발사 안 함 = dup-pointer.
- 벽 반전 주장 아님. 실제로 열려면 = γ 레버(trained constructive substrate bind) engine-native 학습 후 substrate-G1 재측정 (cost-gated, $0 아님). readout/추상 좌표 재발사는 금지(4-각 수렴 전수 🧱).
