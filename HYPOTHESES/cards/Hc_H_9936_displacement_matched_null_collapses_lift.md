# H_9936 · 실현-변위(D)로 맞추면 GRAFT 의 우위가 무너진다 — 판정점서 학습본이 무작위 null q99 밑

**한 줄:** 진폭 축을 `gate_strength` 가 아니라 **실현 변위 `D ≡ mean_i KL(p_i‖p_base) = MI + L_common`**
(Fable·Sol 공통 지목)로 잡고 같은 D 에서 학습본을 **149-뽑기 학습 0회 null 앙상블**과 비교하면,
판정점 gs=0.1 에서 학습본 MI 가 null q99 를 **밑돈다**(−0.047/−0.046/−0.050 · 3/3 FAIL). 같은 gs 로
잰 lift +0.19~+0.25(z=+20~+26)는 순전히 진폭 교란이었다.

- 계기: `anima-py graft fit --steps 1`(받침대·null=학습 전 측정) · organ `trained57`(d=64·L=2·117kB) ·
  설치본 = origin/main `b6f7406fb` 정본과 파일 동일 확인 후 측정(병렬 세션 종료 후 그 미커밋 WIP 미사용).
- regime `no-corpus` · scope **TOY** — p9 상 계기 검증. `a_toy_scale_recheck`.
- 설계 위임: `sidecar lab full`(Fable 5 + Codex Sol) 조율. 두 모델 **일치**: D 로 축을 바꾸고 같은 D 에서
  학습 vs 무작위 비교하라 · 현 lift 뺄셈 무효(학습·무작위 곡선 비평행) · ablation 하락은 통제군이 세지는
  것. Sol 이 더 엄격(무작위 **회전** null — 노름+상태간 Gram 보존 · 자기샘플 연속열 금지 · held-out 고정
  접두사); 이번 판은 등방 무작위 null 로 먼저 실측, 회전 null 은 다음.

## 무엇을 쟀나 — 두 축의 정면 비교
| 지표 | gs-매칭(기존) | D-매칭(이번) |
|---|---|---|
| lift = 학습 − 무작위 | +0.19~+0.25 (z=+20~+26 vs null) | 판정점 gs=0.1 : **−0.047/−0.046/−0.050** |
| 판정 | 압도적 PASS | **3/3 FAIL** (null q99 밑) |

## D-매칭 판정 (null n=149 학습0회 · 밴드 ±0.08 · q99 봉투)
| seed | gs | 학습 D | 학습 MI | null q99@D | n | 초과분 | 판정 |
|---|---|---|---|---|---|---|---|
| 1 | 0.10 | 0.2991 | 0.2182 | 0.2652 | 25 | −0.0470 | FAIL |
| 2 | 0.10 | 0.3751 | 0.2772 | 0.3230 | 28 | −0.0458 | FAIL |
| 3 | 0.10 | 0.3417 | 0.2494 | 0.2997 | 23 | −0.0503 | FAIL |
| 1 | 0.20 | 0.5928 | 0.4275 | 0.4110 | 12 | +0.0165 | PASS(간신히) |

상위 진폭(gs≥0.3, 학습 D 0.85~1.09)은 **null 이 같은 gs 로 그만큼 못 움직여 밴드가 빈다**(n≤6) ⟹
"학습본이 무작위보다 멀리 간다"는 gs-매칭 우위의 정체가 여기서 드러난다: 우위는 상태-구분(MI/D)이 아니라
**변위 자체**다. η=MI/D 로 보면 학습본 0.67~0.74 vs null 0.85~0.96(저진폭) — 무작위가 오히려 **변위당
상태구분 효율이 더 높다**(mean-center 가 임의 게이트의 L_common 도 1차로 0 으로 눌러서다).

## 판정
🔴 **GRAFT 우위 = 변위 매개 (판정점서 무작위 null 밑) · DIRECTIONAL(toy)**
- gs-매칭 lift 와 q95 ablation 비 둘 다 **은퇴**(진폭 교란 · H_9935 와 같은 뿌리).
- 판정 가능한 지점(밴드 채워진 gs 0.1·0.2): gs=0.1 3/3 FAIL, gs=0.2 seed1 만 +0.0165 로 간신히.
- H_9932 의 "GRAFTED 0/3" 은 **강화**된다 — 절대 MI 바 미달을 넘어, 같은 변위의 무작위보다도 못하다.
- 아직 종결 아님: **회전 null(Sol)** 이 등방 null 보다 강한 통제이므로 그걸로 재확인 필요. 상위 진폭은
  학습본이 null 도달범위 밖이라 D-매칭 자체가 불가 = "더 멀리 감"이 결론의 일부.

## 다음
① 회전 null(트레인 오프셋 벡터를 상태간 무작위 직교회전 · 노름+Gram 보존)로 gs 0.1~0.3 재판정 —
등방 null 이 못 잡는 "기하는 같고 C-매핑만 파괴" 대조. ② py303(TERMINAL 은 거기서만).
③ 이 판이 서는 실현-변위 프레임을 `graft check` 에 D·η 출력으로 박제(계기화).
산출물 `~/.fire-recover/graft_toy_3seed/{null_ensemble,null_ensemble_hi,dose_response,lab_criterion_fable_sol}*`.
