# H_9243 · synergy_pid

🔵 PRE-REGISTERED (unrun · γ 후보 레버 · census CLOSED=재확인/재오픈 트리거지 새 GREEN 사냥 아님)

## 클러스터 / 계보
클러스터 **C additive-대조 discriminator** · Fable γ 발산 #9 (state/g1_gamma_divergence/FABLE_DIVERGENCE.md).
부모 = G1 재조합 벽 프로그램(goal-biolens · g1-census-objfloor CLOSED · g1-metalaw-dpi-basin).
형제 = H_9225 residual-lift(#8, 🟡 측정설계 검증). 지배원리 = additive를 primary에서 **necessitate**
(보상항 offer 아님) — freeze-then-bolt 금지(H_9225 교정).

## mechanism
부분정보분해(PID)로 joint가 marginal에 없는 synergistic 정보 분리→목표/probe. additive floor=synergy 0(정의상).

## why-beats-additive (DPI 함정 회피)
정보이론적으로 additive/bound 원리적 구분 유일지표. FORM 게임불가(추정량이 marginal 명시적 뺌).

## 결합-파괴 통제 (THEATER 차단)
shuffle→synergy 0. redundancy/unique 항이 대조.

## 측정 (engine-native scope)
numpy 가능하나 고차원 추정편향 큼(소형 probe 방향, H_9225의 진단파트너=measurement). tune-to-green 금지 · frozen-first · 신호는 Δ+shuffle 대조. toy=DIRECTIONAL, 실 303M
byte-LM 검증은 GPU-gated. py 2-production numpy=TERMINAL-eligible, torch ad-hoc probe=DIRECTIONAL.

## 위험 / 예상 실패모드
PID 추정 불안정→학습신호로는 약함. objective보다 measurement로 쓰는 게 맞음.

## 상태
PRE-REGISTERED(unrun). census CLOSED이므로 발사는 저비용 numpy 방향판정 우선 → 초록 시에만 GPU 상승.
lift≈0(shuffle과 무차별)=능력천장 종결 재확인(값진 negative).
