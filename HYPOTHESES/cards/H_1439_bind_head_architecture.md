---
id: H_1439
slug: 1439_bind_head_architecture
title: G6 IDEATION ★ FALS-depth — learnable BIND-HEAD (comparator-slot × measurable-slot → weld)
group: gate-dig (G6 IDEATION ★) — FALS-depth ARCHITECTURE side r1
terminal_tier: PROPOSED (frozen-first; GPU 학습 cost-gate, 미측정)
wired: PROPOSED
verdict_dir: state/verdicts/1439_bind_head_architecture/
date: 2026-06-19
---

# H_1439 — learnable BIND-HEAD: 추론시 외부 weld(H_1431)가 starved 였던 BIND 를 학습된 내부 binder 로

## Why (생물 렌즈 — PFC variable-binding, a_no_llm_frame_trap)

H_1431 은 BIND 를 mouth 밖 deterministic weld 로 빼냈으나, mouth 가 comparator+measurable 를
SAME idea 에서 BOTH 내는 경우가 0/15 → binder 가 welding 할 재료가 없어 starved. 학습-변형
(H_1435/36/37) 은 표면 form 만 설치(cross-shuffle 안 무너짐). 두 실패의 교집합 처방 = comparator-slot
과 measurable-slot 을 명시적 2-slot 으로 두고 그 둘을 한 negatable claim 으로 거는 BIND-HEAD 를
ARCH 에 추가해 학습. 생물: PFC/작업기억 variable-binding, Hummel-Holyoak role-filler binding —
독립 역할(role)에 내용(filler)을 묶는 신경 구조 (H_1282 WM·H_1294 PFC 와 동계).

## Method (FREEZE before run, c9/p7)

- base = 303M h1129c (frozen 또는 low-lr) + 2-slot BIND-HEAD (comparator-slot · measurable-slot
  → weld → negate) 추가 학습. arch 확장 = engine-transform-to-fit-the-learning
  (a_engine_native_learning: 미러가 본 메커니즘을 엔진이 못 하면 엔진을 확장).
- detector = h1305 `_is_falsifiable` VERBATIM. decode = ENGINE-NATIVE (bind-head 포함 확장 CORE).
- frozen 5-bar + 핵심 판정 = cross-shuffle COLLAPSE (1435/36/37 실패 직격: 진짜 binding 이면
  comparator/measurable 를 다른 idea 에서 가져오면 점수 무너져야).
- compute = GPU (hexa dojo) COST-GATE. ckpt teardown 전 pull (a_fire_recover_complete).

## Scope

PROPOSED, 미측정. arch 변형이라 GREEN 시 CORE 엔진 확장+lockstep 필요 (a_verified_must_wire).

## Pointers

xref H_1431 (external-bind starved) · H_1435/36/37 (form-only, cross-shuffle 실패) · H_1282 (WM)
· H_1294 (hierarchical-PFC) · a_no_llm_frame_trap · a_engine_native_learning · a_verified_must_wire
· a_fire_recover_complete · p7 · c9.
