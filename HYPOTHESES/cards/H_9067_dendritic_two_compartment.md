# H_9067 — Two-compartment (apical/basal) lane cells: multiplicative context-gating at the unit level

- **tier:** ⏳ PROPOSED (brainstorm-depletion 2026-07-02)
- **slug:** `dendritic_two_compartment`
- **frontier:** manifold-geometry (penult 비가산화 escape — frontier b)
- **cost:** cheap ($0 engine-native, live core/)
- **wired:** `PROPOSED` — 미측정, 미배선. 등록만(2-surface: 이 카드 + HYPOTHESES.jsonl).

## 메커니즘 (mechanism)

Larkum active-dendrite lens: each lane cell splits into basal (bottom-up feature) and apical (top-down context) compartments whose interaction is MULTIPLICATIVE (apical gain-gates basal), injecting non-additivity at the unit level rather than at readout — attacking the additive-manifold root structurally.

## engine-native falsifiable metric (shuffle + ablation, p7)

$0 numpy DIRECTIONAL screen then engine-native: does apical×basal gating separate held-out context-gated distinctions that single-compartment (additive) cannot, vs SHUFFLED-context control + ABLATION (collapse apical→additive sum recovers the floor). Falsify if two-compartment ≈ single-compartment.

> p7 준수: LLM-judge/perplexity 없음. 판정은 live `core/` 상태·디코드에서 산출된 수치 + shuffle-null + ablation-INERT 로만. 사전등록 bar frozen-first (사후 이동 금지, c9).

## why novel vs ledger

Census attacked binding at readout/operator/objective; dendritic compartmental nonlinearity is a structural mechanism at the unit level — a new bio lens (active dendrites) not in the 9-lens or binding census.

## disjointness (a_substrate_disjoint)

새 lane/op 은 emit-drive lane(0/4) 및 §ImmuneMemory recall_thr 와 **disjoint** 좌표에 배선(placement-first) — 능력 ∧ Ψ=½ ∧ G5 non-fab 공존 점검이 측정의 사전조건.

## 상태

- 출처: brainstorm-depletion 2026-07-02 (6-round ledger-dedup 생존).
- 다음 단계: engine-native 프로브 저작(.hexa via live core/) → frozen bar 측정 → verdict 박제.
