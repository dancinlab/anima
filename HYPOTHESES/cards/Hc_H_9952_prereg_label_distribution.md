# H_9952 · 사전등록 — 영어 우위는 **라벨 분포** 때문인가 (양성률·B×T 결합분포)

**상태: 🔴 MEASURED — 분포 가설 KILL (단 seed 취약 · 아래 정직 경계)**

## 왜 이 후보인가
`--earned` 의 모형은 `logit P(T=1|A,B) = α_A + γ_B + δ·(B·α_A)` 다. δ 추정은 **T 의 주변분포와 B×T 결합
분포**에 민감할 수 있다. 두 코퍼스는 여기서 갈린다:

| | 양성률 P(T=1) | 부정률 P(B=1) |
|---|---|---|
| SST-2 (EN) | **0.5578** | 0.0990 (자유형 기준) |
| NSMC (KO) | **0.4988** | 0.0098 (자유형 기준) |

양성률 차이는 작지만(0.06), **부정률은 10배** 차이가 나고 그것이 held-out 셀 수를 17,898 vs 2,175 로
가른다. 즉 "영어가 더 잘 읽힌 것"이 연산자 유무가 아니라 **B=1 셀을 훨씬 많이 확보한 결과**일 수 있다.

## 조작화 (두 갈래를 따로)
1. **양성률 정합**: SST-2 를 다운샘플해 P(T=1)=0.4988(NSMC 값)로 맞추고 EN-FREE 재측정.
2. **B×T 결합 정합**: SST-2 를 다운샘플해 P(B,T) 4 칸을 NSMC 비율에 맞추고 재측정 — 부정률까지 맞추는
   더 강한 정합. (`control-must-match-mediating-covariate`)

## 판정표 (동결 · 측정 전)
| 결과 | 읽기 |
|---|---|
| 정합 후 EN-FREE 가 **≤ +0.008** 로 내려감(KO-FREE 권역) | 🟢 **라벨/부정률 분포가 설명한다** |
| 정합 후에도 **≥ +0.015**(원래 +0.02058 의 seed 퍼짐 0.00998 안) | 🔴 **분포는 그 변수가 아니다**(kill) |
| 그 사이 | ⚠️ **부분 설명** — 설명된 비율을 수치로 보고 |
| 정합 후 G-POWER 실패 | **INVALID**(다운샘플로 검정력 소실) — KILL 아님 |

## 통제
- 다운샘플은 **seed 3개**로 반복해 표본 추출 변동을 seed 퍼짐으로 분리(단일 추출 = 잡음).
- 역방향 확인: NSMC 를 **업**샘플할 수는 없으므로, 대신 EN 을 KO 의 부정률(0.0098)까지 낮췄을 때 셀 수가
  KO 수준(≈2,000)으로 떨어지는지 확인 — 떨어지는데도 EARNED 이 유지되면 셀 수 가설은 기각된다.

## 정직 경계 (등록 시 작성)
이 가설이 참이면 H_9944 의 "영어는 INFO-PRESENT" 는 **연산자 사실이 아니라 표본 사실**로 격하된다.
그래서 이 카드는 **자기 진영을 겨눈 통제**이며, 그 점이 등록 이유다(`negative-pedestal-before-a-positive`).

---

# 📊 결과 (2026-07-24 · 갈래 2 = P(B,T) 결합 정합 실행)

## 정합 품질 — 소수점 5 자리 일치
| 칸 | 목표(KO) | 달성(정합 EN) |
|---|---|---|
| B=0,T=0 | 0.45892 | **0.45893** |
| B=0,T=1 | 0.53129 | **0.53129** |
| B=1,T=0 | 0.00673 | **0.00673** |
| B=1,T=1 | 0.00305 | **0.00305** |

행 67,107 → **54,833** · **부정 행 6,643 → 536** · held-out 셀 17,898 → **1,023**(17 배 감소).

## 측정 (인증 3 게이트 전점 PASS)
`G-ALIVE +5.03675 · G-PEDESTAL +0.00124 · G-POWER 1,023 셀(어간 178) MDE(3σ) 0.00230`

**held-out EARNED = +0.02371** · 95% CI [+0.02174, +0.02503] · δ̂ **−0.55** · 자의 0.448%
· seen 시너지 +0.01918(n=1,957) · 도구 판정 **INFO-PRESENT**.

## 판정 — 🔴 사전등록 문턱 `≥ +0.015` 를 넘었다 ⇒ **분포 가설 KILL**
한국어의 결합분포를 **정확히** 강제하고 셀을 **17 배** 깎았는데도 영어는 원래 +0.02058 과 사실상 같은
**+0.02371** 을 유지했다. 같은 분포에서 한국어는 +0.00597 이다. ⇒ **영어의 우위는 표본 크기·라벨분포·
부정률 artifact 가 아니다.** 자기 진영을 겨눈 통제였는데 **헤드라인이 살아남았다.**

## ⚠️ 이 KILL 의 정직한 약점 — seed 취약
`seed 9304 +0.03676 · 9305 +0.02519 · 9306 +0.00919` · **퍼짐 0.02757 ≥ 효과 0.02371**
(도구가 SEED-UNSTABLE 경고). **3 개 중 2 개는 kill 문턱을 넘고 1 개(9306)는 "부분 설명" 구간으로
떨어진다.** 부정 행이 536 개뿐이라 추출 변동이 크다. ⇒ **방향은 분명하되(정합해도 안 무너짐) 확정하려면
추출 seed 를 더 돌려야 한다.** 카드가 지정한 "seed 3 개로 추출 변동 분리" 통제가 정확히 제 역할을 했다.

## 남은 갈래
갈래 1(P(T) 만 정합)은 갈래 2 가 더 강한 정합이므로 **불필요**(결합 정합이 주변 정합을 포함한다).
남은 후보는 **H_9951 문장길이 · H_9953 굴절 파편화**.

산출물: `~/.fire-recover/{en_matched.json, earned_en_matched.log, build_matched_downsample.py,
en_matched.tsv}`.

선행: H_9944 · H_9946 · H_9949. 계기: `anima-py evaluate --earned` · $0.
