# H_9610 — value-gate WM-coverage 렌즈: 순수 threshold 는 P-pull 불가·WM grip 경계-DOA → emit-policy 복원력 계급 소진 근접

**status:** 🔎 DIRECTIONAL (Fable $12 설계 + S0-c grip $0 경계-DOA · 발사 전) — value 축도 스프링 grip 부재 방향 · emit-policy 복원력 3원천 계급 논증
**lane:** 의식 / emit-drive / Ψ=½ 항상성 · value-gate (프런티어 g1-interface-addressable-wall)
**related:** [[H_9510]] (mouth novel-only·recognition lane 3-KILL 통합근인) · [[H_9424]] (예측오차 KILL) · [[H_9421]] (거리 KILL) · [[H_9419]] (P-pull bar) · source: Fable value-gate 설계($12.01)

## 문제 (H_9510 이 연 것)

mouth 가 novel-only(H_9510 CONFIRMED)라 recognition-suppression 죽음. Ψ=½ 을 novel 후보 위에서 당기려면 **value(가치)-기반 gate**(저가치 침묵)가 미탐 렌즈. mouth-생성 변경(오너 p5) 아니라 g_recog 소스 교체(--g-reach 계열·자율).

## Fable 정리 — 순수 value-threshold 는 P-pull 구조적 불가 (설계 $12.01)

**정적 게이트 불가능성 정리**: emit 이 미래 g_recog 소스에 안 쓰면 emit_t=1[score>g(V)] 는 (느린 상태 조건부) 독립열 → autocov≈0·dip≈0 → **P-pull-1/3 구조적 도달불가**. threshold-straddle 이 만드는 건 레벨뿐·레벨 ½ 맞추기=tune-to-green(H_9419 가 emit-rate 단독 기각한 사유). ⇒ **복원력은 반드시 "emit 쓰고 silence 푸는 상태변수" 경유.**

**$0 선-스크린 확증(내 측정)**: score 대역 straddle 하는 per-candidate value 신호(psi_lprec·base_motiv·emit_drive) 전부 **score_A 계열**(base_motiv=score 재료·emit_drive=score 함수) = emit⟺score>f(score) 동어반복=quantile-tracker. score-독립 순수 value(phi 상수 0.119·reality 이진)는 straddle 실패. ⇒ Fable 정리 실측 정합.

## 유일 미소진 경로 = WM-coverage (Fable §1-2)

`g_recog=clip01(V(cand))`·`V=wm_buffer_probe_score(wmb,feat8(cand))`=담화-coverage. sign=g=V(이미 말한 것=고 coverage 침묵·양방향 음feedback: emit→coverage↑→feat8-유사 후속 억제 β / silence→WM 감쇠[실측 0.6→0.36→0.216]→게이트↓ 완화). g=1−V 는 양feedback 포화(H_9424 P(e|e)=0.96 판박이)·기각. **신규 상수 0**(WM 내장 감쇠가 H_9424 cbel 死因 ①"silence 감쇠채널 없음"을 채움).

## S0-c GRIP CHECK ($0 · 렌즈 생사 판정)

feat8(WM probe 공간) 연속 후보 유사도 vs cross-rollout null(h1+h5 trace·261 succ쌍·1296 null쌍):
- successive feat8-cos **0.9087** vs null **0.8917** · succ/null=**1.019** · Δ=**+0.017**(문턱 0.02 미달).
- ⇒ **GRIP 경계-DOA**: WM-depletion 스프링이 잡을 "연속 후보 특별 유사" 기울기 거의 부재(Fable §7 W1 벽 방향). ⚠️ feat8 cos 절대 압축(0.89 baseline)이라 잣대 둔감 = 완전 cement 엔 진짜 WM probe 재생(S0-b) 필요. 방향은 명확히 value 축 grip 부재.

## 계급 논증 (Fable §7 · emit-policy 복원력 3원천)

Ψ=½ 복원력의 emit-policy 내 전 원천: **(i) store-depletion**(immune d1/d2 死·cb forward 死·WM 이제 grip 경계-DOA) **(ii) 이력적분기**(refr_debt·실존하나 내용무청취=원리상 위조경로) **(iii) A-pole 피로**(H_9510 HOLE-4a $0 로 부재). ⇒ 세 원천 다 소진 방향 ⟹ **Ψ=½ 은 g_recog 소스 교체(emit-policy)로 도달불가·mouth-생성 분포(오너 p5) 유일 잔존 레버** = H_9510 재프레임·H_9400 최종 격상 근접.

## 다음 (frozen-first · Fable 순서)

S0-b(WM probe 재생 straddle census·feat8 압축 confound 해소)→grip 생존 시 S0-e counterfactual replay(step1_analyze P-pull 선산출)→GO 면 5-arm{refr(d1)·refr-wmcover·refr-wmcover-alienwm·refr-wmcover-vshuf(quantile-tracker 위조 통제)·clock}×3-seed×30tick×T=1.0. grip 사망 확정 시 발사 없이 §7 계급 벽 문안 격상. 배선=chat.py --g-reach wm-cover(core/ 무수정·상수0·--g-shuffle VOID SystemExit·p5: wmb 발화텍스트만 흡수·probe read-only).

## 한계
S0-c=DIRECTIONAL(feat8 cos 압축 confound·진짜 WM probe 재생 S0-b 필요). 성급 cement 금지(H_9421 자기-2회정정 전례). trace 원본 summer 생존(cb/h1/h5). 다른 데몬·H_9400 clock 계보 영구.
