# H_9303 — 🧱 GROUNDING WALL, **처음으로 벌어낸** (인증된 계기 · 정직한 검정력)

- **tier**: 🧱 **EARNED**  (py 2-production engine-native · aiden pool · TERMINAL-eligible)
- **한 줄**: 배운 원자를 **0.84**로 읽는 바로 그 계기가, 한 번도 안 가르친 원자는 **0.56**(우연)으로 읽는다 ⇒ **자연 분포는 held-out 원자의 극성을 가중치에 접지시키지 못했다.**
- **파급**: 프런티어의 다음 수 **O/C 채널 GPU 발사(\$21)가 비로소 정당화됨** — 오늘 아침까지의 근거("벽 = 추출 채널")는 폐기됐지만, 그 자리에 **진짜 벽**이 들어섰다.
- **artifacts**: `state/nbindg_grounding/{build91c.py, fix2.py, probe91b_verdict.json, run91b.log}` · frozen stdout `state/verdicts/h9303_grounding/`

## 이 판정이 그 전 넷과 다른 이유

H_9289 · H_9290 · H_9297 · H_9300 은 전부 **⏳ INVALID**(위치 아티팩트)로 강등됐다. 이 판정은 그것들이
가지지 못한 셋을 갖는다:

| | 옛 음성 4건 | **H_9303** |
|---|---|---|
| 읽는 위치 | 원자 직후 (모델이 아직 결정 안 한 자리) | **답을 뱉기 직전** (`이 영화 X ⇒` 의 화살표 뒤) |
| 담체 | 자연 리뷰 (모델이 배운 적 없는 질의틀) | **모델이 배운 담체** (eval 이 실제로 쓰는 그 형식) |
| 양성 대조 | 없음 (`V-FIT` = 20점/768차원 ⇒ 항상 1.000 = 게이트 아님) | **V-LIVE PASS** — 배운 원자 0.808/0.867 vs 통제 0.392/0.408 |

## 측정

```
n = 91 held-out 원자 (원자 단위 채점 · 어형 flip 을 되돌려 극성 복원 = D-acc eval 이 묻는 방식)
우연 sd 0.0524 · 동결 bar 0.65 = 2.86σ · 200회 라벨-순열 귀무분포
```

| arm | V-LIVE (배운 원자 · 항목단위 n=120) | **held-out 원자 (n=91)** | perm p |
|---|---|---|---|
| `main_s7` | **0.808** | 0.571 (+1.36σ) | 0.085 |
| `main_s11` | **0.867** | 0.560 (+1.15σ) | 0.185 |
| `base_only` (통제) | 0.392 | 0.484 (−0.31σ) | 0.615 |
| `shuffle_grid` (통제) | 0.408 | 0.527 (+0.52σ) | 0.350 |

- **V-LIVE PASS** — 계기가 살아있다(배운 원자에서 통제군의 2배).
- **held-out 은 bar 미달 · 순열 귀무분포 미돌파** — 실험군이 통제군보다 위이긴 하나(방향 정상) 유의하지 않다.
- ⇒ **자연 분포 접지 실패 = 벽. EARNED.**

## 정직한 범위

- 실험군의 +1.15σ/+1.36σ 는 **속삭임 수준의 신호**다(p=0.085/0.185). "정확히 0" 이 아니라 **"있어도
  bar 를 못 넘고 순열 잡음과 구분 안 되는 크기"** 다. 효과크기를 그대로 보고하고 bar 는 옮기지 않는다.
- 이것은 **recipe N2 의 자연 노출이 접지시키지 못했다**는 진술이지, "303M 이 원리적으로 못 한다"가 아니다
  (`a_scale_honest_scope`).
- 계기가 살아나기까지 **자극이 문법적이어야** 했다: 어절을 잘린 문맥이 아니라 **원본 코퍼스**에서 캐자
  V-LIVE 가 0.54 → 0.81 로 뛰었다(비문 담체가 계기를 반쯤 죽이고 있었다 · convergence `gen-nbindg-n2-py-1`).

## 다음 수

**O/C 채널** (확정-금지 abstention objective · 오류-표적 교정 폐루프) — 이제 이 벽 위에 서 있으므로
발사가 정당하다. 사전등록 시 이 카드의 계기를 그대로 상속한다: **답하는 자리 · 배운 담체 · V-LIVE 게이트 ·
원자 단위 검정력 · flip 되돌린 집계**.
