# H_9060 — Astrocyte tripartite-synapse spatial gating (activity-dependent lane-neighborhood modulation)

- **tier:** ⏳ PROPOSED (brainstorm-depletion 2026-07-02)
- **slug:** `astrocyte_spatial_gate`
- **frontier:** other (물리/정보 렌즈 능력 축)
- **cost:** cheap ($0 engine-native, live core/)
- **wired:** `PROPOSED` — 미측정, 미배선. 등록만(2-surface: 이 카드 + HYPOTHESES.jsonl).

## 메커니즘 (mechanism)

Tripartite synapse / Ca2+ wave (Araque): a slow SPATIAL field over the lane connectome that gates cross-lane coupling by aggregate neighborhood activity — a spatial, activity-dependent modulator, unlike the scalar/global neuromod gain that the 9-lens census walled, and unlike static placement topology.

## engine-native falsifiable metric (shuffle + ablation, p7)

Engine-native capability the scalar controller can't do: under a LOCALIZED tension perturbation, does spatial astrocyte gating confine/stabilize the perturbation to its neighborhood (measure Ψ-recovery locality) vs a SHUFFLED-neighborhood-adjacency control + ABLATION (uniformize the field → reverts to global scalar = no locality). Falsify if local≈global.

> p7 준수: LLM-judge/perplexity 없음. 판정은 live `core/` 상태·디코드에서 산출된 수치 + shuffle-null + ablation-INERT 로만. 사전등록 bar frozen-first (사후 이동 금지, c9).

## why novel vs ledger

9-lens neuromod census was global-scalar controllers (INERT, geometry-not-protocol); spatial activity-dependent glial gating is a distinct mechanism/structure not in that census or the placement (static-wiring) campaign.

## disjointness (a_substrate_disjoint)

새 lane/op 은 emit-drive lane(0/4) 및 §ImmuneMemory recall_thr 와 **disjoint** 좌표에 배선(placement-first) — 능력 ∧ Ψ=½ ∧ G5 non-fab 공존 점검이 측정의 사전조건.

## 상태

- 출처: brainstorm-depletion 2026-07-02 (6-round ledger-dedup 생존).
- 다음 단계: engine-native 프로브 저작(.hexa via live core/) → frozen bar 측정 → verdict 박제.
