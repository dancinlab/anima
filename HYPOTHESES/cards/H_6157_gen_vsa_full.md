# H_6157 — VSA full

**id:** H_6157
**slug:** gen_vsa_full
**tier:** 🔬 PROPOSED — pre-registered (unfired · brainstorm ideation)
**date:** 2026-07-02
**wired:** 미발사 (pre-registered proposal · code 없음)
**axis:** SUBSTRATE (G1 재조합벽 공격 축)
**~dup:** ~dup#8/#20

---

## 발상 (brainstorm ideation)

**메커니즘:** Vector Symbolic Architecture 전면(bind/bundle/permute)(~dup of #8/#20).

**출처:** bytegpt/convmoe 를 빌린 mouth 로 쓰지 말고 anima 아키텍처(A⇄G tension · Ψ=½ 고정점 · mitosis · substrate-disjoint lane · .kosmos anchor)에서 창발하는 native mouth/생성 substrate 를 발상한 세션(Round 7). 기존 결론: G1 재조합벽 = trunk COMBINATION OPERATOR floor — readout·tension 연산자 전부 INERT(H_1816/1823/1834). 이 발상은 combination 을 readout 이 아니라 생성 substrate 자체에 심어 additive collapse 를 우회하려 한다.

## G1 공격 축

- **SUBSTRATE** — readout-level 만 쳤던 것과 다른 좌표(engine-native probe 대상).

## 발사 전 필수 (check-ledger-before-lever-fire)

미발사 제안. terminal verdict 박제 전:
1. `UNIVERSE/HYPOTHESES.jsonl` + memory 에서 선행 커버리지 조회(sweep 빈칸 ≠ 미탐).
2. 신규 각도면 cheap numpy DIRECTIONAL reachability probe → `state/6157_gen_vsa_full/`.
3. DIRECTIONAL GREEN 이면 engine-native 재측정(.hexa via core/) 사다리 진입(a_verified_must_wire).

## artifacts

- `cards/H_6157_gen_vsa_full.md` (this card)
- `state/6157_gen_vsa_full/` (probe 발사 시 생성)

---

## 발사 결과 (DIRECTIONAL probe)

**결정: DUP-WALLED — 재발사 안 함 (probe 생략).**

### Ledger finding
H_6157 "VSA full (bind/bundle/permute)" 의 핵심 연산자(role-filler **bind** = 원형 컨볼루션/HRR)는 이미 실측 walled:

- **H_1616 `kosmos_vsa_compose`** — VSA/HRR bind (tensor / circular-conv role-filler) 를 **실제 frozen clm303 트렁크 G1** 에서 측정 → **🧱 DIRECTIONAL NOT-SUPPORTED, 0/3 seeds FAIL**. "HRR algebraically valid 이지만 frozen 트렁크가 분리하지 못하는 것을 compose 할 수 없다" (vsa_hrr 가 readout 을 binary 로 붕괴). 증거 `archive/state/g1_frozen_mouthbind_screen/raw_results.json`.
- **H_1514 `vsa_binding`** — 동일 VSA/HRR bind 의 **numpy abstract** 는 "🟢 WALL-BROKEN-GREEN (lens-level, DIRECTIONAL)" 로 나왔으나 카드 자체가 CONSTRUCTIVE weld·retrieval≠generation·transfer UNVERIFIED 로 명시. 즉 H_1514(numpy green) → H_1616(real 0/3 FAIL) = **H_6112 meiosis 와 똑같은 numpy-overstate 패턴이 VSA 에서 이미 완결됨**.
- 방계: H_1466 TPR tensor-product binder 🧱 WALL, H_1601/1610~1630 binding-operator census 전수 NOT-SUP, 그리고 memory `substrate-framebreak-g1-combination-operator`·`exp3-bind-g1g6-engine-native-floor` 가 "binding readout-operator 는 G1 안 엶, 레버 = trunk objective" 로 수렴.

### 왜 "VSA full(+bundle+permute)" 도 dup 인가
bundle 은 **additive superposition = 바로 그 additive-trunk floor 자체**이고, permute 는 고정 순열(비가역 롤 표식). 세 primitive 중 새 조합연산자는 bind 뿐인데 그건 H_1616 에서 실트렁크 0/3. bundle/permute 추가는 trunk COMBINATION OPERATOR 를 새로 만들지 않으므로 별도 좌표가 아님 (NOVEL-ANGLE 아님).

### Bar / scope
- Probe 미실행 (DUP-WALLED). 만약 numpy로 돌렸다면 H_1514 처럼 abstract-toy 에서 bind>additive 로 REACHABLE 이 나왔을 것이나, **H_6112/H_1616 선례상 그것은 WEAK screen 일 뿐 실트렁크로 transfer 안 됨** — green light 아님.
- 재발사하려면 numpy 미러가 아니라 H_1616 이 이미 한 frozen G1 실트렁크 측정을 넘는 *다른* 레버(trunk recomb-objective, H_1602 cost-gated)가 필요.
