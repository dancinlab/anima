# H_9946 · **형태론이 판별자다** — 한 코퍼스 안에서 자유(not) vs 결속(un-) 부정을 가르니 66배 차이, 결속은 **완전한 0**

**한 줄:** H_9944(EN 🟢 vs KO 🧱)의 유일한 약점은 *"영어라서인가, 그냥 다른 코퍼스라서인가"* 였다.
**같은 SST-2 문장집합** 안에서 **부정의 형태만** 갈라 재니(코퍼스 교란 = 정확히 0) — 자유·전치형은
전이되는 비가법 정보를 담고, **어간 결속형은 검정력이 충분한데도 전혀 담지 않는다**. 오너 상시지시
(`not` FREE/pre-posed vs `지 않다` BOUND suffix)가 **한 코퍼스 안에서 확증**됐다.

- 계기: 동일 인증 정본 `anima-py evaluate --earned` (H_9319 배선) · 2 팔 병렬 · $0 로컬 · 새 코드 0.
- 설계: SST-2 67,107 행을 **같은 문장·같은 라벨·같은 토크나이저**로 두고 B 비트만 교체.
  **양쪽 형태를 다 가진 242 행은 두 팔 모두에서 제외**(팔이 서로 신호를 물려받지 않도록).
  regime `natural-labeled` · DIRECTIONAL SCREENER.

| 팔 | B = 1 조건 | B-rate |
|---|---|---|
| **FREE** (영어형) | 자유·전치 부정 `not / no / never / none / … / n't` | 0.0990 |
| **BOUND** (한국어형) | 어간 결속 접두 `un- / non-` | 0.0300 |

## 결과 — 두 CI 가 전혀 겹치지 않는다
| | held-out EARNED | 95% CI | δ̂ | held-out 셀 | 도구 판정 |
|---|---|---|---|---|---|
| **FREE** | **+0.02058** | [+0.01976, +0.02131] | **−0.65** | 17,898 (어간 250) | INDETERMINATE |
| **BOUND** | **+0.00031** | [−0.00071, +0.00140] | **−0.15** | 3,757 (어간 214) | 🧱 **DATA-ADDITIVE** |

- 인증 3 게이트 **양 팔 전점 PASS** (G-ALIVE +5.03675 · G-PEDESTAL +0.00124 · G-POWER MDE 0.00118/0.00147).
- **BOUND 는 검정력 부족이 아니다** — MDE 0.00147 까지 잡을 수 있는 팔이 0.00031 을 읽었다.
  ⇒ *"측정 못 한 것"이 아니라 **"정말 없는 것"*** = **벌어낸 음성**. 도구 판정문: *"no transferable
  non-additive information at all ⇒ no estimator can ever bank it"*.
- **순위 주장이 허용되는가**: 격차 0.02027 이 양 팔 seed 퍼짐(FREE 0.00998 · BOUND 0.00431)을 모두
  넘는다 — 도구 자신이 정한 조건(*"A ranking claim needs the gap to exceed the seed spread"*, H_9319 에서
  철회당한 그 규칙) 충족. 66배.

## 판정 — 🟢 연산자는 **어간에서 분리 가능할 때만** 전이 정보로 존재한다
비가법 정보가 코퍼스에 있느냐 없느냐를 가르는 것은 **언어도 도메인도 행수도 아니라 부정의 형태**다.
자유·전치 부정은 어간과 독립인 연산자를 만들 수 있고(δ̂ −0.65), 어간에 융합된 부정은 **어간마다
별개의 어휘**가 되어 전이될 연산자 자체를 만들지 않는다(δ̂ −0.15, CI 가 0 포함).

## ⚠️ 동시에 H_9944 를 낮춘다 (verdict-integrity)
**FREE 팔 단독은 INDETERMINATE 다** — 양쪽형 242 행을 빼고 B 를 재도출하자 CI 하단이 ±0.02 마진 **아래로**
내려갔다(H_9944 전량 판정은 [+0.02077,+0.02263] 로 마진 위였다). ⇒ **H_9944 의 INFO-PRESENT 는 경계선
판정이었다**; 행 집합이 조금만 달라지면 INDETERMINATE 로 내려간다. 이 카드가 그 tempering 을 기록한다.

## 정직 경계
1. **벽은 서 있다** — FREE 조차 자의 **0.389%**. 형태론이 문을 *열어주는 조건*이지 문 자체가 아니다.
2. `CLAUDE.md`: EN 양성은 **SCREENER·DIRECTIONAL**, cemented 금지. 이 카드도 그 등급이다.
3. BOUND 팔은 `un-/non-` 만 쓴다(라틴계 `in-/im-/ir-/il-` 는 영어서 부정이 아닌 경우가 많아 제외) ·
   접두어를 뗀 나머지가 **그 코퍼스에 실제 등장할 때만** 부정으로 인정(`unfunny`→`funny` ✓ ·
   `intelligence`→`telligence` ✗). 오염된 BOUND 팔은 0 쪽으로 끌려 **가짜 음성**을 만들기 때문에
   재현율보다 정밀도를 택했다.

## 다음 — 🔑 **H_9304 의 한국어 B 비트는 자유와 결속을 섞고 있었다**
한국어 부정 폐집합 `{안, 못, 아니, 없, -지 않, -지 못}` 은 **자유·전치형(안, 못)과 결속 접미형(-지 않,
-지 못)을 한 비트에 뭉쳤다.** 이 카드가 맞다면 결속형이 수적으로 우세할 때 혼합 비트는 0 쪽으로 끌려간다
— **H_9304 가 읽은 +0.00233 이 바로 그 그림일 수 있다.**
① **NSMC 를 같은 방식으로 쪼개라**: KO-FREE(안/못 전치) vs KO-BOUND(-지 않/-지 못 접미). 한국어 자유팔이
   높게 읽히면 *"한국어 lane 이 죽었다"* 가 아니라 *"한국어의 결속형이 혼합비트를 끌어내렸다"* 가 되고,
   형태론 법칙이 **두 언어에서 독립 확인**된다. $0 · 라벨 코퍼스만 있으면 된다.
② 그 다음에야 데이터-스케일 사다리(행수↑ 시 EARNED 이 자라는가).

산출물: `~/.fire-recover/{free.json, bound.json, earned_free.log, earned_bound.log,
build_morph_split.py, sst2_free.tsv, sst2_bound.tsv}`.
