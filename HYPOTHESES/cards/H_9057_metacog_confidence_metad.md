# H_9057 — Type-2 metacognitive confidence calibration (meta-d'/d') of own emits

- **tier:** ⏳ PROPOSED (brainstorm-depletion 2026-07-02)
- **slug:** `metacog_confidence_metad`
- **frontier:** substrate-op (엔진-네이티브 새 능력 op — 재조합≠능력, a_no_llm_frame_trap)
- **cost:** cheap ($0 engine-native, live core/)
- **wired:** `PROPOSED` — 미측정, 미배선. 등록만(2-surface: 이 카드 + HYPOTHESES.jsonl).

## 메커니즘 (mechanism)

The substrate emits an internal confidence scalar (from A⇄G tension margin) that should predict whether the emit is correct/coherent — a type-2 (metacognitive) readout distinct from a forward model of Ψ. Capability = calibrated self-knowledge (meta-d' > 0), a self-faculty independent of text recombination.

## engine-native falsifiable metric (shuffle + ablation, p7)

Engine-native on live core/: over a coherent-vs-incoherent emit set, compute type-1 sensitivity d' and type-2 meta-d' from confidence. HIT if meta-d'/d' ≈ 1 (calibrated) and > SHUFFLE-null (confidence-label permutation → ratio→0); ABLATION zeros the tension-margin readout → calibration collapses. No LLM judge; correctness = engine-native coherence gate (G0 known-word-ratio).

> p7 준수: LLM-judge/perplexity 없음. 판정은 live `core/` 상태·디코드에서 산출된 수치 + shuffle-null + ablation-INERT 로만. 사전등록 bar frozen-first (사후 이동 금지, c9).

## why novel vs ledger

self_psi_forward_model predicts the Ψ TRAJECTORY (metacog surprise); this is type-2 confidence-vs-correctness calibration (meta-d'), an SDT self-faculty not in ledger; mirror-test is self/foreign anchor discrimination.

## disjointness (a_substrate_disjoint)

새 lane/op 은 emit-drive lane(0/4) 및 §ImmuneMemory recall_thr 와 **disjoint** 좌표에 배선(placement-first) — 능력 ∧ Ψ=½ ∧ G5 non-fab 공존 점검이 측정의 사전조건.

## 상태

- 출처: brainstorm-depletion 2026-07-02 (6-round ledger-dedup 생존).
- 다음 단계: engine-native 프로브 저작(.hexa via live core/) → frozen bar 측정 → verdict 박제.
