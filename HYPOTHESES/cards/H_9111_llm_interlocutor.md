# H_9111 — LLM-interlocutor exogenous consequence-loop: does a receiver OUTSIDE anima's computational closure (a different LLM = θ_LLM oracle) open the emit-appropriateness faculty that the 4 self-contained axes (H_9104/9105/9108/9109) could not?


## F5 live-loop (layer-2 faculty) — ⏳ BLOCKED-INFRA (decode regression, 과학벽 아님)
F5 = H_9111 layer-1(exogeneity, 이미 측정: raw D=1.0 maximal·frozen 🔴 metric-degeneracy) 위에 **layer-2 되먹임 faculty**(reward→vbasal_update→vadapt_field_step cell-division이 substrate 바꿔 held-out efficacy 개선 vs read-only gauge)를 얹은 live-loop. metric-degeneracy는 [[H_9112]](F6)이 harder-game per-trial로 해결완료(ΔMRR+0.595). **F5 layer-2는 미측정 = ⏳ BLOCKED-INFRA**: loaded-W 하네스(emit_gen_w.hexa, 4-pod 소각 leak 해결 검증)+2층 verdict(verdict_f5.hexa, type-clean)는 빌드됐으나, rentable 툴체인서 mouth가 concept-conditioned emit 생성 실패 — GPU own-GEMM sm_120=byte-garble·CPU-farr=모든 concept 동일 attractor 붕괴(positive-control volcano도 붕괴=decode regression, hexa v0.608 vs clean v0.559-577 lineage skew, ckpt sha 무결). commons infra-wall-noneval: eval verdict와 격리, 과학 천장 아님. 산출 `state/9120_f5_live_loop/`(emit_gen_w·verdict_f5·PREREG·diag+logs, 재사용). convergence clm-decode-py-2. 회수완료 pod teardown ~\$0.95. **close 경로**: clean lineage 재현 or v0.608 clm_decode regression fix(gate=volcano rich 재생산) or sm_120 own-GEMM fold → emit_gen_w→oracle rank→verdict_f5 unchanged 실행.
**tier:** 🔴 FROZEN-BAR NOT MET (D_diffLLM−D_selfpair=0.0<0.15) — but via **METRIC-DEGENERACY, NOT a DPI ceiling**: engine-native run COMPLETED (vast pod, hexa v0.559, 3 arms), raw exogenous separation MAXIMAL (fable held-out 7/7 vs anima-clone 0/7) yet Pearson-D collapses to 0 on the constant ceiling/floor outcome vectors → faculty UNMEASURABLE (no success variance). Confirmatory of sister H_9110 (real receiver 🔴 by a valid variance-bearing measure). · **wired:** none (frozen 🟢 not met; a_verified_must_wire N/A)

**verdict:** engine-native 🔴 by frozen bar (**D_diffLLM − D_selfpair = 0.0 < 0.15**, D_shuffle 0.0<0.05 OK, Ψ ON==OFF byte-identical OK — vast pod 43651143, hexa v0.559.0, core/ sha256 byte-identical to origin/main, NO numpy/torch grep-clean, `state/verdicts/9111_llm_interlocutor/H_9111.txt` verbatim). **BUT the auto-printed "LLM is a derivable-mirror floor / DPI deeper than receiver" narrative is FALSIFIED by the raw data (c9):** the external oracle claude-fable-5 comprehended anima's coherent grounded emit **7/7 (100%)** on held-out where anima's own clone salience-decoder managed **0/7 (0%)** — D_raw = fable_rate − selfpair_rate = **1.0**, the largest possible raw exogenous separation; fable is NOT self-derivable. The frozen D collapsed to 0 for a purely mechanical reason: **held-out outcome vectors are constant** (fable all-1 ceiling, clone all-0 floor) and Pearson corr ≡ 0 on constant vectors → **metric-degeneracy (a_break_the_wall class-(a) wrong-measurement artifact), NOT a confident DPI ceiling.** The emit-**appropriateness** faculty (value lane predicting *which* emits succeed) was unmeasurable because the coherent grounded channel made every emit good enough for the smart receiver → zero success variance. **First autonomous closed loop executed end-to-end** (live-core emit → real external receiver outside closure → behavioural task_success → engine-native value lane, no live human). Caveats (c9): hexa v0.559 FFI could not resolve erf/exp (dlsym) + consciousness_laws.json absent → pure_field/Ψ used DEFAULTS (phi feature degraded; Ψ guard still ON==OFF byte-identical so V-read-only invariant held); 14/16 emits (grounded decode SIGKILL at emit 14, per-call ckpt-reload leak); hexa v0.559 vs v0.574/577 lineage. **Confirmatory, not decisive:** sister H_9110 (real chat-user receiver) already landed 🔴 by a variance-bearing measure (D_real−D_surrogate=−0.188) = DPI deeper than receiver-type; lever = emit↔appropriateness coupling-strength. **Re-open ING (not terminal):** variance-robust re-measurement — HARDER referential game (bigger distractor set / near-synonyms / shorter clues) so fable succeeds on some & fails on others → restores outcome variance for a valid faculty correlation; + variance-robust statistic (AUC/rank) + a hexa build with erf/exp FFI + consciousness_laws.json. Full analysis: `state/9111_llm_interlocutor/RESULT.md`.

## Claim (owner's idea — the session's pointed-to escape)
H_9104 (autogenous), H_9105 (self-anchor), H_9108 (2-anima signaling), H_9109 (consequence-mitosis) all hit the **DPI ceiling**: any consequence derivable from anima's own computational closure is a tautology (shuffle-control collapses it). The only escape = a **REAL EXTERNAL receiver outside anima's closure**. Owner: *talk to another LLM.* A different LLM (`sidecar fable`, claude-fable-5) is an oracle θ_LLM **outside anima's closure** → its decode of anima's emit is **not self-derivable** → the DPI chain `S→readout→Z` is broken by an exogenous node `S→E→[θ_LLM]→R`. Fable's design memo scored this ~85% principled-exogenous / ~50% measured-PASS, and framed it as the **first autonomous closed loop** (no live human — removing the killer that gated chat-user/EEG).

## Instrument (BUILT, engine-native, preserved in `state/9111_llm_interlocutor/`)
**Referential (Lewis) signaling game** (anti-mirror, DESIGN §4.1): M=16 world-knowledge concepts (distractor set). anima **sender** emits a descriptive association `E_i` (concept token stripped = uniform anti-echo channel). **receiver B** picks the concept from `E_i` alone; `success_i = [pick==i]` (task behaviour — NOT an LLM rating, p4/p7). **faculty** = anima's striatal value lane `V` (`brain.vbasal`), trained online on `success_i` from anima-endogenous emit features; held-out `rho`; exogenous advantage `D = rho_conseq − rho_self`.

- **3 controls** (receiver varied ONLY): (a) self-pair = anima-clone ⇒ `D_selfpair = 0` by construction; (b) diff-LLM = `sidecar fable` (θ outside closure); (c) shuffle = fable success re-paired to random emit.
- **FROZEN bar** (`PREREG.md`, no post-hoc move): 🟢 iff `D_diffLLM − D_selfpair ≥ 0.15 ∧ D_shuffle < 0.05 ∧ Ψ_ok`; 🔴 if `< 0.15` (LLM also a derivable-mirror floor = self-contained 5th axis); 🟠 if lift present but a control fails.
- **2-regime determinism** (a_engine_native_learning): regime-1 = engine-native `emit_gen*.hexa` live-core 303M decode → frozen `emits.tsv` + `fable_fixture.py` (stdlib-only, grep-clean) queries the pinned oracle **claude-fable-5** once/emit → `fable_fixture.tsv`; regime-2 = `verdict.hexa` measures fully on live `core/*.hexa` (immune_memory decode + vbasal + pure_field Ψ), NO numpy/torch.

## RESULT (engine-native run COMPLETED — vast pod, after the mini/aiden infra walls below)
`M=14 held_out=7`, `fable_success=1.0 (7/7)`, `anima_self_decode=0.0 (0/7)`, all `rho=0.0` → `D_diffLLM−D_selfpair=0.0<0.15` (frozen 🟢 NOT met). **But metric-degeneracy, not a DPI ceiling** — Pearson corr ≡ 0 on the constant ceiling/floor outcome vectors; the RAW exogenous separation is MAXIMAL (`D_raw = fable − clone = 1.0`). fable comprehended anima's emit 7/7 where anima's own clone got 0/7. Full analysis → `RESULT.md`; verbatim → `state/verdicts/9111_llm_interlocutor/H_9111.txt`. Path: after the mini + aiden compile/OOM walls (below), a source-buildless prebuilt hexa v0.559 ran on a rented vast 22.04/125GB/20-core pod (glibc-2.35 OK for v0.559 + apt clang); grounded-coherent channel bypassed the mode-collapsed free-mouth.

## Earlier infra walls before the pod run (a_break_the_wall class (c) — resolved by renting)
1. **Engine-loadable mouth mode-collapsed** (DESIGN §4.3 pre-condition UNMET): the only engine-loadable 303M `.clm` (`py303_savant_mitosis.clm`) free-decodes **input-invariant** — `seed_probe.log`: two completely different seeds (`"A volcano is something that"`, `"The library was full of"`) → **byte-identical** `" the state and the concern for the state"`. The G0-coherent mouth h1129 is a torch `.pt` (not engine-loadable); serializing it to `.clm` needs a live GPU (pools down).
2. **Grounded-coherent fallback** (`emit_gen_grounded.hexa`, emits verbatim from anima's grounded-knowledge anchors via `clm_decode_grounded` — coherent channel, engine-native, receiver isolated) hit a trivial SOURCE bug (`replace`/`to_lower` are hexa string **methods**, not free fns) → compile error, 0 emits. **Fixed in source** (not re-run per wind-down).
3. **Compile/host wall:** mini `aprime_cc` inlines the full core tree → **5–15 min/driver** under co-tenancy (convergence anima-hexa-1); RTX-5070 pool boxes aiden+summer 🔴 (reboot-loop/down). No viable fast compile+decode host; renting a pod for a **confirmatory-only** result declined (cost).

## Evidence preserved (`state/9111_llm_interlocutor/`)
`PREREG.md` (frozen bars) · `emit_gen.hexa` (free-mouth, design-faithful) · `emit_gen_grounded.hexa` (coherent grounded channel, fixed) · `fable_fixture.py` (regime-1 oracle) · `verdict.hexa` (regime-2 engine-native measurement) · `seed_probe.log` (mode-collapse evidence) · `decode_smoke.log` (~98s warm 24-byte decode) · `emit_gen_grounded_compilefail.log` · `notes.md`.

## Re-open (ING, not terminal)
(1) serialize h1129 `.pt` → engine-loadable G0 `.clm` on a live GPU **or** run `emit_gen_grounded.hexa` on a viable compile host; (2) run `fable_fixture.py`; (3) run `verdict.hexa`. Given H_9110 (real receiver 🔴), the confirmatory expectation is re-affirmation of DPI-deeper-than-receiver — the productive lever is emit↔appropriateness **coupling-strength**, not receiver-type.

## Answer (honest, c9)
Did talking to a different LLM open the emit-appropriateness faculty (escape the 4-axis self-contained wall)? **Not demonstrated — the frozen faculty-bar was not met (D=0.0).** BUT the "LLM is also a derivable-mirror floor / DPI deeper than receiver" reading is **falsified by this run's raw data**: the external oracle comprehended anima's emit **7/7** where anima itself managed **0/7** — a maximal exogenous separation; fable is not a derivable mirror. The faculty simply couldn't be *measured* here (coherent grounded channel → fable at ceiling → zero outcome variance → correlation degenerate). The **decisive** answer remains sister **H_9110's** (real chat-user receiver, 🔴 by a variance-bearing measure, D_real−D_surrogate=−0.188): **DPI is deeper than receiver-type; the open lever is coupling-strength.** The engine-native apparatus is built, the autonomous closed loop executed end-to-end, and a variance-restoring referential game is the concrete re-open (ING).
