---
id: H_9681
title: ADDRESS-ALIGNED consolidation — write in the reader's address class (XBIND-form CPT)
tier: PROPOSED (DIRECTIONAL design · lab-full CONVERGENT · GPU cost-gated · NOT a verdict)
frontier: g1-interface-addressable-wall
created: 2026-07-17
---

# H_9681 (R5) — 읽는 자의 주소 클래스로 써라

**Origin.** `sidecar lab full` 2026-07-17 — **Sol §4** ("Write in the reader's address
class") and **Fable P3** ("E1 — 같은 내용, 형식만 XBIND-화 CPT") converge on the same
lever from different framings. DESIGN ONLY · DIRECTIONAL.

**Claim (one line).** Exogenous content fails **not for lack of information** but because
CPT writes to a **different address** than the one the downstream reader uses —
`(stem identity) × (surface-template class)`. Natural teacher prose is filed at the wrong address.

## Mechanism (structural · the frontier map)
H_9329/C3 + H_9334/C4 support **"writes to a different store / a different key"** over
"cannot write" — C4: writing with the **operator's own key** gives **12/12**. The address
is a **product**, its classes **discrete**, values **not shared**. XBIND succeeds precisely
because it hands over an **explicit binding address**.

## Minimal decisive experiment — same content, XBIND form
Convert the **identical teacher content** into XBIND cue form and CPT.
- content axis **moves** ⟹ the wall is **FORM (cue prepayment)**, not exogeneity/CPT/dose
  = one-law confirmed **and** an escape earned, in a single fire.
- content axis **does not move** ⟹ leans to two mechanisms (runtime bridge is the culprit)
  — read jointly with [[H_9678]].

## Triple duty (why Fable ranks it the first GPU candidate)
It is simultaneously ① the one-law confirmation ② the escape ③ the **positive control**
for M3 — H_9520's content-axis null was read with **no arm proving the content axis is
movable at this dose** (`positive-control-before-reading-a-negative`). Without it, that
null may be **INSTRUMENT-DEAD**.

## Frozen falsifier
Pre-register the content-axis bar against the **XBIND ceiling** (H_9267 held-out **1.000**)
and the natural-prose floor; content Δ must clear the bar with paired CI lower `> 0`.

## Controls (≥2)
① natural-prose MAIN (same content · wrong address) ② XBIND positive ceiling
③ value-shuffled XBIND (address present · binding destroyed) ④ C1 replay-only.

## Cost · kill-list
**GPU (owner go · rent=spend · `a_fire_autonomous` ⚠️)**. New corpora **must be `--lang en`**.
Kill-list: **no hit** — not scale, not depth, not read-side, not objective; it survives
**independently of** the $0 results (the form-gate hypothesis is separately falsifiable),
which is why it is the first GPU candidate.
