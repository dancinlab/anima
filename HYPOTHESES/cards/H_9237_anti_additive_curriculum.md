# H_9237 · anti_additive_curriculum

🔵 PRE-REGISTERED (unrun · γ 후보 레버 · census CLOSED=재확인/재오픈 트리거지 새 GREEN 사냥 아님)

## 클러스터 / 계보
클러스터 **A additive-불가능 타깃/생성자** · Fable γ 발산 #2 (state/g1_gamma_divergence/FABLE_DIVERGENCE.md).
부모 = G1 재조합 벽 프로그램(goal-biolens · g1-census-objfloor CLOSED · g1-metalaw-dpi-basin).
형제 = H_9225 residual-lift(#8, 🟡 측정설계 검증). 지배원리 = additive를 primary에서 **necessitate**
(보상항 offer 아님) — freeze-then-bolt 금지(H_9225 교정).

## mechanism
primitive A·B가 오직 additive 예측이 틀리는 문맥에서만 등장하도록 데이터 스케줄 — train에서도 additive를 벌함.

## why-beats-additive (DPI 함정 회피)
죽은 gen-split(MLC/SCAN)은 additive가 train 통과·held-out만 실패→모델이 train-최적=additive 안착. 여기선 train-최적 자체가 non-additive.

## 결합-파괴 통제 (THEATER 차단)
additive-baseline이 train-loss부터 못내려야(양성신호). shuffle→스케줄 붕괴.

## 측정 (engine-native scope)
데이터생성 numpy 방향 + curriculum효과 GPU. tune-to-green 금지 · frozen-first · 신호는 Δ+shuffle 대조. toy=DIRECTIONAL, 실 303M
byte-LM 검증은 GPU-gated. py 2-production numpy=TERMINAL-eligible, torch ad-hoc probe=DIRECTIONAL.

## 위험 / 예상 실패모드
모델이 additive+틀린-문맥 예외 lookup(memorized correction)으로 우회. 예외를 held-out 조합으로 강제확장 필요.

## 상태
PRE-REGISTERED(unrun). census CLOSED이므로 발사는 저비용 numpy 방향판정 우선 → 초록 시에만 GPU 상승.
lift≈0(shuffle과 무차별)=능력천장 종결 재확인(값진 negative).
