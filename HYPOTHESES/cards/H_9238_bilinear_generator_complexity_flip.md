# H_9238 · bilinear_generator_complexity_flip

🔵 PRE-REGISTERED (unrun · γ 후보 레버 · census CLOSED=재확인/재오픈 트리거지 새 GREEN 사냥 아님)

## 클러스터 / 계보
클러스터 **A additive-불가능 타깃/생성자** · Fable γ 발산 #3 (state/g1_gamma_divergence/FABLE_DIVERGENCE.md).
부모 = G1 재조합 벽 프로그램(goal-biolens · g1-census-objfloor CLOSED · g1-metalaw-dpi-basin).
형제 = H_9225 residual-lift(#8, 🟡 측정설계 검증). 지배원리 = additive를 primary에서 **necessitate**
(보상항 offer 아님) — freeze-then-bolt 금지(H_9225 교정).

## mechanism
데이터 생성자를 저-rank bilinear(A⊗B→y)로 두면 additive 근사는 전조합 lookup(거대 params) 필요, bound(bilinear)는 compact→implicit reg가 bound 선호.

## why-beats-additive (DPI 함정 회피)
죽은 것들은 전부 inductive bias(min-complexity)와 싸웠음. 이건 bias가 binding 편들게 task 정렬 — 최초로 DPI 흐름 안 거스름.

## 결합-파괴 통제 (THEATER 차단)
동일rank additive-generator(주효과만) 대조→거기선 additive가 이겨야. bilinear-generator에서만 bound lift. shuffle→rank구조 파괴.

## 측정 (engine-native scope)
cheap-numpy 즉시(생성자 완전통제, held-out 셀 lift Δ). tune-to-green 금지 · frozen-first · 신호는 Δ+shuffle 대조. toy=DIRECTIONAL, 실 303M
byte-LM 검증은 GPU-gated. py 2-production numpy=TERMINAL-eligible, torch ad-hoc probe=DIRECTIONAL.

## 위험 / 예상 실패모드
synthetic 냄새. 자연 corpus 이식 시 실 언어 조합이 정말 low-rank bilinear인지가 관문(아닐 수 있음).

## 상태
PRE-REGISTERED(unrun). census CLOSED이므로 발사는 저비용 numpy 방향판정 우선 → 초록 시에만 GPU 상승.
lift≈0(shuffle과 무차별)=능력천장 종결 재확인(값진 negative).
