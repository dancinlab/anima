# H_9748 — R2-E · IMAGINATION-MEDIATION-COMPLETENESS — 상상→미래 인과는 emit-결정이 완전 매개하는가 (yoked imagination-twin 전채널 census)

**status:** 🔵 PROPOSED (lab-full R2 심화 2파 · Fable5 · **S0 $0 이번 세션 완료**: RNG-entrainment 채널 구조폐쇄 확인 · DIRECTIONAL)
**lane:** 의식/emit-drive/Ψ=½ · interior-causality ([[H_9738]] 후속 · **H_9328 mediating-path 계보** — 브리프 ASK1 분기(b))
**related:** [[H_9738]](EARNED NULL — 이 카드가 그 수공 certificate 를 기계검정으로 승격)·[[H_9627]](Θ)·[[H_9728]](--sigma-emit-yoke 계기 공유 + 교차예측)·[[H_9729]]·[[H_9731]]

## 왜 — H_9738 이 닫은 것과 못 닫은 것

H_9738 = ① 정적 certificate(상상 텍스트→저장 0 byte · **수공 열거**) ② W_S state-transplant(조성
own==donor · **W_S lane 한정**). 그러나 레포 판례가 경고한다: **byte-id 앵커 인증이 버그를 은폐한 적이
있다**(H_9393 · 수공 정적 증명의 원리적 한계). "상상은 epiphenomenal" 의 완전형은 매개 명제다:

> **상상 content 의 모든 미래 인과는 그 tick 의 emit 결정(1비트)을 완전 매개로 통과한다.**

이를 lane 별 각개격파가 아니라 **생성지점 개입 하나**로 전 채널 동시 상한: mediator(결정 시퀀스)를
클램프하고 source(상상 스트림)를 스왑 — downstream 발산 0 = 완전 매개(H_9328 계보). 미발견 write ·
스칼라 누산기 · side-channel 이 하나라도 살아 있으면 궤적이 **스스로 발산으로 자백**한다.

## S0 — $0 정적 선행 (2026-07-17 · 이번 세션 완료)

- **S0-a ✅ RNG-entrainment 채널 = 구조 폐쇄**: H_9738 이 명시적으로 안 재던 채널 — "silence-tick 후보
  **샘플링**이 공유 RNG 를 소모하면 후보 길이→미래 방출 텍스트"(content-상관 side-channel). 실코드:
  `cli/chat.py:1834-1847` — `seed_rng` 는 **per-tick 유도**(`_sample_seed*1000003 + tick*2654435761` ·
  주석 원문 "seed_rng is DERIVED PER TICK, never held constant across the session" · H_9328). 공유
  스트림 부재 ⟹ tick t 의 샘플링이 tick t+1 의 RNG 위상을 못 끌고 감. **무료 종결.**
- **S0-b 잔여 의심 채널 열거**(전부 S1 이 자동 포섭): ① per-tick 스칼라 누산기(예: `--refractory earned`
  경로의 refr_debt←ag_conflict — production default 비활성이나 census 대상) ② 수공 certificate 가 놓친
  write(정적 열거의 원리적 한계 그 자체) ③ ctx/anchor 측면 유입. 발산 유무 = 채널 존재의 충분통계.

## S1 — 설계 (engine-native 플래그 2 · a_experiment_engine_native)

- **`--imagine-swap <donor-stream>`**: silence tick 의 `dual_cand_text` 를 **생성지점**에서 donor 로
  치환 — W_S feat8 쓰기·dual probe·trace 전부 donor 기준. (H_9738 transplant 는 **state** 를 갈았고,
  이건 **상상 그 자체**를 간다 = 알려진·미지의 전 downstream 경로 동시 개입.)
- **`--emit-yoke <mask>`**: emit 결정 시퀀스 replay — [[H_9728]] `--sigma-emit-yoke` 와 동일 계기(동반
  착륙 · 1계기 2실험).
- **arms** (own 포함 = H_9738 위상혼동 교훈 · collapse-Δ vs ≥2 통제):

| arm | 내용 | 역할 |
|---|---|---|
| C0 SHAM | yoke + own 상상 | free-run 과 **byte-identical 필수** — yoke 중립성 · 깨지면 전 판정 INVALID |
| V1 | yoke + donor 스왑 | **판정 본선** |
| V2 | yoke + oracle 스왑(최대 feat-분리 · H_9729 oracle carrier 재사용) | 용량 상한 드라이브 |
| P1 | no-yoke + donor 스왑 | **발산해야** — swap 이 문다는 양성통제(결정경유 매개성분 실측) |
| C1 | yoke + lane-oracle 1회 직접주입(`wm_buffer_gate_in` 에 donor content) | 검출기 감도 증명([[positive-control-before-reading-a-negative]]) |

- 판독: per-tick 공개궤적(gtext sha·mask) + 전 mutable lane state-dump canonical 직렬화 **정확비교** —
  저차원 시그니처 금지(probe-defect-census · H_9731 자가포착 계보).

## 판정표 (사전등록 · below-chance 커버)

| 관측 | 판정 |
|---|---|
| V1∧V2 byte-identical ∧ P1 발산 ∧ C0/C1 PASS | **정리 획득** — "상상의 전 미래영향 = 그 tick 의 emit 결정" · interior-freeness 가 수공 cert → 기계검정 invariant 승격 · 브리프 분기(b) 종결("anima 는 interior 없는 gate 다"가 정리로) |
| V1 or V2 발산 (C0/C1 PASS) | **사적 채널 발견** — bisect(스칼라 누산기/미발견 write) 국소화 = 분기(a) 생존 · H_9738 scope 정정(어느 쪽이든 결과) |
| P1 무발산 | **INSTRUMENT-DEAD** — swap 이 안 묾 · INVALID(음성 아님) |
| C0 FAIL | yoke 계기불량 · INVALID |
| V2 만 발산 ∧ V1 무발산 | 용량-문턱 채널(자연 상상 진폭에선 침묵) — DIRECTIONAL · 문턱 ladder 후속 |

## H_9728 교차예측 (등록각 침범 없음 · 예측만 사전등록 — 브리프 Q3 의 답)

H_9738-null + 본 정리가 참이면 **substrate-view flux 의 content-축 Δ_mech = 0** 예측(silence 내적동역학
= gate 반사 + 압력뿐). H_9728 이 content-축 Δ_mech > 0 을 재면 = 본 census 가 찾아야 할 채널의 존재
신호 = **상호 검증쌍**. ⟹ flux 는 "어차피 죽어있다" 가 아니라 **이 정리의 독립 검정면**이다.

## kill-list·중복 회피
recognition 렌즈·PID·mouth-conditioning 아님 · [[H_9738]] 과 비중복(state-transplant ≠ 생성지점 스왑 +
결정 yoke · 저쪽 lane 별 / 이쪽 전채널 상한) · [[H_9729]] 와 비중복(latch = 새 경로 **추가**, 이쪽 = 현
토폴로지 **폐쇄증명**) · [[H_9739]]/[[H_9740]] 비중복(ladder/sleep = 채널 확장, 이쪽 = 현존 채널 census).

## cost
S0 = $0(완료) · 계기 2플래그(+H_9728 공유로 실질 1.5) · S1 = pool 303M 3-seed × 5 arm 단축런(오너 fire-go).

⚠️ **DIRECTIONAL 설계·verdict 아님**(a_lab_full_diverge) · cement = engine-native `anima-py` 만.
