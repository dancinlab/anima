# H_9236 · xor_primary_target

🔵 PRE-REGISTERED (unrun · γ 후보 레버 · census CLOSED=재확인/재오픈 트리거지 새 GREEN 사냥 아님)

## 클러스터 / 계보
클러스터 **A additive-불가능 타깃/생성자** · Fable γ 발산 #1 (state/g1_gamma_divergence/FABLE_DIVERGENCE.md).
부모 = G1 재조합 벽 프로그램(goal-biolens · g1-census-objfloor CLOSED · g1-metalaw-dpi-basin).
형제 = H_9225 residual-lift(#8, 🟡 측정설계 검증). 지배원리 = additive를 primary에서 **necessitate**
(보상항 offer 아님) — freeze-then-bolt 금지(H_9225 교정).

## mechanism
A+B 타깃을 marginal-비정보(E[y|A]=E[y|B]=chance)·joint-결정적 XOR/parity로 설계 — aux 없이 primary next-byte 예측 자체가 joint 요구.

## why-beats-additive (DPI 함정 회피)
additive 해의 상한이 task상 chance로 수학적 고정(보상항 아니라 additive에 천장을 씌움). 죽은 additive-aux는 primary가 additive-solvable이었음.

## 결합-파괴 통제 (THEATER 차단)
예제간 (A,B) 쌍 shuffle→label chance 붕괴(정의상 내장). 양성대조=동일데이터 additive-solvable label 버전은 통과해야(FORM 살아있음).

## 측정 (engine-native scope)
cheap-numpy 방향 + GPU(held-out 커버리지로 lookup 배제 규모). tune-to-green 금지 · frozen-first · 신호는 Δ+shuffle 대조. toy=DIRECTIONAL, 실 303M
byte-LM 검증은 GPU-gated. py 2-production numpy=TERMINAL-eligible, torch ad-hoc probe=DIRECTIONAL.

## 위험 / 예상 실패모드
byte-LM이 XOR을 전조합 lookup-table 암기로 풀면 held-out 붕괴=재조합 아닌 암기실패. held-out 조합 커버리지가 관문.

## 상태
PRE-REGISTERED(unrun). census CLOSED이므로 발사는 저비용 numpy 방향판정 우선 → 초록 시에만 GPU 상승.
lift≈0(shuffle과 무차별)=능력천장 종결 재확인(값진 negative).
