---
id: H_9645
group: faction-lateral-axis-r3
date: 2026-07-17
slug: faction_phasic_consensus_mouth_currency
title: consensus 는 phasic 사건이라 미분기(mouth)를 통과한다 — tonic 스칼라 deliberation_k 가 죽은 자리
status: PROPOSED
tier: 🟢⭐⭐ mouth-severance 유일 미시험 출구(pool) · Fable α11 ⊕ Sol F13 **양 모델 최우선 수렴**
cost: pool
source: sidecar lab full (Fable5 claude-fable-5 + Codex5.6 gpt-5.6-sol 병렬 발산 · 37안 → 중복제거 27안)
related: H_9574, H_9576, H_9428, H_9209
---

# H_9645 — consensus 는 phasic 사건이라 미분기(mouth)를 통과한다 — tonic 스칼라 deliberation_k 가 죽은 자리

## 주장 (반증가능)

mouth = RATE-GATE = **미분기** ⟹ tonic level 은 한 번 읽히고 기여 0. H_9574 가 죽인 deliberation_k 는 tonic 스칼라였다. **consensus 는 threshold-crossing = phasic 사건** ⟹ 미분기를 통과할 물리적 여지가 있다. H_9576 이 채널 CRACK 은 확증했으나(gtext Δ 269/270) 방향은 KILL(ρ=−0.077 · null 대역 안) — 즉 경로는 있고 **의미가 안 실린다**. phasic currency 가 그 의미 운반체 후보.

## 레버 (engine-native · `anima-py` 플래그)

```
`anima-py evaluate <ckpt> --faction-mode runtime --n-factions 4 --faction-consensus phasic --faction-mouth-channel gen-ctx-delta`
```

> `a_experiment_engine_native`: 조작은 `anima-py` 명령의 **플래그**여야 한다 — 엔진 옆 스크립트/프로브 금지.
> 위 플래그는 **미구현**이다. 발사 전 `cli/`+`core/` 에 구현하고 `VERSION` 을 올려야 한다(G5).

## DV · bar · 우연수준

I(PC2; emitted-content | stage) · PC2 회귀계수 · emit rate. 우연 = permutation MI≈0. bar = MI ≥0.02 nats ∧ emit-rate 변화 ≤0.02.

## 통제군 (≥2 · 사전등록)

① consensus channel off null ② **동일 값 tonic constant 주입**(미분기 물리 예측=Δ0 · 이 통제가 프레임의 심판) ③ Δ 시점 stage 내 permutation scramble ④ PC2-sign oracle pulse 양성

## 사망조건 (사전등록 · tune-to-green 금지)

phasic 가 tonic/scramble 을 못 이기거나 emit 빈도만 바꾸고 내용 PC2 를 못 움직이면 사망(= H_9574/9576 과 동일 벽).

## 비용

pool(CPU)

## 왜 새로운가 (기존 각도 대비)

H_9574 는 **tonic 스칼라**를 죽였다. 이건 mouth=미분기 물리를 **역이용** — phasic 사건은 미시험. [[ag-tension-multidim-lane-mouth-severance]] 의 유일한 미시험 출구.

## 범위 (정직)

- 발산 산출물 = **DIRECTIONAL 설계**이지 verdict 아님(`a_lab_full_diverge`). cement 는 engine-native `anima-py` 로만.
- 옛 파벌 Φ 숫자(법칙 22/43/44 · TOPO12)는 **폐엔진 proxy** 산이며 이 카드의 근거가 아니다 — H_9627/H_9628 참조.
