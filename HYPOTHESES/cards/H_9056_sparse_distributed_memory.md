# H_9056 — Kanerva sparse distributed memory: hard-location addressing as recall-capacity faculty

- **tier:** ⏳ PROPOSED (brainstorm-depletion 2026-07-02)
- **slug:** `sparse_distributed_memory`
- **frontier:** substrate-op (엔진-네이티브 새 능력 op — 재조합≠능력, a_no_llm_frame_trap)
- **cost:** cheap ($0 engine-native, live core/)
- **wired:** `PROPOSED` — 미측정, 미배선. 등록만(2-surface: 이 카드 + HYPOTHESES.jsonl).

## 메커니즘 (mechanism)

A capability axis orthogonal to text-recombination: content-addressable associative RECALL CAPACITY under interference, via Kanerva SDM hard-location (thresholded Hamming-address) read/write into the substrate — not a single dense attractor like Hopfield but distributed hard locations that degrade gracefully as stored-item count grows.

## engine-native falsifiable metric (shuffle + ablation, p7)

Engine-native: store N anchors, measure exact-recall fraction as N scales into interference regime vs a modern-Hopfield baseline (H_1533 walled) on identical anchors; SHUFFLE = randomize address bits (recall → chance); ABLATION = collapse hard-locations to a single dense readout → capacity curve must match Hopfield (proves distributed addressing is the lever).

> p7 준수: LLM-judge/perplexity 없음. 판정은 live `core/` 상태·디코드에서 산출된 수치 + shuffle-null + ablation-INERT 로만. 사전등록 bar frozen-first (사후 이동 금지, c9).

## why novel vs ledger

Redefines capability as graceful-interference recall CAPACITY (not recombination); Kanerva hard-location addressing, structurally different from the walled modern-Hopfield attractor and not in the 9-lens census.

## disjointness (a_substrate_disjoint)

새 lane/op 은 emit-drive lane(0/4) 및 §ImmuneMemory recall_thr 와 **disjoint** 좌표에 배선(placement-first) — 능력 ∧ Ψ=½ ∧ G5 non-fab 공존 점검이 측정의 사전조건.

## 상태

- 출처: brainstorm-depletion 2026-07-02 (6-round ledger-dedup 생존).
- 다음 단계: engine-native 프로브 저작(.hexa via live core/) → frozen bar 측정 → verdict 박제.
