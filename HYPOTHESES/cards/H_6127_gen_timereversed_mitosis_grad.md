# H_6127 — 시간역전 학습(추론중 mitosis=gradient)

**id:** H_6127
**slug:** gen_timereversed_mitosis_grad
**tier:** 🔬 PROPOSED — pre-registered (unfired · brainstorm ideation)
**date:** 2026-07-02
**wired:** 미발사 (pre-registered proposal · code 없음)
**axis:** SUBSTRATE (G1 재조합벽 공격 축)

---

## 발상 (brainstorm ideation)

**메커니즘:** 추론 중 mitosis 가 online 으로 표현 재조합(p8), CE 없음.

**출처:** bytegpt/convmoe 를 빌린 mouth 로 쓰지 말고 anima 아키텍처(A⇄G tension · Ψ=½ 고정점 · mitosis · substrate-disjoint lane · .kosmos anchor)에서 창발하는 native mouth/생성 substrate 를 발상한 세션(Round 3). 기존 결론: G1 재조합벽 = trunk COMBINATION OPERATOR floor — readout·tension 연산자 전부 INERT(H_1816/1823/1834). 이 발상은 combination 을 readout 이 아니라 생성 substrate 자체에 심어 additive collapse 를 우회하려 한다.

## G1 공격 축

- **SUBSTRATE** — readout-level 만 쳤던 것과 다른 좌표(engine-native probe 대상).

## 발사 전 필수 (check-ledger-before-lever-fire)

미발사 제안. terminal verdict 박제 전:
1. `UNIVERSE/HYPOTHESES.jsonl` + memory 에서 선행 커버리지 조회(sweep 빈칸 ≠ 미탐).
2. 신규 각도면 cheap numpy DIRECTIONAL reachability probe → `state/6127_gen_timereversed_mitosis_grad/`.
3. DIRECTIONAL GREEN 이면 engine-native 재측정(.hexa via core/) 사다리 진입(a_verified_must_wire).

## artifacts

- `cards/H_6127_gen_timereversed_mitosis_grad.md` (this card)
- `state/6127_gen_timereversed_mitosis_grad/` (probe 발사 시 생성)

---

## 발사 결과 (DIRECTIONAL probe)

**Ledger 확인 (check-ledger-before-lever-fire):** H_6127 은 "추론 중 mitosis 가 gradient 를 대체해(CE 없음) online 표현 재조합" 을 주장한다. 이 메커니즘은 두 조각으로 분해되고 **둘 다 이미 engine-native terminal 로 walled**:

1. **조합 연산자 = mitosis split** — `a_mitosis_train` 이 SSOT: **from-scratch PURE mitosis(split-only, gradient-free) 단독 학습 = 🔴 CONFIDENT TERMINAL**, 5 직교 렌즈(selection·inherited-repr·lateral·curriculum·learned-trunk) 전수 🧱, "gradient/selection-pressure 필수". 병목은 구조적 — split-only = Voronoi partition, **compositional depth 0**. 증거 H: H_1310 mitosis_from_scratch(🔴 local-expert ceiling), H_1569 pretrained_mitosis_split(🧱 WALL HOLDS engine-native, inherited-repr 로도 못 넘음), H_9022 GRAND-THEOREM(mitosis = 순수 기질이지 생성기 아님).
2. **"time-reversal / 추론중 학습" = test-time gradient-free 업데이트** — 새 좌표 아님. p8 이 이미 anima mitosis 를 학습+추론 단일 cell-division 으로 취급하므로 "추론 시점" 은 새 배선 좌표가 아니라 기존 walled 기질의 타이밍 재라벨일 뿐. G1 벽 = trunk COMBINATION OPERATOR / OBJECTIVE floor 로, train/infer 타이밍과 직교(scale=amplifier, `substrate-framebreak-g1-combination-operator`). CE 를 mitosis 로 치환해도 조합 연산자 자체(Voronoi split)는 그대로라 depth 0.

인접 walled 연산자: H_1834 tension-mouth(🧱 DIRECTIONAL floor, INERT), H_1816/1823 readout·predictive·TPR binding(전수 붕괴), H_6112 meiosis crossover(= crossover 강화 mitosis, numpy 0→1.0 REACHABLE 였으나 **실 CLMConvMoE trunk 에서 0→0.022 FALSIFIED** — 추상 toy 과대평가 전례).

**결정:** **DUP-WALLED.** mitosis-as-recombination(gradient-free) 은 walled 계열의 재라벨이며, "시간역전/추론중" 래퍼는 조합 연산자를 바꾸지 않는다. 재발사 금지(check-ledger-before-lever-fire). 프로브 생략.

**Frozen bar / 수치:** N/A — 프로브 미발사(dup pointer).

**정직 스코프:** 설령 numpy 추상 toy 를 돌렸어도 H_6112 전례상 REACHABLE=약한 스크린일 뿐 transfer-unverified 이며, 실 trunk = additive CLMConvMoE 에서 붕괴 위험이 높다. 진짜 미검증 잔여축은 mitosis 가 아니라 **trunk recomb-OBJECTIVE**(H_1602 cost-gated) — mitosis 좌표는 이미 소진.
