# H_9933 · GRAFT 받침대 arm — **C-swap 은 포화가 아니라 비판별이다** (H_9932 의 대조군 부록)

**한 줄:** H_9932(병렬 세션 · #4506)는 학습본만 `graft check` 에 통과시켜 "swap 은 천장에 핀돼 무정보"라고
**추론**했다. 여기서는 **학습을 한 번도 안 한 step-0 받침대를 같은 check 에 통과**시켰고, 그 결과
받침대가 swap 정확도 **0.750~0.875**(우연 0.125) · perm p **0.0010** 을 받는다 — swap 은 포화된 게 아니라
**학습 여부를 판별하지 못한다**. 동시에 ablation 의 판별력은 대조군으로 **증명**된다(받침대 0.51~0.63× vs
학습 3.71~6.32×, 구간 비겹침).

- `a_parallel_session_compare` — H_9932 를 읽고 등록. **AGREES**: MI 0.2182/0.2772/0.2494 · 받침대
  0.0250/0.0228/0.0262 · lift +0.1932/+0.2543/+0.2232 · organ parity 7.629e-06 · `MI≥0.5` 0/3 미달 ·
  바를 내리지 않음 · 지탱축은 ablation 과 pedestal-lift 둘뿐 (수치까지 동일 = 같은 코드·같은 seed).
  **CONFLICTS**: seed3 학습본 swap 1.000(여기) vs 0.875(H_9932), ablation seed2/3 3.74×/3.71× vs
  4.05×/6.32× — check 시점 난수 차이일 뿐 결론 동일. **NOVEL**: 받침대 arm 전체(아래).
- **regime `no-corpus`** · **scope TOY**(`trained57` 117 KB · d=64 · L=2) — p9 상 **계기 검증**이며
  faculty 근거로 인용 불가. `a_toy_scale_recheck`: 종결 아님.

## 받침대 arm — 이번에 새로 잰 것

| arm | swap acc | MI_swap (bits) | perm p | ablation KL(ON‖OFF)/noise-q95 |
|---|---|---|---|---|
| seed1 학습본 | 1.000 | 3.000 (천장) | 0.0010 | **6.32×** |
| seed1 **받침대** | **0.875** | 1.927 | **0.0010** | **0.55×** |
| seed2 학습본 | 1.000 | 3.000 (천장) | 0.0010 | **3.74×** |
| seed2 **받침대** | **0.750** | 1.189 | **0.0010** | **0.63×** |
| seed3 학습본 | 1.000 | 3.000 (천장) | 0.0010 | **3.71×** |
| seed3 **받침대** | **0.750** | 2.216 | **0.0010** | **0.51×** |

(우연 acc 0.125 · K=8 · probes 4 · 999 perm · 받침대 = `<out>.step0.clm`, 학습본과 **동일한 구조 bound** 를
가진 무작위-초기화 결합)

## 왜 이렇게 되나 — 기전은 사전에 지목돼 있었다
각 연속열은 **자기 상태의 분포에서 뽑혀 같은 분포로 채점**된다. 따라서 충분히 큰 **아무** 상태-의존
게이트나 대각 우도 이점을 수확하며, 이는 학습 여부와 무관하다(Sol 사전-부검 · `cli/graft.py` 상단에
박제). H_9932 의 "천장 핀 = 무정보" 진단은 방향이 맞았지만 **강도를 과소평가**했다: 무정보(상수)가 아니라
**적극적 오판**(학습 0회를 0.875 로 통과시킴)이다.

## 이 부록이 바꾸는 것
1. **swap 단독 = 판정표에서 제거**(H_9932 의 "K 확대로 포화 해제 또는 제거" 갈래 중 **제거** 확정 —
   K 를 키워도 대각 이점은 그대로라 받침대가 같이 올라간다).
2. **ablation-vs-noise = 매칭 대조군으로 검증된 판별자**(가정이 아니라 실측). 받침대의 게이트는 같은 노름의
   난수보다도 **조용하다**(0.5× 대).
3. **모든 GRAFT 판정에 받침대 arm 의무화** — `fit` 이 `<out>.step0.clm` 을 항상 쓰는 이유가 여기서 값을 했다.
   받침대를 check 에 안 돌리면 그 계기가 무엇을 재는지 알 수 없다
   (`positive-control-before-reading-a-negative` 의 쌍대: **음성 받침대 없이 양성 읽지 말 것**).

## 판정
🟡 **DIRECTIONAL(토이) · 계기 1개 KILL · 판별자 1개 EARNED**
결합 자체의 판정은 H_9932 그대로 유지(GRAFTED 0/3 · py303 승급 안 함 · 바 유지). 이 카드는 **판정표를
고친다**: swap 삭제, ablation 승격, 받침대 arm 의무화.

산출물 `~/.fire-recover/graft_toy_3seed/`(ckpt 6 + json 3 + 로그) · 명령
`anima-py graft check /tmp/graft_s<N>.clm.step0.clm --k 8 --probes 4 --seed <N>`.
