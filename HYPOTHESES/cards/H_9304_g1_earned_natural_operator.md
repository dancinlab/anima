# H_9304 — G1-EARNED: 자연 코퍼스에 전이 가능한 재조합 연산자가 실재하는가

**Tier: 🧱 DATA-ADDITIVE (코퍼스-사실 · 추정기/학습 무관) · group g1-crack-natural-emergence · 2026-07-14**

- freeze → `state/h9304_g1_earned/FREEZE.txt` (발사 전 동결) · 계기 설계 = Fable 5
- script → `state/h9304_g1_earned/h9304_g1_earned.py`
- result → `state/h9304_g1_earned/results/{h9304_summary.json, run.log}`
- 선행 → H_9267 🟢🟢 XBIND CRACK · H_9286 🧱 GROUNDING-WALL · H_9265 🧱 instrument NOT-CERTIFIED
- 자매 → H_9298/H_9301 🟢 MITOSIS (추정기-계급 법칙 — 이 카드가 그 법칙의 **사정거리**를 정한다)

## 아무도 안 물은 질문

MITOSIS 캠페인의 구조가 이것이었다:

```
H_1336  "정보가 실재한다" 를 추정기와 무관하게 먼저 증명 (EARNED · pairing-파괴 통제)
   ↓                        ← 이 단계가 있었기에
H_9298  그제서야 그 정보를 담을 추정기(WB shrinkage)를 찾아 벽을 뚫음
```

**G1 에는 첫 단계가 없다.** H_9265 가 유일하게 근처까지 갔으나 verdict 원문이 **"instrument NOT-CERTIFIED · 측정불가 NOT NEGATIVE"** — 계기가 죽어서 답이 안 나온 것이지 '없다'가 아니다. H_9286 은 *학습된 모델이 배웠나*를 물었지 *코퍼스 통계에 그게 있나*를 묻지 않았다 (다른 질문).

⇒ **정보가 없으면 어떤 추정기도 영원히 못 담는다.** 이 카드는 그것을 먼저 답한다.

## 조작적 정의 (XBIND 의 자연 대응물 · 렉시콘 의존 0)

| XBIND | 자연 (NSMC) |
|---|---|
| atom a (숨은 극성) | 어간 **A** — 숨은 극성 = **학습되는 계수 α_A** (외부 감성사전 불필요) |
| atom b | 부정 비트 **B** ∈ {안, 못, 아니, 없, -지 않, -지 못} — 폐집합 **사전등록** |
| target = xor(pol) | **T** = NSMC 인간 별점 라벨 (0/1) — **토큰 스트림 외부** |
| held-out (a,b) 셀 | **(A, B=1)** 중 그 A 가 TRAIN 엔 **비부정으로만** 등장한 것 |
| 연산자 = xor | "부정은 **어간과 무관하게** 극성을 뒤집는다" |

```
logit P(T=1 | A,B) = α_A + γ_B + δ·(B · α_A)
                                  └─ δ=0 → 가법 · δ=−2 → 완전 flip (= XBIND 의 xor)
δ 는 다른 어간들의 B=1 셀에서 학습되는 전역 파라미터 1개 = 전이되는 연산자.
held-out 어간의 (A,1) 셀은 추정에 절대 안 들어간다.
```

🔑 **T 누출 차단**: A·B·T 를 전부 같은 토큰 스트림에서 뽑으면 "trigram > bigram-product" 는 **구성상 참**(동어반복) — 실험이 아니다. NSMC 인간 라벨은 토큰 외부라 이 함정을 구조적으로 회피한다. **⇒ 순수 web/wiki 로는 이 질문을 물을 수 없다** (라벨 코퍼스 필수 · 정직 제약).

코퍼스: NSMC train 149,995 행 · sha256 `e03b7d14…` · 부정률 0.204 · $0 summer RTX5070 · wall 95s.

## 계기 인증 — BLOCKING (주 bar 판독 前 · 하나라도 실패 → 계산조차 안 함)

이 lane 에서 **계기가 죽은 전례 2건**(H_9265 · H_9303)이 있다. "죽은 프로브의 침묵은 아무것도 증명하지 않는다."

| 게이트 | 참값 | 측정 | 판정 |
|---|---|---|---|
| **G-ALIVE** (합성 XOR 양성대조) | flip 실재 | EARNED **+5.29653** nats · **δ̂ = −3.00** | ✅ PASS (bar ≥ +0.30) |
| **G-PEDESTAL** (합성 가법, 참값 0) | 시너지 **0** | EARNED **+0.00285** nats · δ̂ = −0.10 | ✅ PASS (\|·\| ≤ 0.02) |
| **G-POWER** (held-out 인구조사) | — | 35,911 셀 · sd_null 0.000197 · **MDE(3σ) 0.00059** | ✅ PASS (≤ 0.02) |

### ⚠️ 인증이 잡아낸 계기 결함 2건 (bar 무이동 · 주 bar 판독 前에 수리)

1. **통제 결함** — B 를 평범하게 순열하면 A–B 짝뿐 아니라 **B→T 채널까지 파괴**된다. 참값 0 인 가법 아암이 **−0.618 nats** 를 읽었다. → **T-층화 셔플**(같은 T 안에서만 B 순열)로 수리: `P(B,T)` 를 정확히 보존하고 A–B 짝만 깬다 (`control-must-match-mediating-covariate`).
2. **추정량 결함 (더 근본)** — 첫 `M_op` 가 flip 을 **하드코딩**(δ = −2)했다. 가법 아암에선 flip 이 *적극적으로 틀린* 예측이라 CE 가 크게 나빠져 EARNED 가 0 이 아니라 **−0.704** 가 됐다 — **방향 가설을 측정량에 심어버린 것**. → δ 를 **데이터에서 학습**하도록 수리하니 참값 0 아암이 정확히 **+0.003** 로 중심을 잡았다.

⇒ **참값 0 PEDESTAL 이 없었으면 두 결함 모두 통과했을 것이고, 나는 −0.6 을 '자연엔 반(反)시너지가 있다'로 오독했을 것이다.** (`phi-estimator-needs-zero-truth-pedestal`)

## 결과 — 🧱 DATA-ADDITIVE

| | seen 시너지 | **held-out EARNED** | δ̂ |
|---|---|---|---|
| **자연 (NSMC)** | +0.00619 (n=73,959) | **+0.00233** · 90% CI [+0.00209, +0.00260] | **−0.35** |
| 합성 XBIND (**동일 인증 계기**) | — | **+5.29653** | −3.00 |
| 등가 마진 Δ_eq (사전등록) | ±0.02 | ±0.02 | — |

- **TOST(±0.02) PASS** ⇒ held-out EARNED 는 **0 과 등가**. INFO-PRESENT = FALSE.
- **seen 시너지도 +0.006 로 등가 마진 안** ⇒ OPERATOR-ABSENT(비가법성은 있는데 전이만 안 됨)가 **아니다**. 자연 코퍼스엔 **비가법 정보가 collocation 수준으로도 사실상 없다** — Fable 의 사전 예측(OPERATOR-ABSENT)보다 **강한** 결과.
- 같은 계기가 XBIND 에선 **+5.30** ⇒ **2,280배** 차이.

### 정직한 뉘앙스 (숨기지 않음)

**δ̂ = −0.35 는 0 이 아니고 방향도 맞다** — 자연 부정은 실제로 극성을 *부분적으로* 뒤집는다. EARNED 의 CI 도 0 을 배제한다(+0.00209 ~ +0.00260). ⇒ 효과는 **통계적으로 확실히 실재하나 실용적으로 무(無)**. 검정력이 워낙 커서(MDE 0.0006) 0.002 nats 같은 티끌도 유의하게 잡히는 것이지, 크기는 벽을 넘기에 **2,280배 부족**하다. 이것이 'ns' 가 아니라 **사전등록 TOST** 로 판정해야 하는 이유다.

## 결론 — MITOSIS 법칙의 사정거리가 확정됐다

> **두 법칙은 직교한다. 유비가 아니라 실측으로.**

```
        MITOSIS (H_9298)              │        G1 (이 카드)
   ─────────────────────────          │   ─────────────────────────
   정보 실재 (+0.076 EARNED)          │   정보 부재 (+0.002 ≈ 0)
   추정기가 못 담고 있었음             │   담을 것이 없음
   → 추정기 계급을 바꾸니 뚫림         │   → 어떤 추정기도 영원히 무력
   축 = {추정기 계급}                  │   축 = {measure 안 신호의 존재}
```

- **G1 벽의 정체 격상**: 종래 문구 "corpus×CE 결합 measure" → **"자연 코퍼스에 재조합 연산자가 실을 정보 자체가 없다. H_9267 XBIND crack 은 자연이 공급하지 않는 연산자를 우리가 주입한 것이다."**
- **shrinkage 를 G1 에 이식하려는 시도는 사전에 사망**했다 (담을 정보가 없으므로). 추정기 탐색은 정당화되지 않는다.
- **reopen 조건**: held-out 로 전이되는 비가법 정보를 **실제로 담은** 자연 코퍼스 × task class 의 발견. 이 측정이 그 탐색의 **저비용 사전 스크리너**가 된다 (라벨 코퍼스만 있으면 $0 · 95s).

## HONEST (c9 · a_scale_honest_scope)

- **코퍼스-사실 측정**이지 학습 verdict 가 아니다 — 303M 불필요 · engine-native 요구 대상 아님 ($0 numpy).
- 이 카드는 "정보가 있는가"만 답한다. "모델이 배울 수 있는가"는 별개이며 H_9286 이 이미 음성.
- **단일 구성** 사전등록(부정-플립 × 감성극성): researcher-DoF 차단. 2번째 구성 시도 시 Bonferroni 명시 의무.
- **단일 도메인**(NSMC 영화 리뷰) · 단일 언어(한국어). 도메인/언어 일반화 주장 **없음**.
- bar 발사 후 이동 0. 계기 결함 2건은 **주 bar 판독 前**에 잡혀 수리됐고 그 사실을 verbatim 보고했다.
