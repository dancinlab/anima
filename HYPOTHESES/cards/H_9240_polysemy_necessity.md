# H_9240 · polysemy_necessity

🔵 PRE-REGISTERED (unrun · γ 후보 레버 · census CLOSED=재확인/재오픈 트리거지 새 GREEN 사냥 아님)

## 클러스터 / 계보
클러스터 **A additive-불가능 타깃/생성자** · Fable γ 발산 #5 (state/g1_gamma_divergence/FABLE_DIVERGENCE.md).
부모 = G1 재조합 벽 프로그램(goal-biolens · g1-census-objfloor CLOSED · g1-metalaw-dpi-basin).
형제 = H_9225 residual-lift(#8, 🟡 측정설계 검증). 지배원리 = additive를 primary에서 **necessitate**
(보상항 offer 아님) — freeze-then-bolt 금지(H_9225 교정).

## mechanism
A를 다의어로 설계(뜻이 B에 의존)→A 확정하려면 반드시 B 참조=강제 joint(word-sense disambiguation 그 자체).

## why-beats-additive (DPI 함정 회피)
additive는 A뜻을 B와 독립으로 고정→체계적 오답. 자연어에 실재하는 구조(가장 corpus-친화적).

## 결합-파괴 통제 (THEATER 차단)
B 랜덤 치환→A뜻 오확정 붕괴. context-B 있음/없음 대조.

## 측정 (engine-native scope)
실 corpus에서 numpy 즉시(다의 토큰 문맥의존 예측 Δ, 문맥 shuffle) — 가장 싼 실데이터 probe. tune-to-green 금지 · frozen-first · 신호는 Δ+shuffle 대조. toy=DIRECTIONAL, 실 303M
byte-LM 검증은 GPU-gated. py 2-production numpy=TERMINAL-eligible, torch ad-hoc probe=DIRECTIONAL.

## 위험 / 예상 실패모드
attention LM은 이미 어느정도 수행→Δ 작아 additive와 안 갈릴 수 있음. hard-polysemy(문맥없으면 chance) 셋 필요.

## 상태
PRE-REGISTERED(unrun). census CLOSED이므로 발사는 저비용 numpy 방향판정 우선 → 초록 시에만 GPU 상승.
lift≈0(shuffle과 무차별)=능력천장 종결 재확인(값진 negative).
