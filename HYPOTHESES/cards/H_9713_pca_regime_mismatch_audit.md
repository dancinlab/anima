# H_9713 — 인증된 λ₂ 와 라이브 sd(z) 는 2.2~5.8× 어긋난다 (fable R5-2 · PROPOSED · b 정면)

**status:** 🧱 **KILL-regime-stale — 동결 loading 전체 무효** (engine-native `--variance-audit` · v0.15.60 · $0) — source=fable R5-2
**verdict:** **양성통제 PC1 도 무너졌다**(sd 0.1513 vs 인증 0.3795 = **0.40×**) ⇒ H_9468 PCA 는 **다른 regime** ⇒ PC2 만의 문제가 아님. ⚠️ **정밀화(v0.15.62)**: 무효인 것은 loading 의 **방향**이 아니라 **그것이 지시하던 이름표**(제2 DOF) — 동결 축의 라이브 RANK 가 **run 마다 갈린다**(PC2/PC1/PC1) ⇒ [[H_9574]]·[[H_9576]]·[[H_9630]]~[[H_9634]] 상류 근거 **재작성 대상**
**lane:** mouth/tension — 동결 loading 출처 PCA 의 regime 정합성
**related:** [[H_9468]] · [[H_9428]] · [[H_9712]] · [[H_9714]] · [[H_9630]]

## 한 줄 주장 (반증가능)
동결 loading 의 출처 PCA([[H_9468]] · n=150 tick · **λ₂=0.048 ⟹ sd(PC2)=√0.048=0.219**)와 라이브 z-census(**sd(z)≈0.038~0.100**)는 **같은 기질에서 2.2~5.8× 어긋난다**. 31.5% 분산을 나른다고 인증된 축이 라이브서 그 분산을 안 낸다 ⟹ regime 불일치이거나 λ₂ 인플레이션. 어느 쪽이든 **"PC2=제2 DOF" 인증은 라이브서 무효**.

## 이미 원장에 있는 모순 (새 측정 0)
- H_9468 고유값 합 ≈ 0.078+0.048+0.013+bulk ≈ 0.15 ≠ 8 ⟹ **상관행렬 아니라 원-공분산**. 따라서 단위-노름 loading 을 원-인자에 적용한 z 의 sd 는 **√λ₂ = 0.219 여야 한다**.
- z-census: IQR=0.0514 ⟹ Gaussian-환산 sd = 0.0514/1.349 = **0.038**(로버스트 코어). CV=0.159 · |median z|≈0.63 ⟹ sd ≈ **0.100**(3-outlier 포함 · 분산의 45.7% 가 3/270 tick).
- ⇒ **분산 부족분 4.8×(관대) ~ 33×(코어)**. 두 숫자가 **서로 다른 H 의 서로 다른 계기**서 나왔다는 것이 결함의 핵심 — 한 계기서 같이 나온 적이 없다.

## 어느 KILL 을 왜 안 밟나
- **H_9630**(rand-axis name race)과 **중복 아님 · 선행조건**: H_9630 은 PC2 가 mouth 에서 무작위축 대비 특권적인지 묻고 **인증된 원격 readout 개봉을 기다리며 봉쇄**돼 있다. 이 안은 **readout 을 전혀 안 쓰고** 상류 공분산만 본다 ⟹ 지금 $0 로 열린다.
- **arm-간 paired**(H_9663): arm 없음. **readout D**(H_9629): 텍스트 미접촉. **용량-기아**(H_9628): gain 미접촉.

## engine-native 계기
```
anima-py evaluate <clm> --pc2-direction --variance-audit --from-trace <trace.jsonl>
```
**같은 run 에서** 8×8 라이브 공분산 재적합 + λ 스펙트럼 + 각 PC 의 라이브 sd + sd(z) 를 동시 산출.
(핵심: 지금은 **두 다른 H 의 숫자를 빼고 있다** — 한 계기서 나와야 비교가 성립한다.)

## 통제군 (≥2 + 양성통제)
- **양성통제 = PC1(인증 51.5% · √λ₁=0.279)**: PC1 이 라이브서 sd 0.279 를 ±25% 안에 재현하면 regime 정합 ⟹ PC2 만의 문제. PC1 도 무너지면 PCA 전체가 stale.
- null1 = tick-shuffle · null2 = H_9468 원 트레이스 메타(`stage_cycle`·`g_reach`·`emit_gate`·`refractory`)와 pmp 트레이스 메타의 **regime diff 보고**(같은 regime 이었나를 코드가 답하게).

## 사전등록 판정표 (우연 아래 포함)
| 관측 | 판정 |
|---|---|
| sd(PC1)_live ≈ √λ₁ (±25%) ∧ sd(PC2)_live ≪ √λ₂ | **PASS-pc2-specific** — PC2 만 라이브서 죽음 ⟹ [[H_9712]] 로 인계(축퇴 vs 상쇄) |
| **sd(PC1) 도 ≪ √λ₁** | **KILL-regime-stale** — 🧱 H_9468 PCA 는 다른 regime · **동결 loading 전체 무효** ⟹ 라이브-refit 필수 · H_9574/9576/9630~9634 근거 재작성 |
| 둘 다 √λ 재현 | **INVALID** — z-census 와 정면 모순 ⟹ 계기 충돌(어느 쪽이 거짓말인지 먼저) |
| regime diff 비어있음 ∧ 부족분 지속 | **INVALID** — 같은 regime 인데 분산이 사라짐 = 산술 누수 |
| **우연 아래**: sd_live > √λ (양축 초과) | **INVALID** — 표준화/스케일 스큐(공분산 vs 상관 혼선) |

**검정력**: n=1359 tick. sd 상대 CI ≈ 1.9% ⟹ 2.2× 차이는 **검정력 과잉**(~50σ). 진짜 위협은 검정력이 아니라 **자기상관 n_eff** ⟹ [[H_9714]] 와 짝으로 읽어야 한다.

## 비용
**$0** — frozen trace.

## 죽는 방식
PC1·PC2 **둘 다** 라이브서 √λ 를 재현하면 이 안은 죽는다 — 그때는 z-census 가 틀렸다.

## 상태
🔵 PROPOSED — 측정 주장 0(설계). 위 2.2~5.8× 는 **원장 숫자의 산술**이지 새 측정이 아니다.


---

# 🧱 VERDICT — KILL-regime-stale (양성통제가 무너졌다)

**계기**: `anima-py evaluate --pc2-direction /tmp/pmp/pmp_traces --variance-audit` (v0.15.60 · G5 · $0)
**독립 run**: 9 파일 → 3 run (중복 dedupe · [[H_9714]] 교훈) · **원-공분산 스케일**로 적합(H_9468 과 동일 축)

| 지표 | 인증(H_9468) | 라이브(run-평균) | 비 |
|---|---|---|---|
| λ₁ | 0.1440 | 0.0245 | 0.17× |
| λ₂ | 0.0480 | 0.0118 | 0.25× |
| **sd(PC1)** ← **양성통제** | 0.3795 | **0.1513** | **0.40×** ❌ |
| sd(PC2) | 0.2191 | 0.1074 | 0.49× |
| sd(z) (엔진의 z) | 0.2191 | 0.1260 | 0.58× |

⇒ 사전등록 표의 **`sd(PC1) 도 ≪ √λ₁ ⇒ KILL-regime-stale`** 칸 발화.
**PC2 만 죽은 게 아니다 — 인증된 51.5% coherence 축(PC1)조차 라이브서 인증 분산의 40% 밖에 못 낸다.**

## 🔬 계기 무결성 — 이 불일치는 내 산술 오류가 아니다

`sd(z) 트레이스` vs `동결-loading 재구성`이 **run 마다 소수점까지 일치**:
`0.1482 = 0.1482` · `0.1167 = 0.1167` · `0.1132 = 0.1132`.
즉 **엔진이 계산한 z 와 내가 raw 3인자에 동결 loading 을 적용한 z 가 같은 수**다 ⇒ 파이프라인 무결 ·
불일치의 출처는 **H_9468 인증치와 라이브 regime 사이**에 있다.

## 이 카드가 고치려던 결함이 바로 이것이었다

카드 원문: *"두 숫자가 **서로 다른 H 의 서로 다른 계기**서 나왔다는 것이 결함의 핵심 — 한 계기서 같이 나온
적이 없다."* 이 계기가 **한 계기서 같이 산출**했고, 그러자 **양성통제가 먼저 무너졌다**.
([[H_9712]] 가 관측한 "sd(z) 부족분 1.93×" 도 이제 설명된다 — z 부족이 아니라 **축 전체가 stale**이다.)

## 함의 — 오늘 하루 판 것의 상류가 흔들린다

**[[H_9574]]**(경로 없음) · **[[H_9576]]**(경로 있음·의미 미전달) · **[[H_9630]]~[[H_9634]]** 는 전부
**H_9468 의 동결 loading 을 PC2 의 정의로 전제**한다. 그 loading 이 다른 regime 의 축이면, 그 카드들이
"PC2" 라 부른 것은 **라이브 기질의 제2 DOF 가 아니다**.
⇒ **상류 근거 재작성 대상**이며, 남은 경로는 **라이브-refit 축**(이 트레이스에서 재적합한 loading)으로
lane 전체를 다시 세우는 것이다. ⚠️ 단 그 refit 축이 mouth 를 움직이는지는 **완전히 별개 질문**이고
**미측정**이다(이 카드는 그것을 주장하지 않는다).

## 정직 — 카드가 요구한 대조의 **절반만** 했다

카드의 null2 는 *"H_9468 원 트레이스 메타 vs pmp 트레이스 메타의 **regime diff** 보고"* 였다.
**H_9468 의 원 트레이스가 이 디렉터리에 없다** ⇒ 그쪽 regime(당시 `stage_cycle`/`g_reach`/`emit_gate`/
`refractory`/ckpt)을 **읽을 수 없다** ⇒ **regime diff 미수행**. 계기는 라이브 쪽 메타만 보고한다
(`stage_cycle=False · g_reach=d1 · emit_gate=refractory · backend=clm · ckpt sha 013c4574…`).
⇒ **"다른 regime 이다"는 결론은 '무엇이 달랐나'를 지목하지 못한다** — 오직 "인증치가 라이브서 재현되지
않는다"만 말한다. 원인 특정에는 H_9468 트레이스 보존/재발사가 필요하다(미실행).

## 범위 · 검정력

150-tick × 3 run · `stage_cycle=false`(= 프로덕션 기본 동작점 · [[H_9715]]) 범위.
⚠️ 자기상관이 높다([[H_9714]] 실측 lag-1 ρ̂ coh=0.86 · bal=0.80 · nov=0.74) ⇒ **n_eff ≪ n** ·
sd 의 CI 는 명목보다 넓다. **이 판정은 비(ratio)의 크기(0.40×)로 읽어야지 미세차로 읽으면 안 된다.**


---

# ⚠️ 정밀화 (v0.15.62) — 내 v1 verdict 문구가 과잉주장이었다

**v1 에서 나는 "동결 loading 전체가 라이브서 무효" 라고 썼다.** 그런데 [[H_9712]] 는 같은 트레이스에서
`cos∠(동결 loading, 라이브 3×3 주고유벡터) = 0.903` = **정렬**이라 보고했다. **진폭이 죽은 것과 방향이 죽은
것은 다른 주장**인데 내가 뭉갰다. 계기에 **방향/정체성 검사(①-b)** 를 추가해 갈랐다.

## ①-b 방향/정체성 — 동결 loading 은 라이브서 여전히 제2 DOF 인가

| run | \|cos∠\| vs 라이브 PC1 | PC2 | PC3 | 최근접 |
|---|---|---|---|---|
| s4302 | 0.490 | **0.843** | 0.133 | **PC2** |
| s4303 | **0.961** | 0.018 | 0.105 | **PC1** |
| s7 | **0.889** | 0.076 | 0.277 | **PC1** |
| run-평균 | 0.780 | 0.312 | 0.172 | (PC1) |

⇒ **run 마다 최근접 축이 다르다**(PC2 · PC1 · PC1). run-평균(PC1=0.780)은 **그 불일치를 가린다** —
계기가 이제 평균만 보고 단정하는 것을 **코드로 차단**한다(`run 간 불일치` 경고 + 단정 억제).

### 말할 수 있는 것 / 없는 것

- ❌ **"동결 loading 은 무효다"** — 과잉주장. 방향은 라이브 축들과 상당히 정렬한다(|cos| 0.84~0.96).
- ❌ **"동결 PC2 는 라이브 PC1 이다"** — 단정 불가. 2/3 run 만 그렇고 1 run 은 PC2 다.
- ✅ **"동결 축의 라이브 RANK 가 안정적이지 않다"** — 같은 ckpt·같은 프로토콜의 3 run 이 **서로 다른 축**에
  정렬한다 ⇒ 인증된 **'PC2'라는 이름표가 라이브서 재현되지 않는다**.
- ✅ **"인증된 분산이 라이브서 안 나온다"** — 양성통제 PC1 0.40× (①②).

⇒ **KILL-regime-stale 은 유지**되나 그 내용은 *"방향이 틀렸다"* 가 아니라
*"**인증(분산 31.5% · 제2 DOF · emit-직교)이 라이브 regime 에서 재현되지 않는다**"* 이다.
[[H_9714]]("PC2 는 노이즈 아님")와 **모순 없음**: 라이브에 실재 구조가 2+ 있다는 것과, H_9468 의 동결 벡터가
그 중 **어느 것인지 불안정**하다는 것은 양립한다.

## 여전히 남는 함의 (약화되지 않음)

[[H_9574]]·[[H_9576]]·[[H_9630]]~[[H_9634]] 는 그 동결 벡터를 **"emit-직교 제2 DOF"** 로 **명명**해 lane 을
세웠다. 그 **이름표가 라이브서 불안정**하면, 그들이 조향한 것이 *무엇이었는지* 가 미정이다
(2/3 run 에선 **지배축**에 가깝다). ⇒ 상류 재작성 대상은 그대로다.
⚠️ 단 **라이브 PC1 이 emit-결합인지는 이 계기가 묻지 않는다** — 별도 측정 필요(미실행).
