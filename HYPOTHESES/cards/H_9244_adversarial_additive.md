# H_9244 · adversarial_additive

🔵 PRE-REGISTERED (unrun · γ 후보 레버 · census CLOSED=재확인/재오픈 트리거지 새 GREEN 사냥 아님)

## 클러스터 / 계보
클러스터 **C additive-대조 discriminator** · Fable γ 발산 #10 (state/g1_gamma_divergence/FABLE_DIVERGENCE.md).
부모 = G1 재조합 벽 프로그램(goal-biolens · g1-census-objfloor CLOSED · g1-metalaw-dpi-basin).
형제 = H_9225 residual-lift(#8, 🟡 측정설계 검증). 지배원리 = additive를 primary에서 **necessitate**
(보상항 offer 아님) — freeze-then-bolt 금지(H_9225 교정).

## mechanism
γ joint-rep을 adversary가 emb(A)+emb(B)로 재구성 못하게 minimax 학습. adversary 성공=additive로 설명됨=γ 패.

## why-beats-additive (DPI 함정 회피)
additive 가설류를 명시적 적대자로 세워 non-additive 다양체로 밀어냄. 죽은 contrastive와 달리 additive class 전체 상대(hard-neg 자동생성).

## 결합-파괴 통제 (THEATER 차단)
adversary 수렴 대조 — 진짜 bound면 adversary loss 바닥 안침. shuffle→adversary 쉽게 성공.

## 측정 (engine-native scope)
소형 GAN-류 numpy 방향, 안정성 위해 GPU 권장. tune-to-green 금지 · frozen-first · 신호는 Δ+shuffle 대조. toy=DIRECTIONAL, 실 303M
byte-LM 검증은 GPU-gated. py 2-production numpy=TERMINAL-eligible, torch ad-hoc probe=DIRECTIONAL.

## 위험 / 예상 실패모드
minimax 불안정→γ가 non-additive지만 무의미한 다양체로 도피(useless bind). downstream 능력 Δ 게이팅 필수.

## 상태
PRE-REGISTERED(unrun). census CLOSED이므로 발사는 저비용 numpy 방향판정 우선 → 초록 시에만 GPU 상승.
lift≈0(shuffle과 무차별)=능력천장 종결 재확인(값진 negative).
