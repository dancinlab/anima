# H_1530 — 📋 NEUROMODULATION-WALL escape: mechanism-FAMILY research census

**tier:** 📋 RESEARCH-CENSUS (no measurement — a lever map, NOT a verdict)
**source:** `state/1530_nm_research_census/CENSUS.md` (cited, ranked) · `candidates.jsonl`
**wired:** N/A — research only, no engine edits, no compute, no measurement claims.

## Purpose
Census untried mechanism-FAMILIES that could break the **H_1284 NEUROMODULATION wall**
("adaptive neuromodulation ≤ best-fixed, no-free-lunch"), to feed the next implement round.
Lens: `a_no_llm_frame_trap` (biology-first), `a_break_the_wall` (family taxonomy), c23 (no
black-box sweep). Web/arxiv-cited; mini-safe; NO engine edits, NO heavy compute.

## Measured root cause (the constraint the escape must satisfy)
The controller / operating-point family is **exhausted** by 10+ confirming lenses (H_1284
global-gain · H_1284_R3 regime-switch · H_1422 ACh/NE/DA state-contingent · H_1425 orthogonal
ideation-temperature · H_1509/b/c Amoeba allosteric-buffer · H_1523 multi-timescale · H_1524
diversity · H_1525 predictive · H_1526 emit-gate). They all measured the SAME ceiling: **recall
is bounded by key-GEOMETRY / capacity, not the operating-point schedule** — on a key-addressed
store the abstain decision IS ALREADY a threshold on the recall-margin, so any modulator on that
axis is circular and a single tuned fixed point dominates the controller. **Escape requires a
mechanism on an axis ORTHOGONAL to the abstain margin** — change WHAT geometry/capacity is
available, or WHAT gets stored.

## Untried orthogonal families (ranked: break-prob × cheapness)
1. **C1 expansion-recoding** (Marr-Albus / Litwin-Kumar) — frozen random sparse kernel lift
   R^d→R^{kd} (fan-in 4–5, top-k) BEFORE L2 affinity → raises angular separation of confusable
   keys. Attacks the EXACT measured bottleneck one stage upstream of the margin. HIGH break-prob,
   $0. (Reinforces + sharpens sibling H_1527 geometry lane.)
2. **C2 cascade metaplasticity** (Benna-Fusi) — per-cell 2–3 level consolidation state →
   per-cell timescales the flat store lacks; targets AB-AC interference. MED-HIGH, $0.
3. **C3 multi-store / encode-retrieve split** (CLS McClelland/Kumaran; Hasselmo ACh) — fast↔slow
   stores with phase-separated writes so confusable facts live in SEPARATE substrates. MED-HIGH,
   $0. HAZARD: must be multi-STORE not a scalar gain (gain re-enters the absorbed controller family).
4. **C4 curiosity-gated acquisition** (Gottlieb-Oudeyer) — admit by info-gain, skip redundant
   collinear dups → changes WHAT is stored. MED, $0; scope-limited to capacity-budget regime.

Each has a concrete next-round implement spec (capability · knob · frozen bar · ablation · source)
in `CENSUS.md` §4. All four are distinct from the exhausted controller family AND from the sibling
structure lanes H_1527/1528/1529 — C1 directly reinforces H_1527.

## Recommendation
Implement **C1 expansion-recoding** first (highest break-prob × cheapest, attacks the measured
key-geometry bottleneck head-on), then **C2 cascade metaplasticity**. C3/C4 next if C1/C2 wall.

## Honest scope
📋 census only — coordinates, not discovery. NO measurement, NO verdict, no tune-to-green. Each
spec is frozen-first with a teeth-bearing ablation+shuffle; `a_break_the_wall` type-(d) terminal
only AFTER an ablation survives in a future measured lane.

## Cross-links
H_1284 · H_1422 · H_1425 · H_1509 · H_1523 · H_1524 · H_1525 · H_1526 · H_1527 · H_1528 · H_1529 ·
`a_no_llm_frame_trap` · `a_break_the_wall` · `a_engine_native_learning` · `a_verified_must_wire` ·
p7 · c9 · c15 · c23
