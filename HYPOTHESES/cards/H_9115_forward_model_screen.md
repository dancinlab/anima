# H_9115 — Screen-B: does anima's emit have forward-model re-packaging HEADROOM ($0 pre-screen before GPU)?

**tier:** 🟢 HEADROOM-EXISTS (DIRECTIONAL) — the $0 screen fable prescribed to decide whether §2 GPU is justified. **b50_raw 3.5B → b50_compressed 2.2B (−37%) at held full-clue accuracy (1.0 vs 1.0).** Forward-model lever is PHYSICALLY POSSIBLE, not DPI-walled → engine-native mini justified. · **wired:** none (research screen; DIRECTIONAL — single receiver + hand transform, not anima learning).

**verdict:** 🟢 (`state/verdicts/9115_forward_model_screen/H_9115.txt` verbatim). fable's §2 design (DESIGN_fable_s2.md) prescribed a $0 screen BEFORE burning GPU: does the frozen emit have RE-PACKAGING HEADROOM — can the SAME referent MI be decoded from FEWER bytes at held accuracy? If no → emit at receiver-coding floor → forward-model INERT/DPI-walled → GPU stop. If yes → lever physically possible. **Result: info-preserving compression (strip article + non-discriminative stopwords, content words IN ORDER) lowers b50 3.5→2.2 bytes while full-clue accuracy holds at 1.0; compressed dominates raw at every truncation (3B: 0.786 vs 0.214).** anima's emit is ~6× over-packaged vs the info-theoretic floor (log₂14≈0.48B); repackaging recovers a large chunk → the §2 lever moves b50 (coding-efficiency), exactly as fable's DPI framing predicted (accuracy is trunk-bounded and stays 1.0).

## Why this matters (fable §2 DPI framing)
b50 measures coding-EFFICIENCY, not total referent-MI. DPI bounds accuracy (can't add referent MI the trunk lacks) but NOT b50 (the same MI can be re-packaged receiver-efficiently). Screen-B tests whether that headroom is real or whether 3.2B is already the receiver-conditioned coding floor. **It's real** — so a forward-model lane that learns to front-load discriminative content is a live lever, and GPU spend on it is now data-justified rather than faith-based.

## Method (fable §2, cheapest-valid first step)
Frozen H_9111 emits (14). Transform = `compressed` (info-preserving: leading article + STOP-word filler removed, content words in original order) vs `raw`. Byte-truncation sweep {full,8,6,4,3,2}, K=14 near-synonym distractors, single receiver claude-fable-5 ($0 DIRECTIONAL). Measure b50 per transform + full-clue accuracy-hold. STDLIB harness (grep-clean).

## Honest scope (c9)
DIRECTIONAL, not GREEN: single receiver, a HAND compression (not anima's engine learning it), frozen emit. Proves headroom EXISTS; does NOT prove anima's forward-model lane can LEARN to reach it. Next gate = engine-native mini (lane15 side-loop: read A/G tension, write mouth-gate only, disjoint from emit-drive 0/4 & recall_thr; supervised head, G non-diff — DESIGN_fable_s2.md §2). GPU cost-gate, explicit go.

## Answer
Should §2 GPU be burned? **Yes — data-backed.** The forward-model lever is physically possible (headroom real: b50 3.5→2.2 at held accuracy). Sequential gate now opens: Screen-B 🟢 → engine-native mini → GPU full. The $0 screen did its job (avoided blind GPU spend; if it were 🔴 INERT we'd have stopped here honestly).

## Evidence (`state/9115_forward_model_screen/`)
`DESIGN_fable_s2.md` (fable §2 design) · `screenb.py` (STDLIB, grep-clean) · `screenb_fixture.jsonl` · `RESULT.md` · `../9111_llm_interlocutor/emits.tsv` (14).
