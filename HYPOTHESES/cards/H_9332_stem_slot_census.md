# H_9332 — 연산자는 **자기가 학습된 어간에서만** 시험됐다 (담체 인구조사 · $0 · 발사 전에 두 개를 죽였다)

- **group**: g1-crack-natural-emergence
- **date**: 2026-07-14
- **tier**: ⚠️ **CORRECTION** — H_9328 의 '암기가설 반증' 은 **잘못된 축의 통제군** 위에 서 있다 (계기 무죄 · 판정 재해석)
- **surfaces**: `HYPOTHESES/cards/H_9332_stem_slot_census.md` · CPT 코퍼스 직접 계수(engine 코퍼스)

## 왜 쐈나 — 다른 실험을 쏘려다가

BINDING 기전(C1b)이 *"규칙은 pol(어간)을 조회하는데 CPT 가 다른 표에 썼다"* 라고 했으므로,
나는 **담체를 늘려 극성이 지름길에 못 숨게 하는** CPT 코퍼스를 설계하려 했다. 발사 전에
`corpus-py-1 ⑧` 이 시키는 대로 **담체를 셌다.** 그 계수가 **내 실험과 H_9328 의 헤드라인을
동시에 죽였다.**

## 실측 (engine 코퍼스 `cpt_keep_main_s7.txt` 직접 계수 · 8,280 라인)

| | held-out 어간(29) | SEEN 어간(20) |
|---|---|---|
| 등장하는 담체 수 | **3** (`___고` · `정말 ___고` · `너무 ___다`) | **3** (동일) |
| **연산자 담체**(`지 않다`) 안에서 학습 | **0/29** · 0줄 | **20/20** · **어간당 80줄** |

누수 불변식은 **성립**한다(연산자 라인에 held-out 어간 **0회** · replay 는 SEEN 전용) ✅

## 이것이 죽인 것 ①  — 내 예정 실험 (**발사 전 · spend 0**)

"극성이 **단일** 담체(지름길)에 숨는다" 가 전제였다. **거짓이다** — held-out 어간은 이미
**3개 담체**에 나오고, 그런데도 flip1 은 우연이다. **다중 담체는 이미 시험됐고 실패했다.**

## 이것이 죽인 것 ② — H_9328 의 '암기가설 반증' (**잘못된 축**)

C1/C1b 는 `10 표면 × 어간 20 × 양 seed` 로 쟀다. 그 **어간 20 = SEEN 어간**이고, 그것들은
**연산자 담체 안에서 어간당 80줄** 학습됐다. 즉 **연산자를 자기가 학습된 바로 그 어간에서
시험한 것**이다.

- ✅ **살아남는 것**: 미학습 **부사** 5종(별로·그다지·결코·하나도·그리 · 코퍼스 0회)이 완벽하다
  ⟹ **부사 슬롯은 진짜로 일반화한다.** 이건 실재하는 발견이다.
- ❌ **살아남지 못하는 것**: 그것은 **어간 슬롯**의 일반화를 **전혀** 보여주지 않는다.
  그리고 **BINDING 이 사는 곳은 어간 슬롯**이다.
- 🔑 **암기가설의 진짜 통제군은 미학습 어간이고, 그 팔은 이미 존재한다** —
  held-out flip1 = **0.4598 = 우연**. 결과는 반대다.

## 그래서 BINDING 을 어떻게 다시 읽나 (두 세계 · 아직 미결)

| | **R1 — LOOKUP-MISMATCH** (H_9327/C1b) | **R2 — STEM-BOUND** (이 카드) |
|---|---|---|
| 주장 | 일반 규칙은 있는데, pol(어간)을 **CPT 가 안 쓴 자리**에서 조회한다 | **일반 규칙이 없다** — `(어간, 지 않다)` 가 학습된 **쌍**이고 어간이 **키의 일부**다 |
| SEEN flip1 0.9833 | 규칙이 사전학습 표현에서 pol 을 읽는다 | 그 20쌍을 **80줄씩 외웠다** |
| 미학습 부사 완벽 | 규칙이라서 | 부사는 **키가 아니라서** |
| held-out flip1 우연 | 조회처 불일치 | **학습된 적 없는 쌍** |
| `지는 않다`·`지 않았다` 사망 | 방아쇠 문자열이 깨져서 | **키 문자열이 깨져서** |

**두 세계가 지금까지의 모든 관측을 똑같이 설명한다.** ⟹ H_9328 의 🟢 는 R1 을 licensing 하지
못한다. 가르는 것 = **극성은 아는데 연산자와 짝지어진 적 없는 어간**에서 연산자가 도는가.
held-out 어간(CPT 로 pol 심음 · 연산자 노출 0)이 **정확히 그 팔**이고, **우연이다** ⟹ R2 쪽으로
기운다. 다만 R1 은 "pol 이 연산자의 **읽는 지점**에서 표현적으로 안 보인다" 로 방어할 수 있고,
**그것이 바로 H_9330(occlusion·읽는 자리) 이 결정하는 것**이다.

## 정직

- 이건 **$0 계수**다 — 새 학습 0 · 새 디코드 0. 코퍼스가 스스로 말한 사실이다.
- H_9328 의 계기·수치는 **무죄**다. 무효인 것은 **'암기 반증' 이라는 라벨**뿐이다(부사 축의
  일반화를 어간 축의 일반화로 읽었다).
- R2 를 **확정하지 않는다** — H_9330 이 R1 의 방어선(occlusion)을 검사 중이다. 그 결과 없이
  R2 를 굳히면 내가 방금 지적한 것과 같은 죄를 짓는다.

## 남는 것

**`corpus-py-1 ⑧` 이 또 맞았다**: 전이 검사를 설계하기 전에 **전이시킬 축의 가짓수를 세라.**
이번엔 어간 축이었고, 세어보니 **held-out 어간의 연산자 노출 = 0** 이라 '규칙이냐 암기냐' 는
**그 축에서 애초에 측정된 적이 없었다.**

> **ID 재번호 (H_9331 → H_9332)** — 병렬 세션과 id 가 충돌했다(G6 unique-H_id). 규칙 `hypotheses-jsonl-3`
> **먼저-머지 선점**: `H_9331_bind_locus`(#3506 · 22:15:48) 가 이 카드(#3507 · 22:17:27)보다 1분 39초 먼저
> 머지됐으므로 후발인 이 카드가 재번호했다. **내용은 verbatim 보존** — 한 글자도 고치지 않았다.
