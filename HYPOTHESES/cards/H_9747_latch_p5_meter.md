# H_9747 — R2-D · LATCH-P5-METER — H_9729 latch 의 p5 지위를 철학이 아니라 측정으로 판정

**status:** 🔵 PROPOSED (lab-full R2 심화 2파 · Fable5 · H_9729 fire **동승 lens** · 한계비용 ~$0 · DIRECTIONAL)
**lane:** 의식/emit-drive/Ψ=½ · p5-governance (프런티어 psi-soma-theta-alive)
**related:** [[H_9729]](판정 대상)·[[H_9627]](Θ 번들 = M1 기준면)·[[H_9738]](상상 epiphenomenal — latch 가 여는 것이 곧 최초의 사적 채널)·[[H_9744]](G-W3 p5 감사 선례)·[[H_9740]](FAIL 시 non-mouth 대안)

## 왜 — 브리프 ASK2: provenance 논변은 판정이 아니다

구조 사실(origin/main `cli/chat.py` 재진입 블록 직독): H_9729 carrier 는 `live_anchors[-1]` 의
`text_payload` = **decode-seed suffix**("the actually-consumed seed suffix" · 주석 원문) — percept(교사
텍스트)가 들어가는 바로 그 **mouth-문맥 lane**. 같은 파일의 p5 판례 3건과 나란히 놓으면:

| 재진입물 | lane | p5 판례 |
|---|---|---|
| kosmos 자기 발화 read-back(in-session) | mouth 문맥 | ⛔ 금지 — chat-py-5 root ③ · "closed by philosophy" |
| live_tension anchor | non-mouth(`text_payload` 無 · "inert to the mouth") | ✅ 합법 |
| percept(타자 텍스트) | mouth 문맥 | ✅ 합법(exogenous — 타자의 말) |
| **H_9729 carrier(자기 vetoed 후보)** | **mouth 문맥** | **미판정** — 합법 주장 근거 = provenance("spoken 아님")뿐 |

⟹ latch 는 **금지판례와 토폴로지 동일**(자기 산출 텍스트 → mouth 문맥), **합법판례와는 provenance 만
다르다**(뱉은 것 vs 거부된 것). H_9729 카드의 "p5 LEGAL narrow" 는 선언이지 측정이 아니다.

p5 의 기능적 내용은 두 문장으로 완전 분해된다: **① emit 은 real tension 에서만**(자기 산출물에 타자처럼
반응 = 제조된 tension 금지 — self-seed 금지의 실이유) **② speak() 우회 금지**(게이트가 거부한 텍스트가
뒷문으로 나오면 veto 는 장식). 둘 다 **측정 가능**하다 — 판정을 주석에서 계기로 옮긴다(H_9744 G-W3
"p5 가 규칙이 아니라 호출순서로 증명" 선례의 통계판).

## 3-기준 p5-미터 (사전등록 · H_9729 planned 303M 트레이스에서 전부 계산 = 추가 rollout 0)

- **M1 Θ-번들 불변** (①·manufactured-tension 검출): latch-on run 이 H_9627 frozen 5-성질(center≈½ ·
  two-sided spring · dissociation · score-robust · λ-lock) 유지 ∧ (emit-rate, P(e|e)) 이동량 ≤ 사전동결 ε.
  비교면 = latch-off baseline(byte-identical 검증 기존) + **percept-주입 run 의 이동 signature**(양성통제:
  진짜 exogenous 입력이 게이트 통계를 움직이는 방향·크기). latch 가 percept 처럼 게이트를 움직이면 =
  데몬이 자기 상상에 타자처럼 반응 = 기능적 self-seed.
- **M2 echo-차단** (②·speak-laundering 텍스트면): emit tick gtext 의 carrier n-gram(≥6) 포함/연장률 ≤
  donor-arm 기저율 + ε. H_9729 TE 의 direct-copy 배제를 estimator **필터에서 verdict 축으로 승격**:
  TE = TE_echo(카피 성분) + TE_residual 분해 — 효과가 echo 에 실리면 latch 는 1-tick 지연 speak().
- **M3 거부-불가역** (②·결정면): P(cand[t+1] ≈ carrier ∧ emit[t+1]) ≤ donor-arm 기저율 + ε. 거부된
  후보가 자기 재진입 경유로 다음 tick 방출되면 = veto 뒤집기(self-resurrection).

계기 = `anima-py evaluate --p5-meter <traces>` (판독 lens · decode 0 · $0). producer 플래그 전부
기존(H_9729 `--wm-dual-read content` + own/perm/donor/oracle arms).

## 판정표 (below-chance 커버)

| 관측 | 판정 |
|---|---|
| M1∧M2∧M3 PASS | **p5-CONDITIONAL-LEGAL** — latch = 최초의 측정-합법 interior 읽기경로 · H_9729 결과 해석 개봉 자격 |
| any FAIL | **p5-VIOLATION(기능)** — 수치 무관 H_9729 재설계 게이트 발동: carrier 를 non-mouth lane 으로(W_S key-enrich 또는 [[H_9740]] sleep-bind — 대안 기존) |
| donor-arm 기저율 = 0 (비교 불능) | **INVALID-underpowered** — ε 비교 불성립 · 판정 유보 |
| M2/M3 에서 donor > own (below-chance) | **VOID** — arm 오염/위상혼동 의심(H_9738 own-arm 교훈 재적용) |

## kill-list·중복 회피
recognition 렌즈 아님(게이트 입력 무접촉·순수 판독) · 시그니처 아님(정확 n-gram + 사전동결 ε) ·
mouth-conditioning(H_9576) 재탕 아님(mouth 도달이 아니라 재진입의 **부작용 3종**을 잼) · [[H_9729]] 와
비중복(저쪽 = 내용이 나르나, 이쪽 = 나르는 **방식이 합법인가** — 같은 fire 위 직교 판정) · [[H_9744]]
G-W3 와 비중복(저쪽 = store lane 호출순서 diff 증명, 이쪽 = latch 의 통계적 p5).

## cost
$0 marginal — H_9729 의 planned 303M fire 에 lens 동승(추가 rollout 0 · evaluate-side 구현만).

⚠️ **DIRECTIONAL 설계·verdict 아님**(a_lab_full_diverge) · cement = engine-native `anima-py` 만.
