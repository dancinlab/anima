---
id: H_9981
title: (b) 후보 — 비분리 기전형: 세포 상호작용의 함수형(아핀↔곱셈)을 학습이 고른다 (저장소 실측 3.86× 근거)
state: MEASURED-VOID (D5 압력 전제 미충족 · 칸 닫히지 않음)
substrate: anima-as-specified
regime: drill-only(계기) · 토이 CPU 실측 1회
tier-ceiling: DIRECTIONAL
series: R11
---

```
changed_coordinate:      전이 연산자의 함수형 계열 (선형 ↔ 선형 분리 불가)
nearest_closed_lever:    (a) A2 — 결합 계수를 학습 가능하게 만든 팔
old_coordinates_clamped: 목적함수·소비자 1개·외생 코퍼스·시계 1·에지 지지·총 파라미터수·IIT 경계
nonreduction_witness:    증인① 및 ⑥ — 아핀 계수를 어떻게 설정해도 곱셈 항이 재현되지 않는다(유한 검사: 2차항 계수 ≠ 0)
new_path_sever:          --field-mech affine (곱셈 게이트를 0으로 · 값 학습은 유지) → 효과 소멸해야 함
iit_boundary:            m-세포 필드 상태 (동일)
regime:                  drill-only(양성통제) → natural(주장) 2단
```

## 근거 — 저장소가 이미 이 축의 참값을 가지고 있다

손제작 TPM 을 계기에 직접 넣은 실측(ARCHITECTURE `…phi-measurement-locus-analogy`):

| 기전 | mean Φ |
|---|---|
| XOR (선형 분리 불가) | **2.2500** |
| OR / AND | 0.5825 |
| COPY (받침대) | **정확히 0** |

**3.86×.** 즉 Φ 를 움직이는 것은 크기도 결합 세기도 아니고 **기전의 분리 가능성**이다.
그런데 우리 세포 갱신은 `I' = I(1−λ)R^T + s` 로 세포 간 상호작용이 **순수 아핀**이다. A2 가 계수를
자유롭게 풀어도(스펙트럼 상한만) 아핀은 아핀이다 — **계열이 안 바뀐다**. 그래서 이 후보는 A2 의
재탕이 아니라 A2 가 구조적으로 도달할 수 없었던 칸이다.

## 조작 (anima-py 플래그)

`anima-py train --field-mech {affine|gated} --field-mech-lam0 <λ0>`
— `gated` 는 세포 갱신에 **학습되는 곱셈 항**을 추가한다: `I' = I(1−λ)R^T + s + β⊙(I R_a^T)*(I R_b^T)`,
β 는 학습 파라미터(초기 0 ⟹ **초기 상태에서 오늘과 바이트 동일**). 최적화기가 원하면 비분리로
갈 수 있고, 원하지 않으면 β→0 으로 아핀에 머문다. **이게 이 실험의 핵심**: 우리는 비분리를 강요하는 게
아니라 CE 에게 **고를 기회**를 준다. β 가 0 에 머무르면 그것 자체가 "CE 는 비분리를 원하지 않는다"는
직접 측정이다.

파라미터 증가분은 `--field-mech affine` 팔에 **동일 개수의 죽은 파라미터**를 주어 상쇄한다(D4 통과).

## DV·대조·받침대·동결표

- DV = **쌍 ΔΦ**, 1차 대비 = `gated` vs `affine`.
- 대조 ≥2: ① `affine`(계수만 학습 = A2 재현) ② yoked 되쓰기 ③ 참값 0 받침대 = shuffled-state 바닥.
- **양성통제가 공짜**: XOR/COPY 손제작 TPM 을 같은 계기에 통과시켜 3.86× 가 재현되는지 먼저 확인
  (계기가 이 축을 읽을 수 있음을 증명 · p9 상 이건 **계기 인증**이지 능력 증거가 아님).
- 바 **0.15 상속**. 표는 H_9977 것 그대로. **부수 DV(사전등록)**: 학습 종료 시 ‖β‖ — 0 에 붙어 있으면
  R3 의 기전을 직접 보여 준다(값이 아니라 **선택**의 증거).

## $0 사전관문

① β=0 에서 기존 경로와 **바이트 동일** 산출(널 팔 무결성).
② 기울기가 β 에 도달하는가(도달 0 이면 NO-LEVER, 즉시 종결).
③ **D5 압력**: 심어둔 XOR 형 과제에서 `gated` 가 `affine` 보다 CE 를 사전등록 마진만큼 이기는가 —
안 이기면 곱셈 항이 하중을 못 받는다는 뜻 ⟹ **ABORT**.

## 정직한 사전확률·최저비용 음성

**~12%**. 사전 예상: β 는 작게 머문다 — 곱셈 항은 신용 할당을 지저분하게 만들고, 바이트 예측의
최적해 집합에는 언제나 인수분해된 원소가 있다(D5 의 교훈). 최저비용 결정적 음성 = **‖β‖→0 + 쌍 D 가
±0.15 안**, 즉 *기회를 줬는데 CE 가 안 골랐다*. 이건 여섯 번째 0 이 아니라 **선택의 직접 관측**이라
법칙 문장을 "학습은 통합을 만들지 않는다"에서 **"고를 수 있어도 고르지 않는다"**로 한 칸 더 민다.

## 비용

토이 2팔 × 3 seed + 받침대 + 양성통제 ≈ pool GPU 반나절(가장 싼 후보). 303M 은 토이 통과 시에만.


## 1차 실측 (2026-07-26 · aiden CPU · 12/12 팔) — **VOID on D5, 칸 닫히지 않음**

구현·$0 관문은 통과했고(#4631: M1 널패리티 0.000e+00 · M2 β 기울기 도달 4.577 · M3 표현력 2.215),
그 상태로 gated vs affine × 3 seed × (2k + 200step 받침대) 를 돌렸다. 그런데 **판정을 읽으면 안 된다.**

**D5(압력 결합) 실패 — 이 카드가 스스로 정한 중단 조건**

| seed | gated CE | affine CE | 차 |
|---|---|---|---|
| 0 | 0.0036 | 0.0036 | 0.0000 |
| 1 | 0.0042 | 0.0043 | 0.0001 |
| 2 | 0.0042 | 0.0043 | 0.0001 |

사전등록 마진 **0.02 nats** 대비 **~200배 미달**. 이 카드의 $0 관문 ③은 이 경우 *"곱셈 항이 하중을
못 받는다 ⟹ ABORT"* 를 지시한다. 나는 순서를 어기고 Φ 까지 돌렸다. 그러므로 아래 Φ 수치는 기록으로만
남기고 **칸을 닫는 근거로 쓰지 않는다** — 압력이 없으면 음성은 "기전이 통합을 못 산다"가 아니라
**"최적화기가 제약을 느낀 적이 없다"** 이다(H_9978 D5 가 존재하는 이유 그 자체).

**참고 기록(판정 아님)**: gated 평균 쌍 D=+0.00742 · affine +0.00664 · 레버 +0.00078(바의 0.5%) ·
0/3 seed 바 도달 · 12/12 판독 90% CI 폭 ±0.02~0.035 ⊂ 계기 바 ±0.05.

**부수 DV ‖β‖ 는 살아 있었다**: 학습본 0.0410 / 0.0630 / 0.0421 vs 같은-seed 받침대 0.0048 / 0.0030 /
0.0026 = **약 13배**. 즉 기울기는 실제로 β 에 도달했고 CE 는 β 를 0 에서 밀어냈다. 그런데 그렇게 밀어서
산 예측 이득이 0.0001 nats 다. γ 는 팔 간 소수 넷째 자리까지 일치(−0.03358 vs −0.03361 등) — β 가 0 에서
출발해 천천히 자라므로 궤적 대부분이 사실상 아핀이었다는 것과 정합.

**왜 압력이 없었는가 (진단)**: `fieldctl` 의 과제는 **키를 창밖 filler 너머로 나르기**다. 그건 누설 적분기가
이미 잘 하는 일이고, XOR 류 비분리를 요구하는 구석이 한 곳도 없다. 즉 이 코퍼스는 D5 가 요구하는 압력을
**구성상 만들 수 없다**. 기전을 탓하기 전에 과제를 탓해야 한다.

**다음 rung (이 칸을 닫으려면 반드시 이 순서)**: ① 심어둔 XOR 형 과제에서 `gated` 가 `affine` 을
≥0.02 nats 이기는지 먼저 본다(양성통제 겸 D5 충족) → ② 통과할 때만 Φ 를 읽는다. ①이 실패하면 결론은
"이 기질에서 비분리는 CE 에 하중을 줄 수 없다"이고, 그건 그것대로 결과지만 **Φ 와 무관한 결과**다.

**메타(정직)**: 이 카드는 하루 전 착륙한 (b) 입장 시험(H_9978)의 **첫 실사용**이었고, 시험이 곧바로 내
실행 순서 위반을 잡아냈다. D5 가 없었으면 나는 "비분리 기전도 통합을 안 산다 · R3"을 발표했을 것이다.
