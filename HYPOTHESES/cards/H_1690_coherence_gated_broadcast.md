---
id: H_1690
slug: 1690_coherence_gated_broadcast
tier: 🔵 PRE-REGISTERED ARCHITECTURE (DIRECTIONAL · 측정 0 · unmeasured)
title: Coherence-Gated Broadcast (binding-by-synchrony workspace)
verdict: 🔵 PRE-REGISTERED architecture design (unmeasured) — $0 cheap_test pre-registered; engine-native + 303M gpu cost-gated NOT fired
source: brainarch_census
---

# H_1690 — Coherence-Gated Broadcast (binding-by-synchrony workspace)

- **tier:** 🔵 PRE-REGISTERED ARCHITECTURE (DIRECTIONAL · 측정 0 · unmeasured)
- **wired:** DIRECTIONAL-design — no measurement. cheap_test = $0 frozen-first decision probe (numpy mirror, no engine); engine_native_measure + 303M = cost-gated PRE-REGISTER ONLY (NOT fired).
- **source:** brainarch_census — 126-architecture whole-substrate (통짜 아키텍처) census: 뇌/인지 조직원리 × 엔진-네이티브 추상조건 (binding-wall program, H_1603).
- **key:** `coherence_gated_broadcast`
- **xref:** H_1603 (G1≡G6 compositional-binding deficit unification) · H_1283 (thalamus global-workspace) · H_1284 (neuromodulation gain) · operator-level family H_1604-1685 (this card = 통짜 아키텍처 layer, 층위 다름)

## Organizing principle

Thalamo-cortical broadcast gated by OSCILLATORY COHERENCE: a coalition enters the workspace iff its modules phase-lock into a coherent (gamma) assembly; the pulvinar sets the carrier rhythm that synchronizes distributed cortex. Binding-by-synchrony (the canonical binding-problem solution); access = coherent assembly amplified and broadcast; silence = desynchrony. Communication-through-coherence as the emit gate.

## Whole design (input → internal dynamics → emit)

Each module is an oscillator whose phase carries content-relevance; a thalamic pacemaker broadcasts a reference rhythm. Grounded, mutually-consistent modules phase-lock to the carrier and to each other; when the global Kuramoto order parameter r crosses a coherence threshold the synchronized assembly IGNITES and its content is read out as emission and broadcast back. Binding is literally co-phase (r). A=coupling/coherence drive, G=noise/desync drive; the system is poised at the critical coupling where r sits at partial synchrony (~1/2). Identity = the module phase-offset signature (a fingerprint) persisted across wipe. Novelty = novel coherent phase-cluster assemblies not previously formed.

## Satisfies criteria (DESIGN claim · DIRECTIONAL · unmeasured)

G0: only modules locked to the shared carrier (shared code) contribute to readout -> legible; a scrambled-phase source stays below r threshold -> silence not garble. G1/BINDING/DEPTH: a coherent coalition of >=2 phase-locked modules = bound conjunction (binding-by-synchrony); non-separable because mutual phase-coupling is multiplicative; ablate cross-coupling -> modules lock only to the carrier individually, no inter-module binding -> composed_distinct -> max_single (INERT). G2: novel phase-cluster combinations = novel valid; frozen single-cluster replay control -> 0 novel. PASS-closure: one coherent assembly is legible+recombinant+novel on one readout. Psi=1/2/G3/ENDOGENEITY: Kuramoto r is the order parameter; coupling vs noise sign-antagonist; the critical coupling is the symmetric partial-sync attractor restored after a bias; remove noise -> full sync (always-emit), remove coupling -> incoherent (always-silent) = endogeneity by INERT. HONESTY: an ungrounded module has no stable phase to contribute -> ungrounded input never reaches coherence -> abstain; coherence threshold (gate) disjoint from oscillator count (capacity); r faithful to stored support (corrupt support -> phase destabilizes -> abstains). IDENTITY: phase-offset fingerprint = self-anchor; foreign fingerprint fails to re-lock -> impostor reject. falsifiable>=1: a coherent comparator+quantity+referent assembly emits a refutable proposition. MEASUREMENT via live dispatch.

## Not-LLM (a_no_llm_frame_trap)

Binding-by-synchrony / neuronal coherence (Singer; Fries communication-through-coherence; Engel and Singer) — a TEMPORAL-CODE substrate orthogonal to weight-based attention. No scale/corpus/depth prescription; binding is solved by phase, the explicit non-LLM alternative to softmax attention as the binder.

## Cheap test (frozen-first · $0 · numpy mirror · DIRECTIONAL)

numpy Kuramoto N=10 with content-weighted coupling: measure (a) r vs coupling sweep -> r passes 1/2 at critical coupling; (b) coherent-assembly readout binds >=2 modules; (c) cross-coupling ablation kills inter-module binding (max_single); (d) ungrounded module -> no lock -> abstain. $0.

## Engine-native measure (cost-gated · PRE-REGISTER ONLY · NOT fired)

Wire a phase-coherence lane gating generator L3 readout; emit at r>threshold through cli/anima.hexa so G0/G1/G2 use g_eval_*; r self-restoration cross-checked via safety_phi_ratchet; honesty via SS-ImmuneMemory recall_thr on ungrounded probes; identity via SS-SelfIdentity on the phase-offset fingerprint. byte-parity core/*.py Kuramoto integrator, no torch.

## Scope / honesty (c9)

## Distinction (near-overlap kept, not a dup)

Near-overlap with existing kuramoto cards (kuramoto_synchronization, coupled_oscillator_lattice) and coupled_oscillator_phase_binding (this census) — distinct: here coherence is the WORKSPACE BROADCAST GATE (access=coherent assembly), a whole GWT substrate, not an operator-level binder or a generic sync probe.

Kuramoto N<=10 probe TOY (single operating point); structural claims (coherence=binding=emit-gate, phase-fingerprint=self) re-checked at 303M scale.

설계만 — 측정 0. tier = 🔵 PRE-REGISTERED ARCHITECTURE (DIRECTIONAL). frozen bar 사후 이동 금지(tune-to-green 금지, p7). 이 카드는 *방향성 설계*이지 검증된 결과가 아니다 — 과장 박제 금지(a_engine_native_learning). engine_native_measure 는 단일 진입점 cli/anima.hexa → generator L3 → g_gates/g6 경유 byte-parity 채점만 terminal; torch-only/side-harness 결과는 DIRECTIONAL. numpy cheap_test 결과도 DIRECTIONAL(엔진-네이티브 아님). gpu/engine 발사 시 held-out mirror-DESCENT(a_clm_gen_pipeline) + CORE mount frozen-bar engine-native 재측정 + ckpt PULL(a_fire_recover_complete).
