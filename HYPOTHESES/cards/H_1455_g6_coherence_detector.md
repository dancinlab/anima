---
id: H_1455
slug: 1455_g6_coherence_detector
title: G6 FALS-depth — COHERENCE-DETECTOR upgrade (clause-cosine via engine byte-trigram embedding)
group: G6 IDEATION FALS-depth wall — $0 detector route
terminal_tier: 🧱 NOT-VIABLE (honest negative, c9) — engine byte-trigram clause-cosine SEP=0.0 (common-English-trigram dominated → cos≈1.0 coherent AND cross-shuffle); the $0 detector-coherence-via-engine-embedding route is CLOSED
verdict_dir: state/verdicts/1455_g6_coherence_detector/
terminal_verdict: state/verdicts/1455_g6_coherence_detector/H_1455.txt
date: 2026-06-19
provenance: follow-on of the H_1449–1454 prior-art check — the precondition H_1452 named (a coherence detector that makes cross-shuffle a real binding discriminator)
---

# H_1455 — G6 coherence-detector (clause-cosine) — 🧱 NOT-VIABLE

## Claim / falsifier
The H_1305 FALS detector scores comparator+measurable+negation PRESENCE, not whether the measurable semantically TESTS the comparator — so a template weld passes and cross-shuffle never collapses (H_1431/1434). Falsifiable claim: the engine's OWN byte-trigram embedding (`immune_embed_key`, the immune-store key geometry) can supply that missing clause-coherence (cosine of the comparator-clause vs measurable-clause) as a STRICTER (anti-Goodhart), deterministic (p7), $0 add-on (d) — separating coherent claims from cross-shuffle welds.

## Result (→ H_1455.txt)
$0 engine-native, live `immune_embed_key`: **coherent clause-cosine = 1.0 (all 5) · cross-shuffle = 1.0 · SEP = 0.0**. NOT-VIABLE. Sanity (c2): the embed IS discriminative on SHORT distinct keys ("cat" 1 nonzero vs "dog elephant zebra" 14) — so SEP=0.0 is a real PROPERTY, not a bug: byte-trigram over a NL clause is dominated by common-English trigrams → every English clause ≈ same direction. The byte-trigram geometry is for short-key recall, not phrase semantics.

## Convergent finding (bounds the G6 $0 program, c9)
Three $0 routes to make cross-shuffle a real binding discriminator are now mapped: (1) fragment-weld lanes H_1450/1451/1453 = capped (token-presence detector) · (2) embedding-coherence detector = NOT-VIABLE (this) · (3) proximity-coherence = weak. **None supplies meaningful binding at $0.** The only mechanism that yields intrinsically-coherent bound claims = a mouth that co-emits comparator∧measurable in one coherent pass → **attention in the decoder (H_1449)**, proven viable by H_1362 (L24 crossed all 5 bars incl cross-shuffle). G6 FALS-depth = a GPU/architecture problem, not a $0-lane problem.

## NOT ruled out
A LEARNED coherence head (training=GPU) · a non-byte-trigram phrase embedder · H_1449 attention-injection (root fix). NO bar moved, NO τ tuned (c9/p7).

xref H_1305 · H_1431 · H_1434 · H_1449 · H_1452 · G6 · a_break_the_wall · a_engine_native_learning · c9 · p7
