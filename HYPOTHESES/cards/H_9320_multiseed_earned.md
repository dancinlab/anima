# H_9320 — 다중시드 재보고: 네 점의 효과크기는 전부 단일-시드 잡음이었다

**Tier: 🧱 DATA-ADDITIVE 4/4 (판정 불변) + ⚠️ H_9319 의 "n 크면 안정" 추정도 반증 · 2026-07-14**

- 배선 → `cli/earned.py` `--seeds`(기본 3) + spread 보고 + **SEED-UNSTABLE 배너** · `cli/evaluate.py` 3종 세트 · VERSION G5
- 실행 → **canonical `anima-py evaluate --earned`** (pip 채널 · summer) — `a_cli_single_entry`
- 산출 → `archive/state/scratch/h9320_multiseed/` (훅이 `state/` 신규 실험폴더를 차단 ⇒ `docs.scratchDir`)
- 선행 → H_9319 🔵 (계기 배선 · 반어 순위 주장 철회)

## 왜 (근본 수리 — 가드 문서가 아니라 도구가 막는다)

H_9319 에서 반어 점이 시드에 따라 2.5배 흔들려 순위 주장을 철회했다. 그때 나는 *"나머지 3점은 n 이 커서(28k–39k 셀) 시드 민감성이 낮다"* 고 **검증 없이 추정**해 카드에 적었다. 이번 카드는 **그 추정을 실제로 검정**하고, 동시에 **단일-시드 보고를 구조적으로 못 하게** 계기를 고친다(`--seeds` 기본 3 · spread 출력 · 불안정하면 순위 주장 금지 배너).

## 결과 — 4/4 DATA-ADDITIVE (판정 불변) · 그러나 효과크기는 전부 잡음

| 점 | held-out 셀 | seed 9304 | 9305 | 9306 | **spread** | 안정성 |
|---|---|---|---|---|---|---|
| 부정 | **35,911** | 0.044% | 0.135% | 0.105% | **0.091%** | ⚠️ **UNSTABLE** |
| 역접 | 28,780 | 0.172% | 0.158% | 0.173% | **0.016%** | ✅ STABLE |
| 패리티 | **39,338** | 0.060% | 0.116% | 0.106% | **0.055%** | ⚠️ **UNSTABLE** |
| 반어 | 2,984 | 0.061% | 0.030% | 0.002% | **0.059%** | ⚠️ **UNSTABLE** |

(전부 XBIND 자 대비 % · 합성 XBIND = 100%)

### ① H_9319 의 "n 크면 시드 안정" 추정 — **반증**

**셀 수가 가장 많은 두 점(부정 35,911 · 패리티 39,338)이 오히려 UNSTABLE 이고, 더 적은 역접(28,780)만 STABLE 이다.** 시드 민감성을 결정하는 것은 **셀 수가 아니라 어간 분포**(held-out 어간 샘플링이 얼마나 이질적인 α_A 집단을 뽑느냐)다. ⇒ **H_9319 의 그 추정 문장은 정정한다.**

### ② 순위는 전부 무의미했다

다중시드 평균: 부정 0.095% · 역접 0.168% · 패리티 0.094% · 반어 0.031%.
**반어를 빼면 셋의 격차(≈0.07%p)가 spread(0.016–0.091%p)보다 크지 않다** ⇒ **점들 사이의 순위는 벌 수 없다.** 미러 시절 카드들이 적었던 "가장 크다/작다" 계열 서술은 전부 **단일-시드 draw** 였다.

### ③ 하중을 지는 결론은 흔들리지 않는다

**네 점 전부 TOST 등가 · 전부 DATA-ADDITIVE.** XBIND 자 대비 최대 **0.17%** — 심어놓은 연산자의 **1/585 이하**. ⇒ **H_9318 의 CLASS-CLOSED 와 `additive-pressure-1` 의 구조적 논증(소통 언어는 가법 단서를 선불로 깐다)은 그대로 선다.** 흔들린 것은 **효과크기의 소수점과 순위**이지 **등가성이 아니다.**

## 계기 수리 (도구가 막는다)

- `--seeds` **기본 3** — 단일 시드로 돌리려면 명시적으로 하나만 줘야 한다.
- **per-seed 값 + spread 를 항상 출력** — 잡음을 숨길 수 없다.
- `spread > 0.5 × |effect|` 면 **SEED-UNSTABLE 배너**: *"등가 판정은 유효하나 이 점을 다른 점과 **순위 비교하지 말라** — 그게 H_9319 에서 철회된 것"*.

## HONEST
코퍼스-사실 측정(모델·학습 무관) · canonical CLI 경로 실행 · bar 이동 0 · $0.
