# H_9712 — 절단인가 축퇴인가 상쇄인가 — z 3인자 분해 (fable R5-1 · PROPOSED · a 정면)

**status:** 🔵 PROPOSED (미실행 · lab full R5 · 사전등록 · 지금 $0 발사 가능) — source=fable R5-1
**lane:** mouth/tension — z 상수성의 상류 인자 분해
**related:** [[H_9468]] · [[H_9428]] · [[H_9713]] · [[H_9714]] · [[H_9715]]

## 한 줄 주장 (반증가능)
라이브 z 가 상수인 것은 배타적 두 원인 중 하나다 — (①) 기저 3인자(`nov_ctx`/`bal_lane`/`coh_lane`)가 **각각 라이브서 축퇴**했거나(= 기질 tension 자체가 축퇴 = **상류 대발견**), (②) 인자는 변하는데 **동결 loading 이 라이브 변동방향과 직교/상쇄**한다(= **loading 이 범인**). 두 원인은 **동일 트레이스에서 갈린다**: 표준화 인자별 sd + cos∠(frozen loading, 라이브 3×3 공분산 주고유벡터).

## 절단은 사전 무죄 (원장 산술 · 새 측정 0)
브리핑 (a) 는 "8-벡터가 아니라 3항"을 **절단** 후보로 든다. 그러나 PC2 는 **단위 고유벡터**다:
`0.84² + 0.44² + 0.28² = 0.9776` ⟹ 구현된 3항이 loading 에너지의 **97.8%**, 버려진 5인자(rel·gap·cur·pain·dyn)는 합쳐 **2.2%**(노름 0.149).
⇒ **절단은 sd(z) 를 최대 ~1.1% 만 깎는다 — 부족분(2.2~5.8×)을 설명 못 한다.** (a) 는 3-갈래가 아니라 **축퇴 vs 상쇄 2-갈래**다. 이 카드가 그 정정을 등록한다.

## 어느 KILL 을 왜 안 밟나
- **arm-간 paired 설계**(H_9663 · sd 0.14 캐스케이드): arm 자체가 없다 — 단일 트레이스 인자 분해라 tick 분산이 DV 에 안 들어온다.
- **readout D**(H_9629 3중 고장): 텍스트 미접촉 — DV 가 인자 sd 와 각도다.
- **용량-기아**(H_9628 사망): gain 미접촉 — 인자 **분산**을 묻지 이득을 안 묻는다.
- **H_9576**(VOID): mouth 도달을 안 묻는다(상류 전용).

## engine-native 계기
```
anima-py evaluate <clm> --pc2-direction --factor-census --from-trace <trace.jsonl>
```
산출: 3인자별 sd(원단위·표준화) · 3×3 라이브 공분산 주고유벡터 v̂ · **cos∠(v̂, [0.84,−0.44,−0.28])** · 재구성 sd(z).
트레이스가 `nov_ctx`·`bal_lane`·`coh_lane`·`pc2_z` 를 **이미 tick 단위로 기록**하므로 새 decode 0·forward pass 0.

## 통제군 (≥2 + 양성통제)
- **양성통제 = PC1 축(coh 지배 · 인증 51.5% · √λ₁=0.279)**: 같은 계기로 통과 — PC1 은 라이브서 변해야 한다. PC1 도 상수면 **계기/regime 고장**이지 PC2 고유문제가 아니다(→ [[H_9713]] 이관).
- null1 = tick-shuffle(인자 주변분포 보존) · null2 = 라이브 무작위 단위 3-벡터 k=12 의 sd 분포.

## 사전등록 판정표 (우연 아래 포함)
| 관측 | 판정 |
|---|---|
| 3인자 표준화 sd 전부 < 0.10 | **PASS-degenerate** — 🔑 상류 대발견: 기질 tension 자체 축퇴 ⟹ mouth lane 전체 재프레임(문제는 축이 아니라 기질) |
| ≥2 인자 sd ≥ 0.10 ∧ **\|cos∠\| < 0.30** | **PASS-cancel** — loading 이 범인: 동결 축이 라이브 변동과 직교 ⟹ 라이브-refit 축 후속([[H_9713]]) |
| 인자 varies ∧ \|cos∠\| > 0.70 ∧ sd(z) ≪ √λ₂ | **INVALID** — 정렬됐는데 z 안 움직임 = **산술 모순** ⟹ z 계산경로 누수 점검(`core/brain.py:305`) |
| 양성통제 실패(PC1 도 상수) | **VOID** — regime 문제 → [[H_9713]] 선행 |
| **우연 아래**: 인자 sd = 0 정확 ∧ sd(z) > 0 | **INVALID** — z 가 이 3인자서 안 나옴(카드-코드 스큐) |
| **우연 아래**: sd(z) 가 rand 3-축 패널 5pct 미만 | **INVALID-suspicious** — 동결 축이 무작위보다 *체계적으로* 좁음 = 상쇄의 강한 형태 → PASS-cancel 승격 |

**검정력**: n=1359 tick(9칸×151). 표준화 sd 의 상대 CI ≈ 1/√(2n) ≈ 1.9% ⟹ sd 0.10 문턱 여유 충분. cos∠ 부트스트랩 CI(k=1000) 반폭 ≲0.05 ⟹ 0.30/0.70 문턱 분리 안전. ⚠️ 자기상관 시 n_eff ≪ n — ρ̂ 동시보고 필수([[H_9714]]).

## 비용
**$0** — frozen trace · ckpt 미로드.

## 죽는 방식
3인자가 전부 변하고 **cos∠ ≈ 1** 이고 sd(z) ≈ 0.219 면 이 안은 죽는다 — z 는 애초에 상수가 아니었고 z-census 가 틀린 것이다.

## 상태
🔵 PROPOSED — 측정 주장 0(설계).
