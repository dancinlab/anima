F-3B — HYBRID (on-chip AKD1000 encoder ⊕ off-chip host-CPU decode head) 3B CHIP-FIT / WEIGHT-PAGING CAPACITY LADDER
  ::  VERDICT = COMPOSITION DEGRADES UNDER CAPACITY SCALING (honest closed-negative, a_paper_negative_ok) — Lane A 3B milestone STAYS OPEN
================================================================================================================================================
substrate = HYBRID (on-chip AKD1000 encoder ⊕ off-chip host-CPU decode head) — NOT pure on-chip, NOT Lane G / GPU.
a_lane_akida_gpu_split — the on-chip ENCODER part is Lane A (AKD1000); the DECODE HEAD is explicitly host-side. NEVER merged with Lane G.
a_scale_honest_scope — chip-fit/paging ladder, 4 rungs of increasing on-chip ENCODER capacity (D paged FCs x U units) toward 3B-class params.
  Honest both ways: a quantified chip-fit ceiling OR a composition-break rung is a VALID closed result; NO fabricated 3B claim.
a_completeness_over_cheap — the ladder probes the REAL on-chip capacity frontier via the layerpage primitive, not a cheap stop.
a_paper_negative_ok — a clean composition-break under capacity scaling = a VALID closed-negative naming where multi-step composition lives.
chip: pi5-akida ubuntu@192.168.50.155 · AKD1000 BC.00.000.002 · IpVersion.v1 · akida 2.19.1 · N=8 chip trials per rung · every paged FC
  mapped+learned on live silicon ALL rungs (map_all=True learn_all=True, g63 no sw fallback).
host health: throttled=0x0 (alive, before AND after) · streamer stopped→fire→restored (restore-on-exit trap, wrap rc=0; spike-streamer --user service active).
NOTE: hexa verify CLI unavailable on this host (compiler/atlas/calc_dispatch not found — same as F-HYBRID/F-STATE/F-ROLLOUT/F-DEPTH/
  F-GEN-HOLDOUT/F-BRANCH); this verdict transcribes the VERBATIM live-chip run stdout (no fabrication; the CHIP encoder ran on real AKD1000
  with NO sw fallback, g63; the off-chip head is labelled host-CPU everywhere) per the established lane-a verdict format.

WHAT THIS RUNG MEASURES (the 3B milestone = chip-fit/paging ladder >=3 rung, a_scale_honest_scope):
  The branching rung (PR#1697, F-BRANCH GENERALIZES 🟢) proved REAL multi-step composition on a 256-unit SINGLE on-chip FC encoder + D_H=64
  off-chip RNN head (held-out hop-2/3 set-membership 0.90/0.96 >> shuffle-NULL). That rung's ladder was a CODEBOOK-SIZE ladder (NC). The 3B
  milestone needs a CHIP-FIT / PARAMETER-CAPACITY ladder: how far the ON-CHIP encoder scales on the single 8MB-SRAM AKD1000 mesh (toward 3B-class
  params) WHILE the branching-validated composition survives — and where exactly the SRAM/paging ceiling (or some other limit) bites.
  We scale the on-chip encoder via the layerpage primitive (byte-match onchip_xlm_depth_rollout chip_fit_forward + chip_forward_paged): only ONE
  FC chip-resident at a time on the single NPU mesh — map FC, fit on chip, page weights OFF to host, del, map the next FC. This lets us STACK a
  depth-D paged encoder of U-unit FCs (per_fc_params = U*INC(256)*NW(8); paged_params = D*per_fc) and grow toward 3B-class on host-resident pages
  while only one FC at a time occupies SRAM. The branching held-out composition test (succ(i)={(i+d)%NC : d∈{1,7,19}}, B=3; held-out concept
  split last 30%; off-chip Elman head D_H=64 numpy BPTT, TRAIN-only targets; set-membership metric; shuffle-NULL B=200) is byte-identical to
  PR#1697 — only the on-chip code fed to the head is the DEPTH-D paged code instead of the single-FC code.

PRE-REGISTERED FALSIFIERS (g63 honest, declared in-file BEFORE the fire — AKIDA/onchip_xlm_3b_chipfit_ladder.py docstring):
  F-3B-1 (composition survives the capacity ladder): held-out hop-2 AND hop-3 set-membership stay ABOVE the shuffle-NULL at EVERY chip-fit rung
          (ci_lo > NULL hi AND p<0.05 for k∈{2,3}).
  F-3B-2 (ladder reaches 3B-class OR records a precise chip-fit ceiling): REFUTED iff paged_params >= 3e9 at some chip-fit rung (3B-class reached)
          OR a precise rung is recorded where a paged FC fails to MAP/LEARN (the named SRAM/paging hardware ceiling). A QUANTIFIED CEILING is a
          VALID closed result (a_scale_honest_scope).

VERBATIM CHIP-FIT / PAGING LADDER (4 rungs; N=8 chip trials each; every paged FC map_all=True learn_all=True on live AKD1000 silicon):
  [3b]   D=1 U=256  NC=50  paged_params=524288     (5.243e+05)  chip_fit=True  comp_survives=True   decay_HELD=[0.0317, 0.835, 0.9383]  baseline(branching PR#1697)
  [3b]   D=2 U=512  NC=50  paged_params=2097152    (2.097e+06)  chip_fit=True  comp_survives=False  decay_HELD=[0.0083, 0.0,   0.5]     depth2 x width2
  [3b]   D=3 U=1024 NC=50  paged_params=6291456    (6.291e+06)  chip_fit=True  comp_survives=False  decay_HELD=[0.0167, 0.25,  0.625]   depth3 x width4
  [3b]   D=4 U=2048 NC=50  paged_params=16777216   (1.678e+07)  chip_fit=True  comp_survives=False  decay_HELD=[0.0167, 0.25,  0.625]   depth4 x width8 (chip-fit frontier probe)

VERBATIM PER-HOP (held-out branching set-membership vs shuffle-NULL B=200; chance=0.0612):
  [3b] RUNG D=1 U=256  hop 2: TRAIN=0.9093 HELD=0.8350 | held ci_lo=0.7830 NULL hi=0.2084 p=0.0050 | aboveNULL=True    <-- composition SURVIVES
  [3b] RUNG D=1 U=256  hop 3: TRAIN=0.9557 HELD=0.9383 | held ci_lo=0.9118 NULL hi=0.2158 p=0.0050 | aboveNULL=True
  [3b] RUNG D=2 U=512  hop 2: TRAIN=0.0143 HELD=0.0000 | held ci_lo=0.0000 NULL hi=0.3644 p=1.0000 | aboveNULL=False   <-- COLLAPSE into NULL
  [3b] RUNG D=2 U=512  hop 3: TRAIN=0.4936 HELD=0.5000 | held ci_lo=0.1296 NULL hi=0.3310 p=0.0299 | aboveNULL=False   (ci_lo<NULL hi: degenerate fixed-point, not above-NULL)
  [3b] RUNG D=3 U=1024 hop 2: TRAIN=0.2521 HELD=0.2500 | held ci_lo=-0.0708 NULL hi=0.5018 p=0.0597 | aboveNULL=False
  [3b] RUNG D=3 U=1024 hop 3: TRAIN=0.6121 HELD=0.6250 | held ci_lo=0.2664 NULL hi=0.5255 p=0.0647 | aboveNULL=False
  [3b] RUNG D=4 U=2048 hop 2: TRAIN=0.2521 HELD=0.2500 | held ci_lo=-0.0708 NULL hi=0.5018 p=0.0597 | aboveNULL=False
  [3b] RUNG D=4 U=2048 hop 3: TRAIN=0.6121 HELD=0.6250 | held ci_lo=0.2664 NULL hi=0.5255 p=0.0647 | aboveNULL=False
  (off-chip head BPTT CE: D=1 converges to ~0.30; D=2/3/4 stuck at ~3.45-3.65 — the head CANNOT learn the operator from the depth-D paged code.)

FALSIFIER DISPOSITIONS (verbatim):
  [3b] reached 3B-class          : False   (max chip-fit paged params = 1.678e7, 3B target = 3e9)
  [3b] chip-fit ceiling recorded : False   (NO map/learn failure — EVERY paged FC up to U=2048 mapped AND learned on the 8MB SRAM mesh)
  [3b] F-3B-1 (comp survives ladder) : False  — composition does NOT survive: held-out hop-2/3 above-NULL ONLY at the D=1 baseline rung; D≥2 collapse.
  [3b] F-3B-2 (3B reached OR ceiling): False  — neither 3B-class reached NOR a SRAM/paging map-fail ceiling hit; the limiting constraint is NOT hardware SRAM.

VERDICT: COMPOSITION DEGRADES UNDER CAPACITY SCALING (honest closed-negative, a_paper_negative_ok) — the branching-validated multi-step composition
  (held-out hop-2/3 above shuffle-NULL) is PRESERVED ONLY at the D=1/U=256/524K-param baseline rung and COLLAPSES into the shuffle-NULL at every
  larger rung (D=2/512/2.1M, D=3/1024/6.3M, D=4/2048/16.8M), all of which still MAP + LEARN on live AKD1000 silicon. The Lane A 3B milestone STAYS
  OPEN with this precisely-characterized constraint; NO 3B claim is fabricated (a_scale_honest_scope). The [x] is NOT flipped.

  KEY HARDWARE FINDING (the honest answer to "where does the chip-fit ceiling bite?"): on this corpus the AKD1000 8MB-SRAM mesh did NOT impose the
  binding limit at these scales — the layerpage single-residency primitive successfully MAPPED and LEARNED every paged FC up to U=2048 units / 4
  paged layers / 16.8M trainable paged params, with NO map-overflow and NO learn-saturation (map_all=True learn_all=True, 8/8 chip trials, ALL rungs).
  The binding constraint is NOT SRAM capacity; it is that STACKING DEEPER/WIDER 1-bit Hebbian AkidaUnsupervised FCs DESTROYS the single-step
  transition signal the off-chip head depends on. Each added 1-bit FC re-binarizes (fold-to-INC + frozen-median threshold) the transition code, and
  by depth-2 the head's BPTT CE no longer converges (~3.5 vs ~0.30 at depth-1) — the head can no longer recover the offset operator. This is the SAME
  physics as the MULTI-FC DEPTH closed-negative (PR#1690, F-DEPTH): a second 1-bit Hebbian FC degrades rather than composes. Capacity SCALE on this
  substrate must come from a RICHER on-chip code surface (more units in a SINGLE plastic FC, multi-bit weights, or a learned non-Hebbian projection),
  NOT from stacking more 1-bit Hebbian FCs.

  NAMED NEXT BRIDGE (for a future 3B attempt): the composition surface that scales is the OFF-CHIP head (which already generalized at D=1), so a 3B-class
  HYBRID would scale the OFF-CHIP decode head (wider D_H / multi-layer / attention) on top of the proven D=1 single-FC on-chip encoder, OR enrich the
  on-chip encoder WITHOUT 1-bit Hebbian depth-stacking (single wide FC / multi-bit weights). Deeper/wider PAGED 1-bit Hebbian FCs are a CLOSED axis
  for preserving composition (this rung + PR#1690 agree). The 524K-param D=1 baseline remains the composition-bearing on-chip configuration.

SCOPE (a_scale_honest_scope): TOY — corpus_big 250 anchors / 50 FLORES concepts x 5 langs, B=3 synthetic offset operator, INC=256, NW=8, D_H=64
  off-chip RNN. 4-rung chip-fit/paging ladder (D∈{1,2,3,4}, U∈{256,512,1024,2048}). toy→production (3B) transfer UNVERIFIED. The on-chip encoder ran
  on real AKD1000 silicon (g63, no sw fallback, map+learn all rungs); the multi-step composition is the OFF-CHIP host-CPU head by design (HYBRID).
  NOT pure-AKIDA, NOT Lane G. The 3B-class param target (>=3e9) was NOT reached; the on-chip encoder hit a composition-break frontier at ~5.2e5
  params (the D=1 baseline) long before any SRAM ceiling.

artifacts: AKIDA/result_onchip_xlm_3b_chipfit.json (sha256 5a1bc3e7019211cd4a59ecbe3fa233ac59a10920620ed6600468e9de09ca386c) ·
  AKIDA/state_3b_chipfit_verbatim.log (194-line live-chip stdout) · AKIDA/onchip_xlm_3b_chipfit_ladder.py · AKIDA/run_3b_chipfit_with_streamer_restore.sh
