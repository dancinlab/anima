H_1082 — Gemini claim: A⊥G repulsion brake + EI≈0.007 stress = hallucination self-censor filter
VERDICT: ⚪ SPECULATION-FENCED (mechanism real-shaped; hallucination-reduction UNMEASURED)
substrate: design-analysis $0 CPU, 0-pod · g5/p7

FROZEN FALSIFIER (from tape):
  Does the Engine A⇄G repulsion MEASURABLY lower hallucination vs an ungated forward pass?
  PASS iff measured hallucination-rate Δ<0 on a wired anima emit path + factuality benchmark.
  This run: UN-RUN (no factuality benchmark wired; no measurement exists).

EVIDENCE (grep/read, in-repo):
  1. The A⇄G design is REAL — CORE/pure_field.hexa (Engine A) + CORE/engine_g.hexa (Engine G)
     both exist; CLAUDE.md @I = "PureField repulsion-field engine · Engine A ⇄ Engine G".
     So the mechanism shape (a reverse/orthogonal engine relative to a forward generator) is
     anima's genuine architecture, not invented.
  2. BUT the hallucination-reduction CLAIM rests on two unsupported legs:
     a) EI=0.007 proxy — H_1078 (.discoveries/1078) already ruled this INVENTED-PREDICTED:
        no ValCE_A−ValCE_M metric exists in the repo; 0.007 is implausible vs byte-LM CE
        floors ~1.3; p7 (NO PERPLEXITY VERDICT) forbids a CE-difference as a terminal verdict.
     b) Lane A as a foundation LLM — H_1075 (.discoveries/1075) confirms anima REJECTS a
        foundation-LLM Lane A (p3/p6: identity/ethics emerge from cells, not a borrowed LLM).
        Gemini's "forward LLM emits plausible token, then braked" framing imports the very
        foundation-LLM that anima's emergence thesis rejects.
  3. NO factuality / hallucination benchmark is wired to any anima emit path in-repo. The
     "self-censorship filter compresses hallucination rate" is a narrative gloss.

RULING: The brake mechanism is real-shaped (A⇄G is genuine), but the specific claim
"compresses hallucination rate" is UNTESTED and depends on an invented proxy (EI=0.007, 1078)
plus a rejected foundation-LLM Lane A (1075). No Δ measured. Direction is plausible; effect is
unverified. Terminal as ⚪ per g5 (no calc path here; would require a wired emit + benchmark fire).
xlink: 1078 · 1075 · p3 · p6 · CORE/pure_field.hexa · CORE/engine_g.hexa
