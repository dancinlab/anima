H_869 — clm-dispatch-kl-routing · backing verdict dir (hexa-native-guard anchor)
slug: 869_clm_dispatch_kl_routing
falsifier: F-CLM-DISPATCH-KL → .verdicts/clm-dispatch-kl/F-CLM-DISPATCH-KL.txt
status token: 🟢/SUPPORTED scaffold committed · 🔴/CLOSED-NEGATIVE possible (a_paper_negative_ok)

This directory is the hexa-native-guard backing anchor for H_869, created and
committed BEFORE UNIVERSE/H_869_clm_dispatch_kl_routing.md so the new hypothesis
node has a SUPPORTED-token backing dir on disk at registration time.

H_869 = dispatch-KL distill routing (P4 @L3 lever A). It adds a 4th routing-
escape lever — distill a balanced (uniform) dispatch target into the MoE router
via a per-token KL term — onto the mid/measurement rung (the pluggable routing
slot), and re-tests routing diversity (routing-z > 3.0, the chip-array deploy
gate) against the toy 847/852/853 routing-z 🔴 finding.

scope (a_scale_honest_scope): routing-z > 3.0 is the chip-array (expert=chip)
DEPLOY gate, NOT the "can converse" gate. Conversation is already carried by
content-escape lever B (H_863 🟢 SUPPORTED-NUMERICAL). A 🔴 here closes the
deploy gate at the measurement rung only — it does not block conversation.

artifacts (this campaign):
  .verdicts/clm-dispatch-kl/F-CLM-DISPATCH-KL_prereg.txt   (W2 frozen thresholds)
  .verdicts/clm-dispatch-kl/F-CLM-DISPATCH-KL.txt          (verbatim harness verdict)
  CLM/model/h869_dispatch_kl_routing.hexa                  (lever-A interface extension)
  CLM/model/h869_dispatch_kl_routing.py                    (measurement harness · CODE g5)
  UNIVERSE/H_869_clm_dispatch_kl_routing.md                (hypothesis node)
