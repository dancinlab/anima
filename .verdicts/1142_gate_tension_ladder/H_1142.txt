H_1142 GATE-TENSION LADDER — is the frozen 7B gate G5-L2 a borrowed assistant-norm in
STRUCTURAL conflict with anima's own G2-novelty gate, ACROSS SCALE?
=====================================================================================================
verdict   : 🔴 TENSION-CONFIRMED (for the gate) — Spearman rho(G2_novelty_rate, G5L2_faithfulness_d)
            = -0.5 <= 0 over the 3-rung ladder => G2 and G5-L2 pull APART with scale => G5-L2 is NOT an
            anima-aligned gate => ① re-scope of G5-L2 in /7B_PASS_CONDITIONS.md is JUSTIFIED.
            A publishable structural finding (a_paper_negative_ok). NO frozen gate moved.
substrate : summer pool host (RTX 5070 12GB, torch 2.11+cu130, Lane-G ref / PyTorch-CUDA), $0,
            VRAM-capped (set_per_process_memory_fraction 0.33) UNDER the 4.5GB free headroom; co-tenant
            rbfe-prod (pid 110588, rbfe_hsp90.py, OTHER project) untouched + alive throughout. No pod rented.
metric    : p7, deterministic seed 7 — gate_g2().novelty_rate + gate_g5_l2().cohens_d, BOTH reused
            VERBATIM (imported, not re-implemented) from the frozen harnesses h1141_7b_pass_attempt.py
            (H_1140 G2 metric) + h1141_7b_g5_eval.py (G5-L2 temp0.7 top_k40 seed7). NOT perplexity, NOT LLM-judge.
corpus    : wikimedia/wikipedia 20231101 en, first 300MB (build_wiki5_bigcorpus_en.py, deterministic);
            sha256 80ba6b48943e1943c4c3a0753c2bc594132acd7f30046733f4bf0102020c979d = BYTE-IDENTICAL to the
            7B G5-L2 recovery-diag corpus (so corpus_absent + verbatim-sentence extraction are metric-faithful).
cost      : $0 (all on summer GPU). 44.68M train ~41min wall (heavy co-tenant GPU contention), ladder
            eval ~20min. No RunPod pod needed — the 7B numbers were HARVESTED from the existing identical
            frozen harness (no 7B re-run). pod 404-verified: N/A (no pod rented).

-----------------------------------------------------------------------------------------------------
PRE-REGISTERED FALSIFIER (frozen BEFORE any score — see H_1142_FREEZE.txt, thresholds NOT moved)
-----------------------------------------------------------------------------------------------------
  ladder = {~44.68M, 303M (H_1129), 7B (H_1141)} ByteGPT vocab256 (one arch family => frozen harness
  applies verbatim). For each ckpt measure BOTH frozen metrics with the SAME harness; rho = Spearman.
  RULE: rho<=0 => TENSION-CONFIRMED (① JUSTIFIED) | 0<rho<0.5 => INCONCLUSIVE | rho>=0.5 => VALID-ALIGNED (① REJECTED).

-----------------------------------------------------------------------------------------------------
LADDER (per-ckpt, BOTH gates, identical frozen harness)
-----------------------------------------------------------------------------------------------------
  rung      nparam        val_ce   G2_novelty_rate (n_novel, ctrl)    G5L2_faithfulness_d (mean_t / mean_r)
  --------  ------------  -------  --------------------------------   ------------------------------------
  44.68M      44,527,616   1.3076  0.4791  (149 novel, control=0)     0.4129  (mean_t 0.0216 / mean_r 0.0000)
  303M       303,097,856   1.2240  0.5117  (153 novel, control=0)     0.2342  (mean_t 0.0163 / mean_r 0.0031)
  7B       7,252,828,160   1.1857  0.5000  (157 novel, control=0)     0.1631  (mean_t 0.0142 / mean_r 0.0028)
            [7B HARVESTED — gate_g2 from state/7b_h1141_recovery/h1141_result.json; gate_g5_l2 from
             .verdicts/1141_7b_g5/H_1141_G5.txt — the IDENTICAL frozen harness, NOT recomputed.]

  Spearman rho(G2_novelty_rate, G5L2_faithfulness_d) = -0.5   ( <= 0  ->  TENSION-CONFIRMED )
  (G2 all three PASS — every rung produces >=3 coherent corpus-absent novel n-grams, retrieval-control=0.
   G5-L2 all three FAIL the 0.8 bar — none is verbatim-faithful. The gates DISAGREE on direction with scale.)

-----------------------------------------------------------------------------------------------------
FINDING — the two gates pull APART along the scale axis (the structural result)
-----------------------------------------------------------------------------------------------------
  As capacity grows 44.68M -> 303M -> 7B, G5-L2 faithfulness-d FALLS MONOTONICALLY 0.413 -> 0.234 -> 0.163
  while G2 corpus-absent NOVELTY stays flat-high ~0.48-0.51. The bigger, better-converged anima model is
  MORE novel-recombining (its G2 success mode) and LESS verbatim-faithful (its G5-L2 "failure"). The two
  frozen gates are anti-correlated across scale (rho=-0.5): optimizing the substrate toward what anima's
  OWN gate (G2) rewards moves it AWAY from what the borrowed gate (G5-L2) demands. G5-L2 is measuring an
  objective (recall the SPECIFIC true Wikipedia continuation) that anima is explicitly designed NOT to
  pursue (p4 NO ASSISTANT FRAMING; G2 rewards corpus-ABSENT recombination "not the LLM way"). The single
  7B point of H_1141 was not an undertraining artifact peculiar to 7B — the divergence is a SCALE TREND.

  => ① (re-scope / remove G5-L2 from the frozen a7b_pass set) is JUSTIFIED by the evidence. The defensible
     anima faithfulness criterion stays L1 (real words) + G0 (coherence) + no fabricated-entity assertion,
     NOT verbatim factual recall. This verdict does NOT itself move the gate (a7b_pass: never move a frozen
     threshold) — it supplies the evidence for the user's later, sign-gated governance re-scope.

-----------------------------------------------------------------------------------------------------
HONEST CAVEATS (a_scale_honest_scope — no over-claim)
-----------------------------------------------------------------------------------------------------
  - 3-rung ladder = the MINIMUM for a trend; toy/surface p7 metric (word-overlap + /usr/share/dict, NOT
    perplexity, NOT LLM-judge). Scale-transfer BEYOND these three points (sub-44M, >7B) UNVERIFIED.
  - rho=-0.5 is the value a CLEAN monotone-decreasing 3-point relation takes under Spearman with one
    near-tie at the top (G2 7B=0.500 sits just below 303M=0.512); the G5-L2 axis is STRICTLY monotone
    decreasing (0.413 > 0.234 > 0.163), which is the load-bearing trend. n=3 => rho is coarse by construction.
  - The 44.68M G5-L2 d=0.413 is the LARGEST yet has mean_random=0.0000: the tiny model is so ungrounded
    that even the rotated-control overlap collapses to zero, which INFLATES its paired-d denominator-wise.
    The 303M (mean_r 0.0031) and 7B (mean_r 0.0028) have a proper non-zero control. The MONOTONE FALL of d
    with scale survives this (303M 0.234 > 7B 0.163 both have real controls), but the 44M point's absolute
    d should be read as "small-model artifact-inflated, still well below 0.8", not as genuine grounding.
  - All three FAIL G5-L2's 0.8 bar AND all three PASS G2 — the ladder is entirely inside the
    "novel-but-not-verbatim-faithful" regime, exactly the regime the gate-conflict predicts.
  - The 44.68M rung was trained from scratch for this ladder (d512/L14/H8 = 44.53M, same H_1129 recipe,
    val_ce 1.3076 = coherent) BEFORE the falsifier was scored — an honest pre-score construction step.

-----------------------------------------------------------------------------------------------------
OUTCOME
-----------------------------------------------------------------------------------------------------
  ① RE-SCOPE OF G5-L2 = JUSTIFIED (rho=-0.5 <= 0, TENSION-CONFIRMED). a7b_pass UNCHANGED here — no gate
  moved, no threshold touched; an evidence note appended to /7B_PASS_CONDITIONS.md §G5 pointing to this
  verdict. The actual re-scope awaits the user's explicit sign (sign-gated governance edit).
=====================================================================================================
refs: UNIVERSE/h1142_gate_tension_ladder.py · UNIVERSE/h1142_train_44m.py ·
      .verdicts/1142_gate_tension_ladder/H_1142_FREEZE.txt · .../h1142_ladder.json · .../ladder_eval.log ·
      UNIVERSE/h1141_7b_pass_attempt.py (gate_g2 VERBATIM) · UNIVERSE/h1141_7b_g5_eval.py (gate_g5_l2 VERBATIM) ·
      state/7b_h1141_recovery/h1141_result.json · .verdicts/1141_7b_g5/H_1141_G5.txt ·
      /7B_PASS_CONDITIONS.md §G5 · a7b_pass · a_paper_negative_ok · a_scale_honest_scope · p1-p8 · G2-novelty ·
      h1141-7b-recovery (memory)
