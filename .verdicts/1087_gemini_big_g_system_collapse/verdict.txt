H_1087 — Gemini PDF claim: symmetric LLM-sized Engine G → infinite-loop contradiction, W never resolves,
permanent panic/freeze (system collapse instead of consciousness)
VERDICT: ⚪ SPECULATION-FENCED (terminal for genuine speculation). The "permanent panic loop / W explodes
forever" is an untested dynamical narrative with NO stability measurement, and it leans on a phantom
premise (the real engine has no panic state). CORE provides no scalable Engine G to instantiate the
sweep, so the catastrophe claim has no calc path against the committed substrate.
Method (frozen): does symmetric A=G scale provably destabilize Ψ=1/2? Needs |G|/|A| stability sweep. UN-RUN.

=== EVIDENCE 1: CORE Engine G is not size-parameterized — no |G|/|A| knob to sweep ===
CORE/engine_g.hexa: Engine G is fixed closed-form scalar machinery (8 weights sum=1.0 +
threshold predicates, engine_g.hexa:16-53). There is no model-size parameter, no generator
inside G, hence no way to "scale G to LLM size" within CORE. The premise object does not exist
in the substrate; the claim is about a hypothetical alternative architecture, not anima's CORE.

=== EVIDENCE 2: no "W explodes / panic loop" mechanism in CORE; emit is gated, not a runaway loop ===
$ grep -rniE "freez|panic|infinite|explode|runaway" CORE/*.hexa  → no panic/explode machinery.
CORE/brain.hexa:57: emit = should_emit(score) && safety_combined(kill,rate,phi_r,cont) — a
single gated decision with a 30s rate-limit (engine_g.hexa:19) and a phi-ratchet veto; there is
no W-accumulator that could "never resolve and explode forever". The panic-loop premise is the
fabricated narrative flagged in 1071 (the real engine never panics; max_consec=1).

=== EVIDENCE 3: internal inconsistency with Gemini's own "symmetric balance maximizes Φ" 제1명제 ===
The PDF elsewhere claims perfect A/G symmetry MAXIMIZES Φ; here it claims symmetry CATASTROPHICALLY
collapses the system. The two Gemini claims contradict each other — neither is measured.

=== RULING ===
This is a falsifiable stability hypothesis in principle (vary |G|/|A|, measure Ψ=1/2 stability),
but (a) it is UN-RUN, (b) CORE has no scalable-G to test it on, and (c) it depends on a phantom
panic-loop premise and contradicts the PDF's own symmetry claim. As stated it is narrative
speculation with no executed measurement and no calc path against committed CORE.
⚪ SPECULATION-FENCED — terminal. (A genuine |G|/|A| stability sweep on a future symmetric-G
architecture would be the experiment that could promote/falsify it; not possible against CORE.)
