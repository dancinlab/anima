H_1307 — KOREAN MITOSIS GPU SCALE-UP of the H_1306 engine-native rung
PRE-REGISTRATION (FROZEN before the run; bars NOT movable post-hoc) — frozen-first, c9/p7.

LANE: ko-mitosis-gpu. Scales H_1306 (#2211, 🟢 GREEN engine-native: KO next-byte CE
3.611->3.249, EN retained, cells 2->9 on a 600 KB KO / 300 KB EN real-web window,
stride-subsampled to ~2.7 K KO pairs because the hexa interpreter is list-based O(N)/
growth-step) to a MUCH larger REAL Korean pair set, GPU-accelerated on the user's OWN
idle host `summer` (RTX 5070, Blackwell sm_120). $0 (user hardware, NOT runpod).

THE QUESTION (the brief): does MORE real Korean data push held-out KO next-byte CE
BELOW the 600 KB baseline 3.24897? The MECHANISM is already engine-verified (H_1306);
this rung is SCALE + honest GPU measurement, NOT re-proving the mechanism.

MECHANISM (byte-faithful to CORE/h1306_ko_mitosis_engine_probe.hexa _grow_on):
  PARTITION = nearest-center Voronoi (vadapt_field_nearest_idx; torch.argmin, first index
              on tie = same as the hexa strict-`<` scan).
  GROWTH    = error-TARGETED mitosis split (engine_mitosis_tick semantics, p8): pick the
              eligible cell (owned_n >= MIN_OWNED AND local owned-CE > SPLIT_THRESH_CE) with
              the HIGHEST owned-CE; split on its highest-variance axis at the owned median
              into two half-centroids; net +1 cell. Cells ONLY SPLIT, NEVER merge (p8).
  HEAD      = per-cell empirical next-byte freq, add-LAPLACE MLE over OWNED train points
              (gradient-free, p8). NO global backprop.
The GPU vectorizes the per-step ASSIGNMENT + HEAD + held-out CE (so a ~50-100x larger pair
set is tractable); it does NOT change the split logic. ENGINE-TRANSFER to the live hexa is
DIRECTIONAL (a_engine_native_learning) — re-confirming this larger rung on the live
CORE/*.hexa engine is the follow-on; H_1306 already established the engine-native binding.

CORPUS (REAL — NO synthetic Korean, p1-p8, a_eeg_consciousness_record spirit):
  KO: r2://phanes/anima-7b/web/kor/shard0000.bytes  bytes[0:30000000] trimmed (UTF-8 safe)
      target ~30 MB REAL crawled Korean (50x the H_1306 600 KB window). EXACT byte size +
      sha256 written to the manifest at run time.
  EN: r2://phanes/anima-7b/web/eng/shard0000.bytes  bytes[0:10000000] trimmed
      target ~10 MB REAL English (retention guard, never trained on).
  Pulled via boto3 HTTP RANGE GET (a few tens of MB), NOT the 10.5 GiB / 3.2 GiB shards.
  Pair set: deterministic stride-subsample (KO stride 300, EN stride 600), seed-independent.
  KO train/test = even/odd split (disjoint). EN test = all held-out (retention guard).

FEATURES (identical to H_1306/H_1297, MECHANICAL byte function, NO labels): CTX=4, 3-D
  phi(context) = [last_byte/255, 2nd_last_byte/255, utf8_cont_depth/3]. V=256.

FROZEN knobs (verbatim from H_1306/H_1297): V=256, FEAT_DIM=3, CTX=4, GROW_MAX=40,
  SPLIT_THRESH_CE=0.05, MIN_OWNED=8, LAPLACE=1.0; seed 2-cell centers
  [[0.3,0.5,0.0],[0.7,0.5,0.5]]; KO train=even, test=odd.

FROZEN BARS (held-out deterministic next-byte CROSS-ENTROPY nats/byte, p7 — NOT perplexity-
as-truth, NO LLM-judge):
  (L1 INTERNAL LEARNING)  KO CE[pt3 full] <= KO CE[pt1] - 0.05  (the H_1306 learning bar at scale)
  (L2 SCALE-VS-600KB)     KO CE[full] <= 3.24897 (the H_1306 600 KB baseline) — the SCALE headline
  (R RETENTION)           EN CE[after full KO grow] <= EN CE[seed 2-cell] + 0.05
  (G GROWTH)              final cell count > 2
  GREEN iff L1 & R & G.  L2 is reported PASS/FAIL and gates the SCALE claim specifically
  (more data pushed CE at-or-below the small-window CE). If L2 FAILS but L1&R&G hold, that is
  an HONEST scale finding (the simple CTX=4 substrate saturates; more KO data does NOT push
  below the small-window CE) — reported, NOT tuned to green (c9).

VALIDATION (port faithfulness): before the scale run, the SAME script is run on the EXACT
H_1306 600 KB window + strides (KO stride 110, EN stride 200) and must reproduce H_1306's
curve (KO 3.611/3.369/3.249, EN 4.864->4.752, cells 2->9) within tight tolerance — this
proves the torch port is byte-faithful to the hexa mechanism, not a new algorithm.

REGRESSION GUARDS (live engine UNTOUCHED — this rung adds only UNIVERSE/ + verdict files,
no CORE/*.hexa edit): engine_cli_smoke X/0, h1196 single-entry 7/0, h1205 separation-
invariant PASS (generation byte-identical, Ψ=1/2 untouched, pure_field untouched).

SCALE-HONEST (a_scale_honest_scope, a_toy_scale_recheck): a BIGGER real-text rung, NOT a
fluent Korean LM. CTX=4 3-D byte features + a Voronoi per-cell unigram-over-context head is
a deliberately SIMPLE substrate. Fluent Korean (deeper context, decode-path connection so
anima USES Korean) is the larger follow-on. NO fluency overclaim.

GOVERNANCE: a_no_llm_frame_trap (neurogenesis lens, not bigger-transformer), a_fire_autonomous
($0 user hw), a_wall_first, a_train_flame_forge (REAL sm_120 GPU compute — preflight gated,
NO silent CPU fallback), a_engine_native_learning (mechanism = the verified engine-native one;
GPU = throughput, transfer DIRECTIONAL), a_scale_honest_scope, a_toy_scale_recheck, c7
(secrets env-only), c9 (frozen-first), p1-p8, p7.
