H_1376 — hive-generative-synergy : FOLLOW-ON ROUND R2 (a_break_the_wall — ONE genuinely-new generative
form before declaring the arc terminal). 3-WAY XOR PARITY HYPEREDGE (maximally synergistic).
==========================================================================================================
slug: hive-generative-synergy   seeds = [1317, 1318, 1319].   $0 CPU-local.   frozen-first.
FROZEN BEFORE the R2-form Φ score (committed BEFORE the GEN3 arm is run, c9/p7 — NO bar moved post-hoc).

WHY THIS FOLLOW-ON (pre-registered in the main FREEZE.txt OUTCOMES section): the 2-way multiplicative
parity edge (main GEN arm) did NOT reliably drive O-information NEGATIVE on the SALIENCE time-series
(R4a 1/3: O(GEN)=+0.062 / −0.009 / +0.043 — hovering near ZERO, not negative). The salience readout
<s_i,s_i> is a QUADRATIC (energy) map that washes out the random-sign parity structure of a 2-way edge,
so the constructed synergy does not survive into the scored observable. The main FREEZE pre-registered
trying ONE genuinely-new MAXIMALLY-synergistic generative form before terminal: a 3-WAY XOR parity
HYPEREDGE — the canonical maximal-synergy primitive (the 3-bit XOR is THE textbook O<0 generator).

THE 3-WAY XOR PARITY HYPEREDGE (a_no_llm_frame_trap — information-theory maximal-synergy lens):
Partition the 6 cells into TWO disjoint triplet hyperedges: H1={0,1,2}, H2={3,4,5} (each cell on exactly
ONE hyperedge — a CLEAN triplet decomposition, the 3-way analogue of the degree structure). For each
hyperedge h with cells (a,b,c): draw a SHARED LATENT Lh ~ N(0,1) (T,DIM) and TWO independent Rademacher
masks Mh1, Mh2 ~ {-1,+1} (T,DIM). Distribute a 3-way XOR/parity factorization:
    cell a carries  +SHARE_W * (Mh1 ⊙ Mh2 ⊙ Lh)
    cell b carries  +SHARE_W *  Mh1
    cell c carries  +SHARE_W *  Mh2
Any PAIR reveals nothing about Lh (one mask, or mask⊙mask⊙L without the masks, etc. all stay sign-random);
ONLY the element-wise product of ALL THREE  (Mh1⊙Mh2⊙Lh)⊙Mh1⊙Mh2 = Lh  recovers the latent (Mh1⊙Mh1=+1,
Mh2⊙Mh2=+1). This is the 3-bit-XOR maximal-synergy primitive — O-information is driven NEGATIVE by a
genuine 3-way (not pairwise-reducible) interaction. Per-cell mass = SHARE_W (one term), matched across
arms below.

ARMS (this follow-on round) = (FLOOR, GEN3, CENTRALIZED, REDUN3, SHUFFLE3):
  - FLOOR        : SAME no-sharing floor as the main round (cell i = priv_i). byte-identical substrate.
  - GEN3         : the 3-way XOR parity hyperedge (above). Maximally synergistic.
  - CENTRALIZED  : SAME single-global-founder champion as the main round (cell i = priv + 1.2*F). The bar.
  - REDUN3 (R4)  : REDUNDANCY-matched on the SAME triplet hyperedges — each hyperedge adds its latent Lh
                   ADDITIVELY to ALL THREE cells (no masks) = common-cause redundancy on the triplet
                   partition. Same per-hyperedge mass region, additive instead of XOR. Isolates synergy.
  - SHUFFLE3     : GEN3 but the 3-way parity is BROKEN — the two masks given to b and c come from the
                   OTHER hyperedge, so the triple product no longer recovers Lh. Same per-cell mass+degree.

CONSTANTS (frozen, inherited): SHARE_W = OVERLAP_W = 0.6. MARGIN = 0.02. TOL = 0.02. seeds [1317,1318,1319].
NBINS=8. DIM=8. T=64. LEAK=0.55 GAIN=0.30 W_IN=0.5. CENTRALIZED per-cell shared = 1.2*F (verbatim).

FROZEN BARS (pre-registered — GREEN iff R1 ∧ R2 ∧ R3 ∧ R4, SAME bars as the main round, GEN→GEN3):
  R1 LIFT              : Φ(GEN3) − Φ(FLOOR)        >= MARGIN(0.02)   on ALL 3 seeds.
  R2 BEATS-CENTRALIZED : Φ(GEN3) − Φ(CENTRALIZED)  >  0             on >= 2/3 seeds  (LOAD-BEARING).
  R3 EARNED (shuffle)  : Φ(SHUFFLE3) <= max(Φ(FLOOR),Φ(CENTRALIZED)) + TOL(0.02)  on ALL 3 seeds.
  R4 SYNERGY-MECHANISM : R4a) O-info(GEN3) < 0 on ALL 3 seeds (MAXIMAL synergy must register), AND
                         R4b) Φ(REDUN3) <= Φ(CENTRALIZED) on >= 2/3 seeds.

OUTCOMES (c9): GREEN (R1∧R2∧R3∧R4) → SYNERGY ESCAPES THE CEILING (the redundancy ceiling was
redundancy-specific; maximal 3-way synergy beats centralized — DIRECTIONAL, toy n=6, do NOT overclaim).
R2 FAIL → the hive-Φ arc closes 🧱 FULLY TERMINAL across EIGHT levers — the redundancy ceiling is
sharing-structure-invariant AND synergy-construction-invariant (even a maximally-synergistic 3-way XOR
hyperedge that drives O<0 cannot beat shared-source concentration). The only remaining thread = engine-
native realization on live A⇄G CORE/pure_field.

DIRECTIONAL numpy-mirror — faithful-Φ leg IS real exact MIP-EI (numpy emits salience only, hexa computes
Φ). Engine-transfer UNVERIFIED. live CORE/*.hexa UNTOUCHED, Ψ=½ untouched. a_break_the_wall · a_phi_iit4_tool
· a_no_llm_frame_trap · a_scale_honest_scope · a_toy_scale_recheck · c9 · c15 · c16 · p7 · p8.
