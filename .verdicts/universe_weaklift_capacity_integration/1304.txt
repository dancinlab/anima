### Hc_1304 — 🟠 DEFERRED
### reason: no $0 CPU-local measurement path in this tree.
### measurement path: recurrent-edge / plastic-locus ablation at matched param budget (on-chip or GPU sim); H_278 MIP machinery local but runner hardcodes cross-tree import path
### out of scope for CPU-local lane (a_cpu_local_no_waiter); NO GPU/chip fire dispatched here.
