### Hc_1305 — 🟠 DEFERRED
### reason: no $0 CPU-local measurement path in this tree.
### measurement path: factorial backbone-seed × chip-run (≥3×3) trace collection on AKD1000; no checkpointed multi-seed Lane A traces in local tree
### out of scope for CPU-local lane (a_cpu_local_no_waiter); NO GPU/chip fire dispatched here.
