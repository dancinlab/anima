H_1391 — §UsageStore engine-native wire-in (tool-USAGE learning layer-2) : the BINDING follow-on of H_1389.
PRE-REGISTERED FREEZE — the FOUR gating bars are the H_1389 mirror's frozen thresholds, re-scored
ENGINE-NATIVE on the LIVE CORE/engine_cli.hexa §UsageStore. NO threshold moved (frozen-first, c9, NO
tune-to-green, p7). substrate: $0 CPU, hexa-native, deterministic fixed env (no rng), byte-repeatable.

=== WHAT IS RE-SCORED ===
H_1389 R1 (numpy MIRROR, 🟢 GREEN, DIRECTIONAL) proved tool-USAGE learning is a DISTINCT learnable
LAYER-2: a usage-learning FULL arm rises 0.250→0.750 while a SELECTION-only arm (right tool, default
arg) stays at 0.250. §UsageStore realizes that lever ENGINE-NATIVE — the TWIN of §SkillStore (H_1382),
value = (corrected arg, ordered steps) keyed by (ctx+tool+observed-error), same immune_embed_key
geometry + L2 FIRE/ABSTAIN band (0.55) + engine_mitosis_tick clonal split (p8).

=== ENGINE ENV (deterministic, no rng — byte-repeatable) ===
12 tasks across 6 tools (2 per tool). Each task = (ctx, tool, correct_arg, ordered_steps). correct_arg
is biased AWAY from the tool's DEFAULT arg so a fixed-default SELECTION arm fails the usage even with
the right tool. SUCCESS = proposed arg == correct arg (tool is GIVEN; selection solved by layer-1).
Arms: FULL teaches the corrected usage on each usage failure (mitosis ON) · SELECTION right tool /
fixed default arg / never teaches · SHUFFLE teaches toward a PERMUTED ctx→arg, SCORED vs the TRUE arg.

=== FROZEN GATING BARS (verbatim from H_1389 FREEZE — NOT moved) ===
(1) USAGE-LEARNS:           FULL final completion - FULL init       >= +0.30
(2) DISTINCT-FROM-SELECTION FULL final - SELECTION final            >= +0.30   (KEY BAR)
(3) EARNED (shuffle):       SHUFFLE final - SELECTION final         <= +0.15
(4) NO-FAB / ABSTAIN:       untrained far (ctx+tool+err) abstain    == "" (no fabricated arg)
(non-gating) ORDERED STEPS: a fired usage-cell carries the ordered step sequence IN ORDER.

=== TIER MAPPING (frozen) ===
bar1 AND bar2 AND bar3 AND bar4  -> GREEN engine-native BINDING (usage IS a distinct learnable layer-2,
                                    re-confirmed engine-native; a_verified_must_wire follow-on closed).
NOT bar2 (selection completes)   -> PARTIAL/wall — report honestly, do NOT manufacture the gap.

=== NO-REGRESSION GUARDS (additive faculty — must not perturb Ψ; same as §SkillStore) ===
engine_cli_smoke tally (was 110/0 → +5 usage cases 120-124) · h1196 single-entry 7/0 ·
h1205 separation-invariant (generation byte-identical ON==OFF + Ψ Φ-checksum invariant).

p1/p2/p3/p6 GUARD: a usage-cell binds from OUTCOME ONLY (the executed call's success/failure + the
observed error), NO injected "use arg A" label / RLHF / persona — the SHUFFLE collapse proves the lift
is the EARNED ctx→arg correspondence the split encodes, not the act of splitting. Ψ-DISJOINT by
construction (own usage-store; pure_field Φ/phase/Ψ + decoder untouched). p7 (script-checked completion).
