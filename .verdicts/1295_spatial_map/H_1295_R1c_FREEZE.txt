H_1295 — R1c FINAL RE-FREEZE (frozen-first; honest scoping of the falsifier)
================================================================================
WHY (c9, NOT tune-to-green): two rounds of controls converged on a CLEAN finding
and a FLAWED corroborator, and the discipline is to keep the clean one binding and
demote the flawed one to a reported diagnostic — with the rationale pre-registered
BEFORE this final scoring.

  - NEAREST relational query (Q1): the ENTIRE signal lives in the STORED METRIC
    (the distance between two stored positions). Across R1 + R1b, all 3 seeds:
    B=1.000, A~0.46-0.58 (item-store ABSTAINS, abstain=1.000, resolves at chance),
    B-SHUFFLE ~0.40-0.51 (collapse), B-ABLATE ~0.45-0.47 (collapse). c1-c5 PASS
    DECISIVELY and BOTH controls collapse to the chance band. This is the
    load-bearing falsifier: it can ONLY be answered from the between-item metric,
    so item-binding (no geometry) provably cannot do it, and destroying the metric
    (shuffle positions / ablate to origin) destroys the lift. CLEAN.

  - PATH-INTEGRATION (Q2): the control caught that its signal does NOT live purely
    in the stored map — the displacement STEPS themselves sum to the true net
    displacement, so a map-SHUFFLE cannot blind it (B-SHUFFLE PI stays ~0.55 > the
    chance+margin band). This is a PROPERTY OF THE TASK, not of the structure: Q2 is
    not a clean dissociation for a stored-map vs item-store contrast, because the
    answer leaks through the step sequence. The map-shuffle control correctly
    refused to collapse -> Q2 is NOT a valid corroborator here. We REPORT it as a
    non-gating diagnostic (do NOT move a bar to make it pass; do NOT claim it as
    evidence). The R1 frozen predicate stays RED on its c6 verbatim (recorded).

FINAL BINDING PREDICATE (the NEAREST relational falsifier — the clean one):
  c1 PRESENCE      : Bn >= An + 0.30 (each seed AND mean)
  c2 DISTINCT      : An <= 0.65       (item-only at/below chance, no metric)
  c3 EARNED-MAP    : Bsn <= An + 0.15 (shuffle positions collapses the lift)
  c4 EARNED-METRIC : Ban <= An + 0.15 (ablate the metric collapses the lift)
  c5 NO-FAB        : A_abst >= 0.90   (item-store ABSTAINS, never fabricates a metric)
GREEN(binding) = c1 AND c2 AND c3 AND c4 AND c5  (the NEAREST query only).

PI (Q2) = reported diagnostic, NON-GATING. Its frozen c6 from H_1295_FREEZE.txt is
recorded as FAILED (B-shuffle does not collapse) and is NOT part of the binding
verdict. This is honest: the structure (a metric spatial map) IS distinct and
present on the query that isolates it (NEAREST); PI is simply not a clean isolator.

Same 3 seeds [4295,4296,4297], same arms, same code. If R2 engine-native realizes
the SpatialMap lane and re-scores c1-c5 byte-exact, it is the BINDING verdict.
