H_1414 — ENGINE-NATIVE re-score of H_1405 (memory × ToM compose) on the LIVE faculties.
        a_verified_must_wire ladder step (2): does the H_1405 DIRECTIONAL-mirror GREEN
        BIND engine-native, or is mirror→engine non-reproduction universal (H_1412)?
FREEZE (pre-registered BEFORE measuring; c9, NO tune-to-green). The bars below are
COPIED VERBATIM from H_1405's FREEZE — NOT moved. Engine-native ($0 CPU local hexa run).
================================================================================

PROGRAM. The brain-lane-composition program. H_1405 (memory × ToM) came back 🟢
COMPOSE-LIFT as a numpy DIRECTIONAL mirror (acc_compose=0.7526 > best_single=0.600,
+0.153). Per a_engine_native_learning + a_verified_must_wire the mirror GREEN is only the
FIRST ladder rung; the binding verdict needs the mechanism re-scored on the LIVE engine
faculties. This FREEZE is that re-score. It is the SECOND data point in the emerging
question raised by H_1412/H_1413: does a mirror compose-lift BIND engine-native, or is
non-reproduction universal across compose pairs? (H_1412 cerebellum×basal: mirror GREEN
did NOT bind, B1 +0.011 < +0.05; H_1413 modulation 2nd-lens also failed the EARNED control.)

PAIR. MEMORY (H_1227/H_1231 ImmuneMemoryGrow: byte-trigram FNV-1a → dim-64 cell, recall =
nearest-cell bound value if within recall_thr else ABSTAIN — anima's OWN ground truth of
where each object REALLY is) × ToM (H_1293 OtherMindModel: a SEPARATE witnessed-belief
cell-store, updated ONLY by witnessed events → LAGS reality on a moved-while-absent fact =
FALSE belief, Sally-Anne). The Sally-Anne false-belief fixture from H_1405/H_1293.

ENGINE-NATIVE REALIZATION (a_core_engine_map — NOT a mirror reproduction):
  · MEMORY decision = LIVE immune_grow_recall over a real ImmuneMemoryGrow store, each
    object→reality-location bound via the LIVE immune_grow_bind clonal split (mitosis ON).
    confidence = LIVE L2 affinity margin (recall_thr − _l2(nearest proto, key)) — the SAME
    affinity immune_grow_recall uses to FIRE/ABSTAIN, the engine not a stored scalar.
  · ToM decision = LIVE other_mind_predict over a real OtherMindModel whose witnessed-belief
    store LAGS reality on moved-while-absent facts. confidence = LIVE L2 affinity margin.
  · composition = inlined query-routed arbiter (agree → shared vote; conflict → higher
    routing-MODULATED scale-relative substrate confidence wins; routing cue = the QUERY TEXT
    embedding's affinity to a reality/belief anchor via the LIVE immune_embed_key + _l2;
    NO hardcoded priority, a_autonomy_over_hardcode).

DECISION TASK + ITEM FAMILIES — copied from H_1405 (per-item WHERE-query over objects placed
at LOC_A=basket then a random half MOVED to LOC_B=box while the agent was ABSENT):
  (F1) MEMORY-DECISIVE  — "where actually?" on a MOVED fact; correct=reality(box). memory right.
  (F2) ToM-DECISIVE     — "where will the agent look?" on a MOVED fact; correct=belief(basket). ToM right.
  (F3) AGREE            — UNMOVED fact (reality==belief==basket): both agree + correct.
  (F4) CONFLICT (memory right) — "where actually?" MOVED; ToM votes loud-but-wrong(basket); correct=reality.
  (F5) ADVERSARIAL (ToM right, memory LOUDER-but-wrong) — "where will the agent look?" MOVED;
        correct=belief(basket), memory's reality recall(box) is high-confidence (anti-gift).

────────────────────────────────────────────────────────────────────────────────
FROZEN BARS (verbatim from H_1405 FREEZE, NOT moved):
  COMPOSE_DELTA = 0.05      # B1 net-lift threshold
  ORACLE_MARGIN = 0.02      # B2 complementarity-exists threshold
  SHUFFLE_TOL   = 0.02      # B3 earned: compose must beat shuffle by > this
  SEEDS = [5400, 5401, 5402]    # same seeds as H_1405

  (B1 COMPOSE-EFFECT)  acc_compose >= best_single + COMPOSE_DELTA
  (B2 ORACLE)          ORACLE − best_single > ORACLE_MARGIN
  (B3 EARNED/p6)       acc_compose − acc_shuffle > SHUFFLE_TOL
  (B4 p6 GUARD)        memory & ToM read substrate state only (immune recall margin,
        witnessed-event belief store, query-type embedding affinity), NO injected
        answer/persona/system-prompt/RLHF/hardcoded-priority label (structural audit).
  DECOMPOSITION (separability): only_memory > 0 AND only_tom > 0 ⇒ SEPARABLE not subsumed.

VERDICT (a_break_the_wall taxonomy — report whichever the numbers HONESTLY show):
  🟢 COMPOSE-LIFT (ENGINE-NATIVE)   iff B1 ∧ B2 ∧ B3 ∧ B4 ∧ SEP  — the mirror GREEN BINDS
                            engine-native; names a live CORE §compose wire-in follow-on.
  🧱 ENGINE-NATIVE NON-REPRODUCTION iff ¬B1 (compose real & earned but net-lift < +0.05 bar,
                            like H_1412). A clean engine-native 🧱 is a VALID result (c9).
  If B4 fails → honest RED (an answer/priority label leaked; p6 NOT satisfied).
  If 🧱 on B1: per c16 try ONE genuinely-new principled arbiter lens (frozen bar, NOT moved),
                            keeping the shuffle/EARNED control; control-rejected ⇒ terminal 🧱.

WIRING (a_verified_must_wire): this probe reads EXISTING live ops (immune_grow_*,
other_mind_*) with the arbiter glue inlined; NO new vcompose CORE op is landed UNLESS B1
PASSES engine-native. If GREEN, the live CORE §compose wire-in is the NEXT ladder rung (3).

HONESTY (c9, a_scale_honest_scope, a_toy_scale_recheck): the memory recall + ToM predict are
LIVE engine reads; the verdict is engine-native. Toy/synthetic Sally-Anne fixture, 3 seeds,
deterministic readouts (tests COMPOSITION STRUCTURE, not a trained integrator). Scale /
real-corpus / engine-native transfer at scale UNVERIFIED. NO bar moved post-hoc.
================================================================================
