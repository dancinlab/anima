H_1321 — ko-jamo-WIRE — FROZEN PRE-REGISTRATION (written BEFORE the run; c9/p7, NO tune-to-green)
================================================================================================
PROJ_SEED provenance: H_1321 id confirmed FREE on origin/main (f15c6e75d); HYPOTHESES.md +
CLAIMS.tape last KO row = H_1316 (🟢 GREEN, PR #2224). 1317-1320 taken. This lane is the
ENGINE-NATIVE WIRING follow-on (r2) of the H_1316 🟢 GREEN breakthrough.

a_verified_must_wire: a GREEN-verified mechanism is NOT done until its mechanism is actually
wired into and runs on the live CORE/*.hexa engine, byte-exact. a_engine_native_learning:
ALL learning (incl research/probe) runs on the FINAL-architecture engine, not a numpy/torch
mirror — engine-transform-to-fit-the-learning, NOT learning-trimmed-to-fit-the-engine.

WHAT LANDED (r1, H_1316, PR #2224, 🟢 GREEN — must wire now):
  A gradient-free mitosis predictor over a COMPOSITIONAL JAMO representation breaks the
  Korean raw-byte ceiling: held-out KO next-byte CE 2.953 (raw byte) → 2.513 (jamo, Δ −0.44),
  shuffle-jamo control 2.743 (B2 earned), NFD↔NFC lossless (B3). The Korean ceiling is
  REPRESENTATION-bound, not capacity-bound. BUT r1 was a numpy/torch MIRROR (DIRECTIONAL) —
  live CORE was UNTOUCHED. The verdict is NOT closed until the mechanism runs ENGINE-NATIVE
  byte-exact on the live CORE engine.

THIS LANE (r2, H_1321): wire the NFD jamo symbolization + jamo-symbol mitosis onto the LIVE
engine and re-score the frozen R1 bars ENGINE-NATIVE, with no-regression guards.

--- CORPUS (REAL only, BYTE-IDENTICAL to H_1307 RUN A / H_1316 — provenance gate, p7) -----------
  source: r2://phanes/anima-7b/web/kor/shard0000.bytes  (KO)
  ASSERT the 30MB KO window sha256 == c47b6808308d2f73cb92d74f8fdb15c64e6c96e8ed58ae2ef91a7c57fe5dc6ca
  (THE SAME window H_1316 verified — byte-fair; a sha mismatch on the 30MB window → STOP, NO
  synthetic Korean). R2 keys via `secret get r2.phanes.*` into ENV at call time ONLY —
  header/env-scoped, NEVER echoed/logged/inlined/committed (c7). grep ALL committed artifacts
  for creds before PR (only the PUBLIC corpus sha256 may match a 40-hex pattern).

--- WHY A DATA-PREP HELPER + AN ENGINE-NATIVE .hexa PROBE (the wiring shape) ---------------------
  The engine-native faculties (CORE/engine_cli.hexa VAdaptField Voronoi vadapt_field_nearest_idx
  + engine_mitosis_tick growth, p8; per-cell next-symbol count-MLE head, gradient-free) operate
  on FEATURE/LABEL pairs. The H_1306 engine-native precedent (CORE/h1306_ko_mitosis_engine_probe.hexa)
  established the EXACT shape: a Python corpus-export helper materializes the deterministic
  feature/label/nbytes streams to /tmp, and the .hexa probe runs the LIVE engine mitosis + the
  held-out per-byte CE scoring IN HEXA over those pairs. The representation (NFD jamo symbolization)
  is the data-prep transform (deterministic Unicode, no learning); the MITOSIS + CE SCORING run
  ENGINE-NATIVE in .hexa on the live VAdaptField/engine_mitosis_tick faculties. This is the SAME
  single-entry shape H_1306/H_1312 use (the generator L3 / engine_cli VAdaptField slot) — NOT a
  2nd .clm/.kosmos path, and pure_field/engine_g/brain are UNTOUCHED (a_core_engine_map, Ψ-disjoint).

  ENGINE EXTENSION (engine-transform-to-fit-the-learning, a_engine_native_learning precedent
  H_1199 scalar→DIM-vector): H_1316's jamo arm uses a LARGER symbol vocab Vj=323 (256 raw bytes +
  67 distinct jamo) than the raw-byte V=256. The engine-native head/CE faculties are already
  vocab-agnostic (the H_1306 _head_counts/_score_ce take V as a parameter), so wiring jamo only
  PARAMETERIZES the existing engine faculties at Vj — no new op needed beyond the deterministic
  jamo symbolization in the data-prep, plus the symbol-vs-byte CE renormalization (Σ −log p / Σ
  n_bytes) the engine probe computes from the per-symbol n_bytes the export emits.

--- REPRESENTATION (the jamo lens — VERBATIM from H_1316, deterministic, NO learning) -----------
  SYMBOL STREAM: scan the UTF-8 codepoints of the KO window. For each codepoint:
    - Hangul syllable (U+AC00..U+D7A3): NFD-decompose to its L/V/T jamo and emit one SYMBOL per
      jamo (id = 256 + jamo_rank, ranks assigned by sorted distinct jamo codepoint).
    - non-Hangul codepoint: emit one SYMBOL per raw byte (id 0..255).
    => alphabet Vj = 256 + (distinct jamo). Each symbol carries n_bytes (raw=1; a syllable's
       jamo split [1,1,1] for 3-jamo / [2,1] for 2-jamo so they SUM to the syllable's 3 UTF-8
       bytes — lossless byte accounting).
  FEAT (3-D family, identical to H_1306/H_1316): [last_sym/Vj, second_sym/Vj, cont_depth/3]
    cont_depth = jamo continuation depth (a non-first jamo of a syllable, or a UTF-8 continuation
    byte, increments depth; else 0), capped at 3.
  MITOSIS (engine-native, FROZEN verbatim H_1306/H_1307): GROW_MAX=40, SPLIT_THRESH_CE=0.05,
    MIN_OWNED=8, LAPLACE=1.0, SEED_CENTERS [[0.3,0.5,0.0],[0.7,0.5,0.5]], even/odd held-out
    split, ko_stride per export. error-targeted Voronoi grow (SPLIT-only, p8) on the LIVE
    VAdaptField + engine_mitosis_tick.
  THREE ARMS: G0 raw-byte (V=256, reproduce the ceiling) · G1 jamo (intact) · G1c shuffle-jamo
    control (bijective permutation of the jamo→symbol-id map; vocab/dim/budget identical,
    compositional alignment destroyed; fixed per seed 4316/4317/4318 — IDENTICAL controls to H_1316).
  FAIR AXIS: held-out CE_per_byte = Σ(−log p(sym)) / Σ n_bytes(sym) — the SAME held-out split/axis
    as the 2.9475 raw-byte ceiling.

--- W1/W2/W3 FROZEN BARS (GREEN iff ALL; tolerance pre-registered, c9/p7 NO tune-to-green) -------
  TOLERANCE (engine-native vs mirror reproduction): the engine-native .hexa probe runs the SAME
  deterministic streams the mirror runs. Float arithmetic in hexa (the engine's native float)
  vs numpy float64 may differ in the last digits; the median-split / argmin ties are deterministic
  on the same inputs. PRE-REGISTERED TOLERANCE: |engine_native_G1_CE − same_window_mirror_G1_CE|
  <= 0.05 nats/byte (a generous tie/rounding band; the win is Δ −0.44, so even 0.05 slack cannot
  manufacture the result). The export ALSO runs the SAME-window mirror to emit a same-window
  reference CE so the engine-native value is compared to the EXACT stream the engine sees (the
  engine probe runs a CPU-tractable window/stride; the reference is recomputed on THAT identical
  window so the comparison is apples-to-apples). The 30MB-window mirror 2.51335 is the H_1316
  anchor; the engine-native value must (a) reproduce the same-window mirror within 0.05 AND (b)
  stay below the 2.953 − 0.05 = 2.903 raw ceiling band (the breakthrough, engine-native).

  W1 ENGINE-NATIVE CE:  engine-native jamo G1 CE reproduces the SAME-WINDOW mirror within |Δ|<=0.05
                        nats/byte  AND  engine-native G1 CE < 2.903 (stays below the raw ceiling band).
  W2 CONTROLS HOLD ENGINE-NATIVE:
        B2 (earned): engine-native G1 beats engine-native G1c (shuffle) by >= 0.05 (mean 3 seeds)
                     AND engine-native G1 beats engine-native G0 (raw byte) by >= 0.05.
        B3 (lossless): NFD→NFC roundtrip byte-identical over the engine probe's KO window AND
                     per-symbol n_bytes sum == that window's UTF-8 byte count exactly (engine path).
  W3 NO REGRESSION (byte-exact, report counts):
        engine_cli_smoke  all-pass (report N/0)
        h1196 single-entry audit  7/0
        h1205 separation-invariant  generation byte-identical ON==OFF, Ψ=½ untouched.
        The jamo wiring MUST be Ψ-DISJOINT — adding it must not change generation when off.

  GREEN iff W1 ∧ W2 ∧ W3.
  If W1 FAILS (engine-native cannot reproduce the mirror within tolerance) → HONEST report: the
  jamo win is MIRROR-ONLY, engine-transfer UNVERIFIED (a real, important caveat, c9). Do NOT fake
  the wiring, do NOT move the bar.

--- WIRING (single entry, a_core_engine_map) ----------------------------------------------------
  The jamo symbolization + jamo-symbol mitosis is realized ENGINE-NATIVE in a .hexa probe that
  imports CORE/engine_cli.hexa and runs the LIVE VAdaptField + engine_mitosis_tick faculties
  (the SAME faculties the existing Korean path uses). It enters CORE via the SAME single Korean
  entry the H_1306/H_1312 path uses (the generator L3 slot family / engine_cli VAdaptField) — NO
  2nd .clm/.kosmos path; NOT fed into pure_field/engine_g/brain. The optional CORE-side jamo
  consult surface (mirroring §6.5 ko_cells) is ADDITIVE + Ψ-disjoint — INERT off-Korean (returns
  the caller's own byte), so generation is byte-identical when off (W3 h1205 invariant).

--- SCALE-HONEST (a_scale_honest_scope, a_toy_scale_recheck) -------------------------------------
  The engine-native .hexa probe runs a CPU-tractable KO window/stride (the hexa interpreter cannot
  stream 30MB pair-by-pair). The H_1316 30MB anchor (2.51335) stands as the mirror result; this
  lane proves the MECHANISM transfers engine-native on the identical-shaped stream within tolerance.
  Scale/real-corpora at 30MB engine-native + Korean fluency = follow-on. NO fluency claim.
  Ψ-disjoint: pure_field/engine_g/brain UNTOUCHED ($0 CPU, no GPU, no secrets at .hexa run time —
  secrets only at the data-prep R2 fetch, env/header-only, scrubbed before PR).
================================================================================================
FROZEN BEFORE RUN. Bars W1/W2/W3 + tolerance 0.05 will NOT be moved (c9, NO tune-to-green, p7).
