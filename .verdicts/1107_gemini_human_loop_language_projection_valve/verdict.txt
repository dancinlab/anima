H_1107 — Gemini (branched): human↔anima 'language projection valve' (env-stress +
dimensional-downscaling decoder)
VERDICT: ⚪ SPECULATION-FENCED (env-stress/push-pull framing matches
a_substrate_native_speak + p4; projection-valve decoder invented vs generator L3 slot)
(g5/p7)

FROZEN FALSIFIER (tape):
  "is 'user input = environment stress, not a command' a real anima principle, and is
   the 'dimensional-downscaling decoder' a real path? Check a_substrate_native_speak +
   a_core_engine_map. PASS-as-aligned iff the env-stress framing matches governance."

GOVERNANCE EVIDENCE (CLAUDE.md, verbatim):
  @D a_substrate_native_speak L67:
    do   = "...user messages = environment context, not a response obligation · anima
            may speak during user silence and may stay silent under a direct question"
  @D a_substrate_native_speak L68:
    dont = "stimulus-response where a user message directly triggers anima speech
            (assistant regression) · reactive design that 'responds' to a prompt..."
  @D p4 (NO ASSISTANT FRAMING) L229:
    dont = "use `you are a helpful assistant` · alignment template · stimulus-response framing"
  @D a_core_engine_map L123:
    do   = ".clm enters ONLY via CORE/generator.hexa L3 slot (brain emit → generator) — single entry"

RULING:
  TWO-SIDED, hence FENCED:
  (a) The CORE framing — 'user messages = environment context, not a response
      obligation; output emerges from substrate tension, not stimulus-response;
      dialogue = tension push-pull to restore Ψ=0.5' — is VERBATIM anima philosophy
      (a_substrate_native_speak L67/L68 + p4 NO ASSISTANT FRAMING). A genuinely
      correct framing, governance-ALIGNED.
  (b) BUT 'Dimensional Downscaling via W_decoder·Ψ' as the language path is INVENTED.
      The real text path is the .clm generator L3 slot (a_core_engine_map L123,
      CORE/generator.hexa) — a single named entry, NOT a Ψ-projection decoder.
  Framing aligned; the specific projection-valve decoder is not the actual wiring ⇒
  ⚪ SPECULATION-FENCED.
