H_1326 — ko-featural r2 — FROZEN PRE-REGISTRATION (written BEFORE the run; c9/p7, NO tune-to-green)
==================================================================================================
PROJ_SEED provenance: H_1326 id confirmed FREE on origin/main (395a00146; last KO/MITOSIS rows =
H_1316 🟢 GREEN jamo, H_1322 🧱 HONEST-FLOOR featural-r1 #2229). H_1316..H_1325 all taken. This
lane is r2 of H_1322 — a 🧱 result whose r1 agent DISCLOSED two confounds (c16 / a_break_the_wall:
wrong method, NOT a real wall):
  (1) GEOMETRY CONFOUND — r1's seed_centers_dim(3)=[[0.3,0.3,0.0],[0.7,0.7,0.5]] differed from
      H_1316's exact seed centers [[0.3,0.5,0.0],[0.7,0.5,0.5]] (middle coord 0.5). The gradient-
      free mitosis is SEED-CENTER-SENSITIVE, so r1's in-run jamo re-port was 2.85983 (NOT the
      locked 2.51335). The featural-vs-locked-jamo F1 comparison was geometry-confounded. Under
      the SAME (r1) geometry, featural beat the in-run jamo re-port by -0.129 and the shuffle by
      +0.042 (a real but sub-bar signal).
  (2) LOSSY PATH — features drove ONLY the Voronoi PARTITION; the prediction TARGET stayed the
      OPAQUE jamo id, so Hangul's designed systematicity never entered the TARGET.

This lane fixes BOTH, both pre-registered frozen-first.

a_no_llm_frame_trap: linguistics / script-design lens (Hunminjeongeum featural design), NOT scale.

--- CORPUS (REAL only, BYTE-IDENTICAL to H_1307 RUN A / H_1316 — provenance gate, p7) ------------
  source: r2://phanes/anima-7b/web/kor/shard0000.bytes bytes[0:30000000] trimmed[:29999999]
  ASSERT ko_window_sha256 == c47b6808308d2f73cb92d74f8fdb15c64e6c96e8ed58ae2ef91a7c57fe5dc6ca
  If the sha differs → STOP and report honestly (NO fabricated Korean; REAL only). R2 keys via
  `secret get r2.phanes.*` into ENV at call time ONLY — env-scoped, NEVER echoed/logged/committed
  (c7). grep all committed artifacts for leaks before PR. (cache /tmp/h1311_ko_raw.bytes preserved
  for siblings — first 29999999 B == the H_1307 RUN A KO window.)

--- LOSSLESS FEATURAL BIJECTION (the honesty requirement for a fair CE) --------------------------
  Each NFD CONJOINING JAMO codepoint maps to a small INTEGER FEATURE VECTOR. The maps are
  pre-verified LOSSLESS BIJECTIONS within each jamo class (so (feature tuple) <-> (jamo id) is
  invertible → the factorized-target CE is honestly comparable to the opaque-jamo CE):
    INITIAL (choseong, 19 jamo, U+1100..U+1112): [artic, manner, nasal, liquid, affric]
       artic {1 velar,2 alveolar,3 bilabial,4 sibilant,5 glottal,6 zero-ㅇ}; manner {0 plain,
       1 aspirated,2 tense}; nasal {0,1}; liquid {0,1}; affric {0 fricative/other,1 affricate ㅈㅉㅊ}.
       → 19 distinct tuples (VERIFIED lossless). ㄱ=(1,0,..) ㅋ=(1,1,..) ㄲ=(1,2,..) ONE column apart.
    MEDIAL (jungseong, 21 jamo, U+1161..U+1175): [7, vbase, polar, iota, round]
       vbase {1 vertical,2 horizontal,3 combined}; polar {0 neutral,1 yang,2 yin}; iota {0,1};
       round {0,1}. → 21 distinct tuples (VERIFIED lossless).
    FINAL (jongseong, 27 jamo, U+11A8..U+11C2): [artic, manner, nasal, liquid, cluster_idx]
       single finals reuse the initial articulator families; cluster_idx>0 enumerates the compound
       finals so the tuple stays invertible. → 27 distinct tuples (VERIFIED lossless).
    RAW BYTE (id 0..255, non-Hangul): byte-derived columns (hi-nibble, lo-nibble) IDENTICAL across
       every arm → ASCII/markup stays byte-LM-identical; only the Hangul representation varies.
  (Bijection verified by an offline collision check BEFORE freeze; columns/maxima recorded in the
  script. This is a structural fact about the maps, NOT a result.)

--- FIX A: GEOMETRY-FAIR seed-center protocol (identical to EVERY arm) ---------------------------
  ONE pre-registered seed-center protocol, applied IDENTICALLY to every arm (jamo / featural-
  partition / label-factorization / shuffle), so no arm gets a seed-center advantage:
    PROTOCOL = best-of-a-fixed-bank-by-TRAIN-CE. For an arm of partition-dim d, the bank is the
    fixed set of 2-center seed patterns generated by ONE deterministic rule:
      bank(d) = { [a, b] : a=[lo]*d with a[-1]=alo,  b=[hi]*d with b[-1]=ahi,
                  for (lo,hi,alo,ahi) in BANK_GRID }
      BANK_GRID (FROZEN, 5 patterns) =
        (0.3,0.7,0.0,0.5)   # the H_1316 / H_1307 canonical pattern (last coord 0.0/0.5)
        (0.3,0.7,0.3,0.7)   # last coord follows the body (r1's accidental pattern)
        (0.5,0.5,0.0,0.5)   # body fixed at 0.5 (the H_1316 middle-coord shape lifted)
        (0.25,0.75,0.0,0.5) # wider spread, canonical last coord
        (0.4,0.6,0.2,0.8)   # narrow body, spread last coord
    For d=3 this bank INCLUDES the exact H_1316 centers [[0.3,0.5,0.0],[0.7,0.5,0.5]] as pattern
    (0.5,0.5,0.0,0.5)→[[0.5,0.5,0.0],[0.5,0.5,0.5]]? NO — H_1316's body is 0.3/0.7 with MIDDLE
    coord pinned to 0.5. So we ALSO inject the EXACT H_1316 3-D centers into the d=3 bank as a 6th
    explicit member, and the analogous "body 0.3/0.7, all-but-last interior coords pinned to 0.5,
    last coord 0.0/0.5" member into EVERY d's bank (the H_1316-FAMILY pattern lifted to dim d).
    SELECTION = grow the mitosis from EACH bank member on TRAIN ONLY, pick the member with the
    lowest TRAIN held-out CE (even/odd within train — NO test peeking), score that ONE on TEST.
    The SAME bank-generation rule + SAME selection criterion apply to every arm → geometry-fair.
  CALIBRATION ANCHOR: the jamo arm under this protocol MUST reproduce H_1316's 2.51335 byte-exact
  (the H_1316-exact member is in its bank and is its best). If the jamo arm's protocol-best CE !=
  2.51335 (±0.0005) → STOP: the protocol is not faithfully reproducing the anchor, report honestly.

--- FIX B: LABEL-FACTORIZATION (the deeper test — design enters the TARGET) ----------------------
  A SECOND scoring head whose TARGET is the FACTORED feature vector, not the opaque jamo id:
    per cell, predict (a) the next symbol's CLASS c ∈ {INITIAL, MEDIAL, FINAL, BYTE} via a count-
    MLE over classes, then (b) GIVEN the class, predict each design feature COLUMN INDEPENDENTLY
    via per-column count-MLE heads (low cardinality: 2..8 each), then COMPOSE:
       -log P(jamo) = -log P(class) + Σ_col -log P(feat_col | class, cell)        (factorized)
    For a raw BYTE target, class=BYTE and we predict the byte id directly (256-way), so ASCII CE is
    unchanged vs the jamo arm (apples-to-apples on the non-Hangul tail). Because every within-class
    feature map is a LOSSLESS BIJECTION, the factored distribution is a valid distribution over the
    SAME jamo alphabet (the independence is a MODELLING choice — an UPPER BOUND on the true joint —
    which is exactly the test: does factorizing a hard 51-way choice into easy low-cardinality
    choices PAY OFF?). CE axis = SAME nats/UTF-8-byte (Σ -log P(sym) / Σ n_bytes(sym)).
    CONVERSION stated: a factored prediction for jamo j is scored as the sum of its class-NLL and
    its per-column feature-NLLs; this equals -log of a normalized product distribution over the
    lossless tuple space, re-expressed on the jamo alphabet, divided by the SAME UTF-8 byte count.
    Both Fix-A geometry and the label-factorization head are evaluated; G3 isolates which pays off.

--- MITOSIS MECHANISM (FROZEN, verbatim from H_1306/H_1307/H_1316) -------------------------------
  GROW_MAX=40. SPLIT_THRESH_CE=0.05. MIN_OWNED=8. LAPLACE=1.0. Grow-op = error-targeted Voronoi
  split (highest-owned-CE eligible cell → hi-var-axis owned-median split → two half-centroids;
  +1 cell; gradient-free; cells only SPLIT — p8). even/odd train/test split. ko_stride=300. 3 seeds
  [4326,4327,4328]. The ONLY changes vs H_1316 = (A) the geometry-fair bank protocol applied to
  every arm and (B) the optional label-factorization scoring head.

--- ARMS ----------------------------------------------------------------------------------------
  G0  raw-byte ceiling (opaque-id, V256)            — sanity, must ≈ 2.95342
  A1  jamo opaque-id partition + opaque-id target    — Fix-A protocol; CALIBRATION = 2.51335
  A2  featural partition + opaque-id target          — Fix A only (r1's arm, now geometry-fair)
  A3  featural partition + FACTORIZED target         — Fix A + Fix B (design in partition AND target)
  A2s featural partition + opaque target, SHUFFLED feature map (per-seed bijection) — control
  A3s factorized target + SHUFFLED feature map (per-seed bijection)                 — control
  The shuffle permutes the jamo→feature-vector assignment (a fixed random bijection over the jamo
  set, per seed), destroying the DESIGNED systematicity (ㄱ/ㅋ no longer one-apart) while keeping
  the SAME number of dims/values/marginal. If the lift is the DESIGN, shuffling RAISES CE.

--- FROZEN BARS (pre-registered; GREEN iff ALL of G1 ∧ G2) --------------------------------------
  Let BEST = min(A2, A3) (the better of {featural-partition, label-factorization}, mean 3 seeds).
  G1 GEOMETRY-FAIR DEPTH: under the identical Fix-A protocol, BEST beats the jamo arm A1 by
     ≥ 0.03 (mean 3 seeds) AND beats raw 2.953. (A1 is the protocol-fair jamo, reproducing 2.51335.)
  G2 EARNED: BEST beats its OWN shuffled-feature control (A2 vs A2s if BEST=A2; A3 vs A3s if
     BEST=A3) by ≥ 0.05 — the gain is the DESIGNED systematicity, not dims/vocab.
  G3 FACTORIZATION-ATTRIBUTION (diagnostic, REPORTED even if non-gating): does A3 (label-
     factorization) beat A2 (featural-partition-only)? Isolates whether the design pays off in the
     TARGET (Fix B) vs the PARTITION (Fix A).

  OUTCOMES (all VALID, c9):
   - G1 ∧ G2 → 🟢 GREEN: once the test is geometry-fair AND the design enters the target/partition,
     Hangul's design gives a measurable depth advantage BELOW jamo (overturns the r1 floor).
   - G1 fails geometry-fair → jamo is the genuine, confound-free decomposition FLOOR for this
     mechanism family (terminal 🧱, now confound-free — a clean honest closure).
   - only A3 (Fix B), not A2 (Fix A), clears it → the design pays off in the prediction TARGET, not
     the partition (a precise finding; G3 attributes it).

--- SCOPE HONESTY -------------------------------------------------------------------------------
  toy/DIRECTIONAL numpy/torch mirror; engine-transfer to live CORE/*.hexa = follow-on
  (a_engine_native_learning, a_verified_must_wire). NO Korean-fluency claim (a_scale_honest_scope,
  a_toy_scale_recheck). Held-out DETERMINISTIC next-symbol CE; NO perplexity-as-truth (p7).
==================================================================================================
