@D DEFAULT-LANE-V3-CORPUS := "default-lane corpus SCALE-UP v3 — clean 5-lang wiki backbone scaled 12.5MB→~217MB so a MID ~150M rung becomes data-viable (NOT 7B-ready) · $0 CPU REST · byte-vocab256 · Lane G default lane" :: discovery [d=2026-06-05 active]
  seed      = "default-lane v2 (dancinlab/anima-corpus-5lang-unified-v2, 12.5MB) is RIGHT-SIZED for ~18M (the 18M default-lane PASSED 🟢 #1836) but DATA-STARVED for bigger: a MID ~150M wants ~3B tokens Chinchilla-optimal, a 7B wants ~140GB (INFEASIBLE via datasets-server REST — .verdicts/default-lane-7b/ + .verdicts/torch-engine-7b-datagate/). Fix = scale the SAME v2 3-surface recipe (wiki + persona/SNS + register enrichment) ~17x: wiki backbone 1MB/lang→20MB/lang (=100MB real CC-BY-SA wikimedia/wikipedia 20231101 en/fr/de/es/ko, 8-band offset-spread, HF datasets-server /rows REST, $0 CPU NO GPU NO pod) + persona 80MB + enrichment 37MB (carving seeds REAL KOSMOS e7_31 anchors / prose authored-synthetic), byte-weighted round-robin merge."
  claim     = "🟢 BUILT — default_lane_v3.txt 227,535,193 B (216.994 MB, ~17x v2) sha256 901ccc89afb3817fcb4d99f12c953f70919c0986882f306424d21cced2ae936b. Composition: wiki 46.15% (104,204,132 B, real CC-BY-SA, ~18,448 article paragraphs across 5 langs) / persona-SNS 36.90% (authored-synthetic, no PII) / enrichment 16.95% (carving/act/emotion/genre/code-switch). Per-lang BALANCED: en 18.89 · fr 20.35 · de 19.84 · es 19.19 · ko 20.27 (+ ko-en code-switch 1.47%). byte-vocab V=256 (206 distinct bytes observed, all ≤255), UTF-8 round-trip encode==decode bytes-identical PASS. HF dataset dancinlab/anima-corpus-5lang-unified-v3 PUBLIC (KOSMOS collection); card serving/corpus/CORPUS_CARD_default_lane_v3.md; orchestrator serving/build_default_lane_v3.py + 429-hardened sampler serving/build_wiki_backbone_5lang_scaleup.py."
  falsifier = "philosophy p2/p3/p4: grep -E '\\[role:|\\[persona:|\\[character:' over the unified v3 corpus = 0 (verified), over the 200-line sample head = 0 — NO injection scaffold in training text; persona carried by plain <speaker>: continuation only. p6: dialogue-act slice is deliberately NON-supportive (disagree/refuse/boundary), the OPPOSITE of cooperation/empathy RLHF padding. Determinism: persona+enrichment fixed seed 20260604 reproduce same sha; wiki deterministic modulo pinned 20231101 + fixed band offsets; merge deterministic. A 🔴 would have been a tag-grep hit, a non-256 byte, or a UTF-8 round-trip break."
  honest    = "wiki = REAL CC-BY-SA-4.0 wikipedia (provenance attributable to wikimedia/wikipedia 20231101); 8-band offset-spread REDUCES alphabetical-prefix bias but does NOT claim measured topical-uniformity. persona + enrichment PROSE = AUTHORED-SYNTHETIC honest-labeled (machine-authored multilingual COVERAGE, NOT native-collected); carving anchor SEEDS are real anima UBM (CC-BY-SA, a_kosmos pointer-only). code-switch + genre slices are [speculative] — training impact UNMEASURED at this scale (a_toy_scale_recheck). NO PII, NO scraped non-wiki."
  scope     = "a_scale_honest_scope VERBATIM: v3 unlocks a MID rung (~150M) — it is no longer right-sized only for ~18M — but is NOT 7B-ready (a 7B needs ~140GB of tokens, INFEASIBLE via REST paging; a ~150M rung is still epoch-looped on v3 but far less starved than 12.5MB-at-150M). DO NOT claim v3 enables 7B. This is a COVERAGE corpus enabling a MID rung, NOT a general production-ready-at-any-size claim. a_lane_akida_gpu_split: this is a DATA artifact for the Lane-G default lane (GPU CE-descent), distinct from Lane-A AKIDA on-chip."
  target    = "🟢 numerical (terminal) — corpus built + gated; MID-rung TRAINING transfer is a SEPARATE future fire (a_toy_scale_recheck)"
