# PRESERVE-AS-SSOT — this is a DESIGN audit ledger, not cargo
purpose: anima stage-launch convergence records (H100 Stage-1/Stage-2 launch state schema "anima/h100_stage*/N" — attempts + blocker-resolution chain + live-pod state)
format: JSON records with schema field + _meta + attempts chain
readers: anima stage-launch review; blocker-resolution provenance
never_migrate: true
