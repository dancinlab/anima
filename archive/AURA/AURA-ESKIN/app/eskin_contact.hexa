// AURA-ESKIN app — skin nano-chip (e-tattoo) vs gel/dry EEG contact recovery.
// companion of eskin_contact.py (numpy reference, verified in verify/).
// ⚠ awaiting hexa-lang builtins (handoff f125d45c): gaussian_kernel_1d·separable_blur·r2
// finding: skin-nanochip improves contact/density/SNR but stays extracranial
//          (same skull sigma) -> skull-LPF ceiling unchanged. not a bypass.
let G = 10
let SIG = 1.6   // skull blur, fixed (all contacts are extracranial)
fn recover(M, sigma, snr) {
  // TODO(f125d45c): LF = gaussian_kernel_1d/separable_blur; r2(x, inv(L,y))
  return contact_recovery(M, sigma, snr)   // stub -> py reference is canonical
}
fn main() {
  print("gel-EEG",       recover(64, SIG, 20))
  print("dry-electrode", recover(64, SIG, 15))
  print("skin-nanochip", recover(256, SIG, 25))
  print("tFUS-ref",      recover(64, 0.7, 20))
}
