# ENCODER — log (append-only)

## 2026-05-30 — 도메인 신설

- ENCODER 도메인 신설 (anima 트리). @goal: `.kosmos` `coord`를 실측하는 `E_m` 인코더 구축 → `@anchor`/`@corpus` placement가 design-placeholder → measured 로 전환.
- 동기: kosmos/2.0 `@corpus` 데이터셋(설계 Q1=B 메타-앵커, coord 보유)을 선택하면서, coord를 *재는* 주체가 필요 → 영구 `pending` 회피 위해 도메인으로 격상 (사용자 지시 "인코더도 도메인생성").
- milestone E1~E5 등록 (text → centroid → tension → image/audio → cross-modal verify).
- sibling: ⇄ CLM(corpus 멤버 coord 측정) · ⇄ kosmos(encoder provenance §4.4 주체) · ⇄ UNIVERSE(falsifier 검증).
- 현 상태: 인코더 0개 구현 → 모든 `.kosmos` coord 는 정직하게 `# design placeholder, measured later` (spec §4.3). E1 착수가 다음.
