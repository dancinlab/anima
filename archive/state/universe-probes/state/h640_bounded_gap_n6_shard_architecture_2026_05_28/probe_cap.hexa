// H_640 timing probe — ONE (rule110, cap=K) shard at n=6, state st=21.
// cap passed via constant CAP edited per-run; default cap=2.
import "/tmp/u-h640/HEXAD/IIT4/lib/iit4_eca.hexa"
import "stdlib/consciousness/iit4_bounded.hexa"
let N = 6
let CAP = 2
fn main() {
    let tpm = eca_tpm(110, N)
    let r = big_phi_bounded(tpm, N, 21, CAP)
    println("PROBE n6 r110 st21 cap" + str(CAP) + " phi=" + str(r[0]))
    println("PROBE_DONE")
}
