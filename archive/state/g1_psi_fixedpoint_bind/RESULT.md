# H_1826 (N1) — Ψ=½ dynamical fixed-point bind — RESULT (cheap-numpy HE pre-screen)

**run:** part of the unified 6-lever pre-screen `state/g1_anima_native_levers_he/he_levers.py` (arm N1) on **summer pool** (CPU-numpy, $0, did NOT touch GPU train). EXIT=0, deterministic fp64.
**verbatim stdout:** `RESULT.txt` (this dir, the N1 lines + summary) · full campaign run `state/g1_anima_native_levers_he/RESULT.txt` + `RESULT.md`.
**scope:** DIRECTIONAL — embed = `core/clm_decode.py` 303M trunk penultimate (β embed); fp64-numpy operator. NO torch. NOT terminal (G1 SSOT = engine-native `anima evaluate`).

## Result

- **N1 Ψ=½ dynamical fixed-point bind: 2/32 (0.06)** held-out G1 (5-fold CV over 32 EN/KO/ZH compound pairs).
- control: iteration-OFF=1/32 (iteration weakly causal +1, far below 2/3 bar).
- **frozen bar** (composed_distinct≥2 ∧ recoverable ∧ >control, ≥2/3 of 32 ≈ ≥22): **FAILS on both axes.**

## Verdict

**🧱 NOT-SUPPORTED (DIRECTIONAL, cheap-numpy HE pre-screen).** Does NOT clear the cheap pre-screen → does NOT earn engine-native/GPU measurement. self-test SEPARATES=PASS (metric is a live discriminator, floor is real); single-parent leak=10/32 (shared byte-prefix lexical leakage, γ precedent).

Campaign context: 0/6 anima-native levers cleared the cheap pre-screen → the G1 wall holds against anima-native mechanisms; lever = trunk OBJECTIVE, not readout/operator/embedding/mechanism. Full synthesis: `state/g1_anima_native_levers_he/RESULT.md`.
