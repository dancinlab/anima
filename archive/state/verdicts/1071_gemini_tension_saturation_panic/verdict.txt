H_1071 — Gemini v3 "Tension Saturation panic loop" core-vulnerability claim
VERDICT: 🔴 FALSIFIED — panic does NOT occur in the real CORE engine (g5/p7)
date: 2026-06-10 · CPU-local $0 · REAL CORE engine via hexa · NO GPU/AKIDA · RE-CONFIRM

FROZEN FALSIFIER (tape): run the real emit gate under constant max drive (all 8
motivation factors=0.9, 1000 ticks); PANIC iff max_consecutive_emits>1 (tick-by-tick).
brain.hexa:57 emit = should_emit(score) && safety_combined(.., rate, ..);
rate=safety_rate_limit_ok(s)= s>=spont_min_emit_interval()=30s (engine_g.hexa:52-53).

REAL RUN (hexa run state/anima_v3_bench/real_engine_emit_rate.hexa), 2026-06-10:
  === REAL anima CORE · sustained MAX stress · 1000 ticks (tick=1s) ===
  emits=33  emit_rate=0.033  max_consecutive_emits=1  (PANIC iff >1)
  rate_limit interval=30s -> structural cap = 1000/30 = 33.3 emits

GREP-CONFIRMED rate-limiter (CORE/engine_g.hexa):
  16: fn spont_im_threshold() -> float        { return 0.3 }   // PROACTIVE_THRESHOLD
  19: fn spont_min_emit_interval() -> float   { return 30.0 }  // rate-limit convention
  46: fn should_emit(score: float) -> bool    { return score > spont_im_threshold() }
  52: fn safety_rate_limit_ok(seconds_since_last: float) -> bool {
  53:     return seconds_since_last >= spont_min_emit_interval()

RESULT: max_consecutive_emits=1 under sustained MAX stress. The rate-limiter caps emit
to exactly the structural ceiling (1000/30 = 33.3 -> 33 measured) and FORBIDS two
consecutive emit-ticks BY CONSTRUCTION. The Gemini "saturation panic loop" (every-tick
비명 난사) is a PHANTOM: the shipped engine already has homeostatic 30s rate-limiting +
target_emit_rate 0.27 (emit_policy.hexa, H_637). Gemini's v3-redesign premise omitted
the existing rate-limiter and fabricated a non-existent failure mode.

TIER: 🔴 FALSIFIED · numerical (real-engine code-measured, g5, p7, no LLM self-judge).
RE-CONFIRMED: tape result reproduced bit-exact (emits=33 rate=0.033 max_consec=1).
SCOPE: real CORE emit gate, sustained max stress, tick=1s, 1000 ticks. Closes the v3
premise — the vulnerability it targets does not exist in the shipped engine.
ptr: state/anima_v3_bench/real_engine_emit_rate.hexa · CORE/engine_g.hexa:46,52-53
