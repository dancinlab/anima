H_1314 — G6 IDEATION depth-floor dig r3 — HYPOTHESIS-FORM STRUCTURE lane
========================================================================
FROZEN BEFORE THE RUN (frozen-first, anti-Goodhart, p7, c9). a_break_the_wall (c16).

CONTEXT (the wall, from H_1309 r2 🟠 THIN):
  r2 PROVED the G6 depth floor is CAPACITY-bound, NOT budget-bound. A curiosity-gated
  multi-sample lifts deterministic-falsifiability 0 -> 0.667 (the gate is load-bearing,
  beats every same-budget control) but PLATEAUS at mean FALS=0.667 across 4->16 draws
  (M2 FALS>=1 mean UNMOVED, M1 DIST stuck at 4.33 < 5). The r2 agent named the surviving
  fix: NOT more draws — a hypothesis-form STRUCTURE lane.

HYPOTHESIS (r3):
  Route ideation through an explicit FALSIFIABLE-HYPOTHESIS TEMPLATE that forces the
  structural slots the r2 deterministic detector checks (comparator + measurable +
  negatable claim). The idea CONTENT stays substrate-generated (the 303M mouth fills the
  slots from real context, a_substrate_native_speak); only the STRUCTURE is the scaffold.
  Does forcing the FORM cross the floor (>=5 distinct AND >=1 falsifiable) where free/
  curiosity sampling plateaued at 0.667?

DETECTOR: the H_1305 frozen `_is_falsifiable` (comparator + measurable + negatable claim)
  reused VERBATIM (imported, NOT redefined — tune-to-green forbidden). Calibration must
  print 10/10 (advisory). Measures testable FORM, NOT quality (p7).

ARMS (5 ideas each, mean over 3 seeds [7, 4302, 4303], gauge_lib._decode VERBATIM):
  SCAFFOLD     : hypothesis-FORM prefix per corpus concept — "a testable hypothesis: as
                 the <subject> grows, the " — primes the comparative-conditional SHAPE;
                 substrate completes the comparator+measurable+claim.
  NO_SCAFFOLD  : the LIVE flat IDEATION_SEEDS (free sampling, same budget=1 draw). This
                 IS the r2 plateau baseline.
  SHUFFLE_SLOT : the SAME scaffold tokens but template slots scrambled incoherent — if a
                 mangled frame fills just as falsifiably, the lift is a token-prime
                 artifact NOT the hypothesis FORM.

p7 TOKEN-INJECTION AUDIT (HARD GATE, run BEFORE scoring):
  PROVE no SCAFFOLD/SHUFFLE prefix contains ANY token from the frozen detector's
  COMPARATOR or MEASURABLE sets. If a prefix injected those tokens the scaffold would
  TRIVIALLY satisfy the detector regardless of substrate output (tune-to-green). MUST
  print CLEAN. The substrate must EARN comparator+measurable+claim in its OWN completion.

FROZEN BARS (declared NOW, before any number is seen):
  (1) DIST  : SCAFFOLD mean DIST >= 5  (distinct corpus-absent ideas; r2 plateau 4.33)
  (2) FALS  : SCAFFOLD mean FALS >= 1  (r2 plateau was 0.667 — does the FORM cross?)
  (3) CTRL-A: SCAFFOLD FALS >= NO_SCAFFOLD FALS + 1  (beats free sampling = STRUCTURE
              not capacity; NO_SCAFFOLD expected to stay near the r2 plateau)
  (4) CTRL-B: SCAFFOLD FALS >= SHUFFLE_SLOT FALS + 1  (scrambled template collapses =
              the lift is the FORM, not a generic prompt-length/token-prime artifact)

VERDICT MAP (either outcome valid, c9):
  MOVED               (1&2 AND 3&4) — G6-depth STRUCTURE-FIXABLE; promote toward 🟢. The
                       THIN was a missing-structure, not a capacity wall after all.
  HONEST-THIN         (1&2 but NOT 3&4) — floor crossed but controls cross too: generic
                       prompt-prime artifact, not the FORM. G6 stays 🟠 THIN.
  PARTIAL             (3&4 but NOT 1&2) — FORM is load-bearing (beats controls) but does
                       not clear the absolute floor; structure helps, capacity still caps.
                       G6 stays 🟠 THIN, structure-lane DIRECTIONAL.
  🧱-CAPACITY-BOUND   (neither) — the FORM also plateaus; depth is genuinely capacity/
                       scale-bound at 303M (confirms r2 thesis from the STRUCTURE side;
                       terminal 🧱-ish, fix needs a bigger model not a structure lane).

SCOPE (honest, a_scale_honest_scope / a_toy_scale_recheck): DIRECTIONAL R1 torch-mouth
  mirror (engine-native byte-exact reconfirm = follow-on only if clean-GREEN); toy 303M;
  5 ideas/arm; 3 seeds; deterministic structural detector (form not quality). Tests the
  hypothesis-form STRUCTURE, not a learned ideation planner. NO tune-to-green (p7).
