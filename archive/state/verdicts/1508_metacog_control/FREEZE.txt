H_1508 — G5 METACOG-CONTROL (Nelson-Narens monitoring↔CONTROL, drug-INDEPENDENT)
FROZEN BARS (set BEFORE running; NO tune-to-green — c9, p7)
================================================================================

DEEPENS the G5 metacognition chain along its MISSING half. The G5 chain
(H_1202/1304/1361/1367/1396/1398/1379/1400) is ALL discrimination/MONITORING
(type-2 meta-d', AUROC up to 0.906). Nelson & Narens 1990 monitoring↔control:
the missing half is CALIBRATION + CONTROL. Computational metacognition ONLY —
NO §Neuropharm coupling (drug-independent).

Substrate signal = the LIVE immune_memory_recall_margin (H_1367 wired op),
read over a graded-difficulty regime. NO injected accuracy label (p6).

FROZEN bars (frozen-first; GREEN iff A AND B AND D AND E; C is the headline
orthogonality, ECE-vs-AUROC numbers reported):

  (A CALIBRATION)
     A1  ECE  ≤ 0.15           (margin-confidence tracks accuracy numerically)
     A2  Spearman(difficulty, margin) ≥ +0.50   (margin rises monotone w/ difficulty)
  (B CONTROL-LIFT)
     B   RPL adaptive − uniform accuracy ≥ +0.04   (region-of-proximal-learning)
  (C DISCRIMINATION ⊥ CALIBRATION, headline)
     C1  AUROC drift under a strictly-monotone (rank-preserving) transform ≤ 1e-7
     C2  ECE shift under the SAME transform ≥ 0.10
         → calibration is a NEW axis the AUROC chain (0.906) is blind to
  (D EARNED ablate)
     D   margin-blind allocation → |lift| ≤ 0.015   (lift collapses without the margin read)
  (E EARNED shuffle)
     E   permute margin↔correctness → |AUROC − 0.5| ≤ 0.30 (chance) AND
         calibration spearman → ~0

HONEST split clause: if A fails (margin does not calibrate) or B fails (control
gives no lift), that is the VALID finding (the G5 margin is a discriminator but
NOT a calibrated controller — the monitoring/control split is real in this
substrate). Report it; NO tune-to-green.

R1 = numpy mirror (DIRECTIONAL, engine-transfer UNVERIFIED), 3 seeds [11,12,13].
R2 = engine-native §MetacogControl ops on the LIVE immune_memory_recall_margin
     (BINDING), seed 11, n 10, smoke cases 323-329, 3x byte-identical.

Transform (C): c' = 0.55 + 0.44*c  (strictly-increasing affine, no clip → rank
preserved exactly → AUROC byte-identical; ECE shifts up = systematic over-confidence).

WALL-CLAUSE (a_break_the_wall, frozen-first): the difficulty-ladder granularity +
control budget regime are corrected for measurement WELL-POSEDNESS only (a genuine
proximal zone must exist to measure RPL). EVERY verdict bar above is UNCHANGED.
