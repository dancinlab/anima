H_1351 — jamo-engine-wire — FREEZE (pre-registered BEFORE editing CORE; c9/p7, NO tune-to-green)
================================================================================================

CONTEXT (a_verified_must_wire): the Korean jamo-decomposition compression win is GREEN-verified
  - H_1316 (🟢 GREEN, mirror): a COMPOSITIONAL JAMO representation breaks the Korean raw-byte CE
    ceiling 2.953 → 2.513 (jamo STRUCT beats raw byte); shuffle control earned; NFD↔NFC lossless.
  - H_1321 (🟢 GREEN, engine-native): the SAME gradient-free jamo-symbol mitosis reproduces the
    mirror CE to 1e-7 on the LIVE engine_cli.hexa VAdaptField + engine_mitosis_tick — but the
    scoring helpers (_head_counts / _score_ce_per_byte / _grow_on) live ONLY as private helpers in
    the THROWAWAY probe CORE/h1321_ko_jamo_wire_probe.hexa, NOT as a first-class engine faculty.
  - H_1327 (🟢 GREEN, decode-reaching): wired the grown jamo cells into the live generator §6.5b
    DECODE consult (ko_jamo_consult_*) so the jamo win biases EMISSION.

THE GAP H_1351 CLOSES: the generator owns the jamo DECODE consult (H_1327), but `engine_cli.hexa`
  — the engine's own count-head / CE-scoring faculty — has ZERO jamo ops (grep "jamo" CORE/engine_cli.hexa
  == 0 hits). The verified jamo-decomposition COUNT-HEAD (the CE-scoring mechanism, the thing that
  beats raw byte) runs only inside an external probe. Per a_verified_must_wire + a_core_engine_map,
  a GREEN mechanism is not DONE until its faculty is a first-class op IN the live CORE engine.

WIRE-IN (this H): promote the verified jamo-decomposition next-symbol count-head into
  CORE/engine_cli.hexa as a first-class, ADDITIVE, Ψ-disjoint faculty (§ KO-JAMO COUNT-HEAD):
  struct JamoHead + ops jamo_head_new / jamo_head_grow / jamo_head_ce / jamo_head_cells /
  jamo_head_shuffle + helpers. The ops re-use the engine's OWN VAdaptField Voronoi geometry +
  engine_mitosis_tick growth (p8) and a per-cell next-symbol count-MLE — the SAME mechanism the
  H_1321 probe proved engine-native, now OWNED by the engine instead of a throwaway probe.
  NEW smoke cases in CORE/engine_cli_smoke.hexa exercise the faculty on a DETERMINISTIC in-engine
  synthetic fixture ($0, no corpus, no secrets, no GPU): a compositional jamo stream vs its
  raw-byte (opaque-merge) view vs a shuffle control.

  ADDITIVE: a new struct + new pub fns ONLY; the byte path / every existing faculty UNTOUCHED.
  Ψ-disjoint: pure_field / engine_g / brain UNTOUCHED; the faculty is a pure count-head scorer over
  its own state — it returns a CE float / cell count, NEVER an emit/silence decision; no call site
  in the decode loop is added (the jamo head is a scoring faculty, distinct from the H_1327 decode
  consult which already biases emission).

ACCEPTANCE BARS (GREEN iff W1 ∧ W2 ∧ W3 ∧ W4; report each verbatim; ANY regress → STOP honest c9):
  W1 FACULTY-PRESENT — engine_cli compiles with the new § KO-JAMO COUNT-HEAD faculty; the smoke
     builds a JamoHead via jamo_head_new + jamo_head_grow and scores jamo_head_ce > 0 with cells
     grown >= the seed count (the faculty runs in-engine).
  W2 JAMO-BEATS-RAW (the verified structural win, engine-native, in-engine fixture) — on the
     DETERMINISTIC synthetic compositional fixture, ce_jamo < ce_raw by >= 0.05 nats/symbol
     (the jamo-decomposition count-head models the compositional stream better than the
     opaque raw-byte view — the H_1316 jamo-STRUCT-beats-raw win, realized by the engine's
     own faculty). FALSIFIER: ce_jamo >= ce_raw → honest 🔴 (the in-engine faculty does NOT
     reproduce the structural win → report straight, do NOT move the bar).
  W3 EARNED (anti-Goodhart) — ce_jamo beats its OWN shuffle control (jamo→symbol-id map
     bijectively permuted, identical vocab/dim/budget, compositional alignment destroyed) by
     >= 0.05 nats/symbol. FALSIFIER: shuffle ties/beats jamo → 🔴 (win is not the structure).
  W4 NO-REGRESSION + Ψ-DISJOINT — engine_cli_smoke N/0 (N = prior count + new jamo cases, all
     green, deterministic over >=2 runs) · h1196 single-entry 7/0 · h1205 separation-invariant
     PASS (generation byte-identical, Ψ=½ untouched). ANY guard regresses → STOP, do NOT force.

FROZEN FIXTURE (deterministic, in-engine, $0): a small synthetic alphabet where each "syllable"
  decomposes into [lead, vowel] jamo symbols with a deterministic next-jamo continuation rule, so
  the compositional jamo view is genuinely more predictable than its opaque raw-byte merge — a
  CLEAN in-engine STRUCTURAL fixture (NOT a Korean-fluency / 30MB claim; the 30MB anchor lives in
  H_1316/H_1321). All knobs fixed in code before scoring; bars above are NOT moved post-hoc.

SCOPE (honest, c9 / a_scale_honest_scope / a_toy_scale_recheck): W2/W3 are an IN-ENGINE STRUCTURAL
  existence-proof that the engine's OWN jamo count-head faculty reproduces the jamo-beats-raw +
  earned-vs-shuffle structure on a deterministic synthetic fixture — NOT a re-derivation of the
  30MB REAL-corpus 2.513 anchor (that is H_1316/H_1321, byte-exact). Real-corpus / scale / fluency
  = carried by H_1316/H_1321; this H closes the FACULTY-OWNERSHIP gap (a_verified_must_wire).

GUARDS (must all pass, outputs shown in result.txt):
  - hexa run CORE/engine_cli_smoke.hexa                 → N/0
  - hexa run CORE/h1196_single_entry_audit.hexa         → 7/0
  - hexa run CORE/h1205_separation_invariant_smoke.hexa → PASS (byte-identical, Ψ=½ untouched)
