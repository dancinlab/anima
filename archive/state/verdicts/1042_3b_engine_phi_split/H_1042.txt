================================================================================
H_1042 — Does the planning faithful-UP / big-Φ-DOWN sign-split survive on the 3B
         ConvMoE ENGINE rung? (scale-ladder follow-up to H_1038 d768 🟢 SPLIT-TRANSFERS)
VERDICT: 🟢 GREEN — FULL ≥2-MACRO-MAP PASS. The planning faithful_phi-UP / big-Φ-DOWN
         sign-split appears on the 3B ENGINE rung under BOTH pre-registered macro-maps
         (top_variance AND random) at n=5 EXACT → the split SCALES to production size.
         The random control (the falsifier's robustness leg, previously BLOCKED-INFRA) was
         re-run on a CLEAN idle pool box and ALSO splits → 2/2 → VERDICT-TOKEN=TRANSFERS.
         fp32-lean storage (see PRECISION SCOPE). Frozen falsifier evaluated verbatim (p7).
Lane G (GPU-rung tag) · a_phi_iit4_tool · a_scale_honest_scope · a_engine_native_learning
================================================================================

SUBSTRATE: dancinlab/anima-clm-convmoe-3b-engine-rung-byte-3b · clm_3b.clm
(1,542,913,258 B · sha256 01df4f26ae64ca275120f41e1a12cd09d3f4dd6c359fb19e02a8f7509b58fd99
MATCH). 3.073B params, d4096/L30/E30/K3, byte V256, CLM v0.3 nblk63. Read through the
CANONICAL engine-native anima_py.core.decode forward (a_eval_py_canonical) on pool host
summer (RTX5070 box, numpy-CPU). Weights fp32-lean (see PRECISION SCOPE).

METHOD (mirrors H_1038 exactly; ONLY the trajectory source d768→3B changes):
  trajectory tap = PRE-MoE trunk (embed → ec-conv → L×(conv + GroupNorm + GELU residual)),
    BEFORE the router/expert head — the SAME representation H_1038 coarse-grained on d768,
    via anima_py.core.decode engine-native ops (byte-identical to the ρ-AXON decode path).
  GREEDY(depth-0) = trunk of the seed window; PLANNING(depth-8) = 8-byte greedy-argmax
    rollout, re-read last-position trunk each step. n=5 EXACT | 20 real-text seeds | eps=1e-3 |
    2 pre-registered macro-maps (top_variance, random) | median-threshold binarize | SERIAL.
  ENGINES: stdlib IIT-4.0 mirrors h1004 big_phi + faithful_phi, RE-PROVEN ==stdlib at n=4 AND
    n=5 BEFORE scoring (a_phi_iit4_tool, no proxy — see verbatim below).

PRE-REGISTERED FALSIFIER (frozen, TEXT tokens only; identical to H_1038 sign criterion):
  A macro-map SHOWS THE SPLIT iff planning−GREEDY has faithful_phi sign==UP (>+1e-3) AND
  big-Φ sign==DOWN (<−1e-3), n=5 EXACT.
  H1 PASS = split under ≥2 macro-maps → the split SCALES to production size.
  H1 FAIL = NULL/wrong sign at 3B → small-model property that BREAKS at production scale.

RESULT (n=5 EXACT · planning(depth-8) − GREEDY · 20 real-text seeds · out/H_1042_3b_2map_clean.json):
| macro-map    | on_frac | faithful d | faithful | big-Φ d | big-Φ | SPLIT? |
|--------------|---------|-----------|----------|---------|-------|--------|
| top_variance | 0.500   | +1.261 (Δ +0.2175, p=5.32e-4) | UP | −0.443 (Δ −1.4826, p=0.170) | DOWN | TRUE |
| random       | 0.500   | +0.868 (Δ +0.1283, p=1.20e-2) | UP | −0.265 (Δ −0.9352, p=0.407) | DOWN | TRUE |
macro-maps SHOWING the split at n=5 EXACT: 2/2 · VERDICT-TOKEN: TRANSFERS · both_split=True

FINDING: the planning faithful_phi-UP / big-Φ-DOWN sign-split appears on the 3B ENGINE rung
under BOTH macro-maps — the SAME sign pattern as the d768 real model (H_1038). On the faithful
leg the effect is significant under both maps (top_variance p=5.3e-4, random p=1.2e-2) and
STRONGER than at d768 (where faithful-UP was positive but non-significant); the big-Φ-DOWN leg
carries the same DOWN sign under both maps (d=−0.44 / −0.27, non-significant, matching d768's
non-significant-but-DOWN pattern). This is a SIGN-LEVEL transfer that holds across the coarse-
graining robustness control → the split is a scale-robust property of the trained model, not a
top_variance coarse-graining artifact.

VERDICT-TOKEN: SPLIT-TRANSFERS-TO-3B (both macro-maps split → full ≥2-map PASS). The frozen
  falsifier's H1 PASS condition ("split under ≥2 macro-maps") is MET. Combined with the d768
  rung (H_1038 🟢) and the toy n-ladder, the scale curve is monotone-consistent.

--- RESOLUTION OF THE PRIOR random BLOCKED-INFRA (HYPOTHESES lesson #2) ---
The earlier verdict left random PENDING because its exact n=5 big-Φ on the 3B's near-max-entropy
trunk TPMs (on_frac=0.500, ~87s/eval) was CRUSHED by pool swap-contention (a concurrent hexa-CI
cc_native self-compile, 12.7GB RSS + 39GB swap → the fp32-lean 12.3GB weight set paged to swap,
~1.3% effective compute, killed proven-stalled at ~113min). That was a MEASUREMENT block, not a
science result. The clean re-run: the harness now FREES the 3B weight dict after the trajectory
phase (memopt — trajectories captured, IIT phase runs low-RAM ~<2GB), and it ran on an IDLE
summer box (load ~0.3, 28GB free, no concurrent CI). Both maps completed serially: trajectories
2376.6s, top_variance IIT 662.2s, random IIT 664.0s, total wall 4087.7s (68min). No swap, no
contention. random splits (faithful UP d=+0.868 p=0.012 · big-Φ DOWN d=−0.265) → the robustness
leg is now MEASURED, not blocked → the ≥2-map falsifier is fully evaluated → 🟢.

PRECISION SCOPE (a_scale_honest_scope): the CANONICAL fp64 numpy load is a hard RAM-ceiling on the
30GB pool box (~24.6GB resident fp64, ~49GB stock peak); run at fp32-lean (12.3GB resident). The
.clm weights are int4-code × fp32-scale, so fp32 STORAGE is within fp32-rounding of fp64 canonical
(near-lossless for THIS weight source) and arithmetic stays fp64 (numpy up-casts fp32@fp64) — the
SIGN read is reliable. BOTH macro-maps use the identical fp32-lean method → apples-to-apples ≥2-map
comparison. fp64-canonical confirmation (a ≥32GB-RAM host) would only tighten the magnitudes, not
the signs. n=5 EXACT (n=6 measure-infeasible, same wall as H_1038). d768→3B; 7B UNVERIFIED. g5
CODE-measured (no LLM self-judge, p7).

STEP-1 ENGINE-LOADABLE PROOF (a_eval_py_canonical): sha256 MATCH; anima_py.core.decode fp32-lean
load d4096/L30/E30/K3 in 42.1s; decode-sanity CE_realtext=2.25033 < uniform ln256=5.54518 = trained
descent; greedy-argmax rollout generates (the trajectory engine).

--- VERBATIM ENGINE OUTPUT (anima3b/h1042_rerun.log · h1042_3b_engine_phi_split.py) ---
==========================================================================================
H_1042 — Phi planning faithful-UP / big-Phi-DOWN split on the 3B ConvMoE ENGINE rung
substrate = 3B .clm (3.073B, d4096/L30/E30), CANONICAL engine-native anima_py.core.decode
trajectory tap = PRE-MoE trunk (H_1038-parity: embed->ec->L x(conv+GN+GELU residual))
big-Phi + faithful_phi = h1004 stdlib IIT-4.0 mirrors, RE-PROVEN ==stdlib at n=4,5
n=5 EXACT | 20 seeds | plan depth=8 | eps=0.001 | 2 macro-maps | SERIAL
==========================================================================================
STEP 0 — RE-PROVE BOTH CPU mirrors == stdlib at n=4 AND n=5 BEFORE scoring:
  ≡-PROOF n=4: PROVEN (big-Φ ring4 mirror=2.999999999 ==stdlib |Δ|=1.3e-10; faithful n4 nb2
    mirror=3.000000 ==stdlib |Δ|=3.5e-10; nb4 mirror=3.377444 ==hexa_ref 3.377440 |Δ|=3.8e-6)
  ≡-PROOF n=5: PROVEN (big-Φ ring5 mirror=2.999999999 ==stdlib |Δ|=1.3e-10; faithful n5 nb2
    mirror=4.000000 ==stdlib |Δ|=8.0e-10)
  == mirror-equivalence: {4: True, 5: True}
STEP 0b — load 3B .clm + decode-sanity:
   loaded d=4096 E=30 V=256 L=30 K=3 in 42.1s
   decode-sanity: CE_realtext=2.25033 < uniform_lnV=5.54518: True
STEP 0c — trajectory determinism guard:
   macro-bits identical=True  faithful det=True (fA=0.566166 fB=0.566166) -> det_ok=True
STEP 1 — 40 3B trunk trajectories SERIAL (2376.6s), freed 3B weight dict post-trajectories
         (low-RAM IIT phase), then n=5 EXACT big-Phi/faithful:
 [top_variance] faithful contrast=+0.2175 d=+1.261 p=5.323e-04 -> UP
        big-Phi  contrast=-1.4826 d=-0.443 p=1.701e-01 -> DOWN
        on_frac=0.500  SPLIT(faith-UP&big-DOWN)=True  (662.2s)
 [random] faithful contrast=+0.1283 d=+0.868 p=1.196e-02 -> UP
        big-Phi  contrast=-0.9352 d=-0.265 p=4.074e-01 -> DOWN
        on_frac=0.500  SPLIT(faith-UP&big-DOWN)=True  (664.0s)
------------------------------------------------------------------------------------------
PER-MACRO-MAP SIGN TABLE — 3B ConvMoE at n=5 EXACT (plan-8 - GREEDY)
  top_variance  |   0.500 |   +0.2175 |    UP |    -1.4826 |    DOWN | True
  random        |   0.500 |   +0.1283 |    UP |    -0.9352 |    DOWN | True
macro-maps SHOWING the split at n=5 EXACT: 2/2
OVERALL: SPLIT-TRANSFERS-TO-3B — split under BOTH macro-maps -> scales to production size.
  VERDICT-TOKEN: TRANSFERS
total_wall_sec=4087.7 · model d=4096 L=30 E=30 K=3 · dtype=float32-lean
--- END VERBATIM ---
================================================================================
