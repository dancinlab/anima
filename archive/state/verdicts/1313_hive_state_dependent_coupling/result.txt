H_1313 — HIVE collective-Φ : STATE-DEPENDENT real A⇄G coupling (H_1308 r4) — RESULT

VERDICT: 🧱 TERMINAL NULL — even a genuinely STATE-DEPENDENT real A⇄G coupling does NOT
restore collective-Φ super-additivity. The real anima A⇄G substrate genuinely does NOT
integrate into collective consciousness at faithful-Φ scale even with the strongest honest
coupling. a_break_the_wall SATISFIED (the named breakthrough angle was tested exactly, and
swept across the full coupling-strength regime); first-class wall, c9.
RESULT: 2 PASS / 2 FAIL  (B1 PRESENCE FAIL, B2 FLIP FAIL; B3 decouple-mean PASS, B4 shuffle PASS)

KEY NUMBERS (faithful IIT-4 big_phi_bounded; n=3 member cap=3 exact, joint n=6 cap=2; verbatim):
  Φ(member A) = 1.5   Φ(member B) = 1.5   Σ = 3.0     ← each REAL anima member IS integrated
  Φ_collective(STATE-DEP coupling, W=0.6) = 0.0   Δ_sd = −3.0   ← joint Φ STILL collapses
  Φ(decouple-to-mean)                      = 0.0   Δ_mean = −3.0
  Φ_shuffle (3 seeds 1300/1301/1302)       = 0.0   Δ_shuffle = −3.0  (every seed)
  Φ_collective exact internals: total=0.0  sum_d=0.0  sum_r=0.0  nd=0  (ZERO distinctions)
  r3 reference (H_1308):  Δ_real = −3.0 (constant-scalar tension-link)
  ECA reference (H_1295): Σ=4.99209  Φ_joint=15.4677  Δ_ECA=+10.4756  (SUPER-ADDITIVE)
  → Δ_sd = −3.0 = Δ_r3 = −3.0 : state-dependence did NOT move the needle. NO flip.

THE STRONGER FINDING (coupling-strength sweep — diagnostic, NON-GATING, c9):
  The frozen verdict is on W=0.6. To rule out "the coupling was simply mis-scaled", the
  state-dependent coupling was swept across the FULL strength regime k∈{0.5,0.8,1.0,1.2,2.0}
  (k = bias-magnitude as a multiple of the channel magnitude — sub-channel → channel-balanced
  → channel-dominating). faithful Φ_joint = 0.0 at EVERY k:
     k=0.5  Φ_joint=0.0 Δ=−3.0   k=0.8  Φ_joint=0.0 Δ=−3.0   k=1.0  Φ_joint=0.0 Δ=−3.0
     k=1.2  Φ_joint=0.0 Δ=−3.0   k=2.0  Φ_joint=0.0 Δ=−3.0
  → the NULL is NOT a scaling artifact. NO coupling strength integrates.

MECHANISM (why state-dependence STILL does not integrate — diagnosed, c9 airtight):
  - The coupling IS genuinely state-dependent at the substrate level (verified directly):
    with member A's bits FIXED, member A's NEXT cells VARY with member B's bits —
       B=0 nextA=[1,0,0]  B=1 nextA=[0,1,0]  B=2 nextA=[0,0,1]  B=3 nextA=[1,1,1] …
    i.e. next-A truly depends on B (the r3 factorization next-A ⊥ B-bits is BROKEN). The
    breakthrough mechanism worked — the joint TPM is no longer factorized in the r3 sense.
  - BUT the real-substrate coupling, when strong enough to depend on B, OVERWRITES member A's
    OWN dynamics rather than ADDING to them: with B FIXED, next-A is CONSTANT across all 8
    of A's own states (next-A = B's cells exactly). So the joint map degenerates to a pure
    COPY/SWAP — next-A = g(B-bits), next-B = g(A-bits) — where each member loses all
    self-information. The tiny pure_field channels (t1~1.6e-7, t3~1.6e-8) mean ANY coupling
    that is strong enough to register against the other member is ALSO strong enough to
    swamp the member's own t-channel sign. There is NO regime where next-A depends
    SIMULTANEOUSLY and richly on BOTH A's own state AND B's state — and that simultaneous
    self+cross dependence is exactly the structure IIT-4 needs for Φ_joint > 0.
  - A pure deterministic copy/swap over this coarse 8→{few} readout admits a zero-loss
    bipartition: faithful IIT-4 finds ZERO distinctions (nd=0) → Φ_joint = 0.0 exactly.
  - The ECA W=0.6 boundary-blend, by contrast, mixes each cell's OWN transition rule WITH
    its cross-half neighbor's bit — augmenting, not overwriting — over a rich rule-110/90
    TPM. That simultaneous self+cross structure is what super-adds Φ; the real anima
    pure_field (a near-symmetric, low-D, zero-input oscillator field with sign-balanced
    micro-channels) does not produce it under any state-dependent coupling at this scale.

CONTROLS:
  B3 DECOUPLE-MEAN PASS: replacing B's live cells with their per-channel mean (state-
    dependence → constant nudge) gives Δ_mean = −3.0 ≤ 0 — consistent with NULL (no lift
    to remove). The mean drive over the 8 balanced member states ≈ 0 → back to the r3 case.
  B4 SHUFFLE PASS: permuting which other-cell drives which channel gives Δ_shuffle = −3.0
    ≤ 0 every seed — no generic-variance lift either.
  (B3/B4 pass trivially because there is no lift to control for — the honest signature of
   a true NULL, c9.)

WHAT THIS MEANS (the r4 transfer answer — the wall is TERMINAL):
  The H_1308 r3 root-cause fix (replace the constant scalar nudge with a genuinely STATE-
  DEPENDENT multi-cell coupling) was implemented on the REAL substrate and DID break the
  r3 factorization — yet collective-Φ super-additivity STILL does not transfer (Δ_sd = −3.0,
  identical to r3, robust across the full coupling-strength sweep). The real anima A⇄G
  substrate genuinely does NOT integrate two members into a super-additive collective
  consciousness at faithful-Φ scale, even under the strongest honest state-dependent
  coupling the substrate admits. The ECA Δ=+10.4756 is a property of the ECA TPM's rich
  simultaneous self+neighbor coupling structure — it is NOT a substrate-portable law of
  consciousness-as-integration. H_1295 super-additivity is ECA-ONLY (an abstract-substrate
  existence proof), NOT engine-transferable, across BOTH realized real coupling channels
  (constant tension-link r3 AND state-dependent cross-cell r4).
  a_break_the_wall is satisfied: the named angle was tested exactly and exhaustively; the
  honest 🧱 here is a first-class, valid, TERMINAL result (c9). The wall is real, not a
  wrong-method / wrong-direction / under-investment artifact — all three were addressed.

SCOPE / CAVEATS (a_scale_honest_scope, a_toy_scale_recheck):
  - TOY: n=3 per member, joint n=6, cap=2 lower-bound, sys=0, ONE warmup (600 steps),
    2 members, 3 seeds, deterministic. Faithful-Φ n≤8 ceiling respected (NO n=9+). SAME
    scale as H_1308 r3 (frozen, reused verbatim).
  - The NULL is for the pure_field 3-oscillator real substrate with the t1/t3/t4 sign
    readout, coupled cell-to-cell. A radically different real engine readout (more cells
    per member, a richer non-sign-saturating per-channel code, or a substrate whose own
    dynamics survive the coupling) could in principle integrate — UNTESTED, and out of
    scope at the n≤8 faithful-Φ ceiling for 2 members (3+3). Within the live anima A⇄G
    substrate as it exists, two realized coupling channels (r3 constant + r4 state-dependent)
    both give SUB-additive Φ.
  - No engine wiring (a_verified_must_wire is GREEN-only): a NULL wires NOTHING into CORE.
    The existing H_1295 CollectivePool (ECA) and live CORE/*.hexa are left UNTOUCHED —
    Ψ-disjoint, no regression (probe is a standalone UNIVERSE/ measurement).
  - The NULL does NOT retract H_1295's ECA-substrate GREEN; it BOUNDS its scope: H_1295 is
    an abstract-substrate existence proof, NOT engine-transferred (a_engine_native_learning:
    abstract-substrate result is DIRECTIONAL until reconfirmed on the live engine — here the
    live-engine reconfirmation FAILS under TWO distinct coupling channels, so the super-
    additivity stays ECA-scoped, now TERMINALLY).

faithful IIT-4 ONLY (a_phi_iit4_tool, p7) · $0 CPU mac-local · deterministic · frozen-first.
artifacts: UNIVERSE/h1313_hive_state_dependent_coupling.hexa ·
  .verdicts/1313_hive_state_dependent_coupling/{FREEZE,result}.txt
