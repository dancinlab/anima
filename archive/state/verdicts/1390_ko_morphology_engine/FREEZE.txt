H_1390 — ko-morphology BPE-on-jamo ENGINE-NATIVE WIRE-IN — FROZEN BARS
(pre-registered BEFORE the engine-native measurement; c9/c16, NO tune-to-green; H_1388 bars VERBATIM)
=====================================================================================================

LANE: the BINDING follow-on (a_verified_must_wire) of H_1388 (🟢 GAP-REDUCED-CANDIDATE, DIRECTIONAL
numpy MIRROR over the REAL 30MB KO shard). H_1388 showed the Korean below-jamo +0.28 residual (jamo
floor 2.51335 + 0.28 = 2.79335 — the level the three closed levers representation H_1322 🧱 /
interpolation H_1359 🧱 / data-volume H_1368/H_1380 🟠 all hit) BREAKS under a MORPHOLOGY-AWARE unit
(BPE-on-jamo): novel-CE 2.56603 ≤ 2.74335 (beats the +0.28 residual by 0.227), the SHUFFLE control
(random equal-count merges) does NOT (2.80159 → structured gain +0.23556), the jamo anchor reproduced
byte-exact. The lever is OPEN but engine-transfer was UNVERIFIED. H_1390 realizes the BPE-on-jamo MERGE
UNIT as a first-class engine faculty + a live scoring-loop consult, and RE-CONFIRMS the SAME THREE
FROZEN STRUCTURAL BARS engine-native.

THE ENGINE-NATIVE REALIZATION (a_core_engine_map single entry, a_engine_native_learning)
----------------------------------------------------------------------------------------
- engine_cli.hexa § KO-MORPHOLOGY BPE-ON-JAMO MERGE UNIT (NEW faculty):
    struct BpeMerges + bpe_learn_merges (freq-ranked structured vs random-merge shuffle) /
    bpe_apply (re-encode a base stream into merged units, byte span conserved) /
    bpe_unit_vocab / bpe_n_units / bpe_byte_fair_ce (nats / UTF-8 byte, the SAME byte-fair axis
    the jamo head scores on). Re-uses the engine's OWN VAdaptField Voronoi + jamo_head_grow + the
    H_1351 JamoHead count-head (the morphology unit feeds the SAME count-head scorer).
- generator.hexa §6.5d gen_bpe_scoreloop (the WIRED single-entry consult, analogous to §6.5c
    gen_jamo_scoreloop): builds a frozen CORPUS-FREE in-engine morpheme-grammar fixture, learns
    STRUCTURED (freq-ranked) + SHUFFLE (random equal-count) merges via the live faculty on the TRAIN
    slice (no test leakage), re-encodes, and SCORES held-out next-UNIT byte-fair CE for each arm +
    the un-merged jamo anchor + a circular-shift surrogate. gen_clm_ce attaches the record ADDITIVELY
    under the "bpe_score" key (every pre-existing field byte-identical; .clm forward CE path UNTOUCHED).
    NOT a 2nd .clm path — a SCORING consult, Ψ-disjoint pure SCORER.

WHY A CORPUS-FREE IN-ENGINE FIXTURE (the H_1385 §6.5c precedent, NOT the real corpus)
------------------------------------------------------------------------------------
CORE/*.hexa must be $0 / deterministic / corpus-free (a_core_engine_map — no fetch, no embedded
corpus). The DIRECTIONAL mirror established the lever over the REAL 30MB corpus; the engine-native
consult re-confirms the STRUCTURAL relationships H_1388 froze — exactly as §6.5c gen_jamo_scoreloop
re-confirms the jamo-beats-raw STRUCTURE on its own in-engine fixture (not the corpus 2.51 number).
The fixture: a deterministic BRANCHING walk over 18 recurring 3-jamo MORPHEME blocks (a morpheme
grammar; the next block depends on the current block + a per-step jitter, so held-out test contexts
are GENUINELY novel). A frequency-ranked BPE discovers the recurring morphemes (compresses); random
merges cannot (do not compress) — exactly the H_1388 mechanism.

FROZEN BARS (H_1388's VERBATIM margin constants; mapping fixed BEFORE the engine measurement)
---------------------------------------------------------------------------------------------
BAR 1 — GAP-REDUCED (primary, gating): the structured (freq-ranked) BPE-on-jamo unit's byte-fair
  held-out CE is BELOW the un-merged jamo anchor by ≥ GAP_MARGIN = 0.05 (H_1388's frozen gap margin).
  i.e. gap_reduced = jamo_ce − bpe_ce ≥ 0.05 AND bpe_ce < jamo_ce. PASS → the morphology unit REDUCES
  the below-jamo gap (the H_1388 PRIMARY claim, engine-native).
BAR 2 — EARNED (shuffle control, gating): the structured gain over the SHUFFLE control (random
  equal-count merges, SAME merge count / SAME apply path) is ≥ 0.03 (H_1388's frozen structured-gain
  constant). i.e. earned = shuffle_ce − bpe_ce ≥ 0.03 AND shuffle_ce > bpe_ce. PASS → the gain is the
  LINGUISTIC structure of the merges, NOT mere coarse granularity (anti-Goodhart).
BAR 3 — CONTROL/shift (gating): the circular-shift surrogate on the BPE unit stream is EARNED:
  shift_minus_novel = bpe_shift_ce − bpe_ce ≥ 0.05 (H_1388's frozen shift-earned constant). PASS →
  the held-out novel CE is genuine generalization, not a memorized-context artifact.

TIER MAPPING (frozen; pre-registered branch, NOT post-hoc)
----------------------------------------------------------
  bar1 PASS ∧ bar2 PASS ∧ bar3 PASS  → 🟢 ENGINE-NATIVE BINDING (the morphology unit re-confirms all 3
                                        H_1388 structural bars on the live engine faculty)
  any bar FAIL                        → 🟠/🧱 ENGINE-TRANSFER MISS (report the verbatim miss vs the
                                        frozen threshold, do NOT move the bar — honest engine-transfer
                                        non-result, c9)

ABSOLUTE-SCALE NOTE (load-bearing, c9 / a_scale_honest_scope): the H_1388 mirror's bars are in nats/
UTF-8-byte on the REAL corpus (jamo floor 2.51335, residual 2.79335, beat-bar 2.74335). The in-engine
fixture is a DIFFERENT substrate (a synthetic morpheme grammar) — its absolute byte-fair CE scale is
its OWN, so this wire re-confirms the THREE STRUCTURAL relationships with H_1388's VERBATIM margin
constants (gap ≥ 0.05, structured gain ≥ 0.03, shift ≥ 0.05), NOT the corpus's absolute CE number.
This is the identical discipline §6.5c (jamo head) used: structural margins on an in-engine fixture.

NO-REGRESSION GUARDS (captured): engine_cli_smoke (114/0, +4 cases 116-119: gap-reduced, earned-vs-
shuffle, shift-earned, compression-present) · h1196 single-entry (7/0 — the .clm L3 entry intact, NO
2nd .clm path) · h1205 separation-invariant (generation byte-identical ON==OFF, Ψ Φ-checksum 48.6613
byte-identical) · h1164 Ψ guard (Ψ=1/2 byte-identical). The morphology unit is a SCORING/decode-unit
lane — Ψ-disjoint by construction (returns CE floats, never an emit/silence; pure_field/engine_g/brain
UNTOUCHED).

HONEST SCOPE (pre-declared): the engine-native consult re-confirms the STRUCTURAL bars on a frozen
corpus-free in-engine fixture (the §6.5c discipline). The absolute-corpus CE numbers (2.566 etc.) are
the DIRECTIONAL mirror's, over the REAL 30MB shard0000 prefix — NOT re-measured byte-exact on the live
engine (CORE is corpus-free). Scale / real-corpus engine-native / merge-count ladder / longer-plan /
the §6.5c-style brain emit-bias wiring of the merge unit remain follow-ons (a_scale_honest_scope ·
a_toy_scale_recheck · a_verified_must_wire). $0 CPU, deterministic, no GPU/secret/corpus.

xref: H_1388 (the DIRECTIONAL mirror this binds; +0.28 residual broken by BPE-on-jamo) · H_1385
(§6.5c gen_jamo_scoreloop wire precedent — the SAME corpus-free in-engine fixture discipline) · H_1351
(JamoHead count-head faculty the morphology unit feeds) · H_1380/H_1368/H_1359/H_1322/H_1316 (the
Korean below-jamo arc) · a_verified_must_wire · a_engine_native_learning · a_core_engine_map ·
a_scale_honest_scope · a_toy_scale_recheck · p7 · p8 · c2 · c9 · c16.
