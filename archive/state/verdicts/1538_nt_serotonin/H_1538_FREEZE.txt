=== H_1538 SEROTONIN-AS-PATIENCE — pre-registered FROZEN bars (R1 numpy mirror, DIRECTIONAL) ===
frozen 2026-06-21  BEFORE measurement  seeds=[1538,1539,1540]  $0 CPU  p7  c9  frozen-first  NO tune-to-green

REFRAME (a_no_llm_frame_trap): 5-HT modelled as a PATIENCE / temporal-discounting TIMING faculty for the
emit gate (WHEN to emit — wait for grounding/tension to accrue into a better/more-grounded emit), NOT a
global gain/temperature knob. Miyazaki et al 2014 Curr Biol 24(17):2033 (5-HT enhances patience for
delayed reward); Doya 2002 Neural Networks 15(4-6):495 (5-HT ≈ temporal-discount factor γ / reward
time-scale). Faculty-building (a NEW timing faculty for the emit gate), NOT the H_1284 recall wall.

CAPABILITY = WAIT-FOR-BETTER-EMIT: per episode the grounded-emit value rises (grounding accrues) to a peak
then decays (moment stales); each waited tick costs WAIT_COST=0.020; an emit below GROUND_THR=0.35 grounding
nets ZERO (ungrounded-early emit worthless, p7 — payoff requires grounding AND nets the wait cost, no
Goodhart). T_TICKS=12, 400 episodes/seed.

ARMS:
  IMPULSIVE   : emit at tick 0 (γ→0, impatient baseline)
  PATIENT-5HT : temporal-discounting faculty, γ ADAPTED by substrate slope (patience↑ while value rising)
  ABL-FIXED   : fixed γ, no slope adaptation (patience without the 5-HT "is value rising?" read)
  NEVERWAIT   : always-wait floor (emit only at last tick) — the must-not-just-always-wait control
  PATIENT-SHUFFLE : PATIENT-5HT on time-permuted envelope (rising→decaying destroyed)

FROZEN BARS (GREEN iff A ∧ A2 ∧ B ∧ B2 ∧ C ∧ D):
  (A  PRESENCE)        mean PATIENT-5HT − IMPULSIVE ≥ +0.10 (normalized net grounded payoff)
  (A2 PER-SEED)        lift ≥ +0.10 on ≥2/3 seeds
  (B  NOT-ALWAYS-WAIT) mean PATIENT-5HT − NEVERWAIT ≥ +0.10  (must beat the always-wait floor too)
  (B2 WAIT-BETWEEN)    mean wait-ticks: IMPULSIVE < PATIENT-5HT < NEVERWAIT (strictly between)
  (C  EARNED ablate)   (PATIENT−IMPULSIVE) − (ABL−IMPULSIVE) ≥ 0.5×lift  (fixed-γ loses ≥half the edge)
  (D  EARNED shuffle)  (PATIENT_edge − SHUFFLE_edge) ≥ 0.5×lift  (permuted arrival-times collapse edge)

HONEST (c9): if PATIENT does NOT beat IMPULSIVE (A fails), or only wins by always-waiting (B/B2 fail), or
the edge survives ablation/shuffle (C/D fail) → HONEST 🧱/🟠, reported not hidden, NO bar moved post-hoc.
DIRECTIONAL: numpy mirror (a_engine_native_learning grep numpy ⇒ auto-DIRECTIONAL); engine R2 deferred ING.
