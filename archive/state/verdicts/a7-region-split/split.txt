verify --verifier-cmd  (VERIFY-KIT V8 — pluggable external verifier, phanes tenant-verifier model)
  claim    = AURA A7: per-region IIT4 big-Phi (n<=4 exact, no averaging) — bypass-region (DLPFC/insula hub) big-Phi >= M1-region (motor local) big-Phi, AND 16->4-region averaging destroys coupling (Phi_avg < Phi_perregion)
  verifier = hexa run /Users/ghost/core/anima/AURA/toy/a7_region_split.hexa
  ext rc   = 0
  ext out  = ================================================================
  AURA A7 — per-region big-Φ split (IIT4 n<=8 exact, n<=4/region)
================================================================
  system: 16 units, 4 regions x 4 units (synthetic montage).
  WHY NOT 16-unit exact: IIT4 exact needs 2^(2n) states;
    n=16 -> 2^32 ~ 4.3e9 -> infeasible (BRAIN.md M1 n<=8 limit).
  WHY NOT 4-region averaging: mean-collapse erases intra-region
    coupling = the irreducibility big-Φ measures (BRAIN.md M1).
  HONEST PATH: per-region n<=4, native resolution, no averaging.
----------------------------------------------------------------
  PER-REGION (honest, n=4 exact):
    M1-region     (motor / local self-copy)   big-Φ = 0.0
    bypass-region (DLPFC+insula hub/majority)  big-Φ = 17.6639
    ΔΦ(bypass - M1) = 17.6639
  AVERAGED SHORTCUT (wrong, bypass -> 1 super-node):
    bypass-region averaged to mean super-node  big-Φ = 0.0
    coupling erased: Φ_avg=0.0 vs Φ_perregion=17.6639
----------------------------------------------------------------
  [PASS] M1-region big-Φ finite >= 0
  [PASS] bypass-region big-Φ finite >= 0
  [PASS] averaged super-node big-Φ finite >= 0
  [PASS] FALSIFIER H: per-region big-Φ(bypass) >= big-Φ(M1)
  [PASS] SHORTCUT WRONG: averaged Φ < per-region Φ (coupling erased)
  [PASS] engine-exact: per-region n<=4 (2^(2n) feasible)
  [PASS] determinism: bypass-region re-run identical
================================================================
  RESULT: 7 PASS / 0 FAIL
================================================================
  expect = matched ("SHORTCUT WRONG: averaged Φ < per-region Φ (coupling erased)")
  tier   = 🟢 SUPPORTED-NUMERICAL  (external verifier passed AND stdout matches --expect — delegated, deterministic)
