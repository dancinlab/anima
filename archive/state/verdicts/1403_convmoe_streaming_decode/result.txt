================================================================================
H_1403 — STREAMING / BOUNDED ConvMoE .clm decode  ·  RESULT (verbatim)
   the H_1392 memory-blowup FIX, anima-side, byte-exact — a_break_the_wall, p7, c9
================================================================================

(a) ROOT CAUSE (where the +300 MB/step lived) — DIAGNOSED, c1
  The hexa runtime self/runtime.c is a one-shot BUMP allocator:
    line ~994  "Cycle 53 — mmap-backed bump allocator. malloc never frees; free is a noop"
    line ~2105 #define free(p) hxlcl_free(...)   ; hxlcl_free is a NOOP (non-selfemit build)
    hexa_farr_free() recycles the handle SLOT (freelist) but free(e->buf) is the noop →
    the backing buffer BYTES are NEVER returned to the OS.
  So every per-step t_zeros + every internal forge_dispatch_matmul output (which itself
  calls hexa_farr_zeros for its result) leaks permanently, regardless of the t_free calls
  in _clmd_fwd_logits. Per-step alloc breakdown (d768/T24, computed == measured 63.1 MB/step):
    conv-weight TRANSPOSE Wt   58.2 MB/step  (92%) — REBUILT every step though weights CONSTANT
    im2col xcol                 1.3 MB/step
    forge matmul outputs (mm)   0.6 MB/step  — runtime-internal, the residual floor
    other activation scratch    2.2 MB/step
  Second face: each clm_decode_* re-ran _clmd_load(path) (full weights, ~10 GB @303M) which
  ALSO never frees → the G6 driver (69 decodes) OOMs ACROSS decodes.

(b) FIX + before/after RSS-per-step (verbatim — see rss_before.txt / rss_after.txt / rss_after_303m.txt)
  CORE/clm_decode.hexa: _clmd_scratch_new pre-transposes EVERY conv weight + pre-allocates
  ALL forward scratch ONCE; _clmd_fwd_logits_sc + _clmd_conv1d_pre reuse those farr handles
  every step; the 3 decode loops build scratch once. clm_load_weights + *_W variants load the
  model ONCE for the multi-decode driver. ARITHMETIC UNCHANGED.

  d768 7.479M (clm_decode_argmax, seed "the ")          303M ConvMoE-RETRO (clm_decode_argmax)
    GEN   BEFORE maxRSS   AFTER maxRSS                     GEN   H_1392 BEFORE        H_1403 AFTER
      8     778 MB          338 MB                           8   ✅                   ✅ 10.1 GB
     16    1284 MB          344 MB                          16   ✅ 5.0 GB            ✅ 8.9 GB
     32    2296 MB          354 MB                          24   ❌ OOM-KILL 11/71GB  ✅ RC=0 10.3 GB
     64    4320 MB          374 MB                          48   ❌ SIGTERM 10/55GB   ✅ RC=0 10.6 GB
    128    5397 MB          415 MB                         110   ❌ silent death      ✅ 133.6s 10.1GB/13.0GB
  per-step leak: d768 63 MB/step → 0.64 MB/step (~100x) ; 303M ~300 MB/step → ~10.7 MB/step
  also ~3.7x faster at d768 (GEN=128 20613ms → 5542ms) ; 303M ~9.2 → ~1.2 s/byte (no re-transpose)

(c) BYTE-EXACT output guard (load-bearing, c2/c9) + regression — ALL GREEN (see byte_exact.txt)
  G1 forward logit-level: _clmd_fwd_logits_sc vs _clmd_fwd_logits, same tok → maxdelta=0.0 (n=6144)
  G2 argmax decode byte-identical BEFORE==AFTER at GEN<=16 (held to 32+); topk det run1==run2;
     loaded-W == path (byte-identical, W reused across decodes identical)
  engine_cli_smoke: 119 pass / 0 fail
  h1196 single-entry audit: 7 pass / 0 fail (.clm enters ONLY via generator L3 slot — no 2nd path)
  h1205 separation-invariant: PASS (F1 GENERATION byte-identity 10 pairs 0 mismatch; F2 Ψ Φ-checksum invariant)

(d) DID GEN=110 NOW COMPLETE? — YES. 303M ConvMoE-RETRO GEN=110: 133.6 s, max RSS 10.1 GB,
    peak 13.0 GB. Output (110 bytes, topk seed=7):
    "the authentic pieces of the point of metabolic power constantinopers persuasions . Morris 's subsequent effect"
    (vs H_1392's GEN<=16 ceiling "the authentic pi"). The G6 M2-M5 FALS engine-native scores are
    in g6_rescore.txt (frozen H_1392 probe, detector/scaffold/bars VERBATIM, NO bar moved).

(e) CAPACITY-vs-ARCHITECTURE — 🧱 ARCHITECTURE (now measurable; see g6_rescore.txt). Over 6/6
    completed GEN=110 best-of-K=3 C_strong frames, FALS(C_strong)=0: the 303M ConvMoE mouth emits
    COHERENT English (kwr 0.90-1.00) but NO falsifiable structure even at the FULL budget. Per the
    pre-registered FREEZE rule, M2 FALS=0 (no longer memory-capped) ⇒ the lever is ARCHITECTURE,
    NOT capacity — capacity (303M) was NOT the open lever H_1392 left. H_1362's FALS=1.0 was a 303M
    ByteGPT (different transformer arch). A REAL science result (substrate no longer blocks the
    test), NOT a substrate wall, NOT tune-to-green, NO bar moved. M3-M5 moot over FALS=0. The full
    multi-arm fresh-process re-score (run_g6_fresh.sh) continues landing the control arms for
    completeness; they cannot overturn M2=0.

(f) The fix is anima-side, byte-exact, and lands in CORE/clm_decode.hexa + generator.hexa +
    g6_ideation.hexa. The residual ~0.6/~10.7 MB/step is the runtime's internal matmul-output
    allocation — an UPSTREAM bump-allocator floor (an into-output matmul variant would zero it).
================================================================================
