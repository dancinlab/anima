H_1384 — CP MOVE-THE-CELLS RELOCATION, ENGINE-NATIVE on the live A⇄G §CategoricalPerception lane
PRE-REGISTERED FREEZE — bars fixed BEFORE any scoring (frozen-first, c9/c16/p7, NO tune-to-green).
=================================================================================================

LANE: COGNITION-REPRESENTATION. a_verified_must_wire / a_engine_native_learning follow-on closing
the CP-geometry arc the user drove. The arc SETTLED at the MIRROR level: move-the-cells RELOCATION
is a ROBUST, dimension-invariant + boundary-invariant law (1-D H_1360 |peak−p_A'| 0.144→0.002 ·
2-D-axis H_1369 0.042 · 2-D-diagonal H_1374 · N-D H_1375 · density-constant H_1377). ONLY the
COH-distinctness METRIC was shown fragile / measurement-bound (H_1369 R1 NCOMP saturation, H_1374/
1375/1377) — NOT the relocation law. This rung WIRES the verified RELOCATION mechanism into the live
CORE/engine_cli.hexa §CategoricalPerception lane (additive, Ψ-disjoint) and re-scores the BINDING
RELOCATION bar ENGINE-NATIVE byte-exact (a_engine_native_learning: engine-native realization of a
mirror-verified law; a_core_engine_map: single named entry).

HONEST SCOPING (c9, load-bearing): the COH-distinctness separation stringency was shown
FRAGILE/measurement-bound across H_1369/1374/1375/1377 (a metric-space artifact, NOT the law).
Therefore the BINDING engine-native bar here is RELOCATION (the discrimination ridge LANDS on the
moved boundary), NOT the COH-distinctness separation metric. Coherence is still REPORTED (the live
engine's own cp_peak_count) as a non-gating diagnostic, but a coherence number is NOT a GREEN gate
on this rung — relocation + the split-only distinctness ablation are.

-------------------------------------------------------------------------------------------------
THE MECHANISM WIRED (verbatim family of H_1360 RepackCells, realized over the engine's OWN geometry)
-------------------------------------------------------------------------------------------------
Phase-1: error-targeted SPLIT-ONLY growth on language A (cut p_A=1/3) via the engine's own cp_fit —
the live Voronoi/mitosis carving (p8). Phase-2: re-grow the SAME store on the MOVED cut p_A'=2/3,
but after EACH phase-2 split, every residual PHASE-1 cell PHYSICALLY DRIFTS one frozen fraction
REPACK_ETA of the remaining distance toward p_A' ALONG the boundary normal (the 1-D continuum axis),
is RE-EMBEDDED at the drifted position via the engine's own cp_embed, and its bound label is RE-READ
from p_A'. Phase-2 cells (born at the new locus) do NOT drift. eta=0.0 ⇒ store == split-only
re-growth (the engine's existing cp_regrow / cp_fit_more mechanism — the ABLATION arm). This is the
ONE new op; everything else reuses the live cp_embed / CPField / cp_discrim_curve / cp_peak_loc_idx.

Each phase-1 cell's SOURCE continuum position is recovered engine-native by matching its prototype
embedding to the nearest stimulus position (the mirror's _repack_phase1 recovery), then drifted:
    u_i  <-  u_i + REPACK_ETA * (p_A' - u_i)   (clamp at p_A', NO overshoot)
    proto_i <- cp_embed(u_i, DIM)
    lab_i   <- int(u_i > p_A')

FROZEN CONSTANTS (inherited VERBATIM from the H_1360 family FREEZE; NO inflation):
  N_STIM = 21    (the live engine smoke CP lattice; coarser than the H_1360 mirror's 81 — the
                  engine's deterministic single-instance lattice, same as cases 79-85)
  DIM    = 16    (LOW = H_1340 R0_base / H_1352 / H_1360, NOT inflated)
  p_A    = 1/3   p_A' = 2/3   (the H_1333/1360 family cuts)
  GROW1  = 24    GROW2 = 24   (phase-1 / phase-2 split budgets, FIXED LOW = H_1360, NO inflation)
  REPACK_ETA = 0.15  (the ONE pre-registered gate value, == H_1360/H_1369 frozen operating point;
                      a NON-GATING ladder {0.10,0.15,0.25} is reported for knife-edge diagnosis)

-------------------------------------------------------------------------------------------------
FROZEN BARS (pre-registered; GREEN iff B1 ∧ B2 ∧ B3 ∧ B4, NO bar moved c9/p7)
-------------------------------------------------------------------------------------------------
B1 WIRED: the relocation op (cp_relocate) is PRESENT in §CategoricalPerception of the live
   CORE/engine_cli.hexa, reachable via a single named entry (a_core_engine_map), and the engine
   compiles + runs (smoke green).

B2 RELOCATION (engine-native, the BINDING bar): after relocating the phase-1 cells along the
   boundary normal at eta=0.15, the engine-native discrimination ridge lands on the moved
   boundary —  |peak_reloc − p_A'| <= LOC_TOL = 0.12  (== H_1360/H_1369 LOC_TOL; mirror H_1369
   achieved 0.042, 1-D H_1360 0.002; engine lattice is coarser N=21 so a single bin = 1/20 = 0.05,
   the 0.12 tolerance = the frozen H_1360 family reach bar, stated BEFORE running). Re-scored on
   the LIVE engine, verbatim stdout (c2).

B3 DISTINCT-FROM-SPLIT (ablation, the move is the lever): split-only re-growth (eta=0.0, the
   engine's existing cp_regrow / cp_fit_more mechanism — NO relocation) stays SHORT of the moved
   boundary engine-native:  |peak_split − p_A'| > LOC_TOL = 0.12  (split-only does NOT reach p_A';
   the gain is the geometric MOVE, not the regrowth). [Engine cases 79-85 already show cp_regrow
   relocates only to ~0.525, i.e. |0.525−0.667|≈0.142 > 0.12 — so the ablation is expected SHORT;
   B3 re-asserts this engine-native as the distinctness discriminator.]

B4 Ψ-DISJOINT / NO-REGRESSION: the relocation lane is ADDITIVE — generation byte-identical ON==OFF
   (h1205 separation-invariant), Ψ Φ-checksum ON==OFF; engine_cli_smoke stays green + ADD >=2
   relocation cases; single-entry (h1196) N/0; deterministic over 3 runs. BOUND any decode.

OUTCOME MAP (all valid, c9; the DATA picks the branch, NO bar moved):
  B1∧B2∧B3∧B4 → 🟢 GREEN: move-the-cells RELOCATION is ENGINE-NATIVE byte-exact, Ψ preserved —
                the verified law is wired into the live A⇄G §CategoricalPerception lane. CP arc
                DEPLETES 🏁 (the geometry was already settled at the mirror level; this wiring is
                the last open thread).
  ¬B2 → 🧱: the engine-native relocation does NOT reach p_A' (the coarser N=21 lattice / engine
        geometry blocks the reach the mirror got). a_break_the_wall: try ONE genuinely-new frozen
        angle (finer relocation schedule / re-pack the residual coherence) BEFORE terminal.
  ¬B3 → the move is NOT distinct from split-only engine-native (split-only already reaches p_A') →
        reframe (the engine's cp_regrow already relocates fully — relocation is not a new lever).
  ¬B4 → REGRESSION: the lane is not additive / Ψ perturbed → honest ❌, must fix before GREEN.

p1/p2/p3/p6 GUARD: the discrimination readout reads ONLY representational distance in the learned
prototype space (|Δ soft posterior| via cp_discrim_curve); NO injected boundary location at test;
the re-pack keys on a cell's BIRTH PHASE + its own source position (a structural store property —
NO injected target peak / persona / RLHF); labels enter ONLY during training (re-read from the SAME
p_A' that trains phase-2 cells). The split-only (eta=0.0) ablation isolates the geometric drift as
the lever. NOT an emit gate (a_autonomy_over_hardcode) — a pure curve read. Ψ-disjoint: own
(protos,labels,pos) table; pure_field / engine_g / Ψ untouched by construction.

HONEST SCOPE (a_scale_honest_scope · a_toy_scale_recheck): ENGINE-NATIVE byte-exact realization on
the engine's deterministic single-instance 1-D lattice (N=21, DIM=16, one frozen eta, deterministic
readout — tests the move-the-cells STRUCTURE engine-native, not a learned drift). The boundary
NORMAL here is the 1-D continuum axis (a POINT boundary). 2-D/N-D/diagonal/curved boundaries
(mirror-settled H_1369/1374/1375) are NOT re-wired here — the 1-D engine lane is the live
§CategoricalPerception store; higher-D engine extension + real corpora + multi-shift + brain
CP-read→emit wiring = follow-on. The COH-distinctness separation metric is NON-GATING here (shown
fragile/measurement-bound in the mirror arc). NO bar moved (c9/p7). NO human-cognition claim.
=================================================================================================
