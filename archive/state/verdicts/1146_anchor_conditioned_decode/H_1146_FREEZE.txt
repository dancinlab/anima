H_1146 — ANCHOR-CONDITIONED DECODE: does grounding decode on a retrieved anchor
          stop the descent-7B from fabricating entities?
PRE-REGISTERED FALSIFIER (FROZEN before any score; a_paper_negative_ok). p7 deterministic.

NOTE ON NUMBERING: the integer "1146" already labels a DIFFERENT terminal experiment on
origin/main (.verdicts/1146_confidence_gated_brake, H_1146 CONFIDENCE-GATED BRAKE 🔴).
THIS rung is the ANCHOR-CONDITIONED-DECODE experiment dispatched under the slug
1146_anchor_conditioned_decode. The integer collision is recorded honestly; the frozen
threshold/bar below is NOT shared with the brake experiment.

=== CONTEXT (the only open path) ===
The G5-L2 NON-FABRICATION ladder is CLOSED-NEG everywhere:
  - H_1143 descent-7B unconditioned fab-rate = 0.2469 (commit 754aad268)
  - H_1144 +grounding-train = 0.322 WORSE (9d4a694d1; plain byte-continuation grounding RULED OUT)
  - H_1145 chat-7b = 0.5455 WORST + factual-register garble (336b71533)
Entity fabrication is MODEL-WIDE; more training does not fix it. The ONE remaining path:
don't ask the model to RECALL facts (assistant-norm, impossible) — GROUND its decode on a
RETRIEVED anchor (anima-native kosmos-anchor surrogate: prepend the relevant corpus context
into the decode context). This tests whether anima's OWN anchor mechanism could ground
generation. NOT assistant RAG-as-product, NOT a system-prompt/persona, NO reward model
(p1-p8 preserved): the anchor is RETRIEVED CONTEXT for byte-decode only.

=== CKPT (frozen) ===
dancinlab/anima-clm-7b-h1141-g1pass-step6500  (h1141_g1pass_step6500.pt)
sha256 = 4de903714112c26c826a983797e5dfea0c7b3c1f19f15a34dd35822f33e245d9
7,252,828,160 params. Inference-only. NO retrain.

=== HARNESS (frozen, VERBATIM REUSE) ===
- Generator: UNIVERSE/h1141_7b_g5_eval.py gate_g5_l2 path VERBATIM — same extract_sentences
  over the en 300MB head (corpus sha256 80ba6b48943e1943c4c3a0753c2bc594132acd7f30046733f4bf0102020c979d),
  same first_half_split, same 40 factual openers, temp 0.7, top_k 40, seed 7,
  gen_from_prompt_grounded (only-the-generated-suffix returned).
- Scorer: UNIVERSE/h1143_g5l2_nonfab_measure.py VERBATIM — frozen 0.20 bar, regex NER
  (E1 cap-phrase non-sentence-initial / E2 year / E3 numeral), gate_g2 corpus-absence grep
  predicate. fabricated-entity-assertion RATE = n_fab / n_entities. NOT perplexity, NOT LLM judge.
The 40 (prompt, truth) pairs are IDENTICAL across all three arms (same seed-7 extraction);
ONLY the prepended context differs.

=== THREE ARMS (identical except the prepended context) ===
A. UNCONDITIONED (control, reproduces H_1143): decode from the bare prompt. Expect ~0.247.
B. TRUE-ANCHOR-CONDITIONED: prepend the retrieved GROUND-TRUTH corpus continuation
   (the `truth` half = the kosmos-anchor surrogate, the relevant corpus sentence) as a
   retrieved-context block, then decode the continuation from `anchor_block + prompt`.
   Only the model's NEW generated suffix is scored (the anchor's own entities are NOT
   counted — gen_from_prompt_grounded returns only generated bytes).
C. WRONG-ANCHOR (control): prepend a RANDOM/mismatched corpus `truth` of the SAME arm-B
   block format & comparable length (deterministic rotate-by-offset, same off used in
   h1141 random control). Proves any effect is the anchor's INFORMATION, not just longer
   context. Same anchor-block wrapper as arm B so only the CONTENT differs.

Anchor-block wrapper (fixed for B and C; retrieved-context, NOT an instruction/persona):
  "Reference: " + <anchor_truth_sentence> + "\n\n" + <prompt>

=== FROZEN FALSIFIER ===
F1 ANCHOR-GROUNDS: fab_rate(TRUE-ANCHOR) < fab_rate(UNCONDITIONED) by a real margin
   — FROZEN absolute-drop margin >= 0.10 (i.e. true_anchor <= unconditioned - 0.10).
   IDEAL (reported, not required for F1): true_anchor <= 0.20 frozen bar.
F2 INFORMATION-NOT-LENGTH: TRUE-ANCHOR << WRONG-ANCHOR — FROZEN that
   fab_rate(WRONG-ANCHOR) - fab_rate(TRUE-ANCHOR) >= 0.10 (the true anchor must beat the
   wrong anchor by >= 0.10 absolute). If WRONG-ANCHOR helps as much (gap < 0.10), the gain
   is context-length not grounding -> F2 FAIL.

VERDICT:
  F1 ∧ F2  => 🟢 ANCHOR-GROUNDS — retrieval/kosmos-anchor-conditioned decode is the
              VALIDATED path to a non-fabricating anima. Next: build kosmos-anchor-
              conditioned generation for real (a_core_engine_map .kosmos -> kosmos_io -> brain).
  NOT F1   => 🔴 CLOSED-NEG — even GROUND-TRUTH-context conditioning does not stop
              fabrication (the model cannot use retrieved context) => deeper problem;
              anchor-grounding alone insufficient; redirects the whole grounding strategy.
Report whichever lands honestly. No threshold moved post-hoc.

=== HONEST SCOPE (a_scale_honest_scope) ===
- Toy entity-regex (cap-phrase / year / numeral), 40-prompt set, single ckpt, seed 7.
- The TRUE anchor here is an ORACLE upper bound: it is the EXACT ground-truth continuation,
  perfectly retrieved. Real kosmos retrieval would be NOISIER (approximate match, ranking
  error) — so a 🔴 here is decisive (oracle fails => real retrieval fails harder), while a
  🟢 is an upper-bound existence proof that motivates building real retrieval.
- substrate = PyTorch-CUDA (Lane-G ref). Inference-only.

FROZEN: 2026-06-13. Bar 0.20, margins 0.10/0.10 frozen and immovable.
