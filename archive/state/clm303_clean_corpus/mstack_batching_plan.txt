{
  "summary": "Design M-stack batching for clm_decode to collapse ~13k per-token GPU launches (fixes host-RSS leak + glue-bound slowness) — source-only, no pods",
  "agentCount": 5,
  "logs": [],
  "result": {
    "mapped_sites": 28,
    "transforms": 4,
    "plan": {
      "impl_steps": [
        "STEP 0 (UNBLOCKED, ship today, zero .hexa edit) — RANK 3 devres flip. Production decode must set CLM_PROD_DEVRESIDENT=1 (or HEXA_FUSE_ALL) so _clmd_devres() (core/clm_decode.hexa:506-509) returns true and every forge_dispatch_* in _clmd_fwd_logits_sc (523/526/540/551/555/556/564/569/572) takes the device branch instead of the interpreted host-scalar fallback (t_get/t_set over T*d=24*3784=90,816 elems/op = the CPU 0%-util glue sink). This alone collapses the ~13k host-bookkeeping aggregate into the ~36 real per-token device launch floor and stops the per-op H2D/D2H ping-pong → fixes BOTH the host-RSS leak and the 16-19min first-decode. Wire it where the decode CLI/daemon sets env (cli/anima.hexa decode entry), NOT inside clm_decode (keep the engine env-gated). Verify with [OWN-GEMM-FIRED]/groupnorm tags on stderr + nvidia-smi util>0.",
        "STEP 1 — Add the readout D2H read-scope narrowing (UNBLOCKED, tiny, byte-exact). At the readout conv (clm_decode.hexa:572) only row (T-1) of out_logits[T*V] is consumed for argmax; narrow the device→host copy to the last [V=256] row instead of all [T*256]. Exact (argmax over last position only). This is the only RANK-3 change that touches .hexa and it is a pure read-scope shrink.",
        "STEP 2 (BLOCKED on hexa-lang) — file TWO hexa-lang follow-ons via `harness ing add --to hexa-lang`: (2a) inside hexa_forge_dispatch_matmul_batched (runtime.c:15292-15350) add a HEXA_CUDA-guarded device branch that routes to _hx_cuda_farr_matmul_batched_gpu (runtime_cuda.c:1272 → _hx_k_gemm_strided_batched:1240) before the existing CPU per-problem loop; the CPU loop stays as the no-CUDA byte-eq fallback. codegen.hexa:7998 already emits the builtin, only the runtime body is missing the route. (2b) add a NEW forge_dispatch_gemm_residual(a,M,K,b,N,r,c) host wrapper (HEXA_CUDA → _hx_cuda_farr_gemm_residual_fused_gpu runtime_cuda.c:1946; no-CUDA → host matmul-then-add byte-eq fallback) PLUS a codegen.hexa builtin entry — there is currently NO forge_dispatch_gemm_residual symbol anywhere in runtime.c (grep-confirmed empty).",
        "STEP 3 (after 2a lands) — RANK 1 caller rewire in clm_decode.hexa. Add a flame_mm wrapper mm_batched(A_all, M, K, B_all, N, batch, C_all) over forge_dispatch_matmul_batched (CPU oracle fallback already there). Then replace the E-expert while-loop (clm_decode.hexa:553-562): build im2col xcol ONCE from the shared xt, pack the E expert weights into B_all (already in sc as eWt[ej]; pre-pack once in _clmd_scratch_new), replicate xcol into A_all[E*T*Kd], one mm_batched → ex_out[E*T*d], one batched forge_dispatch_gelu over the whole [E*T*d] buffer, dropping the per-expert host stack-copy (560). Router at 564 reads the same packed ex_out layout (RANK 4 invariant — preserved).",
        "STEP 4 (after 2b lands) — RANK 2 caller rewire in _clmd_conv1d_pre (clm_decode.hexa:394-408): replace forge_dispatch_matmul(394)+forge_dispatch_residual_add(397)+t_free(mm)(408) with ONE forge_dispatch_gemm_residual(xcol,T,Kdim,Wt,Cout, bfull, y_out) writing straight into the reused y_out scratch (eliminates the per-conv mm cudaMalloc/cudaFree = the RSS-leak source). bfull is the pre-expanded per-channel bias (clm_decode.hexa:416-425/445) so A*B+bfull == current matmul-then-channel-bias. Keep the host matmul-then-add byte-eq fallback for devres OFF. Applies to all 9 conv callers (526/536/551/555/572).",
        "STEP 5 — RANK 4 is NO-OP: groupnorm_gelu_residual (540) and moe_router (564) are already optimally fused. Do NOT re-batch them and do NOT cross-batch the L trunk convs (536) — sequential residual chain with dilation 1→2→4→8, layer l+1 reads layer l output, impossible. Documented to prevent double-batching and to lock the contiguous ex_out invariant RANK 1 must preserve."
      ],
      "launch_reduction": "\"~13,000/token is NOT 13k real CUDA kernel launches — it is CUDA-driver per-launch host bookkeeping on unbatched micro-launches PLUS the interpreted host-scalar fallback (t_get/t_set over T*d=90,816 elems/op) that runs because _clmd_devres() defaults OFF (clm_decode.hexa:506). Collapse path: RANK 3 (env flip, unblocked today) converts the ~13k aggregate into the real per-token device floor ~36 and kills H2D/D2H ping-pong → fixes both the host-RSS leak (~97-233 MB/s churn) and the 16-19min first decode. RANK 1 makes the expert block E-independent: 4E≈8 launches (8 at E=2, 16 at Emax=4) → ~3 (1 shared im2col + 1 strided-batched GEMM + 1 batched gelu) at ANY E. RANK 2 takes the 9 conv calls from 3→2 launches each (27→18) and eliminates the per-conv cudaMalloc/cudaFree. Net per-token: ~13,000 aggregate → ~20-23 real device launches, byte-exact. Highest immediate ROI = RANK 3 (shippable now, no wrapper); RANK 1+2 are gated on the two hexa-lang wrapper fixes.\"",
      "byte_exact_gate": "\"Mac CPU oracle gate (NO CUDA needed, run BEFORE any GPU): on Mac cuda_available()==0 so mm() and forge_dispatch_matmul_batched both take the CPU farr path — this IS the oracle. (1) RANK 1: assert the batched expert path is byte-identical to the E-serial loop. Harness: load clm303 .clm, take a fixed xt, run (a) the current E-serial _clmd_conv1d_pre+gelu+stack loop producing ex_out_serial[E*T*d] and (b) the new mm_batched+batched-gelu producing ex_out_batched[E*T*d]; assert max|ex_out_serial[i]-ex_out_batched[i]|==0 over all E*T*d elems (NOT <eps — exactly 0). Byte-identity holds by construction: hexa_forge_dispatch_matmul_batched calls the SAME hexa_farr_matmul per problem (runtime.c:15330) with the same FP64 k-loop order as the E serial calls; shared im2col is bit-identical because all experts read the same xt; elementwise erf-GELU is stacking-invariant. (2) RANK 2: assert A*B+bfull (forge_dispatch_gemm_residual CPU fallback) == current matmul-then-per-channel-bias (clm_decode.hexa:402) on the same fixed input, max|Δ|==0 — this is the LOW-MEDIUM risk one because bfull is a per-CHANNEL broadcast, must pass the pre-expanded bfull (clm_decode.hexa:416-425) as R, verify the broadcast row-periodicity matches. (3) Full-forward parity: run _clmd_fwd_logits_sc with devres OFF (host path) vs the rewired path on Mac, compare out_logits[T*V] and the argmax token stream over a fixed prompt — max|Δ|==0 / identical token ids. Reuse the existing CPU mirror infra (state/clm303_g6/tools/fastmirror.py / torch_golden_fwd.py pattern) as cross-check. Gate: any nonzero Δ blocks merge (production clm_decode is protected engine).\"",
      "cuda_verify_plan": "\"On a CUDA host (follow-on, ING #34 / #15; cannot run on Mac). Host selection per a_train_flame_forge: CUDA-12 devel image + verified GPU, avoid summer (reboots) / aiden (CUDA-13 full-decode breaks). (1) Rebuild hexa-lang runtime with the two new wrapper routes (stage_resolve_runtime_a HEXA_CUDA=1 SM=<cc>); clear stale ~/.hexa-cache/hexa_run.<hash>* (CPU-era cached binary would mask the rebuild). rsync core/ cli/ to host. (2) Confirm GPU path actually fires: cuda_available()==1, nvidia-smi util>0, and the stderr tags — [OWN-GEMM-FIRED] _hx_k_gemm_strided_batched DEVICE path (runtime_cuda.c:1315) for RANK 1, [FUSE-EPILOGUE-FIRED] _hx_k_gemm_fused_residual (runtime_cuda.c:1963) for RANK 2. (3) Launch-count down: nsys/nvprof trace per-token kernel-launch count BEFORE (devres OFF / unbatched ≈ ~13k aggregate) vs AFTER (RANK3 alone ≈ ~36; RANK3+1+2 ≈ ~20-23). (4) Host-RSS flat: sample RSS across N decoded tokens — must be FLAT (no ~97-233 MB/s growth) once per-conv cudaMalloc/cudaFree eliminated by RANK 2 fused-residual writing into pre-allocated _clmd_scratch_new scratch. (5) GPU byte-exact re-confirm: decode a fixed prompt on GPU vs the Mac CPU-oracle token stream — identical token ids (the device kernels are documented byte-eq twins; this re-asserts on real hardware). Harvest ckpt/logs before teardown (a_fire_recover_complete).\"",
      "risk": "\"SCOPE: clm303 303M CLMConvMoE decode is PROTECTED ENGINE (core/clm_decode.hexa) — must NOT alter byte-exact decode output. Gating: CPU-oracle max|Δ|==0 (Mac, runnable now) is a hard pre-merge gate; CUDA launch-count↓ + RSS-flat is a post-merge verify follow-on (no CUDA on Mac). RANK 3 risk NONE (env-gated device path is the documented byte-eq twin of the host scalar path it replaces; readout D2H last-row narrowing is exact argmax). RANK 1 risk NONE — strided-batched calls the identical per-problem FP64 matmul in the same accumulation order, shared im2col bit-identical, elementwise gelu stacking-invariant. RANK 2 risk LOW-MEDIUM — the conv bias is a per-CHANNEL broadcast (bfull) not a generic [M,N] residual; MUST pass the pre-expanded bfull as R and VERIFY A*B+bfull == matmul-then-channel-bias on a CUDA host before claiming terminal byte-exact. BLOCKERS: RANK 1+2 caller rewires are BLOCKED until two hexa-lang wrapper fixes land (matmul_batched device route missing; gemm_residual wrapper absent entirely) — both handed off via `harness ing add --to hexa-lang`; do not rewire the anima caller before they land (would dispatch to the CPU per-problem loop = no GPU win, and gemm_residual would not even resolve). RANK 3 is unblocked and shippable today. Honest: Mac validates shapes+math byte-exactly but the cuBLAS-free own-GEMM device bodies can only be exercised on a CUDA host.\"",
      "highest_value_draft": "\"HIGHEST-VALUE SHIPPABLE-TODAY change is RANK 3 = zero .hexa edit (set CLM_PROD_DEVRESIDENT=1 at the decode entry in cli/anima.hexa; _clmd_devres() at core/clm_decode.hexa:506-509 already reads it). The highest-value CALLER .hexa DRAFT (RANK 1, BLOCKED on hexa-lang fix 2a) — replace the E-expert serial loop at core/clm_decode.hexa:553-562:\\n\\n// flame_mm.hexa — new wrapper (CPU oracle already exists via forge_dispatch_matmul_batched):\\npub fn mm_batched(A_all, M, K, B_all, N, batch, C_all) -> int {\\n    return forge_dispatch_matmul_batched(A_all, M, K, B_all, N, batch, C_all)\\n}\\n\\n// clm_decode.hexa — REPLACES lines 553-562 (E-serial conv+gelu+stack):\\n// Precondition (build ONCE in _clmd_scratch_new): eWtPacked = contiguous [E*Kd*d]\\n// pack of eWt[0..E]; A_all = sc[\\\"expA\\\"] sized [E*T*Kd]; ex_out = sc[\\\"ex_out\\\"] [E*T*d].\\nlet Kd = d * K\\n// 1 shared im2col from xt (identical across experts)\\nif !(dev && forge_dispatch_im2col(xt, xcolD, T, d, K, 1) == 0) {\\n    _clmd_im2col_host(xt, xcolD, T, d, K, 1)   // byte-eq host fallback (same order as 378-392)\\n}\\n// replicate the shared xcol into each of the E A-slabs (memcpy, byte-identical)\\nlet mut e = 0\\nwhile e < E { let _ = farr_copy_slice_gpu(xcolD, 0, expA, e*T*Kd, T*Kd); e = e + 1 }\\n// ONE strided-batched GEMM: A_all[E*T*Kd] @ eWtPacked[E*Kd*d] -> ex_out[E*T*d]\\nif !(dev && mm_batched(expA, T, Kd, eWtPacked, d, E, ex_out) == 0) {\\n    // CPU oracle fallback = same forge_dispatch_matmul_batched per-problem farr_matmul loop\\n    let _ = mm_batched(expA, T, Kd, eWtPacked, d, E, ex_out)\\n}\\n// ONE batched erf-GELU over the whole contiguous stack (elementwise, stacking-invariant)\\nif !(dev && forge_dispatch_gelu(ex_out, ex_out, E*T*d) == 0) {\\n    nn_gelu_fwd(ex_out, ex_out, E*T*d)\\n}\\n// NOTE: expert bias add folds into RANK 2 gemm_residual once 2b lands; until then add\\n// per-expert eB[e] over each [T*d] slab in a byte-eq host loop (same add as 402).\\n// ex_out keeps the [E*T*d] packed layout the moe_router at line 564 reads in place\\n// (RANK 4 invariant — DO NOT change router).\\n\\nByte-exact because: forge_dispatch_matmul_batched runs the SAME hexa_farr_matmul per problem (runtime.c:15330, same FP64 k-loop order as the E serial calls), the shared im2col is bit-identical (all experts read the same xt), and erf-GELU is elementwise. Gate on the Mac CPU oracle (max|Δ|==0) before any GPU run.\""
    }
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Map"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Design"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "map:clm_decode-forward",
      "phaseIndex": 1,
      "phaseTitle": "Map",
      "agentId": "a3c15d34649445d90",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782400237260,
      "queuedAt": 1782400237254,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "clm_decode-forward (MAP: per-token CUDA launch sites in the…",
      "promptPreview": "clm303 M-stack batching — MAP lens: clm_decode-forward.\n\nGOAL: reduce the ~13,000 GPU kernel launches PER TOKEN in clm303 303M decode (CLMConvMoE) to a small number, by\nBATCHING/FUSING the per-token forge ops in the anima CALLER (clm_decode.hexa / generator.hexa / flame_mm.hexa).\nWHY: the workflow forensics proved the GPU-path host-RSS leak (~97-233 MB/s) is CUDA-driver per-launch host\nbookkeeping…",
      "lastProgressAt": 1782400380372,
      "tokens": 106251,
      "toolCalls": 12,
      "durationMs": 143112,
      "resultPreview": "{\"lens\":\"clm_decode-forward (MAP: per-token CUDA launch sites in the anima caller). 303M canonical shape recovered from cli/train.hexa:962 = d=3784, L=4 trunk layers, E=2 experts (Emax4 under mitosis), K=3, V=256; decode window T=24 (clm_decode.hexa:749). The per-token forward is _clmd_fwd_logits_sc (clm_decode.hexa:512), driven once per generated byte by clm_decode_argmax:765 / clm_decode_topk_sa…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "map:moe-expert-loop",
      "phaseIndex": 1,
      "phaseTitle": "Map",
      "agentId": "a5cb9072d570765a5",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782400237264,
      "queuedAt": 1782400237254,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "moe-expert-loop / MAP — clm303 CLMConvMoE per-token forge l…",
      "promptPreview": "clm303 M-stack batching — MAP lens: moe-expert-loop.\n\nGOAL: reduce the ~13,000 GPU kernel launches PER TOKEN in clm303 303M decode (CLMConvMoE) to a small number, by\nBATCHING/FUSING the per-token forge ops in the anima CALLER (clm_decode.hexa / generator.hexa / flame_mm.hexa).\nWHY: the workflow forensics proved the GPU-path host-RSS leak (~97-233 MB/s) is CUDA-driver per-launch host\nbookkeeping (n…",
      "lastProgressAt": 1782400339427,
      "tokens": 110974,
      "toolCalls": 13,
      "durationMs": 102163,
      "resultPreview": "{\"lens\":\"moe-expert-loop / MAP — clm303 CLMConvMoE per-token forge launch sites in core/clm_decode.hexa::_clmd_fwd_logits_sc (the streaming forward all decode loops use), with the E-expert GEMV loop as the prime strided-batched target. KEY FINDING: the stdlib ALREADY provides the byte-eq batched primitives the decoder is NOT using — conv2_fwd_via_forge_batched (E=2 expert fusion → 1 strided-batche…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "map:conv1d-norm-act",
      "phaseIndex": 1,
      "phaseTitle": "Map",
      "agentId": "ae132ecec1ec51c0e",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782400237268,
      "queuedAt": 1782400237254,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "MAP conv1d-norm-act, per-token forge launch sites in clm303…",
      "promptPreview": "clm303 M-stack batching — MAP lens: conv1d-norm-act.\n\nGOAL: reduce the ~13,000 GPU kernel launches PER TOKEN in clm303 303M decode (CLMConvMoE) to a small number, by\nBATCHING/FUSING the per-token forge ops in the anima CALLER (clm_decode.hexa / generator.hexa / flame_mm.hexa).\nWHY: the workflow forensics proved the GPU-path host-RSS leak (~97-233 MB/s) is CUDA-driver per-launch host\nbookkeeping (n…",
      "lastProgressAt": 1782400388643,
      "tokens": 101531,
      "toolCalls": 15,
      "durationMs": 151375,
      "resultPreview": "{\"lens\":\"MAP conv1d-norm-act, per-token forge launch sites in clm303 (CLMConvMoE, L=4 trunk / E=2 experts / d=3784 / V=256). Per-token forward = _clmd_fwd_logits_sc (core/clm_decode.hexa:512-573). Every conv goes through _clmd_conv1d_pre (core/clm_decode.hexa:373-409) which emits a FIXED 3-launch sequence: forge_dispatch_im2col (line 377) -> forge_dispatch_matmul (line 394) -> forge_dispatch_resid…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "design",
      "phaseIndex": 2,
      "phaseTitle": "Design",
      "agentId": "a80b5779fba54b4f1",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782400388649,
      "queuedAt": 1782400388644,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The ~13,000/token figure is NOT 13k real CUDA kernel launch…",
      "promptPreview": "Design the M-stack batching transform for clm303 decode from these mapped launch sites:\n[\n {\n  \"site\": \"clm_decode.hexa:512 _clmd_fwd_logits_sc (the per-token forward; called once per generated byte by clm_decode_argmax:765 / clm_decode_topk_sampled_W:900 / clm_decode_grounded:1012)\",\n  \"launches_per_token\": \"sum of all sites below; ~9 GEMM-bearing conv calls + ~9 device epilogues. On GPU path eac…",
      "lastProgressAt": 1782400481248,
      "tokens": 115478,
      "toolCalls": 7,
      "durationMs": 92599,
      "resultPreview": "{\"transforms\":[{\"site\":\"core/clm_decode.hexa:553-562 — E-expert conv loop (while ej<E { _clmd_conv1d_pre(xt, eWt[ej]) ; forge_dispatch_gelu }); all E experts read the SAME xt, identical shape [T,Kd]@[Kd,d]->[T,d], independent weights, no inter-expert dependency\",\"transform\":\"RANK 1 (highest leverage × low byte-risk). (a) Build im2col xcol ONCE from the shared xt (it is identical across experts — c…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "synth",
      "phaseIndex": 3,
      "phaseTitle": "Synthesize",
      "agentId": "a022b63dc1de8deb5",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782400481260,
      "queuedAt": 1782400481249,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "\"~13,000/token is NOT 13k real CUDA kernel launches — it is…",
      "promptPreview": "Synthesize the clm303 M-stack batching IMPLEMENTATION PLAN from this design:\n{\n \"transforms\": [\n  {\n   \"site\": \"core/clm_decode.hexa:553-562 — E-expert conv loop (while ej<E { _clmd_conv1d_pre(xt, eWt[ej]) ; forge_dispatch_gelu }); all E experts read the SAME xt, identical shape [T,Kd]@[Kd,d]->[T,d], independent weights, no inter-expert dependency\",\n   \"transform\": \"RANK 1 (highest leverage × low …",
      "lastProgressAt": 1782400590889,
      "tokens": 82108,
      "toolCalls": 8,
      "durationMs": 109628,
      "resultPreview": "{\"impl_steps\":[\"STEP 0 (UNBLOCKED, ship today, zero .hexa edit) — RANK 3 devres flip. Production decode must set CLM_PROD_DEVRESIDENT=1 (or HEXA_FUSE_ALL) so _clmd_devres() (core/clm_decode.hexa:506-509) returns true and every forge_dispatch_* in _clmd_fwd_logits_sc (523/526/540/551/555/556/564/569/572) takes the device branch instead of the interpreted host-scalar fallback (t_get/t_set over T*d=2…"
    }
  ],
  "totalTokens": 516342,
  "totalToolCalls": 55
}