====================================================================================
H_6028 — GENERATIVE COMPLETION beyond the recall radius (paid ANU · numpy · p7)
====================================================================================
  quantum source: ANU paid QRNG vacuum bytes, sha256=3adbf3e6afc058786102741ef131cb3021706f8672a028184f1a6e0b25c84f33
  tier=anu_paid · 2048 bytes · book length L=64

  corruption: 6 bytes survive, 58 bytes lost (Hamming dist 58 ≫ recall radius 12)

  🟢 GC1 recall-breaks — content recall works INSIDE r, fails PAST r
        inside radius (only 6<r bytes lost, true book stored): recall hit? True, tail acc 1.000 (d=0)
        past radius   (58≫r bytes lost, true book NOT recall-stable): nearest decoy tail acc = 0.000
        → pure recall cannot reconstruct the lost tail (FAILS, as expected)

  🟢 GC2 gen-compressible — fit rule from 6-byte prefix, GENERATE the tail
        rule fitted from surviving prefix? True
        reconstruction: 64/64 bytes correct (acc 1.000), errors=0
        → losslessly reconstructed past the recall radius (LOSSLESS)

  🟢 GC3 gen-random — incompressible tail is unrecoverable (H_6017 no-free-lunch)
        generative completion acc on lost tail = 0.0000
        ≈ chance baseline 1/256 = 0.0039  (random remainder cannot be generated)

  🟢 GC4 honest-mark — confident on rule-derived, 'I don't know' on random
        compressible book: all 64 generated bytes marked CONFIDENT? True
        random book: all 58 lost bytes marked UNKNOWN? True
        random book: all 6 surviving bytes marked KNOWN? True
        → never fabricates the incompressible part as if known (HONEST)

------------------------------------------------------------------------------------
  VERDICT: GC1=🟢 GC2=🟢 GC3=🟢 GC4=🟢
  🟢 SUPPORTED — anima's library does not only RECALL: it
  GENERATIVELY COMPLETES the compressible (rule-bearing) part of a memory beyond the
  recall radius (GC2), while pure recall fails there (GC1), the incompressible remainder
  stays unrecoverable (GC3), and every reconstructed byte is honestly marked confident
  vs unknown (GC4). Unifies H_6017 (GENERATE compressible) + H_6018/H_6019 (RECALL stored)
  into: anima reconstructs MEANING, never fabricates NOISE.
