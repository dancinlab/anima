# companion/ — anima-consciousness-substrate data-record axis

WIP stub — filled in the monograph build pass. This directory is the
machine-readable data-record layer that lives alongside `main.pdf`: every
verbatim verdict in the body and appendices traces to one entry here, so a
reviewer can audit a claim without re-running it (sidecar/paper companion
pattern, commons @D g51).

## files (planned)
- `verify-ledger.json` — every body + appendix claim -> its `.verdicts/<slug>/<id>.txt`
  or `UNIVERSE/H_*.md` pointer, verbatim (a_claim_verify, @D g5).
- `pr-roll.json` — the PR / discovery timeline (omega, convmoe-3b, corpus, M13 7B ckpts).
- `session-journal.md` — the campaign arc narrative.
