"""_qseed.py — Lane-P torch trainer bridge to the qentropy SSOT (H_924 M4). WIP skeleton."""
from __future__ import annotations


def resolve_seed(fallback: int, label: str = "lane_p_torch_init"):
    raise NotImplementedError("WIP skeleton — filled in H_924 M4")
